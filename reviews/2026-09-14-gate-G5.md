# G5 — cumulative review gate: complete crash/resume across the max_parallel = 1 topology (first run, after repair round 3)

**Gate** G5, *Complete crash/resume across the max_parallel = 1 topology (bounded census + typed site
bijection registry + derived run outcome and finalization)*.
**Frozen input range** `caf6bed0f9f5f749ed9ecb99fb44bfb3e08fb2ac` — the merge commit that put pull request
#279 (PR10) on `master` at 2026-09-14T17:29:20Z, confirmed on entry with `git rev-parse HEAD` in
`/srv/worktrees/g5-run1` (detached at that commit, `git status --porcelain` empty; branch `gate/g5-run1`
was created from it and carries this report and nothing else). Its first parent is `2b504d4f` (`master`
after #284), its second `80968596` (#279's head); #279's merge base with `master` is `231c1aad`. The
previous gate's range is `7e0110a1` (G4, `reviews/2026-09-10-gate-G4.md`); this range holds every
commit from there to the merge: 255 commits under 21 first-parent merges (§0). Every number in this
report binds to `caf6bed0` unless it names another sha.
**Platform of every Linux measurement** `x86_64-unknown-linux-gnu`, kernel 6.8.0-137-generic, cargo
1.97.1 (c980f4866 2026-06-30), rustc 1.97.1 (8bab26f4f 2026-07-14), git 2.43.0, Docker 29.7.2 (client
and server), shell umask `077`, on the build box (`spec/platform-versions-host.log`, measured on this run).
**Platform of every Windows measurement** the persistent guest `windowsguest`: Windows Server 2025
Standard Evaluation, build 10.0.26100.1742, cargo 1.97.1, rustc 1.97.1, git 2.50.1.windows.1,
`core.autocrlf=true` from the system gitconfig (`C:/Program Files/Git/etc/gitconfig`, read with
`--show-origin`), in its own tree `C:\upstroke-g5` at the range sha with its own target directory
`C:\upstroke-g5\target` (`guest/guest-platform-versions.log`, measured on this run). Nothing here was
measured on macOS; CI's macOS leg is read in §1.
**Date** 2026-09-14 (the first head that day; the heads after repair rounds 1 and 2 the same day); this
head, after repair round 3, 2026-09-15.
**Evidence** every quoted figure appears in a file this report names, under
`~/tactus-artifacts/g5-evidence-caf6bed/` on the build box (`MANIFEST.sha256` lists every file with
its sha256; §12 is the index; the committed report is copied in as `report-<sha7>.md`). Paths below of
the form `gates/…`, `mutations/…` are relative to that directory.

**Verdict: G5 does NOT pass on this range.** Four of the nine positive clauses of the pass rule are
not established at `caf6bed0` (§13): clause 7, by an executed counterexample of the tree's own
(`PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION`, whose recipe fails at this range as it
predicts) and by the committed matrix's missing assertion (`PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION`);
clause 9, by a filed P1 in the range on the gate's own subject; clause 2, for 140 of 168 registry
coordinates at which no committed test meets ST-07's three obligations — the exact durable prefix
constructed, the tabled action performed, replay twice equal — 86 of them constructed and recovered by
committed tests that assert no replay-twice equality and 54 that no committed test constructs and
recovers (§5.3); and clause 1, against the packet's own
bounds line on two dimensions where the tree's reading and the unamended text differ, a decision that
is the owner's. The other five positive clauses are established and the three prohibited claims are
not made. §13 names, for each failing clause, the exact tree change (file, function, change, the
witnessing test) or the decision that would let it hold, so that a fix can be cut from `origin/master`
and the gate re-run on the corrected range as run 2. **PR11 and PR12 stay blocked.**

## 0. What this run is, and what moved after the first head

This is G5's **first** run; this is its head after **repair round 3** (the head after round 2 was
`3649147f`, after round 1 `00616451`; the first head, `0642c87f`, passed the gate with qualifications).
Round 3's three lenses (`~/orch-pr10/reviews/rg5-3/`, verify, regression and record) again agreed the
FAIL is justified and returned CHANGES_REQUIRED on the coordinate audit, the power-loss witness's loss
model and figures left stale; the third table below says what each found and what this head does. Round
2's three lenses (`~/orch-pr10/reviews/rg5-2/`) are the second table. Round 1's findings first. Two independent verification lenses (`~/orch-pr10/reviews/rg5-1/`,
verify and record) returned CHANGES_REQUIRED on it, and both were right: the verify lens found the
pass resting on an executed counterexample (F1), an exhaustive-matrix claim promoted from an
observation bijection (F2), the census's bounds certified as the packet's without establishing
equivalence (F3), seven re-deferred findings missing from the deferral assessment (F4) and three
evidence statements to correct (F5); the record lens found a dependency-review claim no test performs,
a group filter that ran zero tests, five sampler loops and five histogram producers claimed where the
evidence held three of each, six figures disagreeing with their logs, a Docker claim the test result
lines cannot establish, a histogram attributed to the wrong run, and universal claims two logs did
not support. **Every one of those is fixed here by producing the evidence or by narrowing the sentence
to what the evidence shows, and the verdict changed with them** (the brief's rule: never edit a pass
around a counterexample). What was executed in round 1 — on the report heads, not at the detached
range: the phase-2 witnesses, the Docker-required run, the producer-provenance loops, the corrected
group 4 and the retrospective checks ran on `0642c87f`, whose compiled inputs are byte-identical to
`caf6bed0` (the method note "Which sha each execution ran on" below; round 1's head
said "all at `caf6bed0`", which round 2's verify lens found):

| Finding | What this head does |
|---|---|
| verify F1 (P1) — the pass contradicts the power-loss counterexample | the verdict is a failure on clause 7 and names the fix (§13); Q2's answer is stated as negative for that resource (§10) |
| verify F2 (P1) — observations promoted to exhaustive fault/recovery proof | a per-entry audit of all 168 observed registry coordinates against the test bodies; round 1's audit (`inventory/per-entry-recovery-audit-round1.md`: F 129, S 30, O 9) credited companion coordinates, inject-and-observe tests and the gate's own uncommitted witnesses, as round 2's lenses found; round 2's (`inventory/per-entry-recovery-audit-round2.md`: F 80, F= 14, S 34, O 40) credited classification and plans as recovery and a disk read as the replay clause, as round 3's lenses found; both are superseded by round 3's audit on ST-07's three obligations (`inventory/per-entry-recovery-audit.md`, §5.3): **F 4, N 24, R 86, O 54** — clause 2 fails on 140; group 1 and ST-07 are graded on the audit, not on the bijection |
| verify F2 — group 3's "pruned non-authoritative refs recovered" reasoned | **executed**: a run finalized at BudgetExceeded prunes the queued candidate's pin (R23) and the resume with a raised ceiling publishes the candidate without it (§3.4, §8) |
| verify F2 — group 6's "only still-open questions" reasoned | **executed**: an answer file published under a budget stop is ingested by the resume exactly once, and the same file, its question closed, is never polled by a later resume; G4's M08 re-applied kills the committed one-off test and the witness (§3.4, §8) |
| verify F2 — the killed-prefix test's name promises more than its body executes | **executed**: the create suite's child killed at each of the 13 prefixes and the next census run on the killed fixture itself, converging to ST-19's disposition at every prefix (§3.4, §8) |
| verify F3 (P1) — the census's bounds certified as the packet's | both readings stated, the measurement, where the tree's reading was recorded, the clause graded against the unamended text, and the one sentence a packet correction would have to say (§4); the decision is the owner's |
| verify F4 (P2) — seven re-deferred findings omitted | each judged against the clause it touches, with the registry's order condition re-measured (§9.3) |
| verify F5 (P3) — 13 Windows binary tests; `Present(2)` at Complete; the report absent from the evidence directory | 12 passed and 1 ignored (§1); `Present(3)` (§7); the committed report copied in and manifested (§12) |
| record 1 (P1) — the effects censuses' "reading of `Cargo.lock`" | narrowed to what the tests do; the range's dependency delta measured: `Cargo.lock` unchanged since the G4 range (§5.1, §10 Q5) |
| record 2 (P1) — a group filter that ran zero tests | the filter corrected (`select::tests::`), every filter of every group checked against the binary's own `--list` (171 unique, all resolve), group 4 re-run: 13 passed (§2, §8) |
| record 3 (P1) — five sampler loops and five histogram producers | the two missing samplers run alone five times each; the producers counted from the tree: three; the residue record rebuilt with each histogram's producer and run named (§5.4) |
| record 4 (P1) — six figures | 12 + 1 ignored; 19 census tests; the census kills recounted by module; 171 unique filters; four Linux full-suite runs; the body's line and file counts from the final manifest (§1, §2, §4) |
| record 5 (P2) — "Docker-gated lanes ran" from result lines | the 23 Docker-gated tests run with `UPSTROKE_REQUIRE_DOCKER=1`, under which a test that would skip panics: 21 passed, 2 the box's poll flake, each 3 of 3 alone (§1) |
| record 6 (P2) — the PR5 histogram's provenance | identified as the isolated fifth run's (its four budgets equal that log's); the three producers re-run alone with their files copied in the same second (§5.4) |
| record 7 (P3) — the universal pre-check claim; platform figures without a log | seven recipes checked in `CHECK=1` mode **after** their executions, retrospectively at 20:42:06Z (`mutations/check-pass-g5-round2.log`, seven entries), and the two answered-question recipes checked retrospectively in round 2 (`mutations/check-pass-g5-round3.log`); the count and the chronology are stated as they happened in §3.2 (round 1's head called them nine and a pre-check, which round 2's record lens found); host and guest versions measured into logs (above) |

**Round 2's findings, and what this head does about each** (the lens texts are posted on #288,
[round 2](https://github.com/sourcemaps/upstroke/pull/288#issuecomment-5671414793)):

| Finding | What this head does |
|---|---|
| verify F1 (P2), record 1 (P1) — the audit's "159 of 168" does not follow: rows marked F cited tests that construct a different coordinate (`RunDir.WriteQuestionPayload`/after, `Ref.CreateIntegration`/after), or inject and observe only (`Event.OpenLog`/create/error_return), or the gate's uncommitted witnesses (23 rows) | every one of the 168 coordinates re-audited in round 2 against ST-07's definition, each cited test opened (kept as `inventory/per-entry-recovery-audit-round2.md`: F 80, F= 14, S 34, O 40); **that audit is itself withdrawn in round 3** (the third table) for crediting classification and plans as recovery and a disk read as the replay clause, and replaced by the audit on the three obligations: **F 4, N 24, R 86, O 54** (§5.3); the gate's temporary witnesses that would close a row are listed apart (A and B) |
| verify F2 (P2), regression P2, record 2 (P1) — the prescribed power-loss regression test recreates A unconditionally and still fails under the prescribed fix (both lenses executed it) | **the witness corrected** so its loss model reads the durability ledger (a directory-entry change under `public` survives the loss only if a `SyncedDirectory` of `public` was recorded after it), and **both halves executed** (`tmp-tests-3/`): at `caf6bed0` unchanged it fails, `cargo exit=101`, with `public synced at []` and A standing after the retry; with the prescribed `src/rundir.rs` change applied temporarily (a `cp` backup, the record lens's own reproduction diff, the run, the restore, both files' sha256 equal to `git show HEAD:…` before and after, `git status --porcelain` empty) the same witness passes, `cargo exit=0`, with `public synced at [0]`, A gone and nothing passed over (§3.4). Round 3's regression lens then executed a barrier moved before A's deletion and this model passed it too, so **round 3 replaces it** with the model that requires a barrier after the observed deletion (the third table, §3.4, `tmp-tests-4/`), and §13 row 7 cites that one |
| verify F3 (P2), record 4 (P1) — the conclusion's "every check … established by a named execution" contradicts §8's asserted kill equivalence and ST-13's inherited evidence | the limitation kept in the conclusion and in the body, and put on the fix list with what discharges it and which the pass rule requires (§13) |
| verify F4 (P3), record 3 (P1), record 5 (P3) — the precheck count and chronology; the group-7/group-4 attribution of `a_state_with_admissible_work_and_no_budget_exceeded_classifies_not_ending`; "all at `caf6bed0`"; `packet-check.log` does not hold the bounds line; unmeasured load and helper-age figures | seven retrospective checks and the two missing ones supplied retrospectively, described as they happened (§3.2); the census test attributed to group 4, where it ran (§4); every execution after the range dated to its sha with the tree-identity argument (§0, method notes); the bounds line extracted from the packet itself into `spec/packet-bounds-line.log` and the whole `bounded_census` block into `spec/packet-bounded-census.json`, cited where the line is quoted (§4, §12); the load and helper-age figures withdrawn — no measurement was saved for those runs — and this head's gate runs measured into `gates/box-load-<sha7>.log` (§11) |

**Round 3's findings, and what this head does about each** (the lens texts are posted on #288,
[round 3](https://github.com/sourcemaps/upstroke/pull/288#issuecomment-5672137527)):

| Finding | What this head does |
|---|---|
| verify 1 (P2), record 1 (P1) — the strict audit still credits recovery its cited tests never perform (`RunDir.WriteQuestionPayload`/before: `rematerialize_question`; `Ref.PinCandidatePrepared`/before: `recovery_for(..)` returns a plan; `Event.AppendFirst`/written/kill: classification; `Event.OpenLog`/create/kill: the created log never reopened — the record lens executed a mutation that breaks that reopen and the cited tests stayed green) and labels 118 rows `disk` where ST-07 says "replay twice equal" | **every credited row re-audited on the three obligations separately** — (i) the exact durable prefix and residue constructed at the coordinate, (ii) the tabled action performed by the recovery (a plan or a classification is not), (iii) replay twice equal asserted — all 112 cited test bodies read again and what each constructs, performs and asserts recorded (`inventory/per-entry-recovery-audit-round3-test-facts.md`), the grades hand-written per row into a generator that refuses a citation not resolving to one `#[test]` at the cited line of `HEAD` (`inventory/round3-generator/`): **F 4, N 24 (nothing to replay: a creation husk), R 86 (constructed and recovered, replay-twice not asserted), O 54**. Only two of the 112 cited tests assert replay-twice equality (`recover/tests.rs` 7246–7249 and 11520–11522); `kill_inside_closure_recovers`' "replay twice agrees" is one replay compared with an outcome. **Not established: 140 of 168**, not 40; the fix table gives all 140 their file and shape (§5.3, §13 clause 2), and the header, §5.1, §5.3, §8 groups 1 and 7, §9.1–§9.3, Q5, §13 clauses 2 and 8, the verdict and the fix list carry the round-3 counts |
| regression 2 (P2, executed) — the corrected power-loss witness accepts a barrier placed before A's deletion | **the loss model requires a barrier after the observed deletion**: A's deletion survives the loss only on a `SyncedDirectory` record of `public` that observed A absent, which the reclaim's barrier writes when it reads the recorded name immediately before its sync (`sync_dir_observing`, the shape of `workspace_manager::sync_checkout_removed`). **Five runs at `3649147f`** (`tmp-tests-4/phase.log`, 23:41:21Z–23:43:52Z): unfixed `cargo exit=101`; the prescribed fix `0`; the prescribed fix with its barrier moved before the deletion `101` (the negative control); that misplaced barrier under round 2's model `0` (its false green, reproduced); round 2's plain-`sync_dir` fix under the stricter model `101` (a barrier that observes nothing cannot show it followed the deletion). Both files restored from `cp` backups with sha256 equal to `HEAD` and `git status --porcelain` empty (§3.4, §13 row 7); the fix list tells the fix pull request to carry the negative control as a mutation witness |
| regression 1 (P1), record 3 (P3) — `spec/class-searches.log` replaced while the paragraph kept the old log's figures (44 lines, five empty patterns, ten classes) | each round's figures attributed to that round's log: round 1's `spec/class-searches-round1.log`, 16 patterns, 44 report lines, 5 empty; round 2's, kept as `spec/class-searches-round2.log`, 25 patterns — nine added, not ten — 63 report lines, 5 empty (50 lines and 3 empty over the first sixteen); round 3's `spec/class-searches.log` as measured over this head (§0, "The class rule, applied") |
| regression 3 (P3) — a literal `\1` at the head of §3.4's corrected-witness row drops its `run-unfixed.log` cell | removed; the row renders its four cells (§3.4) |
| regression 4 (P3), verify 2 (P3), record 2 (P1) — "six" Event points O where the audit had seven; Q5's and clause 2's universal Event-point sentences; `129 F, 30 S, 9 O` left in §5.1 and group 1 | the Event points restated from round 3's audit: of the 21, **N 1, R 10, O 10** (§5.3); Q5's and clause 2's universal sentences replaced by those counts; §5.1 and group 1 carry the round-3 counts |
| record 3 (P3) — the recipe partition 40/3/3; the witness recipe's execution placed in phase 2 | **41** code recipes, **3** finding recipes, **2** witness recipes (`mutations/summary-g5-recipes.tsv`); the seven retrospective checks' executions dated to their logs: G5 chain chunk 3 (19:15:32Z–19:20:11Z), chunk 6 (19:31:15Z–19:32:39Z), and the witness recipe in phase 1 (19:33:10Z–19:34:32Z, `tmp-tests/phase.log`, `rc=101`) (§3.2) |

**The class rule, applied.** For each false figure or claim the lenses named, every copy across this
report and the pull request body was searched for before this head was built, and the searches are
stated with their counts in §0's last paragraph, "The class rule, applied".

### The specification, checked against the packet

The gate's entry is `cumulative_review_gates.gates[4]` of `~/tactus-artifacts/tactus-parallel-design-neutral-v16.json`
as amended on 2026-09-13 (sha256 `a97a8096ace2b9026d1f6508a1e20ef42cdcda07c41bf2e665dd4d50d399cc5f`,
the amendment record's "After" hash), read through the working extracts `~/tactus-artifacts/G5-DEFINITION.txt`
(sha256 `24667e80…`) and `~/tactus-artifacts/G5-QUESTIONS.txt` (sha256 `81c89a15…`). Both were checked
against the packet with python on this run (`spec/packet-check.log`): the extract's pass rule, reviewer
remit and every adversarial test are the packet's verbatim, the six standing questions are the
packet's verbatim, and the gate's entry holds **seven** `adversarial_fault_tests`, **five**
`required_artifacts`, **five** `integrated_invariants` and a **six**-row ownership matrix. Copies of
the extracts, the amendment, the lens, the brief, the packet's `bounded_census` block and the seam
tests ST-07..ST-20 are under `spec/` with their hashes (`spec/SHA256SUMS`).

### What the range contains

The range is "merged PR10 on top of G4". Between the G4 range `7e0110a1` and `caf6bed0`, `master`
took 21 first-parent merges (`git log --first-parent --oneline 7e0110a1..caf6bed0`): #260 (the G4
report), #261, #232, #251, #266, #267, #269, #265, #271, #272, #273, #270, #274, #275, #277, #276,
#278, #281, #282, #284, and #279 itself. The ones that change what this gate measures: #279 (PR10:
closure, finalization, the report, the census family, the ledger, ST-07 over the full inventory — the
record is `reviews/2026-09-12-pr10-record.md`); #275 (`PR8-CRASH-002` fixed); #272
(`PR8-R2-SPEND-REPLAY` closed by a design reading, §9.3); #281, #282 and #284 (`src/rundir/classify.rs`,
`discovery.rs`, `retention.rs`, `src/engine/topology/startup.rs` and their tests — the six files by
which `src/` at the range differs from PR10's own code head `09d6887a`; `effects/`, `Cargo.toml`,
`Cargo.lock` and `clippy.toml` are byte-identical between the two, tree ids
`872065345cc0d7b6593214dec15521a4af73f776`, `fd82478e…`, `432887da…`, `c1662cc8…`). Across the whole
range `Cargo.lock` is unchanged (`git diff --stat 7e0110a1 caf6bed0 -- Cargo.lock` is empty) and
`Cargo.toml` changed once, by `86069bdc` ("build: exclude the finding ledger from the published
package", +7 lines, a package `exclude`; no dependency). The gate reviews the integrated tree, not
#279's diff: every measurement below was taken at `caf6bed0`.

### Disagreements with the brief and the record, recorded

1. **The brief says the two G4 deferrals were fixed on master before #279's merge base "according to
   #279's record".** The record's §6 says "Not touched: `PR8-R2-SPEND-REPLAY`, `PR8-CRASH-002`". Both
   were verified in git rather than taken from either (§9.3): `PR8-CRASH-002`'s finding file was
   deleted by `1c309d49` (#275, 2026-09-12, "Finding: PR8-CRASH-002" in its message, a code fix), and
   `PR8-R2-SPEND-REPLAY`'s by `7da321c0` (#272, 2026-09-12, a `docs(design)` commit whose message says
   the finding's file is deleted under a design reading of §26). Both commits are ancestors of
   `231c1aad` and of the range, and neither is an ancestor of the G4 range. Neither is a deferral in
   range.
2. **The brief says the cumulative suite's "ten gates" are `w1-eight-iso` plus the audit script.** That
   is what was run (§1); the runner prints `ALL 9 PASS` for its nine and the tenth is run separately.
3. **The brief's flake list names the cherry-pick sampler and the descriptor-hold test.** Both
   reddened one Linux full run each here (§1), each fingerprinted by assertion text, each green on
   every isolated re-run, and neither is a G5 site.
4. **The record's R16 says the prefix reaches every bound on its own.** Re-measured: it does
   (`census-run2/census-summary.json`, `members[0].reaches_at_bound` lists all ten), on Linux twice and
   on the guest, byte-identical summaries (§4). What the record does not say, and this report does, is
   that two of those bounds are the tree's reading of the packet's bounds line and not its text (§4).

### Method notes that bind the evidence below

- **Nothing was taken on trust from the record or the brief.** The record's test-name mapping (§7)
  was re-derived against the tree (§2); every mutation recipe PR10 saved was re-applied and re-run at
  `caf6bed0` (§3); every artifact was regenerated here; every finding in range was executed where it
  carries a recipe (§9).
- **Mutation is the detection test.** Where this report says a test *detects* something, a mutation
  this report names was observed to kill that test at `caf6bed0`; where a mutation survived, the
  survival is reported and explained, never smoothed over (§3).
- **Every mutation and every temporary test was restored from a `cp` backup, never with
  `git checkout --`.** The mutation runner asserts each anchor occurs exactly once, copies the file
  aside, patches, runs, restores and compares (`cmp`); the temporary witness tests (§3.4, two phases)
  were appended to test modules whose backups were hashed against `git show HEAD:<path>` before and
  after (`tmp-tests/phase.log`, `tmp-tests-2/phase.log`: every restore `sha256 … == HEAD`,
  `git status --porcelain` empty).
- **Nothing this run constructed is committed.** `src/` is untouched on `gate/g5-run1`; the commits
  that carry this report add and change files under `reviews/` and nothing else, staged by explicit
  path.
- **Every figure names a saved file.** Where a sentence says *ran*, the log named beside it holds the
  test's result line; where it says *reasoned*, that word is in the sentence.
- **Which sha each execution ran on.** At `caf6bed0`, detached: the gate attempts at the range, the two
  export runs, the guest, the nine group invocations of the first head, PR10's 80 recipes and the first
  head's G5 recipes, the phase-1 witnesses and the Q1 probe, the first head's sampler loops, the merge
  checks. On `0642c87f` (the first head's second commit): the phase-2 witnesses (`tmp-tests-2/`), the
  Docker-required run (`docker/`), the producer-provenance loops (`residue/provenance/`,
  `samplers/linux-loops-b.log`), the corrected group 4, the seven retrospective anchor checks. On
  `00616451` (round 1's head): the two retrospective anchor checks of the answered-question recipes and
  the corrected power-loss witness (`tmp-tests-3/`). On `3649147f` (round 2's head): the power-loss
  witness's five runs under the stricter loss model (`tmp-tests-4/`). Every one of those commits adds or
  changes files under `reviews/` only: `git diff --stat caf6bed0 <head> -- src effects Cargo.toml
  Cargo.lock` is empty at each (logged in `tmp-tests-3/phase.log`, `tmp-tests-4/phase.log` and
  `mutations/check-pass-g5-round3.log`), so the
  compiled inputs, the test binary's sources and the fixtures are the range's; the logs name the sha
  they ran on, and so does this report where it cites them.
- **Concurrency on the box.** Every cargo command ran through `upstroke-build` on a private target base
  (`/mnt/ramtarget/iso-g5-run1` for the gate runner, `/mnt/ramtarget/iso-g5_run1` for every other run
  through round 2, renamed `/mnt/ramtarget/iso-g5_fix_r3` for round 3's runs because the ramdisk was 99%
  full),
  one cargo command at a time on the worktree; the gate binary bakes this worktree's path and no
  other (`gates/binary-provenance.log`: `/srv/worktrees/g5-run1` occurs 9 times in the lib test binary,
  `/srv/worktrees/pr10`, `/srv/tactus`, `/srv/worktrees/g4r3` and `/srv/worktrees/review-279` 0 times).

**The class rule, applied.** Each figure, name or control claim a lens found false was treated as a
class, not a site: one `grep -n -i -E` pattern per class, run over the whole report and, before the push,
over the report and the body together, by `spec/class-searches.sh` (the patterns are in the script; each
log lists every matching line and the count per pattern, and `spec/class-searches-tally.py` counts each
log into `spec/class-searches-tally.log`). **Round 1** added sixteen classes — the guest binary's test
count; the group-7 census test count; the census-kill count; the filter count; the number of Linux
full-suite runs; the body's file and line counts; the wrong-module group-4 filter; sampler loop counts and
histogram producers; the "Docker-gated lanes ran" claim; the PR5 histogram's run label; the universal
pre-check claim; the `--global` gitconfig figure; `Present(2)` beside Complete; pass language (`G5
PASSES`, `nine of nine`, `unblocked by G5`); the "reads `Cargo.lock`" claim; and "dependency review" as a
held claim — and over round 1's report (1,334 lines) its sixteen patterns matched 44 lines and five
matched nothing (`spec/class-searches-round1.log`). **Round 2** added nine patterns — the "159" and "nine
of 168" coordinate counts; the "twenty"/"20 census" count; the pre-check claims; "all at `caf6bed0`";
`packet-check.log` named as holding the bounds line; the load and helper-age figures; "every check …
established by a named execution"; "fails today and passes with the change"; and a `+G5` witness cited
as committed — and over round 2's report (1,451 lines) the twenty-five matched 63 lines, 50 of them for
the first sixteen, with five patterns matching nothing, three of the sixteen and two of the nine
(`spec/class-searches-round2.log`, the round-2 head's log kept under its round's name; this paragraph's
round-2 version gave round 1's 44 and five as that head's and called the nine patterns ten, which round
3's regression and record lenses found). **Round 3** adds thirteen patterns for the four classes its
lenses named: **a count that disagrees with its log** (the audit counts `129 F, 30 S, 9 O` and `F 80, F=
14, S 34, O 40` with their 128, 114 and 40; the "six" Event points O; the recipe partition "forty … three
recipes run against"; witness C's "ten S rows"; the "two `Answer.Ingest` coordinates"), **a universal
sentence the audit contradicts** (every Event point "with a committed test that kills or faults there and
drives the recovery", "all F in the audit", a recovery that reads the disk standing for the replay
clause), **a log replaced under old figures** (the scan's 44 lines, five empty patterns and ten classes;
round 2's loss model "recorded before record(B)"), and **a chronology** (the witness recipe in "the
phase-2 window from 20:54:55Z"), with the `\1|` render artifact as a fifth, searched beyond its pattern too:
`spec/table-rows-check.py` lists, in every table of a file, each row whose cell count differs from its
header and each line standing between two rows; over round 2's report it lists that line and three rows
of §3's G5 recipe table broken where a recipe's comment ran onto a second line (rows 22–24, so the table
ended at row 22), and over this report and the body nothing, the three rows joined
(`spec/table-rows-check.log`). Over this report as committed
the thirty-eight patterns match 84 lines — 48 for round 1's sixteen, 14 for round 2's nine and 22 for
round 3's thirteen — and ten match nothing (three of round 1's, two of round 2's and five of round 3's);
over the pull request body before the push they match 37 lines, 9 of them for round 3's patterns, and
eighteen match nothing (`spec/class-searches.log`, `spec/class-searches-tally.log`). Every hit is §0's or a method note's own quotation of a withdrawn figure
beside its correction, the corrected figure, the preserved row of an earlier round's evidence labeled as
superseded, or another use of the word; the log lists each.

## 1. Step A — the cumulative suite on the exact frozen range

The packet requires *"the cumulative test suite up to that gate runs on that exact range"*.

**Run:** `~/bin/w1-eight-iso /srv/worktrees/g5-run1` (nine gates on the private base
`/mnt/ramtarget/iso-g5-run1`, box-wide lock, foreground), then `bash .github/scripts/test-pr-ready-audit.sh`.
The private base was pre-warmed with `cargo test --no-run` through the wrapper (`build/prewarm-gate-target.log`:
`Compiling upstroke v0.1.0 (/srv/worktrees/g5-run1)`, three executables), so both attempts below ran
that binary (`gates/binary-provenance.log`). Clean tree, detached at the merge commit.

| # | Command | Attempt 1 (17:41:22Z) | Attempt 2 (17:45:16Z) |
|---|---|---|---|
| 01 | `cargo fmt --check` | PASS 1s | PASS 1s |
| 02 | `cargo clippy --all-targets --all-features -- -D warnings` | PASS 22s | PASS 0s |
| 03 | `cargo test --all-targets --all-features` | **FAIL 58s** (one test, below) | PASS 52s |
| 04 | `cargo +1.85.0 check --locked --all-targets --all-features` | PASS 19s | PASS 0s |
| 05 | `test-release-record.sh` | PASS | PASS |
| 06 | `test-pr-policy.sh` | PASS 9s | PASS 8s |
| 07 | `test-pr-ledger-evidence.sh` | PASS | PASS |
| 08 | `test-docs-consistency.sh` | PASS | PASS |
| 09 | `test-internals-notes.sh` | PASS | PASS 2s |
| 10 | `test-pr-ready-audit.sh` (run separately, 17:46:51Z) | — | PASS (`test-pr-ready-audit: ok`) |

Logs: `gates/w1-eight-iso-range-run1.log`, `gates/w1-eight-iso-range-run2.log`,
`gates/eight-logs-caf6bed-failed-20260914T174313Z/` (attempt 1, preserved by the runner before the
rerun), `gates/eight-logs-caf6bed-run2/` (attempt 2), `gates/gate10-range.log`.

**Observed counts, attempt 2, the green one, Linux.** `running 2663 tests` in the lib binary
(`gates/eight-logs-caf6bed-run2/03-test.log`); **2590 passed, 0 failed, 73 ignored** in 52.15s;
`running 10 tests` in the `main.rs` binary, all passed; 0 in the `probe` example — **2600 passed in
total**. G4 measured 2487 declared and 2443 passed at `7e0110a1`; the range adds 176 declared lib tests.

**The Docker-gated lanes, established by running them with Docker required.** The suite's 214
`runner::container::` result lines do not establish that Docker executed: `docker_gate`
(`src/runner/container/fake.rs`) returns an early success when Docker is unavailable unless
`UPSTROKE_REQUIRE_DOCKER` is set, under which a test that would skip panics instead. So the 23
Docker-gated tests (`DOCKER_GATED_TESTS`) were run with that variable set (`docker/real-docker-required.log`,
on `0642c87f`, whose compiled inputs are the range's: §0):
**21 passed, 2 failed**, the two `real_docker_kill_on_an_already_exited_container_is_tolerated` and
`real_docker_returns_both_streams_of_a_container_separately`, each panicking at
`src/runner/container/tests.rs:3226:5` with *"`upstroke-f1-…` is still running after 200
observations"* — the box's Docker termination-poll flake, filed as
`findings/P3_correctness_202609121234_the-docker-termination-poll-counts-yields-not-time.md` and the
`PR274-DOCKER-*` red of PR10's record — and each **3 of 3 green alone** with the variable still set
(`docker/real-docker-reds-alone.log`). Twenty-one passes under the requirement, and two reds whose
assertion is about a container that did start, are the execution evidence; the round-1 sentence that
inferred it from result lines is withdrawn.

### The one red, fingerprinted

Attempt 1's test leg failed on exactly one test (`gates/eight-logs-caf6bed-failed-20260914T174313Z/03-test.log`,
line 2720):

```
test rundir::tests::a_hold_whose_command_never_spawns_is_released_with_the_descriptor ... FAILED
thread '...' panicked at src/rundir/tests.rs:4447:5:
released with the descriptor
test result: FAILED. 2589 passed; 1 failed; 73 ignored; 0 measured; 0 filtered out; finished in 57.85s
```

Fingerprinted by assertion text and location: the descriptor-hold flake PR10's record §8 censused on
this box — 12 of 1,014 gate logs hold that `FAILED` line (`~/pr10-evidence/r8/descriptor-hold-flake-census.log`,
its population in `descriptor-hold-flake-census-population.log`), the inherited-descriptor window
under the parallel suite. The test was **added inside this range** by `1c309d49` (#275, 2026-09-12),
so no control at the G4 range can run it; the control taken instead is the test alone at `caf6bed0`,
ten times: **10 of 10 passed**, `cargo exit=0` each (`flakes/hold-alone-caf6bed.log`,
`flakes/hold-alone-caf6bed-run-<n>.log`). It is not a G5 site; no finding covers it by that name.
**Attempt 2 is the gate's cumulative-suite result**, green on the exact frozen range.

### The suite twice more, with every evidence export set

The five artifacts of §4–§7 are written by the suite's own tests through their export switches, so the
full suite was run twice more on the second private base, `iso-g5_run1`, with
`UPSTROKE_HOOK_OBSERVATIONS`, `UPSTROKE_LEDGER_EXPORT` and `UPSTROKE_CENSUS_SUMMARY` set. **Four
full-suite runs of the Linux binary were made at the range in all** — the two gate attempts and the
two export runs — and two of the four were red, each on one known flake:

| Run | Result | Log |
|---|---|---|
| export run 1 (17:48Z) | **2589 passed, 1 failed, 73 ignored**, `cargo exit=101`; the one failure is `engine::topology::recover::tests::sampled_cherry_pick_child_kills_every_residue_classified_and_recovered`, panicked at `src/engine/topology/recover/tests.rs:8355:10`, *"the resume settles the planted state: Refused { message: "run … still has a process of its own alive in worktree … an agent-cleanup reaper, or a Git child writing one of its refs -- and that process holds the run's cleanup lease; refusing overlapping engine ownership" }"* — the cleanup-lease refusal the record's §9 attributes to the inherited-descriptor window under the parallel suite (its round-1 full suite failed the same sampler the same way); the sampler is PR8's `Object.ProposalCherryPick` site, not a G5 site. Alone at `caf6bed0`: **5 of 5 passed** (`samplers/cherry-pick-alone.log`) | `full-suite-export.log` |
| export run 2 (17:57Z) | **2590 passed, 0 failed, 73 ignored**, the 10 binary tests passed, `cargo exit=0`; 335 observation files, 6 ledger tables, the census summary | `full-suite-export-run2.log`, `observations-run2/`, `ledger-run2/`, `census-run2/` |

Run 2's exports are the ones §4–§7 quote; run 1's are kept beside them (`observations/`, `ledger/`,
`census/`) and, where compared, are byte-identical (the census summary and all six ledger tables).
Run 2's export directory is also where two of the histograms of §5.4 come from; the PR5 histogram
quoted in §5.4 is **not** from either export run (§5.4 says whose it is).

### The Windows guest

The guest fetched the range as a bundle into its own tree `C:\upstroke-g5` (a fresh clone, checked out
detached at `caf6bed0`, `git status --porcelain` empty), compiled into its own target (`guest/guest-build-caf6bed.log`:
`Compiling upstroke v0.1.0 (C:\upstroke-g5)`, `Finished` in 54.59s after its dependency artifacts were
copied from the box's earlier PR10 target), and ran `cargo test --all-targets --all-features` with the
same three exports set (`guest/g5-full.cmd`, cargo's exit appended by the batch file):

**2541 declared, 2487 passed, 0 failed, 54 ignored** in 382.59s in the lib binary; the `main.rs`
binary **12 passed, 0 failed, 1 ignored** (the ignored one is `tests::cli_ambient_latch_helper`, a
subprocess helper); `cargo exit=0`; `git status --porcelain` empty before and after; the SHA-256 of
`effects/residue-synthetic.json` identical before and after
(`34505068387db309e4e3dc94d9a02bd9c5dd39e5e4fd15b38f4e93bfc8df8a71`); 17:50:21Z to 17:56:45Z
(`guest/guest-run-caf6bed.log`, `guest/guest-fulltest-caf6bed.log` lines 2598 and 2617). Its exports
are under `guest/exports/` (331 observation files, 6 ledger tables, the census summary). The first
head of this report said "the binary's 13 tests passed"; the log says 12 passed and 1 ignored.

### CI at the frozen sha, all three platforms

Two CI runs exist at `caf6bed0` (`ci/ci-at-range-summary.txt`, the JSON of each under `ci/run-<id>.json`):

| Run | Event | Result |
|---|---|---|
| 34874013564 (17:19:08Z) and 34874013597 | `merge_group` — the merge-queue build that landed the commit | **every job success**: lint, lint (macos), lint (windows), test (ubuntu-latest), test (macos-latest), test (winguest), msrv ×3, upstroke-ci; upstroke-pr-policy success |
| 34875053084 (17:29:22Z) | `push` — the run on `master` after the merge | lint ×3, test (ubuntu-latest), test (winguest), msrv ×3 success; **test (macos-latest) failure**, so upstroke-ci failure |

The macOS red, fingerprinted from the job log (`ci/ci-34875053084-macos-job-104080064906-full.log`,
lines 3063–3070): `agent::proc::tests::kill_tree_settles_the_whole_unix_group_before_it_returns`
panicked at `src/agent/proc/tests.rs:879:5`, *"kill_tree returned while a member of the child's
process group was still running and still holding its pipes: only the direct child was killed"*;
**2564 passed, 1 failed, 56 ignored**. That is, word for word and at the same location, the filed
finding `PR281-MACOS-KILL-TREE-SETTLE-ONLY-THE-DIRECT-CHILD`
(`findings/P2_correctness_202609131200_the-macos-kill-tree-settle-fingerprint.md`, P2, deferred,
filed 2026-09-13 with four earlier sightings, one on `master` itself). The test predates this range
(it is in `7e0110a1`) and is not a G5 site. **The newest run per check name at the range is the push
run, so the macOS check at `caf6bed0` is red in its newest run and green in the merge-queue run of the
same sha; both are stated here rather than the greener one chosen.** No merge-base control was run
for it: the failing test is unchanged across the range and the merge-queue run of the same sha is the
control this report has.

**Step A verdict: the cumulative suite is green on the exact frozen range on Linux (attempt 2 and
export run 2) and on the Windows guest, with one known-flake red in each of two of the four Linux
runs recorded and fingerprinted; CI is green on all three platforms in the merge-queue run at the sha
and red on macOS alone, on a filed P2 flake, in the push run.** This is a necessary condition of the
pass rule and not a sufficient one; detection is established by mutation in §3.

## 2. Step B — the packet's test names against the tree

The record's §7 maps the packet's tests to the tree by name. The mapping was re-derived here, not
trusted: every backticked identifier of 13 or more characters in §7 of the record (84 of them) was
searched as `fn <name>(` under `src/` (`names/record-s7-test-names-in-tree.txt`): **79 are defined exactly
once**; 3 are absent, each explained — `every_declared_unobservable_coordinate_has_its_reason_in_the_code`
(removed in PR10's round 2 with the declarations it held, as §7 says), `generation_closed` (an event
name, not a test) and `resume_finalizes_then_refuses` (the packet's slug; the tree's is
`resume_finalizes_halted_then_refuses`, as §7 says); 2 are defined more than once and are helpers,
not tests (`assert_live_equals_replay`, `spawn_kill_child`).

**The tests this gate runs by name, and the one filter that was wrong on the first head.** The nine
group invocations of §8 take their filters from `groups/group-tests.txt`: **177 filters listed, 171
unique — 170 exact test names and one module filter (`effects::`)**. On the first head the check was
`fn <name>(` under `src/`, which every name passed, but one filter carried the wrong module —
`engine::topology::recover::tests::a_breach_appends_budget_exceeded_and_integration_and_run_end_cross_the_checkpoint`,
whose test lives in `engine::topology::select::tests` — so the round-1 group-4 invocation ran **12 of
its 13 filters** and reported 12 passed with `cargo exit=0` (`groups/group4-st17-closure-round1.log`;
the record lens found it). The check is now against the binary's own list: `cargo test -- --list` at
`caf6bed0` names 2,673 tests (`groups/test-list-caf6bed.txt`), and every one of the 170 exact filters is
in it (`groups/filter-check-round2.txt`: "not a test in the binary list: none"); the filter was
corrected in place (`groups/group-tests-round1.txt` is the first head's list) and group 4 re-run:
**13 passed** (`groups/group4-st17-closure.log`, the corrected test's own `ok` line in it).

The packet's seam tests in range — ST-07, ST-09, ST-10, ST-11, ST-12, ST-13, ST-14, ST-15, ST-17,
ST-18, ST-19, ST-20 — were extracted from the packet with their statements (`spec/seam-tests-ST07-ST20.txt`)
and each is placed against the tree in §8, group 7. `src/effects/tests/contract_mappings.rs` holds
the mechanical mapping the tree carries for ST-16 and ST-20 (the ST-20 report/status row maps to
`finalized_report_names_runner_identity`, which is a `#[test]` defined once); the other seam tests have
no mechanical mapping in the tree, and the obligation was discharged by hand here as G4 did.

## 3. Step C — detection, established by mutation

A green suite proves the tests pass, not that they still detect. Two campaigns ran at `caf6bed0`,
both through PR10's own mutation runner (`~/pr10-evidence/r10/mutations.sh`, copied as
`mutations/mutations-pr10-recipes.sh` with exactly two lines changed — its worktree root and its
target base — `mutations/runner-diff.log` holds the `diff` and the two files' sha256,
`88f88f76…` and `92c7b44f…`). The runner asserts each anchor occurs **exactly once** in the file,
copies the file aside, patches it with a python script, saves `git diff` of the patched file as
`<name>.diff`, runs **the one test the recipe names** through the wrapper, restores the file from
the `cp` backup and `cmp`s it, and records `rc` (cargo's exit as captured) in `summary.tsv`. Every
chunk log (`mutations/chain-chunk-01..08.log`, `chain-g5-chunk-02..06.log`) ends with
`tree after: 0 paths`, and `git status --porcelain` was empty after every chunk.

**The detection set is the named test, not the full suite.** This differs from G4's campaign, which
ran the full library suite under every mutation. Here "killed" means the recipe's named test failed
(`rc=101`) under the mutation, which establishes that *that test* detects the mutation; "survived"
means the named test passed, which says nothing about other tests, and each survival below is
explained by reading the test that passed. A recipe expected to pass (`sampler-kills-leader-only`,
a fixture reversion that is no witness either way; `torn-registration-both-without-gitdir`, a
test-side recipe) is reported as what it is.

### 3.1 PR10's 80 recipes, re-run at the range

Before any test ran, every recipe was applied and restored in the runner's `CHECK=1` mode
(`mutations/check-pass.log`: 80 `anchor applied`, 80 diffs, `tree after: 0 paths`), so an anchor
that no longer matched would have been found first; all 80 match at `caf6bed0` (the six files
`master` changed after PR10's code head are none the recipes patch). Four recipes assume umask
`022` (the report writer's mode tests); the box's shell umask is `077`, so chunks 5–8 ran under
`umask 022`.

**Result: 80 recipes, 80 with their expected result — 78 killed at `rc=101`, the 2 expected to
pass passing at `rc=0`** (`mutations/summary-pr10-recipes.tsv`, consolidated from the chunk logs
because the runner truncates its own `summary.tsv` per invocation; one `.log`, `.diff` and
`.backup` per recipe under `mutations/run/`).

| # | Recipe | Mutation | Test | Expected | Observed (`rc`, result line) |
|---|---|---|---|---|---|
| 1 | `ledger-report-skipped` | ledger: the report skipped at finalization (finalize.rs:105 recipe) | `engine::topology::recover::tests::the_ledger_balances_at_complete` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.15s (`mutations/run/ledger-report-skipped.log`) |
| 2 | `ledger-prunes-unreachable` | ledger: finalization prunes an already-unreachable object (R27) | `engine::topology::recover::tests::the_ledger_balances_at_complete` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.15s (`mutations/run/ledger-prunes-unreachable.log`) |
| 3 | `projection-no-live-snapshot` | projection: the emitter never reports the live projection | `engine::topology::recover::tests::projections_are_equal_between_live_and_replay_at_every_prefix` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.05s (`mutations/run/projection-no-live-snapshot.log`) |
| 4 | `projection-stale-prefix` | projection: the live snapshot is taken from a stale prefix | `engine::topology::recover::tests::projections_are_equal_between_live_and_replay_at_every_prefix` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.05s (`mutations/run/projection-stale-prefix.log`) |
| 5 | `st18-order-swapped` | ST-18: snapshots scrubbed ahead of task worktrees (finalize.rs:111 recipe) | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.53s (`mutations/run/st18-order-swapped.log`) |
| 6 | `st17-halted-with-backoff-refused` | ST-17: closure refuses a halted run with a pending backoff (closure.rs:38 recipe) | `engine::topology::recover::tests::run_finished_halted_and_budget_exceeded_accepted_with_deferred_items` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.03s (`mutations/run/st17-halted-with-backoff-refused.log`) |
| 7 | `st17-budget-outranks-halt` | ST-17: a budget stop outranks the halt (closure.rs:17 recipe) | `engine::topology::recover::tests::run_finished_budget_exceeded_refused_after_halting_drain_settlement` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.02s (`mutations/run/st17-budget-outranks-halt.log`) |
| 8 | `classifier-drops-beta` | classifier: beta's open generation dropped from the plan (reachability.rs:119 recipe) | `topology::census::tests::every_fault_rows_durable_prefix_is_a_reachable_state_classified_as_its_resume_action` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 17.44s (`mutations/run/classifier-drops-beta.log`) |
| 9 | `settle-scrub-removed` | settle: the closed-settlement scrub removed (the pre-repair shape the record quotes) | `engine::topology::recover::tests::the_ledger_balances_at_parked` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.08s (`mutations/run/settle-scrub-removed.log`) |
| 10 | `settle-scrub-before-close` | settle: the scrub moved ahead of the durable close (record P2-5 recipe) | `engine::topology::recover::tests::a_closed_settlement_scrubs_the_generations_worktree_and_intent` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.05s (`mutations/run/settle-scrub-before-close.log`) |
| 11 | `m22-conflict-deletes-candidates-ref` | M22: a conflict rejection deletes the rejected candidate's candidates ref (G4 O1) | `engine::topology::integrate::tests::terminal_shape_coverage_table_drives_every_shape_and_each_converges_on_replay` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.36s (`mutations/run/m22-conflict-deletes-candidates-ref.log`) |
| 12 | `m23-code-rejection-deletes-candidates-ref` | M23: a code rejection deletes the rejected candidate's candidates ref (G4 O1) | `engine::topology::integrate::tests::terminal_shape_coverage_table_drives_every_shape_and_each_converges_on_replay` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.46s (`mutations/run/m23-code-rejection-deletes-candidates-ref.log`) |
| 13 | `census-bound-not-reached` | census: the generator stops one sequence short of the declared bound (contract P1-2, the equality recipe) | `topology::census::tests::every_declared_dimension_is_reached_at_its_bound` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.89s (`mutations/run/census-bound-not-reached.log`) |
| 14 | `census-trace-ceiling-silent` | census: the trace ceiling stops expansion without saying so (contract P1-2, the truncated recipe) | `topology::census::tests::a_census_that_hits_its_ceiling_says_so` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.14s (`mutations/run/census-trace-ceiling-silent.log`) |
| 15 | `census-defers-not-reached` | B2: the verification-defer bound offered one short | `topology::census::tests::every_declared_dimension_is_reached_at_its_bound` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.82s (`mutations/run/census-defers-not-reached.log`) |
| 16 | `census-lineage-collapsed` | B2: every repair collapsed into one lineage | `topology::census::tests::every_declared_dimension_is_reached_at_its_bound` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.85s (`mutations/run/census-lineage-collapsed.log`) |
| 17 | `ledger-verification-defers-zero` | B3: R14 verification_defers observed as zero | `engine::topology::ledger::tests::a_rows_named_facts_are_each_consumed_by_its_verdict` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/ledger-verification-defers-zero.log`) |
| 18 | `projection-skips-run-resumed` | B4: the live projection skipped for run_resumed | `engine::topology::recover::tests::projections_are_equal_between_live_and_replay_at_every_prefix` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.04s (`mutations/run/projection-skips-run-resumed.log`) |
| 19 | `closure-deletes-candidates-refs` | B5: the closure deletes every candidates ref before closing | `engine::topology::recover::tests::a_fault_between_the_closure_close_and_its_scrub_is_reclaimed_by_the_next_resume` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.09s (`mutations/run/closure-deletes-candidates-refs.log`) |
| 20 | `closure-swallows-close-append-error` | B6: the run-ending close swallows its append error | `engine::topology::recover::tests::an_append_error_at_the_run_ending_close_ends_the_command_and_the_next_resume_closes_the_generation` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.06s (`mutations/run/closure-swallows-close-append-error.log`) |
| 21 | `finalize-removes-answers-at-complete` | B7: the public husk (answers included) removed at Complete | `engine::topology::recover::tests::the_ledger_balances_at_complete` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.17s (`mutations/run/finalize-removes-answers-at-complete.log`) |
| 22 | `process-phases-not-recorded` | B8: the production adapter drops the process hook phases | `engine::topology::coverage::tests::the_process_funnel_fires_both_hook_phases_of_spawn_and_terminate_under_the_production_adapter` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.06s (`mutations/run/process-phases-not-recorded.log`) |
| 23 | `census-budget-class-gated` | B9a: the budget class gated on a reservable pipeline again | `topology::census::tests::the_census_runs_at_the_packets_bounds_and_says_where_it_stopped` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.08s (`mutations/run/census-budget-class-gated.log`) |
| 24 | `census-truncations-counted-refused` | B9c: truncated offers counted as refusals again | `topology::census::tests::the_census_summary_names_every_fault_row_and_serializes` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 17.37s (`mutations/run/census-truncations-counted-refused.log`) |
| 25 | `matches-row-unconditional` | contract P2: the three rows unconditional again | `topology::census::tests::every_fault_rows_durable_prefix_is_a_reachable_state_classified_as_its_resume_action` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.52s (`mutations/run/matches-row-unconditional.log`) |
| 26 | `finalize-keeps-staging-leftovers` | crash P2-3: finalization leaves the staging leftovers | `engine::topology::recover::tests::resume_finalizes_halted_then_refuses` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.05s (`mutations/run/finalize-keeps-staging-leftovers.log`) |
| 27 | `sampler-kills-leader-only` |  | `engine::topology::coverage::tests::sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered` | pass | passed: `rc=0`, ok. 1 passed; 0 failed; finished in 1.03s (`mutations/run/sampler-kills-leader-only.log`) |
| 28 | `classifier-reads-worktree-list` | B1a: the classifier reads Git's enumeration again (SWEEP-WORKTREE-012's shape) | `workspace_manager::tests::a_registration_git_cannot_enumerate_classifies_as_unpopulated_and_converges` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.03s (`mutations/run/classifier-reads-worktree-list.log`) |
| 29 | `proving-removal-refuses-empty-gitdir` | B1b: the proving removal refuses an empty gitdir like the plain one | `workspace_manager::tests::a_torn_registration_is_passed_over_and_the_slot_converges_when_no_writer_is_alive` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.02s (`mutations/run/proving-removal-refuses-empty-gitdir.log`) |
| 30 | `proving-removal-deletes-by-name` | B1c: the proving removal binds a torn registration by its name and removes it | `workspace_manager::tests::a_torn_registration_is_passed_over_and_the_slot_converges_when_no_writer_is_alive` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.03s (`mutations/run/proving-removal-deletes-by-name.log`) |
| 31 | `census-truncated-labels-refused` | B2: a truncated offer counted as a refusal again | `topology::census::tests::a_census_that_hits_its_ceiling_says_so` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.34s (`mutations/run/census-truncated-labels-refused.log`) |
| 32 | `report-write-hooks-swallowed` |  | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.30s (`mutations/run/report-write-hooks-swallowed.log`) |
| 33 | `finalize-swallows-leftover-removal-error` | B3b: the leftovers'"'"' removal error swallowed | `engine::topology::recover::tests::a_fault_at_a_staging_leftovers_own_removal_stops_finalization_and_the_next_resume_converges` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.06s (`mutations/run/finalize-swallows-leftover-removal-error.log`) |
| 34 | `closure-swallows-undetermined-close-append` | B4: the close swallows an undetermined append and scrubs | `engine::topology::recover::tests::an_append_error_at_the_run_ending_close_ends_the_command_and_the_next_resume_closes_the_generation` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.21s (`mutations/run/closure-swallows-undetermined-close-append.log`) |
| 35 | `closure-refuses-verification-deferred` | F2: closure refuses a verification-deferred candidate | `engine::topology::recover::tests::run_finished_halted_and_budget_exceeded_accepted_with_deferred_items` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.12s (`mutations/run/closure-refuses-verification-deferred.log`) |
| 36 | `acceptance-untracked-note-overwritten` | record P2-3: an untracked note overwritten before the checkout is compared (a test-side mutation) | `engine::topology::recover::tests::max_parallel_one_completes_a_two_task_chain_with_one_linear_commit_per_task_and_the_checkout_unchanged` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.18s (`mutations/run/acceptance-untracked-note-overwritten.log`) |
| 37 | `report-write-unsynced` | crash P2: the report written in place with no barrier | `rundir::tests::every_site_this_module_owns_is_reached_through_a_funnel_in_both_phases` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-write-unsynced.log`) |
| 38 | `report-write-unsynced-finalization` | crash P2, at finalization: the same mutation against the durability-before-pruning test | `engine::topology::recover::tests::the_report_is_durable_before_any_ref_is_pruned_and_a_current_report_is_not_rewritten` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run/report-write-unsynced-finalization.log`) |
| 39 | `residue-synthetic-pin` | regression P2: the tracked synthetic evidence edited (the pin) | `workspace_manager::tests::every_registered_residue_element_is_constructed_and_recovers` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.82s (`mutations/run/residue-synthetic-pin.log`) |
| 40 | `report-direct-write` | B1a: the report written in place — staged at its published name, no rename | `rundir::tests::every_atomic_publication_syncs_the_staged_file_then_renames_then_syncs_its_directory` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-direct-write.log`) |
| 41 | `report-stage-error-swallowed` | B1b: the staged report's sync error discarded | `rundir::tests::a_report_whose_staged_file_will_not_sync_is_not_published` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-stage-error-swallowed.log`) |
| 42 | `finalize-fresh-branch-skips-barrier` | B1b: the fresh branch skips its barrier (round 3'"'"'s shape), against the two-resume fault test | `engine::topology::recover::tests::a_report_directory_barrier_that_fails_refuses_pruning_on_every_resume_until_it_holds` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run/finalize-fresh-branch-skips-barrier.log`) |
| 43 | `finalize-fresh-branch-skips-barrier-ordering` | B1b: the same mutation against the path-specific ordering test | `engine::topology::recover::tests::a_report_rename_without_directory_sync_is_proven_before_pruning` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run/finalize-fresh-branch-skips-barrier-ordering.log`) |
| 44 | `acceptance-ignored-note-overwritten` | B2: the ignored note overwritten before the comparison (the lens'"'"'s survivor recipe) | `engine::topology::recover::tests::max_parallel_one_completes_a_two_task_chain_with_one_linear_commit_per_task_and_the_checkout_unchanged` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.14s (`mutations/run/acceptance-ignored-note-overwritten.log`) |
| 45 | `report-mode-not-preserved` | B3: the existing report'"'"'s mode not preserved (round 3'"'"'s writer), under umask 022 | `rundir::tests::the_payload_writers_keep_their_exact_legacy_bytes` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-mode-not-preserved.log`) |
| 46 | `common-git-dir-read-as-utf8` | B4: the common git dir read as UTF-8 text (round 3'"'"'s reader) | `workspace_manager::tests::an_absent_add_target_in_a_byte_named_repository_still_classifies` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.01s (`mutations/run/common-git-dir-read-as-utf8.log`) |
| 47 | `ledger-verification-defers-from-queue` | B5: R14 reads the queue again (round 3'"'"'s observer) | `topology::census::tests::consumed_verification_defers_survive_publication` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/ledger-verification-defers-from-queue.log`) |
| 48 | `proving-removal-skips-locked-without-gitdir` | B6: a locked registration without gitdir skipped without the proof (round 3'"'"'s arm) | `workspace_manager::tests::a_torn_registration_is_passed_over_and_the_slot_converges_when_no_writer_is_alive` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.03s (`mutations/run/proving-removal-skips-locked-without-gitdir.log`) |
| 49 | `torn-registration-both-without-gitdir` | B6: the lens'"'"'s recipe on the test itself — both torn entries without gitdir before the plain funnel is asked (expected to PASS after the fix; it failed at dc05c7b6, ~/pr10-evidence/r4/before-dc05c7b/) | `workspace_manager::tests::a_torn_registration_is_passed_over_and_the_slot_converges_when_no_writer_is_alive` | pass | passed: `rc=0`, ok. 1 passed; 0 failed; finished in 0.56s (`mutations/run/torn-registration-both-without-gitdir.log`) |
| 50 | `finalize-fresh-barrier-outside-the-sites` | B2: the fresh branch's barrier outside the report's sites (round 4's shape) | `engine::topology::recover::tests::a_report_rename_without_directory_sync_is_proven_before_pruning` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run/finalize-fresh-barrier-outside-the-sites.log`) |
| 51 | `finalize-fresh-barrier-outside-the-sites-matrix` | B2: the same shape against the ST-18 matrix, whose third resume selects the fresh branch'"'"'s sites | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.11s (`mutations/run/finalize-fresh-barrier-outside-the-sites-matrix.log`) |
| 52 | `finalize-fresh-barrier-complete-only` | B3: the fresh branch'"'"'s barrier at Complete only (the crash lens'"'"'s recipe) | `engine::topology::recover::tests::a_report_directory_barrier_that_fails_refuses_pruning_on_every_resume_until_it_holds` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.16s (`mutations/run/finalize-fresh-barrier-complete-only.log`) |
| 53 | `report-staged-at-the-umask` |  | `rundir::tests::a_private_report_is_staged_at_its_mode_before_any_byte_is_written` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-staged-at-the-umask.log`) |
| 54 | `fresh-barrier-hooks-swallowed` | B2: the fresh branch'"'"'s four hook answers discarded — the coordinate observed, its error swallowed | `engine::topology::recover::tests::a_report_rename_without_directory_sync_is_proven_before_pruning` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run/fresh-barrier-hooks-swallowed.log`) |
| 55 | `fresh-barrier-swallows-rundir-before` | B1: RunDir.WriteReport/Before of the fresh branch swallowed | `engine::topology::recover::tests::fresh_report_hook_errors_stop_cleanup_and_retry` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run/fresh-barrier-swallows-rundir-before.log`) |
| 56 | `fresh-barrier-swallows-report-before` | B1: Report.Write/Before of the fresh branch swallowed | `engine::topology::recover::tests::fresh_report_hook_errors_stop_cleanup_and_retry` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.11s (`mutations/run/fresh-barrier-swallows-report-before.log`) |
| 57 | `fresh-barrier-swallows-report-after` | B1: Report.Write/After of the fresh branch swallowed (the crash lens'"'"'s recipe) | `engine::topology::recover::tests::fresh_report_hook_errors_stop_cleanup_and_retry` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.09s (`mutations/run/fresh-barrier-swallows-report-after.log`) |
| 58 | `fresh-barrier-swallows-rundir-after` | B1: RunDir.WriteReport/After of the fresh branch swallowed | `engine::topology::recover::tests::fresh_report_hook_errors_stop_cleanup_and_retry` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.10s (`mutations/run/fresh-barrier-swallows-rundir-after.log`) |
| 59 | `report-staged-wider-than-preserved` | B2: the staged report created wider than its preserved mode (the fix-check lens'"'"'s survivor) | `rundir::tests::a_private_report_is_staged_at_its_mode_before_any_byte_is_written` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-staged-wider-than-preserved.log`) |
| 60 | `report-staged-wider-and-written-before-settle` | B2: the same, with the bytes written before settle_mode (the regression lens'"'"'s survivor) | `rundir::tests::a_private_report_is_staged_at_its_mode_before_any_byte_is_written` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-staged-wider-and-written-before-settle.log`) |
| 61 | `census-seeded-does-not-close` | the family: the seeded census stopped by its ceiling — it no longer closes | `topology::census::tests::the_prefix_reports_its_truncation_and_every_seeded_census_closes` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 17.91s (`mutations/run/census-seeded-does-not-close.log`) |
| 62 | `census-dimension-dropped-from-union-check` | the family: a bound dropped from the union check (`lineages` gone from `dimensions()`) | `topology::census::tests::every_declared_dimension_is_reached_at_its_bound` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/census-dimension-dropped-from-union-check.log`) |
| 63 | `census-seeded-row-misattributed` | B2: a seeded census's row misattributed — `from = usize::MAX` on deep_census's first row (the fix-check lens's recipe) | `topology::census::tests::the_census_transition_table_is_reproducible_from_the_folds_alone` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 29.26s (`mutations/run/census-seeded-row-misattributed.log`) |
| 64 | `st18-fresh-branch-prunes-objects` | B3: the fresh-report branch prunes Git's unreachable objects after its barrier (the crash lens's recipe at finalize.rs:106) | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.12s (`mutations/run/st18-fresh-branch-prunes-objects.log`) |
| 65 | `st18-fresh-branch-prunes-objects-real-kill` | the same, against the real-kill restart | `engine::topology::recover::tests::a_kill_inside_finalization_after_the_execution_root_is_removed_converges_on_the_next_resume` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 1.19s (`mutations/run/st18-fresh-branch-prunes-objects-real-kill.log`) |
| 66 | `report-group-not-preserved` | regression P2: the existing report's group not given to the staged file (the `fchown` replaced by a no-op) | `rundir::tests::rewriting_report_preserves_its_group` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-group-not-preserved.log`) |
| 67 | `histogram-under-the-manifest` | regression P3: the sequential histogram written under the manifest directory again | `engine::topology::coverage::tests::the_sequential_histogram_lives_beside_the_export_or_under_the_build_never_under_effects` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/histogram-under-the-manifest.log`) |
| 68 | `observations-loader-reads-the-histogram` | regression P3, the loader: the export loader reads the histogram as a record again | `engine::topology::coverage::tests::the_export_loader_passes_over_the_histogram_the_sampler_writes_beside_the_records` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/observations-loader-reads-the-histogram.log`) |
| 69 | `report-group-bits-before-fchown` | B2: every group bit granted by a chmod immediately before the group transition (the fix-check lens's survivor) | `rundir::tests::rewriting_report_preserves_its_group` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-group-bits-before-fchown.log`) |
| 70 | `report-leftover-not-reclaimed-on-write` | B3: the report site does not reclaim a dead writer's staged report on the write branch | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.08s (`mutations/run/report-leftover-not-reclaimed-on-write.log`) |
| 71 | `report-leftover-not-reclaimed-on-fresh` | B3: the same on the fresh branch | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.10s (`mutations/run/report-leftover-not-reclaimed-on-fresh.log`) |
| 72 | `report-leftover-not-reclaimed-unit` | B3: the next write reclaims nothing (the unit witness) | `rundir::tests::a_dead_writers_staged_report_is_reclaimed_by_the_next_write` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-leftover-not-reclaimed-unit.log`) |
| 73 | `scrub-checkout-unsynced-before-intent` | B4: the checkout's parent directory not synced before the funnel returns (the barrier removed) | `engine::topology::recover::tests::a_checkouts_deletion_is_made_durable_before_its_intent_is_removed` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run/scrub-checkout-unsynced-before-intent.log`) |
| 74 | `checkout-barrier-error-swallowed` | B1 (crash P1): the checkout barrier's error swallowed — the fused helper's result discarded | `engine::topology::recover::tests::a_failed_checkout_barrier_is_retried_before_its_intent_is_removed` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.05s (`mutations/run/checkout-barrier-error-swallowed.log`) |
| 75 | `checkout-synced-before-its-deletion` | B3 (fix-check P1): the barrier moved before the removal — the sync no longer follows the deletion | `engine::topology::recover::tests::a_checkouts_deletion_is_made_durable_before_its_intent_is_removed` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.06s (`mutations/run/checkout-synced-before-its-deletion.log`) |
| 76 | `report-group-bits-at-the-fchown` | B4 (fix-check B2, P1): the full mode granted by a chmod immediately before the fchown, between the two fstats | `rundir::tests::rewriting_report_preserves_its_group` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/report-group-bits-at-the-fchown.log`) |
| 77 | `sync-absent-slot-directory-skipped` | B2 (the slot kind's directory absent): the directory's absence taken on faith — no barrier in the root | `engine::topology::recover::tests::an_absent_slot_directory_is_made_durably_absent_in_the_root_before_the_intent_is_removed` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.05s (`mutations/run/sync-absent-slot-directory-skipped.log`) |
| 78 | `root-barrier-error-swallowed` | B1 (every lens, P1): the execution root's barrier error swallowed at the helper's end (the record lens's exact form) | `engine::topology::recover::tests::a_failed_execution_root_barrier_is_retried_before_the_intent_is_removed` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.05s (`mutations/run/root-barrier-error-swallowed.log`) |
| 79 | `staging-created-before-its-record` | B2 (rule 3): the staging directory made before its record is published — the swap | `rundir::tests::a_report_staging_directory_is_recorded_before_it_is_made` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/staging-created-before-its-record.log`) |
| 80 | `staging-reclaimed-without-a-record` | B2 (rule 3): reclamation removes every staging-shaped directory, record or not | `rundir::tests::legacy_report_preserves_unowned_staging_directory` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run/staging-reclaimed-without-a-record.log`) |

### 3.2 The gate's own 46 recipes

Forty-six recipes are in `mutations/mutations-g5-recipes.sh`: forty-one against the code the pass rule
names — the stable-prefix barrier's refusal predicates and its `SyncPrefix` point, the open funnel's
prefix sync (the two-crash proof), the effect-before-barrier order, `derived_outcome`'s totality, the
fold's end, answer, resume and runner checks, the cleanup table's outcome column, the run lock, the
config-drift warning, the ceiling breach, the Event funnel's kill and error-return points, the
`IdUnread` point, the pinned registry and the frozen sampling N, recovery's retained-close step,
the fast publication's two relations, finalization on resume, the report's freshness check, the
finished-run guard, the census generator's `run_resumed` and `question_raised` offers, and G4's M08
re-applied (an answered question stays open in the fold) — plus the three recipes the round-11
lenses wrote for PR10's findings and the record filed as unexecuted, and two recipes run against the
gate's temporary witnesses (§3.4; valid only while those are appended) — 41, 3 and 2 by
`mutations/summary-g5-recipes.tsv`, where this sentence's earlier versions said forty and three, which
round 3's record lens found. **The `CHECK=1` pass, as it happened**: 37
of the 46 were applied and restored in `CHECK=1` mode before they ran, in the first head's pass
(`mutations/check-pass-g5.log`, 37 distinct `anchor applied`); seven — the three re-aimed barrier
recipes, the two re-aimed `IdUnread` recipes, the re-aimed frozen-N recipe and the witness recipe
`r11-third-resume-leftover-record-only-vs-g5-witness` — were checked **after** their executions,
retrospectively at 20:42:06Z on `0642c87f` (`mutations/check-pass-g5-round2.log`, seven entries;
their mutation runs are in G5 chain chunk 3, 19:15:32Z–19:20:11Z (the three barrier recipes,
`mutations/chain-g5-chunk-03.log`), chunk 6, 19:31:15Z–19:32:39Z (the two `IdUnread` recipes and the
frozen-N one, `chain-g5-chunk-06.log`), and the phase-1 witness window at `caf6bed0`, 19:33:10Z–19:34:32Z
(the witness recipe, `rc=101`, `tmp-tests/phase.log`; this sentence's round-2 version placed that one in
phase 2, which round 3's verify and record lenses found));
and the two `g5-answered-question-stays-open` recipes, applied by the phase-2 script with no check
entry, were checked retrospectively in round 2 at 22:07:58Z on `00616451`
(`mutations/check-pass-g5-round3.log`, two entries; the anchor file's sha256 equal to `HEAD` after).
The first head's sentence that every recipe had been pre-checked was false of nine; round 1's head
counted the later checks as nine and called them a pre-check, which round 2's record lens found. The
mutation results themselves are unaffected: a check applies the anchor and runs nothing.

**Result: 46 runs, one per recipe: 35 killed at `rc=101`, 11 passed at `rc=0`**
(`mutations/summary-g5-recipes.tsv`, one `.log`, `.diff` and `.backup` per recipe under
`mutations/run-g5/`). Of the eleven survivals, **three are the round-11 findings confirmed** (expected
to survive), **five were mis-aimed filters re-aimed and then killed**, and **three are explained by
reading the test** and are not detection gaps; each is taken up in §3.3.

| # | Recipe | Mutation | Test | Expected | Observed (`rc`, result line) |
|---|---|---|---|---|---|
| 1 | `g5-barrier-reread-mismatch-ignored` | G5-01: the barrier's byte-equality predicate dropped | `runner::container::census::tests::the_stable_prefix_barrier_refuses_each_of_its_four_predicates_independently` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 0.00s (`mutations/run-g5/g5-barrier-reread-mismatch-ignored.log`) |
| 2 | `g5-barrier-torn-tail-reappearance-ignored` | G5-02: the barrier's torn-tail-reappeared predicate dropped | `runner::container::census::tests::the_stable_prefix_barrier_refuses_each_of_its_four_predicates_independently` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 0.00s (`mutations/run-g5/g5-barrier-torn-tail-reappearance-ignored.log`) |
| 3 | `g5-barrier-length-mismatch-ignored` | G5-03: the barrier's length predicate dropped | `runner::container::census::tests::the_stable_prefix_barrier_refuses_each_of_its_four_predicates_independently` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 0.00s (`mutations/run-g5/g5-barrier-length-mismatch-ignored.log`) |
| 4 | `g5-sync-prefix-point-not-consulted` | G5-04: OpenLog no longer consults its SyncPrefix point | `engine::topology::recover::tests::barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.04s (`mutations/run-g5/g5-sync-prefix-point-not-consulted.log`) |
| 5 | `g5-open-sync-skipped` | G5-05: the open funnel no longer syncs the prefix (the two-crash proof) | `engine::topology::recover::tests::unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.02s (`mutations/run-g5/g5-open-sync-skipped.log`) |
| 6 | `g5-derived-outcome-parked-as-fold-error` | G5-06: a parked shape derives FoldError (totality) | `topology::census::tests::the_derived_outcome_is_total_over_every_explored_state` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 15.77s (`mutations/run-g5/g5-derived-outcome-parked-as-fold-error.log`) |
| 7 | `g5-run-finished-outcome-unchecked` | G5-07: run_finished accepted with any outcome (the derived-outcome equality dropped) | `engine::topology::recover::tests::replayed_conflicting_outcome_refused` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.01s (`mutations/run-g5/g5-run-finished-outcome-unchecked.log`) |
| 8 | `g5-answer-accepted-under-budget-stop` | G5-08: an answer accepted under the epoch's budget stop | `topology::fold::tests::an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-answer-accepted-under-budget-stop.log`) |
| 9 | `g5-resume-keeps-budget-stop` | G5-09: run_resumed keeps the budget stop | `engine::topology::recover::tests::resume_clears_budget_stop_and_wakes_deferred` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.02s (`mutations/run-g5/g5-resume-keeps-budget-stop.log`) |
| 10 | `g5-resume-does-not-wake-deferred` | G5-10: run_resumed wakes nothing deferred | `engine::topology::recover::tests::resume_clears_budget_stop_and_wakes_deferred` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.02s (`mutations/run-g5/g5-resume-does-not-wake-deferred.log`) |
| 11 | `g5-candidates-refs-pruned-at-halted` | G5-11: candidates refs pruned at Halted too | `engine::topology::recover::tests::halted_report_lists_candidate_refs` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.04s (`mutations/run-g5/g5-candidates-refs-pruned-at-halted.log`) |
| 12 | `g5-candidates-refs-kept-at-complete` | G5-12: candidates refs never pruned at Complete | `engine::topology::recover::tests::the_ledger_balances_at_complete` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.15s (`mutations/run-g5/g5-candidates-refs-kept-at-complete.log`) |
| 13 | `g5-run-lock-shared` | G5-13: a second process is not refused the run lock | `rundir::tests::a_second_process_is_refused_the_run_lock` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-run-lock-shared.log`) |
| 14 | `g5-drift-warning-dropped` | G5-14: a resume with an edited config no longer warns | `engine::topology::recover::tests::resume_rebuilds_runner_from_record_and_warns_on_config_drift` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.02s (`mutations/run-g5/g5-drift-warning-dropped.log`) |
| 15 | `g5-breach-ends-without-budget-exceeded` | G5-15: a ceiling breach admits the step instead of budget_exceeded | `engine::topology::recover::tests::over_budget_prefix_without_budget_exceeded_is_not_ending` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.04s (`mutations/run-g5/g5-breach-ends-without-budget-exceeded.log`) |
| 16 | `g5-event-written-kill-point-skipped` | G5-16: Event.Append no longer consults its Written kill point | `engine::topology::coverage::tests::every_kill_point_of_the_event_funnels_kills_the_child_under_the_production_adapter` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 3.19s (`mutations/run-g5/g5-event-written-kill-point-skipped.log`) |
| 17 | `g5-event-written-error-point-skipped` | G5-17: Event.Append no longer consults its Written error-return point | `engine::topology::coverage::tests::every_error_return_point_of_the_event_funnels_fires_under_the_production_adapter` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-event-written-error-point-skipped.log`) |
| 18 | `r11-third-resume-leftover-record-only` | R11-1: the fresh branch removes the record alone (PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION, expected to survive) | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | pass | passed: `rc=0`, ok. 1 passed; 0 failed; finished in 5.88s (`mutations/run-g5/r11-third-resume-leftover-record-only.log`) |
| 19 | `r11-staging-record-publish-swallowed` | R11-2: the staging record's publish error swallowed (PR10-PRIVATE-RECORD-BARRIERS-UNWITNESSED, expected to survive) | `rundir::tests::a_report_staging_directory_is_recorded_before_it_is_made` | pass | passed: `rc=0`, ok. 1 passed; 0 failed; finished in 0.00s (`mutations/run-g5/r11-staging-record-publish-swallowed.log`) |
| 20 | `r11-staging-record-stage-swallowed` | R11-3: the staging record's stage error swallowed (the same finding, expected to survive) | `rundir::tests::a_report_staging_directory_is_recorded_before_it_is_made` | pass | passed: `rc=0`, ok. 1 passed; 0 failed; finished in 0.02s (`mutations/run-g5/r11-staging-record-stage-swallowed.log`) |
| 21 | `g5-effect-before-barrier` | G5-18: a fold-derived effect (RunDir.RemoveMarker) before the stable-prefix barrier | `engine::topology::recover::tests::resume_establishes_stable_prefix_barrier_before_any_fold_derived_effect` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.02s (`mutations/run-g5/g5-effect-before-barrier.log`) |
| 22 | `g5-barrier-reread-mismatch-ignored-log` | G5-01b/02b/03b: the three barrier predicates, re-aimed at the event log's own three-cell test (the census test named above is a pure-data test of the container census's barrier abstraction) | `events::log::tests::an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-barrier-reread-mismatch-ignored-log.log`) |
| 23 | `g5-barrier-torn-tail-reappearance-ignored-log` | G5-01b/02b/03b: the three barrier predicates, re-aimed at the event log's own three-cell test (the census test named above is a pure-data test of the container census's barrier abstraction) | `events::log::tests::an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-barrier-torn-tail-reappearance-ignored-log.log`) |
| 24 | `g5-barrier-length-mismatch-ignored-log` | G5-01b/02b/03b: the three barrier predicates, re-aimed at the event log's own three-cell test (the census test named above is a pure-data test of the container census's barrier abstraction) | `events::log::tests::an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-barrier-length-mismatch-ignored-log.log`) |
| 25 | `g5-census-run-resumed-not-offered-dimension` | G5-19a: the census never offers an identical run_resumed (the resumes dimension) | `topology::census::tests::every_declared_dimension_is_reached_at_its_bound` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 16.19s (`mutations/run-g5/g5-census-run-resumed-not-offered-dimension.log`) |
| 26 | `g5-census-run-resumed-not-offered-arms` | G5-19b: the same mutation against the arm-coverage test | `topology::census::tests::every_plan_transition_arm_is_executed_by_the_census` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 16.41s (`mutations/run-g5/g5-census-run-resumed-not-offered-arms.log`) |
| 27 | `g5-census-question-not-offered-dimension` | G5-20a: the census never offers question_raised (the questions dimension) | `topology::census::tests::every_declared_dimension_is_reached_at_its_bound` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 15.79s (`mutations/run-g5/g5-census-question-not-offered-dimension.log`) |
| 28 | `g5-census-question-not-offered-arms` | G5-20b: the same mutation against the arm-coverage test | `topology::census::tests::every_plan_transition_arm_is_executed_by_the_census` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 16.09s (`mutations/run-g5/g5-census-question-not-offered-arms.log`) |
| 29 | `g5-id-unread-point-skipped` | G5-21: the commit-tree funnel no longer consults its IdUnread point | `workspace_manager::tests::a_kill_at_id_unread_leaves_a_gc_owned_object_nothing_adopts` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 0.03s (`mutations/run-g5/g5-id-unread-point-skipped.log`) |
| 30 | `g5-registry-entry-dropped` | G5-22: one entry dropped from the pinned registry document | `engine::topology::coverage::tests::the_sequential_registry_is_pinned` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-registry-entry-dropped.log`) |
| 31 | `g5-residue-frozen-n-changed` | G5-23: the frozen sampling N of the declarations changed | `engine::topology::coverage::tests::a_recovery_proven_entrys_n_is_held_to_the_declarations` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 0.00s (`mutations/run-g5/g5-residue-frozen-n-changed.log`) |
| 32 | `g5-retained-idle-not-closed-on-resume` | G5-24a: recovery step (e) closes no retained generation | `engine::topology::recover::tests::a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.08s (`mutations/run-g5/g5-retained-idle-not-closed-on-resume.log`) |
| 33 | `g5-retained-idle-not-closed-on-resume-settle` | G5-24b: the same mutation against the packet-named settle-level test | `engine::topology::settle::tests::retained_generation_closed_before_run_resumed` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 0.00s (`mutations/run-g5/g5-retained-idle-not-closed-on-resume-settle.log`) |
| 34 | `g5-fast-publication-other-sha-accepted` | G5-25: a fast merge_prepared naming another commit is accepted | `topology::census::tests::the_publication_relations_are_exercised_in_both_directions` | fail | passed (UNEXPECTED): `rc=0`, ok. 1 passed; 0 failed; finished in 16.61s (`mutations/run-g5/g5-fast-publication-other-sha-accepted.log`) |
| 35 | `g5-fast-publication-other-head-accepted` | G5-26: a fast merge_prepared expecting another head is accepted | `topology::census::tests::the_publication_relations_are_exercised_in_both_directions` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 16.51s (`mutations/run-g5/g5-fast-publication-other-head-accepted.log`) |
| 36 | `g5-finished-run-not-finalized-on-resume` | G5-27a: recovery step (b) neither finalizes nor refuses a finished run | `engine::topology::recover::tests::resume_finalizes_halted_then_refuses` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.04s (`mutations/run-g5/g5-finished-run-not-finalized-on-resume.log`) |
| 37 | `g5-finished-run-not-finalized-on-resume-late-answer` | G5-27b: the same mutation against the late-answer test | `engine::topology::recover::tests::late_answer_after_finalization_is_inert_and_reported_not_live` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.04s (`mutations/run-g5/g5-finished-run-not-finalized-on-resume-late-answer.log`) |
| 38 | `g5-report-freshness-always-true` | G5-28: a stale report is always fresh (never rewritten) | `engine::topology::recover::tests::finalized_report_names_runner_identity` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.04s (`mutations/run-g5/g5-report-freshness-always-true.log`) |
| 39 | `g5-run-is-over-guard-dropped` | G5-29: the finished-run guard dropped (a Complete or Halted run admits events again) | `topology::fold::tests::a_finished_run_is_continued_only_by_the_resume_its_outcome_allows` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-run-is-over-guard-dropped.log`) |
| 40 | `g5-run-resumed-runner-difference-ignored` | G5-30: run_resumed accepted with a different runner (the difference ignored) | `topology::census::tests::run_resumed_is_accepted_with_an_identical_runner_and_refused_with_any_different_field` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 14.35s (`mutations/run-g5/g5-run-resumed-runner-difference-ignored.log`) |
| 41 | `g5-id-unread-point-skipped-kill` | re-aimed: the IdUnread consult removed, against the candidate commit-tree kill test and the record-order test | `engine::topology::candidate::tests::kill_at_commit_tree_id_unread_point_leaves_gc_owned_object` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 1.34s (`mutations/run-g5/g5-id-unread-point-skipped-kill.log`) |
| 42 | `g5-id-unread-point-skipped-record` | re-aimed: the IdUnread consult removed, against the candidate commit-tree kill test and the record-order test | `workspace_manager::tests::a_kill_at_id_unread_aborts_before_the_id_is_recorded` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.23s (`mutations/run-g5/g5-id-unread-point-skipped-record.log`) |
| 43 | `g5-residue-frozen-n-changed-pin` | re-aimed: the declarations frozen N changed, against the artifact pin | `effects::tests::the_checked_in_residue_class_record_is_what_the_enums_generate` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.00s (`mutations/run-g5/g5-residue-frozen-n-changed-pin.log`) |
| 44 | `r11-third-resume-leftover-record-only-vs-g5-witness` | the round-11 survivor against the gate's temporary witness (valid only while tmp-tests are appended) | `engine::topology::recover::tests::g5_gate_next_resume_reclaims_a_recorded_leftover_on_the_fresh_branch` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.06s (`mutations/run-g5/r11-third-resume-leftover-record-only-vs-g5-witness.log`) |
| 45 | `g5-answered-question-stays-open` | G5-31 (G4's M08 re-applied): an answered question stays open in the fold, so a second answer is not refused as already answered | `engine::topology::recover::tests::a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.07s (`mutations/run-g5/g5-answered-question-stays-open.log`) |
| 46 | `g5-answered-question-stays-open-vs-g5-witness` | the same mutation against the gate's temporary witness (valid only while tmp-tests-2 are appended) | `engine::topology::recover::tests::g5_gate_answers_on_disk_are_ingested_only_by_the_resume_and_only_for_still_open_questions` | fail | killed: `rc=101`, FAILED. 0 passed; 1 failed; finished in 0.10s (`mutations/run-g5/g5-answered-question-stays-open-vs-g5-witness.log`) |

### 3.3 The eleven survivals, each explained

| Recipe | Why the named test passed | What was done |
|---|---|---|
| `g5-barrier-reread-mismatch-ignored`, `-torn-tail-reappearance-ignored`, `-length-mismatch-ignored` | the test named, `runner::container::census::tests::the_stable_prefix_barrier_refuses_each_of_its_four_predicates_independently`, is a pure-data test of the container census's barrier abstraction (`PrefixBytes`, `PrefixReread`, `PrefixSync`, `PrefixReplay`) and never calls `establish_stable_prefix`; the mutations change nothing it reads. A filter chosen by name, exactly the class the brief warns of | re-aimed at the event log's own three-cell test `events::log::tests::an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle` ("a torn tail reappeared", "the length changed", "a byte changed"): **all three killed** (`g5-barrier-*-ignored-log`). The predicates are guarded |
| `r11-third-resume-leftover-record-only` | the ST-18 matrix `kill_after_report_before_each_cleanup_step` passes with the fresh branch's reclaim replaced by a removal of the record alone — exactly what `PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION` reasoned and never executed: the third-resume block asserts on the first leftover (already gone) and on an oracle that lists the directory only through the record | **the finding is confirmed by execution.** The gate's temporary witness (§3.4) asserts the planted leftover's file, its directory and the record gone after the fresh branch: it passes unmutated and **fails under this recipe** (`r11-third-resume-leftover-record-only-vs-g5-witness`, `rc=101`), so the missing assertion is a guard gap of the committed test, not a behaviour gap. It bears on clause 7 and clause 9 (§13) |
| `r11-staging-record-publish-swallowed`, `r11-staging-record-stage-swallowed` | `rundir::tests::a_report_staging_directory_is_recorded_before_it_is_made` exercises successful barriers only and reads ledger entries the helpers write whether or not the barrier held — what `PR10-PRIVATE-RECORD-BARRIERS-UNWITNESSED` reasoned | **the finding is confirmed by execution**: both swallows leave the suite's only guard green. The production code propagates both errors (read at `src/rundir.rs`, `begin_report_staging`); the defect is the missing rule-2 witness (§9.2) |
| `g5-census-question-not-offered-dimension` | `every_declared_dimension_is_reached_at_its_bound` still reaches the `questions` bound (2) without the `question_raised` offer, because the generator's rejection class registers repairs whose admission opens a question (`merge_rejected` with a `HumanRequired`/`HumanBinding` spawn), so open questions reach the bound by another route | the same mutation against the arm-coverage test kills it (`g5-census-question-not-offered-arms`): the `question_raised` arm is guarded by arm coverage, not by the dimension. No gap |
| `g5-id-unread-point-skipped` | `workspace_manager::tests::a_kill_at_id_unread_leaves_a_gc_owned_object_nothing_adopts` is a classifier test that spawns no child and consults no hook (0.03s); the removed consult is in `commit_tree` and is not on its path | re-aimed at the two tests that kill or record at the point: `engine::topology::candidate::tests::kill_at_commit_tree_id_unread_point_leaves_gc_owned_object` and `workspace_manager::tests::a_kill_at_id_unread_aborts_before_the_id_is_recorded`: **both killed** (`g5-id-unread-point-skipped-kill`, `-record`) |
| `g5-residue-frozen-n-changed` | `a_recovery_proven_entrys_n_is_held_to_the_declarations` constructs its own entry citing 1 against a site's declaration; the first site's N moved from 8 to 7 and the test's own case still holds | re-aimed at the declarations' pin `effects::tests::the_checked_in_residue_class_record_is_what_the_enums_generate` ("is stale; regenerate"): **killed** (`g5-residue-frozen-n-changed-pin`) |
| `g5-retained-idle-not-closed-on-resume-settle` | `engine::topology::settle::tests::retained_generation_closed_before_run_resumed` — the packet's slug — exercises `close_retained` at the settle level and never runs recovery step (e), where the mutation is | the loop-level test `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` kills the same mutation (`g5-retained-idle-not-closed-on-resume`). The packet-named test does not guard recovery's call; the recover test does — the same observation G4 made of this slug |
| `g5-fast-publication-other-sha-accepted` | `MergePrepared::self_consistency()` (`src/topology/events.rs`, the wire type's own check) already refuses `proposed_sha != candidate_sha` before the fold's `check_merge_prepared` runs, so removing the fold's check changes no behaviour and the census label `merge_prepared/fast/other-proposed/aleph/g0` is still refused | behaviour-preserving mutation: the fold's check is redundant defence. The head relation has no such duplicate and its recipe is killed (`g5-fast-publication-other-head-accepted`). The relation "proposed_sha == candidate commit" is established by the census test in both directions and guarded by the wire check; no gap |

### 3.4 The temporary witnesses, in four phases

Eight tests and one probe were appended to test modules for this run and removed before each report
head was built (sources under `tmp-tests/` for the first head's four, `tmp-tests-2/` for round 1's
three, `tmp-tests-3/` for round 2's corrected power-loss witness and `tmp-tests-4/` for round 3's, with
its driver `phase4.py`; `tmp-tests/phase.log`, `tmp-tests-2/phase.log`, `tmp-tests-3/phase.log` and
`tmp-tests-4/phase.log` record the backups' sha256 equal to `git show HEAD:<path>` before and after, and
`git status --porcelain` empty after each restore). Every one ran alone, `--nocapture`, on the
private base. **None is committed; a witness this gate wrote establishes a behaviour at this range
and does not close the committed-witness gap it stands in for.**

| Witness | What it does | Expected | Observed |
|---|---|---|---|
| `g5_gate_fresh_branch_reclaims_the_recorded_leftover_by_its_record` (rundir; phase 1) | plants a dead writer's staging directory through the writer's own helper, plants a staging-shaped directory by hand beside it, calls `sync_report_dir` — the fresh branch of terminal finalization — and reads what is left | pass | **pass**: `G5FRESH leftover_gone=true staging_gone=true record_gone=true passed_over=[the hand-planted directory]`, nothing of the protocol left staged (`tmp-tests/run-g5_gate_fresh_branch_reclaims_the_recorded_leftover_by_its_record.log`) |
| `g5_gate_next_resume_reclaims_a_recorded_leftover_on_the_fresh_branch` (recover; phase 1) | a finished run at Halted and at Complete, resumed once (finalize then refuse), a recorded leftover planted, resumed again; asserts the planted file, its directory and the record gone — the assertion `PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION` says the matrix lacks | pass | **pass** at both outcomes: `G5THIRD halted: leftover_gone=true staging_gone=true record_gone=true`, the same for `complete` (`tmp-tests/run-g5_gate_next_resume_reclaims_a_recorded_leftover_on_the_fresh_branch.log`) |
| the same witness under `r11-third-resume-leftover-record-only` | the round-11 mutation the matrix admits, against this witness | fail | **killed**, `rc=101`, at *"halted: G5 — the staged file inside the recorded directory is gone after the fresh branch"* (`mutations/run-g5/r11-third-resume-leftover-record-only-vs-g5-witness.log`) |
| **round 2's corrected witness**, `g5_gate_a_reclaimed_report_stage_remains_reclaimable_after_power_loss` (rundir; phase 3, `tmp-tests-3/rundir-tests-append.rs`), at `caf6bed0` unchanged — **superseded in round 3** (the rows after the next) | the same sequence, with the loss simulated from the durability ledger the funnel wrote: A is restored only if no `SyncedDirectory` of `public` was recorded before record(B)'s `DirectoryCreated`, B is discarded only if none was recorded after it, and record(B)'s durability (`SyncedDirectory` of `private` after its `Renamed`) is asserted as the finding's premise. The first version recreated A unconditionally, so it modelled a loss the fix prevents and could not pass under the fix — the regression and record lenses executed that | **fail**: the defect | **fails**, `cargo exit=101`: `G5POWERLOSS ledger: B created at 4, public synced at []; A's deletion durable=false B durable=false record(B) durable=true`, then `after the retry: A exists=true passed_over=[A] record exists=false`, panicking at *"G5: the old directory A is not left standing after the loss and the retry"* (`tmp-tests-3/run-unfixed.log`) |
| round 2's corrected witness under round 2's **prescribed fix**, applied temporarily (`tmp-tests-3/prescribed-fix.diff`, the record lens's reproduction diff's `src/rundir.rs` hunks: the ledger passed to `reclaim_report_staging` from both callers, `sync_dir(public, ledger)` after the recorded directory's removal and before the record's, and `sync_dir` of the report's parent before `publish_report` removes the record; `prescribed-fix-applied.diff` is `git diff` of the applied state) | the same witness, unchanged, against the fixed tree; then both files restored from their `cp` backups | **pass**: the fix | **passes**, `cargo exit=0`: `public synced at [0]; A's deletion durable=true B durable=false record(B) durable=true`, then `after the retry: A exists=false passed_over=[] record exists=false` (`tmp-tests-3/run-fixed.log`); restored: `src/rundir.rs` and `src/rundir/tests.rs` sha256 equal to `git show HEAD:…` before and after, `git status --porcelain` empty (`tmp-tests-3/phase.log`, 22:36:17Z–22:37:21Z on `00616451`) |
| **the round-3 witness** (R1): the same test with a loss model that requires a barrier **after the observed deletion** (rundir; phase 4, `tmp-tests-4/rundir-tests-append.rs`), at `caf6bed0` unchanged | A's deletion survives the loss only on a `SyncedDirectory` record of `public` whose observed entry is A with `present: false`, which only a barrier taken after A was gone writes, since the barrier reads the recorded name immediately before its sync; B is discarded unless a barrier of `public` follows its creation; record(B)'s durability asserted as the finding's premise. Round 2's model took any barrier of `public` before B's creation as proof, and round 3's regression lens executed a barrier moved before the deletion that it passed | **fail**: the defect | **fails**, `cargo exit=101`: `public barriers (index, A observed present) []`, `A's deletion durable=false`, then `A exists=true`, one directory passed over, panicking at *"G5: the old directory A is not left standing after the loss and the retry"* (`tmp-tests-4/run-unfixed.log`) |
| the round-3 witness under **round 3's prescription** (R2), applied temporarily (`tmp-tests-4/prescribed-fix.diff`: round 2's hunks with the reclaim's barrier taken through `sync_dir_observing(public, &recorded, ledger)`, which reads the recorded name's presence immediately before `util::fsync_dir` and records it on the `SyncedDirectory` entry, after the match on the recorded name and before the record's removal) | the same witness against the fixed tree | **pass**: the fix | **passes**, `cargo exit=0`: `public barriers [(0, Some(false))]`, `A's deletion durable=true`, then `A exists=false passed_over=[] record exists=false` (`tmp-tests-4/run-prescribed.log`) |
| **the negative control** (R3): round 3's prescription with the reclaim's barrier moved before `remove_dir_all` (`tmp-tests-4/barrier-before-deletion-vs-prescribed.diff`) | the round-3 witness against a barrier that no longer follows the deletion | **fail** | **fails**, `cargo exit=101`: `public barriers [(0, Some(true))]` — the barrier observed A still present — `A's deletion durable=false`, `A exists=true`, one directory passed over (`tmp-tests-4/run-barrier-before-deletion.log`) |
| round 2's witness under the same misplaced barrier (R4) | the regression lens's false green, reproduced | **pass**, the defect missed | **passes**, `cargo exit=0`: `public synced at [0]; A's deletion durable=true` although the barrier preceded the deletion (`tmp-tests-4/run-barrier-before-deletion-round2-model.log`) |
| the round-3 witness under round 2's prescription (R5; `tmp-tests-4/round2-prescription.diff`: a plain `sync_dir(public, ledger)` after the removal) | a barrier in the right place that records no observed entry | **fail**: the model credits no barrier that does not say what it observed | **fails**, `cargo exit=101`: `public barriers [(0, None)]`, `A's deletion durable=false` (`tmp-tests-4/run-round2-prescription.log`) — so round 3's prescription carries the observing barrier. All five runs 23:41:21Z–23:43:52Z at `3649147f`; `src/rundir.rs` and `src/rundir/tests.rs` restored from their `cp` backups with sha256 equal to `git show HEAD:…` and `git status --porcelain` empty (`tmp-tests-4/phase.log`) |
| `g5_gate_rewriting_report_keeps_its_posix_acl` (rundir, Unix; phase 1) | `PR10-LEGACY-REPORT-REWRITE-DROPS-ITS-POSIX-ACL`'s recipe as written: `setfacl -m u:65534:r--,g::---,m::r--,o::---` on `report.json`, `getfacl -cpn` before and after `write_report` | **fail** where ACLs exist | **fails as predicted** on ext4: before `user::rw-/user:65534:r--/group::---/mask::r--/other::---`, after `user::rw-/group::r--/other::---` — the named user is dropped and the mask has become real group bits (`tmp-tests/run-g5_gate_rewriting_report_keeps_its_posix_acl.log`). The finding is real at `caf6bed0` |
| the Q1 probe (`tmp-tests/q1-probe.py`; phase 1) | an `eprintln!` inside `assert_live_equals_replay` and inside the projection test's per-prefix loop | — | §6 |
| `g5_gate_a_resume_after_finalization_pruned_the_pins_continues_the_queued_candidate` (recover; phase 2) | group 3's "pruned non-authoritative refs recovered": the BudgetExceeded ledger fixture (alpha closed for retry, beta queued with its candidates ref and candidate-prepared pin) driven to the stop; finalization's step (iv) prunes the pin; the resume with a raised ceiling | pass | **pass**: `G5PINS after the stop: ["BudgetExceeded", "finished(BudgetExceeded, 0, true, true)"]`, `prepared_pin=None candidates_ref=Some("b8f98abd…")`, then `G5PINS after the resume: ["integrated(k1, s0)", "settled(k0, false)", "finished(Parked, 0, true, true)", …]` — the queued candidate published without its pruned pin, the integration ref at its commit, replay twice equal (`tmp-tests-2/run-g5_gate_a_resume_after_finalization_pruned_the_pins_continues_the_queued_candidate.log`) |
| `g5_gate_answers_on_disk_are_ingested_only_by_the_resume_and_only_for_still_open_questions` (recover; phase 2) | group 6's clause at the loop: an over-limit repair's question open, a `budget_exceeded` appended in epoch 0, the answer file staged and published; the resume (epoch 1) ingests it; a further resume with the same file still on disk and the question closed | pass | **pass**: `G5ANSWERS resume 1: ["answered(k2, declined false)", "settled(k2, true)"]`, `question_answered=1 epoch=Some(1) file_retained=true`; `G5ANSWERS resume 2: ["integrated(k2, s2)", "finished(Complete, 0, true, true)"]`, `question_answered=1 file_retained=true` (`tmp-tests-2/run-g5_gate_answers_on_disk_are_ingested_only_by_the_resume_and_only_for_still_open_questions.log`). The stopped epoch's own refusal is the fold's, executed by the committed `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch` (group 6) and killed by `g5-answer-accepted-under-budget-stop` |
| the same witness, and the committed `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`, under `g5-answered-question-stays-open` (G4's M08: the fold no longer removes an answered question from the open set) | the refusal of a second answer, and the loop's "only still-open" polling, with the fold's open set wrong | fail | **both killed**, `rc=101` (`mutations/run-g5/g5-answered-question-stays-open.log`, `…-vs-g5-witness.log`) |
| `g5_gate_killed_publication_prefixes_converge_under_the_next_census` (startup; phase 2) | ST-19 on the killed fixtures themselves: the create suite's `create_kill_child` spawned with its own env protocol at each of the 13 prefixes (`p0`, `p1staged`, `p1`, `p2`, `p3a`, `p3astaged`, `p3b`, `p4`, `p5`, `p5b`, `p6`, `p7`, `p8`), then `census_run_dirs` over the same repository and private root | pass | **pass**, 13 of 13 converging to ST-19's disposition, printed per prefix (`tmp-tests-2/run-g5_gate_killed_publication_prefixes_converge_under_the_next_census.log`): P0–P2 reclaimed public-only (bare; staged marker only; private half gone), both halves absent after; P3a and P3aStaged retained, nothing deleted ("its private half carries no owner record"); P3b, P4, P5 "reclaimed the private half under the ownership proof, then the public directory with the marker last", both halves absent after; P5b retained ("carries a commit record, so the run may have started"); P6 "a committed run: removed its stale `.creating` marker"; P7 and P8 committed, nothing to do, untouched |

### 3.5 What §3 establishes

Every "test X guards Y" sentence in §4–§8 names a recipe from the two tables. The eleven survivals
leave **no detection gap the gate could not close by re-aiming**, with two exceptions that are the
round-11 findings themselves: the ST-18 matrix's third resume does not observe the fresh branch's
directory reclamation (a witness this gate wrote does, and it is uncommitted), and the two
private-record barriers of `begin_report_staging` have no swallow witness. Both are judged in §13.
The witnesses of phase 2 execute three clauses the first head had reasoned (§8, groups 3, 6 and 7).

## 4. Required artifact #2 — the bounded census artifact

**The file:** `census-run2/census-summary.json`, written by
`topology::census::tests::the_census_summary_names_every_fault_row_and_serializes` during export run 2;
byte-identical to export run 1's (`census/census-summary.json`) and to the guest's
(`guest/exports/census-summary.json`), checked with a python equality on this run. It lists the
family's two members, each with its bounds, ceiling, counts, closure flag, outcomes, resume actions,
fault rows and the dimensions it reaches, and the union's reach.

**The bounds the tree explores** (`CensusBounds::default()`): originals 3, repairs 2, generations per
task 2, attempts per generation 2, sequences 4, lineages 2, defers 1, questions 2, review passes 1,
resumes 2; `max_trace` 48; the prefix's state ceiling 20,000; the prefix explores the fan-out plan
(`MAIN_SHAPE`). **They are the packet's on eight dimensions and the tree's reading of the packet on
two**, which the first head of this report did not say and the verify lens found (F3):

- **The packet's bounds line, verbatim** (`decisions.bounded_census.bounds`, unchanged by the
  2026-09-13 amendment; extracted from the packet with python into `spec/packet-bounds-line.log`, which
  names the packet, its sha256 and the JSON path, and the whole block into `spec/packet-bounded-census.json`
  — `spec/packet-check.log` holds the packet's hash and the extracts' checks, not this line, as the
  record lens found): *"originals <= 3, repairs <= 2
  (lineages <= 2), generations per task <= 2, attempts per generation <= 2, integration sequences
  <= 4, verification defers per candidate <= max_defers (fixture value 2), open questions <= 2,
  review passes <= 1, resumes <= 2, plan shapes: chain, fan-out, diamond"*.
- **Verification defers.** The packet's bound is `max_defers` with the fixture value 2. The tree's
  bound is 1, asserted in `the_census_runs_at_the_packets_bounds_and_says_where_it_stopped` as
  `bounds.defers + 1 == run_started().limits.max_defers` ("verification defers per candidate are
  bounded by the fixture's `max_defers`, whose last outage parks rather than defers") and
  `assert_eq!(bounds.defers, 1)`. The reading: the fold's `check_defer_allowance`
  (`src/topology/fold/check_integration.rs`) refuses a deferral at `defers >= max` with "this run
  allows {max}, and the {max}th outage parks rather than defers", so the deferrals a candidate can
  carry are `max_defers - 1`, and the union reaches 1 by equality (the summary's `union_reaches.defers`
  is 1 against `bounds.defers` 1). PR10's record recorded this reading in R16 ("its bound is what the
  fixture's `max_defers` of two admits … asserted as `max_defers - 1` against the fixture's limit, not
  typed"). Against the unamended text, which reads `<= max_defers`, the bound reached by equality is
  2, and the union reaches 1: **not established on that reading**.
- **Plan shapes.** The packet names chain, fan-out, diamond. The tree's `PlanShape` is `Chain`,
  `FanOut`, `Join` — `Join` is "a task after two independent ones", which the record's R16 calls "the
  diamond's join with three originals"; a four-node diamond does not fit the packet's own `originals
  <= 3`. The family artifact holds the fan-out prefix and one seeded member; the chain and the join
  are explored by `every_plan_shape_is_explored` to a ceiling of 3,000 states each, truncated, with
  every outcome reached and no `FoldError`, **and those two explorations are not members of the
  family artifact**. Against the unamended text: the diamond is **not explored** and the shapes are
  not in the family's union.
- **What a packet correction would have to say** for this clause to be established as the tree
  stands, in one sentence: *"verification defers per candidate <= max_defers − 1 (the max_defers-th
  outage parks; fixture value 2, so 1), and plan shapes: chain, fan-out, and the three-node join (the
  diamond's join under originals <= 3), the chain and the join explored as truncated prefixes beside
  the family"*. That decision is the owner's; this report does not adjudicate it and grades the clause
  as the unchanged text supports (§13, clause 1).

| Member | Generator | Ceiling | States | Transitions | Accepted / refused / truncated offers | Closed? | Reaches at bound |
|---|---|---|---|---|---|---|---|
| `prefix` | full | 20,000 | **20,000** | 5,194,414 | 97,608 / 5,067,348 / 29,458 | **no — `truncated: true`, reported** | all ten of the tree's dimensions |
| `seeded: sequences, repairs, lineages` | restricted, seed of 15 events | 5,000 | **25** | 1,405 | 24 / 1,381 / 0 | **yes — `closed: true`**, frontier exhausted under its ceiling, no state at the trace ceiling | sequences, repairs, lineages |

`union_reaches` equals the tree's bounds line for every dimension and `every_bound_reached_over_the_union`
is `true`. The prefix's outcomes: budget exceeded 4,182, complete 793, halted 1,809, parked 913, not
ending 12,303; 21 distinct resume actions, `finalize then refuse` for every Complete (298) and Halted
(611) state; every one of the 19 fault rows inside the fold reached from the prefix with
`every_reachable_state_classifies_as_tabled: true`, and the two outside the fold (`T-APPEND`,
`T-CONTAINER`) carry `outside_the_fold: true` with 0 reachable states — said so rather than counted
as covered; `classification_equal_live_and_on_replay: true`. The seeded member: complete 2, parked 2,
not ending 21; 7 actions; 11 fault rows reached; classification equal.

**Ran, not reasoned.** Group 7's invocation (§8; `groups/group7-seam-tests.log`, 37 passed) holds
**19** `topology::census::tests` tests (the first head said 20); the twentieth named in the list
below, `a_state_with_admissible_work_and_no_budget_exceeded_classifies_not_ending`, ran in **group 4**
(`groups/group4-st17-closure.log`), not group 7, which round 1's head got wrong (round 2's record
lens); every one passing at `caf6bed0`: the closing
test `the_prefix_reports_its_truncation_and_every_seeded_census_closes` (the prefix at its ceiling and
truncated; each seeded member closed, under its ceiling, off the trace ceiling, reaching what it
names; **the set of dimensions the prefix misses asserted empty**), `every_declared_dimension_is_reached_at_its_bound`
(equality over the union, and the prefix alone at all ten), `every_plan_transition_arm_is_executed_by_the_census`
(the arm enumeration read from the production dispatch's source), `the_derived_outcome_is_total_over_every_explored_state`
(one evaluation per state, no `FoldError`, no disagreement with a fresh evaluation, every state exactly
one of NotEnding/Ending — over every family member), `a_state_with_admissible_work_and_no_budget_exceeded_classifies_not_ending`
(the pre-`budget_exceeded` counterexample), `every_explored_state_classifies_and_the_classification_is_the_same_live_and_on_replay`,
`every_fault_rows_durable_prefix_is_a_reachable_state_classified_as_its_resume_action`,
`run_resumed_is_accepted_with_an_identical_runner_and_refused_with_any_different_field`,
`the_publication_relations_are_exercised_in_both_directions` (`merge_prepared/fast/{match,moved-head,other-proposed,with-pin,with-verification}`,
`stale_clean/{match,mismatch}`, `already_present/{match,mismatch}`), `every_deferred_state_has_a_legal_next_transition`,
`replaying_every_explored_trace_reaches_the_state_it_was_explored_at`, `the_census_transition_table_is_reproducible_from_the_folds_alone`
(over every member), `no_offer_is_unmapped_and_every_class_is_offered_everywhere`, `every_plan_shape_is_explored`,
`the_census_runs_at_the_packets_bounds_and_says_where_it_stopped`, `a_census_that_hits_its_ceiling_says_so`,
`the_census_reaches_every_outcome_and_says_what_it_did_not_reach`, `a_rejection_and_its_repairs_dispatch_are_reachable_prefixes_classified_as_tabled`,
`a_retained_generation_and_its_retry_are_reachable_prefixes_classified_as_tabled`, `the_census_summary_names_every_fault_row_and_serializes`.

**Detection, counted by the module of the killed test.** Nineteen recipes kill a test of
`topology::census::tests` at this range: `census-bound-not-reached`, `census-trace-ceiling-silent`,
`census-defers-not-reached`, `census-lineage-collapsed`, `census-budget-class-gated`,
`census-truncations-counted-refused`, `census-truncated-labels-refused`, `census-seeded-does-not-close`
(the seeded ceiling lowered to 10 states: "a seeded census closes, and this one reports truncation"),
`census-dimension-dropped-from-union-check`, `census-seeded-row-misattributed`, `classifier-drops-beta`,
`matches-row-unconditional`, `ledger-verification-defers-from-queue` (R14's consumption test, which
lives in that module), `g5-derived-outcome-parked-as-fold-error` (totality: a parked shape made to
derive `FoldError` is caught), `g5-census-run-resumed-not-offered-dimension` and `-arms`,
`g5-census-question-not-offered-arms`, `g5-run-resumed-runner-difference-ignored`,
`g5-fast-publication-other-head-accepted` (`mutations/summary-*.tsv`, the `test` column). Two more
kill the fold's end guards through other suites: `g5-run-finished-outcome-unchecked` kills
`engine::topology::recover::tests::replayed_conflicting_outcome_refused` and
`g5-answer-accepted-under-budget-stop` kills `topology::fold::tests::an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch`.
The first head counted "nineteen recipes kill census tests" from a list that included the recover
recipe and omitted the R14 one; both are named now with their modules.

**What is claimed, in the amended packet's words and no more.** The family completes over the tree's
bounds: the prefix explores to its stated ceiling and reports truncation; the one seeded census closes;
the union reaches every one of the tree's ten bounds by equality — and so does the prefix alone, which
is more than the amendment requires and is measured. Every coverage assertion runs over the union.
**Closure of the space under the bounds is claimed of nothing**: 20,000 states is a breadth-first
prefix of a space the record's round-2 measurement and the orchestrator's research put at more than
50 million abstract states without closing (`~/orch-pr10/answers/repair_279_r2-1.md`, not re-measured
here). **Unbounded closure is not claimed. No effect-phase claim is made by the census**;
effect-phase distinctions are the registry's (§5). Whether the tree's bounds are the packet's is the
two-dimension question above, graded in §13.

## 5. Required artifact #3 — the effect-site inventory, the fault-injection registry and the residue-class evidence record

### 5.1 The inventory and its lists (`inventory/`)

| File | What it is | At `caf6bed0` |
|---|---|---|
| `inventory/site-enums-export.txt` | the site-enums export: every site of the sequential inventory with every registry coordinate, derived from the pinned document | **68 distinct sites** — RunDir 14, Worktree 11, Ref 8, Container 8, Object 6, Lock 6, Event 5, Snapshot 4, Answer 3, Process 2, Report 1 — **180 entries**: 168 `executed` (observed), 9 `recovery_proven`, 3 `not_executed` (the fast no-execution record) |
| `inventory/sequential-registry.json` | the pinned fault-injection registry (`effects/sequential-registry.json`; the tree id of `effects/` is `872065345cc0d7b6593214dec15521a4af73f776`) | pinned by `the_sequential_registry_is_pinned`: ok on Linux and on the guest (§5.2); `g5-registry-entry-dropped` (one entry removed) kills the pin |
| `inventory/per-entry-recovery-audit.md` | **this head's audit of every observed entry** against the test bodies on ST-07's three obligations: which committed test constructs the coordinate's exact durable prefix, performs its tabled action and asserts replay twice equal, and the fix table for every coordinate that falls short; round 2's and round 1's audits kept as `-round2.md` and `-round1.md` (both withdrawn); what each of the 112 cited test bodies constructs, performs and asserts in `per-entry-recovery-audit-round3-test-facts.md`; the hand grades and the generator in `round3-generator/` | 168 entries: **F 4, N 24, R 86, O 54** (§5.3) |
| `inventory/per-entry-evidence-tests.txt`, `inventory/evidence-tests-keyword-profile.txt`, `inventory/armings-scan.txt` | the 29 distinct tests the registry cites, grouped with their entries; a keyword profile of each cited test's body; a multi-line scan of every test that arms a fault and the sites it names — the raw material the audit was read from | — |
| `inventory/orders-per-site.txt` | the `order` values per site in the pinned registry | 68 sites; every executed entry of a site carries one order; the three sites with two values (`Worktree.AddStaging`, `Ref.PinPrepared`, `Object.ProposalCherryPick`) carry the fast no-execution record's `None` beside it (§9.3) |
| `inventory/clippy.toml` | the disallowed lists the clippy leg is handed: `disallowed-methods`, `disallowed-types`, `disallowed-macros` (104 `path =` entries; blob `c1662cc8…`) | `every_effectful_wrapper_is_on_the_disallowed_list` and the other effects censuses: 171 `effects::` tests passed in one invocation (`groups/group8-effects-censuses-q5.log`) |
| `inventory/allowlist.toml`, `inventory/wrappers.toml`, `inventory/funnel-modules.json` | the frozen legacy allowlist, the wrapper classification (effectful / effect-free / funnel rows, and the libc list) and the funnel-module record | held by `every_externally_reachable_fn_of_a_legacy_or_shared_module_is_classified`, `every_allow_of_a_governed_lint_is_module_level_and_in_the_allowlist`, `the_three_blunt_governed_lints_are_used_by_nobody`, `every_libc_item_the_tree_names_is_classified_and_the_effects_are_denied` — all in the 171 |
| `inventory/residue-classes.json`, `inventory/residue-synthetic.json` | the residue declarations (elements, classes, frozen N) and the synthetic half | pinned by `the_checked_in_residue_class_record_is_what_the_enums_generate` (`g5-residue-frozen-n-changed-pin` kills it) and `every_registered_residue_element_is_constructed_and_recovers` (`residue-synthetic-pin` kills it) |
| `inventory/event-and-report-entries.txt` | the Event, Report, `RunDir.WriteReport`, Process and `IdUnread` entries listed with their executed-evidence tests | §5.3 |

**The dependency review, stated as what exists.** No test at this range reviews the locked
dependencies' effectful APIs. The effects censuses run `cargo metadata --no-deps` to find this
package's source roots and `extern_dependencies` (`src/effects/tests.rs`) hands the compiled `.rlib`s
beside the test binary to the denial fixtures, so that a fixture naming a dependency compiles; that
is not a review of what those crates can do. The review the standards name (`docs/internals/effects.md`
§4, standards §15: "a new dependency performing filesystem, process, lock or container effects has
its API added to the denylist or is confined to a funnel module") is a change-time obligation on the
change that adds a dependency. What this gate can establish is the range's dependency delta:
`Cargo.lock` is byte-identical between the G4 range `7e0110a1` and `caf6bed0` (`git diff --stat` empty)
and `Cargo.toml`'s one change is a package `exclude` (§0), so **no dependency entered the range and
there is nothing for the review to review**; the first head's sentence that the censuses "read
`Cargo.lock`" was false and is withdrawn (the record lens, P1).

### 5.2 The bijection, executed on both hosts

The merge check `st07_the_sequential_range_is_a_bijection_over_the_exported_observations` (ignored;
reads the observation export a full suite wrote) was run over export run 2 on Linux and over the guest's
own export on Windows — the latter **a merge check PR10 did not run** (its record's §11: the Windows-only
points were "witnessed by the guest's run of the suite, not by a merge check on the guest").

| Host | Records | Funnel executions | Inventory | Entries | Witnessed claims | Failures | Log |
|---|---|---|---|---|---|---|---|
| `unix` (Linux, export run 2) | 334 | 328 | 68 | 180 | 163 | **0** | `st07/st07-merge-check.log`, `st07/st07-summary.json` |
| `windows` (the guest, its export) | 330 | 324 | 68 | 180 | **164** | **0** | `guest/guest-st07-merge-check.log`, `guest/exports/st07-summary.json` |

The one more claim on Windows is the Windows-only `the_ambient_job_join_error_return_point_fires_under_the_process_funnel`
(`Process.Spawn`'s `ambient_job_joined` error-return point); the `fast_sequences` of both summaries
are the two the no-execution record names. The pin test passed on both hosts (`st07/st07-merge-check.log`,
`guest/guest-st07-pin.log`). The observation export's 335 files on Linux and 331 on the guest include
the sequential sampler's histogram beside the records, which the loader passes over by name
(`observations-loader-reads-the-histogram` kills that reading). **What the bijection establishes is
observation**: that every site, phase and parent-side point was executed by the test its entry names,
in the mode the entry names. It does not establish, per entry, that the durable prefix at that
coordinate was constructed and the tabled recovery driven; that is §5.3.

### 5.3 The per-entry recovery audit, the Event points, `OpenLog`'s `SyncPrefix`, the barrier's refusal cases, `IdUnread`, and the two-crash record

**The audit** (`inventory/per-entry-recovery-audit.md`, round 3; round 2's strict audit is kept as
`inventory/per-entry-recovery-audit-round2.md` and round 1's as `-round1.md`, both withdrawn). ST-07
requires of every entry that "the exact durable prefix and residue are constructed (by kill or
synthetically), resume performs the tabled action, replay twice equal". The registry's
`evidence.executed.test` names the test that *observed* the coordinate. Round 3 grades each of the 168
observed entries on those three obligations separately, read from committed test bodies at `caf6bed0`:
every test round 2 cited was opened again (112 bodies; what each constructs, performs and asserts is
`inventory/per-entry-recovery-audit-round3-test-facts.md`), and the grade of every row is written by
hand (`inventory/round3-generator/grades_a.py`, `grades_b.py`, `grades_c.py`) into a generator
(`gen_audit.py`) that refuses a citation unless the name resolves to exactly one `#[test]` `fn` at the
cited line of `HEAD`. **Round 2's audit is withdrawn**: it credited F to tests that classify or return a
plan without performing the recovery — `RunDir.WriteQuestionPayload`/before, whose test calls
`rematerialize_question`; `Ref.PinCandidatePrepared`/before, whose test asserts
`recovery_for(..).settles_interrupted`; `Event.AppendFirst`/written/kill, whose test calls `husk_report`;
`Event.OpenLog`/create/kill, whose test never reopens the created log (round 3's record lens broke that
reopen and the cited tests stayed green) — and it let a recovery that reads the disk stand for the
replay clause (118 rows `disk` against five `twice`, one of which replays once), as round 3's lenses
found. The obligations as applied (the audit's header states them in full):

- **(i) constructed** — before the recovery, the fixture holds the durable effects of every site up to
  and including the coordinate and none after it, with the registry's residue: by a real kill (K), an
  injected error return whose command ends with that state (E; the ST-18 matrix asserts the post-fault
  state per cell with `assert_finalization_order`), a plant (P), or at an adjacent coordinate with
  nothing durable between the two, read in the funnel code (=).
- **(ii) the tabled action performed** — the component a later process runs (a resume, the census, the
  next open and its barrier, or the recovery function the resume calls) performs the action and the test
  asserts its effect; the creating, launching or releasing command's own error path counts where it ends
  in the tabled disposition. A classification, a plan, or the test finishing the effect through the
  site's own funnel does not.
- **(iii) replay twice equal** — after the action the test replays the durable log twice and asserts the
  replays equal (`Run::replay_twice_equal`, `live_vs_replay`, the once/twice pair); vacuous where the
  state holds no committed schema-4 event and the recovery commits none (a creation husk).

| Grade | Entries | What they are |
|---|---|---|
| **F** — all three, replay twice asserted | **4** | `Worktree.Verify` both phases (an old log's dispatch resumed at the base it recorded, replay twice equal); `Ref.CompareAndSwapIntegration` both phases (the two-crash proof, replay twice equal) |
| **N** — (i) and (ii); nothing to replay | **24** | the creation prefixes P0–P5b through the creator's own error path or the census over the planted husk (`RunDir.CreatePublicDir`, `StageMarker`, `PublishMarker`, `CreatePrivateDir`, `StageOwnerRecord`, `PublishOwnerRecord`, `StageCommitRecord`, `PublishCommitRecord` and `WritePlan`, both phases each); `RunDir.RemovePrivateHusk`/after and `RemovePublicHusk`/before (the second census); `Event.AppendFirst`/before and its written/error_return point (the torn first line truncated by the reopen); `Lock.AcquireRun` both phases |
| **R** — constructed and recovered, **replay-twice not asserted** | **86** | the ST-18 matrix's 26 finalization coordinates with the closure, barrier and leftover witnesses; the dispatch, attempt, candidate and repair kill children with their settlement (12 coordinates); the orphan staging intent and prepared pin reclaimed by a resume (3); the stale-marker repair and the committed run left alone (2); `Event.OpenLog` both phases and its `sync_prefix` point in both modes, `Event.ProvePrefixStable` both phases, `Event.AppendFirst`/after and its written_full and synced error returns, `Event.Append`'s phases and five points, `Event.AppendInformational`'s synced/error_return (17); the answer writer and ingestion before its effect (5); the lease, probe and hold observations (5); the 16 container launch, release and census coordinates, whose unit fixtures carry no run log |
| **O** — no committed test constructs the prefix and performs the action | **54** | round 2's 40, and 14 it credited: `Ref.CreateIntegration` both phases (every fixture keeps the run's own marker standing, one site short of P7/P8); `Ref.PinCandidatePrepared`/before and `Object.CandidateCommitTree`/after (`recovery_for` only); `Ref.PinPrepared`/before and `Object.ProposalCherryPick`/after (planted already pinned, or as the pick's internal residue); both `IdUnread` points (no settlement after the kill); `RunDir.WriteQuestionPayload`/before (a plan; no production topology code writes the payload at this range); `RunDir.RemovePrivateHusk`/before (no later census over the refused husk); `RunDir.RemovePublicHusk`/after (an absent runs root, or the funnel called by hand); `Event.OpenLog` create/kill and truncate_torn_tail/kill (no reopen); `Event.AppendFirst` written/kill (classification) |

**So clause 2's "executed evidence for every Topology/Shared site, order, and parent-executed point" is
established on ST-07's own terms, by committed tests, for 28 of the 168 observed coordinates (4 with
replay twice asserted, 24 with nothing to replay) and not for 140**: 86 are constructed and recovered by
committed tests that assert no replay-twice equality — only two of the 112 cited tests assert it,
`unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing` and
`a_dispatch_recorded_before_this_rule_resumes_at_the_base_it_recorded` — and 54 have no committed test
that constructs the prefix and performs the tabled action. **That is many more than the 40 round 2
reported, and it is the finding.** For the fix pull requests it means: the 86 R rows close by adding
the replay-twice assertion after the recovery at 30 sites in 31 committed tests (the ST-18 matrix
`kill_after_report_before_each_cleanup_step` alone closes 23; the two `events::log` sites repeat their
kill on a log that replays) and one new resume of the container states
in a run with a log; the 54 O rows need a construction and a recovery as well as the replay; the audit's
fix table, "The coordinates a fix pull request must witness", gives every one of the 140 its file and
its shape, in round 2's shape; **a pull request that implements round 2's 40 alone leaves 100
coordinates unwitnessed**. The gate's temporary witnesses that would close a row if committed: A closes
`Worktree.CreateExecutionRoot`/before on all three obligations (it asserts replay twice equal); B closes
`Answer.Ingest`/after once a replay-twice assertion is added to it; C closes no row (the creation
prefixes it kills and censuses are N already, and it replays nothing at P6 and P7). Of the points the
pass rule names: `OpenLog`'s `SyncPrefix` point is R in both modes; the barrier's refusal cases are
executed behaviours (group 1), not registry coordinates of their own; both `IdUnread` points are O; of
the 21 Event points, **1 is N** (`Event.AppendFirst` written/error_return), **10 are R** and **10 are O**
(`Event.OpenLog`'s create and truncate_torn_tail points in both modes, `Event.AppendFirst`'s written/kill
and synced/kill, and `Event.AppendInformational`'s four points other than synced/error_return).

**The Event sites' points and the barrier, executed** (`inventory/event-and-report-entries.txt` lists
the entries: `Event.OpenLog` 8, `Event.ProvePrefixStable` 2, `Event.AppendFirst` 7, `Event.Append` 7,
`Event.AppendInformational` 7; `Object.SnapshotCommitTree` and `Object.CandidateCommitTree` each with
the `id_unread` kill point; `Process.Spawn`'s parent-side points; `Report.Write` and
`RunDir.WriteReport` both phases). Group 1 in §8 (31 tests in one invocation, `groups/group1-kill-matrix.log`):
`every_kill_point_of_the_event_funnels_kills_the_child_under_the_production_adapter` and
`every_error_return_point_of_the_event_funnels_fires_under_the_production_adapter` (every point of the
four schema-4 event sites, kill and error return, through the production adapter — the child dies at
the point; the funnel returns the injected error), `a_kill_at_each_open_point_leaves_the_shape_the_packet_tables`
and `a_kill_at_each_append_point_leaves_the_shape_the_packet_tables` (the shape each kill leaves), the
barrier's refusal cases — `an_injected_sync_failure_at_open_names_syncprefix_and_hands_out_no_handle`,
`unstable_reread_after_open_sync_refuses_resumably`, `an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle`
(three rewrite cells), `unsynced_line_made_durable_by_barrier_survives_later_power_loss`,
`resume_establishes_stable_prefix_barrier_before_any_fold_derived_effect` (`OpenLog` and
`ProvePrefixStable` observed before the census's first reclaim and before every recovery append; the
`SyncPrefix` point consulted), `resume_refuses_before_any_fold_derived_effect_when_prefix_sync_fails`
(the refusal names `SyncPrefix`; no reclaim, no append); the `IdUnread` kill tests
`kill_at_commit_tree_id_unread_point_leaves_gc_owned_object`, `kill_at_snapshot_commit_id_unread_point_leaves_gc_owned_object`,
`a_kill_at_id_unread_aborts_before_the_id_is_recorded`. **Detection:** `g5-event-written-kill-point-skipped`
and `g5-event-written-error-point-skipped` (the funnel no longer consults a point: the child is not
killed / the error never fires), `g5-sync-prefix-point-not-consulted`, `g5-effect-before-barrier` (a
`RunDir.RemoveMarker` funnel call placed before `establish_stable_prefix` is caught by the ordering
test), the three `g5-barrier-*-ignored-log` recipes (each predicate dropped, each caught),
`g5-id-unread-point-skipped-kill` and `-record`, `process-phases-not-recorded`.

**The two-crash proof record** (`groups/group2-two-crash.log`, both tests passed at `caf6bed0`):
`unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing`
kills a real child (`two_crash_kill_child`, armed at `Event.Append`'s `Written` point) after a complete
but unsynced `merge_prepared` line survives a first crash; the child's report is asserted to read
exactly `synced <the prefix length including merge_prepared>` then `cas` — the barrier's sync covered
the line and preceded the swap; the integration ref is at `proposed_sha`; the complete `task_merged`
line reached the file; then `lose_unsynced_writes` discards every unsynced write, the log still holds
`merge_prepared` and not `task_merged`, the ref is at `proposed_sha`, and the next resume records the
completed swap without swapping again (`merged_sequences == [0]`, zero CAS entries, no open
transaction, alpha `Merged`, replay twice equal). The negative variant
`barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` arms `SyncPrefix`
error-return: the refusal names `SyncPrefix`, **no CAS is issued**, the ref did not move, nothing was
appended, the ledger records no sync, and after the loss the candidate is still queued and integrates
under sequence 0. **Detection:** `g5-open-sync-skipped` (the open funnel's prefix sync removed kills
the positive proof: the child reports no `synced` line) and `g5-sync-prefix-point-not-consulted`
(kills the negative: the injected error never fires and a CAS is issued). The raw log bytes before
and after the simulated loss are not archived; the test's own assertions on them are what is
executed.

### 5.4 The residue-class evidence record (`residue/residue-class-evidence-record.md`)

Nine residue sites are declared (`Worktree.Add`, `Worktree.AddStaging`, `Snapshot.Add`,
`Object.CandidateStage`, `Object.CandidateWriteTree`, `Object.SnapshotCommitTree`,
`Object.CandidateCommitTree`, `Object.ProposalCherryPick`, `Object.RepairMaterialize`), each with its
elements, its class (`object_internal -> internal`), its row and its frozen sampling N of 8. The
record tables, per element, the synthetic half's `constructed`/`classified`/`recovered` from
`effects/residue-synthetic.json` (every element constructed, classified `internal`, recovered), and,
per site, the sampling histograms **observed on runs of this gate**, each table naming its producer
and its run.

**Which samplers write a histogram, read from the tree — three, not five as the first head said.**
PR5's sampler (`workspace_manager::tests::sampled_git_child_kills_every_residue_classified_and_recovered`)
writes `effects/residue-histogram.json` for its four sites; the ST-07 remaining-sites sampler
(`engine::topology::coverage::tests::…`) writes `residue-histogram-sequential.json` beside the
observation export, or under the build's profile directory, for its five sites; the T-ATTEMPT capture
sampler (`engine::topology::attempt::tests::sampled_git_add_and_write_tree_child_kills_every_residue_classified_and_recovered`)
writes `effects/attempt-residue-histogram.json` for `Object.CandidateStage` and
`Object.CandidateWriteTree`. The cherry-pick sampler (`engine::topology::recover::tests::…`) and the
repair-materialization sampler (`engine::topology::dispatch::tests::…`) write no file and print
nothing under `--nocapture`; their per-sample classification is asserted inside the test, and their
sites are also sampled by PR5's and the ST-07 sampler, which do write. **Every histogram in the record
has its provenance**: the three writing samplers were each run alone in round 1 with the file
copied in the same second (`residue/provenance/producers-alone.log`, the three logs and files beside
it); the ST-07 sampler's histograms from export run 1, export run 2 and the guest's export are kept
(`residue/residue-histogram-sequential-{run1,run2,guest}.json`); and the PR5 histogram the first head
attributed to export run 2 (`residue/residue-histogram-pr5-sampler.json`) is the isolated fifth run's
— its four budgets equal `samplers/pr5-alone-run-5.log`'s printed budgets — because that sampler
rewrites the file on every invocation and the copy was taken after the loop (the record lens, P2). It
is kept, relabeled.

**Every histogram row on this gate's runs has `unclassified: 0` and `recovered: true`; the nine
declared sites and the nine sites with a written histogram are the same set** (the record's coverage
check). The nine `recovery_proven` registry entries carry the synthetic half and the pinned run's
sampling half (the pin sets the machine-varying counts aside).

**Labeling.** Every class is **recovery-proven**: synthetic construction per element plus kill
sampling with every observed residue classified and recovered. **No observed-execution claim is made
for any Git-internal residue**: the parent never observes the child's internal execution, and the
record says so on every table.

**The samplers, run alone — five of five on each host.** Linux, five runs each, every run green:
PR5's sampler, the ST-07 sampler, the cherry-pick sampler (`samplers/linux-loops-a.log`,
`samplers/cherry-pick-alone.log`), the repair-materialization sampler and the add-and-write-tree
sampler (`samplers/linux-loops-b.log`, round 1; the first head claimed five isolated runs of these
two where the evidence held their green runs inside group 1 alone — the record lens, P1). The guest:
PR5's 10 of 10, the ST-07 sampler 10 of 10, the cherry-pick, the repair-materialization and the
add-and-write-tree samplers 5 of 5 each, every log ending `cargo exit=0` (`guest/loops/`, the driver
logs `guest/guest-loops-*.log`; the first add-and-write-tree batch ran nothing because its batch file
named the wrong module, and was re-run with the right name — both logs are kept).

**One residue outside the registry's vocabulary is named here and judged in §9 and §13**: what a
report writer that dies between `Report.Write`'s two hook phases leaves — the private record and the
recorded staging directory with its half-written `report.json` — has no residue class
(`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`). It is not an Object site's command-internal residue
and no kill sampler produces it; its reclamation on both branches is executed here (§3.4) and its
power-loss window is confirmed (§3.4).

## 6. Required artifact #4 — projection equivalence outputs

**The file:** `tmp-tests/run-q1-probe.log`. With the temporary `eprintln!` probe in
`assert_live_equals_replay` and in the projection test's per-prefix loop (§3.4), two tests were run
`--nocapture`: `projections_are_equal_between_live_and_replay_at_every_prefix` and the acceptance
test `max_parallel_one_completes_a_two_task_chain_with_one_linear_commit_per_task_and_the_checkout_unchanged`
(both passed, `cargo exit=0`). Every comparison printed:

| Source | Comparisons | `state_equal` | live digest == replay digest | `replay_twice_equal` |
|---|---|---|---|---|
| `G5Q1` after each step (3 steps of the projection run: log 3,634 → 3,765 → 3,894 bytes; 5 steps of the acceptance chain: 4,258 → 4,976 → 6,945 → 7,663 → 7,785 bytes) | 8 | true ×8 | equal ×8 (the digests are printed in full) | true ×8 |
| `G5Q1PROJ` per durable prefix the projection run appended (prefixes 2–7) | 6 | — | equal ×6 | — |

What each line compares: `live_vs_replay` parses the bytes on disk (`TopologyFold::parse_log(&fixture.log_bytes())`),
replays them into a fresh fold, compares the stepped run's own `fold().state()` with `==` against that
replay (every field of `RunState`), derives the schema-4 report from the live fold and from the replay
and compares their digests, and replays twice. The projection test additionally holds the live
snapshots the hooks bundle's `folded` hook recorded at every applied delta to exactly one per durable
prefix this process appended, in order, from `run_resumed` to `run_finished`, each equal to the report
derived from a replay of that prefix. **Detection:** `projection-no-live-snapshot`, `projection-stale-prefix`,
`projection-skips-run-resumed` (§3.1). The witnesses of §3.4 add two more driven runs whose final
states replay twice equal (the pins witness) and whose folds are read from the bytes on disk after
every resume (the answers witness).

## 7. Required artifact #5 — per-resource ledger outputs

**The files:** `ledger-run2/ledger-complete.md`, `ledger-parked.md`, `ledger-halted.md`,
`ledger-budget-exceeded.md`, `ledger-no-run-finished.md` (the live observation against the after-drop
one) and `ledger-no-run-finished-then-complete.md`, written through `UPSTROKE_LEDGER_EXPORT` by the
five ledger tests of the recover suite during export run 2; byte-identical to export run 1's
(`ledger/`). Each table has **54 verdict rows** — R1 to R28 and the named parts of R14 (9), R21 (13)
and R27 (4) — and **every one of the 54 reads `yes` in every one of the six tables** (a python census
over the `Holds` column on this run; 324 of 324). The guest's six tables (`guest/exports/ledger/`)
read `yes` in every row too; each differs from Linux's in exactly two lines — R21's evidence sentence
and `R21 › cleanup_lock_file`, expected `Any` and observed `Absent` on Windows, where the Unix cleanup
lease file does not exist — a platform-shaped difference, not a verdict.

Read off the tables (the `Expected | Before | After` columns): at Complete R11 `Absent | Present(2) | Absent`,
R17 and R18 `Absent | Present(1) | Absent`, R27 balanced with the planted unreachable object still
present and unreachable — `R27 › unreachable_objects` `Monotone | Present(3) | Present(3)` (the first
head wrote `Present(2)` here; that is the Halted table's figure); at Halted R11
`Retained | Present(2) | Present(2)`, R21's report `Present | Absent | Present(1)`, R27's store 11
objects monotone with `unreachable_objects` `Present(2) | Present(2)`; at Parked R15
`Present | Held(1) | Held(1)` (the open question), R11 `Retained | Present(1) | Present(1)`, R6 and R7
`Retained | Zero | Zero`, R18 `Absent | Present(1) | Absent`; at BudgetExceeded R6 and R7
`Retained | Held(1) | Held(1)`, R11 `Retained | Present(1) | Present(1)`, R23 `Absent | Absent | Absent`;
at NoRunFinished (live against after-drop) R1 `Retained | Held(1) | Held(1)`, R9, R18 and R23
`Present(1)` on both sides, R17 `Absent | Present(1) | Absent` (the lock went with the process); and
after that run's resume, R1, R6, R7, R9, R15 `Zero`/`Absent`, R11 `Absent | Present(2) | Absent`. The
three ledger unit tests (`every_outcome_equation_names_every_row_once`,
`requirements_read_before_and_after_as_named`, `a_rows_named_facts_are_each_consumed_by_its_verdict`)
passed in group 7. **Detection:** `ledger-report-skipped`, `ledger-prunes-unreachable` (R27),
`ledger-verification-defers-zero`, `ledger-verification-defers-from-queue`, `finalize-removes-answers-at-complete`
(R21's answer files), `settle-scrub-removed` (R9 at Parked), `g5-candidates-refs-kept-at-complete`
(R11 at Complete), `g5-candidates-refs-pruned-at-halted` (R11 at Halted), `st18-fresh-branch-prunes-objects`
and `-real-kill` (R27 at every finalization).

**What the ledgers observe and what they do not.** The durable rows R1, R2, R5–R12, R14–R16, R18,
R21, R23–R25 and R27 are observed from the fold, the ledger and physical inventories. R3, R4, R13, R17,
R22 and R28 are the process-local rows: observed as the loop reports them (the invocation ledger
balanced, the run lock free after the drop, R28 refused while a reaper hold is observed —
`a_surviving_reaper_hold_refuses_the_next_coordinator_until_released` and
`a_host_integration_reaper_holds_the_runs_cleanup_lease` passed in group 3) and **certified at G6, not
here**, as the definition says. R19 and R26 are observed `Absent` because the ledger fixtures drive the
loop through the recording runner and create no container; the fake-runtime container rows are the
census tests of group 3 and are not container ledger runs per outcome. R20 is observed by
classification (no site names a volume; the map `run_started` recorded is the one the fold ends with).
**The tables establish the rows they enumerate; they do not establish that every engine-created
resource has a row**: the report's staging directory after the power-loss sequence executed in §3.4
ends with no row, no class and no reclaim, which is the resource the outcome equations do not cover
and the reason clause 7 fails (§13).

## 8. The seven adversarial fault-test groups

Read from `cumulative_review_gates.gates[4].adversarial_fault_tests` in the packet (`spec/G5-DEFINITION.txt`),
counted here: **seven**. Each row says what holds the claim, what this run **executed** — the committed
tests in one of the nine group invocations, the witnesses of §3.4, the audit of §5.3 — what it
**reasoned** and says so, the grade in the verification lens's vocabulary (established / asserted /
silent, per check), and the recipe from §3 that shows the named test still detects. Every committed
test named here is defined exactly once under `src/` (§2; the one duplicated name is cited
module-qualified) and ran by name in one of the nine group invocations under `groups/`
(`groups/group<N>-….log`, the per-test `ok` lines and cargo's exit in each; `groups/run-groups-1-4.log`,
`run-groups-4-round2.log`, `run-groups-5-8.log`, `run-groups-9.log` are the drivers' summaries:
**31, 2, 38, 13, 23, 16, 37, 171 and 16 tests passed, 0 failed, every `cargo exit=0`** — group 4 is
the corrected run of round 1, 13 tests for 13 filters; the first head's group-4 invocation ran 12
of 13 filters because one filter named the wrong module, and its log is kept as
`groups/group4-st17-closure-round1.log`).

| # | Adversarial claim (the definition's words, abridged) | Held by, and what ran | Grade, check by check | Detection witness (§3) |
|---|---|---|---|---|
| 1 | the exhaustive kill matrix over every Topology/Shared site × order × resume, incl. kills during recovery, run-end closure and terminal finalization, at Event append points (kill and error return), at OpenLog's SyncPrefix point (kill and error return; no fold-derived effect precedes the barrier; a failed sync or unstable reread refuses resumably), and inside Git commands (sampled; every observed residue classified and recovered) | **what ran:** the registry's 168 observed entries with the bijection checked over the export on both hosts (§5.2) and **the per-entry audit of §5.3 on ST-07's three obligations (F 4, N 24, R 86, O 54)**; the ST-18 matrix `kill_after_report_before_each_cleanup_step` (26 Complete and 24 Halted cells asserted as exact counts, `src/engine/topology/recover/tests.rs:15548`) — **injected error returns** at both hook phases of every finalization site with `assert_finalization_order` at every cell; the real kills `kill_inside_closure_recovers` (`closure_kill_child` at `Event.Append`'s `Written` point), `a_kill_inside_finalization_after_the_execution_root_is_removed_converges_on_the_next_resume` (`finalization_kill_child`), `kill_during_recovery_repeats_recovery`, `kill_after_run_started_creates_integration_ref`, the create suite's kills at every prefix (group 9) with round 1's census witness over the killed fixtures (§3.4); the Event points and the barrier (§5.3, group 1, 31 passed); `every_kill_point_of_the_process_funnel_kills_the_child_on_this_host`; the three writing samplers and the two non-writing ones, alone, on both hosts (§5.4) | **kills during recovery — established** (`kill_during_recovery_repeats_recovery`); **kills during run-end closure — established for the named points** (`kill_inside_closure_recovers`, torn and complete shapes); **kills between every terminal-finalization effect — asserted**: the 50 cells are error returns, and the reading that an error return at a hook phase leaves the durable state a kill there leaves is the record's R11, executed as a real kill at one finalization point only; **the Event points executed in both modes, `SyncPrefix` in both modes, the barrier's order and its refusals — established as executions** (§5.3); **the recovery at the 21 Event points on ST-07's three obligations — established at 1 (N) and not at 20** (R 10, O 10; §5.3); **sampled Git-internal residues — established for the samples** (§5.4); **the matrix as "exhaustive over every site × order × resume" — not established**: the registry is complete as an *observation* inventory (0 failures on both hosts) and round 3's audit finds all three obligations met at 28 of 168 coordinates (F 4, N 24) and not at 140 (R 86 without a replay-twice assertion, O 54 without a construction and recovery) (§5.3), so clause 2 fails on those 140 (§13) | `st18-order-swapped`, `report-write-hooks-swallowed`, `finalize-swallows-leftover-removal-error`, `finalize-fresh-branch-skips-barrier` and `-ordering`, `finalize-fresh-barrier-*`, `fresh-barrier-*` ×5, `st18-fresh-branch-prunes-objects` and `-real-kill`, `g5-event-written-kill-point-skipped`, `g5-event-written-error-point-skipped`, `g5-sync-prefix-point-not-consulted`, `g5-effect-before-barrier`, `g5-barrier-*-ignored-log` ×3, `g5-id-unread-point-skipped-kill` and `-record`, `g5-registry-entry-dropped`, `process-phases-not-recorded`, `classifier-reads-worktree-list`, `proving-removal-*` ×3, `residue-synthetic-pin`, `g5-residue-frozen-n-changed-pin` |
| 2 | the two-crash proof: a complete but unsynced `merge_prepared`, restart, the barrier of step (a1) syncs and proves the prefix before the pre-CAS recovery, CAS, a second simulated power loss discarding every unsynced write — the log still contains `merge_prepared`, the ref is at `proposed_sha`, the next resume appends `task_merged` (INV-02, T-APPEND, T-PREPARED); the negative variant with an injected sync failure at the barrier issues no CAS, ends resumably, and after the loss converges to the before-append order with the candidate still queued | `unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing` (a real child, `two_crash_kill_child`, killed at the `task_merged` write; the report `synced <prefix>` then `cas`); `barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` — both in §5.3; 2 passed (`groups/group2-two-crash.log`) | **established**, both variants | `g5-open-sync-skipped` (the positive proof), `g5-sync-prefix-point-not-consulted` (the negative), `g5-effect-before-barrier` |
| 3 | two coordinators refused by locks; resume with edited config warns; deleted worktrees and pruned non-authoritative refs recovered; Retained generations closed before `run_resumed`; snapshot, container (own-run earlier incarnations and dead runs), husk (every prefix; private halves only under the ownership proof and without a commit record; creator-error shapes) and stale-marker residue reclaimed, repaired or retained and reported | locks: `a_second_process_is_refused_the_run_lock` (a real second process), `a_surviving_reaper_hold_refuses_the_next_coordinator_until_released`, `a_surviving_ref_writer_of_the_dead_coordinator_refuses_the_resume_until_it_exits`, `a_host_integration_reaper_holds_the_runs_cleanup_lease`; config: `resume_rebuilds_runner_from_record_and_warns_on_config_drift`, `a_config_that_differs_warns_naming_the_field_that_moved`, `resume_warns_when_reference_moved_and_uses_recorded_image_id`; worktrees: `kill_after_dispatch_recreates_worktree_without_spend`, `a_live_worktree_missing_close_reclaims_the_generations_worktree_and_intent`, `retained_generation_closed_when_worktree_missing`, `a_retained_generations_reclaim_the_closing_recovery_never_reached_is_finished_by_the_next`; **pruned non-authoritative refs: round 1's witness** `g5_gate_a_resume_after_finalization_pruned_the_pins_continues_the_queued_candidate` (§3.4) — finalization's step (iv) prunes the candidate-prepared pin at a BudgetExceeded stop, the resume with a raised ceiling continues and integrates the queued candidate without it, replay twice equal — beside `kill_after_run_started_creates_integration_ref` (the integration ref re-created) and `resume_finalizes_halted_then_refuses` (the pins observed pruned); retained before `run_resumed`: `retained_generation_closed_before_run_resumed` (settle level), `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` (through the loop), `retained_generation_not_continued_after_kill`; snapshots: `snapshot_residue_reclaimed`; containers (fake runtime): `orphan_reclaimed_before_slot_reset`, `same_run_resume_reclaims_earlier_incarnation_orphan`, `repeated_crashes_reclaim_every_dead_incarnation`, `a_dead_foreign_owners_container_naming_this_incarnation_is_reclaimed`, `live_owner_untouched_while_dead_orphan_reclaimed`, `labeled_orphan_without_intent_reclaimed`, `concurrent_reclaimers_converge`, `a_live_runs_dead_earlier_incarnation_is_untouched_by_a_foreign_census`; **husks at every prefix: round 1's witness** `g5_gate_killed_publication_prefixes_converge_under_the_next_census` (§3.4) — the create suite's own kill child at each of the 13 prefixes, then `census_run_dirs` over the killed fixtures, 13 of 13 at ST-19's disposition — beside `kill_at_each_prefix_p0_to_p8_converges` (classification and `husk_report` only, group 9), `bound_husk_without_commit_record_reclaimed_private_then_public`, `copied_husk_from_other_repository_retained_repo_key_mismatch`, `markerless_husk_with_content_retained`, `locked_husk_skipped`, `a_husk_whose_private_half_cannot_be_removed_is_reported_and_the_census_continues`, `resume_reclaims_a_provable_husk_beside_the_run_and_retains_a_possibly_committed_one`, `resume_completes_past_a_husk_whose_private_half_cannot_be_removed`, `the_resume_census_reports_the_husk_it_could_not_reclaim`, `kill_during_containerized_probe_before_run_started_reclaims_container_and_husk_and_reports_boundary` (a creator-error shape), `resume_of_nondefault_root_run_reclaims_earlier_incarnation_intents_in_recorded_root`; markers: `committed_run_with_stale_marker_repaired_by_census_when_lock_free`, `a_live_owners_stale_marker_is_removed_by_the_owner_and_by_nobody_else`, `committed_run_with_stale_marker_listed_and_repaired_by_resume`; the torn registrations: `a_torn_registration_is_passed_over_and_the_slot_converges_when_no_writer_is_alive`, `a_registration_git_cannot_enumerate_classifies_as_unpopulated_and_converges`; 38 passed in group 3, 16 in group 9 | **every check established by execution**: the two the first head reasoned — pruned non-authoritative refs recovered, husks at every publication prefix converging under the next census — are executed by round 1's two witnesses, which are **uncommitted** (§3.4): the behaviour holds at `caf6bed0`; no committed test carries either by name, and that is said here rather than counted as a committed guard. Containers are established **with the fake runtime**; the real-Docker census tests ran under `UPSTROKE_REQUIRE_DOCKER=1` in §1 | `g5-run-lock-shared`, `g5-drift-warning-dropped`, `g5-retained-idle-not-closed-on-resume`, `finalize-keeps-staging-leftovers`, `classifier-reads-worktree-list`, `proving-removal-refuses-empty-gitdir`, `proving-removal-deletes-by-name`, `proving-removal-skips-locked-without-gitdir`; G4's M13, M29, M30 (recovery step (e), closed-generation reclaim, a failed verify) were **not** re-run here and are G4's evidence |
| 4 | ST-17 run-end closure per class and outcome incl. every derived-outcome refusal listed, and the pre-`budget_exceeded` counterexample | `run_finished_complete_refused_with_queued_candidate`, `run_finished_parked_refused_with_admissible_work`, `run_finished_parked_or_complete_refused_while_deferred_items_exist`, `run_finished_halted_and_budget_exceeded_accepted_with_deferred_items` (a live verification-deferred candidate and a Deferred task, Halted void, BudgetExceeded resumably open and woken), `run_finished_budget_exceeded_refused_after_halting_drain_settlement`, `run_finished_halted_accepted_after_declined_verification_park`, `over_budget_prefix_without_budget_exceeded_is_not_ending` (NotEnding before the event; `budget_exceeded → run_finished` last), `replayed_conflicting_outcome_refused`, `a_budget_stopped_run_with_a_retained_generation_is_closed_run_ending_and_ends`, `closure_refuses_an_in_flight_generation_and_an_unresolved_transaction_before_any_append` (the record's R1: steps (2)–(4) refused naming PR11), **`select::tests::a_breach_appends_budget_exceeded_and_integration_and_run_end_cross_the_checkpoint`** (executed in round 1's group-4 run under its right module; the first head's filter placed it under `recover::tests` and ran nothing for it), `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`, the census's `a_state_with_admissible_work_and_no_budget_exceeded_classifies_not_ending`; the three refusals the definition lists without a test of their own name — an open question at Complete, a missing halting settlement at Halted, a missing budget event at BudgetExceeded — are one fold guard, `check_run_finished`'s equality of the recorded outcome with the derived one (halt > budget > parked > complete), exercised by the census in both directions for every outcome (`the_census_reaches_every_outcome_and_says_what_it_did_not_reach`: `run_finished/<outcome>` offered at every state); **13 passed in group 4** (`groups/group4-st17-closure.log`), the census tests in group 7 | **established**, every listed refusal executed by a named test or through the census's outcome equality; the pre-`budget_exceeded` counterexample by name and by the census | `st17-halted-with-backoff-refused`, `st17-budget-outranks-halt`, `closure-refuses-verification-deferred`, `closure-swallows-close-append-error`, `closure-swallows-undetermined-close-append`, `closure-deletes-candidates-refs`, `g5-breach-ends-without-budget-exceeded`, `g5-run-finished-outcome-unchecked`, `g5-derived-outcome-parked-as-fold-error`, `g5-answer-accepted-under-budget-stop` |
| 5 | ST-18 terminal finalization: kills after `run_finished`, after the report write, and between every cleanup effect converge to the outcome equations; resume of Complete/Halted finalizes then refuses; the report lists retained candidates refs at Halted; answer files and `.partial` residue untouched and never ingested; a late answer after finalization writes an inert file | the matrix (26 + 24 cells) and `kill_after_run_finished_before_report`, the real kill, `resume_finalizes_halted_then_refuses`, `halted_report_lists_candidate_refs`, `answer_files_untouched_by_finalization` (and every finished-run fixture's `PlantedAnswerFiles` compared at every cell), `private_records_untouched_by_finalization`, `late_answer_after_finalization_is_inert_and_reported_not_live` (the answer file written after finalization, `is_running` false, two more resumes refuse "already finished", the file and the log byte-identical), `late_answer_before_halting_settlement_is_inert_and_retained`, `finalized_report_names_runner_identity` (a tampered report regenerated), the report barrier tests (`the_report_is_durable_before_any_ref_is_pruned_and_a_current_report_is_not_rewritten`, `a_report_directory_barrier_that_fails_refuses_pruning_on_every_resume_until_it_holds`, `a_report_rename_without_directory_sync_is_proven_before_pruning`, `fresh_report_hook_errors_stop_cleanup_and_retry`), the checkout and root barriers (`a_checkouts_deletion_is_made_durable_before_its_intent_is_removed`, `a_failed_checkout_barrier_is_retried_before_its_intent_is_removed`, `an_absent_checkout_retries_its_parent_barrier`, `an_absent_slot_directory_is_made_durably_absent_in_the_root_before_the_intent_is_removed`, `a_failed_execution_root_barrier_is_retried_before_the_intent_is_removed`), the staging protocol (`a_report_staging_directory_is_recorded_before_it_is_made`, `legacy_report_preserves_unowned_staging_directory`, `a_dead_writers_staged_report_is_reclaimed_by_the_next_write`, `a_report_whose_staged_file_will_not_sync_is_not_published`); 23 passed in group 5; the gate's witnesses of §3.4: the fresh branch's reclamation (passes at both outcomes; fails under the mutation the matrix admits) and the power-loss recipe (**fails at this range, `cargo exit=101`**) | **finalize-then-refuse, idempotent repeated finalization, the Halted listing, the untouched answer files and `.partial`, the inert late answer — established** for the exercised cases; **"kills … between every cleanup effect converge to the outcome equations" — not established**: (a) the cells are error returns with the kill equivalence reasoned (row 1); (b) the committed matrix's third resume does not assert the fresh branch's reclamation of the recorded leftover (`PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION`, confirmed by `r11-third-resume-leftover-record-only` surviving it, §3.3); (c) under the crash model one engine-created directory ends outside every equation (`PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION`, executed, §3.4). (b) and (c) are defects of the tree; they fail clause 7 (§13) | the writer and finalization recipes of row 1, `g5-finished-run-not-finalized-on-resume` and `-late-answer` (a resume that neither finalizes nor refuses is caught by both tests), `g5-report-freshness-always-true`, `g5-run-is-over-guard-dropped`, `g5-candidates-refs-pruned-at-halted`, `g5-candidates-refs-kept-at-complete`, `report-leftover-not-reclaimed-*` ×3, `staging-created-before-its-record`, `staging-reclaimed-without-a-record`, `checkout-barrier-error-swallowed`, `checkout-synced-before-its-deletion`, `sync-absent-slot-directory-skipped`, `root-barrier-error-swallowed`; **and the three round-11 recipes that survive**, judged in §9 and §13 |
| 6 | Halted retains and lists candidates refs; Complete prunes all and leaves objects to Git; BudgetExceeded resume with a raised budget continues queued candidates, parked repairs with sources present and woken deferred items; answers left on disk during a budget-stopped epoch are ingested only by the resume and only for still-open questions | `halted_report_lists_candidate_refs`; the six ledgers (R11 `Absent` at Complete with R27 balanced and the planted unreachable object kept, R11 `Present(2)` retained at Halted, §7); `resuming_with_a_higher_ceiling_continues_the_run_the_budget_stopped`, `resume_clears_budget_stop_and_wakes_deferred`, `a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume` (the parked repair's source present across the stop), `deferred_task_woken_by_defer_wait_elapsed_or_resume`, `run_finished_halted_and_budget_exceeded_accepted_with_deferred_items` (the resume wakes them); answers: `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch` (the fold's refusal in the stopped epoch), `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step` (which stops at Parked, not BudgetExceeded — the lens is right), `a_delivered_answer_is_ingested_before_unrelated_runnable_work_dispatches`, `a_verification_park_answer_is_ingested_and_the_candidate_re_verifies`, the over-limit and admission-answer pair; **the whole sequence at BudgetExceeded, executed by round 1's witness** `g5_gate_answers_on_disk_are_ingested_only_by_the_resume_and_only_for_still_open_questions` (§3.4): the question open, `budget_exceeded` in epoch 0, the answer file published on disk, the resume in epoch 1 ingests it once, a further resume with the file still on disk and the question closed ingests nothing; **"only still-open"** rests on `ingest_answers` (`src/engine/topology/run.rs:787`) polling `fold.open_questions()` and on the fold's `check_question_answered` refusal, both executed: G4's M08 (`g5-answered-question-stays-open`, the fold no longer removes an answered question from the open set) **kills the committed `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run` and the witness**; 16 passed in group 6 | **every check established by execution**; the budget-stopped-epoch sequence and the still-open filter by an **uncommitted** witness and a re-run of G4's M08 (§3.4), said so | `g5-resume-keeps-budget-stop`, `g5-resume-does-not-wake-deferred`, `g5-answer-accepted-under-budget-stop`, `g5-answered-question-stays-open` and `-vs-g5-witness`, `g5-candidates-refs-pruned-at-halted`, `g5-candidates-refs-kept-at-complete`, `ledger-prunes-unreachable`; G4's M10 not re-run |
| 7 | the packet's seam tests in range: ST-07 full, ST-09 durable rows incl. R21 answer files, the owner and commit records and R27 with the fsck inventory, ST-10, ST-11, ST-12, ST-13 sequential subset incl. the fast no-object proof, ST-14 incl. totality, live-versus-replay classification equality and `merge_prepared` relations, ST-15, ST-19, ST-20 | **ST-07**: §5.2 (both hosts, 0 failures), the coverage tests of group 7, **the per-entry audit** (§5.3); **ST-09**: §7 — R21's parts in the Complete ledger read `1 answer file(s), 1 .partial` retained, `owner_record` and `commit_record` `Present(1)`, R27's `unreachable_objects` from `git fsck` `Present(3)` monotone at Complete and `Present(2)` at Halted; **ST-10**: §6's replay comparisons, the barrier tests of row 1, the append-error protocol tests (`an_append_error_at_the_run_ending_close_ends_the_command_and_the_next_resume_closes_the_generation`: `Written`, `WrittenFull`, and the undetermined case; `append_error_inside_closure_ends_command_and_resume_completes_closure`), the NoRunFinished ledger (nothing derived from the poisoned fold; R17 released with the process), the husk, marker and intent reclaims of row 3, the Runner rebuild of row 3; **ST-11**: row 3's retained tests; **ST-12**: `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`, `an_override_replaces_the_frozen_binding_for_every_later_attempt`, `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`, `declining_a_repair_admission_fails_the_lineage_and_allows_the_run_to_end` (group 9); **ST-13**: the fast no-object proof `fast_path_publishes_exact_candidate_without_staging_or_proposal_object` (group 9) and R13 `Zero` ("0 provisional reservation(s) held at process end") in every ledger; **ST-14**: §4; **ST-15**: `retry_refused_after_resume` (settle and recover levels), `retry_refused_with_stale_incarnation`, `retained_worktree_with_residue_closed_not_retried`, `kill_during_retry_attempt_closes_generation`, `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again`, `same_generation_retry_regates_cumulative_tree` (groups 7 and 9); **ST-19**: `kill_at_each_prefix_p0_to_p8_converges`, row 3's husk and marker tests, **and round 1's census witness over the killed fixtures** (§3.4); **ST-20**: `finalized_report_names_runner_identity` (the report names kind, policy, image reference, id and digest; the `contract_mappings.rs` row), `resume_rebuilds_runner_from_record_and_warns_on_config_drift`, `the_rebuild_returns_the_recorded_runner_exactly_however_the_config_differs`, `container_created_from_recorded_image_id_and_verified`, `substituted_image_id_refused_before_start`, `run_resumed_is_accepted_with_an_identical_runner_and_refused_with_any_different_field`; groups 7 and 9 green | **ST-07 — established on its three obligations for 28 of 168 observed coordinates (4 with replay twice asserted, 24 with nothing to replay) and not for 140** (86 constructed and recovered without a replay-twice assertion, 54 without a committed construction and recovery; §5.3; clause 2); **ST-09 — established for the exported rows**, and not for the completeness of the inventory the rows enumerate: the reclaimed staging directory after the power-loss sequence has no row (§7, §3.4); **ST-10 — established for reconstruction** (replay, the barrier, the append-error protocol) **and not for universal recovery** of every resource of the run's own making, by the same counterexample; **ST-11, ST-12, ST-15, ST-20 — established** for the named cases; **ST-13 — established for the fast no-object proof; the provisional-reservation lifecycle beyond it is G3's**, not re-run by name here (no test at this range carries "provisional" in its name; R13 `Zero` in every ledger is an end-state count, not the conversion and cancellation cases), said so; **ST-14 — established for the implemented family and asserted against the packet's bounds line** on two dimensions (§4; clause 1); **ST-19 — established** by the census witness on the killed fixtures (uncommitted) beside the committed classification test | §4's census recipes (19 against `topology::census::tests`, one against `recover::tests::replayed_conflicting_outcome_refused`); `g5-run-resumed-runner-difference-ignored`, `g5-drift-warning-dropped`, `g5-report-freshness-always-true`; ST-15's G4 mutations (M12, M30–M32) not re-run |

**The sampled half of group 1, measured here.** Five samplers kill real `git` children and classify
what they leave; on Linux each ran alone five times and green (`samplers/`: `linux-loops-a.log` for
PR5's, the ST-07 and the cherry-pick samplers, `linux-loops-b.log` for the repair-materialization and
the add-and-write-tree samplers), on the guest ten or five times each and green (`guest/loops/`), and
the three that write a histogram wrote the ones §5.4 tables. `sampler-kills-leader-only` (the fixture
reverted to killing the leader alone) passed, as PR10's record says it does most runs, and is no
witness either way.

## 9. Invariants, findings, deferrals and flakes

### 9.1 The five integrated invariants, and where each is established here

| Invariant (abridged) | Where, and what is not established |
|---|---|
| INV-01–INV-17 (all portions), INV-19, INV-20 (fold level), INV-22 (the durable rows, outcome equations, derived outcome, closure, finalization), INV-23 (host and fake container) at max_parallel = 1; the process-local rows R3, R4, R13, R17, R22, R28 certified at G6, their host portions asserted here; R19/R26 with real Docker at G7 | §7 (every durable row `yes` in six ledgers on two hosts; the process-local rows observed as the loop reports them and named as G6's), §8 rows 3–6; the earlier gates' portions are not re-claimed. **INV-22's outcome equations do not cover the reclaimed staging directory after the power-loss sequence** (§3.4, §7): one resource of the run's own making ends with no row |
| every Topology/Shared site in the inventory (incl. Event sites with kill and error-return points, OpenLog's SyncPrefix point, IdUnread points) has both hook phases observed executed, registry entries for every observable order, and executed evidence; every Object internal residue class has synthetic evidence per element and a kill-sampling record with every residue classified and recovered (recovery-proven; no observed-execution claim); the bijection passes; disallowed lists, allow-placement scan, wrapper classification and legacy allowlist confirmed against the code | §5: 0 failures on both hosts; 180 entries; every observable order (one per site; §9.3); the residue record; the 171 effects tests. **"Executed evidence" in ST-07's sense — the prefix constructed, the tabled action performed and replay twice equal, by a committed test — holds at 28 of 168 observed coordinates (4 with the replay asserted, 24 with nothing to replay) and not at 140** (86 without a replay-twice assertion, 54 without a construction and recovery; §5.3). The dependency review is a change-time obligation and nothing at this range performs it; no dependency entered the range (§5.1) |
| projections equal between live and replay at every prefix | §6 (14 comparisons printed, all equal; the committed test's one-snapshot-per-prefix assertion) |
| `run_finished` equals `derived_outcome` from durable inputs; `budget_exceeded` before any budget-driven end; pending backoff blocks only Parked and Complete; run-end closure per class; terminal finalization idempotent for Complete and Halted; cleanup prunes only terminal items, never candidates refs on resumable outcomes, never answer files | §8 rows 4–6, §7 (R11 retained at Parked and BudgetExceeded, R21's answer files retained at Complete). Finalization's idempotence across the third resume is established by this gate's witness and not by the committed matrix (§3.3, §3.4) |
| the bounded census family complete for the stated bounds, per-arm coverage, totality over the union, every outcome in both directions (no unbounded closure, no effect-phase claim) | §4: complete for the **tree's** bounds; two of the packet's bounds read differently (§4, §13 clause 1) |

### 9.2 The findings in range, re-verified

The ledger at `caf6bed0` holds 342 files under `findings/` (`README.md`, `PROCESS.md` and 340
findings): 16 P1, every one `disposition: deferred`; 131 P2 (129 deferred, 2 accepted-risk); 193 P3
(189 deferred, 4 accepted-risk). The ledger's disposition vocabulary is `deferred | accepted-risk`
(`findings/README.md`), so **no file carries a disposition `open`**, and `findings/PROCESS.md` calls
every filed finding an open one. Both readings are carried into §13; the plain one governs there.

**The six #279 findings**, each executed here where it carries a recipe, each judged against the
clause it touches:

| Finding | What this run did | What it establishes | The clause it touches, and the effect |
|---|---|---|---|
| `PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION` (P1, `findings/P1_crash-consistency_202609141646_the-st18-third-resume-asserts-on-the-first-leftover.md`) | its recipe `r11-third-resume-leftover-record-only` executed: the matrix **passes** under it (`rc=0`, §3.2); the gate's own witness with the assertion the finding asks for (§3.4) passes unmutated at Halted and Complete and **fails** under the same mutation (`rc=101`) | the finding is real: the committed matrix's third resume does not observe the fresh branch's directory reclamation; the behaviour is established here by an uncommitted witness; the committed witness does not exist | **clause 7 fails** (the committed evidence for "finalization converges" has a gap the tree's own recipe exposes) and **clause 9 fails** (a P1 filed in the range, on the gate's own subject, unfixed). The record's sentence at `reviews/2026-09-12-pr10-record.md` lines 2250–2251 ("its third resume the fresh branch's") is not established by the committed test, as the finding says, and this report does not rest on it. The fix is one assertion (§13) |
| `PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION` (P2, `findings/P2_crash-consistency_202609141646_reclamation-drops-the-record-before-the-deletion-is-durable.md`) | its recipe executed as a temporary test (§3.4): **fails as predicted**, `cargo exit=101` — after the simulated loss the old directory A stands, is passed over as unrecorded, and the record is gone | the power-loss window is real at `caf6bed0`: `reclaim_report_staging` (`src/rundir.rs:1520`) removes the record before the public directory's deletion is durable, so a reclaimed staging directory whose deletion was never synced before its record was dropped is never reclaimed by anything | **clause 7 fails**: an engine-created directory ends outside every outcome equation and outside every ledger row, under the crash model the gate certifies; Q2's "every owned resource has exactly one class and one row" has an executed negative answer. Its P2 grade is the ledger's; the clause reads the behaviour, not the grade |
| `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED` (P2, `findings/P2_crash-consistency_202609141646_a-recorded-staging-leftover-has-no-residue-class.md`) | the registry and the residue authority read (§5.4): `Report.Write`'s and `RunDir.WriteReport`'s before phases carry `rows: []`; no entry covers the recorded leftover; its reclamation on both branches executed (§3.4) | a residue of the run's own making — the private record and the recorded directory with a half-written report, left by a death between the report site's two hook phases — has no registry class; it is reclaimed by the next execution of the site on either branch | clause 4: the clause's subject in the packet's words is the Object sites' command-internal residue classes (nine, every one recovery-proven and sampled here), which this leftover is not; **the report-site residue is outside the registry's vocabulary and this report says so rather than counting it covered**; judged in §13 as a named exception of clause 4 and as part of clause 7's failure (it is the residue the power-loss window strands) |
| `PR10-PRIVATE-RECORD-BARRIERS-UNWITNESSED` (P2, `findings/P2_crash-consistency_202609141646_the-private-record-barriers-have-no-swallow-witness.md`) | both recipes executed: `a_report_staging_directory_is_recorded_before_it_is_made` **passes** with either private-record barrier's error swallowed (`rc=0` ×2, §3.2); the production code read: both errors propagate (`begin_report_staging`, `src/rundir.rs:1561`) | a guard-shape gap (rule 2 of the repair rounds), not a behaviour gap at this head | no pass-rule clause reads the private record's barriers by name; it weakens the detection evidence behind the staging protocol and is named in §13's qualifications |
| `PR10-RECORD-STALE-SENTENCES-AFTER-ROUND-11` (P3, `findings/P3_docs-contract_202609141646_the-record-carries-stale-sentences-after-round-11.md`) | the five copies confirmed present at the cited lines (`reviews/2026-09-12-pr10-record.md` 2797, 2914, 2273, 350, 1688) | the record carries five stale sentences; nothing executes on them | none; this report does not repeat any of the five |
| `PR10-LEGACY-REPORT-REWRITE-DROPS-ITS-POSIX-ACL` (P2, `findings/P2_security-trust_202609132345_rewriting-a-legacy-report-drops-its-posix-acl.md`) | its recipe executed as a temporary test on ext4 (§3.4): **fails as predicted**, the ACL and its mask dropped by the staged rewrite | the finding is real at `caf6bed0` for a schema-3 report an operator ACL'd | none of the pass-rule clauses (the schema-4 report is regenerated only when missing or stale, and a current report is never rewritten — `the_report_is_durable_before_any_ref_is_pruned_and_a_current_report_is_not_rewritten`); a v0.1-path defect the owner deferred |

**The seven re-deferred findings the first head omitted** (the verification lens, F4), each judged
against the clause it touches:

| Finding | What it says | What this run measured | The clause it touches, and the effect |
|---|---|---|---|
| `PR3-REG-001-CONDITIONAL` (P3, `findings/P3_correctness_202608310805_registry-equivalence-holds-only-for-this-inventory.md`) and `PR4-REG-001-STILL-EQUIVALENT` (P3, `…202608310814_registry-equivalence-is-still-conditional.md`) | the registry's order-free key is equivalent to the site × order key only while every site exposes zero or one observable order; the guard is "the change that gives any site a second observable order", re-measured at PR10 and passed to whoever makes that change | **the condition is inactive at `caf6bed0`, measured** (`inventory/orders-per-site.txt`): the pinned registry's 68 sites each carry one `order` value on their executed entries; the three sites with a second value (`Worktree.AddStaging`, `Ref.PinPrepared`, `Object.ProposalCherryPick`) carry the fast no-execution record's `None` beside `effect_before_event`, not a second observable order. `EffectSiteId::observable_orders` returns one or none by construction (`src/topology/effects.rs:338`) and no entry of the range's registry contradicts it | clause 2 ("every … order") and Q5: the equivalence the clause's bijection rests on holds for this inventory, by measurement; **no effect on the grade** of clause 2, which fails on the 140 coordinates without a committed witness on ST-07's three obligations and not on orders. Both stay deferred with their guard; the guard is the right one |
| `PR3-FRAMEWORK-SILENT-1` (non-releasing removals), `-2` (read-only after phases), `-3` (referenced stop and probe), `-4` (OpenLog kill versus error), `-5` (platform containment actions) (P3 docs-contract, `findings/P3_docs-contract_2026083108{08,09,10,11,12}_framework-silent-on-….md`) | the design's residue framework does not state five rules the residue authority derives in code — what a removal that releases nothing leaves, that a read-only site's after phase has no residue, how a referenced stop or probe is treated, that `OpenLog`'s kill and error return differ, and which containment actions are platform-shaped; the guard is the owner — a design section stating the derived rules (`DESIGN.md` §26 is the owner's) | the authority is executed: every registry entry enters through `EffectSiteId::semantics` and the residue authority (`src/topology/effects/residue_authority.rs`), and the five rules are asserted by its own tests, which passed in the 171 of group 8; the `OpenLog` kill-versus-error distinction is executed in group 1 (both modes, §5.3) and the containment actions on both hosts (§1) | clause 2 and Q5 read the registry the authority generates; **the five findings name a missing design sentence, not a missing witness or a wrong action**: every action the authority takes is executed here, and the clause is graded on executions, so **no effect on any clause**. They stay deferred to the owner's design section; the gate has nothing to execute on a docs-contract gap |

Two more findings were observed on this run and are not this gate's: `PR281-MACOS-KILL-TREE-SETTLE-ONLY-THE-DIRECT-CHILD`
(P2, deferred; the macOS CI red at the range, §1; a host-containment fingerprint in G2's portion, not
a G5 site), and the sampler family's `RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE` sibling
(`findings/P2_correctness_202609131201_the-recover-sampler-cold-probe-sibling.md`), whose subject
sampler reddened export run 1 with the cleanup-lease fingerprint and passed 5 of 5 alone (§1). The
Docker termination-poll flake (`findings/P3_correctness_202609121234_the-docker-termination-poll-counts-yields-not-time.md`)
reddened two of the 23 Docker-required tests, each 3 of 3 alone (§1).
`RESIDUE-UNBINDABLE-TASK-REGISTRATION-HAS-NO-DESIGN-SENTENCE` (P3, docs-contract) stays the design's
open sentence for the registration the forced removal cannot bind; the policy is in code and its tests
(`a_torn_registration_is_passed_over_and_the_slot_converges_when_no_writer_is_alive`, group 3).

**This gate files no new finding.** Nothing it found is a defect of the tree that no finding covers:
the eleven mutation survivals are explained (§3.3); the two clause failures of the tree's own are the
filed `PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION` and `PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION`;
the 140 registry coordinates without a committed witness on ST-07's three obligations are a
missing-witness class of G5's own subject that no finding names by coordinate, and rather than file 140
P3s the gate names them in the audit's fix table and in §13 as the tree change clause 2 needs — a successor that fixes the tree files or closes them there.

### 9.3 The deferrals in range, and whether each bears on a clause

- **`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002`** are **not deferrals of this range** (§0): the first
  was closed by #272's design reading (`7da321c0`, its file deleted with the reading recorded in
  `design/26`), the second fixed by #275 (`1c309d49`, "Finding: PR8-CRASH-002"), both inside the range
  and before #279's merge base. Neither file exists at `caf6bed0` and the ids appear only in history and
  in other findings' text. They bear on no clause.
- **The six #279 findings** (§9.2): the P1 fails clauses 7 and 9; the power-loss P2 fails clause 7;
  the unclassified-leftover P2 is clause 4's named exception; the barriers P2 bears on detection
  evidence; the P3 and the ACL P2 on no clause.
- **The seven re-deferred registry and framework findings** (§9.2): the order condition measured
  inactive; the five framework findings are docs-contract gaps whose actions are executed here; none
  changes a grade.
- **G4's own deferrals** still in the ledger (`G4-O2-OVERRIDE-SINGLE-RUNG-EXHAUSTION-UNWITNESSED`,
  `G4B-O9-EVENT-LOG-ANSWER-READER-NEVER-PRUNES-UNWITNESSED`, the two G4R3 record findings): each is a
  missing committed witness in G4's portion; `G4B-O9` is also the missing resume at
  `Answer.Ingest`/after, which the audit marks O (§5.3; `Answer.Ingest`/before is R), so it bears on
  clause 2 here and is named there;
  the others bear on no G5 clause and were not re-measured.
- **`PR281-MACOS-KILL-TREE-SETTLE-ONLY-THE-DIRECT-CHILD`** (P2): bears on the cumulative suite's
  macOS green at the range (red in the newest CI run, green in the merge-queue run); a G2-portion
  containment fingerprint, filed with five sightings now; judged in §13's clause 5.

### 9.4 The flakes on this run, fingerprinted

| Fingerprint | Where it reddened | Control | Finding |
|---|---|---|---|
| `a_hold_whose_command_never_spawns_is_released_with_the_descriptor`, `src/rundir/tests.rs:4447`, "released with the descriptor" | gate attempt 1 (1 of 4 Linux full runs at the range) | 10 of 10 alone (`flakes/`); the test is new in range (#275) | none by name; the record's census 12 of 1,014 |
| `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered`, `src/engine/topology/recover/tests.rs:8355`, the cleanup-lease refusal | export run 1 (1 of 4 Linux full runs) | 5 of 5 alone (`samplers/cherry-pick-alone.log`) | the sampler family (`findings/P2_correctness_202609131201_…`) |
| `real_docker_kill_on_an_already_exited_container_is_tolerated` and `real_docker_returns_both_streams_of_a_container_separately`, the termination-poll fingerprint | the Docker-required run (§1; 2 of 23) | 3 of 3 alone each, Docker still required (`docker/`) | `findings/P3_correctness_202609121234_the-docker-termination-poll-counts-yields-not-time.md` |
| `kill_tree_settles_the_whole_unix_group_before_it_returns`, `src/agent/proc/tests.rs:879` | CI macOS, the push run (1 of 2 CI runs at the sha) | the merge-queue run at the same sha green | `PR281-MACOS-KILL-TREE-SETTLE-ONLY-THE-DIRECT-CHILD` (P2) |
| `a_host_integration_reaper_holds_the_runs_cleanup_lease`, `src/engine/topology/recover/tests.rs:8799`, "the hold outlived the reaper that took it" | the report head `e908c7c`, gate attempts 1 and 2 of 3, and round 3's report commit `bf51946`, attempt 1 of 2 (§11) | 5 of 5 alone at each (`flakes/alone-e908c7c-a_host_integration_reaper_holds_the_runs-run-*.log`, `flakes/alone-bf51946-a_host_integration_reaper_holds_the_runs-run-*.log`); the last attempt green at each | none names this test; the reaper-hold fingerprint is the cleanup-lease family of the sampler finding's red below |
| `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner`, `src/engine/topology/recover/tests.rs:8355`, the cleanup-lease refusal ("still has a process of its own alive … an agent-cleanup reaper") | the report head `e908c7c`, gate attempt 2 of 3, and `bf51946`, attempt 1 of 2 (§11) | 5 of 5 alone at each (`flakes/alone-e908c7c-repeated_container_launch_outages_before-run-*.log`, `flakes/alone-bf51946-repeated_container_launch_outages_before-run-*.log`); the last attempt green at each | none names this test; the same refusal fingerprint as the cherry-pick sampler's red on export run 1 |

No flake reddened the Windows guest, any group invocation, any sampler loop, any witness run, or any
mutation run. At round 1's report head `e908c7c` the reaper-lease fingerprint reddened two of three gate
attempts, and at round 3's `bf51946` the first of two, both tests at once (§11); each red test was green five
of five alone at each.

## 10. The six standing questions

Each answer names G5's portion first and closes with what the evidence supports: **answered with
evidence**, **answered in the negative**, or **answered with evidence for the named scope**; the
earlier gates' portions are not re-claimed. The question text is the packet's (`spec/G5-QUESTIONS.txt`,
verbatim, checked).

### Q1 — reconstruction from durable state alone

**G5's portion:** the finished outcome, `halted_at`, the epoch-scoped budget stop across
finalization and resume, the report's freshness, the Runner identity in the report; the fresh
process's stable-prefix barrier before any fold-derived effect and its refusals; the durable recovery
records this range adds (the report's staging record); the process-local holdings are G6's.
**Answered by replaying the bytes on disk, not by reading the code** (§6): 8 live-versus-replay
comparisons of `RunState` with `==` over every field after every step of two driven runs, and 6
per-prefix report-digest comparisons, all equal; the census's classification equal live and on replay
over all 20,025 explored states (§4); `resume_establishes_stable_prefix_barrier_before_any_fold_derived_effect`
(the barrier's two sites observed before the census's first reclaim and before every recovery append,
the `SyncPrefix` point consulted), `resume_refuses_before_any_fold_derived_effect_when_prefix_sync_fails`
and the `events::log` refusal tests (a failed sync, an unstable reread in three shapes, a replay
refusal: nothing done, the run resumable) — all in group 1; the two-crash proof (§5.3); the husk,
marker, intent and container reclaims of §8 row 3; the Runner rebuilt from `run_started(4).runner`
and the drift warning (`resume_rebuilds_runner_from_record_and_warns_on_config_drift`); the two
witnesses of §3.4 whose folds are re-read from disk after every resume. The private root is derived
read-only before the lock and the records verified before any private write (`RootDerived::derive_with`
before `LocksHeld::take`, `RecordsVerified::verify` after it, read in `src/engine/topology/recover.rs`;
executed by every resume test). **The one durable record this range adds has an executed loss case**:
after the power-loss sequence of §3.4 the staging record is gone and the directory it named stands,
and no later process reconstructs its ownership from anything on disk — reconstruction of the fold and
of every ledger row holds; reconstruction of *that* directory's ownership does not. The process-local
holdings (reservations, slots, invocations, lock holds, process handles): R3, R4, R13, R17, R22
`Zero`/`Absent` after the drop in every ledger and R28 refused-until-released
(`a_surviving_reaper_hold_refuses_the_next_coordinator_until_released`) — observed, and **certified at
G6, not here**. **Answered with evidence for G5's portion, with the staging record's loss case named
as the one thing not reconstructed.**

### Q2 — resource accounting

**G5's portion:** the durable rows at every outcome, incl. answer and staging files, the owner and
commit records, engine-created objects and the reaper hold; the outcome equations incl. the derived
outcome, closure, terminal finalization and repair-source protection. **Evidence** (§7): six ledgers
× 54 verdict rows all `yes` on Linux and on the guest; each row's class per outcome is the table's
`Class` column and each row is named exactly once in every outcome equation
(`every_outcome_equation_names_every_row_once`); R21's answer and `.partial` files retained at Complete,
the owner and commit records present, R27 balanced with the `fsck`-inventoried unreachable objects
monotone and nothing released missing, R11 retained on Parked and BudgetExceeded (repair sources), R28
present in every table. **And an executed negative**: the question asks whether *every* owned resource
has exactly one class and one row; the report's staging directory after the power-loss sequence of
`PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION` (executed here, `cargo exit=101`, §3.4) ends
with no row, no class and no reclaim — an engine-created directory permanently outside the
accounting — and the recorded leftover itself has no residue class (§9.2). Entitlement grants per
generation/transaction (R1, R2, R5, R7, R8) satisfy the equations in every table; the per-process
grants (R3, R4, R13, R17, R22) are G6's. **Answered in the negative for G5's portion**: the enumerated
rows hold at every outcome, and one resource of the run's own making is outside them under the crash
model the gate certifies. It is why clause 7 fails (§13).

### Q3 — stale completions

**G5's portion:** "can a returned append error let a stale in-memory fold drive any later mutation,
retry, report or cleanup?" The stale-completion, slot-release, cancel and provisional-conversion
halves are G3's and G4's. **Evidence:** `an_append_error_at_the_run_ending_close_ends_the_command_and_the_next_resume_closes_the_generation`
(the append errors at `Written`, at `WrittenFull`, and at `WrittenFull` with the replacement barrier
failing — the undetermined outcome named as such — the command ends with the fold poisoned and
nothing removed, and a fresh resume converges), `append_error_inside_closure_ends_command_and_resume_completes_closure`
(poisoned fold, no report, root present, the surviving prefix unchanged), the NoRunFinished ledger
(§7: R17 released with the process, R9/R18/R23 retained, nothing derived), `every_error_return_point_of_the_event_funnels_fires_under_the_production_adapter`;
`closure-swallows-close-append-error` and `closure-swallows-undetermined-close-append` kill their
tests. **Answered with evidence for G5's portion.**

### Q4 — aliasing

**G5's portion:** a resume writing into a private half that is not provably its own; a fast
publication naming any object other than the candidate commit; and this range's new name — the
report's staging directory. **Evidence:** the husk tests of §8 row 3 (private halves reclaimed only
under the ownership proof and only without a commit record; the unprovable and the possibly committed
retained and reported), round 1's census witness over the killed fixtures (13 prefixes, each at
ST-19's disposition, §3.4), `fast_path_publishes_exact_candidate_without_staging_or_proposal_object`
and the census's fast relations in both directions (§4; `g5-fast-publication-other-head-accepted`
killed, the proposed-SHA relation refused by the wire type's own check), the staging directory named
uniquely per write and reclaimed only by its record (`a_dead_writers_staged_report_is_reclaimed_by_the_next_write`,
`legacy_report_preserves_unowned_staging_directory`; `staging-reclaimed-without-a-record` and
`staging-created-before-its-record` killed) — with its power-loss window (§9.2) the one executed case
in which a directory of the run's own making ends unowned. The path-lease, worktree, snapshot,
container-name, entitlement, slot, candidate, queue-position, sequence and settlement-slot halves are
G2–G4's. **Answered with evidence for G5's portion, with the power-loss case named.**

### Q5 — effect/append boundaries

**G5's portion is the whole sequential inventory** (§5). What is established by execution: both hook
phases and every parent-side point of the 68 sites *observed* executed by the suite — the bijection
over the export with 0 failures at host `unix` and at host `windows` (the latter a merge check PR10
never ran), 180 entries with a registry entry for every observable order (one per site, measured) and
the test that executed each; the Event sites' torn-tail, unsynced, synced and returned-error points
and OpenLog's `SyncPrefix` point each executed in the modes the registry names (§5.2) — their recovery on
ST-07's three obligations is established at 1 of the 21 Event points and not at 20 (R 10, O 10; §5.3); the residue classes recovery-proven per element and sampled with every residue
classified and recovered, on this run, on both hosts (§5.4), never claimed as parent-observed
execution; the clippy disallowed list, the allow-placement scan, the wrapper classification, the
funnel-module allowlist and the frozen legacy allowlist held by the 171 `effects::` tests passed in one
invocation at `caf6bed0` (`groups/group8-effects-censuses-q5.log`). What is **not** established:
"executed evidence" in ST-07's sense at 140 of the 168 observed coordinates (§5.3 — 86 constructed and
recovered by committed tests that assert no replay-twice equality, 54 with no committed test that
constructs the prefix and performs the tabled action; round 1's head said nine and round 2's forty, on
audits round 2's and round 3's lenses found over-credited); and a review of the locked dependencies' effectful APIs, which no test
performs — the range adds no dependency (§5.1), so nothing was there to review, and the first head's
sentence claiming the censuses read `Cargo.lock` is withdrawn. The report-site leftover is the one
residue the registry's vocabulary does not hold (§9.2). **Answered with evidence for the observed
inventory and for the 28 coordinates a committed test constructs, recovers and replays twice (4) or
constructs and recovers with nothing to replay (24); not answered for the 140, and the dependency review
is not this gate's evidence.**

### Q6 — sequential guarantees

**G5's portion:** at max_parallel = 1 there is one schedule, so "identical canonical authoritative
trace where scheduling must not matter" has nothing to vary and is **not measured**; what is
measured: one fold and one writer (the barrier is the only way a log becomes a fold —
`the_stable_prefix_barrier_is_the_only_way_a_log_becomes_a_topology_fold` in the green suite; the
run lock refuses a second process, `g5-run-lock-shared` killed); the untouched user checkout (the
acceptance test's `HEAD`, tracked bytes, status, and every untracked and ignored file digested;
`acceptance-untracked-note-overwritten` and `acceptance-ignored-note-overwritten` killed); exact-base
fast publication of the candidate object itself (Q4); atomic settlements (`a_closed_settlement_scrubs_the_generations_worktree_and_intent`
reads the append durable before the scrub's first effect from one timeline; `settle-scrub-before-close`
killed); the derived run outcome from durable inputs with halt > budget > parked > complete
(`closure::ending_outcome`, `st17-budget-outranks-halt` killed) and pending backoff never blocking
Halted or BudgetExceeded (`st17-halted-with-backoff-refused` killed); `budget_exceeded` recorded before
any budget-driven end (`g5-breach-ends-without-budget-exceeded` killed); closure and finalization
(§8 rows 4–5, with finalization's convergence across the third resume and the power-loss window as
§13 grades them); no answer ingestion after a halt or a budget stop in the same epoch
(`g5-answer-accepted-under-budget-stop` killed) and ingestion only by the resume and only for
still-open questions (round 1's witness and M08, §3.4); one Runner identity per run, recorded
before the first probe and honored by every resume (§8 row 7's ST-20 tests;
`g5-run-resumed-runner-difference-ignored` and `g5-drift-warning-dropped` killed). FIFO integration
and no overtaking are G4's. **Answered with evidence for G5's portion; the scheduling-invariance
clause is not measured at width 1 and is said not to be.**

## 11. The ten gates at this report's own head

The ten gates were run at `caf6bed0` before anything else (§1). They were run again at each commit
that carries a version of this report, and once more at each commit that adds its gate rows to this
table — each such second commit differs from its first by this section alone, or by this section and §9.4's
flake rows where one of the first commit's attempts reddened (`00616451`, and round 3's head) — and every head is
recorded here by the last commit (the table below), with the logs under `gates/`; from round 2 on,
each attempt's `uptime` and the box's long-running processes are saved beside its log
(`gates/box-load-<sha7>.log`). §1's restore discipline applies:
every temporarily edited file was restored from its `cp` backup and proven equal to `HEAD` by sha256
before the report head was built (`tmp-tests/phase.log`, `tmp-tests-2/phase.log`,
`tmp-tests-3/phase.log`, `tmp-tests-4/phase.log`), and `git status --porcelain` showed only the file under `reviews/` this
branch adds. The first head's two
commits (`71ca4862`, `0642c87f`) and their gate logs (`gates/w1-eight-iso-head{1,2}-*.log`,
`gates/eight-logs-{71ca486,0642c87}/`, `gates/gate10-head{1,2}-*.log`) are kept as they were.

| Head | What it is | Result | Logs |
|---|---|---|---|
| `e908c7cf48aa85349a5f8732c142783a0e79f534` | this report after repair round 1, committed on `gate/g5-run1` on top of `0642c87f` | **three attempts of the nine-command baseline**: attempt 1 (21:22:20Z) **FAIL** on the test leg alone — a real rebuild of the tree (`Compiling upstroke v0.1.0 (/srv/worktrees/g5-run1)` in its log), 2589 passed, **1 failed** (`a_host_integration_reaper_holds_the_runs_cleanup_lease`, "the hold outlived the reaper that took it", `src/engine/topology/recover/tests.rs:8799`), 73 ignored; attempt 2 (21:25:01Z) **FAIL** on the test leg alone — 2588 passed, **2 failed** (the same test with the same message, and `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner` refused at `recover/tests.rs:8355` with the cleanup-lease fingerprint: "still has a process of its own alive … an agent-cleanup reaper … holds the run's cleanup lease"), 73 ignored; attempt 3 (21:27:40Z) **`ALL 9 PASS at e908c7c`** — **2590 passed, 0 failed, 73 ignored** of 2663, plus the 10 binary tests, the same counts as §1's green runs; the other eight legs passed in every attempt. **Both reds are one fingerprint** — a gate's reaper still holding the run's cleanup lease when the test looks; the box load at those attempts was not saved to a log, so the load and helper-age figures round 1's head gave here are withdrawn (round 2's gate runs save `uptime` and the long-running processes into `gates/box-load-<sha7>.log`) — and **each test passed 5 of 5 alone** between attempts 2 and 3 (`flakes/alone-e908c7c-*-run-{1..5}.log`, `flakes/alone-e908c7c-summary.log`); §9.4 records them. The tenth gate: `test-pr-ready-audit: ok` | `gates/w1-eight-iso-r2head1-e908c7c-run1-failed.log`, `-run2-failed.log`, `-run3.log`; `gates/eight-logs-e908c7c-failed-20260914T212420Z/`, `eight-logs-e908c7c-failed-20260914T212605Z/`, `eight-logs-e908c7c/` (attempt 3); `gates/gate10-r2head1-e908c7c.log` |
| `00616451c5c103afe2d221f354f5ca2dac009264` | round 1's head: `e908c7cf` plus its §11 rows and two §9.4 rows | `ALL 9 PASS at 0061645` at the first attempt (fmt 1s, clippy 1s, test 50s, msrv 0s, the five scripts), **2590 passed, 0 failed, 73 ignored** plus 10; `test-pr-ready-audit: ok`; CI at that head green on every job (`ci/ci-at-pushed-head-0061645.txt`) | `gates/w1-eight-iso-r2head2-0061645.log`, `gates/eight-logs-0061645/`, `gates/gate10-r2head2-0061645.log` |
| `368e4fffc435d66f791693ce06a6c9070a47ac2d` | this report after repair round 2, committed on `gate/g5-run1` on top of `00616451` | `ALL 9 PASS at 368e4ff` at the first attempt (22:44:26Z; fmt 1s, clippy 22s, test 95s, msrv 18s, the five scripts) — a real rebuild of the restored tree (`Compiling upstroke v0.1.0 (/srv/worktrees/g5-run1)` in its test log): **2590 passed, 0 failed, 73 ignored** of 2663, plus the 10 binary tests, the same counts as §1's green runs; `test-pr-ready-audit: ok`. Box load at the start, measured this time: load average 17.34 over the last minute (other sessions building), with the same two 43-hour-old helper processes of another session on slot 1 (`gates/box-load-368e4ff.log`) | `gates/w1-eight-iso-r3head1-368e4ff-run1.log`, `gates/eight-logs-368e4ff/`, `gates/gate10-r3head1-368e4ff.log`, `gates/box-load-368e4ff.log` |
| `3649147fae80fa200d0f9145740bc3653cec9202` | round 2's head: `368e4fff` plus its §11 rows | `ALL 9 PASS at 3649147` at the first attempt (fmt 1s, clippy 0s, test 89s, msrv 1s, release-record, pr-policy 15s, pr-ledger, docs-consistency, internals-notes 2s), **2590 passed, 0 failed, 73 ignored** plus 10; `test-pr-ready-audit: ok`; load average 11.12 at the start (`gates/box-load-3649147.log`); CI at that head green on every job (`ci/ci-at-pushed-head-3649147.txt`) | `gates/w1-eight-iso-r3head2-3649147-run1.log`, `gates/eight-logs-3649147/`, `gates/gate10-r3head2-3649147.log`, `gates/box-load-3649147.log` |
| `bf51946f3b3d0528c4524022819a487b25de71dc` | this report after repair round 3, committed on `gate/g5-run1` on top of `3649147f` | **two attempts of the nine-command baseline**: attempt 1 (00:46:13Z, load average 48.44 at its start) **FAIL** on the test leg alone — a real rebuild of the tree (`Compiling upstroke v0.1.0 (/srv/worktrees/g5-run1)` in its log; clippy 26s, msrv 23s), 2588 passed, **2 failed** (`a_host_integration_reaper_holds_the_runs_cleanup_lease`, "the hold outlived the reaper that took it", `src/engine/topology/recover/tests.rs:8799`, and `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner` refused at `recover/tests.rs:8355` with the cleanup-lease message), 73 ignored — the two tests and the two messages of round 1's attempt 2 at `e908c7c`; **each test passed 5 of 5 alone** between the attempts (`flakes/alone-bf51946-*-run-{1..5}.log`, `flakes/alone-bf51946-summary.log`); attempt 2 (00:51:20Z, load average 51.10) **`ALL 9 PASS at bf51946`** (fmt 1s, clippy 1s, test 103s, msrv 0s, release-record, pr-policy 11s, pr-ledger, docs-consistency, internals-notes 1s) — **2590 passed, 0 failed, 73 ignored** of 2663, plus the 10 binary tests; the other eight legs passed in both attempts; §9.4 records the fingerprint. The tenth gate: `test-pr-ready-audit: ok` | `gates/w1-eight-iso-r4head1-bf51946-run1-failed.log`, `-run2.log`; `gates/eight-logs-bf51946-failed-20260915T004923Z/`, `eight-logs-bf51946/` (attempt 2); `gates/gate10-r4head1-bf51946.log`; `gates/box-load-bf51946.log` (both attempts) |
| the head this branch pushes | that commit plus this table's two rows, this section's opening paragraph, §9.4's record of that commit's first attempt and the report's sweep counts in §0's class-rule paragraph (that commit's row adds one line to round 2's load pattern), and nothing else (`git diff --stat` between the two is this file alone) | gated the same way — ten gates — before the push, every attempt kept and its box load measured; its logs are named beside this table's in `gates/` (`w1-eight-iso-r4head2-<sha7>-run<n>.log`, `eight-logs-<sha7>/`, `gate10-r4head2-<sha7>.log`, `box-load-<sha7>.log`) | `gates/` |

## 12. The evidence directory, indexed

`~/tactus-artifacts/g5-evidence-caf6bed/` on the build box; `MANIFEST.sha256` lists every file with
its sha256 and `README.md` is this index in place. Nothing in it is committed; the committed report is
copied in as `report-<sha7>.md` for the head this branch pushes, and the earlier heads' reports are
kept as `report-round1-0642c87f.md`, `report-round2-0061645.md` and `report-3649147.md` (round 2's head).

| Path | What it holds | Cited in |
|---|---|---|
| `spec/` | the definition, the questions, the amendment, the lens, the brief, #279's body and metadata, the ST-07..ST-20 seam-test statements extracted from the packet, `packet-check.log` (the packet's hash and the extracts' clauses checked verbatim), `packet-bounds-line.log` and `packet-bounded-census.json` (the bounds line and the `bounded_census` block extracted from the packet itself, round 2), `platform-versions-host.log`, the posted bodies of each head (`g5-pr-body-as-posted.md`, `g5-pr-body-round2-as-posted.md`, `g5-pr-body-round3-as-posted.md`, `g5-pr-body-round4-as-posted.md`), `class-searches.sh` with its logs (`class-searches.log` for this head, `class-searches-round2.log` and `class-searches-round1.log` for the earlier heads, round 2's script as it ran kept as `class-searches-round2.sh`), `class-searches-tally.py` with `class-searches-tally.log`, `table-rows-check.py` with `table-rows-check.log`, `SHA256SUMS` (the inputs' hashes) | §0, §2, §4, §8 |
| `build/prewarm-gate-target.log` | the gate target's pre-warm (`Compiling upstroke v0.1.0 (/srv/worktrees/g5-run1)`) | §1 |
| `gates/` | both `w1-eight-iso` attempts at the range with their nine logs each, the tenth gate, `binary-provenance.log`, the gate runs at every report head's commits (every attempt kept, failed ones named so), and from round 2 the box load beside each (`box-load-<sha7>.log`) | §1, §11 |
| `flakes/` | the descriptor-hold test alone, ten runs | §1, §9.4 |
| `docker/` | the 23 Docker-gated tests under `UPSTROKE_REQUIRE_DOCKER=1` (`real-docker-required.log`) and the two reds alone, three runs each (round 1) | §1, §9.4 |
| `full-suite-export.log`, `full-suite-export-run2.log`; `observations/`, `observations-run2/`; `ledger/`, `ledger-run2/`; `census/`, `census-run2/` | the two export runs and what they wrote | §1, §4, §6, §7 |
| `st07/` | the Linux merge check, its summary and the registry pin | §5.2 |
| `guest/` | the guest's build, full suite, exports (`exports/`), merge check and pin, sampler loops (`loops/`), the batch files run there, and `guest-platform-versions.log` (round 1) | §1, §5, §8 |
| `samplers/` | the Linux sampler loops: `linux-loops-a.log` (PR5's, the ST-07 and the cherry-pick samplers, five each) and `linux-loops-b.log` (the repair-materialization and the add-and-write-tree samplers, five each, round 1), with one log per run | §5.4, §8 |
| `inventory/` | the site-enums export, the pinned registry, `clippy.toml`, the allowlists, the wrapper classification, the funnel-module record, the residue declarations and synthetic half, the Event/Report/Process entries listing; `per-entry-recovery-audit.md` (round 3's audit on ST-07's three obligations, every citation with file and line, and the fix table), `per-entry-recovery-audit-round3-test-facts.md` (what each of the 112 cited test bodies constructs, performs and asserts), `round3-generator/` (the hand grades and the generator that checks every citation against `HEAD`), `per-entry-recovery-audit-round2.md` and `per-entry-recovery-audit-round1.md` (the earlier audits, withdrawn), `per-entry-evidence-tests.txt`, `evidence-tests-keyword-profile.txt`, `armings-scan.txt`, `orders-per-site.txt` | §5.1, §5.3, §9.2 |
| `residue/` | the residue-class evidence record (regenerated in round 1 with three producers) and the histograms of this run, each labeled with its producer and run; `provenance/` (the three writing samplers run alone with their files copied beside the logs, round 1); `st07-alone-export/` | §5.4 |
| `mutations/` | the two recipe files, the CHECK passes (`check-pass.log`, `check-pass-g5.log`; the retrospective `check-pass-g5-round2.log` and `check-pass-g5-round3.log` with `check-round3/`), the chunk logs, the consolidated summaries, the two tables, `runner-diff.log`, and one `.log`/`.diff`/`.backup` per recipe under `run/` and `run-g5/` | §3 |
| `tmp-tests/` | the first head's four temporary witnesses' sources, the Q1 probe, the apply and restore scripts, `phase.log`, one log per witness, `run-q1-probe.log` | §3.4, §6, §9.2 |
| `tmp-tests-2/` | round 1's three witnesses' sources (`recover-tests-append.rs`, `startup-tests-append.rs`), the apply and restore scripts, the backups, `phase.log` (the restores' sha256 against `HEAD`), one log per witness | §3.4, §8 |
| `tmp-tests-3/` | round 2's corrected power-loss witness (`rundir-tests-append.rs`, superseded), the prescribed fix as applied (`prescribed-fix.diff`, `prescribed-fix-applied.diff`), both backups, `run-unfixed.log` (`cargo exit=101`), `run-fixed.log` (`cargo exit=0`), `phase.log` (the two files' sha256 against `HEAD` before and after) | §3.4, §13 |
| `tmp-tests-4/` | round 3's power-loss witness (`rundir-tests-append.rs`, the loss model that requires a barrier after the observed deletion), its driver `phase4.py`, the prescription (`prescribed-fix.diff`), the negative control (`barrier-before-deletion.diff`, `barrier-before-deletion-vs-prescribed.diff`), round 2's prescription (`round2-prescription.diff`), both backups, the five runs (`run-unfixed.log` 101, `run-prescribed.log` 0, `run-barrier-before-deletion.log` 101, `run-barrier-before-deletion-round2-model.log` 0, `run-round2-prescription.log` 101) and `phase.log` (the two files' sha256 against `HEAD` before and after, `git status --porcelain` empty) | §3.4, §13 |
| `groups/` | the nine group invocations' test lists, runner and logs; `test-list-caf6bed.txt` (every test the binary lists), `filter-check-round2.txt` (every group filter matched against it), `group-tests.txt` (corrected) and `group-tests-round1.txt`, `group4-st17-closure.log` (13 passed) and `-round1.log` | §2, §8 |
| `names/` | the record's §7 names checked against the tree | §2 |
| `ci/` | the three CI runs at the range as JSON, the summary, the macOS job logs, and the CI result at each pushed head | §1 |
| `report-round1-0642c87f.md`, `report-round2-0061645.md`, `report-3649147.md`, `report-<sha7>.md` | the first head's report, round 1's, round 2's and this head's, byte-identical to the commits they name | — |

## 13. Verdict against the pass rule, clause by clause

The rule, verbatim from the amended definition (`spec/G5-DEFINITION.txt`, equal to the packet's):

> pass iff bounded census family complete (the prefix at its stated ceiling with truncation reported,
> every seeded per-dimension census closed, every bound reached by equality over the union) with
> totality asserted over the union and every plan_transition arm covered, site bijection complete
> with executed evidence for every Topology/Shared site, order, and parent-executed point (incl.
> OpenLog SyncPrefix and the stable-prefix barrier refusal cases), the two-crash proof green,
> residue-class evidence complete and labeled, all rows green, projections equal, per-resource
> ledgers satisfy their outcome equations incl. derived outcome, closure, and finalization,
> questions answered, no open critical/high finding; unbounded closure, effect-phase census, and
> parent-observed Git-internal execution claims are not made

Counted here: **nine** positive clauses and **three** prohibitions. Each positive clause is judged
**established** or **not established** against *this run's* evidence at `caf6bed0`; "established"
means an execution this report names produced the values it quotes and, where the claim is
detection, a mutation this report names was observed to kill the test that owns the claim; a clause
one executed counterexample contradicts is not established, whatever else holds. Each prohibition is
judged **not made** or **made**. For every clause not established, the row names **the exact tree
change, or the decision, that would let it hold**, so that a fix pull request can be cut from
`origin/master` and the gate re-run on the corrected range as run 2.

| # | Clause | Verdict | How, and where | What would let it hold |
|---|---|---|---|---|
| 1 | **bounded census family complete** — the prefix at its stated ceiling with truncation reported, every seeded per-dimension census closed, every bound reached by equality over the union — **with totality asserted over the union and every `plan_transition` arm covered** | **not established against the packet's bounds line; established for the tree's** | §4: the prefix at 20,000 of 20,000 states, `truncated: true`; the one seeded census closed at 25 states under its ceiling of 5,000, no truncated offer; `union_reaches` equal to the **tree's** bounds on all ten dimensions and `every_bound_reached_over_the_union: true` — the summary byte-identical on Linux twice and on the guest; the closing, dimension, totality, arm-coverage and classification tests green in one invocation; the census recipes killed. **Two dimensions read differently in the unamended packet** (§4, `spec/packet-check.log`): the packet's "verification defers per candidate <= max_defers (fixture value 2)" against the tree's `defers: 1` asserted as `bounds.defers + 1 == max_defers` (`src/topology/census.rs:2638`; the second outage parks, so a second defer is unreachable at width 1, the reading PR10's record gave), and the packet's "plan shapes: chain, fan-out, diamond" against the tree's `Chain`, `FanOut`, `Join` (`PlanShape`, two independent tasks then a third, `src/topology/census.rs:389`), with the family explored over `FanOut` and the other two shapes explored separately at 3,000 states truncated (`every_plan_shape_is_explored`). The amendment of 2026-09-13 changes the closure requirement and neither bound. The unchanged text does not support "every bound reached by equality" on those two dimensions; the tree's reading is not adjudicated here | **the owner's decision on the packet**, not a tree change: a packet correction would have to say that verification defers per candidate are bounded by `max_defers − 1` at width 1 because the second outage parks the candidate, and that the third plan shape is a three-node join (two independent tasks then one depending on both) rather than a four-node diamond — or, if the packet stands, the tree must reach two defers and explore a diamond, and the family be re-exported |
| 2 | **site bijection complete with executed evidence for every Topology/Shared site, order, and parent-executed point** (incl. OpenLog SyncPrefix and the stable-prefix barrier refusal cases) | **not established for 140 of 168 observed coordinates; established for 28 (F 4, N 24), and for the bijection** | §5.2–5.3: the merge check over this run's export — 68 sites, 180 entries, one observable order per site (measured), no coordinate excused — **0 failures at host `unix` and 0 at host `windows`**; the Event sites' every point in every mode, OpenLog's `SyncPrefix` in both modes, the barrier's refusal cases and its order before any fold-derived effect, and both `IdUnread` points are executed by named tests, and recipes that drop a point's consult, a predicate, an entry, or place an effect before the barrier are each killed. **"Executed evidence" in ST-07's own sense — the exact durable prefix and residue constructed, resume performing the tabled action, replay twice equal — holds at 28 coordinates and not at 140** (`inventory/per-entry-recovery-audit.md`, round 3; §5.3): **86 (R)** are constructed and recovered by committed tests that assert no replay-twice equality — only two of the 112 cited tests assert it — among them all 26 ST-18 finalization coordinates, the kill children's settlements, 10 of the 21 Event points including `OpenLog`'s `SyncPrefix` in both modes, and all 16 container coordinates; **54 (O)** have no committed test that constructs the prefix and performs the action: round 2's 40 (the execution root's creation both phases, the dispatch's intent-without-worktree, the staging path's start and its clean staging worktree, the snapshot intent without its worktree, the candidates ref before its event, the staged index, the candidate commit-tree before its object, the question payload present, two open-log error-return points, the first line's `synced` kill, the informational append's phases and four points, the answer's ingestion after its append, the worktree lease after its acquisition, the worktree lock file, and the whole process family) and 14 that round 2 credited to a classification, a plan, a husk left for a later census nobody runs, or a fixture one site off (§5.3 names them). Round 1's head said nine and round 2's forty; the audit on the three obligations is the one cited. The one residue between `Report.Write`'s two phases with no coordinate in the frozen inventory is clause 4's | **tree change, tests only**: at each of the 140 coordinates a committed test meeting all three obligations — the coordinate's exact durable prefix constructed (a kill child or an injected error return at the site's hook phase or point through the production adapter, or the residue planted), the tabled action performed by the recovery and asserted, and the durable log replayed twice equal after it — the file each belongs in and its shape is the audit's fix table (`inventory/per-entry-recovery-audit.md`, "The coordinates a fix pull request must witness"). In summary: **the 86 R rows** need the replay-twice assertion added after the recovery at 30 sites in 31 committed tests — `kill_after_report_before_each_cleanup_step` alone closes 23, and the two `events::log` sites repeat their kill on a log that replays — and one new resume of the container launch, release and census states in a run with a log (the container unit fixtures carry none); **the 54 O rows** need a construction and a recovery as well: round 2's shapes, each now ending in the replay-twice assertion — one arm added to `dispatch_kill_child` ("after_intent"), three to `attempt_kill_child` ("after_stage", "after_snapshot_intent", "during_process"), two to `candidate_kill_child` ("before-commit-tree", "after-candidates-ref"), one to `drive_into_the_kill` ("p6synced"), each with the parent driving the existing recovery; two recover tests (a stale queued candidate with nothing staged resumed through the staging path; a clean staging worktree at the head reclaimed); an error return at `Worktree.CreateExecutionRoot`/After in a live start and at `Lock.AcquireWorktree`/After in `resume_with`, each followed by a resume; two `events::log` open tests arming `Create` and `TruncateTornTail` in error-return mode; the informational append added to `every_append_site_reaches_the_protocol_under_its_own_site`'s `Written` and `WrittenFull` points and to the site-parameterized kill helper; the worktree lock file's injected errors and planted absence; the process family in `src/engine/topology/coverage/tests.rs` and `src/agent/proc/tests.rs` — and for the 14 round 3 withdrew: `Fixture::healthy` with its marker removed (P7) and with the ref at the base (P8), each resumed; the settlement performed after `recovery_for` in the candidate kill tests (`Ref.PinCandidatePrepared`/before, `Object.CandidateCommitTree`/after and its `IdUnread` point) and after the snapshot `IdUnread` kill; an unpinned-proposal staging plant; the question payload's kill in the schema-1 engine resumed through `engine::resume`, the only production writer of the payload at this range; a second census after the refused private-half removal and after a completed reclaim; a reopen after the `Create` and `TruncateTornTail` kills; a census and an open over the torn first line's kill — **the cheapest are the gate's witness A, which meets all three as written, and B, which needs its replay-twice assertion** (`tmp-tests-2/recover-tests-append.rs`); and every new witness cited in `effects/sequential-registry.json`'s `evidence` so the pin holds it. **A fix pull request that implements round 2's 40 alone leaves 100 coordinates unwitnessed** |
| 3 | **the two-crash proof green** | **established** | §5.3: both tests green at `caf6bed0` in group 2 — the child's report `synced <prefix>` then `cas`, the ref at `proposed_sha` and the log agreeing after the loss, the next resume recording without swapping; the negative variant: `SyncPrefix` named, no CAS, nothing appended, the candidate still queued and integrated after the loss; `g5-open-sync-skipped` kills the positive proof and `g5-sync-prefix-point-not-consulted` the negative | — |
| 4 | **residue-class evidence complete and labeled** | **established for the packet's nine residue classes, with one residue outside them named** | §5.4: the nine Object internal residue classes each have synthetic evidence per element (constructed, classified `internal`, recovered) and a kill-sampling record on this run — the three writing samplers with their provenance, Linux and the guest — with every observed residue classified and recovered (`unclassified: 0`, `recovered: true` on every row) and the declared and sampled sets equal; every class labeled recovery-proven, no observed-execution claim; `residue-synthetic-pin` and `g5-residue-frozen-n-changed-pin` kill the pins. **The report-site leftover (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`) is a residue of the run's own making that is not an Object site's and has no class**; its reclamation is executed (§3.4) and its power-loss window confirmed (§3.4); it is stated, not covered, and its accounting consequence is clause 7's | the clause holds as the packet words it; **to hold with no exception**: a residue class for the recorded leftover in `src/topology/effects/residue_authority.rs` (the `Report.Write` and `RunDir.WriteReport` before-phase rows, today `rows: []`) with its element constructed in `effects/residue-synthetic.json` and the pinned registry regenerated (`the_checked_in_residue_class_record_is_what_the_enums_generate`) |
| 5 | **all rows green** | **established** | §7: R1–R28 and every named part, 54 verdicts per table, six tables, **324 of 324 `yes`** on Linux and the same on the guest; the cumulative suite green on the exact range on Linux (four full runs, two of them green outright and two red on one filed flake each, every red green alone) and on the guest (§1). CI at the sha: every job green in the merge-queue run; the newest run red on macOS alone on a filed P2 flake in G2's portion (§1, §9.4) — stated as the qualification it is. The rows establish what they enumerate, not that every resource has a row (clause 7) | — |
| 6 | **projections equal** | **established** | §6: 8 full-`RunState` equalities and 8 report-digest equalities after every step of two driven runs, 6 per-prefix digest equalities, all equal, printed and saved; the committed test's one-live-snapshot-per-durable-prefix assertion; three projection recipes killed | — |
| 7 | **per-resource ledgers satisfy their outcome equations incl. derived outcome, closure, and finalization** | **not established** | §7 and §8 rows 4–5: every equation satisfied at Complete, Parked, Halted, BudgetExceeded and NoRunFinished (live and after the drop) **for the rows the equations enumerate**; the derived outcome equal to the recorded one and every listed refusal executed; closure per class. **Two executed facts of the tree's own contradict "finalization" and the equations' completeness**: (a) after the power-loss sequence of `PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION` (§3.4, `cargo exit=101`) a directory the engine created stands with no record, no row, no class and no reclaim on any later resume — an outcome no equation names, on the crash model the gate certifies; (b) the committed matrix `kill_after_report_before_each_cleanup_step` passes with the fresh branch's reclaim replaced by a removal of the record alone (`r11-third-resume-leftover-record-only`, `rc=0`), so the committed evidence that terminal finalization converges across the third resume is the gate's own uncommitted witness (`PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION`). The behaviour at (b) holds at this range and the behaviour at (a) does not | **tree change**: (a) `src/rundir.rs` `reclaim_report_staging` (line 1520): after the match on the recorded name — every arm, the directory removed, something else left as found, or nothing there — take the public directory's barrier **observing that name**: `sync_dir_observing(public, &recorded, ledger)`, which reads the name's presence immediately before `util::fsync_dir` and records the `SyncedDirectory` entry with `util::EntryObserved { path: recorded, present }`, as `workspace_manager::sync_checkout_removed` does — **before** `remove_file_if_present(&report_staging_record(private))`, the ledger passed in from both callers (`write_report`'s staging path at line 1263 and `sync_report_dir` at 1338); and `publish_report` (line 855): `sync_dir(published.parent())` **before** `fs::remove_file(record)`, so that on either branch the record outlives the public deletion it names (`tmp-tests-4/prescribed-fix.diff`); witnessed by committing the gate's **round-3** `g5_gate_a_reclaimed_report_stage_remains_reclaimable_after_power_loss` (`tmp-tests-4/rundir-tests-append.rs`, whose loss model credits A's deletion only to a barrier that observed A absent) in `src/rundir/tests.rs`: executed here, it **fails at `caf6bed0` unchanged (`cargo exit=101`), passes with exactly this change (`cargo exit=0`), and fails with the barrier moved before the deletion (`cargo exit=101`)** — the negative control the fix pull request carries as a mutation witness (§3.4, `tmp-tests-4/run-*.log`). Round 2's version (`tmp-tests-3/rundir-tests-append.rs`) passes that misplaced barrier (`cargo exit=0`, round 3's regression lens, reproduced in `tmp-tests-4/run-barrier-before-deletion-round2-model.log`) and the first head's (`tmp-tests/`) recreates A unconditionally; **neither is the test to commit**. (b) `src/engine/topology/recover/tests.rs` `kill_after_report_before_each_cleanup_step` (line 15548), the third-resume block after `plant_report_leftover(fixture)` (line 15637 of the loop body): keep the path the planter returns and assert, after the third resume, that the planted file, its directory and `report_staging_record(private)` are gone — the assertion the gate's `g5_gate_next_resume_reclaims_a_recorded_leftover_on_the_fresh_branch` makes (`tmp-tests/recover-tests-append.rs`), which fails under `r11-third-resume-leftover-record-only` where the matrix passes. (c) The swallow witnesses for `begin_report_staging`'s two private-record barriers in `src/rundir/tests.rs` beside `a_report_staging_directory_is_recorded_before_it_is_made` (line 5590), killed by `r11-staging-record-publish-swallowed` and `-stage-swallowed`, which survive today |
| 8 | **questions answered** | **established as answers; Q2 answered in the negative** | §10: Q1 by replay of the bytes on disk (14 comparisons) and the barrier tests, with the staging record's loss case named; **Q2 by the ledgers, and in the negative** — one resource of the run's own making ends with no row and no class (the same counterexample as clause 7); Q3 by the append-error protocol tests and the NoRunFinished ledger; Q4 by the ownership proof, the census witness over the killed fixtures, the fast proof and the staging record with its power-loss case named; Q5 by the bijection on both hosts, the strict per-entry audit and the 171 effects tests, with the 140 coordinates without a committed witness on ST-07's three obligations and the absent dependency review named; Q6 by the named guarantees with the width-1 scheduling clause said to be unmeasured. Every question is answered with evidence; the clause does not say the answers must be favourable, and Q2's is not. The earlier gates' portions are not re-claimed | Q2 turns favourable with clause 7's change (a) and clause 4's class |
| 9 | **no open critical/high finding** | **not established** | §9.2: on the plain reading — a filed, unfixed P1 in the range is an open high finding — `PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION` is one, filed on 2026-09-14 against this range's own subject, real (its recipe survives the matrix, executed here) and unfixed. The ledger's vocabulary has no `open` and the owner deferred it with the bearing delegated to this gate; **this report does not read a deferral as a closure**: the finding bears on clause 7's committed evidence, and the clause reads "no open critical/high finding", not "none the owner has not deferred". The other 15 P1s are outside this range's subject and deferred by earlier rulings, as at G4 | **tree change**: clause 7's (b) — the one assertion — closes the P1; its file moves out of `findings/` in the fix pull request per `findings/PROCESS.md` |
| 10 | **unbounded closure … claims are not made** | **not made** | §4 says in as many words that closure of the space under the bounds is claimed of nothing and unbounded closure is not claimed; the prefix is a truncated breadth-first prefix and says so | — |
| 11 | **effect-phase census … claims are not made** | **not made** | §4 and §5: no effect-phase distinction is claimed of the census; effect-phase distinctions are proven by the typed registry alone | — |
| 12 | **parent-observed Git-internal execution claims are not made** | **not made** | §5.4: every residue class is labeled recovery-proven — synthetic construction plus kill sampling — and no sentence in this report claims the parent observed a Git child's internal execution | — |

### The verdict

**G5 FAILS on this range.** Five of the nine positive clauses are established on evidence produced
at `caf6bed0` (3, 4, 5, 6, 8), four are not (1, 2, 7, 9), and the three prohibited claims are not
made. The rule is a conjunction; **PR11 and PR12 stay blocked by G5.**

What fails, in one line each, and what it takes:

1. **Clause 7** fails on two executed facts of the tree's own: the power-loss window that strands a
   reclaimed staging directory outside every equation (`PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION`,
   the gate's witness `cargo exit=101`), and the committed matrix's missing third-resume assertion
   (`PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION`, the recipe `rc=0` where it should be `101`). Two
   small changes in `src/rundir.rs` and one assertion in `src/engine/topology/recover/tests.rs`, both
   witnessed by tests this gate has written and run — the round-3 power-loss witness demonstrated three
   ways, `rc=101` unchanged, `rc=0` under the change and `rc=101` with the barrier moved before the
   deletion (`tmp-tests-4/`) — and the row names them.
2. **Clause 9** fails on the plain reading of "open": that P1 is filed, real and unfixed. Clause 7's
   assertion closes it.
3. **Clause 2** fails at 140 of 168 observed registry coordinates on ST-07's three obligations: 86
   constructed and recovered by committed tests that assert no replay-twice equality, 54 that no
   committed test constructs and recovers; the witnesses, per coordinate with the file and the shape, are
   the audit's fix table (summarized in the row), and their citations in the pinned registry.
4. **Clause 1** fails against the unamended packet's bounds line on the defers bound and the third
   plan shape; that is the owner's decision on the packet (the sentence a correction would have to
   say is in the row) or a tree change to reach the packet's figures.

Three things the failure does **not** say, so they are not read into it:

- **Where a named execution establishes it, the tree's crash/resume behaviour at width 1 is as the
  packet states it; where none does, this report says so and the fix list carries it.** Of the seven
  adversarial groups' checks (§8), every one is established by a named execution **except**: the
  matrix's "kills between every terminal-finalization effect" (the 50 cells inject error returns; the
  equivalence of an error return at a hook phase to a kill there is the record's reading, executed as
  a real kill at one finalization point only — group 1 and group 5 grade it *asserted*); ST-13's
  provisional-reservation lifecycle beyond the fast no-object proof (G3's evidence, not re-run by name
  here — group 7 grades it *inherited*); and the 140 coordinates of clause 2. The three checks the first
  head had reasoned (pruned pins recovered, answers ingested only by the resume and only for still-open
  questions, killed publication prefixes converging under the next census) are executed by witnesses
  that are uncommitted and say so.
- **The evidence is Linux and the Windows guest.** Nothing was measured on macOS beyond reading
  CI's two runs at the sha; the samplers, the merge check and the full suite ran on both hosts.
- **The detection set is the named test, not the full suite** (§3). A recipe killed here is detected
  by the test it names; the eleven survivals are each explained by reading the test, five of them
  re-aimed and then killed, three of them the round-11 findings, three behaviour-preserving or
  otherwise guarded. G4's inherited mutations other than M08 were not re-run and are G4's evidence.

### What a fix pull request cut from `origin/master` should carry, so that run 2 can pass

- **`src/rundir.rs`**: in `reclaim_report_staging`, after the match on the recorded name, take the
  public directory's barrier observing that name — `sync_dir_observing(public, &recorded, ledger)`,
  which reads the name's presence immediately before the sync and records it on the `SyncedDirectory`
  entry, as `workspace_manager::sync_checkout_removed` does — before the record's removal, with the
  ledger passed from both callers; in `publish_report`, sync the report's parent before removing the
  record — the diff is `tmp-tests-4/prescribed-fix.diff`. Commit the gate's **round-3** power-loss
  witness (`tmp-tests-4/rundir-tests-append.rs`,
  `g5_gate_a_reclaimed_report_stage_remains_reclaimable_after_power_loss`) as the regression test: its
  loss model credits A's deletion only to a barrier that observed A absent, and it fails at `caf6bed0`
  (`cargo exit=101`) and passes with that diff applied (`cargo exit=0`), executed here (§3.4). **Carry
  the negative control as a mutation witness**: the same diff with the barrier moved before
  `remove_dir_all` (`tmp-tests-4/barrier-before-deletion-vs-prescribed.diff`) must fail the committed
  witness (`cargo exit=101` here), and the pull request's evidence must show that run. Do not commit
  round 2's version under `tmp-tests-3/` (it passes that misplaced barrier, `cargo exit=0`,
  `tmp-tests-4/run-barrier-before-deletion-round2-model.log`) or the first head's under `tmp-tests/` (it
  recreates A unconditionally and stays red under the fix).
- **`src/engine/topology/recover/tests.rs`**: the third-resume assertion in
  `kill_after_report_before_each_cleanup_step` (the gate's `tmp-tests/recover-tests-append.rs` is that
  assertion standalone); close `PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION` with it. Run
  `r11-third-resume-leftover-record-only` against the matrix afterwards: it must read `rc=101`.
- **The two swallow witnesses** for `begin_report_staging`'s private-record barriers in
  `src/rundir/tests.rs` (`r11-staging-record-publish-swallowed` and `-stage-swallowed` must then kill).
- **The 140 coordinate witnesses** of clause 2's row — the audit's fix table gives each its file and
  shape: the replay-twice assertion added after the recovery at 30 sites in 31 committed tests for 86 of them (the
  ST-18 matrix alone closes 23) and one new resume of the container states in a run with a log; a
  construction, a recovery and the replay for the other 54, of which the cheapest are the gate's
  witnesses A (as written) and B (with a replay-twice assertion added) under `tmp-tests-2/` — each cited
  in `effects/sequential-registry.json`. Round 2's list of 40 is not enough: it leaves 100 unwitnessed.
- **The kill-equivalence obligation of groups 1 and 5**, which no listed repair discharges: a committed
  matrix that **kills** the child at each of the 50 finalization cells through the production adapter —
  `ArmedFinalization::answering(&harness, (site, phase), Injection::Kill)` spawned as
  `finalization_kill_child` is today for one cell (`src/engine/topology/recover/tests.rs`, line 17079) —
  looping `finalization_sites(&outcome)` for both outcomes, asserting `assert_finalization_order` after
  the death and the next resume's convergence (`assert_finalized`) at every cell; **or** the same loop
  executed by the gate itself in run 2 as a temporary witness. **Which the pass rule requires: the
  committed matrix.** The rule's clause is "executed evidence", and the group's words are "kills … between
  every cleanup effect converge"; evidence a future range carries has to be in the tree, where a
  gate-run loop would have to be re-run at every range and is evidence of that run only. The same holds
  for ST-13's provisional-reservation lifecycle: run 2 re-runs G3's lifecycle tests by name
  (`groups/`), since they exist at the range, rather than inheriting G3's record.
- **The recorded leftover's residue class** (clause 4's row), so that Q2 holds with no exception.
- **The packet decision** of clause 1, recorded by the owner in the packet or in the definition
  before run 2 reads it.
- Every one of these reaches `src/` or `effects/`; the residue class and the registry citations reach
  instruments (`effects/`), so the fix pull request is the owner's to merge or carries a delegation
  written for it (`CLAUDE.md`, the first limb).
