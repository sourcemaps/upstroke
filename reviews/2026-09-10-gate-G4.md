# G4 — cumulative review gate: repair execution, retries, lineages (third run, corrected range)

**Gate** G4, *Repair execution, retries, lineages*.
**Frozen input range** `7e0110a13acce525567a7ac43abb016a232d6236` — the merge commit that put pull
request #258 on `master`, confirmed on entry with `git rev-parse HEAD` in `/srv/worktrees/g4r3`,
whose branch `gate/g4-run3` was at that commit with `git status --porcelain` empty. (The brief said
the worktree was detached; it is on that branch. Recorded because it is the sort of small
disagreement that is worth writing down rather than smoothing over.) Every number in this report
binds to that sha unless it names another.
**Platform of every measurement below** `x86_64-unknown-linux-gnu`, kernel 6.8.0-137-generic,
cargo 1.97.1, git 2.43.0, on the build box. Nothing here was measured on macOS or on the Windows
guest; where a claim is platform-shaped this report says so in the sentence that makes it.
**Date** 2026-09-10.
**Evidence** every quoted figure appears in a file this report names, under
`~/tactus-artifacts/g4r3-evidence-7e0110a1/` on the build box.

**Verdict: G4 PASSES.** The clause-by-clause reasoning is §13 and is stated last, as the gate's own
process requires the pass/fail rule to decide after the evidence. Nothing in §13 is a summary of
this header; read it there.

## 0. What this run is, and what moved since the second

This is G4's **third** run. The first two were verified and **both verifications rejected the
report; both were right**:

- **Verification 1** (`~/tactus-artifacts/G4-VERIFICATION.md`) found four of six pass-rule clauses
  merely *asserted*, and a real ordering defect: a released lineage age was reused, so a lineage
  created after a release could overtake an older survivor. That defect was **fixed in this range**
  by pull request #256 (`f3a71c1d`), and the fix is re-measured here (§4.2, §3).
- **Verification 2** (`G4-VERIFICATION-2.md`) ruled that **a gate report cannot narrow its own
  acceptance condition**. Run 2 had written that row 10 "engages no clause of the pass rule". That
  position is **withdrawn explicitly** in §9 below.
- **Verification 3** (`G4-VERIFICATION-3.md`) found the corrected row 10's *per-element*
  classification obligation unmet at `81ee09ef`, on two counts: the synthetic construction planted an
  orphan first and kept it while iterating the remaining elements in one shared store, so no element
  was ever tested alone; and `temporary_object_files` did not scan git's fan-out directories, so
  git's actual temporary file classified `None`. **Both halves are closed in this range** — by
  pull requests #258 and its repair rounds — and §9 establishes that **by running it**, element by
  element, not by citing the range's own commit messages.

Verification 3 also established a great deal at `81ee09ef` — rows 1–9 and 11–13, four pass-rule
clauses, six artifacts, 229 quoted measurement lines traced to logs, the cumulative suite. **None of
that is cited here as evidence.** Every trace, refusal pair, kill, count and classification below was
produced at `7e0110a1` by this run. Where an observation agrees with run 2's it is said to agree;
where it differs the difference is a finding (§11).

### The definition lists thirteen adversarial tests

The packet's `cumulative_review_gates.gates[3].adversarial_fault_tests` holds **thirteen** entries
and nowhere states a count; so does the working extract `~/tactus-artifacts/G4-DEFINITION.txt`. Run
1, its brief and the verification lens all said twelve. They were counted here, from the packet:
**thirteen**. All thirteen are covered in §10, one row each.

### Reading of the remit, and two disagreements recorded

The remit is *"Review repair execution composed with integration transactions and candidates:
lineage lease is the only overlap authority; overrides are the only way an empty ladder runs;
lineage candidates own queue positions but no candidate lease; source objects protected;
materialization objects accounted by the repair worktree then Git incl. the interrupted-command
residue class; answer Q1-Q6."* This report follows the definition and the questions where they and
the run brief disagree, and records the disagreements:

1. **The brief's §5 describes the cherry-pick sampler as it was before this range.** It says
   `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` "schedules all its kills
   from a single unrepresentative probe and can pass eight clean exits, firing its own vacuity
   refusal". In the tree at `7e0110a1` that sampler takes a warm-up plus the **median of three probe
   picks** (`KillBudget::probed`), re-aims inside every pick that completed before its kill, samples a
   whole second batch when no kill landed inside a writing pick, and refuses a run in which none did
   — landed by `f837f4ca`, `62f55943`, `95eece1c`, `77e0eb5d`, `18e75081` and `9f7aafc1`, all in
   range. The cold-probe mechanism the brief names is gone; §10 row 10 records what the sampler did
   here in ten runs.
2. **The brief's §4 lists five findings to carry and two to drop.** One of the five —
   `G4B-O10-REPAIR-MATERIALIZE-SAMPLER-MACOS-KILL-FLOOR` — asks for exactly the repair the range
   landed, so it is **dropped as closed by the range** rather than carried; §11.2 tables it as
   dropped, gives the evidence, and says what that does **not** establish — a rate on macOS, which
   this session could not measure. (The first head of this report said here that it was "carried in a
   re-scoped form", which contradicted §11 and the five finding files, all of which show it dropped.
   The finding files are right; this sentence was wrong and is corrected.)

### The six required artifacts, and where each one is

The packet requires six. Each is **embedded in this report**, with its raw output saved beside the
report's evidence on the build box under `~/tactus-artifacts/g4r3-evidence-7e0110a1/`, so that every
figure quoted here can be found in a file this report names.

| # | Required artifact | Where |
|---|---|---|
| 1 | gate report | this file |
| 2 | lineage/lease trace outputs | **§4**; raw `measurements/meas-run2.log`, split by tag under `measurements/by-tag/` |
| 3 | fault evidence table for G4 sites | **§5**, with §5.1 (two real T-ANSWER kills), §5.2 (the retained generation at the loop) and §5.3 (the answer file across a budget stop) |
| 4 | limit and admission outputs incl. ST-12 | **§6**, with §6.1 (the frozen limit), §6.2 (ST-12's refusals), §6.3 (single-rung exhaustion), §6.4 (the decline) |
| 5 | payload freezing check | **§7**; the frozen spawn JSON in full in `measurements/by-tag/G4C1.txt` and `G4C2.txt` |
| 6 | repair-source protection evidence | **§8**; ref and object readings across five transitions |

Two more evidence sets that are not among the six but that the row 10 obligation needs:
`row10/residue-tests.log` (the per-element classifications, §9.3) and
`row10/where-git-writes-tmp-obj.log` (where git 2.43 leaves a temporary object file, §9.4); the
mutation ledger and its 58 logs are in `mutations/`, and the gate logs in `gates/`.

### Known deferrals carried into this gate, not reopened

`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002` (P1, G3's range, owner-deferred),
`PR249-ANSWER-TEXT-NOT-CARRIED` (P2), `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` (P2),
`PR249-MANIFEST-NORMALIZATION-ALIAS` (P3), `PR249-REFUSED-MANIFEST-HANDOFF` (P3),
`PR80-MACOS-WORKSPACE-SAMPLER-COLD-PROBE-RECURRENCE` (P2),
`PR172-SAMPLER-REFUSED-A-TORN-WORKTREE-LIST-RECORD` (P3),
`SAMPLER-RECOVERY-PROVEN-IS-NOT-PROVEN-FOR-AN-EMPTY-GITDIR` (P2) and
`RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE` (P2, filed on pull request #254, **not in this tree**).
None is reopened, repaired or narrowed. §11 says how each bears on the pass rule and §13 says which
the verdict rests on.

### Method notes that bind the evidence below

- **Nothing was taken on trust from a prior record.** Run 2's report was read for its *shape* — the
  measurement module and the mutation anchors it designed — and every one of those was re-verified
  against this tree (each anchor asserted to occur exactly once) and re-run here. No number, trace or
  classification is carried over.
- **A caution about the carried harness, found by running it.** Two copies of run 2's measurement
  module are saved beside its evidence, and **they are not the same file**:
  `~/tactus-artifacts/g4b-evidence-81ee09ef/g4b_meas.rs` is an earlier draft whose `G4A3` asserts a
  three-lineage publication list, and `recover-tests-with-g4b-module.rs` is the file that produced run
  2's report, whose `G4A3` asserts the two-lineage list, the first step dispatching the survivor's
  repair, and the newer lineage's integration starting only at s5. The draft fails on this range at
  its own assertion (`meas-run1.log`), because three lineages never publish in that fixture — the
  third meets a real conflict. **The module used here is the one extracted from
  `recover-tests-with-g4b-module.rs`**, renamed `g4r3_meas`; `meas-run1.log` is kept as the record of
  the draft, and `meas-run2.log` is the run this report quotes. Recorded because a successor reaching
  for the same harness will otherwise reach for the wrong file.
- **Mutation is the detection test.** A green suite proves the tests pass, not that they still
  detect. Every "test X guards Y" claim below is backed by a mutation of Y observed here to kill X.
  Every mutation ran against the **full** library suite (§3), never a subset.
- **The measurement code is temporary and uncommitted.** Seventeen measurement tests plus one
  ignored subprocess helper (`g4r3_*`) were appended to `src/engine/topology/recover/tests.rs` for
  this run, with the same `three_tasks` flag on the recover fixture run 2 added; read-only
  `eprintln!` probes were added to the two samplers and to the per-element residue test and then
  removed. Every file so edited was restored from a `cp` backup and hash-verified against `HEAD`
  before the report head was built (§1). Their source and every log are kept beside this report's
  evidence.
- **Every live/durable comparison is live against durable.** `live_equals_replay` compares the
  stepped `TopologyRun`'s own fold (`run.fold().state()`) against a fresh replay of the bytes read
  back from `events.jsonl`; `replay_twice_equal` is printed beside it and is the weaker claim.
- **Restores use `cp`, never `git checkout --`.** The measurement module is uncommitted, and a
  `git checkout --` of a file carrying it would have destroyed it silently.
- **Nothing this run constructed is committed, and this report proposes committing none of it.**
  A gate reports; it does not land code. The commit that carries this report changes
  `reviews/` and nothing else: `git status --short` and `git diff --stat HEAD` were read
  immediately before it and every path was under `reviews/`, and the files were staged **by
  explicit path**, never `git add -A`, so that a mutation or a planted probe left behind could not
  ride along. The one construction a successor would benefit from committing — the measurement
  helpers `drive_keep` and `live_vs_replay`, and a counting question-id source for the driven
  recover fixture — is a **recommendation for a later pull request** (§13, §11 O2), deliberately not
  made here, and the gate's evidence does not depend on it: every value this report quotes was
  produced by running the construction, and the construction's source is preserved beside the
  evidence so anyone can re-run it.
- **Verify by hash, not by line count.** The mutation driver asserts each anchor occurs exactly
  once, that each mutated file's hash differs after the edit, and that it equals the pre-mutation
  hash again after the restore, before the next mutation.

### The second measurement round, and what it corrected

The first head of this report, `dc087837`, was verified and returned `CHANGES_REQUIRED` on four
counts. **Four of six pass-rule clauses were already `established`** — no lease double release,
ordering, source protection and the finding ledger — and nothing in this round touches them. The
other two came back `asserted`, and both are fixed here **by producing the evidence, not by
narrowing the sentence**:

| The verification's finding | What this head does |
|---|---|
| Row 12's T-RETRY kill is of the wrong state: the cited child kills an **already-started** retry (`attempt_started` appended, `InFlight{attempt:2}`), not the boundary the packet names | **A kill at the boundary was constructed and run** — §5.4. Row 12's T-RETRY site stays *ran*, on a different witness |
| Q1 claims comparisons at states none of the 19 lines shows: two live lineages, a blocked-queue reason, an active budget stop | **The comparison was taken at the intermediate states** — §4.5. Q1 now names the lines that show each |
| Q3 presents lease-**disposition** refusals as evidence about generation/attempt **identity** | **Four identity-varying refusals with a valid disposition were constructed and run** — §5.2. Q3 now cites those |
| §4.2 quotes a merged SHA from the rejected draft measurement run | Corrected, and **every** quoted figure was re-checked for the same defect — below |

**The sixth round: the behaviour-claim sweep.** The fifth verification returned all six clauses
`established` and three mismatches of **one** class, distinct from the fifth round's: **a claim about
what a named test, harness or measurement *does*, where the code does something narrower.** Swept
rather than patched. **213 candidate sentences** were extracted mechanically — every line in this
report and the five finding files that names a test, a measurement, a harness or a child and uses a
verb of doing (*checks, compares, asserts, observes, evaluates, reads, counts, prints, records,
measures, drives, kills, forges, plants, refuses, verifies, proves*) — and of those, the **54** that
name a committed test were each opened against its body in `src/`, together with every claim about
the measurement harness and the two kill children. **Eleven statements were changed:**

| Where | What the code does, against what was written |
|---|---|
| §7 | *"the committed test is what carries the contents claim"*. It does not: `a_rejection_already_folded_is_refused_when_it_arrives_again` asserts a **length**, three **states** and a member count, and that each refusal's message names `merge_rejected`. **No comparison of registry contents exists in this gate's evidence at all**, and none is claimed now |
| §6.2 | *"Four refusals and one authorization, each evaluated live and on replay"*. The non-option `gpt-5` is driven through the **loop** and observed live only; the other four go through `refusal_pair`. Now: one refusal live at the loop, three refusals and one authorization live and on replay |
| §5, §10 row 10 | `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states` **constructs** the two post-index-write states (`K2`, `K3`) and `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` **plants** an interrupted dispatch; neither kills. Both were listed among real-child kills. Said as what they are |
| §0 | *"Three of them assert"* — **five** do (`g4r3_a3`, `g4r3_c3`, the `t_answer_kill` helper, and the two T-RETRY measurements), and only the first three were installed when the 58 mutations ran, so only those three are in the detection set. Both halves corrected |
| §6.3, O2 finding | the exhaustion's step structure. With `attempts_per = 2` on one rung the **second** failure parks, not the third; the third attempt exists only because a person answered `g4r3-q-1`. The finding said "the first two gate failures settle `Closed{Retry}`", which is one failure out and omits the authorisation. Both rewritten against the trace, event by event |
| §12 Q3 | `retry_refused_with_stale_incarnation` asserts the settlement's `retained_incarnation` comes from the epoch **and** that the fold refuses a **retry** naming a dead incarnation — it is not a refusal of the settlement |
| §2, O9 finding | *"reads the published file"* — `a_staged_partial_is_never_ingested_and_a_published_answer_survives_ingestion` also asserts the bytes are unchanged and that a second ingestion returns the same answer. An under-claim, made precise |
| §3 | the declared-test arithmetic is of the tree **the campaign measured**; later rounds added four entries. Scoped |

The other forty-three committed-test claims were opened and left standing, and this paragraph is the
record that they were opened. Where a name was cited without a behaviour claim — a slug in §2's
inventory, a test name in a mutation row — it was checked only for existence, which is what §2 says
it is.

**The fourth and fifth rounds, and the class behind them.** The third head, `9ae38330`, was verified
with all six clauses `established`; one mismatch remained — §5's fault table still credited the
round-2 child with a ceiling check its §5.4c already denied — and it was corrected at `c963918d`.
The fourth verification then made the point that matters more than any single cell: **each of four
rounds had found one more place where a reduced or derived measurement was described with a stronger
word than what was compared.** This head sweeps that class rather than patching its latest instance.

| Where | The word, and what was actually compared |
|---|---|
| §4.1, §12 Q6, §13 clause 3 | *"the ordered publication list is identical … the traces differ only in the interleaving"*. What is compared is `merged_list`, a **reduced projection** keeping three fields of each `task_merged`. The traces are **not** equal: `G4A1` publishes the younger candidate `StaleClean` with a pin and a passed verification, `G4A2` publishes it `Fast` with none, and the two fixtures pre-plant **different** repair candidates. Rewritten to name the three fields, to state both differences, and to say plainly that canonical-trace equivalence is **not** established |
| §5.4a | *"step 1 is identical in every printed field"* — `run_usd` is a printed field and it differs. Now "identical in every printed field but the ceiling itself", with the four fields named |
| §5.4b | inline identities quoted from `round3-compile.log`, a **preliminary** run, not the designated `meas-run4.log`. Replaced with the designated log's, cited to it |
| §7, §10 row 13, §12 Q3 | *"the registry unchanged"* — the measurement prints `registry.len()` and `lineage_members()`, two **lengths**. Narrowed to size and member count, with the contents claim attributed to the committed test |
| §12 Q6 | *"every repair effect is confined to the generation's own slot"* — no census of effect paths was run here. Narrowed to what was observed, and the clause named as G1–G3's |
| §13 clause 4, §12 R11 | *"present and byte-equal"* for the source ref — what was read is the ref's **target commit id** and the object's existence. Said as that |
| §2 | *"confirmed to exist by name"* — one `fn <name>(` per name, which checks the name and not the body. Said as that |

Every remaining use of *identical*, *equivalent*, *byte-equal*, *equal*, *the same*, *verified*,
*checked* and *proven* in this report was read in place during that sweep; those left standing are
the ones where the comparison really is the thing the word names — full `RunState` equality
(`live_equals_replay`), string equality of two error messages (`live_and_replay_equal`), sha256
equality of files, `BTreeMap<String, Vec<u8>>` equality of checkout bytes, structural equality of a
`RegistryEntry`, and multiset equality of two normalised log projections. Where a word survived, this
paragraph is the record that it was examined rather than overlooked.

**The third round, and the one finding it answers.** The second head, `0fac505d`, was verified in
turn: **five of six** clauses `established`, all thirteen adversarial tests `established` **except row
12**, all 58 mutation ids matching their logs, all six artifacts and all six questions accepted. One
blocking finding remained, and it was right:

| The verification's finding | What this head does |
|---|---|
| §5.4 said the T-RETRY child "calls the **production** `settle::retry` — which checks the ceiling". It does not: `settle::retry` receives no ceiling, and the check lives in the **selector**, reached only through `TopologyRun::step`. The witness bypassed it | **The ceiling claim for that child is withdrawn outright** — it checked none — and the row is established by two new executions instead: an A/B that shows the ceiling gating the **retry branch** with everything before it identical (§5.4a), and a second real subprocess killed on the retry path **through `TopologyRun::step`**, recovered by the real recovery order (§5.4b). §5.4c tables what each witness does and does not show |
| §0 said O10 "is carried in a re-scoped form"; §11 and the finding files show it **dropped** | §0 corrected; the files were right |
| §5 called `repair_materialization_reproduced_after_kill` a "real `git` child" kill | Corrected: it kills a subprocess of the **test binary** at armed hooks; the `git` children are the sampler's |

**The provenance rule this round adds, because that is the defect that sank two earlier records.**
Every quoted line is matched mechanically against the saved logs. On the first head, **117** quoted
`G4…` lines were checked against `measurements/meas-run2.log`, `measurements/samplers.log` and
`row10/residue-tests.log`, and **exactly one** did not occur in any of them — G4A3's step-2 merged
SHA, from the superseded draft run `meas-run1.log`. It is corrected above. The same check is re-run on every
head: on this one, over its **142** quoted lines, against those three logs plus
`measurements/meas-run3.log` and `measurements/meas-run4.log`, and **none** is unsourced; the twelve it flags are the ten `G4R10`
lines this report says it whitespace-aligned for reading and the two it says it elided or joined
(§6.2, §9.3). Every 40-hex identity in the document was checked separately against the same logs:
**none** now comes from the draft run.

**Two designated logs, and why both.** The seventeen measurements of the first round are quoted
from `measurements/meas-run2.log`; the material this round adds — the intermediate comparisons, the
T-RETRY boundary kill, the identity refusals and the budget-stop comparison — is quoted from
`measurements/meas-run3.log`, the run that has them. The two are the same measurements on the same
tree, checked rather than asserted: with 40- and 8-hex object identities and temporary paths
normalised, their `G4…` lines are **535 in each and identical as multisets**
(`measurements/run2-vs-run3-structural.log`). What differs between the runs is the per-run commit
identities the fixtures create, which is why a figure is never taken from the other log.

### The twenty-one measurements, by name

Seventeen were appended to `src/engine/topology/recover/tests.rs` as `mod g4r3_meas`; the second
round added one more there and one to `src/engine/topology/attempt/tests.rs`, beside the real
subprocess child it spawns. All are temporary and were removed before the report head was built.
Source: `harness/g4r3_meas.rs` and `harness/patch_t_retry.py`; output: `measurements/meas-run2.log`
for the first seventeen (split by tag under `measurements/by-tag/`) and `measurements/meas-run3.log`
for the second round (`by-tag-r3/`). **All twenty-one pass at `7e0110a1`** (`21 passed; 0 failed;
2 ignored` — the ignored entry is the subprocess helper the two T-ANSWER kill measurements spawn;
the T-RETRY child is the committed `attempt_kill_child`, already ignored).

| Tag | What it drives | Where quoted |
|---|---|---|
| `G4A1` | two overlapping lineages, the younger's candidate queued first; two forged overtakes | §4.1 |
| `G4A2` | two overlapping lineages again, the **older** lineage's repair candidate pre-planted and queued first — a different starting state, not `G4A1` re-ordered | §4.1 |
| `G4A3` | a lineage created **after** a release, at the loop; a forged overtake by it | §4.2 |
| `G4B1` | an ordinary candidate `InsideLineage`, then integrating on the moved head | §4.3 |
| `G4B2` | the decline exit: the lineage fails, the waiting candidate publishes fast | §4.4 |
| `G4B3` | a decline under a **halting** policy: `halted_at`, `Ending(Halted)`, forged second release | §6.4 |
| `G4B4` | the same decline **without** a halt: `Ending(Complete)`, dependent blocked | §6.4 |
| `G4B5` | a **code** rejection produced by `integrate`, then the repair dispatching from the ref | §8 |
| `G4B6` | a **conflict** rejection produced by `integrate`, then the same | §8 |
| `G4C1` | the frozen limit, the over-limit admission, payload freezing, the duplicate refusals, the parked resume | §6.1, §7 |
| `G4C2` | the empty intersection, the `HumanBinding` answer, ST-12's four refusals | §6.2 |
| `G4C3` | the overridden repair exhausting its single rung to a fresh human rung | §6.3 |
| `G4E` | the budget-stopped resume and the source ref across it | §8 |
| `G4G` | a retained repair generation whose worktree is gone, closed and re-materialized at the loop | §5.2 |
| `G4H` | the answer refused in the stopped epoch; a `.partial` alone; the published file ingested next epoch | §5, §12 Q6 |
| `G4K-torn` | a coordinator killed **inside** the ingestion's append | §5.1 |
| `G4K-complete` | a coordinator killed **just after** a complete append | §5.1 |
| `G4RETRY` | a real child killed strictly **before any byte** of the retry's append — reservation held, worktree verified, `attempt_started(retry)` not appended — and the tabled close on the bytes it left. It calls `settle::retry` directly and so exercises **no** ceiling (round 2) | §5.4c |
| `G4P-budget` | a run ceiling tripped mid-drive, so an **active** budget stop is compared live against durable (round 2) | §4.5 |
| `G4A1`/`G4A3`/`G4B1`/`G4G` **@step** | the same comparison taken after each step, at the intermediate states (round 2) | §4.5 |
| `G4CEIL` | the same two steps under three ceilings, so the **retry branch's** ceiling gate is executed (round 3) | §5.4a |
| `G4RETRY2` | a real subprocess killed on the retry path **through `TopologyRun::step`**, recovered by the real recovery order (round 3) | §5.4b |

**Five of them assert; three were in the detection set.** Counted by opening each body:
`g4r3_a3` (3 assertions), `g4r3_c3` (3), the `t_answer_kill` helper the `G4K` pair share (4),
`g4r3_t_retry_kill_at_the_boundary` (5) and `g4r3_r_t_retry_kill_through_the_selector` (4); the other
sixteen only print. **Only the first three were installed when §3's 58 mutations ran** — the two
T-RETRY measurements were written in the later rounds, and no mutation id in the ledger names either
(checked against `mutations/mutation-report.json`). **One exception, and the log on disk is the
exception's**: `M13` was **re-run in round 2**, deliberately, to ask whether the new T-RETRY
measurement added detection, and that rerun overwrote the campaign's original `mut-M13.log`. The
saved log is therefore the rerun's: it runs `g4r3_t_retry_kill_at_the_boundary` and reports
`2450 passed; 4 failed; 45 ignored; 8 filtered out` — **2507 declared**, against the campaign
control's 2505 — where the campaign's own M13 ran the 2505-test set. **Its four failures are the same
four committed tests either way** and are what the ledger records; the rerun's result is quoted in
§5.4c for the separate purpose it was run for, which is that the new measurement **passes** under
M13 and so is not a detection witness. So the detection set of §3 is the committed
suite plus `G4A3`, `G4C3` and the `G4K` pair, and a mutation caught by one of *those* **and by no
committed test** is reported as a missing committed witness rather than as detection (§11 O2, O9).
The two T-RETRY measurements are executed observations with their own assertions and are **not**
counted as detection anywhere in this report (§5.4c).

---

## 1. Step A — the cumulative suite on the exact frozen range

The packet requires *"the cumulative test suite up to that gate runs on that exact range."* It is
part of the gate, not a pre-push check.

**Run:** `~/bin/w1-eight-iso /srv/worktrees/g4r3` (nine gates, on a target base private to this
worktree), plus the tenth gate separately. Clean tree, detached at the merge commit. Logs
`~/eight-logs/7e0110a/` and `~/eight-logs/7e0110a-failed-20260910T095824Z/`, copied to
`g4r3-evidence-7e0110a1/gates/`.

| # | Command | Attempt 1 (09:56:19Z) | Attempt 2 (10:00:20Z) |
|---|---|---|---|
| 01 | `cargo fmt --check` | PASS 1s | PASS 0s |
| 02 | `cargo clippy --all-targets --all-features -- -D warnings` | PASS 28s | PASS 1s |
| 03 | `cargo test --all-targets --all-features` | **FAIL 76s** (one test, below) | PASS 36s |
| 04 | `cargo +1.85.0 check --locked --all-targets --all-features` | PASS 19s | PASS 0s |
| 05 | `test-release-record.sh` | PASS | PASS |
| 06 | `test-pr-policy.sh` | PASS | PASS |
| 07 | `test-pr-ledger-evidence.sh` | PASS | PASS |
| 08 | `test-docs-consistency.sh` | PASS | PASS |
| 09 | `test-internals-notes.sh` | PASS 1s | PASS 1s |
| 10 | `test-pr-ready-audit.sh` (run separately) | — | PASS (`test-pr-ready-audit: ok`) |

**Observed counts, attempt 2, the green one.** `2487` tests declared in the lib binary
(`running 2487 tests`, `03-test.log:5`); **`2443` passed, `0` failed, `44` ignored** in 36.50s
(`:2509`); **`+10` passed** in the `main.rs` binary (`:2525`) and `0` in the `probe` example
(`:2531`) — **2453 passed in total, Linux**. The count is per-platform and this number is the Linux
one. G4's second run measured `2478` declared and `2435` passed at `81ee09ef`; this range adds nine
lib tests and one ignored entry point, all of them #258's and #259's residue and sampler tests.

**Docker-gated lanes really ran:** `docker ps` succeeds on this box and `213`
`runner::container::*` test lines appear in the log, so the container suite was not silently
skipped. (Run 2's report records the same 213 at `81ee09ef`; that agreement is noted, not relied
on.)

**The binary measured *this* tree.** `w1-eight-iso` assigned `/mnt/ramtarget/iso-g4r3`, private to
this worktree. Attempt 1's log carries `Compiling upstroke v0.1.0 (/srv/worktrees/g4r3)`
(`03-test.log:52`); attempt 2 re-ran that binary without recompiling, which is why its clippy and
msrv legs report 0s. Verified rather than assumed: the lib test binary
`/mnt/ramtarget/iso-g4r3/slot1/debug/deps/upstroke-1f05c71869a3a005` contains the string
`/srv/worktrees/g4r3` **7** times (`/usr/bin/grep -a -c`) and contains none of `/srv/worktrees/g4b`,
`/srv/tactus` or `/srv/worktrees/pr9`. `/mnt/ramtarget` was at 71% before the run.

### The one red, fingerprinted, and what it is

Attempt 1's test leg failed on exactly one test:

```
test workspace_manager::tests::sampled_git_child_kills_every_residue_classified_and_recovered ... FAILED
residue sampling Worktree.Add: n=8 none=1 internal=6 after=0 unclassified=1
thread '...' panicked at src/workspace_manager/tests.rs:9088:9:
Worktree.Add: the classifier refused 1 of 8 samples, which is not the same thing as a residue in no
class — it is an inspection that failed, and the assertions below would have called it unclassified:
["run 2: git error: worktree list record 1 names a HEAD but neither a branch nor a detached checkout"]
```

Fingerprinted **by assertion text and error, not by test name**, against the ledger: this is
`PR172-SAMPLER-REFUSED-A-TORN-WORKTREE-LIST-RECORD`
(`findings/P3_correctness_202609052140_sampler-refused-a-torn-worktree-list-record.md`, P3,
deferred), whose recorded failure sequence is the same test, the same `Worktree.Add` sample point and
the same message word for word. It is **not** a G4 site: it is PR5's workspace-manager sampler,
and G4's own sites are `Object.RepairMaterialize` (§10 row 10) and the four fault rows of §5.

**The finding says *"Passes alone at the same head"* and asks for a rate before anything is called a
flake. Both were measured here, and the "passes alone" half does not hold on this range.** The single test was run alone ten times
(`gates/sampler-wm-isolated.log`): **9 passed, 1 failed** — and the one failure has a *different*
fingerprint from the suite's:

```
thread '...' panicked at src/workspace_manager/tests.rs:10339:10:
forced removal converges: Git { message: "worktree registration
/tmp/upstroke-wm-sample-add-1710168-3/repo/.git/worktrees/kalpha-g1 has an empty gitdir" }
```

which is `SAMPLER-RECOVERY-PROVEN-IS-NOT-PROVEN-FOR-AN-EMPTY-GITDIR`
(`findings/P2_crash-consistency_202609051147_sampler-empty-gitdir-recovery.md`, P2,
deferred) — the same test, the same site, the same `empty gitdir` refusal, and expressly **not**
`PR136-SAMPLER-FORCED-REMOVAL-DOES-NOT-CONVERGE`, whose signature is `DirectoryNotEmpty` (os error
39). So the `Worktree.Add` sampler produced **two distinct known-deferred reds in eleven runs on
Linux at this range**: 1 in 1 full-suite run and 1 in 10 isolated runs. Both are recorded as a
recurrence in §11, and neither is repaired here.

**Attempt 2 is the gate's cumulative-suite result**, and it is green on the exact frozen range.
The failing attempt's logs are preserved (`w1-eight-iso` copies them aside before a rerun can
overwrite them) rather than discarded.

### The restore, and the ten gates at this report's own head

Everything this run constructed is temporary. Before the report head was built, each temporarily
edited file was restored **from its `cp` backup** — never `git checkout --`, which would have taken
the uncommitted measurement module with it — and then proved equal to `HEAD`:
`src/engine/topology/recover/tests.rs` (the `three_tasks` fixture flag and `mod g4r3_meas`),
`src/engine/topology/dispatch/tests.rs` (the row-10 and sampler probes), `src/workspace_manager.rs`
and `src/workspace_manager/residue.rs` (the six row-10 mutations, restored by the mutation driver
after each). Every tracked file that differed from `HEAD` was then compared **by sha256 against
`git show HEAD:<path>`** and the set was empty; `git status --short` showed only the six new files,
all under `reviews/`. Log: `gates/restore-verify.log`.

**The ten gates were run on every committed report head.** Each adds this report and its five
finding files and nothing else; the restore above was re-run and re-verified before the second one.

| Head | What it is | Result | Logs |
|---|---|---|---|
| `68cf5092` | the first round's report | `ALL 9 PASS at 68cf509`, 11:09:25Z, plus `test-pr-ready-audit: ok` | `gates/eight-logs-68cf509/`, `gates/gate10-head.log` |
| `dc087837` | that head plus this subsection | ten gates green before the push | `gates/w1-eight-iso-final.log`, `gates/gate10-final.log` |
| `0d9fd23f` | the second round: §4.5, §5.4, §5.2's identity refusals, Q1, Q3, O9 and the corrected SHA | `ALL 9 PASS at 0d9fd23`, 11:46:17Z (fmt 1s, clippy 17s, test 60s, msrv 12s, the five scripts), plus `test-pr-ready-audit: ok` | `gates/eight-logs-0d9fd23/`, `gates/gate10-round2.log` |
| `0fac505d` | that head plus the round-2 gate record | ten gates green before the push | `gates/w1-eight-iso-round2-final.log`, `gates/gate10-round2-final.log` |
| `3e1ce24a` | the third round: §5.4a's ceiling A/B, §5.4b's kill through the selector, §5.4c, row 12, clause 1 and the two record corrections | `ALL 9 PASS at 3e1ce24`, 12:11:19Z (fmt 1s, clippy 19s, test 63s, msrv 12s, the five scripts), plus `test-pr-ready-audit: ok` | `gates/eight-logs-3e1ce24/`, `gates/gate10-round3.log` |
| `9ae38330` | that head plus the round-3 gate record | ten gates green before the push | `gates/w1-eight-iso-round3-final.log`, `gates/gate10-round3-final.log` |
| `bff15fd7` | the fourth round: §5's T-RETRY cell and the inventory row, attributing the ceiling to the witness that ran it | `ALL 9 PASS at bff15fd`, 12:28:35Z (fmt 1s, clippy 20s, test 70s, msrv 15s, the five scripts), plus `test-pr-ready-audit: ok` | `gates/eight-logs-bff15fd/`, `gates/gate10-round4.log` |
| `c963918d` | that head plus the round-4 gate record | ten gates green before the push | `gates/w1-eight-iso-round4-final.log`, `gates/gate10-round4-final.log` |
| `74bfae2c` | the fifth round: the reduced-measurement sweep — §4.1, §5.4a, §5.4b, §7, §2, Q3, Q6, R11 and clauses 3 and 4 | `ALL 9 PASS at 74bfae2`, 12:47:22Z (fmt 1s, clippy 17s, test 62s, msrv 11s, the five scripts), plus `test-pr-ready-audit: ok` | `gates/eight-logs-74bfae2/`, `gates/gate10-round5.log` |
| `0b00d4da` | that head plus the round-5 gate record | ten gates green before the push | `gates/w1-eight-iso-round5-final.log`, `gates/gate10-round5-final.log` |
| `3de5a580` | the sixth round: the behaviour-claim sweep — §7, §6.2, §5, §10 row 10, §0, §6.3, Q3, §2, §3 and two finding files | `ALL 9 PASS at 3de5a58`, 13:09:18Z (fmt 1s, clippy 16s, test 60s, msrv 11s, the five scripts), plus `test-pr-ready-audit: ok` | `gates/eight-logs-3de5a58/`, `gates/gate10-round6.log` |
| `982a5cbf` | that head plus the round-6 gate record | ten gates green before the push | `gates/w1-eight-iso-round6-final.log`, `gates/gate10-round6-final.log` |
| `227a0f37` | the seventh round: four descriptions — §11.1b and Q4's overlap authority, ST-12's row 4, MD4's edit count, and M13's rerun | `ALL 9 PASS at 227a0f3`, 13:25:44Z (fmt 1s, clippy 17s, test 61s, msrv 12s, the five scripts), plus `test-pr-ready-audit: ok` | `gates/eight-logs-227a0f3/`, `gates/gate10-round7.log` |

Each test log carries `Compiling upstroke v0.1.0 (/srv/worktrees/g4r3)` — a real rebuild of the
restored tree, not a served artefact — and each reports **2443 passed, 0 failed, 44 ignored** of 2487
declared, plus 10 in the binary: the same counts as attempt 2 at the range head, which is itself
evidence that both restores were complete.

**The head this branch pushes differs from `227a0f37` by this table alone**, and was gated the same
way — ten gates — before the push. That is a check on the report file, not on the range: it confirms
this document breaks no gate, in particular `test-docs-consistency.sh`, whose rooted-path check reads
every backticked `src/…`-style path in a document. Every such path in this report was additionally
existence-checked by a script before each commit, and none was missing.

### CI at the frozen sha, all three platforms

The newest check run per name at `7e0110a1`, queried through the GitHub API and saved as
`gates/checkruns-7e0110a1.json` (24 check runs, 14 distinct names):

| Check | Conclusion |
|---|---|
| `test (ubuntu-latest)`, `test (macos-latest)`, `test (winguest)` | success |
| `lint`, `lint (macos)`, `lint (windows)` | success |
| `msrv (Rust 1.85, {ubuntu,macos,windows}-latest)` | success |
| `build`, `deploy`, `report-build-status` | success |
| `upstroke-ci`, `upstroke-pr-policy` | success |

No leg is red at the frozen sha. In particular the **macOS** leg is green, which matters twice:
the RepairMaterialize sampler's macOS kill floor (the P2 run 2 filed) did not fire here, and neither
did the workspace-manager sampler whose Linux reds are recorded above.

**Step A verdict: the cumulative suite is green on the exact frozen range, on Linux, at
`7e0110a1`, and CI is green on all three platforms at the same sha.** This is a necessary condition
of the pass rule and not a sufficient one; detection is established by mutation in §3.

---

## 2. Step B — the packet's test names against the tree

The packet names tests as descriptive slugs in the four fault rows' `test` fields and in PR9's
`proof_tests`; the tree names them as prose. The mapping was re-derived here by searching the tree
(`/usr/bin/grep -rn "fn <slug>(" src/`), not by trusting a name match, and agrees with run 2's:
of the **19** distinct slugs the four rows name, **11 exist verbatim**, **7 exist under prose
names**, and **1 does not exist**.

| Row | Packet slug | In the tree |
|---|---|---|
| T-REPAIR-DISPATCH | `repair_materialization_reproduced_after_kill` | verbatim, `src/engine/topology/dispatch/tests.rs` |
| T-REPAIR-DISPATCH | `repair_materialization_synthetic_residue_recreated_after_forced_removal` | verbatim, same file |
| T-REPAIR-DISPATCH | `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` | verbatim, same file |
| T-REPAIR-DISPATCH | `repair_materialization_objects_released_to_git_on_scrub` | verbatim, same file |
| T-REPAIR-DISPATCH | `repair_source_present_after_parked_resume` | prose: `an_over_limit_repair_spends_nothing_until_its_answer_activates_it` and `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`; the ref itself is read before and after the parked resume in `G4C1` (§8) |
| T-ANSWER | `answer_ingested_after_kill_returns_pending` | prose: `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step` (no kill in it) — **and, executed here, real kills in both shapes**: `G4K-torn`, `G4K-complete` (§5.1) |
| T-ANSWER | `partial_answer_ignored_and_never_pruned` | prose: `a_staged_partial_is_never_ingested_and_a_published_answer_survives_ingestion` — which, opened here, asserts the published file is still there after `ingest_answer` **and** that its bytes are unchanged — plus `G4H` at the loop |
| T-ANSWER | `answer_not_ingested_after_budget_stop_until_resume` | prose: `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch`, plus `G4H` |
| T-ANSWER | `late_answer_before_halting_settlement_is_inert_and_retained` | prose, in two halves (the fold refusal, and R21's retained file); **no single test publishes a file immediately before a halting settlement** — the composition is reasoned, and said to be |
| T-ANSWER | `late_answer_after_finalization_is_inert_and_reported_not_live` | **absent**; see below |
| T-ANSWER | `declined_repair_fails_lineage_and_releases_lease` | prose: `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` (loop) and `declining_a_repair_admission_fails_the_lineage_and_allows_the_run_to_end` (fold) |
| T-ANSWER | `binding_override_replay` (ST-12) | prose: `an_override_replaces_the_frozen_binding_for_every_later_attempt` and `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`; the replay-equality half measured in `G4C2` (§6.2) |
| T-RETAINED | `retained_generation_closed_before_run_resumed` | verbatim, `src/engine/topology/settle/tests.rs` |
| T-RETAINED | `retained_generation_closed_when_worktree_missing` (ST-11) | verbatim for an ordinary generation; the repair half is `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again`, and **executed here through the loop**, `G4G` (§5.2) |
| T-RETAINED | `retained_generation_closed_at_run_end` (ST-17) | verbatim at the `close_retained` level; the run-end closure procedure that would call it is PR10's |
| T-RETRY | `same_generation_retry_regates_cumulative_tree` (ST-15) | verbatim (ordinary); the repair half is `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` |
| T-RETRY | `retry_refused_after_resume` | verbatim, twice (settle-level and recovery-level) |
| T-RETRY | `retry_refused_with_stale_incarnation` | verbatim |
| T-RETRY | `retained_worktree_with_residue_closed_not_retried` | verbatim |

**Why the absent slug is absent.** `late_answer_after_finalization_is_inert_and_reported_not_live`
needs a finalized run. `/usr/bin/grep -rn 'TopologyEventBody::RunFinished' src/` finds nine occurrences in seven files:
the fold's `apply` and `start` (consumers), the wire form in `src/topology/events.rs`, the census
fixture in `src/topology/census.rs`, and four inside test modules — and **no production emitter**.
`run_finished`, the closure procedure and terminal finalization are PR10's by the packet's own
sequence, and this build's checkpoint refuses `Step::Closure` outright, observed here in two
measurements:

```
G4B3 step2 Err("this build does not end a run: closure derives Ending(Halted), and the terminal
finalization `run_end_policy` attaches to `run_finished` is not implemented here, so it refuses
before appending it")
G4B4 step2 Err("... closure derives Ending(Complete), ... refuses before appending it")
```

A run cannot be finalized here, so an answer after finalization has nothing to be inert against.
The `retained_generation_closed_at_run_end` slug exists but proves the settle-level function, not
the closure procedure that would call it, for the same reason.

**The 32 prose-named and neighbouring tests §2 and §10 rely on each have exactly one `fn <name>(`
under `src/` on this tree** (`/usr/bin/grep -rn "fn <name>(" src/`, one hit each); the list and its
output are in `g4r3-evidence-7e0110a1/measurements/test-inventory.txt`. That is a check on the
**name**, not on the test's body or on its being a `#[test]`; what each one asserts is read where
this report cites it, and all of them run in §1's green suite.

**One coverage census the tree carries, stated for what it covers.** G3's filed finding
`findings/P3_correctness_202609090301_fault-row-census-covers-one-row-and-is-named-every.md`
still holds: `every_fault_row_name_is_a_test_in_the_tree` covers the T-CONTAINER row only, so
nothing mechanical holds the 19 slugs above to the tree. The obligation was discharged by hand here,
as it was in run 2.

---


## 3. Step C — detection, established by mutation

A green suite proves the tests pass, not that they still detect. **Fifty-eight mutations** were
applied to the frozen tree, one at a time, by a driver that

- asserts each anchor text occurs **exactly once** in the file before editing;
- copies the file aside, edits, asserts the file's sha256 **changed**;
- runs the detection set;
- restores from the **`cp` backup** (never `git checkout --`, which would have destroyed the
  uncommitted measurement module) and asserts the sha256 equals the pre-mutation one before the next
  mutation begins.

**The detection set is the full library suite**, `cargo test --lib`, with three skips and no other
filter. `--skip sampled_` excludes **six** tests, counted rather than assumed: the four kill samplers
(`sampled_git_child_kills_…`, `sampled_git_add_and_write_tree_child_kills_…`,
`sampled_repair_materialization_child_kills_…`, `sampled_cherry_pick_child_kills_…`) and two argv
censuses (`no_sampled_funnel_builds_its_argv_from_a_literal`,
`the_sampled_argv_census_counts_code_and_never_comments`) that only share the substring. The samplers
are excluded for runtime and are run separately, five times each, in §10; the two censuses are about
argv construction, which no mutation here touches. The other two skips are the allow-placement
censuses
`every_allow_of_a_governed_lint_is_module_level_and_in_the_allowlist` and
`the_three_blunt_governed_lints_are_used_by_nobody`, which this run's own temporary measurement
module trips by carrying a module-level `allow` that is not in `effects/allowlist.toml`. Those two
pass in the clean tree §1 measures.

**The control.** An **unmutated** run of exactly that command, on exactly that tree, was measured
first and is green: `test result: ok. 2452 passed; 0 failed; 45 ignored; 0 measured; 8 filtered out`
(`mutations/control-unmutated.log`). Without it a "caught" would mean nothing, because a suite that
was already red would report a failure for every mutation.

**And the arithmetic closes, which is itself a check on the construction.** 2452 + 45 + 8 = **2505**
tests declared with the measurement module installed **as it stood when this campaign ran**; §1's
clean run declares **2487**. The difference is **18** — the seventeen measurements of the first round
and the one ignored subprocess helper. (Later rounds added four more entries, so a re-run of the
campaign today would declare more; the campaign was not re-run and this arithmetic is of the tree it
measured.) The module adds tests and changes none: the fixture's new `three_tasks` flag is read only under
`Damage { three_tasks: true, .. }`, which no committed test sets.

**Where the mutations came from.** Fifty-two anchors are run 2's catalogue — 47 from its main
catalogue and five from its correction round — each **re-verified here to occur exactly once in this
tree** before use; six (**MT1**–**MT5**, **MT7**) were designed for this run against the code #258
and #259 changed, which run 2 could not have covered. All 58 logs are in
`g4r3-evidence-7e0110a1/mutations/`, one per mutation, with the ledger in `mutation-report.json`.


**Result: 58 mutations, 56 caught, 2 survived**, and 2 of the 56 caught only by this run's temporary measurements. Every mutation restored: the driver asserted the restored file's sha256 equalled the pre-mutation one before the next mutation began, and it did in all 58 cases.

| Mutation | What it changes | Caught | Tests killed | First test killed |
|---|---|---|---|---|
| `M01` | lineage order inverted: a younger lineage member no longer waits behind an older overlapping lineage | **yes** | 5 | `g4r3_a3_lineage_created_after_a_release_waits_behind_the_survivor_at_the_loop` |
| `M02` | an ordinary candidate overlapping a live lineage is no longer skipped (InsideLineage dropped) | **yes** | 1 | `an_ordinary_candidate_waits_for_any_lineage_and_a_member_only_for_older_ones` |
| `M03` | a lineage lease is never released (release of Lineage owner is a no-op) | **yes** | 14 | `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue` |
| `M04` | fail_lineage no longer releases the members' holdings (lineage lease, candidate leases, queue positions) | **yes** | 9 | `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` |
| `M05` | a repair member's terminal Failed settlement no longer folds its lineage | **yes** | 1 | `a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay` |
| `M06` | a non-override attempt_started on an overridden task is accepted (BindingMismatch dropped) | **yes** | 1 | `an_override_replaces_the_frozen_binding_for_every_later_attempt` |
| `M07` | materialization_observed presence parity no longer checked at attempt_started | **yes** | 1 | `an_attempt_runs_the_frozen_binding_or_the_validated_override` |
| `M08` | an answered question stays open, so a second answer is not refused as already answered | **yes** | 25 | `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run` |
| `M09` | an option index outside the frozen option list is accepted | **yes** | 2 | `real_docker_kill_on_an_already_exited_container_is_tolerated` |
| `M10` | an answer is ingested after budget_exceeded in the same epoch | **yes** | 1 | `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch` |
| `M11` | a repair dispatch skips the ceiling check | **yes** | 1 | `a_repair_origin_task_crosses_the_checkpoint_and_the_ceiling_binds_it_like_any_dispatch` |
| `M12` | a same-generation retry of a repair records no materialization (Retained dropped) | **yes** | 1 | `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` |
| `M13` | recovery step (e) closes no retained generation | **yes** | 4 | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` |
| `M14` | a worktree that fails Worktree.Verify is reported recreated but reused as it stands | **yes** | 9 | `a_forced_recreation_preserves_the_object_store_residue_it_recovers_over` |
| `M15` | the materialization no longer restores the base tree before it picks | **yes** | 1 | `a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces` |
| `M16` | a merge_rejected registering a Runnable repair past the frozen limit is accepted | **yes** | 2 | `a_lineage_past_its_repair_limit_registers_only_a_human_required_repair` |
| `M17` | the builder never admits HumanRequired past the limit | **yes** | 13 | `a_delivered_answer_is_ingested_before_unrelated_runnable_work_dispatches` |
| `M18` | an empty tier intersection returns the root's ladder instead of a HumanBinding one | **yes** | 8 | `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append` |
| `M19` | a decline under a halting policy no longer records halted_at | **yes** | 5 | `a_delivered_answer_is_ingested_before_unrelated_runnable_work_dispatches` |
| `M21` | an absent or substituted source candidate is not refused at dispatch | **yes** | 1 | `a_repair_whose_source_candidate_is_missing_is_refused_before_any_append` |
| `M22` | a conflict rejection deletes the rejected candidate's candidates ref (R11 violated at the Conflict arm) | **no — survived** | 0 | `—` |
| `M23` | a code rejection deletes the rejected candidate's candidates ref (R11 violated at the CodeRejected arm) | **no — survived** | 0 | `—` |
| `M24` | an open question no longer blocks work throughout its lineage | **yes** | 8 | `a_continuation_is_offered_only_for_an_open_generation_no_attempt_has_used` |
| `M25` | task_merged may release any lease (check_lease_release accepts everything) | **yes** | 3 | `the_derived_outcome_is_total_over_every_explored_state` |
| `M26` | task_merged may record a satisfies closure other than the one the fold derives | **yes** | 1 | `a_merge_copies_the_authorization_exactly` |
| `M27` | a new lineage's region omits the rejected candidate's own actual paths | **yes** | 1 | `a_conflict_opens_and_closes_its_own_transaction` |
| `M28` | a repair dispatch naming no source candidate, or an ordinary one naming one, is accepted | **yes** | 1 | `a_dispatch_takes_the_holding_its_origin_implies` |
| `M29` | closed generations' worktrees and intents are never reclaimed on resume | **yes** | 5 | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` |
| `M30` | a retry whose worktree fails verification starts anyway instead of closing the generation | **yes** | 3 | `a_retry_whose_retained_worktree_fails_verification_closes_and_destroys_nothing` |
| `M31` | the loop retries a session another incarnation retained | **yes** | 1 | `retry_refused_after_resume` |
| `M32` | the fold accepts a stale-incarnation retry | **yes** | 2 | `a_retained_session_belongs_to_the_incarnation_that_retained_it` |
| `M33` | answers are never polled before a step (ingestion only at the hard block) | **yes** | 9 | `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append` |
| `M34` | an override no longer makes the ladder single-rung (an overridden empty ladder exhausts to Fail, not the human rung) | **yes** | 1 | `g4r3_c3_overridden_repair_exhausts_its_single_rung_to_the_human_rung_at_the_loop` |
| `M35` | a one-off binding is not a pin (E2) | **yes** | 4 | `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run` |
| `M36` | recovery step (g) reports every open generation verified without verifying or recreating | **yes** | 2 | `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` |
| `M37` | the continuation of a repair generation does not materialize it | **yes** | 6 | `a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces` |
| `MA1` | the defect restored: a new lineage takes the live lineage count as its age (reused after a release) | **yes** | 3 | `g4r3_a3_lineage_created_after_a_release_waits_behind_the_survivor_at_the_loop` |
| `MA2` | the age counter never advances: every lineage takes age 0 | **yes** | 5 | `g4r3_a3_lineage_created_after_a_release_waits_behind_the_survivor_at_the_loop` |
| `MA3` | a release resets the counter to the surviving count, so the next lineage repeats a survivor's age | **yes** | 3 | `g4r3_a3_lineage_created_after_a_release_waits_behind_the_survivor_at_the_loop` |
| `MD1` | a merge_rejected no longer closes the transaction it settles, so a duplicate finds it still open | **yes** | 8 | `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append` |
| `MD2` | the spawn check no longer refuses a non-dense (already registered) repair key | **yes** | 3 | `a_registered_entry_is_the_entry_the_event_registers` |
| `MD3` | the spawn check no longer refuses a display id already registered | **yes** | 1 | `a_registered_entry_is_the_entry_the_event_registers` |
| `MD4` | MD1 + MD2 + MD3 together — **three** of the five guards (the settled transaction, the dense repair key, the registered display id); the member-index rule and the R14 budget rule are left standing | **yes** | 11 | `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append` |
| `MDISP1` | check_lease_disposition accepts any recorded disposition | **yes** | 6 | `a_closing_generation_admits_exactly_the_disposition_its_holding_implies` |
| `MDISP2` | a lineage generation's expected closing disposition becomes PredictedReleased on both sides (engine records it, fold expects it) | **yes** | 19 | `a_repair_dispatch_records_what_its_materialization_observed` |
| `MQ1` | the queue offers the last eligible candidate instead of the first (FIFO inverted) | **yes** | 41 | `a_code_rejected_candidate_registers_a_repair` |
| `MR1` | the event-log answer reader deletes a published answer file once it has read it (R21 violated) | **yes** | 2 | `g4r3_k1_t_answer_kill_after_the_complete_append` |
| `ME1` | row 6: an already-present source is no longer observed as Empty (the dispatch maps Materialized::Empty to Clean) | **yes** | 2 | `an_already_present_source_proceeds_as_an_ordinary_attempt_whose_empty_diff_fails_honestly` |
| `ME2` | row 6: an attempt whose materialization observed Empty is refused at attempt_started instead of proceeding as an ordinary attempt | **yes** | 1 | `an_already_present_source_proceeds_as_an_ordinary_attempt_whose_empty_diff_fails_honestly` |
| `ME3` | row 6: a completed attempt with an empty diff no longer fails EmptyDiff (evaluate_outcome returns success for any completed outcome) | **yes** | 56 | `a_declined_spend_approval_fails_the_task_through_the_halt_policy` |
| `MD6` | row 13: the lineage member-index rule removed alone (the fourth guard a duplicate merge_rejected meets) | **yes** | 1 | `a_rejection_creates_or_widens_exactly_one_lineage_and_registers_its_repair` |
| `MD7` | row 13: MD5 plus M16: all five guards a duplicate merge_rejected meets removed together (settled transaction, dense key, display id, member index, R14 budget rule) | **yes** | 14 | `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append` |
| `MT1` | row 10: the temporary-object-file scan loses its fan-out arm (the pre-#258 scan), so Git's real temporary file is not seen | **yes** | 9 | `synthetic_git_add_residue_unreferenced_objects_and_index_lock_then_forced_scrub_converges` |
| `MT2` | row 10: unreachable_objects always answers empty, so an unreferenced object is never observed | **yes** | 19 | `kill_after_capture_leaves_index_referenced_objects_then_scrub_releases_them` |
| `MT3` | row 10: the two object-store elements are declared to break quiescence, so the per-element partition is claimed the other way round | **yes** | 2 | `repair_materialization_synthetic_residue_recreated_after_forced_removal` |
| `MT4` | row 10: the fan-out probe reads the link's own type, so a symlinked fan-out directory is walked past (#258's second defect restored) | **yes** | 3 | `a_fan_out_link_that_loops_is_an_inspection_error_once_no_earlier_fan_out_answers` |
| `MT5` | row 10: the temporary-object-file scan loses its object-root arm, so a streamed object's temporary file is not seen | **yes** | 1 | `temporary_object_files_answers_for_the_files_git_prunes_as_its_own` |
| `MT7` | R27: a worktree removal prunes the shared object store, so the objects an interrupted materialization left do not survive the recovery | **yes** | 8 | `kill_after_ephemeral_snapshot_commit_before_worktree_leaves_gc_owned_object` |

### 3.1 The two survivors, the two measurement-only kills, and three things read off the ledger

**M22 and M23 survive** — a rejection arm that additionally deleted the rejected candidate's
candidates ref through `Ref.DeleteCandidatesRef` leaves the full suite green
(`rc=0`, `2452 passed; 0 failed`). That is the R11 witness gap: §8 and §11 O1.

**M34 and MR1 are caught only by this run's temporary measurements** and by no committed test —
M34 by `G4C3` alone, MR1 by the `G4K` pair alone. Both are reported as missing committed witnesses
(§11 O2, O9) and **neither is counted as detection by the committed suite** anywhere in this report.

**MT7 is caught two ways, and only one of them is the point.** It injects a
`git prune --expire=now` into `WorkspaceManager::remove_worktree` through `std::process::Command`,
so it is caught both by the R27 tests it targets —
`a_forced_recreation_preserves_the_object_store_residue_it_recovers_over`,
`every_registered_residue_element_is_constructed_and_recovers`,
`synthetic_git_add_residue_unreferenced_objects_and_index_lock_then_forced_scrub_converges`,
`synthetic_cherry_pick_residue_unreferenced_objects_and_cherry_pick_head_then_forced_reclaim_converges`
— **and** by the effects census `every_production_process_start_is_classified`, which notices the new
process start. The census catch is incidental; the R27 catch is the one this report relies on, and it
is real because those four die on the assertion that the object-store residue is still there.

**MT3 is the check that the row-10 partition is observed and not tautological.** Flipping
`element_breaks_quiescence` so that the two object-store elements claim to break quiescence kills
exactly two tests — `repair_materialization_synthetic_residue_recreated_after_forced_removal` and
`every_registered_residue_element_is_constructed_and_recovers` — because production `quiescence()`
still reuses, and the expectation no longer matches what was observed. Had the assertion compared
the predicate against itself, this mutation would have survived.

**Two unrelated failures in 58 runs, recorded rather than absorbed.** `M09` and `MD7` each carried
one extra failing test that their mutation cannot explain:
`runner::container::tests::real_docker_kill_on_an_already_exited_container_is_tolerated` and
`runner::container::tests::real_docker_returns_both_streams_of_a_container_separately`. Both are
real-Docker tests, both mutations target the fold, and **no mutation in the campaign was scored
`caught` on the strength of a Docker test alone** (checked: the set of mutations whose only failures
are Docker tests is empty). They are noise from the box, and they are a third data point on the
suite's flake rate beside §1's sampler.

---

## 4. Required artifact #2 — lineage/lease trace outputs

Four traces, each driven through the **production loop** against a real repository, each printing
every durable event with the fold state it produces, each compared live against a replay of the
bytes on disk. Full output: `g4r3-evidence-7e0110a1/measurements/meas-run2.log`, split by tag under
`measurements/by-tag/`.

### 4.1 Two overlapping lineages resolve in creation order, under two schedules — `G4A1`, `G4A2`

Two ordinary candidates are rejected by conflict, creating `L0@age0` and `L1@age1` whose regions
overlap on `shared.txt`. `G4A1` queues the **younger** lineage's repair candidate first; `G4A2`
queues the **older** one's first. The loop is driven three steps in each.

```
G4A1 before-drive: lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } | eligible=None | ...
G4A1 step1 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4A1 step2 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(2), merged_sha: CommitSha("0accc3dc69398d243ce9901dc9542e25b9533990") })
G4A1 step3 Ok(Integrated { key: TaskKey(3), sequence: SequenceId(3), merged_sha: CommitSha("5dd13cfb5bb97ed07b294ecc94570920c71f5400") })
G4A1 merged=[(2, [0, 2], "Lineage { root: TaskKey(0) }"), (3, [1, 3], "Lineage { root: TaskKey(1) }")]
G4A1 replay_twice_equal=true events=25 outcome=Ending(Complete)
G4A1 live_equals_replay=true live_lineages=none replay_lineages=none live_queue=empty replay_queue=empty ... live_epoch=Some(1) replay_epoch=Some(1)
```

```
G4A2 before-drive: ... queue=k2g0[candidate.txt+shared.txt]/L0:ELIGIBLE | eligible=Some("k2g0") | ...
G4A2 step1 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(2), ... })
G4A2 step2 Ok(Settled { key: TaskKey(3), accepted: true, spent_attempt: true })
G4A2 step3 Ok(Integrated { key: TaskKey(3), sequence: SequenceId(3), ... })
G4A2 merged=[(2, [0, 2], "Lineage { root: TaskKey(0) }"), (3, [1, 3], "Lineage { root: TaskKey(1) }")]
G4A2 replay_twice_equal=true events=24 outcome=Ending(Complete)
G4A2 live_equals_replay=true ... live_epoch=Some(1) replay_epoch=Some(1)
```

**What was compared, exactly.** The `merged=` line is not the trace: it is a **reduced projection**
of it, built by `merged_list`, which walks the durable log and keeps, from each `task_merged` event,
**three fields and no others** — the integration `sequence`, the `satisfies` set, and the
`lease_release`. Those three are equal across the two runs, in order:
`[(2, [0,2], Lineage{0}), (3, [1,3], Lineage{1})]`. Separately, each run's **final** state is equal
field-for-field to a replay of its own bytes (`live_equals_replay=true` in both, over the whole
`RunState`), and both end `Ending(Complete)` with an empty lease table, an empty queue, all four
tasks `Merged`, `entitlements_held=0`, `invocations_balance=true`, `transaction_open=false` and
`pipeline_held=0` — **no deadlock**.

**What was *not* compared, and where the two runs genuinely differ.** The full event traces are
**not** identical and this report does not claim they are. Two differences are visible in the saved
traces themselves:

- the younger lineage's candidate publishes by a **different disposition** in each. `G4A1` #23:
  `merge_prepared s3 disp=StaleClean … pin=Some("refs/upstroke/runs/…/prepared/3") satisfies=[1, 3]
  verification=Passed`, with `candidate=8bbba11b` distinct from `proposed=5dd13cfb`. `G4A2` #22:
  `merge_prepared s3 disp=Fast … pin=None satisfies=[1, 3] verification=none`, with
  `candidate=proposed=3ff75884`. The queued regions differ with it —
  `k3g0[other.txt+shared.txt]` in `G4A1` against `k3g0[other.txt]` in `G4A2`.
- and the two are **not one run under two orderings**: the fixtures pre-plant *different* repair
  candidates. `G4A1` plants the **younger** lineage's (`repair_b`, editing `other.txt` and
  `shared.txt`); `G4A2` plants the **older** lineage's (`repair_a`, editing `candidate.txt` and
  `shared.txt`). The other repair is then produced by the drive in each.

**So what the pair establishes, in its own words:** from two different starting states, with the two
lineages' candidates arriving in opposite orders, the runs publish in the **same lineage creation
order**, release the **same leases**, and both complete. It is evidence about **publication order and
lease release**, over the three fields named above. It is **not** an equivalence of canonical traces,
and §12's Q6 says so there too.

**Overtaking refused, live and on replay, with byte-equal errors.** In `G4A1` a forged
`merge_verification_started` and a forged fast `merge_prepared` for the younger lineage's candidate,
ahead of the older lineage, are each refused:

```
G4A1-overtake-verification_started live=Err("`merge_verification_started` starts an integration for task 3 generation 0, which is not the first eligible candidate in the queue (no queued candidate is eligible)")
G4A1-overtake-verification_started replay=Err("`merge_verification_started` ... (no queued candidate is eligible)")
G4A1-overtake-verification_started live_and_replay_equal=true both_refused=true
G4A1-overtake-merge_prepared_fast live_and_replay_equal=true both_refused=true
```

### 4.2 A lineage created after a release waits behind the older survivor — `G4A3`

This is the shape verification 1 found broken and #256 fixed, driven here through the production
loop on a three-task fixture. Phase 1: `L0` publishes and **releases**. Phase 2: a third task's
candidate is rejected, creating `L2`, whose repair candidate widens `L2` onto `shared.txt`, which
the survivor `L1` holds.

```
G4A3 phase1 after (L0 released): lineages=L1@age1[other.txt+shared.txt] | queue=empty | ...
G4A3 phase2 before-drive: lineages=L1@age1[other.txt+shared.txt] L2@age2[gamma.txt+shared.txt] | queue=k5g0[gamma.txt+shared.txt]/L2:BehindOlderLineage { root: TaskKey(1) } | eligible=None | ...
G4A3 ages=[(1, 1), (2, 2)] repair_a=k3 repair_b=k4 repair_c=k5
G4A3-overtake-verification_started live_and_replay_equal=true both_refused=true
G4A3 step1 Ok(Settled { key: TaskKey(4), accepted: true, spent_attempt: true })
G4A3 step2 Ok(Integrated { key: TaskKey(4), sequence: SequenceId(4), merged_sha: CommitSha("67713abceba314788cf558a8b8edd7e4bc0fb282") })
G4A3 step3 Ok(Rejected { key: TaskKey(5), sequence: SequenceId(5) })
G4A3 merged=[(2, [0, 3], "Lineage { root: TaskKey(0) }"), (4, [1, 4], "Lineage { root: TaskKey(1) }")] runs=3
G4A3 replay_twice_equal=true events=35 outcome=Ending(Parked)
G4A3 live_equals_replay=true live_lineages=L2@age2[gamma.txt+shared.txt] replay_lineages=L2@age2[gamma.txt+shared.txt] ...
```

Read off the lines: after age 0 is released the new lineage takes **age 2**, not the released 0 and
not the survivor's 1, because the counter never falls; its member is `BehindOlderLineage { root: 1 }`;
a forged integration start for it is refused live and on replay; the loop's first step therefore
dispatches the **survivor's** repair `k4`, which publishes at s4, and only then does the newer
lineage's candidate start its integration, at s5. The measurement asserts all three of those, and
they hold. At s5 that candidate meets a **real conflict** on `shared.txt` (both repairs edited it),
so the production integration path rejects it with `WidensLineage { root: 2 }` and registers `L2`'s
second repair `HumanRequired{limit:1}` — the fixture's `max_merge_repairs` of one is consumed (R14) —
and the run parks. That rejection is not the point of the measurement, and it is good evidence of its
own: a lineage member's conflict rejection through `integrate` widens rather than creates, and the
second automatic repair is refused by the frozen limit.

### 4.3 An ordinary candidate overlapping a lineage waits, then re-verifies stale — `G4B1`

```
G4B1 before-drive: lineages=L0@age0[candidate.txt+shared.txt] | queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } | eligible=None | ...
G4B1 step1 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4B1 step2 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(1), merged_sha: CommitSha("0accc3dc69398d243ce9901dc9542e25b9533990") })
G4B1 step3 Ok(Integrated { key: TaskKey(1), sequence: SequenceId(2), merged_sha: CommitSha("98667e9c2593f9a96604f9f5ed038072a7915530") })
G4B1 merged=[(1, [0, 2], "Lineage { root: TaskKey(0) }"), (2, [1], "Candidate { key: TaskKey(1), generation: GenerationId(0) }")]
G4B1 replay_twice_equal=true events=20 outcome=Ending(Complete)
```

The ordinary candidate is `InsideLineage` — held, not dropped — until the lineage merges; then it
integrates on the moved head, taking the staging path with a verification, and releases its **own**
candidate lease rather than a lineage one.

### 4.4 The other exit: a declined lineage frees the waiting candidate — `G4B2`

```
G4B2 before-drive: lineages=L0@age0[candidate.txt+shared.txt] | queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } | questions=1 | states=0:AwaitingRepair,1:AwaitingMerge,2:AwaitingInput | ...
G4B2 step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: true })
G4B2 step2 Ok(Integrated { key: TaskKey(1), sequence: SequenceId(1), merged_sha: CommitSha("329b641c6cdd351584083f4cd786262592af4f92") })
G4B2 replay_twice_equal=true events=14 outcome=Ending(Complete)
G4B2 live_equals_replay=true live_lineages=none replay_lineages=none live_queue=empty replay_queue=empty live_questions=0 replay_questions=0 ...
```

The decline fails the lineage, releases its lease and closes its questions in **one** delta — the
`lineages=none`, `states=…Failed`, `questions=0` all appear on the same event's line — and the
waiting candidate publishes in the next step, *fast*, because the head never moved.

---

### 4.5 The live-against-durable comparison taken at intermediate states

The first head compared the live coordinator against a replay of the bytes on disk **only at each
measurement's final state**, and its Q1 then claimed coverage of states none of those nineteen lines
shows. The verification was right. The drive helper now takes the same comparison **after every
step**, tagged with the step, and these are the lines that carry the states Q1 names. All from
`measurements/meas-run3.log`; each is `live.state() == replayed.state()` over the whole `RunState`,
with the fields below printed beside it.

**Two live lineages with distinct ages, and a member's candidate held `BehindOlderLineage`:**

```
G4A1 @step1 live_equals_replay=true live_lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] replay_lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] live_queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } k2g0[candidate.txt]/L0:ELIGIBLE replay_queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } k2g0[candidate.txt]/L0:ELIGIBLE ... live_epoch=Some(1) replay_epoch=Some(1)
G4A3 @step1 live_equals_replay=true live_lineages=L1@age1[other.txt+shared.txt] L2@age2[gamma.txt+shared.txt] replay_lineages=L1@age1[other.txt+shared.txt] L2@age2[gamma.txt+shared.txt] live_queue=k5g0[gamma.txt+shared.txt]/L2:BehindOlderLineage { root: TaskKey(1) } k4g0[other.txt]/L1:ELIGIBLE replay_queue=k5g0[gamma.txt+shared.txt]/L2:BehindOlderLineage { root: TaskKey(1) } k4g0[other.txt]/L1:ELIGIBLE ... live_epoch=Some(2) replay_epoch=Some(2)
```

`G4A3 @step1` is the after-release case: `L1@age1` and `L2@age2` live together, the released age 0
never reused, on **both** sides of the comparison.

**An ordinary candidate held `InsideLineage`:**

```
G4B1 @step1 live_equals_replay=true live_lineages=L0@age0[candidate.txt+shared.txt] replay_lineages=L0@age0[candidate.txt+shared.txt] live_queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } k2g0[candidate.txt]/L0:ELIGIBLE replay_queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } k2g0[candidate.txt]/L0:ELIGIBLE ... live_epoch=Some(1) replay_epoch=Some(1)
```

**A `RetainedIdle` generation with its session and its incarnation:**

```
G4G @step1 live_equals_replay=true ... live_generations=k0g0:Closed,k1g0:Closed,k2g0:RetainedIdle { session: SessionId("claude-code-session"), incarnation: Epoch(1) } replay_generations=k0g0:Closed,k1g0:Closed,k2g0:RetainedIdle { session: SessionId("claude-code-session"), incarnation: Epoch(1) } ... live_epoch=Some(1) replay_epoch=Some(1)
```

**An active budget stop.** None of the seventeen could carry one, and the reason is structural: a
stop planted *before* a drive is cleared by the `run_resumed` the drive itself appends, because the
stop is epoch-scoped and a live `TopologyRun` exists only after that resume. So the coordinator was
made to **record** the stop mid-drive instead, by driving with a run ceiling of `$0.01`
(`g4r3_p_budget_stop_compared_live_while_current`):

```
G4P-budget step1 Ok(BudgetExceeded)
G4P-budget @step1 live_equals_replay=true live_lineages=L0@age0[candidate.txt] replay_lineages=L0@age0[candidate.txt] live_queue=empty replay_queue=empty live_questions=0 replay_questions=0 live_generations=k0g0:Closed,k1g0:Closed replay_generations=k0g0:Closed,k1g0:Closed live_halted=None replay_halted=None live_budget_stop=Some(1) replay_budget_stop=Some(1) live_epoch=Some(1) replay_epoch=Some(1)
G4P-budget after: budget_stop=Some(1) outcome=Ending(BudgetExceeded) repair_state=Some(Pending) epoch=Some(1)
```

`live_budget_stop=Some(1)` beside `live_epoch=Some(1)` is the stop **current in this epoch**, on both
sides.

**What the intermediate comparison adds over the final one, stated for what it is.** A transient
mismatch that resolved before the end — a queue entry consumed, a generation closed, a lineage
released — would not have been caught by a final-state comparison, and the report's first head could
not have said otherwise. It is caught now for these states in these measurements. It is **not** a
claim that every intermediate state of every measurement is compared: the probe runs after each step
of `G4A1`, `G4A3`, `G4B1` and `G4P-budget`, and at two points inside `G4G`.

---

## 5. Required artifact #3 — the fault evidence table for the G4 sites

The four rows the packet names for this range, with what was executed here.

| Site | Kill executed? | Evidence at `7e0110a1` |
|---|---|---|
| **T-REPAIR-DISPATCH** | **yes, real children** | `repair_materialization_reproduced_after_kill` — a real **subprocess of the test binary** (`repair_kill_child`), killed by `std::process::abort()` at a hook armed before and after the materialization site, not a `git` child; the **`git` children** are the sampler's — and the sampler (§10 row 10: 40 real kills in 40 spawns across five runs here, 0 refusals, 11 of them mid-write); and, **not kills but constructed states**, opened here and said as that: `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` **plants** an interrupted dispatch and drives recovery across the process boundary, and `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states` **builds** the two post-index-write states a kill would leave (its own comments name them `K2` and `K3`) and recovers from each — neither spawns a child or aborts, and the "killed" in the second's name is what it models, not what it does |
| **T-ANSWER** | **yes, two real coordinator kills** | `G4K-torn` and `G4K-complete`, §5.1 |
| **T-RETAINED** | **yes** | `retained_generation_not_continued_after_kill`; and **through the loop**, `G4G`, §5.2 |
| **T-RETRY** | **yes, at the boundary the row names — two children, and the ceiling belongs to one of them** | **`G4RETRY2` (§5.4b)** is the witness for the row's boundary *and* its ceiling: a real subprocess killed on the retry path **through `TopologyRun::step`**, so the selector's ceiling check is on the executed path, with `settle::retry` having taken the provisional reservation and run `Worktree.Verify` first; the surviving prefix is the row's `RetainedIdle` durable state and **recovery step (e)** closes it before `run_resumed`. **`G4CEIL` (§5.4a)** is what makes the ceiling an observation rather than a reading: the same two steps under three ceilings, step 1 identical in every printed field and step 2 answering `BudgetExceeded` on the `$1.30` arm. **`G4RETRY` (§5.4c)** is the companion, and it establishes **only** the strictly-before-the-append kill and the tabled close — the provisional reservation held and the worktree verified, `attempt_started(retry)` not appended — and **checks no ceiling**, because it calls `settle::retry` directly and `settle::retry` receives none. The committed `kill_during_retry_attempt_closes_generation` is a real kill too but of the **next** transaction, after the retry has started, and the first head of this report wrongly cited it for this row; `retry_refused_after_resume` (twice), `retry_refused_with_stale_incarnation` and `retained_worktree_with_residue_closed_not_retried` are the refusal side |
| T-* finalization rows | **no — reasoned, and the reason is structural** | this build has no production `run_finished` emitter and refuses `Step::Closure` (§2). Those rows are PR10's |

### 5.1 The two real T-ANSWER kills — `G4K`

A coordinator process is killed **inside** the `question_answered` append (torn tail) and **just
after** a complete one, and the next incarnation resumes from the bytes on disk.

```
G4K-torn after-kill log: grew=true bytes_delta=535 last_byte_is_newline=false question_answered_raw=1 run_resumed_raw=1 published_exists=true published_unchanged=true
G4K-torn last line (truncated): "{\"ts\":\"2026-08-23T09:41:02Z\",\"event\":\"question_answered\",\"data\":{\"key\":2,\"question\":\"q-park-fix"
G4K-torn step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: false })
G4K-torn after-resume: question_answered_events=1 first_via=Some("event-log") published_exists=true published_unchanged=true repair=Some(AwaitingMerge) alpha=Some(AwaitingRepair) open_questions=0
```

```
G4K-complete after-kill log: grew=true bytes_delta=631 last_byte_is_newline=true question_answered_raw=1 run_resumed_raw=1 published_exists=true published_unchanged=true
G4K-complete step1 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4K-complete after-resume: question_answered_events=1 first_via=Some("event-log") published_exists=true published_unchanged=true repair=Some(Merged) alpha=Some(Merged) open_questions=0
```

Both shapes leave **exactly one** `question_answered` in the log after the resume; the torn tail is
truncated on open and the retained answer file is re-ingested, the complete one is not ingested twice,
and in **both** the published answer file is present afterwards and its bytes are unchanged.

**What is asserted and what is observed, separated.** The two measurements **assert** the file's
**existence** — `assert!(published.is_file(), "R21: the answer file is never pruned")` — and they
**print** the byte comparison, `published_unchanged=true`, which is a read of the file against the
bytes taken before the kill rather than an assertion. Both readings are `true` above; only the first
would fail a mutation. That distinction matters for MR1 (§11 O9): a reader that pruned the file after
reading it is caught by the existence assertion, and a reader that *rewrote* it would not be.

### 5.2 A retained repair generation whose worktree is gone, through the loop — `G4G`

```
G4G after step1: ready_retry=true class=Some("RetainedIdle { session: SessionId(\"claude-code-session\"), incarnation: Epoch(1) } lease=InheritedLineage { root: TaskKey(0) }") worktree_exists=true lineage_held=true intents_has=true
G4G worktree removed: exists=false
G4G step2 Ok(GenerationClosed { key: TaskKey(2) })
G4G after step2: generation_closed=["g0 reason=WorktreeMissing lease=LineageHeld"] repair_state=Some(Pending) lineage_held=true ready=true ready_retry=false intents_has=true worktree_exists=false
G4G step3 Ok(Settled { key: TaskKey(2), accepted: false, spent_attempt: true })
G4G after step3: dispatches=["g0 base=33a81e59 source_eq_rejected=true lease=InheritedLineage { root: TaskKey(0) }", "g1 base=33a81e59 source_eq_rejected=true lease=InheritedLineage { root: TaskKey(0) }"] starts=["g0 a1 resume=false mat=Some(Clean)", "g1 a1 resume=false mat=Some(Clean)"] materializations_before=1 after=2 invocations_balance=true entitlements_held=0 closed_g0_intent_still_present=true g1_intent_present=true g1_worktree_exists=true
```

The live retry path closes the generation `WorktreeMissing`/`LineageHeld` — **the lineage lease is
untouched by the close** (`lineage_held=true` after) — and the next step dispatches generation 1
from the *same* protected source and materializes it again. `G4G` also refuses both predicted
dispositions for the close and for an `attempt_interrupted`, live and on replay, and accepts
`LineageHeld`.

**One negative result, reported as one:** `closed_g0_intent_still_present=true` after the close and
after generation 1 dispatched. The live close does not reclaim the closed generation's durable
intent; the next resume's sweep does. That is §11 O3, carried forward from run 2 and re-measured
here.

**Wrong *identity*, correct disposition — four refusals added this round.** The first head of this
report offered `G4G`'s disposition refusals as evidence for Q3's *"a close or settlement for the
wrong generation or attempt is refused"*. The verification was right that those vary only the
`lease`, so they evidence the **disposition** rule and not the **identity** rule, which production
checks first, in `in_flight()` and `open_generation()`. Four events were therefore constructed with
the **valid** `LineageHeld` disposition and a wrong identity, each evaluated live and on replay
(`measurements/meas-run3.log`):

```
G4G-identity-attempt_interrupted-wrong-attempt live=Err("`attempt_interrupted` names attempt 2 of task 2 generation 0, and the open attempt is 1")
G4G-identity-attempt_interrupted-wrong-attempt live_and_replay_equal=true both_refused=true
G4G-identity-attempt_interrupted-wrong-generation live=Err("`attempt_interrupted` names generation 1 of task 2, which is not the open one (generation 0 is the open one); a completion applies only while its identity is the current open one")
G4G-identity-attempt_interrupted-wrong-generation live_and_replay_equal=true both_refused=true
G4G-identity-generation_closed-wrong-generation live=Err("`generation_closed` names generation 1 of task 2, which is not the open one (generation 0 is the open one); a completion applies only while its identity is the current open one")
G4G-identity-generation_closed-wrong-generation live_and_replay_equal=true both_refused=true
G4G-identity-generation_closed-wrong-task live=Err("`generation_closed` names generation 0 of task 1, which is not the open one (no generation of this task is open); a completion applies only while its identity is the current open one")
G4G-identity-generation_closed-wrong-task live_and_replay_equal=true both_refused=true
```

Each error names the **identity** — the open attempt, the open generation, the task with no open
generation — and none names a disposition, which is what makes them evidence for the rule Q3 states.
The `replay=` line of each is byte-equal to the `live=` line and is elided here for width; both are
in the log.

### 5.3 The answer file across a budget stop, a partial and a resume — `G4H`

```
G4H epoch0 (budget stop current): answer refused=Err("`question_answered` arrived after the budget stop in this epoch; the run has stopped admitting work and an answer ingested now would restart a run that already ended")
G4H resume with partial only: progress=[Ok(Blocked { questions: 1 })] partial_exists=true partial_unchanged=true published_exists=false epoch=Some(1) budget_stop=None
G4H resume with published: progress=[Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: false }), Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })] published_exists_after=true partial_exists_after=false
G4H answers=["q=q-park-fixed answer=Answered { option_index: 0, binding_override: None } via=event-log"]
G4H second answer (already answered) refused live=Err("`question_answered` names question `q-park-fixed`, which has already been answered; a question is answered once")
```

Three T-ANSWER properties in one drive: an answer is **refused in the epoch the budget stopped in**
and ingested by the next incarnation's first step; a **staged `.partial` alone** blocks and is left
byte-unchanged, never ingested; and the **published** file is ingested through the production reader
(`via=event-log`) and is still there afterwards. `partial_exists_after=false` is the parent's own
rename, not a prune by the reader.

### 5.4 The kill at T-RETRY's own boundary, and the ceiling on the path to it

**What the row specifies**, verbatim from the packet's `transaction_fault_matrix` entry for
`T-RETRY`:

> `boundary`: attempt_finished{Retained} durable; the retaining incarnation selects the retry:
> ceiling checked, provisional {pipeline} reservation taken, worktree verified (Worktree.Verify:
> present, quiescent, holding the retained tree), attempt_started(retry) not yet appended
>
> `durable_state`: RetainedIdle generation with retained_session and retained_incarnation
>
> `resume_action`: a fresh process never continues here: recovery step (e) closes the generation
> before run_resumed and the fold refuses a stale-incarnation retry

**Two earlier attempts at this row, and what each got wrong.** The report's first head cited
`kill_during_retry_attempt_closes_generation`, whose child **starts** the retry — appending
`attempt_started` — and dies during capture; that is the next transaction, not this one. Its second
head added a child that dies strictly before any byte of the append, but that child calls
`settle::retry` **directly**, so it never reaches the selector — and the ceiling check is in the
selector, not in `settle::retry`, which receives no ceiling at all. **The second verification was
right on both counts, and the ceiling half of that sentence is withdrawn: the round-2 child did not
check a ceiling, and no rewording of it would have.** This head establishes the row with two
executions instead.

#### 5.4a The ceiling is checked on the retry branch — shown by changing only the ceiling

`g4r3_r_ceiling_gates_the_retry_branch` runs the same fixture and the same two steps three times,
varying **nothing but** `Ceiling::run_usd` (`measurements/meas-run4.log`):

```
G4CEIL[unlimited] run_usd=None step1=Ok(Settled { key: TaskKey(2), accepted: false, spent_attempt: true }) spend_after_step1=1.4500 ready_retry=true class_after_step1=Some("RetainedIdle { session: SessionId(\"claude-code-session\"), incarnation: Epoch(1) }")
G4CEIL[unlimited] step2=Ok(Settled { key: TaskKey(2), accepted: false, spent_attempt: true }) attempt_starts=[1, 2] budget_stop=None outcome=Ending(Parked) class_after_step2=Some("Closed")
G4CEIL[breaches-at-the-retry] run_usd=Some(1.3) step1=Ok(Settled { key: TaskKey(2), accepted: false, spent_attempt: true }) spend_after_step1=1.4500 ready_retry=true class_after_step1=Some("RetainedIdle { session: SessionId(\"claude-code-session\"), incarnation: Epoch(1) }")
G4CEIL[breaches-at-the-retry] step2=Ok(BudgetExceeded) attempt_starts=[1] budget_stop=Some(1) outcome=NotEnding class_after_step2=Some("RetainedIdle { session: SessionId(\"claude-code-session\"), incarnation: Epoch(1) }")
G4CEIL[breaches-at-the-dispatch] run_usd=Some(0.1) step1=Ok(BudgetExceeded) spend_after_step1=1.2000 ready_retry=false class_after_step1=None
G4CEIL[breaches-at-the-dispatch] step2=Err("this build does not end a run: closure derives Ending(BudgetExceeded), and the terminal finalization `run_end_policy` attaches to `run_finished` is not implemented here, so it refuses before appending it") attempt_starts=[] budget_stop=Some(1) outcome=Ending(BudgetExceeded) class_after_step2=None
```

Read across the three arms. With **no** ceiling the retry runs: `attempt_starts=[1, 2]`. With a
ceiling of `$1.30` — above the spend at step 1's selection, below the `$1.45` the first attempt
leaves — **step 1 is identical in every printed field but the ceiling itself** (`step1`,
`spend_after_step1`, `ready_retry` and `class_after_step1` all match the unlimited arm's) and step 2
answers `BudgetExceeded`:
`attempt_starts=[1]`, no retry appended, and the generation is **still** `RetainedIdle`, waiting. The
third arm is the control that the two gates are distinct: a ceiling of `$0.10` stops the *dispatch*
branch and never reaches a retry at all. **So the ceiling check is executed on the retry branch**,
and it is executed by the same `TopologyRun::step` the next measurement kills inside.

#### 5.4b The kill, through the selector — `G4RETRY2`

`g4r3_r_t_retry_kill_through_the_selector` spawns the test binary as a **real subprocess**
(`g4r3_t_retry_selector_kill_child`). The child resumes the planted run through the production
recovery order, takes step 1 — the repair dispatches, materializes and its first attempt settles
unaccepted, retaining the generation — and **only then** arms a kill at
`Event(Append)/Written`, so that the next transaction append is the retry's and nothing earlier. Step
2 then goes through `TopologyRun::step`: the **selector** (ceiling checked, §5.4a), `settle::retry`
(retention validated, the provisional `{pipeline}` reservation taken, `Worktree.Verify` run), and the
append of `attempt_started(retry)` — in which the process dies.

```
G4RETRY2 child: code=None timed_out=false printed_test_result=false reached_unreachable=false
G4RETRY2 after-kill log: grew=true bytes_delta=2017 last_byte_is_newline=false attempt_started_raw=4 attempt_finished_raw=1 last_line_truncated="{\"ts\":\"2026-08-23T09:41:02Z\",\"event\":\"attempt_started\",\"data\":{\"key\":2,\"generation\":0,\"attempt\":2,\"rung\":0,\"binding\":{\"t"
G4RETRY2 surviving prefix: events=17 last=Some("attempt_finished") attempt_starts=[1] class=Some("RetainedIdle { session: SessionId(\"claude-code-session\"), incarnation: Epoch(1) }") ready_retry=true
G4RETRY2 after-resume: closes=["g0 reason=ResumeDiscardsRetainedSession lease=LineageHeld"] attempt_starts=[1, 1] last_six_kinds_reversed=["task_candidate_created", "candidate_prepared", "attempt_started", "task_dispatched", "run_resumed", "generation_closed"] class=Some("Closed")
```

The partial last line is the retry's own `attempt_started` — `"attempt":2` — cut mid-record, which is
what "died in the append that would have recorded the retry" looks like on disk. **The surviving
prefix, after the torn tail is truncated on the next open, is exactly the row's `durable_state`**:
17 events ending at `attempt_finished`, one `attempt_started` and not two, and the generation
`RetainedIdle { session, incarnation }` with `ready_retry` true.

**And the tabled recovery is executed by the real recovery order, not by calling its helper.** A
fresh incarnation drives one step; the log shows `generation_closed{ResumeDiscardsRetainedSession,
LineageHeld}` **before** `run_resumed` — reading `last_six_kinds_reversed` back to front:
`generation_closed`, `run_resumed`, `task_dispatched`, `attempt_started`, `candidate_prepared`,
`task_candidate_created`. That is recovery step (e) closing the retained generation before the
resume, and then a **fresh generation** dispatched from the same protected source (`meas-run4.log` `#19`:
`task_dispatched k2 g1 base=b3a0b155 lease=InheritedLineage { root: TaskKey(0) }
source=k0g0@e8b6ca7b`, the same `base` and `source` the closed generation 0 carried at `#14`) and
materialized. `live_equals_replay=true` on the result.

#### 5.4c What the two witnesses do and do not each establish

| | round-2 child (`G4RETRY`) | this head's child (`G4RETRY2`) |
|---|---|---|
| reaches the boundary's durable state | yes | yes — after the torn tail is truncated |
| dies **strictly before** any byte of the append | **yes** | no — it dies **inside** the append |
| goes through the selector, so the **ceiling is checked** | **no** — it calls `settle::retry` directly | **yes** |
| provisional reservation taken and `Worktree.Verify` run before the kill | yes | yes |
| tabled recovery executed | `close_retained` called directly | **recovery step (e)**, through the real recovery order |

**Why no single construction does all of it, stated so a reader can check it.** The harness can
inject a kill only at a declared sub-effect point (`HookHarness::hook` returns `Proceed` for
`HookPhase::Before` and `HookPhase::After`), and `WorktreeSite::Verify` declares **no** sub-effect
points (`sub_effects()` returns `&[]` for every `WorktreeSite`). So there is no armable instant
between the verification returning and the append beginning; a construction that dies there must
bypass the funnel, which is what the round-2 child does at the cost of the selector. The two
together cover the row, and neither is asked to carry the other's half.

**One observation recorded rather than resolved.** The row's `authoritative_state` says *"no
entitlement held until attempt_started(retry) converts the reservation"*, and at the boundary the
round-2 child read `entitlements_held=1`. That accessor is `Reservations::entitlements_held`, which
returns `ReservationKind::Retry::entitlements()` — the number the held **provisional reservation
reserves** and converts at `attempt_started(retry)` — and `TopologyRun::entitlements_held`
(`src/engine/topology/run.rs:651`) returns exactly that number and nothing else, so this build derives
no separate entitlement from durable state to compare it against. Whether the row's sentence is about
the reservation or about a grant the reservation is not is a reading of the packet, and this report
records the number and the code rather than deciding it.

---

## 6. Required artifact #4 — limit and admission outputs, including ST-12

### 6.1 The frozen limit and the over-limit admission — `G4C1`

The fixture freezes `max_merge_repairs = 0`, so the first rejection's repair must be admitted
`HumanRequired` and nothing may be spent until a person answers.

```
G4C1 run_started.limits.max_merge_repairs=0
G4C1 rejection: merge_rejected s1 candidate=k0g0@b9394005 head=33a81e59 disposition=CodeRejected(Rejected) lease_effect=CreatesLineage { root: TaskKey(0), paths: Prefixes { paths: [GitPath("candidate.txt")] } } repair=k2 admission=HumanRequired{limit:0, q:q-park-fixed kind:Continue options:[...]} lineage=Some(Lineage { root: TaskKey(0), parent: TaskKey(0), index: 0 })
G4C1 states: repair=Some(AwaitingInput) alpha=Some(AwaitingRepair) questions=Some([("q-park-fixed", 2, Admission, None)]) outcome=Ending(Parked)
G4C1 parked-resume progress=[Ok(Blocked { questions: 1 }), Ok(Blocked { questions: 1 })] appended=["run_resumed"] spend_delta=0 runs=0 source_ref_after=Some("b93940059217abb518a3f4979297b9c23e4218fd")
G4C1 step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: false })
G4C1 step2 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4C1 step3 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(2), merged_sha: CommitSha("b519397490a22df517762f62dc722aa6dc352546") })
G4C1 entitlements_held=0 invocations_balance=true transaction_open=false pipeline_held=0 spend_delta=0.2500
```

**Nothing spends until the answer**: across the two blocked steps of the parked resume the only
event appended is `run_resumed`, `spend_delta=0` and `runs=0` (no worker process ran at all). After
the answer the repair runs, spends `0.25`, and merges.

### 6.2 The empty intersection, the validated binding, and the refusals — `G4C2` (ST-12)

The repair's tier floor intersects empty with the task's frozen pin and ceiling, so the ladder has
**zero** rungs and the admission is `HumanBinding`.

```
G4C2 repair ladder: tiers=[] rungs=0 floor=Some(Mid) ceiling=None attempts_per=2 admission=HumanBinding { options: ["claude-code"] }
G4C2 admission=HumanBinding{options:["claude-code"], q:q-park-fixed kind:Unblock q.options:["claude-code"]}
```

**Five observations, and they are not all the same shape — an earlier head of this report said they
were.** Opened here, the harness makes them two ways. The **non-option** is driven through the
production loop with the answer source supplying `gpt-5`, and what is observed is the loop's own
`progress`, the appended-kinds delta and the task state: **a live observation, with no replay half.**
The other four — the second answer, the authorized `attempt_started`, the non-override start and the
override's binding unpinned — go through `refusal_pair`/`refusal_pair_on`, which evaluates the event
against the fold **and** against a hostile replay of the prefix plus that event, and compares the two
`Result<(), String>` values, so `live_and_replay_equal=true` there is equality of the two error
messages. So: **one refusal observed live at the loop; three refusals and one authorization evaluated
live and on replay.** From `measurements/by-tag/G4C2.txt` — verbatim except that the authorized
case's three consecutive lines are joined onto one, and the last refusal, which repeats the preceding
one word for word up to its observed pinning, is elided at the marked ellipsis:

```
G4C2 non-option: progress=[Err("question q-park-fixed asks which agent runs task 2, and `gpt-5` is none of the 1 it offers; a one-off binding activates only through an option the spawn froze, so nothing was appended")] appended=["run_resumed"] state=Some(AwaitingInput)

G4C2-second-answer live=Err("`question_answered` names question `q-park-fixed`, which has already been answered; a question is answered once")
G4C2-second-answer replay=Err("`question_answered` names question `q-park-fixed`, which has already been answered; a question is answered once")
G4C2-second-answer live_and_replay_equal=true both_refused=true

G4C2 authorized attempt_started: attempt_started k2 g0 a1 rung0 binding=claude-code/claude-sonnet-4-5/High/tier Mid/pinned true resume=false mat=Some(Clean)
G4C2-authorized-attempt_started live=Ok(()) replay=Ok(()) live_and_replay_equal=true both_refused=false

G4C2 non-override attempt_started forged binding Small/claude-code/claude-haiku-4-5
G4C2-non-override-attempt_started live=Err("attempt 1 of task 2 runs a binding the run never froze for it: a human named `claude-code`/`claude-sonnet-4-5` at effort `high`, which authorizes tier `mid` pinned `true` on this ladder's floor, and it ran `claude-code`/`claude-haiku-4-5` at effort `low`, tier `small` pinned `false`. Run-start exact bindings are execution identity.")
G4C2-non-override-attempt_started live_and_replay_equal=true both_refused=true

G4C2-override-unpinned-attempt_started live=Err("... it ran `claude-code`/`claude-sonnet-4-5` at effort `high`, tier `mid` pinned `false`. Run-start exact bindings are execution identity.")
G4C2-override-unpinned-attempt_started live_and_replay_equal=true both_refused=true
```

The non-option answer is refused **before any append** — `appended=["run_resumed"]` and nothing
else, and the repair is still `AwaitingInput` — which is a live observation at the loop and is not
compared against a replay. The second answer, the non-override start and the override's own binding
**unpinned** are each refused live and on replay with byte-equal errors; only the exact authorized
binding is accepted, in both;

and the override itself is recorded and read by the attempt:

```
G4C2 override=Some(BindingOverride { key: TaskKey(2), question: QuestionId("q-park-fixed"), option_index: 0, agent: "claude-code", model: "claude-sonnet-4-5", effort: High }) rung_binding(0)=Some(RungBinding { tier: Mid, agent: "claude-code", model: "claude-sonnet-4-5", pinned: true, effort: High }) frozen_rung_binding(0)=None
```

`frozen_rung_binding(0)=None` is the point of the row: **the override is the only way this empty
ladder runs.** The run then completes, `live_equals_replay=true` with `live_overrides=1
replay_overrides=1`.

### 6.3 The single-rung ladder exhausts to the human rung, at the loop — `G4C3`

With the fixture's fixed question-id source replaced by a counting one (the reason a committed test
cannot yet drive this — §11 O2), the overridden repair is driven through its `attempts_per`:

```
G4C3 attempts_per=2 admission=HumanBinding{options:["claude-code"], q:q-park-fixed kind:Unblock q.options:["claude-code"]}
G4C3 step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: false })
G4C3 step2 Ok(Settled { key: TaskKey(2), accepted: false, spent_attempt: true })
G4C3 step3 Ok(Settled { key: TaskKey(2), accepted: false, spent_attempt: true })
G4C3 step4 Ok(Answered { key: TaskKey(2), question: QuestionId("g4r3-q-1"), declined: false })
G4C3 step5 Ok(Settled { key: TaskKey(2), accepted: false, spent_attempt: true })
G4C3 after: repair=Some(AwaitingInput) alpha=Some(AwaitingRepair) rung=Some(0) attempts_on_rung=Some(3) generations=Some(["g0:Closed:attempts1", "g1:Closed:attempts1", "g2:Closed:attempts1"]) open_questions=[("g4r3-q-2", 2, Unblock)] override=true answers=2 runs=6
G4C3 replay_twice_equal=true events=25 outcome=Ending(Parked)
```

The ladder stays at **rung 0** throughout — there is only one — and exhaustion reaches a **fresh
`Unblock` question** rather than `Failed`: the repair is `AwaitingInput`, the lineage alive, and the
override still recorded. **Read the steps rather than the summary, because the escalation is
earlier than the step count suggests:** with `attempts_per = 2` on the one rung, step 2's failure
settles `Closed{Retry}` and **step 3's exhausts the rung**, settling `Parked` on a fresh question
(`g4r3-q-1`, *"2 attempt(s) across 1 rung(s) all failed, and the escalation chain is spent"*). Step 4
**answers** that question, and only then does step 5's third attempt run and park again on
`g4r3-q-2`, which is still open at the end. So the third attempt is authorised by a person, not by
the ladder, and `attempts_on_rung=Some(3)` counts attempts across the three generations rather than
attempts the rung allowed before escalating. This is the packet's *"a single-rung ladder with the kind's attempts_per;
exhaustion escalates to the human rung"*, executed.

### 6.4 The decline, and what it halts — `G4B2`, `G4B3`, `G4B4`

```
G4B3 after: alpha=Some(Failed) repair=Some(Failed) beta=Some(Pending) beta_ready=false halted_at=Some(2) run_is_ending=true outcome=Ending(Halted) lineage_or_candidate_lease_held=false questions=0
G4B4 after: alpha=Some(Failed) repair=Some(Failed) beta=Some(Pending) beta_ready=false halted_at=None run_is_ending=false outcome=Ending(Complete) lineage_or_candidate_lease_held=false questions=0
```

The same decline under a **halting** policy sets `halted_at=Some(2)` and derives `Ending(Halted)`;
without it, `halted_at=None` and `Ending(Complete)`. In both the dependent `beta` is blocked
(`beta_ready=false`, still `Pending`), every holding is released
(`lineage_or_candidate_lease_held=false`) and the lineage's questions are gone. **A forged second
release of the lineage lease is refused in both**, live and on replay with equal errors:

```
G4B3-second-lineage-release live=Err("`task_merged` names integration sequence 1, and the open transaction is none; an event applies to the transaction it belongs to or to none")
G4B3-second-lineage-release live_and_replay_equal=true both_refused=true
G4B4-second-lineage-release live_and_replay_equal=true both_refused=true
```

---

## 7. Required artifact #5 — the payload freezing check

The rejection freezes the repair's whole spawn payload in the `merge_rejected` event, and every
later dispatch must run **that** payload rather than one re-derived. `G4C1` prints the frozen JSON
in full (`measurements/by-tag/G4C1.txt`, one line of 1,811 bytes) — the repair's key, display id, origin,
spec (kind, title, body naming the rejected candidate sha and its ref, acceptance, path hints,
artifacts), deps, ladder (tiers, `attempts_per`, rungs, floor, ceiling, effort map, admission),
reviews, allowed agents and lineage — and then compares it against the registry after the answer,
the dispatch and the merge:

```
G4C1 repair ladder: tiers=[Mid] rungs=["Mid/claude-code/claude-opus-5/pinned false"] floor=Some(Mid) ceiling=Some(Mid) attempts_per=2 admission=Runnable
G4C1 freezing: registry_entry_equals_frozen_payload_entry=true dispatched_source_equals_rejected_candidate=Some(true) override=None source_ref_after_merge=Some("b93940059217abb518a3f4979297b9c23e4218fd") object_exists_after=Ok(true) runs=3
```

`G4C2` prints the same for the `HumanBinding` shape, where the frozen ladder is `"rungs":[]` and the
admission carries the option list the answer must name.

**The duplicate a frozen payload must not be re-registered by.** The same `merge_rejected` record is
appended again at its own sequence and at the next, and refused both times, live and on replay, with
**the registry's size and the lineage's member count unchanged** — which is what these two readings
are, and is weaker than "the registry is unchanged": the measurement prints
`registry.len()` and `lineage_members(ALPHA)`, not a comparison of the registry's contents. **Nor
does the committed test compare contents**, and an earlier head of this report said it did: opened
here, `a_rejection_already_folded_is_refused_when_it_arrives_again`
(`src/topology/fold/tests.rs`) applies the rejection once, then for each of two sequences asserts
the refusal's message names `merge_rejected`, and afterwards asserts `registry().len()` is what it
was, `task_state(TaskKey(3))` is `Pending`, `task_state(MID)` is `AwaitingRepair` and
`lineage_members(MID)` is `Some(1)`. **Four state readings and a length — no comparison of the
registry's contents exists anywhere in this gate's evidence, and none is claimed.** What the pair
does establish is that the repeat is refused and that nothing it would have registered appears:

```
G4C1-duplicate-merge_rejected-same-sequence live=Err("`merge_rejected` names integration sequence 1, and the open transaction is none; an event applies to the transaction it belongs to or to none")
G4C1-duplicate-merge_rejected-same-sequence live_and_replay_equal=true both_refused=true
G4C1-duplicate-merge_rejected-next-sequence live=Err("`merge_rejected` names integration sequence 2, and the open transaction is none; ...")
G4C1-duplicate-merge_rejected-next-sequence live_and_replay_equal=true both_refused=true
G4C1 registry_len_after_refusals=3 lineage_members=Some(1)
```

---

## 8. Required artifact #6 — repair-source protection evidence

R11: the rejected candidate's ref *"protected while the run can resume, whatever the candidate's
state"*. Measured by reading the ref and the object at each transition, and by comparing what the
dispatch actually named.

| Transition | Ref after | Object | Dispatch source equals the rejected candidate |
|---|---|---|---|
| after a **code rejection produced by `integrate`** (`G4B5`) | `Some("b93940059217abb518a3f4979297b9c23e4218fd")` | `Ok(true)` | `Some(true)` |
| after a **conflict rejection produced by `integrate`** (`G4B6`) | `Some("b9394005…")` | `Ok(true)` | `Some(true)` |
| across a **Parked** resume, twice blocked (`G4C1`) | `Some("b9394005…")` | — | `Some(true)` at the dispatch after the answer |
| across a **BudgetExceeded** resume (`G4E`) | `Some("b9394005…")` | `Ok(true)` | `Some(true)` |
| after the repair's own **merge** (`G4C1`) | `Some("b9394005…")` | `Ok(true)` | — |

```
G4E before: budget_stop=Some(0) outcome=Ending(BudgetExceeded) source_ref=Some("b93940059217abb518a3f4979297b9c23e4218fd") expected_refs_has=true object_exists=Ok(true)
G4E after: budget_stop=None source_ref=Some("b93940059217abb518a3f4979297b9c23e4218fd") dispatched_source_eq=Some(true) alpha=Some(Merged) repair=Some(Merged) object_exists=Ok(true)
G4B5 source_ref_after=Some("b93940059217abb518a3f4979297b9c23e4218fd") rejection_disposition=Some("CodeRejected(GatesFailed)") rejection_candidate_eq_planted=Some(true) dispatch_source_eq=Some(true) object_exists=Ok(true)
G4B6 source_ref_after=Some("b93940059217abb518a3f4979297b9c23e4218fd") rejection_is_conflict=Some(true) rejection_candidate_eq_planted=Some(true) dispatch_source_eq=Some(true) object_exists=Ok(true)
```

The ref is also in `expected_refs` (`G4E`: `expected_refs_has=true`), so a resume that found it
missing would refuse rather than proceed. A **missing or substituted** source is refused before any
append — `a_repair_whose_source_candidate_is_missing_is_refused_before_any_append`, killed by
mutation M21 (§3).

**What is not proven, stated plainly.** No production caller of `Ref.DeleteCandidatesRef` exists at
this sha, and **no committed test would notice one appearing**: mutations M22 and M23, which add a
ref deletion to the conflict arm and to the code-rejection arm of `integrate`, both survive the full
suite here (§3). So the protection holds at this sha by end-to-end measurement and by the structural
absence of a caller, not by a committed regression witness. That gap is §11 O1, carried forward, and
it is named again in the verdict.

---

## 9. Row 10 — the corrected row, what the correction gave up, and every element constructed alone

### 9.1 The row was corrected in the packet on 2026-09-09. Plainly, and by whom

**The packet's row 10 is not the row the first two runs were judged against.** It was amended on
2026-09-09 by the Gate 4 driver session, under the owner's standing instruction of the same day
recorded verbatim in `~/tactus-artifacts/HANDOFF-G4-DRIVER.md` §1 (*"You have all authorization
necessary to complete the PRs into main and to start new sessions, get Gate 4 verified and done,
whatever it takes"*). The amendment record is
`~/tactus-artifacts/2026-09-09-AMENDMENT-g4-row-10.md`; the decision it applies verbatim is
`~/tactus-artifacts/G4-ROW10-DECISION.md` §6; the amendment was independently audited on 2026-09-10
by `gpt-6-astra` at `max` (`~/tactus-artifacts/2026-09-10-amendment-audit-astra.md`,
`CHANGES_REQUIRED`), and the record carries that audit's findings and their adjudication.

**The amendment was verified here first-hand, not cited.** The packet is on this box
(`~/tactus-artifacts/tactus-parallel-design-neutral-v16.json`) together with its pre-amendment
backup, and this run read both:

| | Measured here | The record's claim |
|---|---|---|
| `gates[3].id` | `"G4"`, name *Repair execution, retries, lineages*, `frozen_input_range` *merged PR9 on top of G3* | — |
| `adversarial_fault_tests` | **13** entries | 13 (the count nowhere stated in the packet) |
| `required_artifacts` / ownership rows | **6** / **8** | 6 / 8 |
| backup sha256, size | `02bfed758c72a0ccdd91c11a6aaf9a59683a6e1f914356ab0153ec341a455df6`, 644,875 bytes | identical |
| current sha256, size | `4d59e71165c417639898ecac5f5560f9f5c7fceb171d7f31a2c840fe732de1ed`, 645,540 bytes (+665) | identical |
| leaf-by-leaf structural diff, before vs after | **exactly one differing path**: `cumulative_review_gates.gates[3].adversarial_fault_tests[9]` | identical |

So the claim that exactly one leaf moved, and which, is established by this run rather than taken on
trust; and the row quoted below is read out of the packet, not out of a working extract.

Both quotations below are the packet's text exactly; **the bold is added here** to mark what moved,
and the packet is backtick-free, so no code formatting appears in either.

**BEFORE** (what runs 1 and 2 were judged against):

> kill after materialization before attempt_started leaves index-referenced merge objects that a
> later scrub releases to Git; the interrupted-materialization residue class (unreferenced objects
> plus CHERRY_PICK_HEAD/index.lock, constructed synthetically and sampled by killing the cherry-pick
> child) **fails Worktree.Verify, so the worktree is recreated and the materialization reproduced
> deterministically**

**AFTER** (the row this gate is judged against, read out of the packet at
`cumulative_review_gates.gates[3].adversarial_fault_tests[9]`):

> kill after materialization before attempt_started leaves index-referenced merge objects that a
> later scrub releases to Git; the interrupted-materialization residue class (unreferenced objects
> plus CHERRY_PICK_HEAD/index.lock, **constructed synthetically element by element** and sampled by
> killing the cherry-pick child) **classifies Internal for every element** and recovers by the tabled
> action: an element in the worktree's git dir (CHERRY_PICK_HEAD, index.lock, and the state files and
> held locks the verifier reads) fails Worktree.Verify, so the worktree is recreated **with force**;
> **the object-store elements (unreferenced objects, temporary object files) are R27, survive reuse
> and recreation alike, and by themselves do not fail Worktree.Verify**, whose rule is HEAD at the
> recorded base, the index unlocked and no cherry-pick/merge/sequencer state; in every case the
> materialization is reproduced deterministically, one pick onto the restored base tree, to the tree
> and checkout an uninterrupted materialization produces, after which the index references the merge
> objects

### 9.2 What the correction relaxed, and what it did not

**It is a relaxation of an acceptance requirement, and this report says so rather than implying
nothing was given up.** BEFORE required the class to *"fail Worktree.Verify, so the worktree is
recreated"*. AFTER says the two object-store elements *"by themselves do not fail Worktree.Verify"*.
So **standalone unreferenced-object and temporary-object-file residue no longer has to demonstrate
verifier failure and a forced recreation.** That removal *is* the correction — the decision's finding
is that the obligation was impossible to meet for those two elements and contradicted the packet's
own verifier rule and its own ownership matrix, which puts them under R27 (Git's object store) while
the worktree and its administrative residue are R9 — but it is a relaxation, it was authorised, and
a reader must not be left thinking the row merely became clearer.

**What did not get easier, and one thing that got harder.** AFTER still requires the synthetic
construction to be **element by element**, still requires `Internal` **"for every element"**, still
requires the sampled child-kill evidence, still requires the tabled recovery, still requires
worktree-local residue to fail `Worktree.Verify` and be recreated — now expressly **"with force"** —
and still requires deterministic reproduction **"in every case"**, now spelled out as *one pick onto
the restored base tree, to the tree and checkout an uninterrupted materialization produces*. BEFORE
never used the words `Internal` or "element by element". **On its face the per-element obligation is
stricter than before, not looser**, and it is precisely the obligation verification 3 found unmet.

**This report does not re-derive the decision.** Its measurements — the strace of the pick, the
48-spawn kill census in which 36 of 36 kills that died inside the pick left `index.lock` and none
left an object-store element alone, the fifteen observables identical after reuse and after
recreation, the 1,218 unreachable objects a store-consulting verifier would refuse this repository
on — are settled and are cited, not repeated. What this report does is discharge the row's evidence
obligation on **this** range, by running it.

### 9.3 Every element constructed alone — observed, not argued

`Object.RepairMaterialize` registers **four** residue elements. The committed test
`repair_materialization_synthetic_residue_recreated_after_forced_removal`
(`src/engine/topology/dispatch/tests.rs`) asserts that count and then calls
`synthetic_materialization_residue_element` once per element. In this range that helper builds **a
repository per element** — `Run::started("synthetic-materialization")` inside the loop, so each
element's store is its own — and makes the reading **twice**: once before the element is constructed,
where nothing the site registers may be observed and the class must be `None`, and once after, where
that element and nothing else must be observed. That pair is what makes the `Internal` between them
the element's own; it is the direct repair of verification 3's finding 1, which found the previous
form planting an orphan first and keeping it while iterating the rest in one shared store.

**Verified here by running it**, with read-only `eprintln!` probes added beside (not in place of) the
committed assertions and then removed. Full output `g4r3-evidence-7e0110a1/row10/residue-tests.log`:

```
G4R10 UnreferencedObject store=/tmp/upstroke-wm-synthetic-materialization-1806020-2/repo  before: observed=[] class=None unreachable=0 tmp_files=false
G4R10 UnreferencedObject  after-construct: observed=[UnreferencedObject] class=Internal unreachable=1 tmp_files=false
G4R10 UnreferencedObject       recovery: reuse=Reused    breaks_quiescence=false materialized=Some(Clean) picks=1 tree_equal=true class_after=After
G4R10 TemporaryObjectFile store=/tmp/upstroke-wm-synthetic-materialization-1806020-12/repo before: observed=[] class=None unreachable=0 tmp_files=false
G4R10 TemporaryObjectFile after-construct: observed=[TemporaryObjectFile] class=Internal unreachable=0 tmp_files=true
G4R10 TemporaryObjectFile      recovery: reuse=Reused    breaks_quiescence=false materialized=Some(Clean) picks=1 tree_equal=true class_after=After
G4R10 IndexLock          store=/tmp/upstroke-wm-synthetic-materialization-1806020-16/repo before: observed=[] class=None unreachable=0 tmp_files=false
G4R10 IndexLock          after-construct: observed=[IndexLock] class=Internal unreachable=0 tmp_files=false
G4R10 IndexLock                recovery: reuse=Recreated breaks_quiescence=true  materialized=Some(Clean) picks=1 tree_equal=true class_after=After
G4R10 CherryPickHead     store=/tmp/upstroke-wm-synthetic-materialization-1806020-20/repo before: observed=[] class=None unreachable=0 tmp_files=false
G4R10 CherryPickHead     after-construct: observed=[CherryPickHead] class=Internal unreachable=0 tmp_files=false
G4R10 CherryPickHead           recovery: reuse=Recreated breaks_quiescence=true  materialized=Some(Clean) picks=1 tree_equal=true class_after=After
```

(Whitespace aligned for reading; the log's lines are otherwise verbatim.) Read off the four
triples, against the corrected row clause by clause:

| Corrected row's clause | Observed here |
|---|---|
| constructed **element by element** | four distinct stores (`…-2`, `…-12`, `…-16`, `…-20`), one per element |
| **alone** — no other residue supplying the answer | each store's reading *before* construction is `observed=[] class=None unreachable=0 tmp_files=false`. The `unreachable=0` is the specific answer to verification 3's finding: no orphan from an earlier element is present |
| **classifies `Internal` for every element** | all four: `class=Internal`, with `observed=[<that element>]` and nothing else |
| git-dir element fails `Worktree.Verify`, worktree **recreated with force** | `IndexLock` and `CherryPickHead`: `reuse=Recreated`, `breaks_quiescence=true` |
| object-store elements are R27 and **by themselves do not fail `Worktree.Verify`** | `UnreferencedObject` and `TemporaryObjectFile`: `reuse=Reused`, `breaks_quiescence=false` |
| the materialization **reproduced deterministically, one pick**, to the tree an uninterrupted one produces | all four: `materialized=Some(Clean)`, `picks=1`, `tree_equal=true` (against a control worktree materialized without interruption), and the committed test additionally asserts the **checkout bytes** equal the control's |
| after which **the index references the merge objects** | all four: `class_after=After` |
| the object-store residue **survives reuse and recreation alike** | the committed test asserts the planted object and the planted temporary file still exist after each recovery; a second committed test, `a_forced_recreation_preserves_the_object_store_residue_it_recovers_over`, constructs both object-store elements *plus* `index.lock`, asserts the recovery really did recreate, and asserts both are still there afterwards — the recreation half the isolated test structurally cannot cover |

**No element failed to classify `Internal` alone.** Had one, that would be a gate failure and this
report would say so.

### 9.4 Why the temporary-object-file half is now real, measured on this box

Verification 3's second half was that `temporary_object_files` scanned `objects/` and `objects/pack/`
only, so git's *actual* temporary file — which git writes in the object's **fan-out** directory —
classified `None`, and the old synthetic test planted its stand-in at the object root, exercising the
arm the scan already had rather than the element. Reproduced here from scratch, git 2.43.0, a real
`SIGKILL` inside `git hash-object -w` of a 300 MB blob
(`g4r3-evidence-7e0110a1/row10/where-git-writes-tmp-obj.log`):

```
$ find .git/objects -name "tmp_obj*"
.git/objects/92/tmp_obj_wTKgLq
$ git count-objects -v
warning: garbage found: .git/objects/92/tmp_obj_wTKgLq
garbage: 1
$ git prune -n
Removing stale temporary file .git/objects/92/tmp_obj_wTKgLq
$ git fsck --unreachable --no-progress --no-dangling --connectivity-only
(names it not at all)
```

The file is in the fan-out directory `92/`, `git prune` calls it its own — which is the set R27 says
Git prunes — and `fsck` does not see it, so `UnreferencedObject` cannot stand in for it. In this
range `temporary_object_files` keeps its object-root and `pack` arms **and** iterates git's canonical
`00`–`ff` fan-out paths, resolving each with `fs::metadata` so a symlinked fan-out is followed and a
case alias is seen. The synthetic element is planted in a fan-out directory the store already holds,
beside real objects. Both halves are witnessed by mutation in §3 (MT1 removes the fan-out arm; MT5
removes the root arm; MT4 restores the symlink defect; MT2 blinds `unreachable_objects`).

### 9.5 The four items §6 of the decision asked run 2's report to rewrite, resolved here by name

`G4-ROW10-DECISION.md` §6 asked for four things in run 2's report — `2026-09-09-gate-G4.md`, which
exists only on the unmerged branch `gate/g4-corrected-range` at `82de0767`, pull request #257. That report is a `CHANGES_REQUIRED` record of a failed run and is **not** being amended: the
packet's process clause says *"A failed gate re-runs on the corrected range"*, and the range has moved
because #258 merged, so editing that report into a pass would substitute an edit for the re-run the
clause requires. (The packet nowhere states a literal prohibition on amending a failed gate's report;
that is an inference from the re-run clause, and it is marked as one — the same marking the amendment
record makes.) **This report is where the four are resolved.**

**Item 1 — run 2's §9 row-10 status cell.** Run 2 wrote: *"not met as written for the two
object-store elements; met on the corrected row, whose correction is owed to the packet and not yet
applied"*. **What is true now:** the correction is applied — 2026-09-09, by the driver under the
owner's standing authorisation, recorded and audited (§9.1) — so there is no longer a row "as
written" that differs from the row this gate is judged against. Row 10 **is met**, on the packet's
current text, and the status cell in §10 below says `met` without qualification. **Evidence:** §9.3's
four per-element triples and §9.4's reproduction, both produced at `7e0110a1`; §10 row 10's sampled
half; the mutations MT1–MT5, M14, M15, M36, M37 in §3.

**Item 2 — the paragraph ending "it engages no clause of the pass rule". Withdrawn, explicitly.**
Run 2's first version wrote that the difference between the tree and row 10 *"engages no clause of the
pass rule"*. **That position is withdrawn in full, and this report does not rest on any part of it.**
It was wrong for the reason verification 2 gave: *"every G4 test green" includes the row's specified
behaviour*, and **a gate report may not narrow its own acceptance condition**. Row 10 is inside the
first pass-rule clause, it is judged there in §13, and it is judged against the packet's text and not
against a reading of it that this report finds convenient. The withdrawal is not conditional on the
amendment: even if the row had not been corrected, the sentence would still be wrong, and the gate
would have failed on it. This paragraph is the explicit withdrawal the amendment record's audit
required, and it is not a mention of the amendment date standing in for one.

**Item 3 — run 2's §11 finding O4.** Run 2 filed
`G4-O4-ROW10-VERIFY-CLAUSE-CORRECTION-OWED` (P2, `docs-contract`, filed as
`P2_docs-contract_202609091845_g4-row-10-mandates-recreation-for-object-store-residue-the-verifier-cannot-read.md`
on that same unmerged branch, never merged, and absent from this tree). **What is true now: the finding is closed by the 2026-09-09
amendment**, which is exactly the act it asked for. It is **not** carried onto this branch. Its
remediation sentence also drew verification 3's third finding — it recommended amending an immutable
dated decision record — and that is a second reason not to carry it forward: the correct treatment of
a retired dated record is to point readers at the dated correction and at the living authority, not to
rewrite it. This report does that: the retired `decisions.effect_site_inventory.
command_internal_sub_effects` text carries the same over-statement and is **not** amended here; its
binding substance in this repository is `design/26_design_merge_queue_protocol.md`, which states the
per-element requirement as *"synthetic records construct, recover and classify every element the class
lists"* with **no `Worktree.Verify` clause**, so the living design already agrees with the tree, and
§6 of the decision supplies the replacement sentence for anyone still consulting the retired text in
the private companion repository.

**Item 4 — run 2's closing item 3.** Run 2's verdict carried a numbered condition: *"Row 10 is not
met as written, and the pass is conditional on the owner correcting it in the packet."* **What is
true now: that condition is discharged.** The correction is applied; the code half verification 3
found missing is landed in this range and is verified here by construction and by mutation. §13
therefore carries **no** row-10 condition, and the verdict below is not a conditional pass on that
account. It remains conditional on nothing except the two inherited owner deferrals, which §13 names
for what they are.

---

---

## 10. The thirteen adversarial fault tests

Read from `cumulative_review_gates.gates[3].adversarial_fault_tests` in the packet, not from a
summary. There are **thirteen**; all thirteen are here, one row each. Each row says what holds the
claim, the result at `7e0110a1`, **whether this gate ran it or reasoned it**, and the mutation from
§3 that shows the named test still detects. Every committed test named here passes in §1's green
cumulative run; the `G4…` measurements are this run's temporary ones, quoted in §4–§9.

| # | Adversarial claim | Held by | Result | Ran / reasoned | Detection witness (§3) |
|---|---|---|---|---|---|
| 1 | two overlapping lineages resolve in creation order; no deadlock | `two_lineages_publish_in_lineage_order_and_the_younger_candidate_waits_behind_the_older`; the range's `a_lineage_created_after_a_release_waits_behind_the_older_survivor_it_widens_onto_live_and_on_replay` and `an_age_once_granted_is_never_reused_and_a_lineage_created_after_a_release_waits_behind_every_survivor`; `G4A1`/`G4A2` (§4.1: both schedules, one publication order, `Ending(Complete)`, empty tables); `G4A3` (§4.2: a lineage created after a release — the survivor dispatched and published first) | **met** | **ran** | M01 (order inverted), M03 (lease never released), MA1 (the original defect restored), MA2 and MA3 (the counter's other two failure shapes) |
| 2 | an overlapping non-lineage candidate is skipped until the lineage merges or fails, then re-verified stale | the queue rule `an_ordinary_candidate_waits_for_any_lineage_and_a_member_only_for_older_ones`; **constructed here** for the loop: `G4B1` (waits `InsideLineage`, the lineage merges, the candidate re-verifies on the moved head) and `G4B2` (the lineage is declined, the candidate publishes in the next step, fast because the head never moved). No committed test drives the loop through this shape | **met** | **ran** (constructed) | M02 (`InsideLineage` dropped), M04 (holdings not released on lineage failure) |
| 3 | over-limit rejection registers `AwaitingInput`; nothing spends until the answer | `an_over_limit_repair_spends_nothing_until_its_answer_activates_it`; `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`; `G4C1` (§6.1: only `run_resumed` appended across two blocked steps, `spend_delta=0`, `runs=0`) | **met** | **ran** | M17 (never `HumanRequired`), M16 (a `Runnable` past the limit accepted), M33 (answers never polled) |
| 4 | empty pin/ceiling intersection registers `HumanBinding`; a validated answer activates a single-rung ladder; a non-option and a second answer refused; a non-override `attempt_started` refused (ST-12) | `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`; `picking_the_last_of_two_offered_agents_binds_the_repair_to_it_rather_than_declining`; `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`; `an_override_replaces_the_frozen_binding_for_every_later_attempt`; `G4C2` (§6.2: **one refusal observed live at the loop** — the non-option `gpt-5`, driven through a step and refused before any append — and **three refusals and one authorization each evaluated live and on replay** with byte-equal errors; the uniform "four refusals … each live and on replay" of an earlier head was withdrawn in §6.2 and is withdrawn here too); `G4C3` (§6.3: the single rung's exhaustion reaches a fresh human rung, at the loop) | **met** | **ran** | M18 (no `HumanBinding` ladder), M06 (non-override start accepted), M08 (second answer accepted), M09 (out-of-range option accepted), M35 (the override not a pin); **M34** (override no longer single-rung) is caught by `G4C3` **only** — no committed test — and is filed (§11 O2) |
| 5 | a declined admission fails the lineage; the lease is released; dependents `Blocked`; a decline under a halting policy sets `halted_at` | `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`; `declining_a_repair_admission_fails_the_lineage_and_allows_the_run_to_end`; `a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay`; `G4B3` (`halted_at=Some(2)`, `Ending(Halted)`, the dependent not ready) and `G4B4` (the same without a halt: `Ending(Complete)`); a forged second release refused live and on replay in both | **met** | **ran** | M04 (holdings kept), M05 (member failure does not fold the lineage), M19 (no `halted_at` on a halting decline) |
| 6 | a repair with an already-present source behaves as an ordinary attempt; an empty diff fails honestly | `an_already_present_source_proceeds_as_an_ordinary_attempt_whose_empty_diff_fails_honestly` (`Materialization::Empty`, the attempt starts, the capture is the base's tree, the assessment is `EmptyDiff`); `a_repair_dispatch_records_what_its_materialization_observed` (`Clean`, `Empty`, `Conflict` each recorded; `G4B6` observes `Conflict` through the loop) | **met** | **ran** | **ME1** (`Empty` observed as `Clean`), **ME2** (an `Empty` start refused instead of proceeding), **ME3** (an empty diff no longer fails) — each kills the named test |
| 7 | a same-session retry on a repair generation records `LineageHeld` and does not repeat the materialization; a wrong disposition refused | `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` (one `Object.RepairMaterialize` execution across two attempts); `a_closing_generation_admits_exactly_the_disposition_its_holding_implies`; `G4G` (§5.2: `attempt_interrupted` and `generation_closed` with either predicted disposition refused, `LineageHeld` accepted, live and on replay, on a real in-flight then retained repair generation) | **met** | **ran** | M12 (`Retained` not recorded); **MDISP1** (any disposition accepted) and **MDISP2** (the expected disposition flipped on both sides) |
| 8 | a `Retained` repair generation missing its worktree, or resumed: `generation_closed{LineageHeld}` then a fresh generation re-materialized from the protected source (ST-11) | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` (the resumed branch, end to end); **`G4G`** (the missing-worktree branch, live, through the loop: the close appended and applied, the lease held, the next step dispatching generation 1 from the same source and materializing it again) | **met** | **ran, both branches driven** | M13 (recovery closes no retained generation), M29 (closed generations never reclaimed), M30 (a failed verify starts the retry anyway). `G4G` itself **passes** under M30 — it prints the close and asserts nothing about it, so it is an observation of the branch and not a detection witness, and this report says so rather than counting it |
| 9 | a parked or budget-stopped lineage redispatches after resume with its source candidate object present (R11) | `a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume`; the over-limit pair (Parked); `G4E` and `G4C1` (§8: ref and object read before and after each resume and after the merge); `G4B5`/`G4B6` (rejections produced by `integrate`) | **met** | **ran** | M21 (an absent or substituted source not refused) caught. **M22 and M23 survive** — a rejection arm that deleted the ref would not be caught by a committed test — and that is filed (§11 O1) and named in the verdict |
| 10 | the interrupted-materialization residue class, in the corrected row's words: constructed synthetically **element by element** and sampled by killing the cherry-pick child; `Internal` for **every** element; git-dir elements fail `Worktree.Verify` and force a recreation; the object-store elements are R27, survive reuse and recreation alike and by themselves do not fail `Worktree.Verify`; in every case the materialization reproduced deterministically, one pick, to the tree and checkout an uninterrupted one produces, after which the index references the merge objects | **the synthetic half:** `repair_materialization_synthetic_residue_recreated_after_forced_removal` — four elements, each in its own repository, each read before and after (§9.3); `a_forced_recreation_preserves_the_object_store_residue_it_recovers_over` (R27 across a recreation); `repair_materialization_objects_released_to_git_on_scrub` (R9 → R27 by `fsck`). **The sampled half:** `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`, run five times here (below). **The composed half, with what each actually does:** `repair_materialization_reproduced_after_kill` **kills a real subprocess** at the site's Before and After hooks; `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states` **constructs** the two post-index-write states (`K2`, `K3`) a kill would leave and recovers from each, without killing; `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` **plants** an interrupted dispatch and drives recovery across the process boundary, without killing | **met** | **ran** — both halves, and the per-element classifications observed and quoted (§9.3) | M14 (a failed verify not recreated), M15 (no base restore before the pick), M36 (recovery step (g) verifies nothing), M37 (the continuation does not materialize); and this run's own **MT1** (the fan-out arm removed), **MT5** (the object-root arm removed), **MT4** (the symlinked fan-out defect restored), **MT2** (`unreachable_objects` blinded), **MT3** (the quiescence partition flipped), **MT7** (a recovery that prunes the shared store) |
| 11 | attempted overtaking refused live and on replay | `an_integration_starts_only_for_the_first_eligible_candidate`; `the_queue_is_ordered_by_creation_and_not_by_preparation`; `G4A1` (a forged `merge_verification_started` and a forged fast `merge_prepared` for the younger lineage's candidate ahead of the older: both refused, live error equal to the replay error); `G4A3` (the same for a lineage created after a release) | **met** | **ran** | M01; MA1, MA2, MA3; **MQ1** (the queue offers the last eligible candidate instead of the first) |
| 12 | kills at T-REPAIR-DISPATCH, T-ANSWER, T-RETAINED and T-RETRY | §5's table, every row: real kills at T-REPAIR-DISPATCH (`repair_materialization_reproduced_after_kill` and the sampler), **at T-ANSWER** (`G4K-torn`, `G4K-complete`: the coordinator killed inside and just after the ingestion's append, §5.1), at T-RETAINED (`retained_generation_not_continued_after_kill`), and **at T-RETRY's own boundary**, twice: `G4RETRY2` (§5.4b) kills a real subprocess on the retry path **through `TopologyRun::step`** — so the selector's ceiling check has run — inside the append that would have recorded the retry, and the truncated prefix is the row's `RetainedIdle` state, from which **recovery step (e)** closes before `run_resumed`; `G4RETRY` (§5.4c) is the companion that dies strictly before any byte of the append. §5.4a shows the ceiling gating the retry branch by changing nothing but the ceiling | **met** | **ran — all four sites, with real children.** Two earlier attempts at this row are corrected rather than reworded: the first head cited a kill of the **next** transaction (after the retry starts), and the second head's child reached the boundary but **bypassed the selector, so it checked no ceiling** — that claim is withdrawn in §5.4. §5.4c tables what each of the two witnesses shows and what it does not. (The fault matrix's finalization rows are **not** among row 12's four; they are PR10's, and §2 and §5 record why this build cannot reach them: no production `run_finished` emitter, and `Step::Closure` refused, observed refusing in `G4B3` and `G4B4`) | as §5's rows cite; the T-ANSWER kills are measurements, and the refusal they rest on (a second answer) is M08's. The answer file's survival at the production reader is **MR1**'s, caught by `G4K` **only** — filed (§11 O9). T-RETRY's tabled recovery is also guarded by the four committed tests **M13** kills; `G4RETRY` survives M13 and is an observation, not a detection witness (§5.4c) |
| 13 | duplicate `merge_rejected` replay refused | `a_rejection_already_folded_is_refused_when_it_arrives_again` (same sequence and next sequence; registry and lineage count unchanged); `G4C1` (the same record appended again at its own sequence and the next: refused live and on replay with byte-equal errors, and the registry's **size** and the lineage's **member count** unchanged — `registry_len_after_refusals=3`, `lineage_members=Some(1)` — which are two lengths, not a comparison of contents; the committed test carries that) | **met** | **ran** | the refusal rests on **five** independent guards — the settled transaction, the dense repair key, the registered display id, the lineage member index and the R14 budget rule — each witnessed alone by committed tests when removed alone (**MD1**, **MD2**, **MD3**, **MD6**, **M16**), and **MD7** — five edits, all five guards — kills the named duplicate test itself. **MD4 is three edits, not four**: it removes the settled transaction, the dense key and the display id and leaves the member-index and R14 rules standing, and the named test survives it. So what is established is that the test dies when **all five** go and survives that **three-guard** subset; the report earlier said "any four", which no construction here ran |

**The sampled half of row 10, measured here.** The row says *"sampled by killing the cherry-pick
child"*, and the sampler that does it for **this** site spawns
`git cherry-pick --no-commit <source>` in the repair generation's own worktree and kills it —
`sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`, not G3's
`Object.ProposalCherryPick` sampler, which kills a cherry-pick at a different site. Five runs here,
with a temporary `eprintln!` printing the census before its floor assertions
(`measurements/samplers.log`):

| Run | Spawns | Died by the kill | Completed (controls) | Classifier refusals | Kill classes (`None`/`Internal`/`After`) | Kills while writing | Probe |
|---|---|---|---|---|---|---|---|
| 1 | 8 | 8 | 0 | 0 | 6 / 1 / 1 | 2 | 917.81 µs |
| 2 | 8 | 8 | 0 | 0 | 7 / 1 / 0 | 1 | 759.37 µs |
| 3 | 8 | 8 | 0 | 0 | 4 / 4 / 0 | 4 | 840.48 µs |
| 4 | 8 | 8 | 0 | 0 | 6 / 1 / 1 | 2 | 800.25 µs |
| 5 | 8 | 8 | 0 | 0 | 6 / 2 / 0 | 2 | 789.29 µs |
| **total** | **40** | **40** | **0** | **0** | **29 / 9 / 2** | **11** | — |

Every kill in every run was classified into one of the site's three classes and recovered to the
control's tree and checkout; every run contained at least one kill that landed **while the pick was
writing**, which is that sampler's own floor and the thing the two earlier reviews of its siblings
found accepted vacuously.

**One nuance in those numbers, read off rather than glossed:** `completed=0` in every run means no
pick outran its kill, so `budget.completed(clock)` was never called and the re-aiming the range added
never had to engage — `completions=0` on every line. The ladder stayed on the probe's median for all
forty spawns and still put 40 of 40 kills inside a running child, 11 of them mid-write. That is the
schedule working on this platform; it is **not** evidence that the re-aiming works, and this report
does not claim it is. It is also why the sampler's *"a pick that completed left its after phase and
nothing else"* assertion passed vacuously here: there were no completions to check.

**And the sampler the brief warned about.** `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered`
— G3's `Object.ProposalCherryPick` sampler, the one carrying
`RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE` — was run five times here too: **40 spawns, 40 kills, 0
refusals, 30 `None` / 9 `Internal` / 1 `After`, 9 kills while writing**, probe 0.98–1.11 ms. **The
cold-probe shape did not fire in any of the ten runs**, and the mechanism the finding names is no
longer in the tree: this range replaced *"the shorter of two probe picks"* (the wording of the
sampler's own doc at `81ee09ef`) with a discarded warm-up plus the median of three, re-aimed inside
every completed pick, plus a whole second batch when no kill landed inside a writing pick, and a
refusal when none ever does. **If it fires on a required leg of this gate's own pull request, that is
the known finding and not a new one** — and the sampled half of row 10 does not rest on it: row 10's
site is `Object.RepairMaterialize`, whose forty kills are tabled above.

---

## 11. Invariants, observations, findings and deferrals

### 11.1 The six integrated invariants, and where each is established here

The definition's `integrated_invariants` are six entries. Each is established below by observation
on this range; none is claimed for a portion an earlier gate owns.

| # | Integrated invariant | Established by |
|---|---|---|
| 1 | INV-11 (execution), INV-16 (PR9 portion: complete lifecycle incl. terminal failure), INV-19 (overrides), INV-06 (`Retained` repair generations incl. same-generation retry) | the full lifecycle driven end to end in `G4A1`/`G4A2` (`Ending(Complete)`, every holding released) and terminated in `G4B3`/`G4B4` (a decline fails the lineage and its members) and in the committed `a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay`; overrides in `G4C2`/`G4C3` (§6.2, §6.3); the same-session retry in `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` and `G4G`. Mutations M04, M05, M06, M12, M19, M34, M35 |
| 2 | `LeaseDisposition` validity; `LineageHeld` on repair generations | `G4G` forges `attempt_interrupted` and `generation_closed` with **either** predicted disposition on a real in-flight and then retained repair generation: both refused live and on replay, `LineageHeld` accepted, and the lineage lease unchanged across the close (`lineage_held=true`). Committed: `a_closing_generation_admits_exactly_the_disposition_its_holding_implies`. Mutations MDISP1, MDISP2 |
| 3 | `satisfies` closure canonical for lineage candidates | each lineage publication's `satisfies` set is the lineage root **and** the member that published, and nothing else: `G4A1` `[(2, [0, 2], Lineage{0}), (3, [1, 3], Lineage{1})]`; `G4A2` the same list under the other schedule; `G4A3` `[(2, [0, 3], Lineage{0}), (4, [1, 4], Lineage{1})]`; `G4B1` a lineage publication `[0, 2]` beside an **ordinary** candidate's `[1]` releasing a `Candidate` lease rather than a lineage one. Mutation M26 |
| 4 | `max_merge_repairs` frozen; human admissions activated only by validated answers | `G4C1` freezes the limit at 0 and the first rejection is admitted `HumanRequired{limit:0}`; `G4A3` with the limit 1 registers `L2`'s **second** member `HumanRequired{limit:1}` through the production integration path; nothing spends before the answer (`spend_delta=0`, `runs=0` across two blocked steps); a non-option answer is refused before any append and a second answer is refused live and on replay (`G4C2`). Mutations M09, M16, M17, M18, M33 |
| 5 | repair source objects present at every dispatch and redispatch, incl. after resume from Parked or BudgetExceeded; materialization objects accounted by R9 until scrub releases them to R27, incl. objects an interrupted materialization left behind; a repair worktree with **administrative** residue is recreated, never reused | §8's five transitions (ref and object present and equal in every one, and the dispatch naming that candidate each time); `repair_materialization_objects_released_to_git_on_scrub` for the R9 → R27 hand-over; §9.3 for the interrupted-materialization objects — and for the last clause, `IndexLock` and `CherryPickHead` each **alone** produce `reuse=Recreated`, while the two object-store elements produce `reuse=Reused`. **The matrix draws that line itself**: administrative residue is R9's, "in the owning worktree's git dir"; the unreferenced and temporary objects are R27's, "materialization objects after scrub or after an interrupted materialization (unreferenced)". The invariant's "recreated, never reused" is about the first. Mutations M14, M15, M21, M36, M37, MT1–MT5, MT7 |
| 6 | rows T-REPAIR-DISPATCH, T-ANSWER, T-RETAINED, T-RETRY (repair) recover as tabled | §5's table: real kills at all four, and the two finalization rows named as PR10's with the structural reason (§2) |

### 11.1b The reviewer remit, proposition by proposition

The remit is five propositions and an instruction. Each is answered here, and each answer is an
observation rather than a reading of the code.

| Remit proposition | Answered |
|---|---|
| *"lineage lease is the only overlap authority"* | **Narrowed to what the printed evidence can carry, and it is a claim about lineage members, not about the whole table.** The traces' `lineages=` field is produced by `lineages_str`, which prints `leases.lineages()` and nothing else, so it cannot show that no other entry exists — and it does not: at `G4A3` #18 an **ordinary** task is dispatched `lease=Predicted { … src/gamma }` while `L1@age1` is live, which is a non-lineage entry beside a lineage one. What is observed is the narrower rule the remit turns on: **a lineage member's candidate never takes a lease of its own.** Every member candidate's `lease_effect` in the traces is `WidensLineage` (`G4A1` #13, `G4A3` #25, #30) and never `ReplacesPredicted`, an ordinary candidate's is `ReplacesPredicted` with a `Candidate{…}` release (`G4B1`), and the fold refuses the cross cases — `an_inherited_lease_on_an_ordinary_task_is_refused_at_the_barrier_before_step_g` (`src/engine/topology/recover/tests.rs`). Overlap between a member and anything else is then decided against the lineage table alone: the queue's ineligibility reasons name a lineage root in every case (`InsideLineage { root }`, `BehindOlderLineage { root }`) |
| *"overrides are the only way an empty ladder runs"* | `G4C2`: the repair's frozen ladder is `tiers=[] rungs=0`, `frozen_rung_binding(0)=None`, and only after the validated answer does `rung_binding(0)` exist; the forged non-override start is refused with *"a binding the run never froze for it"* live and on replay, and so is the override's own agent and model **unpinned**. M06, M18, M35 |
| *"lineage candidates own queue positions but no candidate lease"* | one queue entry per `task_candidate_created` for a member (`G4A3` #26), consumed by its terminal (#34), while the lease table gains nothing for it. M02, MQ1 |
| *"source objects protected"* | §8: five transitions, ref and object present and equal in each, the dispatch naming that candidate each time; M21 caught; **M22 and M23 survive**, which is stated in §8, filed as O1 and named in the verdict |
| *"materialization objects accounted by the repair worktree then Git incl. the interrupted-command residue class"* | §9.3 element by element, plus `repair_materialization_objects_released_to_git_on_scrub` for the hand-over and `a_forced_recreation_preserves_the_object_store_residue_it_recovers_over` for R27 across a recreation |
| *"answer Q1-Q6"* | §12, each with its portion named and the earlier gates' portions not re-claimed |

---
### 11.2 Findings this run carries forward, re-verified against this range

G4's second run filed seven findings on the unmerged branch `gate/g4-corrected-range` (`82de0767`),
which will not reach `master` because pull request #257 is a rejected run. **Each was re-verified
against `7e0110a1` before being carried; two are dropped as closed by the range, and the reasons are
below rather than in a footnote.** The O-numbers are run 2's and are kept so the backlog thread does
not break.

| Label | Finding id | Severity | Re-checked how | Carried? |
|---|---|---|---|---|
| O1 | `G4-O1-REJECTION-KEEPS-CANDIDATES-REF-UNWITNESSED` | P3 | mutations M22 and M23 re-run here: **both survive** the full suite (`rc=0`, 2452 passed, 0 failed) | **carried** |
| O2 | `G4-O2-OVERRIDE-SINGLE-RUNG-EXHAUSTION-UNWITNESSED` | P3 | mutation M34 re-run here: caught **only** by this run's temporary `G4C3`; the fixture's `FixedIds` question-id source is unchanged in range, so a committed test still cannot raise the second question the shape needs | **carried** |
| O3 | `G4B-O3-LIVE-WORKTREE-MISSING-CLOSE-KEEPS-THE-INTENT` | P3 | re-measured by `G4G` at this sha: `closed_g0_intent_still_present=true` after the close and after generation 1 dispatched; the `Close` arm at `src/engine/topology/run.rs:1201` is byte-unchanged in range | **carried** |
| O9 | `G4B-O9-EVENT-LOG-ANSWER-READER-NEVER-PRUNES-UNWITNESSED` | P3 | mutation MR1 re-run here: caught **only** by this run's temporary `G4K` pair, and by their **existence** assertion — the byte comparison beside it is printed, not asserted (§5.1) | **carried** |
| O10 | `G4B-O10-REPAIR-MATERIALIZE-SAMPLER-MACOS-KILL-FLOOR` | P2 | **closed by the range** — see below | **dropped**, with evidence |
| O4 | `G4-O4-ROW10-VERIFY-CLAUSE-CORRECTION-OWED` | P2 | **closed by the 2026-09-09 amendment**, which is the act it asked for (§9.5 item 3) | **dropped**, with evidence |
| O11 | `G4B-O11-TEMPORARY-OBJECT-FILE-SCAN-READS-WHERE-GIT-DOES-NOT-WRITE` | P3 | **closed by #258** — verified by running, not assumed (§9.4) | **dropped**, with evidence |

**Why O10 is dropped.** Its failure sequence names one mechanism: the sampler's kill points *"aimed
as a fraction of a budget taken from the shorter of two probe picks"*, and its remedy is *"schedule
against a median of several probe picks rather than the shorter of two … Keep the floor as a count of
children that actually died by the kill."* At `81ee09ef` the sampler's own doc said *"The budget is
the shorter of two probe picks"* and the code was `budget = cold.min(warm)`. At `7e0110a1` it is
`KillBudget::probed(&picks)` over `PROBE_PICKS = 4` — a discarded warm-up and the median of three —
re-aimed by `budget.completed(clock)` inside every pick that completed before its kill, with the
floor `kills.len() >= SAMPLING_N` kept exactly as the finding asked. **The named remedy is landed,
and `test (macos-latest)` is `success` at the frozen sha** (§1). **What is not established, and is
said rather than implied: one green macOS run is one observation, not a rate**, and this session
measured nothing on macOS. If the shape recurs on a macOS leg the correct reading is the sampler
family — of which this run files a new row (O12) — and re-opening this id costs one `git mv`.

**Why O11 is dropped.** It says the scan reads `objects/tmp_obj_*` and `objects/pack/tmp_pack_*`
while git 2.43 writes `objects/XX/tmp_obj_*`. Both halves were re-measured here rather than taken
from the record: a real `SIGKILL` inside `git hash-object -w` left `.git/objects/92/tmp_obj_wTKgLq`,
which `git prune -n` names and `fsck --unreachable` does not (§9.4); and the scan in this range
iterates git's canonical `00`–`ff` fan-out paths in addition to the root and `pack`, resolving each
with `fs::metadata` so a symlinked or case-aliased fan-out is followed. The element it could not see
now classifies `Internal` **alone** (§9.3), and mutations MT1 and MT5 show each arm of the scan is
witnessed.

### 11.3 What this run adds

| Label | Finding | Severity |
|---|---|---|
| O12 | `G4R3-WM-ADD-SAMPLER-FAILS-IN-ISOLATION` — PR5's `Worktree.Add` sampler fails **alone** at 1 in 10 on Linux at this sha, and produced **two distinct filed fingerprints** in eleven runs. `PR172-SAMPLER-REFUSED-A-TORN-WORKTREE-LIST-RECORD` says it *"passes alone at the same head"* and asks for a rate; the rate is now measured and the "passes alone" reading is withdrawn | P3 |

Nothing else. **No new P1 or P2 was found in this range**, and this report does not manufacture one:
the four carried rows are missing committed witnesses and one accounting gap, none of which reaches an
invariant, and O12 is a measurement added to two already-deferred rows.

### 11.4 The standing deferrals, and how each bears on the pass

A gate may report that a deferral weakens its pass — G3 did exactly that in its §10 — but it may not
treat one as closed. None below is treated as closed, repaired or narrowed.

| Deferral | Bears on the pass how |
|---|---|
| `PR8-R2-SPEND-REPLAY` (P1, G3's range, owner-deferred) | **Bears on clause 6.** It is a P1 and it is `deferred`, so clause 6 is established on the owner's ruling and not on this reviewer's judgement. This range does not touch it and does make it more reachable: every lineage verification and publication runs through the affected replay path. §13 condition 1 |
| `PR8-CRASH-002` (P1, G3's range, owner-deferred) | **Bears on clause 6**, identically; lineage publication uses the affected ref-write path. §13 condition 1 |
| `PR249-ANSWER-TEXT-NOT-CARRIED` (P2) | Bears on clause 1 only in what this report claims for T-ANSWER: `G4H` and `G4K` establish **index-only** ingestion and recovery — the option index, the binding override, the `via` — and **not** delivery of a person's typed guidance to the agent. No claim here exceeds that |
| `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` (P2) | Does not bear on this gate's evidence: it is PR5's sampler and the Windows launcher; G4's sampler spawns the real binary through `fixture::sampled_git`. It does bear on the winguest leg of any pull request |
| `PR249-MANIFEST-NORMALIZATION-ALIAS` (P3) | No claim here rests on manifest normalization |
| `PR249-REFUSED-MANIFEST-HANDOFF` (P3) | Bears on nothing this report claims: the fresh-generation behaviour after a `Retained` close (row 8) is established from the events and the worktree, not from the worker-facing promise the row records |
| `PR80-MACOS-WORKSPACE-SAMPLER-COLD-PROBE-RECURRENCE` (P2) | Bears on the **leg**, not the range. Its test is PR5's workspace-manager sampler; this run adds the Linux isolation rate for it (O12) |
| `PR172-SAMPLER-REFUSED-A-TORN-WORKTREE-LIST-RECORD` (P3) and `SAMPLER-RECOVERY-PROVEN-IS-NOT-PROVEN-FOR-AN-EMPTY-GITDIR` (P2) | **Bear on clause 1's reading.** Both fired here, in the same test, at this sha; the cumulative suite's first attempt was red on the first and one of ten isolated runs on the second. Neither is a G4 site, and clause 1 is judged on the green attempt with the flake rate stated (§1, §13 condition 3) |
| `RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE` (P2, filed on pull request #254, **not in this tree**) | Bears on the leg. The mechanism it names is no longer in the tree (§10) and it did not fire in five runs here; it is nonetheless not this report's to close, since the file is not in this repository |

### 11.5 The state of the finding ledger at `7e0110a1`

`findings/` holds **301** files before this report's additions: **14 P1**, 105 P2, 180 P3,
1 P4 and the README. **Every P1 is `disposition: deferred`** with a named guard, and
`/usr/bin/grep -rln '^disposition: open' findings/` returns **nothing** — there is no open
finding at any severity. The P1 that run 1 found inside G4's own remit, the reused lineage age, is
**fixed in this range** by #256 and its fix is re-measured here (§4.2, MA1–MA3).

---

## 12. The six standing questions

The gate process says a gate *"may claim only resources and invariant portions introduced in its
frozen range, naming the portion and the inventory rows."* Each answer opens by naming G4's portion
and closes with **answered with evidence**, **answered in the G4 portion only**, or **not
answered**. Nothing is claimed for a portion belonging to G1, G2, G3 or a later gate.

**A note on the question text, and how it was obtained.** The two extracts this session was
pointed at are **truncated**: `~/tactus-artifacts/G4-QUESTIONS.txt` and the `standing_questions`
block of `~/tactus-artifacts/G4-DEFINITION.txt` both cut off mid-sentence from Q2 onwards (Q2 at
*"does every physical or logical owned resource have"*, Q6 at *"incl. the immutable image id every
con"*). Answering the truncated text would have silently dropped real clauses — Q2's
*"repair-source protection on resumable outcomes"*, Q5's lint-and-dependency census, Q6's Runner
identity. **The packet itself was therefore read**:
`~/tactus-artifacts/tactus-parallel-design-neutral-v16.json`, whose `cumulative_review_gates.gates[3].id`
is literally `"G4"`. Its complete `standing_questions` block is saved as
`g4r3-evidence-7e0110a1/packet-questions-full.txt` and is what the six answers below are written
against. **The truncation of the extracts is reported to the driver as a defect of the working
files, not of the packet.**

### Q1 — reconstruction from durable state alone

**G4's portion.** The states the range adds or changes: a lineage lease and its age counter, a
lineage-candidate queue position and its eligibility reason, a repair generation's class including
`RetainedIdle{session, incarnation}`, the open admission or binding question, the binding override,
`halted_at`, the epoch-scoped budget stop, and the unresolved transaction as a lineage member's
rejection leaves it. The coordinator-local holdings, the durable recovery records, the stable-prefix
barrier, the authorized private root and the Runner rebuild are G1–G3's obligations and are **not**
re-claimed here.

**How it was answered: by replaying disk bytes, not by reading the code.** The helper
`live_vs_replay` parses `events.jsonl` from disk (`TopologyFold::parse_log(&fixture.log_bytes())`),
replays it into a fresh fold, and compares `run.fold().state()` — the **live** stepped run's own fold
— against that replay with `==`. `RunState`'s `PartialEq` covers all seventeen of its fields,
including `leases: LeaseTable` (and so `next_age`), `queue`, `transaction`, `questions`,
`seen_questions`, `deferred_tasks`, `overrides`, `halted_at`, `halted_epoch`, `budget_stop`,
`finished`, and `tasks: Vec<TaskFold>` — which carries each generation's `GenerationClass`, so
`Promoting` and `RetainedIdle { session, incarnation }` are inside the comparison. A weaker
`replay_twice_equal` is printed beside it and is labelled as the weaker claim.

**Result, and it is stated in two halves because the first head of this report conflated them.**

**(a) Final-state comparisons: 19 across the 17 measurements, all `true`; 17 replay-twice
comparisons, all `true`; zero `false`** (`measurements/meas-run2.log`). These compare the state each
measurement *ends* in. The states they carry are: a surviving lineage with its age on both sides
(`G4A3`: `L2@age2`), a recorded override on both sides (`G4C2`: `live_overrides=1
replay_overrides=1`), an open question on both sides (`G4C3`, `G4H`, `G4A3`), `halted_at=Some(2)` on
both sides (`G4B3`), the epoch on both sides in every line, and a torn event-log tail truncated on
open and re-ingested (`G4K-torn`: `live_epoch=Some(2) replay_epoch=Some(2)`).

**The first head claimed more than that, and the verification of `dc087837` was right to reject
it.** It said the comparisons span *"two live lineages with distinct ages"* and *"a queue entry with
its `BehindOlderLineage`/`InsideLineage` reason on both sides"*. **No final-state line shows either**
— by the time each measurement ends, the lineages are released and the queue is empty. That claim is
withdrawn as a claim about the nineteen.

**(b) Intermediate-state comparisons, added this round and executed: the states are now compared.**
The drive helper takes the same live-against-durable comparison **after every step** (§4.5), and
these lines carry exactly the states the claim named:

| State Q1 names | Line that compares it |
|---|---|
| two live lineages with distinct ages | `G4A1 @step1` (`L0@age0` + `L1@age1`), `G4A3 @step1` (`L1@age1` + `L2@age2`, after a release) |
| a queue entry held `BehindOlderLineage` | `G4A1 @step1`, `G4A3 @step1` — the reason is on **both** sides |
| a queue entry held `InsideLineage` | `G4B1 @step1` — on both sides |
| a generation `RetainedIdle` with its session and incarnation | `G4G @step1` — `RetainedIdle { session: SessionId("claude-code-session"), incarnation: Epoch(1) }` on both sides |
| an **active** budget stop | `G4P-budget @step1` and its final line — `live_budget_stop=Some(1)` beside `live_epoch=Some(1)` |

All of these read `live_equals_replay=true`.

**What is still not claimed.** Not every intermediate state of every measurement is compared: the
probe runs after each step of `G4A1`, `G4A3`, `G4B1` and `G4P-budget`, and at two points inside
`G4G`. A transient mismatch inside a step, or inside a measurement with no probe, would still not be
caught here.

**Q1 verdict: answered with evidence for the G4 portion**, by comparison of the live fold against a
replay of the bytes on disk — at each measurement's final state, and, for the five states tabled
above, while that state is live.

### Q2 — resource accounting

**G4's portion, by row.** The **eight** rows of the definition's `state_resource_ownership_matrix`
and no others. (Run 2's first version claimed a ninth, R24, which is G3's; it is not claimed here
either, and appears below only as context.)

| Row | Resource | Class(es) across its lifecycle | Domain | Observed at `7e0110a1` |
|---|---|---|---|---|
| **R8** | lineage lease | `resumably_open` while active or parked; `released` at `task_merged` whose `satisfies` names the root, or at terminal failure | logical_fold_broker | released exactly once per lineage in every trace (`G4A1` `merged=[…Lineage{0}…, …Lineage{1}…]`, `G4A3` the same); a forged second release refused live and on replay (`G4B3`, `G4B4`); never touched by a `generation_closed` (`G4G`: `lineage_held=true` after the close) |
| **R6** | lineage-candidate queue position | `resumably_open` while queued or skipped; `consumed` at `task_merged` or `merge_rejected` | logical_fold_broker | one entry per candidate, pushed at `task_candidate_created`, removed by its terminal; held-but-ineligible while behind an older lineage, including one created after a release (`G4A3` #26 → #34, consumed by the member's own rejection) |
| **R9** | repair generation worktree + intent, incl. the materialization objects its index references and any administrative residue | `resumably_open` while `OpenNoAttempt` (reused only after `Worktree.Verify`, else recreated with force) or `RetainedIdle`; `pruned` at close, with the intent | external_physical | verified or recreated with force per element (§9.3); the closed generation's worktree and intent reclaimed by the resume's sweep. **One gap, observed and reported as one:** a `WorktreeMissing` close made by the *live* retry path leaves the closed generation's intent in place until the next resume (`G4G`: `closed_g0_intent_still_present=true`), so between that close and the next resume the intent's class is not yet the row's `pruned` — §11 O3 |
| **R27** | materialization objects once unreferenced, or written by an interrupted pick | `released` to Git; never deleted, never adopted | external_physical | the planted unreferenced object and the planted temporary file both still present after **every** recovery, reuse and forced recreation alike (§9.3, and `a_forced_recreation_preserves_the_object_store_residue_it_recovers_over`); `repair_materialization_objects_released_to_git_on_scrub` measures the R9 → R27 hand-over with `fsck` on both sides |
| **R11** | source candidate ref | `resumably_open`, protected while the run can resume | external_physical | §8: the ref resolves to the same commit id, and the object still exists, across a Parked resume, a BudgetExceeded resume, a real conflict rejection, a real code rejection and the repair's own merge; in `expected_refs`; no production `DeleteCandidatesRef` caller at this sha. **Witness gap named** (§11 O1) |
| **R15** | admission / binding question | `resumably_open` while open; `consumed` at `question_answered` | logical_fold_broker | one per parked generation, keyed to the repair (`G4C1`: `questions=Some([("q-park-fixed", 2, Admission, None)])`); removed by the answer's delta; a decline removes every question of the lineage (`G4B2`: `questions=0`); a second question with a fresh id accepted (`G4C3`: `g4r3-q-1`, then `g4r3-q-2`) |
| **R16** | binding override | `persistent_output` fold state, set once | logical_fold_broker | `G4C2`: recorded at the answer, read by every later attempt (`rung_binding(0)`), the frozen half `None`; still present after the merge and across `G4C3`'s three generations (`override=true`) |
| **R14** | repair budget unit | `consumed` once per automatic rejection | logical_fold_broker | `G4C1` with `max_merge_repairs=0` registers the first repair `HumanRequired{limit:0}`; `G4A3` with the limit 1 registers `L2`'s **second** member `HumanRequired{limit:1}` through the production integration path |

*Context, not claimed:* R24 (snapshots for repair attempts) is G3's and unchanged in range; a lineage
candidate's verification runs in the same snapshot slots (`G4B1` #17: a `StaleClean` verification with
a pin), which is evidence that G3's rule still holds for lineage candidates, not a claim of ownership.

**No overlap, one row each**, with the single exception recorded above. The one object two rows could
contend for is the merge blob a materialization writes: **R9** while the repair index references it,
**R27** once the worktree is scrubbed, and the scrub test measures the hand-over at both instants.

**Q2 names this range's hardest case in its own words** — *"engine-created Git objects (accounted by
the worktree, pin, or ref that references them and by R27 once unreferenced, incl. objects left by an
interrupted Git command), Git administrative residue (with its worktree)"* — and that is exactly the
partition §9.3 observes element by element: the objects an interrupted materialization left are R27
and survive both recoveries, the administrative residue is the worktree's and forces its recreation
with force, and after the reproduced pick the index references the merge objects again
(`class_after=After`), which is the R27 → R9 direction of the same hand-over.

**What Q2 asks that G4 cannot close.** The outcome equations over all 28 rows, the closure procedure
and terminal finalization at Complete/Halted are PR10's by the packet's own sequence — this build has
no `run_finished` emitter and refuses `Step::Closure` (§2) — exactly as G3 recorded. *Repair-source
protection on resumable outcomes* is the clause G4 owns and §8 answers it.
**Q2 verdict: answered with evidence for the eight rows G4 owns, with one accounting gap named (R9,
the live close); the total-run-outcome equation is PR10's and is not claimed.**

### Q3 — stale, duplicate and out-of-order completions

**G4's portion.** A stale or foreign-incarnation retry; a duplicate rejection; a second answer; a
close or settlement naming a generation or attempt that is not the open one; a duplicate ingestion
after a kill; and whether the append-error protocol reaches the sites the range adds.

- **A retry from another incarnation cannot continue a retained generation.** Refused at the settle
  layer before the worktree is verified (`retry_refused_after_resume`, mutation M31) and at the fold
  (`StaleIncarnation`, M32); and `retry_refused_with_stale_incarnation`, opened here, does two things rather than one: it
  asserts the settlement wired `retained_incarnation` **from the epoch** rather than from anything
  else, and then that the fold **refuses a retry** whose `resume_session` names a dead incarnation.
  It is a refusal of the retry, not of the settlement.
- **A duplicate `merge_rejected` cannot register a second repair.** Observed refused twice in `G4C1`,
  at its own sequence and the next, live and on replay with byte-equal errors, and the registry
  unchanged in **size and member count** (`registry_len_after_refusals=3 lineage_members=Some(1)`;
  the measurement reads two lengths, not the registry's contents). The refusal rests on five
  independent guards; each is removed alone and together in §3 (MD1–MD3, MD6, M16, MD7).
- **A second answer cannot mutate the task again, and a kill cannot make one answer two.** The
  question is removed by the first answer's delta and a repeat is refused live and on replay
  (`G4C2`, `G4H`); a coordinator killed **inside** and **just after** the ingestion's append leaves,
  after the next resume, exactly one `question_answered` (`G4K-torn`, `G4K-complete`:
  `question_answered_events=1` in both).
- **A close or settlement for the wrong generation or attempt is refused.** Stated in two parts,
  because the first head of this report ran them together and the verification of `dc087837` was
  right to separate them. **Identity:** four events with the **valid** `LineageHeld` disposition and
  a wrong identity — an attempt that is not the running one, a generation that is not the open one
  (twice, for `attempt_interrupted` and for `generation_closed`), and a task with no open generation
  — are each refused live and on replay with byte-equal errors that name the identity and no
  disposition (§5.2). Production checks identity first, in `in_flight()` and `open_generation()`,
  before the disposition (`src/topology/fold/check_attempt.rs`). **Disposition:** separately, `G4G`
  forges the same two events with either *predicted* disposition on a real in-flight and then
  retained repair generation: both refused live and on replay, `LineageHeld` accepted (mutations
  MDISP1, MDISP2).
- **A returned append error cannot let a stale in-memory fold drive anything.** The protocol is
  `src/engine/topology/emit.rs`'s and is **unchanged in this range** (it is not in the range's diff);
  the range's new sites append through it. PR9's own witness for the recovery-time case is
  `a_retained_generations_reclaim_the_closing_recovery_never_reached_is_finished_by_the_next`. This
  half is **read, not re-executed here**, and is said to be.

**Q3 verdict: answered with evidence for the G4 portion.** Slot release, cancel and provisional
conversion under real concurrency are PR11's.

### Q4 — aliasing

**G4's portion.** R8, R6, R9, R11, R15, R16 and R14, as above.

| Aliasing hazard | Why it cannot happen | Witness at `7e0110a1` |
|---|---|---|
| two lineages for one root, or two lineages sharing an age | `LeaseTable::grant` replaces an existing lineage's region rather than adding a second, and `next_age` is a per-table counter that rises at each creation and **never falls** | `G4A3` `ages=[(1, 1), (2, 2)]` after age 0 was released; `an_age_once_granted_is_never_reused_and_a_lineage_created_after_a_release_waits_behind_every_survivor`; mutations MA1, MA2, MA3 |
| a repair holding a lease of its own beside the lineage's | `check_dispatched` refuses `Predicted` on a member and `InheritedLineage` on an ordinary task; `WidensLineage` grants no candidate lease | **the member's own `lease_effect`**, which is `WidensLineage` in every member candidate in the traces and never `ReplacesPredicted` (`G4A1` #13, `G4A3` #25, #30), and the committed `an_inherited_lease_on_an_ordinary_task_is_refused_at_the_barrier_before_step_g`. **Not** the printed `lineages=` field: `lineages_str` prints `leases.lineages()` only, so it cannot show what else the table holds, and at `G4A3` #18 an ordinary task does hold a `Predicted` lease beside a live lineage (§11.1b) |
| two queue positions for one candidate, or a lineage candidate overtaking | one push per `task_candidate_created`; the first-eligible rule with its lineage reasons | M01, M02, MQ1; `G4A1` and `G4A3`'s forged starts refused |
| two repair generations of one task open at once | `check_dispatched` refuses while the task has an open generation and requires the dense next one | `G4G` (`g1` opens only after `g0`'s close), `G4C3` (`g0:Closed`, `g1:Closed`, `g2:Closed` in order) |
| two overrides for one task | `overrides` is keyed by task and the second answer is refused | `G4C2`; M06, M08, M09 |
| a reused question id | `check_new_question` refuses an id the log has used | `G4C3` raises `g4r3-q-1` and `g4r3-q-2`, never a repeat; the fixture's fixed-id source is exactly what made the committed witness impossible (§11 O2) |
| a repair worktree shared by two generations | the slot is `Slot::Task{key, generation}`, so generation 1's path differs from generation 0's | `G4G`: `g1_worktree_exists=true` beside the removed `g0` |
| materialization objects accounted twice | while the repair worktree exists its index references the merge objects (`class_after=After`); once scrubbed they are unreachable and Git's (R27); no ref or pin ever names them | §9.3; `repair_materialization_objects_released_to_git_on_scrub` |
| a duplicate `merge_rejected` registering a second repair or lineage | refused five ways (§3, §7) | `G4C1`; `a_rejection_already_folded_is_refused_when_it_arrives_again` |

**Q4 verdict: answered with evidence for the G4 portion.** Fast publication naming the gated object,
private halves, container names and credential turns are earlier gates' and are not re-claimed.

### Q5 — effect and append boundaries

**G4's portion.** The Object site `Object.RepairMaterialize` with its command-internal residue class;
the Answer sites as reached by T-ANSWER's ingestion into a lineage; and the Event appends the range
adds. The Topology/Shared site inventory as a whole, the platform containment sub-effects, the
run-directory publication and owner/commit-record prefixes, the husk-removal sites and the
commit-tree `IdUnread` points are G1–G3's and are **not** re-claimed.

- **`Object.RepairMaterialize`'s residue class is the one thing Q5 asks that this range owns, and it
  is answered by construction.** The frozen register (`effects/residue-classes.json`) names four
  elements and `sampling_n: 8`. **All four are constructed alone, classified, and recovered by the
  tabled action, with the observed values in §9.3**; the sampling record is non-zero, classifies
  every sample and accounts for exactly the samples it took (§10 row 10: five runs, 8 spawns and 8
  kills each, 0 classifier refusals). This is the clause verification 3 found unmet, and it is now
  met by execution rather than by argument.
- **`Answer.Ingest` executed by the production reader**, observed in `G4H` (`via=event-log`) and in
  both `G4K` kill shapes, with the published file byte-unchanged afterwards
  (`published_unchanged=true`) — the read-not-take property R21 states.
- **`Event.Append`'s torn and complete points**, executed by real coordinator kills: `G4K-torn`
  leaves `last_byte_is_newline=false` with `bytes_delta=535`, and `G4K-complete` leaves
  `last_byte_is_newline=true` with `bytes_delta=631`; the next incarnation truncates the torn tail on
  open and both converge to one `question_answered`.
- **Q5's closing clause — the lint and dependency censuses "for the code and Cargo.lock in
  range".** These are the `src/effects/tests.rs` censuses, 61 tests, and they run in the cumulative
  suite, which is green at `7e0110a1` (§1): the clippy disallowed list
  (`the_denylist_names_every_primitive_the_packet_enumerates`,
  `every_denied_path_this_host_can_resolve_does_resolve`), the allow-placement scan
  (`every_allow_of_a_governed_lint_is_module_level_and_in_the_allowlist`,
  `the_placement_scan_refuses_an_allow_that_is_not_module_level_and_sees_through_no_disguise`,
  `the_three_blunt_governed_lints_are_used_by_nobody`), the wrapper classification (`wrappers`), the
  funnel-module allowlist (`every_allowlist_entry_carries_its_justification_and_names_a_real_file`),
  the frozen legacy allowlist (`the_legacy_section_is_frozen_and_may_only_shrink`,
  `the_legacy_section_never_contains_a_topology_module`) and the dependency review
  (`extern_dependencies`). **Cargo.toml and Cargo.lock are byte-unchanged since the PR9 merge**
  (`git diff --stat 74da2cbb HEAD -- Cargo.toml Cargo.lock` is empty), so no dependency entered this
  range. `cargo clippy --all-targets --all-features -- -D warnings` and
  `cargo +1.85.0 check --locked` both pass (§1). **Stated because it matters:** two of those
  censuses — the allow-placement pair — were **skipped in the mutation runs only** (§3), because
  this run's own temporary measurement module carries a module-level `allow` that is not in
  `effects/allowlist.toml`. They ran, and passed, in the clean tree that §1 measures.
- **What is not claimed.** The bijection over the whole generated inventory, and the parent-executed
  sub-effect points of sites outside this range, are not re-established here. The two Answer *writer*
  sites are exercised by the parent's staging and publication through `NoHooks`, so no hook-phase
  claim is made for them.

**Q5 verdict: answered with evidence for the G4 portion** — the residue class by per-element
construction and by sampling, the ingestion and append points by real kills; the inventory-wide
bijection is not claimed.

### Q6 — sequential guarantees

**G4's portion.** FIFO integration and no overtaking *as they apply to lineage candidates*, including
a lineage created after another was released; atomic settlements for the lineage lifecycle; the
publication order and lease release under two different arrival orders — **not** canonical-trace
equivalence, which this gate does not claim (§4.1); the derived run outcome over the states a
lineage can leave a run in; and no answer ingestion after a budget stop or a halting settlement in the
same epoch. Exact-commit verification, exact-base fast publication, one writer,
`budget_exceeded` before any budget-driven end, and **one Runner identity per run including the
immutable image id every container is created from, recorded before the first probe and honoured by
every resume** are G1–G3's and are not re-claimed here; nothing in this range creates a container or
a Runner.

| Guarantee | Evidence in range |
|---|---|
| **FIFO integration, no overtaking — lineage candidates** | a lineage candidate owns a queue position and **no** candidate lease; eligibility is decided against the lease table (`InsideLineage` for an ordinary candidate overlapping any lineage, `BehindOlderLineage` for a member overlapping an older one) and any start that is not the first eligible entry is refused, with the reason named, live and on replay (`G4A1`, `G4A3`). After a release the new lineage's age is above every survivor's and its member waits (`G4A3` `ages=[(1,1),(2,2)]`). M01, M02, MA1, MA2, MA3, MQ1 |
| **atomic settlements** | `merge_rejected` carries the frozen repair, the lineage creation or widening and the parent's `AwaitingRepair` in one append (§7); a decline fails every unmerged member, releases every holding and closes the lineage's questions in one delta (`G4B2`); `generation_closed{LineageHeld}` closes without touching the lineage lease (`G4G`) |
| **identical canonical authoritative trace where scheduling must not matter** — **answered in part, and the part is named** | What was compared is a **reduced projection**, not the trace: `merged_list` keeps three fields of each `task_merged` — `sequence`, `satisfies`, `lease_release` — and those three are equal, in order, across `G4A1` and `G4A2`: `[(2,[0,2],Lineage{0}), (3,[1,3],Lineage{1})]`. Both runs also end `Ending(Complete)` with an empty lease table and queue and all tasks `Merged`, and each run's final state equals a replay of its own bytes over the whole `RunState`. **The traces themselves are not equal and are not claimed to be**: the younger lineage's candidate publishes `disp=StaleClean` with a pin and `verification=Passed` in `G4A1` and `disp=Fast` with no pin in `G4A2`, with different queued regions; and the two fixtures pre-plant *different* repair candidates, so they are two starting states rather than one run re-ordered (§4.1). **Established: publication order and lease release do not depend on which lineage's candidate arrives first. Not established: canonical-trace equivalence** — that needs a canonical trace to compare and a pair of runs differing only in schedule, and this gate has neither. Schedule-independence under **real** concurrency is PR11's and is not claimed either |
| **derived run outcome: halt > budget > parked > complete** | over the states in range: a halting decline derives `Ending(Halted)` (`G4B3`), a budget stop `Ending(BudgetExceeded)` and the resume clears it (`G4E`), an open admission question `Ending(Parked)` (`G4C1`, `G4A3`, `G4C3`), a declined lineage with a blocked dependent `Ending(Complete)` (`G4B4`). The ordering itself and `run_finished` are G1's and PR10's |
| **no answer ingestion after a budget stop or halting settlement in the same epoch** | `G4H` epoch 0 refuses the answer — *"question_answered arrived after the budget stop in this epoch; the run has stopped admitting work and an answer ingested now would restart a run that already ended"* — and the same published file is ingested at the next incarnation's first step (`G4H`: `published_exists_after=true`, `answers=["q=q-park-fixed … via=event-log"]`). M10 |
| **untouched user checkout; exact-commit verification on immutable snapshots** | **G1–G3's, unchanged in range and not re-established here.** What this gate observed is narrower: every repair worktree it created was addressed by its own `Slot::Task{key, generation}` path, and a lineage candidate is verified and published through the same `integrate` path G3 certified (`G4B1` #17: a `StaleClean` verification with a pin). No census of effect paths was run here, so "every repair effect is confined to its slot" is **not** a claim this report makes |

**Q6 verdict: answered with evidence for the G4 portion**, including the after-release case run 1 did
not cover.

---

---

## 13. Verdict against the pass rule, clause by clause

The rule, verbatim from the definition:

> pass iff every G4 test green, no lease double release, ordering proven, source protection proven,
> questions answered, no open critical/high finding

Six clauses. Each is judged **established**, **asserted** or **silent** — the verification lens's own
vocabulary — against *this run's* evidence, produced at `7e0110a1`. "Established" means an execution
this report names produced the values it quotes, and, where the claim is that something is
*detected*, a mutation this report names was observed to kill a test that owns the claim.

| # | Clause | Verdict | How, and where |
|---|---|---|---|
| 1 | **every G4 test green** | **established** | The cumulative suite is green on the exact range: 2443 lib + 10 binary passed, 0 failed, 44 ignored, Linux, `7e0110a1` (§1), and CI is green on ubuntu, macOS and the Windows guest at the same sha. All **thirteen** adversarial rows are met, each with executed evidence and each with a mutation that kills a test owning its claim (§10). **Row 12's T-RETRY site is the one two verifications rejected, and it is fixed by execution rather than by wording, twice.** `dc087837` cited a kill of the next transaction; `0fac505d` reached the boundary but bypassed the selector and so checked no ceiling, and **that claim is withdrawn rather than reworded** (§5.4). This head establishes the row with an A/B that executes the ceiling gate on the retry branch (§5.4a) and a real subprocess killed on the retry path **through `TopologyRun::step`**, whose truncated prefix is the row's durable state and whose recovery runs through **recovery step (e)** before `run_resumed` (§5.4b). §5.4c states what neither witness shows and why the harness admits no single construction that shows both. **Row 10 is met on the packet's current text** — every element constructed alone classifies `Internal`, git-dir elements force a recreation, object-store elements are reused and survive, one pick reproduces the tree and checkout, `class_after=After` in all four (§9.3) — and the correction that produced that text is stated as the authorised relaxation it is (§9.1, §9.2). **Two qualifications, neither of which this report treats as excusing the clause.** (a) The suite's first attempt was red on one test, PR5's `Worktree.Add` sampler, which fails alone at 1 in 10 here; it is not a G4 site, both its fingerprints are filed and deferred, and the rate is newly filed (§1, §11). A suite whose green is a sample rather than a certainty makes this clause weaker than a deterministic green would, and that is said rather than smoothed over. (b) Two mutations survive the full suite (M22, M23) and two are caught only by this run's temporary measurements (M34, MR1); each is a **missing committed witness**, not a broken guarantee, and each is filed (§11) |
| 2 | **no lease double release** | **established** | A lineage lease is released exactly once, at the publication whose `satisfies` names its root or at the lineage's terminal failure. Committed: `a_lineage_lease_is_released_exactly_once_and_a_second_release_is_refused`. Executed here: every trace in §4 shows one release per lineage; a **forged second release** is refused live and on replay with byte-equal errors in `G4B3` and `G4B4`; a `generation_closed{LineageHeld}` leaves the table untouched (`G4G`: `lineage_held=true` after the close). Detection: M03 (never released), M04 (holdings not released on lineage failure), M25 (any release accepted) |
| 3 | **ordering proven** | **established, including the case run 1 found broken** | Two overlapping lineages resolve in creation order under two different arrival orders, with the same publication order and the same lease releases — over the three fields `merged_list` retains, and **not** as a trace equivalence (§4.1) — and no deadlock (`G4A1`, `G4A2`); a lineage created **after** a release takes an age above every survivor's, its member waits `BehindOlderLineage`, and the loop dispatches and publishes the survivor first (`G4A3`, at the production loop; the committed fold tests through the fold's doors); overtaking is refused live and on replay with byte-equal errors in both shapes. Detection: MA1 (the original defect restored), MA2 and MA3 (the counter's two other failure shapes), M01 (order inverted), MQ1 (FIFO inverted) |
| 4 | **source protection proven** | **established for this tree, with the witness gap named** | The rejected candidate's ref resolves to the **same commit id** before and after each of five transitions, and its object still exists at each — a real conflict rejection, a real code rejection, a Parked resume, a BudgetExceeded resume and the repair's own merge — and the dispatch names that candidate in each (§8); the ref is in `expected_refs`; a missing or substituted source is refused before any append (M21 caught). **What is not proven** is that a future rejection arm deleting the ref would be caught: M22 and M23 survive the full suite. The protection holds at this sha by measurement and by the structural absence of a production `DeleteCandidatesRef` caller, not by a committed regression witness (§11 O1) |
| 5 | **questions answered** | **established, each with its scope named** | **The two the verification of `dc087837` found overstated are fixed by producing the evidence.** Q1's claim to compare two live lineages, a blocked-queue reason and an active budget stop was true of no final-state line; the comparison is now taken **at those intermediate states** and each is tabled with the line that shows it (§4.5, §12 Q1), the withdrawn half is said to be withdrawn, and what is still not compared is named. Q3's *"wrong generation or attempt is refused"* rested on refusals that varied only the **disposition**; four refusals that vary the **identity** with a valid disposition were constructed and run, and their errors name the identity (§5.2, §12 Q3). Q2 covers the **eight** rows the matrix gives G4, with one accounting gap named (R9's intent after a live close) and the total-run-outcome equation named as PR10's; Q4, Q5 and Q6 have executed evidence for G4's portion with the earlier gates' portions explicitly not re-claimed (§12). The packet itself was read for the question text, because the working extracts truncate Q2–Q6 |
| 6 | **no open critical/high finding** | **established on the ledger's own terms** | 301 finding files; **14 P1s, every one `disposition: deferred`** with a named guard, and **no finding anywhere in the ledger with `disposition: open`**. The P1 that run 1 found in G4's own remit — the reused lineage age — is **fixed in this range** (#256) and its fix is re-measured here (§4.2, MA1–MA3). Everything this run adds is P2 or P3 (§11) |

### The verdict

**G4 PASSES.** Six of six pass-rule clauses are established on evidence produced at
`7e0110a1`. **PR10 and the later slices are unblocked by G4.**

The pass is not unqualified, and these are part of the verdict rather than footnotes to it:

1. **It rests on the owner's two inherited P1 deferrals.** `PR8-R2-SPEND-REPLAY` and
   `PR8-CRASH-002` are P1s in G3's range. This range does not touch either and does make both more
   reachable: more lineage verifications and more publications run through the affected replay and
   ref-write paths. Both are `deferred` by owner ruling, not by this reviewer's judgement, and
   clause 6 is established on that basis. **If the owner withdraws either deferral this verdict does
   not survive it**, exactly as G3's did not.
2. **Clause 4 is established by measurement and by a structural absence, not by a committed
   witness.** PR10 is the change that will add the first production caller of
   `Ref.DeleteCandidatesRef`. Until O1 is closed, the only mechanical guard on R11 is that no such
   caller exists. **A successor should close O1 before PR10 lands**, not after.
3. **Clause 1's green is a sample.** One of the two full-suite runs at this sha was red, on a
   deferred non-G4 sampler that also fails alone at 1 in 10 here. Nothing in the range caused it and
   no G4 claim depends on it, but a gate that reports "green" on a suite with a measured flake rate
   should say which run it means, and this one does: attempt 2, 10:00:20Z, logs preserved for both.
4. **Row 10 passes on a corrected row, and the correction was a relaxation.** The row was amended on
   2026-09-09 by the driver under the owner's standing authorisation, removing the obligation on
   standalone object-store residue to fail `Worktree.Verify` and force a recreation (§9.1, §9.2).
   Every other clause of the row was kept, and the two the amendment made explicit — `Internal` for
   every element, element by element — are the ones this run had to discharge and did (§9.3). Run 2's
   "engages no clause of the pass rule" is **withdrawn** (§9.5, item 2); this report judges row 10
   inside clause 1 and nowhere else.
5. **Three heads, and each of the first two carried a claim its witness had not run.** `dc087837`
   claimed a T-RETRY kill it had not made and a Q1 coverage its comparisons did not show;
   `0fac505d` fixed the first by constructing a boundary kill, and then claimed that child checked a
   ceiling it never reached. **No sentence was rewritten to fit the evidence in either round.** The
   kill was constructed (§5.4c), the comparisons were taken at the states named (§4.5), Q3's identity
   refusals were constructed and run (§5.2), the ceiling claim was **withdrawn** and replaced by an
   A/B that executes the gate (§5.4a) and a kill that goes through the selector (§5.4b), and one
   quoted SHA from a superseded draft run was corrected after checking every quoted figure against
   its log (§0). Where the evidence stops, this head says so in the same sentence: not every
   intermediate state is compared; no single construction both dies before the append and passes the
   ceiling, and §5.4c gives the harness reason; and `G4RETRY` observes the boundary without guarding
   the recovery step four committed tests guard. **That this took three rounds is itself a result
   about the report, and it is recorded here rather than smoothed away.**
6. **Everything here is Linux.** Every mutation, every measurement and every sampler run in this
   report was made on the build box. The unmutated tree is green on all three platforms at the frozen
   sha (§1), and the mechanisms the mutations probe — the fold's refusals, the queue's eligibility
   rule, the lease table's counter — are platform-independent by construction; the residue
   classifier's object-store reads are **not**, and `git prune`'s own set, the fan-out location and
   the index-lock ordering were measured here on git 2.43.0 only.

### What a successor should do first

- **O1 first**, and before PR10: one assertion in each rejection arm of the shape table — read the
  rejected candidate's candidates ref, expect the candidate — turns R11's protection from a
  structural accident of this build into a guarded invariant, and kills M22 and M23.
- **O2 is one committed test**: a counting question-id source for the driven recover fixture, then
  the `G4C3` shape. It kills M34 and lifts the fixture's one-question limit for everyone after.
- **O3 is one call** in the live retry path's `Close` arm (`src/engine/topology/run.rs:1201`), or one
  sentence in R9 and the run notes saying the live path defers reclaim to recovery, plus the intents
  read `G4G` makes.
- **O9 is one assertion**: read the published answer file after the loop's ingestion and assert it
  **byte-identical** — not merely present, which is all this run's temporary measurements assert
  (§5.1). It kills MR1 and closes the rewrite case the existence check cannot see.
- **Commit the measurement module's `drive_keep` and `live_vs_replay`.** Every trace, refusal pair
  and live-against-durable comparison in this report came from one helper that returns the stepped
  run beside the driven result. A committed version would make the next gate's Q1 answer a one-line
  call, and would have made run 1's Q1 gap impossible.
- **Take the sampler family up as a family.** Nine findings under `findings/` name a sampler
  and this run adds a tenth. The evidence they produce is real and the tests that produce it are the
  least reliable in the suite.
