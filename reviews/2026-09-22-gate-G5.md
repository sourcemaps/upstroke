# Gate 5 — complete crash/resume across the max_parallel = 1 topology (fourth run)

**Frozen input range: `9bb177ea59e1f97b8f3b3282c19519a31e1253a4`** — merged PR10 on top of G4, with the fix pull
requests §6 judges. Confirmed on entry with `git rev-parse HEAD` in `/srv/worktrees/g5-run4`, with
`git status --porcelain` empty. **Which cited logs carry that provenance in their header, stated exactly** — repair round 7
re-read every cited log for it at this head, and `repair7/provenance-check.txt` is the reading (rounds 4, 5 and 6's,
`repair4/provenance-check.txt`, `repair5/provenance-check.txt` and `repair6/provenance-check.txt`, are kept). **The range's full sha is carried by** the five suite logs (each with a clean-status
record), the nine group logs, the two ST-07 merge-check logs, the two probe run logs under `probes/`, the sampler loop logs
of both hosts, the guest export log, the three Q5-route clippy logs, the third host's four logs and restore log under
`repair5/probes/` (its driver summary carries none), repair round 2's four scratch-clone receipts (`repair2/probes/`, the
released-lock control among them with a clean-status record), the mutation campaign's warm-up log (`mutations/warmup.log`,
with a clean-status record), the clause-2 probe's build log (`audit/probe/build.log`), CI's own log at the range
(`ci/ci-35730965035.log`), repair round 5's carve-out derivation (`repair5/carveout.log`, which names the tree it read), and the sixteen receipts of the
merge and of the range checkout under the repair rounds' directories, each with a clean-status record: repair round 5's three
integration receipts (`repair5/integration/merge.log`, `repair5/integration/range-tree.log`,
`repair5/integration/post-commit-identity-5744b0a.log`), repair round 6's eight — its entry check of the checkout
(`repair6/range-tree-check.log`), the reproduction of the self-check failure (`repair6/selfcheck-reproduction.log`), its two
regenerations against it (`repair6/regeneration-first-head-06a2b8a.log`, `repair6/regeneration.log`) and its post-commit
identity and regeneration receipts at its two heads (`repair6/post-commit-identity-06a2b8a.log`,
`repair6/post-commit-identity-00a16a6.log`, `repair6/post-commit-regeneration-06a2b8a.log`,
`repair6/post-commit-regeneration-00a16a6.log`) — and repair round 7's five, its entry check (`repair7/range-tree-check.log`), its
regeneration for its first commit (`repair7/regeneration-first-head-50c0410.log`; its header line says "round 6" by a copy
error in the script that wrote it, corrected since), its post-commit identity and regeneration receipts at its first head
(`repair7/post-commit-identity-50c0410.log`, `repair7/post-commit-regeneration-50c0410.log` — the latter recording the
assembled report differing from that head's blob, §15.2) and its regeneration for this commit (`repair7/regeneration.log`);
round 7's post-commit receipts at this head are written after this commit. Repair round 6's grouping named two of those
receipts and put the rest among the files carrying no sha; this round's reading lists each.
**Its seven-character prefix, not the full sha,** is carried by the four `range/` logs, repair round 1's witness log
(`repair1/witnesses-in-worktree.log`, with a clean-status record) and `manifest-check.log`. **Not carried, and where the
provenance is instead:** the clause-2 probe's two summary logs, `audit/probe/run-each.log` and `audit/probe/analyze.log`
(**F17**, **F18**), carry no sha — they are the outputs of `audit/probe/run-each.py` and `audit/probe/analyze.py` over the
instrumented scratch clone, and that clone's head is recorded once, in the probe's build log (its `tree HEAD` line, with the
instrumentation's own `git status` under it); the two Q5-route effects-suite logs carry none, and the clippy log written
beside each names the head; the mutation recipe logs carry neither, and their provenance is the backup saved beside each,
checked against the range's blob (§5), the warm-up log's header for the clone they ran in and the six chain logs' clean-status
records; repair round 1's scratch-clone probe logs carry neither, and their provenance is each log's closing restore line,
proved against the clone's blob, and `repair1/README.md`; and the repair rounds' audits, checks and sweeps (`repair2/`,
`repair4/`, `repair5/`, `repair6/` and `repair7/`, but for the sixteen receipts of the merge and the range checkout named
above), the checker's logs under `audit/`,
`groups/filters-check.log`, `residue/generate.log` and `suite/no-red-check.log` are readings of saved files and carry no sha
of their own. **The gate and control logs record the
head they ran at and its clean status (§15.2), and all but one of those heads are heads of this pull request, not the
range:** the fifteen `gates/w1-eight-iso-*.log` runner logs (round 6's final head's and round 7's first head's among them, **F72**, **F75**) and the two head controls (`gates/pr281-control/head.log` at
`e44385b`, `gates/pr281-control-final/head.log` at `26d4bc0`) name a head whose `src/` is the range's — every head of this
pull request up to and including repair round 5's pre-merge head `4611b204` has it; the heads from the merge of the reviewed
master on carry master's `src/` (§15.2, **F66**), and the runner log at this report's own head is written after the commit;
the range control `gates/pr281-control/range.log` names the range itself, `9bb177ea`, with a clean tree and five captured
`cargo exit=0`, and **F53** reads that head from its header — repair round 4's sentence here put every control outside the
range, which was wrong of that one, and the record lens caught it; the attempt-line files (`gates/head-attempt*.log`,
`gates/final-head-attempt.log`) and the gate-ten logs name their head in their attempt lines, and the per-leg logs under
`gates/eight-logs-<sha7>-*/` are named by theirs, with their `Compiling` lines naming the worktree. Run 4 and repair round 1
said every cited log carried both, and the record lens caught it; repair round 2 then wrote that the probe logs carried
the sha, which the two summary logs do not, and the record lens caught that too.

**Verdict: G5 does not pass on this range.** Clause 2 is established; clause 8 is asserted. §16 is the
clause-by-clause verdict and it is stated last.

**Repair round 1 (2026-09-22), after the three review lenses of PR #314.** This is run 4's report with its grades re-derived,
not re-stated: the four clause-2 coordinates run 4 graded not established are credited to committed witnesses the run's
search had excluded (§8.5, executed in `repair1/`), the reaper-hold rows are described as they are graded (§15.2), the
evidence record is finalised after the last gate attempt rather than before (§15.3), and every figure is re-derived from the
finalised files. The verdict changed in its reason and not in its outcome, and §16 says which.

**Repair round 2 (2026-09-22), after the second review of PR #314.** The three lenses that reviewed repair round 1 agreed
with the verdict, the clause grades and the credits, and disagreed with what the report said about its own evidence. This
round changes the latter and nothing else: one mutation receipt whose exit was a rejected command line and not a kill is
replaced by a re-execution with its control (§8.5, `repair2/`), and the other saved test logs its audit selected are read
for the same shape (a population round 2 called the whole directory, and repair round 4 found short and closed: **F57**, §5); the fault-test group graded under ST-07's construction standard where the
definition's words ask for a literal kill matrix is graded as the definition words it (§4); the carried findings that bear
on clauses 2, 3 and 8 and were listed but never judged are judged (§13, §9, §14); and the summaries that still asserted
what their sections had withdrawn are corrected — the snapshot scope (§11, §14, §16), the fault firings behind #305's
credit (§6), the count of gate reds (§2.1, §15.2, §16), the guest disk figures that had no saved source (§10) and the
log-header claim above. **A correction that fixes a section and leaves the summary citing the old version is not a
correction.** After every edit of this round the narrowed claim was searched for across the whole assembled report and
the pull request body, and the sweep is saved (`repair2/claim-sweep.txt`). No grade moved, and §16 says why not. A second
commit of this round drops this pull request's copy of the finding file run 4 filed, which had reached master ahead of this
report (§13); the report is otherwise the same, and its gates ran at both heads (§15.2).

**Repair round 4 (2026-09-23), after the third review of PR #314** — the orchestrator's numbering: a third round was
commissioned and stood down before it did any work, so this round answers the third review directly. The three lenses that
reviewed repair round 2 (`g5r4-3`: verify CHANGES_REQUIRED, record FAIL, regression FAIL) again agreed with the verdict, the
clause grades and every coordinate credit, and found five places where this report still said more than its evidence shows.
Each is corrected here and nowhere restated. **The #311 pin mutation** was written as a kill by its re-aimed twin and as one
of three kills, where the receipts show one kill, one survivor and a test failure at the harness's kill check that never
reaches the recovery (§5, §6, **F61**). **The log audit** was described as a reading of the whole directory's test logs,
where its scanner had selected only the logs its family globs matched — never the ST-07 checks or round 2's own receipts —
and the gate legs at round 2's heads postdated it; it is re-run over a population it enumerates and closes, with the gate
legs at this report's own head outside it by construction (§5, **F57**, §15.2). **The header** said the probe logs carried
the range's sha, where the clause-2 probe's two summary logs carry none and their provenance is the probe's build log
(above, `repair4/provenance-check.txt`). **The pull request body's aggregate** counted the one invocation that ran no test
as a survivor (§5, **F62**). And **§8.3** said neither a plant's nor an adjacent construction's witness records any firing,
where an adjacent construction's witness records its fault at the neighbouring coordinate (§8.3, **F60**). No grade moved,
and §16 says why not. The round's only execution was the ten gates at its own head — one attempt, green (**F51**) — and it
executed nothing under `src/`: every correction is a reading of receipts already saved, and its new files are the audit, the
checks and the figures they feed (`repair4/`). Every narrowed claim was again searched for across
the whole assembled report and the pull request body (`repair4/claim-sweep.txt`).

**Repair round 5 (2026-09-23), after the fourth review of PR #314.** The three lenses that reviewed repair round 4
(`pr-g5r4r4`: verify CHANGES_REQUIRED, regression CHANGES_REQUIRED for a carried defect and no new one, record FAIL) again
agreed with the verdict, the clause grades and every coordinate credit, and found the carve-out wrongly counted and two
readings stale. Each is corrected here and nowhere restated. **The carve-out** (§14) had been enumerated as the allowlist's
forty-five non-empty rows, where v17 names the files that carry a *file-level* allowance: two of those rows are not that at
the range — `src/agent/bin.rs` allows only on its inline test module, `src/agent/proc/test_support/readiness.rs` denies at
file level with per-site expectations and is test-only — so the carve-out is forty-three files, the classified modules
split twenty-nine inside and twenty-five outside rather than thirty and twenty-four, and a third classified module,
`src/agent/bin.rs`, carries neither allowance nor fence (**F42**, **F43**, derived from the source at the range rather than from the
rows). The bypass was executed in it too, by the review's lenses and again by this round in a fresh scratch clone of the
range (**F63**, `repair5/probes/`): an admitted production call site, as run 4's two hosts were, and no runtime write
claimed. Both of run 4's hosts remain outside the corrected carve-out, so the answer to Q5's wrapper limb is unchanged and
the verdict with it. **The required audit's rows 31 and 32** described their integration ref store as the RecordingRefs
double, where the frozen witness resumes through the repository's own refs and reads Git's listing and reflog — §8.4 had
said so, the artifact had not — and the sweep for that class found the same stale call-path citation in fifteen more rows,
three prose references to line numbers the range had moved, and one carried sentence about an alternative witness that now
replays twice; every one is refreshed, and no grade moved (§8.3, **F65**,
`audit/refresh-repair5.json`). **The header** said the gate and control logs record a head that is not the range; the range
control `gates/pr281-control/range.log` names the range itself and a clean tree, and the provenance grouping above is
rewritten from a re-reading of every cited log at this head (`repair5/provenance-check.txt`). No grade moved, and §16 says
why not. The round's executions were the third host's route (§14, **F63**), in a scratch clone restored and proved equal to the
range's blobs, its ten-gate attempts — seven at its pre-merge head and one at its final head (**F51**, **F67**) — and the controls
for those attempts' reds: forty runs of the red tests alone across attempts 2 to 7 and the four runs of attempt 1's launch-shape
demonstration (**F68**); the rest of the round is a reading of saved files or of the frozen source. Every narrowed claim was again searched for
across the whole assembled report and the pull request body (`repair5/claim-sweep.txt`). **The round then merged the
reviewed master into this pull request** (§15.2, **F66**): seven ten-gate attempts at its pre-merge head, whose `src/` is
the range's, were red on the test leg every time — every red but two a fingerprint on the run brief's known-flake list, its
test passing alone in its control, and attempt 1's two terminal-signal reds a cause that round's own asynchronous launch
made and demonstrated, not a listed flake (**F67**, **F68**, §15.2) — and the orchestrator decided the integration of
`5b16f727` (#315, #316, #317) — conflict-free, no rebase, the pull request's diff against master still the report alone.
From that merge on the product paths at this report's head are master's, byte for byte; the gate's historical input, every
historical claim and the verdict are the range's and unchanged, and every historical figure and checker reads an exact
checkout of the range (`~/repair5-work/range-tree`) rather than the worktree. Round 5's receipt for that checkout
(`repair5/integration/range-tree.log`) proves its head, cleanliness and `src/` tree and records the audit, the figures and
the carve-out derivation regenerating byte-identical against it; it also records the self-check exiting 1 there, and it
never invoked the mutation summary — round 5's text called all five regenerated byte-identical, and the fifth review caught
it. The one failure was the report's own path, absent at the range by construction
(`repair6/selfcheck-reproduction.log`), which the checker has exempted since; the complete regeneration against that
checkout, the mutation summary and the self-check included, is repair round 6's `repair6/regeneration.log` (§1.3).

**Repair round 6 (2026-09-23), after the fifth review of PR #314.** The three lenses that reviewed repair round 5
(`pr-g5r4r5`: verify CHANGES_REQUIRED, regression CHANGES_REQUIRED, record FAIL) again agreed with the verdict, the clause
grades and every coordinate credit — the carve-out, the audit's refreshed rows, the third host, both source identities and
every product-diff equality reproduced — and found five statements of this report and its pull request body contradicted
by the receipts they cite, every one a statement about the record and none about the tree or the range. Each is corrected
here and nowhere restated, and no grade moved. **§1.3** denied that any historical execution had run in a worktree, where the
cumulative suite's receipts name the run's worktree at the range and repair round 1's witness receipt names the repair
worktree; **F69** now derives, from every execution receipt's own header, the tree it names and whether that head's `src/`
is the range's, and §1.3 says which executions ran in a worktree and which in a scratch clone. **The integration
paragraphs** (this header, §1.3, §15.2 and the body) cited `repair5/integration/range-tree.log` for the audit, figures,
mutation summary, carve-out and self-check regenerating byte-identical, where that receipt records the self-check exiting 1
and no mutation-summary invocation at all; the receipt is kept as it is, its one failure is identified
(`repair6/selfcheck-reproduction.log`: the report's own path, absent at the range by construction, exempted by the checker
since), and the successful regeneration of every historical generator is this round's own receipt
(`repair6/regeneration.log`). **Clause 5's row and §2.1** counted the reds of this run and its repair rounds as three gate
attempts sharing one fingerprint, an exhaustive summary written before round 5's seven red attempts at `4611b204` joined
**F51**; both now count every red attempt **F51** lists, in a population that closes at round 6's first head `06a2b8a1` so that
the attempts at this report's own head, written after its commit, are outside it by construction (§15.2): the three earlier
ones with their one fingerprint (**F52**), round 5's seven with theirs (**F67**) and round 6's one at its first head (**F71**);
and the checker that had pinned the three-attempt wording is versioned rather than rewritten
(`repair6/checks-r2.py`, `repair6/checks-versioning.diff`; `repair2/checks.py` is unchanged). **The round-5 summaries** here
and in the body called each of those seven attempts' reds a fingerprint on the run brief's known-flake list, where two —
the terminal-signal tests of attempt 1 — are not on that list and are the demonstrated cause of that round's own
asynchronous launch (**F67**, §15.2); every restatement now keeps them apart. **The body's control total** said thirty-five
where the six control logs of attempts 2 to 7 hold forty captured exits; the total is now derived (**F68**) and cited, not
spelled. Two blanket sentences the record lens found are scoped as well: "every receipt … names the range" in §15.2 now
carries the header's exception for the clause-2 probe's two summary logs, and "every repository path … exists at the range"
(below) names the one exception the checker makes for this report's own path. The round wrote nothing under `src/`; what it
executed is in its receipts: the ten gates at its two heads, three attempts in all (**F51**, **F72**), and, for attempt 1's red
at its first head, that test alone five times in the foreground, recorded twice (**F71**: ten isolated runs with captured exits,
the first receipt's header misreading the panic site and kept beside the re-run); every correction is a reading of saved
receipts, and the sweep for each narrowed claim is `repair6/claim-sweep.txt`. Round 6's text here said it had run nothing but
the ten gates, and the sixth review caught it. **Its second commit, `00a16a64`, is the head the sixth review read.** Its first
head, `06a2b8a1`, carried these corrections and went red once at its own gates, on a listed fingerprint, before passing all ten
on its second attempt (**F71**); the orchestrator's answer to that report required the red to stay as visible as every earlier
round's and every count of reds to state its cutoff, so that second commit froze that head's two attempts into **F51** (§15.2)
and said, at each count, which population it counts; its own attempt at `00a16a64`, one and green, is **F72**, after **F51**'s
cutoff.

**Repair round 7 (2026-09-23), after the sixth review of PR #314.** The three lenses that reviewed repair round 6
(`pr-g5r4r6`: verify CHANGES_REQUIRED, regression CHANGES_REQUIRED, record FAIL) again agreed with the verdict, the clause
grades, every coordinate credit, the corrected carve-out, both source identities and every historical regeneration, and found
this report's account of its own repair rounds — what each executed, which population a restated figure holds, what the review
history says of each lens, and which attempts keep their per-leg logs — contradicted by the receipts. Each is corrected here and
nowhere restated, and no grade moved. **Round 6's execution summary** (this header, the pull request body and the evidence
index) said the round had run nothing but the ten gates, where its two control receipts hold ten isolated runs of its attempt 1's
red test, each with a captured exit (**F71**); and **round 5's** (this header and the body) called the third host the round's
one execution, where its seven gate attempts at its pre-merge head, its one at its final head and the forty-four control
invocations behind them are in its receipts as well (**F51**, **F67**, **F68**). Each summary now names the gates, the attempts
and the controls it covers, and no summary of what a round executed is universal without its population. **The body's
descriptions of F69 and F70** were written at round 6's first cutoff and left standing after both figures were re-derived at its
second: **F69** holds four worktrees and two heads carrying master's `src/`, not three and one, and **F70** reads the second
cutoff's population, not the first's; the body now describes each figure at the cutoff the figure states, with the first cutoff
named as history. **The review history** in the body gave round 5's regression lens the verify lens's finding list; it now names
each lens's own findings, for every review, read from the final reports. **The per-leg claim** (§15.2, the body and the evidence
index) said every attempt kept its per-leg logs, where run 4's attempts 1, 2, 5 and 6 keep their runner summaries and no per-leg
log; **F73** now reads from the directory which attempts keep their nine legs and which keep the runner's summary alone. Two
figures make the populations explicit rather than implied: **F72**, round 6's one attempt at its final head `00a16a64`, green,
which lies after **F51**'s stated cutoff and before this round's first commit; and **F74**, this round's log audit at its two
cutoffs — the first equal to round 6's post-commit population, the second adding its first head's legs and control (§5). The
round wrote nothing under `src/` and, before its first commit, ran no test: every correction is a reading of saved receipts.
**This commit is the round's second; its first head, `50c0410b`, is not the head the review reads.** At that head the ten
gates went red once on the test leg, on two fingerprints read inside the failure block — an observed production
cleanup-ownership refusal, the red of round 6's attempt 1 and of round 5's attempts 3 and 5, and an observed count-budget
Docker timeout — each test then passing alone five times in its control, which establishes neither the cause of the
whole-suite failure nor that it is benign, and the second attempt passed all ten (**F75**; the orchestrator's answer authorised
that one further attempt, and this round's two attempts and ten control runs at that head are executions outside the
baseline, counted as such). The finalisation at that head then found a defect of this round's own: **F73**'s derivation had
read every `gates/eight-logs-*/` directory on disk and asserted each claimed by a listed attempt, so once that head's own two
were archived the post-commit regeneration no longer reproduced the figure and the assembled report differed from the
committed blob (`repair7/post-commit-regeneration-50c0410.log`, kept as the failed step it is; every other generator output
byte-identical). This commit bounds **F73** to the directories of the heads it lists, with a later head's outside by
construction as **F51**, **F70** and **F74** already are; freezes that head's two attempts as **F75**; and re-runs the log
audit at a second cutoff, the first kept (**F74**). After this commit the round runs the ten gates at its own head and, for
any red, that test alone as a control, in files written after the commit (§15.2) and inventoried in the pull request body. The sweep for each
narrowed claim is `repair7/claim-sweep.txt`; the checkers of rounds 2, 4, 5 and 6 run in versioned copies with only their stale
predicates re-bound (`repair7/checks-r{6,5,4,2}.py`, `repair7/checks-versioning.diff`; the originals are unchanged); and
`repair7/checks.py` adds a predicate for each contradiction the sixth review executed, each comparing a restatement with the
population its cited file holds — a passing run establishes those predicates and no more.

**How to read this report.** Every measurement it quotes has an id like `F12` and lives once in §0's table, which is
generated by `figures/figures.py` from the saved file named beside it. The prose spells no measured figure of its own;
the counts it gives in words are of things the reader can count in the files it names, and `report/selfcheck.py` lists
every bare number left in the prose for reading. Paths
are relative to the evidence directory `~/tactus-artifacts/g5-evidence-9bb177e/` unless they begin `src/`,
`effects/`, `findings/`, `reviews/`, `design/`, `docs/` or `.github/`, which are repository paths at the range. The
evidence directory deliberately holds no `findings/` of its own — its ledger outputs are under `ledger/` — so no
path in this report is ambiguous between the two, and a check over the assembled text confirms it: every repository
path it names exists at the range — with one exception the check states by name: this report's own path,
`reviews/2026-09-22-gate-G5.md`, is the pull request's file, present at the pull request's head and absent at the range by
construction, and the checker exempts exactly that path (`report/selfcheck.py`, its `REPORT_SELF`) — and every evidence
path it names exists in the evidence directory; the manifest over that directory is finalised after this report's commit
and verified over it then (§15.3).

**Four paths in the run brief were stale copies from the run-2 and run-3 briefs, and were used as corrected**: the
handover file (`~/orch-pr10/handovers/g5_run2.md` -> `~/orch-pr10/handovers/g5_run4.md`), the private build target
(`iso-g5_run2` -> `/mnt/ramtarget/iso-g5_run4`), the gate runner's argument
(`w1-eight-iso /srv/worktrees/g5-run3` -> `/srv/worktrees/g5-run4`: the argument names the worktree to test, and
run 3's tree is not this report's head), and CI's commit (`--commit d3ee2f25…` -> `--commit 9bb177ea…`).

## 0. The figures

| id | figure | value | source (evidence directory) | derivation |
|---|---|---|---|---|
| F1 | the design packet this run read | a34417d6248edbd80ecdabd7fb9a4b8bcd3ff1d98270c67bd43c25f9fb0b712d (the copy in spec/, equal to the packet at ~/tactus-artifacts and to the hash the amendment record states) | `spec/tactus-parallel-design-neutral-v17.json, spec/AMENDMENT-2026-09-22-q5.md` | sha256 of the copy kept in spec/, checked equal to the original and to the amendment record |
| F2 | gates[4] and the questions, v16 against v17 | gates[4] byte-identical: True; questions differing: 1 | `spec/regen-spec.out` | the two comparison lines |
| F3 | the regenerated definition and questions against the derived files on disk | questions differing: 0; definition sections differing semantically: 0 | `spec/definition-diff.txt, spec/questions-diff.txt` | sections and questions that differ other than by json key order or escaping |
| F4 | first-parent merges in the range d3ee2f25..9bb177ea | 9 | `range/merges-in-range.log` | line count |
| F5 | src/ changed across the range | 67 files changed, 7799 insertions(+), 810 deletions(-) | `range/diff-in-range.log` | the diffstat total line |
| F6 | the fault-injection registry across the range | effects/sequential-registry.json at d3ee2f25: 1aeee56fc73ee033072fad814f9733347faca6f0 | effects/sequential-registry.json at 9bb177ea: 1aeee56fc73ee033072fad814f9733347faca6f0 | `range/registry-blob.log` | git blob ids at both ends |
| F7 | registry entries | 180 entries: executed 168, not_executed 3, recovery_proven 9 | `audit/registry-rows.tsv` | the registry read by audit/registry-rows.py |
| F8 | the registry witnesses and the tests the range added | witnesses: 59  unchanged=51  changed=8  new-in-range=0  gone=0; added in range: 60   removed in range: 0 | `audit/witness-change-map.txt, audit/tests-added-in-range.txt` | the two summary lines |
| F9 | the cumulative suite on Linux at the range (the run this report cites) | lib 2736 passed, 0 failed, 90 ignored; main.rs 10 passed; exit 0 | `suite/linux-suite-clear.log` | the whole-binary test result lines and the captured exit |
| F10 | the earlier Linux control run, on a measured quiet box before the neighbour started | lib 2736 passed, 0 failed, 90 ignored; exit 0 | `suite/linux-suite-prehold.log` | the same lines |
| F11 | the two Linux export runs | run 1 lib 2736 passed, 0 failed, 90 ignored, exit 0; run 2 lib 2736 passed, 0 failed, 90 ignored, exit 0 | `suite/exports-run1.log, suite/exports-run2.log` | the same lines |
| F12 | the cumulative suite on the Windows guest at the range | lib 2629 passed, 0 failed, 67 ignored; main.rs 12 passed, 0 failed, 1 ignored; exit 0 | `suite/guest-suite.log` | the same lines |
| F13 | the guest export run | lib 2629 passed, 0 failed, 67 ignored; suite exit 0; ST-07 merge check exit 0 | `guest/guest-exports.log` | the same lines |
| F14 | CI at the range, newest run per check name, judged by the run's conclusion | CI: 35730965035 success; Pull request policy: 35729784205 success; pages build and deployment: 35730963766 success | `ci/runs-at-range.json` | the newest run per name |
| F15 | CI's three platforms at the range, lib target | test (ubuntu-latest): lib 2736 passed, 0 failed, 90 ignored; test (macos-latest): lib 2706 passed, 0 failed, 69 ignored; test (winguest): lib 2629 passed, 0 failed, 67 ignored | `ci/ci-35730965035.log` | the whole-binary result lines per job |
| F16 | every job of both CI runs at the range | 20 jobs completed/success, 0 not | `ci/jobs-at-range.txt` | conclusions counted |
| F17 | the clause-2 probe, re-run on the cleared box | tests 2736 nonzero 8 | `audit/probe/run-each.log` | its own summary line |
| F18 | what the probe recorded per registry row | runs 2736 rows 168; rows with any firing: 123; rows with a firing followed by recovery and a replay pair: 89 | `audit/probe/analyze.log` | its summary lines |
| F19 | the per-coordinate audit over the 168 executed entries | 168 {'established': 168, 'i=P': 32, 'ii=yes': 168, 'iii=yes': 138, 'i=E': 35, 'i=K': 76, 'i==': 25, 'iii=V': 30} — all checks passed | `audit/grade-check.log, audit/per-coordinate-audit.tsv` | the checker's own tally line and its verdict |
| F20 | the coordinates not established, if any | none | `audit/per-coordinate-audit.tsv` | the rows whose class is not established; `none` when every row is established |
| F21 | run 3's grades at its range, for comparison | class: Counter({'established': 123, 'near-exact': 25, 'not established': 20}) | `audit/run3-baseline.txt` | its class counts |
| F22 | carried readings whose cites no longer resolved at this range | 63 rows, 263 cite failures; distinct failing cites: 25 | `audit/reanchor-needed.txt, audit/grade-check-carried.log` | the checker's first run against the carried readings, and the distinct cites it named |
| F23 | rows this run read afresh rather than carrying | 22: 9, 10, 16, 17, 23, 24, 25, 26, 35, 40, 41, 42, 44, 48, 49, 50, 51, 52, 53, 80, 88, 89 | `audit/regrade-*.json` | the rows those files name |
| F24 | #311's planted candidate pin, measured over the whole suite | tests touching either pin funnel: 140; tests with an unmatched pin delete AFTER THE LAST fault of the trace: 6 | `audit/pin-unmatched.txt` | the two counting lines of the cell-aware reading |
| F25 | run 3's near-exact rows, measured per recovery at this range | 25 rows; credited recovery clean of all three repairs: 8; credited recovery enters no RunDir.RemoveMarker: 25; credited recovery enters RunDir.RemoveMarker: 0 | `audit/exactness-per-recovery.txt` | the repairs each recovery of the credited witness performs, in order |
| F26 | ST-07's merge check over each export | Linux export 1: records 431, funnel executions 425, inventory 68, entries 180, witnessed claims 163, failures 0; Linux export 2: records 431, funnel executions 425, inventory 68, entries 180, witnessed claims 163, failures 0; guest: records 428, funnel executions 422, inventory 68, entries 180, witnessed claims 164, failures 0 | `st07/st07-summary-export1.json, st07/st07-summary-export2.json, guest/exports/st07-summary.json` | the summaries the check writes |
| F27 | the effect-site inventory | **68** — RunDir 14, Worktree 11, Ref 8, Container 8, Object 6, Lock 6, Event 5, Snapshot 4, Answer 3, Process 2, Report 1 | `inventory/site-enums-export.md` | its own counts line |
| F28 | the census summary, and whether the two Linux exports and the guest agree | 10991b391643ee97cd0a04ad121bb379109c7d96d1da6f3251be9e7b0e5a388f — identical across all three | `census-export1/census-summary.json, census-export2/census-summary.json, guest/exports/census-summary.json` | sha256 of each |
| F29 | the census family | 4 members: prefix (fan-out, 20,000 states, truncated), seeded chain: sequences, repairs (chain, 25 states, closed), seeded fan-out: sequences, repairs, lineages (fan-out, 31 states, closed), seeded join: sequences, repairs, lineages (join, 75 states, closed) | `census-export2/bounded-census-artifact.md` | the members table |
| F30 | every bound of the amended bounds line over the union | every_bound_reached_over_the_union = True; 10 numeric dimensions, each reached by equality; plan shapes: chain, fan-out, join | `census-export2/bounded-census-artifact.md` | the bounds table and the summary flag |
| F31 | coverage assertion 5's named instances over the family | a_be_refused_under_halt=409 b_halted_after_declined_park=94 c_parked_refused_with_backoff=1167 c_complete_refused_with_backoff=1167 d_halted_accepted_with_backoff=14 d_budget_accepted_with_backoff=312 states_accepting_some_outcome=4947 unfinished_states=17392 | `probes/census-instances/probe-run.log` | the union line of this run's own probe |
| F32 | the bytes on disk at the probe's snapshot points | 11 byte snapshots over 6 tests that pass: 3 at the step-level live-versus-replay comparisons (logs of 5, 6, 7 lines), 2 at the two-crash proof's points, 2 at the negative variant's, 4 at replay-twice points | `probes/projection/probe-run.log` | the G5BYTES lines, counted by the point each was printed at |
| F33 | the two-crash proof, positive variant, before and after the simulated power loss | len=4702 sha256=756d563c2b5b7d932631bdf348777c7f9d9b287b07cc5731d3d44003f24c415a merge_prepared=1 task_merged=1 lines=8 / len=4493 sha256=4ba6d2911d88d5af0b974fa23904eb3b391fc6c4108fae9cb87064bab64631d4 merge_prepared=1 task_merged=0 lines=7 | `probes/projection/probe-run.log` | its two G5BYTES lines |
| F34 | the negative variant, before and after the loss | len=4502 sha256=75d2d09b31431205277aed58ed14a6903d6b0aa5124b3a6125f45fa2120d1079 merge_prepared=1 task_merged=0 lines=7 / len=3993 sha256=9e80f3d7c84047b5393a7f1a6da8a7c08d96c8aab169440dc848fae8e1b47c83 merge_prepared=0 task_merged=0 lines=6 | `probes/projection/probe-run.log` | its two G5BYTES lines |
| F35 | the per-resource ledger tables | 6 tables per export; export 1 and export 2 byte-identical; the guest differs from Linux only in the two `cleanup.lock` R21 lines (24 differing lines in all) | `ledger-export1/, ledger-export2/, guest/exports/ledger/` | the six tables of each export, compared |
| F36 | every row's verdict in the six tables | 324 yes, 0 no | `ledger-export2/` | the verdict column of every row of every table |
| F45 | the five Git-child kill samplers alone, five runs each, on Linux | 25 runs exit 0, 0 not; 75 histogram files; git status lines after each run: 25 of 25 zero | `samplers/linux-loops.log, samplers/*.json` | the captured exit of every run, and the histogram files kept |
| F46 | the same five samplers, five runs each, on the Windows guest | 25 runs exit 0, 0 not; 70 histogram files | `guest/guest-samplers.log, guest/exports/samplers/` | the same |
| F48 | the residue-class evidence record | sites 9 pairs 24 ok 24 histogram rows 110 bad 0 | `residue/residue-class-evidence-record.md, residue/generate.log` | the generator's own counts |
| F37 | the nine group invocations at the range | group1-kill-matrix 86 passed; 0 failed, exit 0; group2-two-crash 2 passed; 0 failed, exit 0; group3-locks-config-residue 53 passed; 0 failed, exit 0; group4-st17-closure 13 passed; 0 failed, exit 0; group5-st18-finalization 36 passed; 0 failed, exit 0; group6-outcomes-and-resume 17 passed; 0 failed, exit 0; group7-seam-tests 40 passed; 0 failed, exit 0; group8-effects-censuses-q5 190 passed; 0 failed, exit 0; group9-seam-supplement-st12-st13-st15-st19-st20 16 passed; 0 failed, exit 0 | `groups/run-groups.out, groups/logs/` | each invocation's own result line and captured exit |
| F38 | the group filters against this range's own test list | groups: 9 filters: 264; filters that do not resolve at this range: 0 | `groups/filters-check.log, groups/test-list-9bb177e.txt` | the check's counts |
| F39 | the finding ledger at the range | total: 354; severity: {'P1': 8, 'P2': 145, 'P3': 201}; disposition (normalised): {'deferred': 348, 'accepted-risk': 6} | `ledger/ledger-normalised.txt` | its counts |
| F40 | findings opened and closed across the range | added 9, removed 4 | `ledger/range-delta.txt` | the two diff blocks |
| F41 | the P1 findings at this range and at run 3's, and their dispositions | 8 at 9bb177ea, 9 at d3ee2f25; all 8 `deferred`, none `open` (the ledger shows {'deferred': 348, 'accepted-risk': 6}) | `ledger/range-delta.txt, ledger/ledger-normalised.txt` | the two P1 listings of the range delta, and the ledger's disposition counts |
| F42 | the carve-out Q5 names: files carrying a file-level allowance of a governed lint, derived from the frozen source's leading inner attributes (repair round 5; run 4 read the allowlist's rows) | files carrying a file-level allowance of a governed lint at the range: 43 (funnel rows 22, legacy rows 21); per lint at file level: disallowed_methods 42, disallowed_types 23, disallowed_macros 19, none of style, all or warnings; of them declared under #[cfg(test)] or an example target: 15; allowlist rows: 51, rows with a non-empty `allows`: 45 (run 4's F42 read that count as the carve-out); rows recording an allowance the file does not carry at file level: 2 (`src/agent/bin.rs`, `src/agent/proc/test_support/readiness.rs`); files carrying a file-level allowance with no row: 0; file-level `#![expect]` of a governed lint: 0 | `repair5/carveout.json, repair5/carveout.txt, questions/q5-carveout-from-allowlist.txt` | every tracked .rs blob at the range read with git show; a file is in the carve-out iff a `#![allow(..)]` among its leading inner attributes names a governed lint; the allowlist's rows re-read beside it and the two counts compared |
| F43 | the carve-out against the classification's own domain (repair round 5: the file-level carve-out; run 4 intersected the allowlist's rows) | wrapper-classification modules: 54; classification modules that are ALSO in the carve-out: 29; classification modules NOT in the carve-out: 25 (run 4's F43, counting rows: 30 / 24); classification modules outside the carve-out with no file-level fence of a governed lint either: 3 (`src/agent/bin.rs`, `src/capacity.rs`, `src/runner/invocation.rs`) | `repair5/carveout.json, questions/q5-wrapper-domain.txt` | effects::CLASSIFIED_MODULES at the range intersected with the file-level carve-out, and the remainder read for a file-level deny or forbid of a governed lint |
| F44 | the residual bypass, executed by run 4 in two of the three classified modules outside the carve-out (src/capacity.rs, src/runner/invocation.rs) | macro witness: clippy exit 0, effects test result: ok. 190 passed; 0 failed; control (macro emits deny): clippy exit 101; control (name written literally): effects test result: FAILED. 184 passed; 6 failed | `questions/q5-route/` | clippy and the effects suite, with both controls |
| F49 | the finalization kill matrices' cells, as the tree asserts them | Complete 24 cells, Halted 22; the site excluded by name is `Ref.DeleteCandidatePin`, driven separately over a declared pin | `src/engine/topology/recover/tests.rs` | the two counts `kill_at_every_finalization_cell` asserts, and the site it excludes by name |
| F50 | the 'no red' and 'no known flake' claims, checked by search over the five suite logs | 15 whole-binary result lines, all `ok` with 0 failed; flake fingerprints searched over all five logs: 12 (6 assertion texts or errnos, 6 test names), appearing as a failure: 0; every `FAILED` line in the five logs (5) is an ignored test's own reason line (`T-FAILED kill tests`), and every `panicked at` line (4) is the deliberate panic of the helper subprocess `sigchld_target_setup_failure_helper` | `suite/no-red-check.log, suite/no-red-check.sh` | every whole-binary result line, every FAILED and 'panicked at' line of all five logs, and each listed flake's fingerprint over all five, labelled by its kind |
| F51 | the ten gates at every head up to round 6's first head 06a2b8a1, the population's stated cutoff: every attempt run 4, repair round 1, repair round 2, repair round 4, repair round 5's pre-merge head, repair round 5's final head and repair round 6's first head made, with the head each ran at; round 6's one attempt at its final head 00a16a64, after that cutoff, is F72, round 7's two at its first head 50c0410b are F75, and the attempts at this report's own head are written after its commit and are outside all three by construction (every head up to 4611b204 has the range's src/; 5744b0aa's, 06a2b8a1's, 00a16a64's and 50c0410b's are the reviewed master's, F66) | attempt 1 at 8b2116c: w1-eight-iso [ALL 9 PASS at 8b2116c exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 2 at f39abbb: w1-eight-iso [ALL 9 PASS at f39abbb exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 3 at e44385b: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 4 at e44385b: w1-eight-iso [ALL 9 PASS at e44385b exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 5 at b19d89a: w1-eight-iso [ALL 9 PASS at b19d89a exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 6 at d1e48bb: w1-eight-iso [ALL 9 PASS at d1e48bb exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 1 at 26d4bc0: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 2 at 26d4bc0: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 3 at 26d4bc0: w1-eight-iso [ALL 9 PASS at 26d4bc0 exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 1 at 5679907: w1-eight-iso [ALL 9 PASS at 5679907 exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 1 at b3a404d: w1-eight-iso [ALL 9 PASS at b3a404d exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 1 at f117679: w1-eight-iso [ALL 9 PASS at f117679 exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 1 at 4611b20: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 2 at 4611b20: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 3 at 4611b20: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 4 at 4611b20: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 5 at 4611b20: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 6 at 4611b20: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 7 at 4611b20: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 1 at 5744b0a: w1-eight-iso [ALL 9 PASS at 5744b0a exit=0 ]; gate10 [test-pr-ready-audit exit=0] // attempt 1 at 06a2b8a: w1-eight-iso [exit=1 ]; gate10 [test-pr-ready-audit exit=0] // attempt 2 at 06a2b8a: w1-eight-iso [ALL 9 PASS at 06a2b8a exit=0 ]; gate10 [test-pr-ready-audit exit=0] | `gates/head-attempts.log, gates/final-head-attempt.log, gates/head-attempt-5679907.log, gates/head-attempt-b3a404d.log, gates/head-attempt-f117679.log, gates/head-attempt-4611b20.log, gates/head-attempt-5744b0a.log, gates/head-attempt-06a2b8a.log, gates/w1-eight-iso-head-<N>.log, gates/gate10-head-<N>.log, gates/w1-eight-iso-final.log, gates/gate10-final.log, gates/w1-eight-iso-5679907.log, gates/gate10-5679907.log, gates/w1-eight-iso-b3a404d.log, gates/gate10-b3a404d.log, gates/w1-eight-iso-f117679.log, gates/gate10-f117679.log, gates/w1-eight-iso-4611b20.log, gates/gate10-4611b20.log, gates/w1-eight-iso-5744b0a.log, gates/gate10-5744b0a.log, gates/w1-eight-iso-06a2b8a.log, gates/gate10-06a2b8a.log` | one line per attempt from each of the eight files (run 4's six at its heads, repair round 1's three at 26d4bc0, repair round 2's one at each of its heads 5679907e and b3a404dd, repair round 4's one at f1176796, repair round 5's seven at its pre-merge head 4611b204 -- every one of those heads with the range's src/ -- repair round 5's one at its final head 5744b0aa and repair round 6's two at its first head 06a2b8a1, the last three on the reviewed master's src/), appended while live, never overwritten, and frozen when this table is generated; round 6's one attempt at its final head 00a16a64 is read by F72 from `gates/head-attempt-00a16a6.log`, and round 7's two at its first head 50c0410b by F75 from `gates/head-attempt-50c0410.log`, each written after that head's commit and frozen since; the attempts at this report's own head, whose src/ is the reviewed master's too, are `gates/head-attempt-<head>.log`, written after the commit (§15.2) |
| F52 | the reds the gate attempts of F51 saw at the heads up to repair round 4's, fingerprinted from each failing leg's own log (repair round 5's seven reds at its pre-merge head are F67's) | 3 red test legs among the attempts F51 lists (attempt 3 at e44385b, attempt 1 at 26d4bc0, attempt 2 at 26d4bc0), every one `test result: FAILED. 2735 passed; 1 failed; 90 ignored`; one fingerprint in all three: the test `engine::topology::recover::tests::a_host_integration_reaper_holds_the_runs_cleanup_lease`, panicking at `src/engine/topology/recover/tests.rs:9268:5` on "the hold outlived the reaper that took it" | `gates/eight-logs-e44385b-attempt3/03-test.log, gates/eight-logs-26d4bc0-final-attempt1-failed/03-test.log, gates/eight-logs-26d4bc0-final-attempt2-failed/03-test.log, gates/head-attempts.log, gates/final-head-attempt.log, gates/head-attempt-5679907.log, gates/head-attempt-b3a404d.log, gates/head-attempt-f117679.log` | every attempt line reading exit=1, and per failing leg its own result line and -- read inside the failure block, not from the first panic in the file -- the test that failed, its panic site and its assertion text, asserted identical across the legs |
| F53 | the controls for those reds: the same test alone, five times at e44385b (the head of run 4's red attempt, whose src/ is the range's, as at every head before the merge of §15.2), five at the merge base, and ten at 26d4bc0 in two batches, the second under the load repair round 1's second red ran under | head   passes=5 fails=0  fingerprint hits=0; range  passes=5 fails=0  fingerprint hits=0; heads: e44385b (head.log), 9bb177e (range.log) // at 26d4bc0: head   passes=5 fails=0  fingerprint hits=0; head batch 2   passes=5 fails=0  fingerprint hits=0; head: 26d4bc0 (pr281-control-final/head.log) | `gates/pr281-control/summary.txt, gates/pr281-control/head.log, gates/pr281-control/range.log, gates/pr281-control-final/summary.txt, gates/pr281-control-final/head.log` | the outcome of every run and the count of the fingerprint; the heads read from the logs' own header lines |
| F54 | rows regraded by repair round 1, with the witness each is now credited to | 4: 9 -> `a_retained_generation_left_by_a_dead_incarnation_is_closed_by_the_next_resume_and_its_worktree_reclaimed`; 10 -> `a_retained_generation_left_by_a_dead_incarnation_is_closed_by_the_next_resume_and_its_worktree_reclaimed`; 88 -> `a_creation_stopped_before_its_private_half_leaves_a_released_run_lock_the_census_reclaims_public_only`; 89 -> `a_creation_stopped_before_its_private_half_leaves_a_released_run_lock_the_census_reclaims_public_only` | `audit/regrade-repair1.json` | the rows that file names, and their witnesses |
| F55 | the plant-domain bound at this range: every test whose process made a test-code replay pair, from this run's own probe | 124 tests; run 3's domain listed 103, of which 103 are in this range's and 0 are not; 21 tests are in this range's only | `audit/grading/plant-domain-124-9bb177ea.json, audit/grading/plant-domain-103.json` | the tests listed, against run 3's domain file |
| F56 | the Windows guest's free disk space before the first sampler run and after the last, in the re-run's own log | 846,245,888 bytes free before the first run; 579,665,920 after the last | `guest/guest-samplers.log` | the two `bytes free` lines of the log, first and last |
| F57 | the saved cargo-test logs, read for an invocation rejected before running any test, a filter matching no test, or a claimed outcome the log does not show: repair round 4's audit over a population it enumerates and closes (repair round 2's audit read the logs its family globs selected and was described as reading every saved log) | population closed at 2026-09-22T23:53:28Z (UTC): 210 logs checked (mutation recipe logs 168, repair1 probes and witnesses 5, repair2 probes 4, group invocations 9, suite runs 5, probes 2, st07 merge checks 2, gate test legs 7, reaper controls 3, q5 route effects suites 2, guest 2, samplers 1); test-bearing .log files enumerated in the directory at that cutoff: 210, outside the audit's families: 0, selected but not test-bearing: 0; affected: 2 -- `mutations/run-g5/g5-answered-question-stays-open-vs-g5-witness.log` (VOID: running 0 tests; CLAIMED-PASS-WITHOUT-A-PASSED-TEST); `repair1/probes/released-lock-called-live.log` (REJECTED-BEFORE-RUN: error: Option 'exact' given more than once; NO-RUNNING-LINE; CLAIMED-KILL-WITHOUT-A-FAILED-TEST); repair round 2's audit had selected 202 of these and none outside them, and the 8 it never read are `gates/eight-logs-5679907-final/03-test.log`, `gates/eight-logs-b3a404d-final/03-test.log`, `repair2/probes/projection-attributed.log`, `repair2/probes/released-lock-called-live.log`, `repair2/probes/released-lock-control.log`, `repair2/probes/two-crash-without-fsync.log`, `st07/st07-merge-check-export1.log`, `st07/st07-merge-check-export2.log`; the affected set is the same in both | `repair4/log-audit.json, repair4/log-audit.log, repair4/log-audit.py, repair2/log-audit.json` | the audit's cutoff, its counts per family, its enumeration check (every `.log` in the directory holding a test-harness run, a per-test outcome line or a harness rejection at the cutoff must be in a family, and every selected log must be such a file), each affected log with what was found in it, and the logs it covers that round 2's did not; the gate legs at this report's own head are written after the cutoff and are outside it (§15.2) |
| F58 | the projection probe re-run with every snapshot labelled by the test that printed it (repair round 2) | 11 snapshots over 6 passing tests, printed by 4 of them: `barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` 3; `projections_are_equal_between_live_and_replay_at_every_prefix` 3; `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step` 3; `unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing` 2; printing none: 2 (`a_clean_staging_worktree_left_at_the_integration_head_is_reclaimed_and_the_candidate_integrates`, `a_kill_after_the_snapshot_add_is_settled_interrupted_and_the_snapshot_reclaimed`); by point kind: live-vs-replay 3, negative 2, replay-twice 4, two-crash 2 | `repair2/probes/projection-attributed.log` | the G5BYTES lines counted per test thread, the passing tests that printed none, and the point kinds; the run must pass and the driver's receipt check read OK |
| F59 | rows 88 and 89's mutation control, re-executed with its control in repair round 2 (round 1's log recorded a rejected invocation) | control (unpatched): `test result: ok. 2 passed; 0 failed`, exit 0, `running 2 tests`; mutation (the released lock read as live): `test result: FAILED. 0 passed; 2 failed`, exit 101, `running 2 tests`, both tests panicking at `src/engine/topology/startup/tests.rs:2088:5` asserting `ReclaimedPublicOnly(TargetAbsent)` and finding `Skipped` (2 times) | `repair2/probes/released-lock-control.log, repair2/probes/released-lock-called-live.log, repair2/probes/released-lock-called-live.diff` | each log's running line, result line and captured exit and, for the mutation, every panic site and the assertion's two sides; the driver's own receipt check must read OK |
| F60 | plant and adjacent credits against the probe: none fires at its own coordinate, and how many of the carried adjacent credits' witnesses record their neighbouring fault (repair round 4) | plant (`P`) credits 32, adjacent (`=`) credits 25; rows whose witness records a fault at the credited coordinate itself: 0; carried `=` rows whose witness records a fault at another coordinate: 19 (rows 7, 33, 34, 47, 55, 58, 67, 70, 76, 91, 92, 99, 100, 101, 102, 108, 109, 132, 133; e.g. row 67 (RunDir.RemoveMarker/before, `=`): Event.AppendFirst/point:synced:kill K; row 132 (Lock.ProbeCleanupExclusive/before, `=`): Lock.AcquireRun/after E); carried `P` rows whose witness records any fault: 0; re-read rows (F23) whose witness records a fault elsewhere: 4 (row 23 (Snapshot.WriteIntent/before, `=`): Object.SnapshotCommitTree/after K; row 51 (Object.SnapshotCommitTree/before, `=`): Object.CandidateWriteTree/after K; row 88 (RunDir.RemovePrivateHusk/after, `P`): RunDir.CreatePrivateDir/before E; row 89 (RunDir.RemovePublicHusk/before, `P`): RunDir.CreatePrivateDir/before E) | `audit/probe/coordinate-firings.json, audit/per-coordinate-audit.tsv, audit/regrade-*.json` | every `P` and `=` row of the audit, its witness's recorded faults in the probe, and whether any fault's coordinate equals the row's own (asserted none); the carried rows are those no regrade file names (F23 lists the re-read ones) |
| F61 | the three recipes aimed at #311's exactness helpers: each one's captured exit, the named test's own outcome line, and where it panicked | `g5r4-census-keeps-the-stale-marker`: exit 101, `a_clean_staging_worktree_left_at_the_integration_head_is_reclaimed_and_the_candidate_integrates ... FAILED`, panicking at `src/engine/topology/recover/tests.rs:7009` on "the creator's marker is gone"; `g5r4-reclaim-after-creation-keeps-the-pin`: exit 0, `a_resume_over_a_stale_queued_candidate_with_nothing_staged_takes_the_staging_path_and_publishes_the_proposal ... ok`; `g5r4-reclaim-after-creation-keeps-the-pin-vs-promotion-kill`: exit 101, `a_kill_before_the_candidate_pin_is_deleted_is_finished_by_the_next_resume_which_deletes_it_once ... FAILED`, panicking at `src/engine/topology/recover/tests.rs:14551` on "the child did not die by the kill at `before-candidate-pin-delete`" | `mutations/chain-g5run4.log, mutations/run-g5run4/g5r4-census-keeps-the-stale-marker.log, mutations/run-g5run4/g5r4-reclaim-after-creation-keeps-the-pin.log, mutations/run-g5run4/g5r4-reclaim-after-creation-keeps-the-pin-vs-promotion-kill.log` | the chain log's rc per recipe (the twin's two chain lines must agree), and from each recipe log the `test ... ok|FAILED` line and the first `panicked at` site with its message; a 101 must have a FAILED line and a panic, a 0 neither |
| F62 | the mutation campaign in aggregate, each recipe classified from its own log and not from its exit alone (repair round 4) | 168 recipes: 148 killed (exit 101, the named test FAILED in its log), 19 survived (exit 0, the named test ok in its log), 1 VOID (exit 0, `running 0 tests` on every binary and no named test line) | `mutations/chain-*.log, mutations/run-*/*.log` | every recipe of the six chain logs (a recipe re-run under its own name counted once), its log read for the named test's outcome line and its `running` lines; a survivor is an exit 0 whose named test passed, a VOID an exit 0 that ran no test |
| F63 | the residual bypass in the third classified module the corrected carve-out leaves with neither allowance nor fence, src/agent/bin.rs: executed by repair round 5 in a fresh scratch clone of the range, in run 4's shape (an admitted production call site; no runtime write is claimed or arranged) | host `src/agent/bin.rs`, called from production `park_question` in `src/engine/topology/integrate.rs`: macro witness (the macro emits `allow`): clippy exit 0, effects `test result: ok. 190 passed; 0 failed`, test exit 0; control (the macro emits `deny`): clippy exit 101, `use of a disallowed method std::fs::write` at `src/agent/bin.rs:98:13`; control (the name written literally): effects `test result: FAILED. 189 passed; 1 failed`, test exit 101, the one failure `every_externally_reachable_fn_of_a_legacy_or_shared_module_is_classified` naming "g5r5_probe_write_bin" unclassified; both files restored to the range's blobs (sha256 equal) and the clone clean | `repair5/probes/bin-macro-witness.log, repair5/probes/effects-bin-macro.log, repair5/probes/bin-control-deny.log, repair5/probes/effects-bin-literal.log, repair5/probes/bin-macro-witness.patch, repair5/probes/bin-control-deny.patch, repair5/probes/bin-control-literal.patch, repair5/probes/restore.log, repair5/probes/driver.log` | each log's header names the range as the tree's head and the diff applied; clippy's and the test harness's captured exits, the effects suite's result lines, the deny control's diagnostic site and the literal control's failing test and the name it refuses; the driver refuses any exit other than the expected one, and the restore log proves both files equal to the range's blobs |
| F64 | the saved cargo-test logs, read for the same three shapes as F57, over the population as it stands at repair round 5's cutoff (round 4's frozen audit is F57; this one adds round 5's own receipts) | population closed at 2026-09-23T01:32:27Z (UTC): 213 logs checked (mutation recipe logs 168, repair1 probes and witnesses 5, repair2 probes 4, repair5 third-host effects suites 2, group invocations 9, suite runs 5, probes 2, st07 merge checks 2, gate test legs 8, reaper controls 3, q5 route effects suites 2, guest 2, samplers 1); test-bearing .log files enumerated in the directory at that cutoff: 213, outside the audit's families: 0, selected but not test-bearing: 0; affected: 2 -- `mutations/run-g5/g5-answered-question-stays-open-vs-g5-witness.log` (VOID: running 0 tests; CLAIMED-PASS-WITHOUT-A-PASSED-TEST); `repair1/probes/released-lock-called-live.log` (REJECTED-BEFORE-RUN: error: Option 'exact' given more than once; NO-RUNNING-LINE; CLAIMED-KILL-WITHOUT-A-FAILED-TEST); against round 4's post-commit audit at f117679 (211 logs) the population adds 2: `repair5/probes/effects-bin-literal.log`, `repair5/probes/effects-bin-macro.log`; the affected set is the same | `repair5/log-audit.json, repair5/log-audit.log, repair5/log-audit.py, repair4/log-audit-post-commit-f117679.json` | round 4's audit script with one family added for round 5's receipts, its cutoff, its counts per family, its enumeration check, each affected log, and the logs it covers that round 4's post-commit audit did not; written after this cutoff and outside it by construction are the seven gate legs at round 5's pre-merge head with their controls (`gates/eight-logs-4611b20-final-attempt<N>-failed/`, `gates/repair5-control/`, F67) and the gate leg at round 5's final head 5744b0aa; round 5's post-commit re-run at that head listed them all in families of their own, and repair round 6's audit at its own cutoff (F70) holds them in a frozen pre-commit population; the gate leg at round 6's final head 00a16a64 is listed by round 6's post-commit re-run at that head and by round 7's audit at its own cutoff (F74); the gate legs at this report's own head are written after every pre-commit cutoff and are listed by round 7's post-commit re-run alone (§15.2) |
| F65 | the required per-coordinate audit swept for descriptions and call-path citations written against a witness the range changed (repair round 5, after the fourth review's finding on rows 31 and 32) | rows swept: 168; before the refresh: (ii) cites into the Given-driven helper `resume_with` on rows whose witness resumes through the real-refs path: 17; descriptions naming the RecordingRefs double on a witness whose body holds none: 2; after the refresh (`audit/refresh-repair5.json`, 17 rows, grades unchanged, 5 shared-note substitutions): 0 and 0; rows naming a function whose body changed or was added in the range, read for staleness: 42 | `repair5/audit-sweep.json, repair5/audit-sweep-before.log, audit/refresh-repair5.json` | every reading's cites checked against the witness's own resume path at the range and its text against the RecordingRefs double; the refresh applied and the sweep re-run; the readings naming a changed function listed and read |
| F66 | the two source domains after repair round 5's integration of the reviewed master: the merge commit's identities, and the frozen-range checkout every historical figure and checker reads (round 5's final report-only head 5744b0aa is certified by its post-commit receipt `repair5/integration/post-commit-identity-5744b0a.log`; round 6's two heads 06a2b8a1 and 00a16a64 by `repair6/post-commit-identity-06a2b8a.log` and `repair6/post-commit-identity-00a16a6.log`, round 7's first head 50c0410b by `repair7/post-commit-identity-50c0410.log`; this report's own head by `repair7/post-commit-identity-<head>.log`, written after its commit) | merge commit `21b46492cc86f18c1bb92cd5f5da0a63bcaa1fb9` (parents `4611b204`, the pre-merge head, and `5b16f727`, the reviewed master) with tree `504827d1895c` equal to the tree measured before merging; merge-base(range, merge) = the range and merge-base(master, merge) = master; product paths (src effects Cargo.toml Cargo.lock .github scripts docs) master..merge: empty; product delta range..merge sha256 `7dd1c8d721b9` = range..master (`4 files changed, 108 insertions(+), 16 deletions(-)`: #315, #316, #317); the pull request's diff against its base, before (range..pre-merge head) and after (master..merge): the one file `reviews/2026-09-22-gate-G5.md`, sha256 `86470330fe57` both times; src/ tree at the merge = master's (`25116b15cdea`), the range's is `4a15dd4a9930`; the frozen-range checkout the historical figures read: HEAD `9bb177ea` = the range, clean, src/ tree `4a15dd4a9930` = the range's | `repair5/integration/merge.json, repair5/integration/merge.log, repair5/integration/range-tree.log` | the merge commit named by the receipt, re-derived from git in the pull request's worktree: its tree against the merge-tree measurement, its parents, both merge bases, the product-path diff against master (must be empty), the range-to-merge product delta against the range-to-master delta (sha256 equal), the pull request's diff sha256 before and after (equal, one file), and the src/ tree ids of range, master and merge; the range checkout's head, cleanliness and src/ tree against the range's (asserted; any failure is a derivation error) |
| F67 | repair round 5's seven ten-gate attempts at its pre-merge head 4611b204 (src/ the range's), all red on the test leg: the reds, their fingerprints and the control of each (old-head evidence, kept; not attempts at this report's head) | seven attempts, every test leg red, every other leg and gate ten green each time; attempt 1: 2733 passed, 3 failed, 90 ignored; reds `terminal_interrupt_kills_the_isolated_tree`, `terminal_quit_kills_the_isolated_tree`, `a_hold_whose_command_never_spawns_is_released_with_the_descriptor`; control `gates/repair5-control/attempt1-reds.log`: 2 runs exit 0, 2 not; attempt 2: 2735 passed, 1 failed, 90 ignored; reds `a_hold_whose_command_never_spawns_is_released_with_the_descriptor`; control `gates/repair5-control/attempt2-red.log`: 5 runs exit 0, 0 not; attempt 3: 2735 passed, 1 failed, 90 ignored; reds `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner`; control `gates/repair5-control/attempt3-red.log`: 5 runs exit 0, 0 not; attempt 4: 2734 passed, 2 failed, 90 ignored; reds `a_host_integration_reaper_holds_the_runs_cleanup_lease`, `a_hold_whose_command_never_spawns_is_released_with_the_descriptor`; control `gates/repair5-control/attempt4-reds.log`: 10 runs exit 0, 0 not; attempt 5: 2734 passed, 2 failed, 90 ignored; reds `a_host_integration_reaper_holds_the_runs_cleanup_lease`, `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner`; control `gates/repair5-control/attempt5-reds.log`: 10 runs exit 0, 0 not; attempt 6: 2735 passed, 1 failed, 90 ignored; reds `a_hold_whose_command_never_spawns_is_released_with_the_descriptor`; control `gates/repair5-control/attempt6-red.log`: 5 runs exit 0, 0 not; attempt 7: 2735 passed, 1 failed, 90 ignored; reds `a_host_integration_reaper_holds_the_runs_cleanup_lease`; control `gates/repair5-control/attempt7-red.log`: 5 runs exit 0, 0 not; by fingerprint: `a_hold_whose_command_never_spawns_is_released_with_the_descriptor` 4, `a_host_integration_reaper_holds_the_runs_cleanup_lease` 3, `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner` 2, `terminal_interrupt_kills_the_isolated_tree` 1, `terminal_quit_kills_the_isolated_tree` 1; attempt 1's two terminal-signal reds reproduce in the background launch shape (job `SigIgn 0000000000000006`, the two FAILED once each) and pass in the foreground (ok twice each): a launcher cause; every other red is a fingerprint on the run brief's known-flake list, its test passing alone in its control | `gates/head-attempt-4611b20.log, gates/eight-logs-4611b20-final-attempt<N>-failed/03-test.log, gates/repair5-control/` | each attempt's line, its failing leg's result line and FAILED tests with the fingerprint asserted present, and its control log's captured exits; the counts per fingerprint over the seven |
| F68 | the control runs behind F67, counted from the logs (repair round 6, after the fifth review found the body's total wrong): every captured cargo exit of the six control logs of attempts 2 to 7, and the four of attempt 1's launch-shape demonstration in order | attempts 2 to 7: 40 control runs, 40 exit 0, 0 not (5, 5, 10, 10, 5, 5 per attempt: five per red test); attempt 1's demonstration log, separately: 4 runs, exits 101, 101, 0, 0 (background-shape run 1: the three tests: exit 101; foreground run 1: the three tests: exit 101; foreground run 2: the descriptor-hold test alone: exit 0; foreground run 3: the two terminal tests alone: exit 0) | `gates/repair5-control/attempt2-red.log, gates/repair5-control/attempt3-red.log, gates/repair5-control/attempt4-reds.log, gates/repair5-control/attempt5-reds.log, gates/repair5-control/attempt6-red.log, gates/repair5-control/attempt7-red.log, gates/repair5-control/attempt1-reds.log` | every `cargo exit=` line of the six control logs counted by value and per attempt; attempt 1's four exits read in order with the run each labels |
| F69 | the tree each historical execution's receipt names (repair round 6, after the fifth review found §1.3 denying that any historical execution ran in a worktree): the worktrees of this pull request and the scratch clones of the range, with every head each ran at and whether that head's src/ is the range's or the reviewed master's -- the population as it stood at round 6's second cutoff, before its second commit: the gate leg at round 6's final head 00a16a64 (F72), the legs at round 7's first head 50c0410b (F75) and the legs at this report's own head are after it and outside it | 74 receipt entries read; in a worktree of this pull request: `/srv/worktrees/g5-run4` at 9bb177e src/ range's, e44385b src/ range's: cumulative suite, Linux (2); the nine group invocations (9); the two ST-07 merge checks (2); the Linux samplers (1); the reaper-red controls (1); the gate attempts' test legs (2) // `/srv/worktrees/repair-g5r4` at 26d4bc0 src/ range's, 5679907 src/ range's, b3a404d src/ range's, d1e48bb src/ range's, f117679 src/ range's: repair round 1's six committed witnesses (1); the reaper-red controls (1); the gate attempts' test legs (6) // `/srv/worktrees/repair-g5r4-r5` at 4611b20 src/ range's, 5744b0a src/ master's: repair round 5's controls for its attempts' reds (7); the gate attempts' test legs (8) // `/srv/worktrees/repair-g5r4-r6` at 06a2b8a src/ master's: repair round 6's control for its attempt 1's red at its first head (2); the gate attempts' test legs (2); src/ tree at every worktree head up to 4611b204 equal to the range's (4a15dd4a9930): True; worktree heads carrying the reviewed master's src/: 5744b0a, 06a2b8a (round 5's final head and round 6's first head: their gate attempts, F51, and round 6's control, F71); in a scratch clone of the range, never a worktree: `/home/ubuntu/g5r4-work/aux-tree` (the projection and census-instances probes (2); the reaper-red controls (1)) // `/home/ubuntu/g5r4-work/mut-tree` (the mutation campaign (warm-up) (1); the mutation campaign (167 recipe logs) (1)) // `/home/ubuntu/g5r4-work/probe-tree` (the clause-2 probe's build (1)) // `/home/ubuntu/g5r4-work/q5-tree` (run 4's Q5 route (5)) // `/home/ubuntu/repair5-work/q5-tree` (repair round 5's third host (5)) // `/tmp/claude-1000/-srv-worktrees-repair-g5r4/b1e2363c-ffb1-4ab5-a1be-8f7f062178d0/scratchpad/range-tree` (repair round 2's scratch-clone probes and mutation controls (4)) // `/tmp/claude-1000/-srv-worktrees-repair-g5r4/e4fabb0d-9008-4e28-8a1f-153269ab672c/scratchpad/range-tree` (repair round 1's scratch-clone probes and mutation controls (4)); the Windows guest's own tree `C:\upstroke-g5-run4` (its samplers log's Compiling line; its suite and export logs name its target directory); receipts naming no tree: `suite/exports-run1.log`, `suite/exports-run2.log`, `suite/guest-suite.log`, `guest/guest-exports.log`, `mutations/run-g5/g5-residue-frozen-n-changed.log` (the two export suite runs and the guest's suite and export logs name the range as HEAD with a clean status; the one recipe log names its backup, §5) | `repair6/execution-trees.json, repair6/execution-trees.txt, repair6/execution-trees.py` | the tree read from each receipt's header (`### worktree:`, `tree <path>`, `in <path>`) or its Compiling/Checking line, from the invoking script's WT= line for the group invocations, the ST-07 checks and the samplers, and from the runner log's `nine-command baseline at <sha7> in <worktree>` line for the three gate legs without a Compiling line; grouped by tree; every worktree head's src/ tree re-derived here from git in the pull request's repository and compared with the range's and the reviewed master's |
| F70 | the saved cargo-test logs, read for the same three shapes as F57 and F64, over the population as it stands at repair round 6's second cutoff, before its second commit (round 5's frozen audit is F64; round 6's first cutoff, before its first head existed, is kept beside it and equals round 5's post-commit population) | population closed at 2026-09-23T05:09:23Z (UTC), round 6's second cutoff: 232 logs checked (mutation recipe logs 168, repair1 probes and witnesses 5, repair2 probes 4, repair5 third-host effects suites 2, group invocations 9, suite runs 5, probes 2, st07 merge checks 2, gate test legs 18, reaper controls 3, repair5 attempt controls 7, repair6 attempt controls 2, q5 route effects suites 2, guest 2, samplers 1); test-bearing .log files enumerated in the directory at that cutoff: 232, outside the audit's families: 0, selected but not test-bearing: 0; affected: 2 -- `mutations/run-g5/g5-answered-question-stays-open-vs-g5-witness.log` (VOID: running 0 tests; CLAIMED-PASS-WITHOUT-A-PASSED-TEST); `repair1/probes/released-lock-called-live.log` (REJECTED-BEFORE-RUN: error: Option 'exact' given more than once; NO-RUNNING-LINE; CLAIMED-KILL-WITHOUT-A-FAILED-TEST); against round 5's frozen audit at its cutoff (F64, 213 logs) the population adds 19: `gates/eight-logs-06a2b8a-final-attempt1-failed/03-test.log`, `gates/eight-logs-06a2b8a-final/03-test.log`, `gates/eight-logs-4611b20-final-attempt1-failed/03-test.log`, `gates/eight-logs-4611b20-final-attempt2-failed/03-test.log`, `gates/eight-logs-4611b20-final-attempt3-failed/03-test.log`, `gates/eight-logs-4611b20-final-attempt4-failed/03-test.log`, `gates/eight-logs-4611b20-final-attempt5-failed/03-test.log`, `gates/eight-logs-4611b20-final-attempt6-failed/03-test.log`, `gates/eight-logs-4611b20-final-attempt7-failed/03-test.log`, `gates/eight-logs-5744b0a-final/03-test.log`, `gates/repair5-control/attempt1-reds.log`, `gates/repair5-control/attempt2-red.log`, `gates/repair5-control/attempt3-red.log`, `gates/repair5-control/attempt4-reds.log`, `gates/repair5-control/attempt5-reds.log`, `gates/repair5-control/attempt6-red.log`, `gates/repair5-control/attempt7-red.log`, `gates/repair6-control/final-attempt1-red-first-header-misread.log`, `gates/repair6-control/final-attempt1-red.log`; round 6's first cutoff (2026-09-23T04:30:06Z, 228 logs, before its first head existed) equals round 5's post-commit population at 5744b0a (228 logs), and the second cutoff adds 4 to it: `gates/eight-logs-06a2b8a-final-attempt1-failed/03-test.log`, `gates/eight-logs-06a2b8a-final/03-test.log`, `gates/repair6-control/final-attempt1-red-first-header-misread.log`, `gates/repair6-control/final-attempt1-red.log`; the affected set is the same in all four | `repair6/log-audit.json, repair6/log-audit.log, repair6/log-audit-first-cutoff.json, repair6/log-audit-first-cutoff.log, repair6/log-audit.py, repair5/log-audit.json, repair5/log-audit-post-commit-5744b0a.json` | round 5's audit script with one family added for this round's own attempt controls, its cutoff, its counts per family, its enumeration check, each affected log, the logs it covers that round 5's frozen audit did not, its first cutoff's equality with round 5's post-commit population and what its second cutoff adds (the two gate legs at round 6's first head and its two control logs); the gate leg at round 6's final head 00a16a64 is written after this cutoff and outside it, listed by round 6's post-commit re-run at that head, `repair6/log-audit-post-commit-00a16a6.log`, and by round 7's audit at its cutoffs (F74); the gate legs at this report's own head are outside every pre-commit cutoff by construction, listed by round 7's post-commit re-run at finalisation, `repair7/log-audit-post-commit-<head>.log`, which this report does not quote (§15.2) |
| F71 | repair round 6's two ten-gate attempts at its first head 06a2b8a1 (src/ the reviewed master's), the head that carried that round's corrections before its second commit 00a16a64: attempt 1 red on the test leg, its fingerprint and control, attempt 2 green (kept and counted, so that round 6's own red is as visible as every earlier round's; its two control receipts hold ten isolated runs, and the round's execution summary must say so; not attempts at this report's head) | two attempts at round 6's first head 06a2b8a1 (src/ the reviewed master's); attempt 1: 2735 passed, 1 failed, 90 ignored, every other leg and gate ten green; the red `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner`, panicking at `src/engine/topology/recover/tests.rs:8824:10` on "refusing overlapping engine ownership" -- production's cleanup-ownership refusal, raised by the resume (#317, in master's src/ at this head, changed the reaper assertion test's bounded wait, not this test and not the refusal); the first fingerprint on the run brief's known-flake list and of F50's twelve, the red of round 5's attempts 3 and 5 as well (F67); control `gates/repair6-control/final-attempt1-red.log` (the test alone, foreground): 5 runs exit 0, 0 not -- which establishes that the test passes alone, not the cause of the full-suite failure nor that it is benign (the first control log, `gates/repair6-control/final-attempt1-red-first-header-misread.log`, 5 runs exit 0, 0 not, is kept: its header's panic site was read from the file's first `panicked at` line rather than the failure block); attempt 2: 2736 passed, 0 failed, 90 ignored, 10 passed, 0 failed, 0 ignored, all nine legs and gate ten green | `gates/head-attempt-06a2b8a.log, gates/eight-logs-06a2b8a-final-attempt1-failed/03-test.log, gates/eight-logs-06a2b8a-final/03-test.log, gates/repair6-control/final-attempt1-red.log, gates/repair6-control/final-attempt1-red-first-header-misread.log` | each attempt's line; attempt 1's failing leg's result line and FAILED test, the panic site and assertion text read inside the failure block; both control logs' captured exits, the re-run's header asserted to name the failure block's site and the first's asserted not to; attempt 2's whole-binary result lines, all ok |
| F72 | repair round 6's one ten-gate attempt at its final head 00a16a64 (src/ the reviewed master's), the head the sixth review read: after F51's stated cutoff and before this commit, frozen here by repair round 7 the way each round freezes the previous head's attempts (not an attempt at this report's head) | one attempt at round 6's final head 00a16a64, green on the first attempt: `attempt 1 at 00a16a6: w1-eight-iso [ALL 9 PASS at 00a16a6 exit=0 ]; gate10 [test-pr-ready-audit exit=0]`; the test leg 2736 passed, 0 failed, 90 ignored, 10 passed, 0 failed, 0 ignored, all nine legs and gate ten green; invoked as a foreground child of the session's shell (no asynchronous job, no nohup, no command substitution), the runner shell's SigIgn 0000000000000004, the runner's argument checked equal to the worktree `/srv/worktrees/repair-g5r4-r6`, which every leg's Compiling/Checking line names; src/ tree at 00a16a64 25116b15cdea = the reviewed master's (25116b15cdea), not the range's (4a15dd4a9930) | `gates/head-attempt-00a16a6.log, gates/w1-eight-iso-00a16a6.log, gates/gate10-00a16a6.log, gates/eight-logs-00a16a6-final/03-test.log, gates/eight-logs-00a16a6-final/` | the attempt line; the test leg's whole-binary result lines, all ok; the runner log's launch shape, signal mask and argument check; the tree every per-leg log's Compiling/Checking line names, asserted equal to the runner's argument; the gate-ten exit; the src/ tree at that head re-derived from git in the pull request's repository |
| F73 | which ten-gate attempts keep their per-leg logs, read from the directory (repair round 7, after the sixth review found §15.2, the body and the evidence index saying every attempt's were kept): for every attempt F51 lists, F72's and F75's, the `gates/eight-logs-<sha7>-*/` directory that holds its nine legs, or the runner's summary log alone where no directory exists -- over the directories of those heads only; a later head's directories, this report's own head's among them, are outside by construction (§15.2) | 25 attempts read (F51's 22, F72's one and F75's two); with their nine per-leg logs in the evidence directory: 21 -- attempt 3 at e44385b (red): `gates/eight-logs-e44385b-attempt3/`; attempt 4 at e44385b (green): `gates/eight-logs-e44385b-attempt4/`; attempt 1 at 26d4bc0 (red): `gates/eight-logs-26d4bc0-final-attempt1-failed/`; attempt 2 at 26d4bc0 (red): `gates/eight-logs-26d4bc0-final-attempt2-failed/`; attempt 3 at 26d4bc0 (green): `gates/eight-logs-26d4bc0-final/`; attempt 1 at 5679907 (green): `gates/eight-logs-5679907-final/`; attempt 1 at b3a404d (green): `gates/eight-logs-b3a404d-final/`; attempt 1 at f117679 (green): `gates/eight-logs-f117679-final/`; attempt 1 at 4611b20 (red): `gates/eight-logs-4611b20-final-attempt1-failed/`; attempt 2 at 4611b20 (red): `gates/eight-logs-4611b20-final-attempt2-failed/`; attempt 3 at 4611b20 (red): `gates/eight-logs-4611b20-final-attempt3-failed/`; attempt 4 at 4611b20 (red): `gates/eight-logs-4611b20-final-attempt4-failed/`; attempt 5 at 4611b20 (red): `gates/eight-logs-4611b20-final-attempt5-failed/`; attempt 6 at 4611b20 (red): `gates/eight-logs-4611b20-final-attempt6-failed/`; attempt 7 at 4611b20 (red): `gates/eight-logs-4611b20-final-attempt7-failed/`; attempt 1 at 5744b0a (green): `gates/eight-logs-5744b0a-final/`; attempt 1 at 06a2b8a (red): `gates/eight-logs-06a2b8a-final-attempt1-failed/`; attempt 2 at 06a2b8a (green): `gates/eight-logs-06a2b8a-final/`; attempt 1 at 00a16a6 (green): `gates/eight-logs-00a16a6-final/`; attempt 1 at 50c0410 (red): `gates/eight-logs-50c0410-final-attempt1-failed/`; attempt 2 at 50c0410 (green): `gates/eight-logs-50c0410-final/`; with the runner's summary log and the gate-ten log alone, no per-leg log: 4 -- attempt 1 at 8b2116c (green): `gates/w1-eight-iso-head-1.log` (`ALL 9 PASS at 8b2116c`, `w1-eight-iso exit=0`) and `gates/gate10-head-1.log` (exit 0), no `gates/eight-logs-8b2116c*/` directory; attempt 2 at f39abbb (green): `gates/w1-eight-iso-head-2.log` (`ALL 9 PASS at f39abbb`, `w1-eight-iso exit=0`) and `gates/gate10-head-2.log` (exit 0), no `gates/eight-logs-f39abbb*/` directory; attempt 5 at b19d89a (green): `gates/w1-eight-iso-head-5.log` (`ALL 9 PASS at b19d89a`, `w1-eight-iso exit=0`) and `gates/gate10-head-5.log` (exit 0), no `gates/eight-logs-b19d89a*/` directory; attempt 6 at d1e48bb (green): `gates/w1-eight-iso-head-6.log` (`ALL 9 PASS at d1e48bb`, `w1-eight-iso exit=0`) and `gates/gate10-head-6.log` (exit 0), no `gates/eight-logs-d1e48bb*/` directory; every `gates/eight-logs-*/` directory of a listed head (21) is claimed by exactly one attempt, and each claimed directory's test leg agrees with its attempt line: True; the directories of a later head -- this report's own, archived after its commit -- are outside this population by construction | `gates/head-attempts.log, gates/final-head-attempt.log, gates/head-attempt-<sha7>.log, gates/eight-logs-<sha7>-*/, gates/w1-eight-iso-head-<N>.log, gates/gate10-head-<N>.log` | each attempt line matched to the one directory named by its head and, for a red attempt, its number (`-final-attempt<N>-failed` or `-attempt<N>`; for a green one `-final` or `-attempt<N>`), the directory's test leg asserted to agree with the line and to hold nine legs, every directory of a listed head claimed by exactly one attempt; an attempt with no directory asserted to be one of run 4's own green attempts, with its runner log's `ALL 9 PASS` and captured exit read; round 7's first commit had read every directory on disk here, and its own head's two, archived after that commit, made the figure fail to regenerate (`repair7/post-commit-regeneration-50c0410.log`), which this commit corrects by bounding the population to the listed heads |
| F74 | the saved cargo-test logs, read for the same three shapes as F57, F64 and F70, over the population as it stands at repair round 7's second cutoff, before its second commit (round 6's frozen audit at its second cutoff is F70; round 7's first cutoff, before its first head existed, is kept beside it and equals round 6's post-commit population) | population closed at 2026-09-23T07:08:44Z (UTC), round 7's second cutoff, before its second commit: 236 logs checked (mutation recipe logs 168, repair1 probes and witnesses 5, repair2 probes 4, repair5 third-host effects suites 2, group invocations 9, suite runs 5, probes 2, st07 merge checks 2, gate test legs 21, reaper controls 3, repair5 attempt controls 7, repair6 attempt controls 2, repair7 attempt controls 1, q5 route effects suites 2, guest 2, samplers 1); test-bearing .log files enumerated in the directory at that cutoff: 236, outside the audit's families: 0, selected but not test-bearing: 0; affected: 2 -- `mutations/run-g5/g5-answered-question-stays-open-vs-g5-witness.log` (VOID: running 0 tests; CLAIMED-PASS-WITHOUT-A-PASSED-TEST); `repair1/probes/released-lock-called-live.log` (REJECTED-BEFORE-RUN: error: Option 'exact' given more than once; NO-RUNNING-LINE; CLAIMED-KILL-WITHOUT-A-FAILED-TEST); round 7's first cutoff (2026-09-23T06:31:52Z, 233 logs, before its first head existed) equals round 6's post-commit population at 00a16a6 (233 logs, `repair6/log-audit-post-commit-00a16a6.log`) and adds 1 over round 6's frozen audit (F70, 232 logs): `gates/eight-logs-00a16a6-final/03-test.log` (the gate leg at round 6's final head 00a16a64, F72); the second cutoff adds 3 to the first: `gates/eight-logs-50c0410-final-attempt1-failed/03-test.log`, `gates/eight-logs-50c0410-final/03-test.log`, `gates/repair7-control/final-attempt1-reds.log` (the two gate legs at round 7's first head 50c0410b and their control receipt, F75); the affected set is the same in all four; round 7 ran no test before its first cutoff | `repair7/log-audit.json, repair7/log-audit.log, repair7/log-audit-first-cutoff.json, repair7/log-audit-first-cutoff.log, repair7/log-audit.py, repair6/log-audit.json, repair6/log-audit-post-commit-00a16a6.json` | round 6's audit script with one family added for this round's own attempt controls, its cutoff, its counts per family, its enumeration check, each affected log, its first cutoff's equality with round 6's post-commit population and the one log that adds over round 6's frozen audit, and what its second cutoff adds (the two gate legs at round 7's first head and their control receipt); the gate legs at this report's own head are written after this cutoff and outside it by construction, listed by the post-commit re-run at finalisation, `repair7/log-audit-post-commit-<head>.log`, which this report does not quote (§15.2) |
| F75 | repair round 7's two ten-gate attempts at its first head 50c0410b (src/ the reviewed master's), the head that carried this round's corrections before this commit: attempt 1 red on the test leg on two fingerprints, each described as observed, with the control of each; attempt 2 green (kept and counted, so that this round's own red is as visible as every earlier round's; not attempts at this report's head) | two attempts at round 7's first head 50c0410b (src/ the reviewed master's), both foreground; attempt 1: 2734 passed, 2 failed, 90 ignored, every other leg and gate ten green, two reds read inside the failure block: `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner` at `src/engine/topology/recover/tests.rs:8824:10` on "refusing overlapping engine ownership" -- an observed production cleanup-ownership refusal, raised by the resume -- the fingerprint the run brief lists first and F50 searches first, the red of round 6's attempt 1 at 06a2b8a1 (F71) and of round 5's attempts 3 and 5 at 4611b204 (F67); #317, in this head's src/, changed a different, test-side wait; `real_docker_kill_on_an_already_exited_container_is_tolerated` at `src/runner/container/tests.rs:3436:5` on "is still running after 200 observations" -- an observed count-budget Docker timeout -- the termination poll at this head's src/ is bounded by a count of observations, not by time, and expired; the fingerprint is the run brief's real_docker_* entry, and the deferred findings PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME (P3) and TESTS-CONTAINER-WAIT-HAS-NO-TIME-BOUND (P2) at this head are class matches, not a cause; another session's cargo test was running on the box at launch, recorded in the runner log and not offered as a cause; control `gates/repair7-control/final-attempt1-reds.log` (each test alone, five times, foreground, one receipt): `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner` 5 runs exit 0, 0 not, `real_docker_kill_on_an_already_exited_container_is_tolerated` 5 runs exit 0, 0 not -- which establishes that each test passes alone, not the cause of the whole-suite failure nor that it is benign; attempt 2: 2736 passed, 0 failed, 90 ignored, 10 passed, 0 failed, 0 ignored, all nine legs and gate ten green; load average at launch 9.89, 7.73, 5.33 (attempt 1) and 3.92, 5.95, 5.49 (attempt 2), recorded and not offered as a cause | `gates/head-attempt-50c0410.log, gates/w1-eight-iso-50c0410.log, gates/eight-logs-50c0410-final-attempt1-failed/03-test.log, gates/eight-logs-50c0410-final/03-test.log, gates/repair7-control/final-attempt1-reds.log` | each attempt's line; attempt 1's failing leg's result line and its two FAILED tests, each panic site and assertion text read inside the failure block and asserted present in the control's header; the control's captured exits per test; attempt 2's whole-binary result lines, all ok; the runner log's launch shapes and load averages |

## 1. The run

### 1.1 The specification

`spec/` holds the regeneration. `spec/regen-spec.py` reads
`~/tactus-artifacts/tactus-parallel-design-neutral-v17.json` (**F1**) and writes
`cumulative_review_gates.gates[4]` and `.standing_questions` as canonical JSON and in the derived files' own format.
**v17, not v16**: Q5's wrapper-classification limb was amended on 2026-09-22 on the project owner's authority
(`~/tactus-artifacts/AMENDMENT-2026-09-22-q5.md`), and grading Q5 from v16 would reproduce run 3's clause-8
assertion against a question the packet no longer asks.

Two claims of that amendment were checked rather than taken (**F2**): G5's own entry is byte-identical between v16
and v17, so `G5-DEFINITION.txt` needed no regeneration; and of the six standing questions exactly one differs.
Against the derived files on disk (**F3**): all six questions match v17 verbatim, and the definition's sections
match, differing from the regeneration only in JSON key order and in the escaping of an em-dash — verified
semantically equal by parsing both (`spec/definition-diff-detail.txt`).

Counted from the regenerated definition: **nine** positive pass-rule clauses and **three** prohibitions (§16),
**seven** adversarial fault-test groups (§4), **five** required artifacts (§15), and six ownership-matrix rows.

### 1.2 What the range contains since run 3

**F4** first-parent merges since run 3's range `d3ee2f25` (`range/merges-in-range.log`), changing **F5** under
`src/`. §6 judges each.

### 1.3 How the evidence was made

- **Builds** go only through `upstroke-build`, on private target bases this session made
  (`/mnt/ramtarget/iso-g5_run4` for the worktree, `/home/ubuntu/disktarget/iso-g5r4-{probe,mut,aux,q5}` for the
  scratch clones). `CARGO_TARGET_DIR` was never set and `cargo test` never ran bare. Repair round 1's executions used
  `/home/ubuntu/disktarget/iso-repair-g5r4` for its worktree and `/home/ubuntu/disktarget/iso-repair314-scratch` for its
  scratch clone of the range (`repair1/`), the same way; repair round 5's third-host execution used
  `/home/ubuntu/disktarget/iso-repair5-q5` for its own scratch clone `~/repair5-work/q5-tree` (`repair5/probes/`).
- **No repair round wrote a line under `src/`. Where each historical execution ran is read from its own receipt (**F69**),
  and the executions divide into two kinds of tree.** In a worktree of this pull request — every one, up to `4611b204`, at a head whose `src/`
  is the range's byte for byte: the cumulative suite's cleared-box and pre-hold runs, the nine group invocations, the two
  ST-07 merge checks and the Linux samplers in the run's own worktree `/srv/worktrees/g5-run4` at the range, where run 4's
  reaper head control and two of its gate legs also ran at `e44385b`; repair round 1's six committed witnesses in the repair
  worktree `/srv/worktrees/repair-g5r4` at `d1e48bbb` (`repair1/witnesses-in-worktree.log`, §8.5), with round 1's second
  reaper control and the gate legs of rounds 1, 2 and 4 at that worktree's later heads; and round 5's seven gate legs and
  the controls of their reds in `/srv/worktrees/repair-g5r4-r5` at `4611b204`; and, at the heads whose `src/` is the reviewed
  master's, round 5's final-head attempt there at `5744b0aa` and round 6's two attempts and its control in
  `/srv/worktrees/repair-g5r4-r6` at its first head `06a2b8a1` (**F51**, **F71**), and round 6's one attempt at its final head
  `00a16a64` in the same worktree (**F72**; after **F69**'s cutoff, round 6's second, so outside its population), and round 7's
  two attempts and its control in `/srv/worktrees/repair-g5r4-r7` at its first head `50c0410b` (**F75**; outside **F69**'s
  population too). Repair round 5's text here denied that any
  had run in a worktree, and the fifth review caught it. In a scratch clone of the range, never a
  worktree: everything instrumented or mutated — the clause-2 probe, the projection and census probes, the mutation
  campaign and the Q5 route witness (`~/g5r4-work/{probe-tree,aux-tree,mut-tree,q5-tree}`, each with its own base), the
  range control of the reaper red (in `aux-tree`), repair rounds 1 and 2's probes and mutation controls (git-archive
  trees of the range), and repair round 5's third-host execution, in a scratch clone of its own, cloned and checked out at
  the range with a clean status, with both touched files backed up, written back from the range's blobs afterwards and
  proved equal to them (`repair5/probes/restore.log`). The two export suite runs and the guest's suite and export logs name
  the range as their head with a clean status and no tree; the guest's tree is its own, `C:\upstroke-g5-run4` (below).
  After repair round 5's merge of the reviewed master (§15.2) the worktree's `src/` is master's and not the range's, so no
  historical execution has run in a worktree since, and every historical figure and checker reads the exact checkout of
  the range at `~/repair5-work/range-tree` instead (**F66**; `figures/figures.py`, `audit/grade-check.py`,
  `report/selfcheck.py`, `mutations/summarize.py`; the sweep for worktree reads is
  `repair5/integration/worktree-read-sweep.txt`). What round 5 proved against that checkout before its provenance commit
  is what `repair5/integration/range-tree.log` records and no more: the audit, the figures and the carve-out derivation
  regenerated byte-identical, the self-check exited 1 — its one failure the report's own path, absent at the range by
  construction, reproduced and identified in `repair6/selfcheck-reproduction.log` and exempted by the checker since — and
  the mutation summary was not invoked; round 5's text called all five byte-identical, and the fifth review caught that
  too. The complete regeneration against that checkout — the mutation summary, the audit, the figures and the carve-out
  each byte-identical to the evidence directory's file, and the assembled report byte-identical to the worktree file this
  round commits, with the self-check exiting 0 under the exemption stated above — is repair round 6's receipt,
  `repair6/regeneration.log`, for the report round 6 committed, and repair round 7's, `repair7/regeneration-first-head-50c0410.log`
  for its first commit and `repair7/regeneration.log` for this one, each made in a scratch copy of the evidence directory so that
  no listed file is written; the assembled report's identity with the committed blob is the post-commit receipt
  `repair7/post-commit-regeneration-<head>.log` (§15.2; round 6's two, `repair6/post-commit-regeneration-06a2b8a.log` and
  `repair6/post-commit-regeneration-00a16a6.log`, are frozen, and round 7's at its first head,
  `repair7/post-commit-regeneration-50c0410.log`, records the assembled report differing from that blob — **F73**'s derivation
  then read every per-leg directory on disk, that head's own two included — and is kept as the failed step it is, corrected by
  this commit), and round 5's
  own later regeneration, outside the evidence
  directory, is `~/orch-pr10/repair-314-evidence/round5/regeneration.log`. Every mutation was
  backed up and restored with `cp`, and each restore was checked against the range's blob and a clean tree (§5).
- **The Windows guest** ran from its own tree `C:\upstroke-g5-run4`, cloned from a git bundle of the range whose
  sha256 was verified on both sides and checked out at the range with a clean status, with its own target directory
  and `CARGO_INCREMENTAL=0`. Its free space was about a gigabyte before the build, so `CARGO_PROFILE_TEST_DEBUG=0`
  and `CARGO_PROFILE_DEV_DEBUG=0` were set as well. That drops debug information only — `debug_assertions` is
  untouched — and it is recorded because it is a difference from CI's own winguest leg, whose counts it nevertheless
  matches exactly (**F12**, **F15**).
- **The clause-2 probe** (§8.3) is a temporary instrumentation of the scratch clone `probe-tree`
  (`audit/probe/instrument.py`, `audit/probe/g5trace.rs`). It records every production hook consultation and its
  answer, entry into each recovery component, and every `TopologyFold::replay` with its `#[track_caller]` call site
  and digests. Every replacement in the patcher asserts its exact occurrence count and **every assertion held at
  this range**, which is itself a fact about the range: the production hook-consultation sites and the recovery
  components' signatures did not move. Each of **F17**'s tests ran in its own process.
- **A load constraint from the orchestrator interrupted the run at 14:47Z** and released it at 15:00:53Z: a
  neighbouring session was running a deliberate two-concurrent-suite load reproduction. `logs/load-timeline.md`
  records what had run by then and what was discarded. **Nothing measured under that load is used to grade
  anything**: the first probe run was discarded and re-run, and the guest suite was stopped mid-run and re-run from
  the start. The one pre-hold result kept is the Linux suite (**F10**), which started sixteen seconds after a
  measured `load average: 0.27` and finished before the neighbour began; it is reported only as a control beside
  the cleared-box run this report cites.

### 1.4 What production reaches at this range

Re-derived here, not carried (`range/production-reach.log`):

- `src/topology/schema.rs` pins `TOPOLOGY_ACTIVATION` to `Inactive` behind a compile-time assertion, and
  `src/engine/topology.rs` carries `#![cfg_attr(not(test), allow(dead_code))]`.
- `create_run` and `run_recovery_order` have no caller outside tests; `startup_census`'s only non-test caller is
  inside `engine::topology` itself.
- **The attempt-scope recoveries are unreachable from production.** `AttemptContext::settle_interrupted`'s only
  non-test caller is `attempt::cancel_in_flight`, which has none; `candidate::complete_promotion`'s is
  `candidate::promote`, which has none. `run_recovery_order` settles interrupted attempts through its own
  `recover::settle_interrupted` (`recover.rs:1019`). **This is what decided eleven of run 3's twenty
  not-established coordinates — the ten attempt-internal coordinates 23–26 and 48–53, and `Ref.CreateCandidates`/before,
  35 — and what #305 repaired** (§8.4).

So every recovery this report credits ran through the test suite's calls of the engine's own entry points. The
process funnel, its reaper and its kill paths are reachable in production through `HostRunner`, which the legacy
engine uses.

## 2. The cumulative suite, the gates and CI, at the range

### 2.1 The suite on Linux

**F9** is the run this report cites (`suite/linux-suite-clear.log`), taken on the cleared box. **Four full Linux
runs at this range agree exactly on the lib target** — the cited run, the pre-hold control (**F10**) and the two
export runs (**F11**) — and so does CI's own ubuntu leg (**F15**).
**No red was seen on Linux at this range in any of the four**, so no flake was attributed and no merge-base
control was needed — and that is a claim this run **checked by search rather than asserted** (**F50**,
`suite/no-red-check.sh`): every whole-binary result line of all five suite logs, every line matching `FAILED` and
every line matching `panicked at` in each of the five, and each flake the run brief lists searched over all five logs by the
fingerprint the brief gives it — the assertion text or the errno where the finding has one, and the test's name where that is
the only fingerprint the brief gives; the script labels each, and **F50** counts each kind. Run 4's script searched the
fingerprints over the four Linux logs only and searched the guest for `FAILED` and one errno, and run 4's text said every
fingerprint was an assertion text or errno; repair round 1 widened the script to all five logs, labelled the kinds and re-ran
it, with the same result. Each test-name fingerprint appears only as the name of a test that passed, and none appears as a
failure. Two strings a reader grepping for trouble will find, and what they are: the
only `FAILED` anywhere is an ignored test's own reason line (`spawned as a subprocess by the T-FAILED kill tests`),
and the only `panicked at` on Linux is the deliberate panic of the helper subprocess
`agent::proc::termination::tests::sigchld_target_setup_failure_helper`, whose parent checks it.

The one place a listed flake fired at all was the probe's 2,736-process run **under the neighbour's load**, where
two `real_docker_*` tests failed; they did not recur on the cleared box (**F17**, §8.3). The other reds this run and its
repair rounds saw, at the heads before this report's own, are in gate logs rather than suite logs: eleven attempts' test
legs, every red attempt **F51** lists — a population that closes at round 6's first head; round 6's one attempt at its final
head, after that cutoff, is green (**F72**); round 7's two at its first head `50c0410b` are the first red on the test leg on two
fingerprints — an observed production cleanup-ownership refusal and an observed count-budget Docker timeout, each test passing
alone in its control — and the second green (**F75**); and the attempts at this report's own head are recorded after its commit
(§15.2) and described in the pull request body. They are run 4's attempt 3 of the ten
gates and repair round 1's first two attempts at its own head, one test each time and the same test all three times
(**F52**, **F53**); repair round 5's seven attempts at its pre-merge head `4611b204`, whose reds are of four kinds — the
descriptor-hold test, that same reaper assertion, the container-launch-outages refusal, and in attempt 1 alone the two
terminal-signal tests that round's own asynchronous launch caused (**F67**, **F68**); and repair round 6's attempt 1 at its
first head `06a2b8a1`, on the reviewed master's `src/`, red on that container-launch-outages refusal — production's
cleanup-ownership refusal, a listed fingerprint, its test passing alone in a control that establishes neither the cause of
the full-suite failure nor that it is benign (**F71**) — and §15.2 fingerprints and controls every one. Repair round 1 wrote of a single red here and counted one in §16; the finalised logs then held three,
and both lenses caught it; repair round 5 left the three standing here after its own seven, and the fifth review caught
that.


### 2.2 The Windows guest

**F12** (`suite/guest-suite.log`), on the guest's own tree at the range, and **F13** for its export run. Both are
exit 0 with no red, and both match CI's `test (winguest)` leg at the same sha (**F15**).

### 2.3 CI at the range

**F14**, taking the newest run per check name and judging by the **run's** conclusion, never a job's. Every job of
both `CI` runs at the sha is completed/success (**F16**), the required `upstroke-ci` among them. Per-platform suite
counts from CI's own logs are **F15**.

### 2.4 The ten gates

§15, at this report's own head.

## 3. The tests this gate runs by name

The range's test list is `groups/test-list-9bb177e.txt`. The group filters are `groups/group-tests.txt`: run 3's
lists, carried, **plus the tests this range added**, appended to the groups they belong to — seventeen kills to
group 1, eight torn-registration and retained-generation recoveries to group 3, and seven finalization and
declared-pin tests to group 5. Every filter was checked against this range's own list (**F38**). The nine
invocations, each ending in a captured `cargo exit`, are **F37** (`groups/run-groups.sh`, `groups/logs/`).

## 4. The seven adversarial fault-test groups

Counted from the definition: **seven** (`spec/G5-DEFINITION.regen`, `adversarial_fault_tests`). Every group ran by
name as a `cargo test` invocation with its status captured (§3, **F37**). Where a group's claim is that something is
*detected*, the mutation that removes it is §5.

| # | The definition's claim (abridged) | What ran, and how it was judged | Grade |
|---|---|---|---|
| 1 | the **exhaustive** kill matrix over every Topology/Shared site × order × resume, incl. kills during recovery, run-end closure and terminal finalization, Event append points (kill and error return), OpenLog's SyncPrefix (kill and error return), and kills inside Git commands (sampled; every observed residue classified and recovered) | **Ran:** group 1's invocation, now carrying the seventeen kills this range added; #291's two finalization matrices and #311's two declared-pin tests in group 5's; the create suite's kills at the thirteen publication prefixes in group 9's; the five samplers on both hosts (§10). **Judged per coordinate** against the generated inventory by §8's audit, whose probe records every fault a test injected through a production funnel's hook. **Recovery and closure:** `kill_during_recovery_repeats_recovery` and `kill_inside_closure_recovers` spawn a child the parent checks died. **Finalization:** every registry coordinate whose fault row is T-FINALIZE is established by a real kill. **Event points and the barrier:** rows 91–121 all established. **Git-internal kills:** sampled, §10 | **asserted** as the definition words it; **established** under ST-07's construction standard. The definition asks for an *exhaustive kill* matrix over every site, order and resume. What §8 grades is ST-07's standard, which admits four constructions — a real kill, an injected error, a plant, or a fault or plant at an adjacent coordinate with nothing durable between — and **F19** counts each: a real kill stands at fewer than half of the coordinates, and at none of rows 9, 10, 88, 89, 138 and 139, whose credits are plants and injected errors (§8.5). Under the standard, every coordinate of the generated inventory has a constructed prefix, a production recovery performing the tabled action and a replay-twice or vacuous credit (**F19**; **F20** empty), and the two families run 3 failed it on are repaired (§8.4); as a literal kill at every cell the matrix is not established, and the Q5 amendment did not change that wording. Run 4 and repair round 1 graded this row established under the standard alone, and the verify lens of repair round 1 caught the substitution. Clause 2 of the pass rule asks for executed evidence per site, order and point, not for a kill, and keeps its grade (§16) |
| 2 | the two-crash proof, positive and negative | `unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing` and `barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` (group 2). The bytes on disk before and after each loss are **F33** and **F34**. Two mutations kill them (§5) | **established** (§9) |
| 3 | two coordinators refused by locks; resume with edited config warns; deleted worktrees and pruned non-authoritative refs recovered; Retained generations closed before `run_resumed`; snapshot, container, husk (every prefix, the ownership proof, the commit record, the creator-error shapes) and stale-marker residue reclaimed, repaired, or retained and reported | Group 3's invocation, now carrying the eight torn-registration and retained-generation recoveries this range added: the lock refusals, the config-drift warning, the husk family, the container census family, the stale-marker repairs and the container-state witness. Per coordinate, §8 | **asserted**, for one remaining reason. The locks, the drift warning, the recovered worktrees and refs, the Retained closure, the stale-marker repair, the creator-error shapes, the container states — no longer near-exact (§8.4) — and the husk at every prefix, rows 87 to 90 all credited and 88 and 89 by §8.5, are **established** for the named cases. Two torn-registration shapes still need an operator (§12), and "deleted worktrees ... recovered" is not established in them |
| 4 | ST-17 run-end closure per class and outcome, every derived-outcome refusal the definition lists, and the pre-`budget_exceeded` counterexample | Group 4's invocation; the census's outcome equality in both directions (§7). Assertion 5's named instances exist in the explored set by this run's own probe (**F31**); the `st17-*` recipes kill their tests (§5) | **established** |
| 5 | ST-18: kills after `run_finished`, after the report write and **between every cleanup effect** converge; resume of Complete/Halted finalizes then refuses; the Halted report lists retained candidates refs; answer files and `.partial` residue untouched and never ingested; a late answer after finalization writes an inert file | Group 5's invocation, now carrying #311's seven finalization and declared-pin tests: the two kill matrices, whose cell counts the tree itself asserts (**F49**), #289's rundir witnesses, the late-answer and answer-file tests, the report-barrier family | **asserted.** Finalize-then-refuse, idempotence across the third resume, the Halted listing, the untouched answer files, the inert late answer, and a real kill at every registry coordinate of T-FINALIZE are **established**. "Between every cleanup effect" is not, in its universal form: the matrices' cells come from a hand-listed `finalization_effects` table, and `kill_after_run_finished_before_report` and `kill_after_report_before_each_cleanup_step` arm `Injection::Error`, so they are error-return witnesses rather than kills. What #311 changed here is the exactness, not the universality: the matrices now assert they plant no damage, and that the fresh resume deletes no candidate-prepared pin but a declared one |
| 6 | Halted retains and lists candidates refs, Complete prunes all and leaves objects to Git; a BudgetExceeded resume with a raised budget continues queued candidates, parked repairs with sources present, and woken deferred items; answers left on disk during a budget-stopped epoch are ingested only by the resume and only for still-open questions | Group 6's invocation | **established** |
| 7 | the seam tests in range: ST-07 full; ST-09 incl. R21, the owner and commit records, and R27 with the fsck inventory; ST-10, ST-11, ST-12; ST-13's sequential subset incl. the fast no-object proof; ST-14 incl. totality, live-versus-replay classification equality and `merge_prepared` relations; ST-15, ST-19, ST-20 | Groups 7 and 9's invocations, plus §7 (ST-14), §8 (ST-07) and §12 (ST-09). The seam statements are quoted verbatim in `spec/seam-tests-in-range.txt` | **ST-07 established** (§8.5, **F19**). **ST-14 established** (§7). ST-09, ST-10, ST-11, ST-12, ST-15, ST-19 and ST-20 are **established** for the named cases. ST-13's provisional-reservation lifecycle beyond the fast no-object proof is G3's evidence and is not claimed here |

## 5. Detection, established by mutation

Six recipe sets ran in the scratch clone `mut-tree` at the range (`mutations/run-campaign.sh` for the five carried
sets, then the new one). This run's own set passed a `CHECK=1` pass first, in which every anchor was applied and nothing
was run (`mutations/check-pass-g5run4.log`); no such log was kept for the five carried sets, run 4's sentence that one
exists for each notwithstanding, and the record lens caught it. That **every anchor of all 161 carried recipes applied at
this range** — itself a fact about the range — is established instead by the chain logs, in which no recipe's patch was
refused and no restore failed, which `mutations/summarize.py` checks and refuses otherwise. Each recipe ran the same way: back up the file with `cp`; apply the patch, whose
anchor must match exactly; run the named test through the wrapper; restore with `cp`; and refuse unless the restored
file equals the range's blob and the tree is clean. **An exit code is not a kill until the log is read**: repair round 1's
control for rows 88 and 89 exited as a kill because the test harness had rejected its command line before running anything
(§8.5), so repair round 2 read the saved test logs its audit selected — the recipe logs, round 1's probes, the group
invocations, the suite runs, the gate legs then present and the controls — for an invocation rejected before running any
test, a filter that matched no test, or a claimed outcome the log does not show, and described that as a reading of the
whole directory. It was not: its scanner never selected the two ST-07 merge-check logs or the four receipts round 2 itself
produced (`repair2/probes/`), and the gate legs at round 2's two heads were written after it (§15.2); all three lenses of the
third review caught it. Repair round 4 re-ran the audit, with round 2's per-log predicates unchanged, over a population it
enumerates and closes rather than assumes: every `.log` in the evidence directory that holds a test-harness run, a per-test
outcome line or a harness rejection at the audit's cutoff, listed by path in `repair4/log-audit.json`, with the union of the
audit's families asserted equal to that enumeration (**F57**, `repair4/log-audit.py`). Two logs of the shape, both already
named in this report: that control, and the one recipe below that matched no test and was declared VOID; every other exit
`101` in that population shows its named test failing and every other exit `0` shows it passing. The gate legs at this
report's own head are written after the cutoff, so they are outside the population by construction and this report claims
nothing of them; the finalisation step re-runs the audit over the directory as it then stands and records the result in a
file of its own that this report, committed before it, does not quote (`repair4/log-audit-post-commit-<head>.log`, §15.2).
Repair round 5 added test-bearing receipts of its own — the third host's two effects-suite logs (`repair5/probes/`, §14) —
and re-ran the same audit over the population as it stands at its own cutoff (**F64**, `repair5/log-audit.py`): the same two
affected logs and no new one; round 4's audit and its post-commit file are frozen. Written after that cutoff were the seven gate
legs at round 5's pre-merge head and the controls for their reds (`gates/eight-logs-4611b20-final-attempt<N>-failed/`,
`gates/repair5-control/`, **F67**) and the leg at round 5's final head `5744b0aa`; round 5's post-commit re-run at that head,
`repair5/log-audit-post-commit-5744b0a.log`, listed them in families of their own, and repair round 6 re-ran the audit over
the population as it stood at each of its two cutoffs (**F70**, `repair6/log-audit.py`): at the first, before its first head
existed, the same population as round 5's post-commit one; at the second, before this commit, the two gate legs at its first
head `06a2b8a1` and their two control logs added — the same two affected logs and no new one at either, both frozen
(`repair6/log-audit-first-cutoff.log`, `repair6/log-audit.log`). Written after round 6's second cutoff was the gate leg at its
final head `00a16a64`, which round 6's post-commit re-run at that head, `repair6/log-audit-post-commit-00a16a6.log`, listed,
and repair round 7 re-ran the audit over the population as it stood at each of its two cutoffs (**F74**, `repair7/log-audit.py`):
at the first, before its first commit, round 6's post-commit population, that one leg added over **F70**; at the second, before
this commit, the two gate legs at its first head `50c0410b` and their control receipt added — the same two affected logs and
no new one at either, both frozen (`repair7/log-audit-first-cutoff.log`, `repair7/log-audit.log`); round 7 ran no test before
its first cutoff. The gate legs at this report's own head are
written after every pre-commit cutoff and outside them by construction; round 7's post-commit re-run at this head,
`repair7/log-audit-post-commit-<head>.log`, lists them, is written at finalisation and is quoted by nothing.

**`g5run4` is this run's own set**, written against what #305 and #311 added, and it is the set that matters for
this report's detection claims:

| recipe | what it removes | named test | outcome |
|---|---|---|---|
| `g5r4-census-keeps-the-stale-marker` | `rundir::remove_marker`'s two removals become a no-op | `a_clean_staging_worktree_left_at_the_integration_head_is_reclaimed_and_the_candidate_integrates` | **killed** — so the first-resume helper's "the creator's marker is gone" is real, which is what makes the P6-hybrid repair a repair |
| `g5r4-settle-interrupted-appends-nothing` | recovery step (d) emits no `attempt_interrupted` | `a_kill_after_the_snapshot_add_is_settled_interrupted_and_the_snapshot_reclaimed` | **killed** — the attempt family's `(ii)` rests on this |
| `g5r4-snapshot-residue-not-reclaimed` | `reclaim_snapshot_residue` skips every snapshot slot | the same | **killed** |
| `g5r4-closed-generation-not-scrubbed` | `reclaim_closed_generations` scrubs nothing | `a_kill_after_the_candidate_stage_is_settled_interrupted_and_the_scrub_releases_the_staged_edit` | **killed** |
| `g5r4-scrub-keeps-the-intent` | `dispatch::scrub` removes the worktree but not the intent | `a_kill_before_the_candidate_write_tree_is_settled_interrupted_and_the_scrub_releases_the_staged_edit` | **killed** |
| `g5r4-reclaim-after-creation-keeps-the-pin` | `reclaim_after_creation` stops deleting the candidate-prepared pin | `a_resume_over_a_stale_queued_candidate_with_nothing_staged_takes_the_staging_path_and_publishes_the_proposal` | **passed** — mis-aimed, and the reason is in the table below |
| `g5r4-reclaim-after-creation-keeps-the-pin-vs-promotion-kill` | the same mutation | `a_kill_before_the_candidate_pin_is_deleted_is_finished_by_the_next_resume_which_deletes_it_once` (row 41) | **the named test fails (exit 101), and not at the recovery** (**F61**): the deletion the mutation removes is the funnel call the witness's kill child is armed to die inside, so with it gone the child runs through to its own panic instead of aborting at `before-candidate-pin-delete`, and the parent refuses the missing kill in `kill_the_candidate_sequence` (`src/engine/topology/recover/tests.rs:14551`) before the next resume or any pin assertion runs. A real failure, and a detection of the removed call — but no recipe of this campaign reaches the assertion that the next resume deletes the pin exactly once; that rests on the unmutated witnesses of rows 41 and 42 (§8.4). Run 4 and repair rounds 1 and 2 wrote this twin down as the correct aim and a kill, and the record lens of the third review caught it |

The five carried sets re-ran at this range and **reproduce run 3's outcome recipe for recipe**, which is the
cross-check worth having on 161 recipes whose subjects mostly did not move.

| set | recipes | run | killed (`rc=101`, the named test FAILED) | survived (`rc=0`, the named test ok) | VOID (`rc=0`, no test ran) | log |
|---|---|---|---|---|---|---|
| `g5run4` | the seven recipes this run wrote against #305's and #311's new properties | 7 | 6 | 1 | 0 | `mutations/chain-g5run4.log` |
| `g5run3` | run 3's seven recipes against #302 | 7 | 7 | 0 | 0 | `mutations/chain-g5run3.log` |
| `g5run2` | run 2's fix-judging recipes (#289, #291, #292, #293) | 20 | 15 | 5 | 0 | `mutations/chain-g5run2.log` |
| `g5` | run 1's G5 recipes | 45 | 34 | 10 | 1 | `mutations/chain-g5.log` |
| `pr10` | PR10's recipes | 80 | 78 | 2 | 0 | `mutations/chain-pr10.log` |
| `g5r2r5` | run 2's review-round-5 recipes (barrier refusals, projection) | 9 | 8 | 1 | 0 | `mutations/chain-g5r2r5.log` |

Across the six sets: 168 recipes: 148 killed, 19 survived, 1 VOID (an exit `0` whose invocation ran no test is not a survivor; repair round 4 separated it, after the record lens found it counted as one).

Every recipe whose named test passed, or that ran no test, and why:

| set | recipe | why |
|---|---|---|
| `g5run4` | `g5r4-reclaim-after-creation-keeps-the-pin` | mis-aimed: that witness's first resume completes no promotion, so `reclaim_after_creation` never runs in it and the pins its plant leaves are pruned by test code. The re-aimed `-vs-promotion-kill` recipe fails its named test (exit 101), but not at the recovery: removing the deletion removes the hook the kill child is armed at, so the child runs through to its own panic instead of dying at `before-candidate-pin-delete`, and the parent refuses the missing kill (`recover/tests.rs:14551`) before the next resume or any pin assertion runs (§5, F61) |
| `g5run2` | `fix289-record-publish-swallowed-vs-refused-barrier-test` | mis-aimed: that test exercises the public barrier's refusal, not the private record's publish; the correctly aimed `-vs-new-test` kills |
| `g5run2` | `fix289-record-removed-before-barrier` | the power-loss witness simulates its loss after the moved barrier, so it cannot see the interval the mutation opens; the earlier loss point is bounded by the refused-barrier witness, which kills the same mutation (`fix289-record-removed-before-barrier-vs-refused-barrier`) |
| `g5run2` | `fix291-complete-keeps-staging-leftovers` | the same, at Complete |
| `g5run2` | `fix291-halted-keeps-staging-leftovers` | the finalization matrices plant no staging-intent leftover, so the step removes nothing in those cells; `fix291-staging-leftovers-vs-dedicated-test` kills the same mutation against the error-return witness |
| `g5run2` | `fix291-matrix-keeps-staging-leftovers` | the same, against the committed error-return matrix |
| `g5` | `g5-answered-question-stays-open-vs-g5-witness` | **VOID**: its filter names run 1's uncommitted witness and matches no test (`0 passed; 0 failed`); the committed twin kills |
| `g5` | `g5-barrier-length-mismatch-ignored` | the same; its `-log` twin kills |
| `g5` | `g5-barrier-reread-mismatch-ignored` | the named test is a pure-data test of the container census's barrier abstraction and never calls `establish_stable_prefix`; the re-aimed `-log` recipe kills the event log's own test (run 2's explanation, re-observed) |
| `g5` | `g5-barrier-torn-tail-reappearance-ignored` | the same; its `-log` twin kills |
| `g5` | `g5-census-question-not-offered-dimension` | the `questions` bound is reached by another route (a rejection's repair admission opens a question); the `-arms` twin kills |
| `g5` | `g5-fast-publication-other-sha-accepted` | `MergePrepared::self_consistency()` refuses that shape in the wire type before the fold's check runs, so the mutation is behaviour-preserving; the head relation's recipe kills |
| `g5` | `g5-id-unread-point-skipped` | the named test is a classifier test that spawns no child and consults no hook; the re-aimed `-kill` and `-record` recipes kill |
| `g5` | `g5-residue-frozen-n-changed` | the named test constructs its own entry against a site's declaration; the re-aimed `-pin` recipe kills the declarations' pin |
| `g5` | `g5-retained-idle-not-closed-on-resume-settle` | the settle-level test never runs recovery step (e), where the mutation is; the loop-level recipe kills |
| `g5` | `r11-staging-record-publish-swallowed` | expected to pass (declared so in the recipe): its original aim does not exercise the private record's publish; #289's added test kills the same mutation (`fix289-record-publish-swallowed-vs-new-test`) |
| `g5` | `r11-staging-record-stage-swallowed` | expected to pass, as above; `fix289-record-stage-swallowed-vs-new-test` kills it |
| `pr10` | `sampler-kills-leader-only` | declared `pass` in the recipe, which is PR10's expected outcome; re-observed. This gate did not re-derive why the remaining-sites sampler is indifferent to it |
| `pr10` | `torn-registration-both-without-gitdir` | declared `pass` in the recipe: the lens's reproduction, which failed before PR10's fix and passes after it; re-observed |
| `g5r2r5` | `barrier-first-line-digest-missing-ignored--commit_record_present_without_committed_` | the named test plants its P5b husk without a log (it asserts the log is absent), so the census never opens a log and the barrier's first-line check is never reached; the same mutation is killed against the event log's own test and against the torn-first-line kill witness (run 2 round 5 observed the same) |

All 168 `.backup` files equal the range's blob of the path their `.diff` names, and no chain log records a refused patch or restore (`mutations/summarize.py`).

## 6. Every fix pull request in the range, judged rather than accepted

Each merge of **F4** (`range/merges-in-range.log`), and what this run did about it rather than accepted.

| merge | PR | what it is | judged |
|---|---|---|---|
| `ad4ff216` | #303 | **run 3's own report** — the operative record this run re-judges | Read as the record. Its verdict is not carried: every clause is re-derived here, and §8.3 says exactly which readings were re-read and which were re-verified mechanically |
| `bae7d5ec` | #305 | **clause 2**: witnesses for seventeen of run 3's twenty not-established coordinates, by its own count and by `git log` over the range (`repair2/witness-attribution.txt` traces every credited witness of the twenty to the merge that introduced it: seventeen to #305, row 80's to #311, rows 88 and 89's to before the range) — run 4 credited fifteen of the seventeen and missed the retained-generation witness for rows 9 and 10 (§8.5). Run 4 and repair round 1 overstated both counts here, and they are corrected | **Judged by execution.** The probe records the production recovery `recover::run_recovery_order` in every one of the seventeen witnesses: after the fault's firing in the fifteen fault-based ones (`audit/candidates-notok.txt`, `recovery=PROD`), and in the rows 9 and 10 witness after its plant, which fires no fault at all — the same file records no firing at either coordinate, and `audit/probe/coordinate-firings.json` records the witness with no fault, no death, the production recovery entered and three replay pairs. That recovery is what run 3's `(ii) no` turned on (§1.4, §8.4); run 4's wording had a firing before the recovery at every one of them, which is false for rows 9 and 10, and all three lenses of repair round 1 caught it. The shared driver's three obligations were read line by line, and three of this run's own mutations remove what it asserts — the settlement, the snapshot reclaim and the scrub's intent removal — and each of the three is killed at that driver's own assertion (§5, its panic sites in the recipe logs) |
| `3cfe883c` | #306 | the facade allowance, 62 classifications, five engine denials, the fences | Held by the effects census, which passes (group 8, **F37**). It closed `PR5-R1-CFG-TEST-SHRINKS-THE-DOMAIN`, one of the two findings run 3 gave clause 8 (§13) |
| `db523cd3` | #307 | findings: the scratch-fixture leak's third measurement | Docs only. It **reclassified `PR7-SCRATCH-FIXTURE-LEAK` P2 -> P1**, which is a new P1 in range and is in §13's table |
| `003463d2` | #308 | closes `PR5-RD-002` — one of the two deferred P1s run 3 named as weakening clause 9 | **Verified closed at this range**: no finding file carries that id, and `dfab458b` is the commit that removed it (§13). What its repair leaves is filed as two P2s, and §12 names them rather than treating the shape as gone |
| `a738abcb` | #309 | closes `PR306-FACADE-INLINE-ESCAPE`; the facade holds no code | Held by `the_engine_facade_allows_no_governed_lint_and_refuses_both_escape_routes` and `a_declaring_module_holds_declarations_and_re_exports_and_nothing_else`, both in group 8's green invocation |
| `db94a8ba` | #283 | **an instrument**: rewrites `.github/scripts/test-pr-ready-audit.sh` | Outside the tree this gate certifies. It runs as gate ten at this report's head (§15), and it closed the P1 `PR273-R1-DECODER-GUARD-MATCHES-A-SPELLING` (§13) |
| `2b24a377` | #312 | the 54-file `deny`->`forbid` fences; **narrows** `PR7-WRAPPERS-EMPTY-DOMAIN` | **Judged, and found narrower than the carve-out Q5 now draws.** The flip is real and `every_fence_of_a_governed_lint_forbids_wherever_forbid_would_compile` holds it. But a classified module can be in neither the fenced set nor the allowance-bearing set, and three are: two by §14's executed reproduction in run 4, filed as `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT`, and a third, `src/agent/bin.rs`, that run 4 had counted inside the carve-out by its allowlist row and the fourth review found outside it by the file's own attributes, executed again in repair round 5 (**F63**). This is what makes clause 8 asserted rather than established |
| `9bb177ea` | #311 | **clause 2**'s remainder: the coordinates #305 did not reach, and the near-exact family | **Judged by measurement, not by its claims.** Its P6-hybrid repair is confirmed per recovery from this run's own traces (**F25**): in **every** one of run 3's near-exact rows the credited recovery enters no `RunDir.RemoveMarker`. Its declared-damage claim — that no credited witness repairs a planted candidate pin — is confirmed the same way (**F24**, §8.4), and the finding it files for the remainder, `PR311-SHARED-PLANTS-WRITE-A-CANDIDATE-PIN-NO-CRASH-LEAVES`, is `deferred`, so clause 9 is unaffected. Of this run's recipes against its exactness helpers (**F61**, §5), the marker-removal mutation is killed at the helper's own assertion (*the creator's marker is gone*, `assert_the_creation_prefix_is_complete`); the pin-deletion mutation is not: its first aim survives, and its re-aim fails the named test at the harness's kill check before any recovery runs, so the exactness of the pin's deletion rests on the unmutated witnesses of rows 41 and 42, not on a kill. Run 4 and both repair rounds wrote all three down as kills, and the record lens caught it |

**Every one of the four coordinates run 4 held back was addressed in this range, and run 4's search did not find it**:
#305 added the retained-generation witness for rows 9 and 10 (its doc comment names them), and rows 88 and 89 are
discharged by the released-lock P2 witnesses #292 added for rows 69 and 129 — in the tree since before run 3's range, and
missed by run 3 as well — whose construction is byte-identical to the state the removal coordinates leave (§8.5). Nothing in
this range stands between it and clause 2.

## 7. Clause 1 — the bounded census family (required artifact 2)

**The artifact** is `census-export2/bounded-census-artifact.md`, generated by
`census-export2/bounded-census-artifact.py` from the suite's own census summary. Every `file:line` in it was
resolved at this range by `audit/anchors.py` (the generator reports unresolved anchors and there are none). The
summary's hash is the same in both Linux export runs **and on the Windows guest** (**F28**).

Against the amended clause — *the prefix at its stated ceiling with truncation reported, every seeded per-dimension
census closed, every bound reached by equality over the union, totality asserted over the union and every
`plan_transition` arm covered*:

- **The members** are **F29**: the prefix explored to its ceiling and reporting its truncation, and three seeded
  members, each closed — not truncated, frontier exhausted under its own ceiling, no state at the trace ceiling.
- **Every bound over the union** is **F30**. The three plan shapes are the closing members' own `plan_shape`
  fields, and `src/topology/census.rs` asserts one closing member per shape of `PlanShape::ALL`, each member's
  shape equal to the dependencies its fold was started with. Defers are bounded at `max_defers - 1`, as the
  2026-09-15 amendment corrected them. **The limit, stated:** the union reaches the numeric bounds through the
  prefix; the seeded members reach only sequences, repairs and lineages at their bounds, so no claim is made that
  each shape reaches every numeric bound.
- **Totality and the arms** are asserted over every member's explored states, and the production dispatch's arms
  over the union.
- **Assertion 5's named instances are measured, not asserted.** They exist in the explored set by this run's own
  probe (**F31**, `probes/census-instances/probe-run.log`), re-executed at this range in a scratch clone whose
  `src/topology/census.rs` was restored and checked against `HEAD` afterwards. `src/topology/census.rs` is
  unchanged across the range, and the probe reproduces run 3's numbers exactly; that is a cross-check, not the
  evidence — the evidence is this run's execution.
- **What the findings do to it.** Four open findings bear on the census's strength and independence
  (`SWEEP-FOLD-OUTCOME-CENSUS-BACKOFF-MIRROR`, `SWEEP-FOLD-OUTCOME-ADMISSIBILITY-NOT-REFUSED`,
  `SWEEP-CHECK_INTEGRATION-002`, `SWEEP-CHECK_INTEGRATION-003`). None falsifies a clause-1 property and none is P1.

**Prohibitions.** No unbounded closure is claimed: closure is claimed only of the three seeded members, each under
its own ceiling and restricted generator. No effect-phase claim is made of the census.

**Grade: established.**

## 8. Clause 2 — the inventory, the bijection, and the per-coordinate audit (required artifact 3, first part)

### 8.1 The inventory and its lists

`inventory/site-enums-export.md` is the site enums export: every site, every registry entry with its order, residue,
tabled action and evidence kind, and how many tests on each host record it. Beside it are the range's copies of the
disallowed lists (`inventory/clippy.toml`), the funnel-module and legacy allowlists, the wrapper classification, the
registry and the residue declarations. The counts are **F27** and **F7**.

**The registry is byte-identical across the range** (**F6**): #305 and #311 added **tests**, not entries, so this
audit's subject is exactly run 3's and the row numbering carries (**F8**).

### 8.2 The bijection, executed on both hosts

ST-07's merge check ran over each observation export and holds every claim of the host's table to its named test's
own observation record: **F26**, **failures 0 on both hosts**. This establishes observed execution of every site and
phase on each host, and of every parent-executed point in every mode on the host that has it, with a registry entry
for every observed order. It does not establish ST-07's per-entry obligations; that is §8.3's work.

### 8.3 The per-coordinate audit on ST-07's three obligations

**The subject** is the registry's `executed` entries (**F7**), numbered in file order (`audit/registry-rows.tsv`). ST-07's words are
judged as three separate obligations, by the rules of `audit/grading/RULES-run3.md`: (i) the exact durable prefix
and residue constructed, every durable file included; (ii) the tabled action *performed* by a production recovery,
where a plan or a classification does not count; (iii) the durable log replayed twice and the replays asserted
equal.

**Executed first: the probe**, re-run on the cleared box (**F17**). Its eight nonzero exits are all source-text
census tests that the probe's own edits to `src/` break; none is a registry witness, and each passes in the clean
suite. For every row, `audit/probe/coordinate-firings.json` records each test in whose process tree a production
funnel answered a fault at that coordinate, whether the process died there, which recovery components ran
afterwards, and which pairs of replays over identical events test code made afterwards (**F18**).

**Then graded, and machine-checked.** `audit/grade-check.py` refuses unless every row appears once with the
registry's coordinate, every witness exists exactly once as a `#[test]`, every cite's anchor resolves to exactly one
line of its named function *at this range*, every `K` has a probe-recorded death at that coordinate in the witness,
every `E` an injected error, and every `(iii) yes` a probe-recorded replay pair — **with one stated exception**: the five
Windows-only rows (144–148, the `Process.Spawn` points the guest alone reaches) never ran on Linux, so the checker holds
their `K`/`E` to the Windows guest's own observation export (a `reached` record at the row's point and mode in the witness or
its kill child) and their replay-twice credit to the probe's pairs for the Unix witness that calls the same driver; row 145
has no such twin and its replay-twice credit is **read, not executed**. `audit/per-coordinate-audit.md` says per row which
check applied; run 4's text omitted the exception, and the record lens caught it. **It passes** (**F19**).

**What this run read afresh, and what it carried.** Saying this exactly matters, because the difference is the
difference between a reading and a re-verification:

- The checker was first run against **run 3's readings unchanged**. It refused **F22** — the range moved code
  between functions, so carried cites named functions that no longer hold them.
- Where the evidence had only moved, each failing cite was re-anchored to the place that now holds it, **after
  reading the new code**, through `audit/reanchor-map.json`, which records the replacement and why. The checker
  then proves each resolves.
- **F23** rows were **read from scratch** and re-graded, in `audit/regrade-*.json`: eighteen by run 4, because the range
  changed what establishes them (run 3's review override on row 40 is withdrawn there, with its reason), and four — rows 9,
  10, 88 and 89, **F54** — by repair round 1 (`audit/regrade-repair1.json`), after the review lenses found the committed
  witnesses run 4's search had excluded.
- Run 4 searched rows 9, 10, 88 and 89 over what it took to be the whole committed suite, and recorded the search per row
  (`audit/search-row9-10.txt`, `audit/search-row88-89.txt`). **Both searches were bounded too narrowly**, and §8.5 says how:
  the first listed only tests that consult `Worktree.Verify`, the second read only tests that inject a fault at a removal
  site; and run 4 carried run 3's plant-domain file rather than regenerating it from its own probe. Repair round 1
  regenerated that bound from run 4's probe (**F55**, `audit/grading/plant-domain-124-9bb177ea.json`): every test whose
  process made a test-code replay pair at this range, which by the rules bounds every witness of a `(iii) yes` row, and it
  holds the witness of rows 9 and 10.
- **Every other row's reading is run 3's, re-verified mechanically at this range** — witness present exactly once,
  every anchor resolving, and the probe recording what the row's construction claims and nothing more: a death for a
  `K`, an injected error for an `E`, no firing at the credited coordinate for a plant or an adjacent construction, and a
  replay pair wherever (iii) is `yes`, under the Windows-only exception stated above; **F19** counts each kind. A plant or
  adjacent credit asks nothing of the probe at its own coordinate, and its witness may record a fault elsewhere: an
  adjacent construction is a fault or plant at the neighbouring coordinate, and **F60** counts the carried `=` rows whose
  witness records exactly that neighbouring fault — the kill at the first line's synced point behind row 67's
  `RunDir.RemoveMarker` credit, the injected error at `Lock.AcquireRun` behind row 132's — and asserts that no `P` or `=`
  row's witness fires at the credited coordinate itself. Repair round 1 wrote this bullet as if every row had a death or
  an error and a replay pair, and the record lens caught it; repair round 2 wrote it as if neither construction's witness
  fired anywhere, which is false of an adjacent one, and the regression lens caught it. That is a re-verification, not a
  fresh reading, and this report does not present it as one. What a mechanical re-verification cannot see is a carried
  sentence written against a witness the range has since changed, and the fourth review's verify lens found two: rows 31
  and 32 still described their integration ref store as the RecordingRefs double, where the frozen driver resumes through
  `resume_with_real_refs` and asserts Git's own ref listing and the ref's reflog (§8.4). Repair round 5 swept the readings
  for that class (`repair5/audit-sweep.py`, **F65**): the same call-path citation — a (ii) cite into the Given-driven helper
  `resume_with`, whose ref seam is that double — was carried in fifteen more rows whose witnesses resume through `resume_as`,
  three prose references named line numbers the range had moved, and one carried sentence said an alternative witness replays
  nothing twice where the range had made it replay twice. Every one is refreshed in `audit/refresh-repair5.json`,
  applied over the readings with every grade, witness and construction asserted unchanged, and the checker re-run (**F19**);
  the readings that name a function the range changed or added were listed and read, and no other sentence was found
  stale.

**The result is F19**: every row established, and **no row near-exact**; **F20** is the list of rows not established, and
it is empty.

### 8.4 What #305 and #311 repaired, measured rather than read

Run 3's grades at its own range are **F21**. Both families it failed the clause on are repaired, and this run
measured each repair from its own probe rather than reading the pull requests' claims:

- **The ten attempt-internal coordinates** (23–26, 48–53) and `Ref.CreateCandidates`/before (35): run 3's
  witnesses recovered through `AttemptContext::settle_interrupted` and `candidate::complete_promotion`, which
  production never calls (§1.4). #305's witnesses drive `resume_as` -> `run_recovery_order`, and the probe records
  that production recovery after the firing at every one of them (`audit/candidates-notok.txt`). The shared driver
  asserts the whole durable prefix before the recovery, the settlement, the scrub, the snapshot reclaim through the
  snapshot's own funnels, the release of the staged objects to Git — and that the recovery performs none of the
  attempt's own sites.
- **The staging and pin coordinates** (16, 17, 40, 44) and the plan-written husk (80): new witnesses with real
  kills, and for row 80 a husk that carries the released `run.lock` the census reads — the exact file run 3 found
  missing.
- **Rows 41 and 42** moved out of the finalization matrices, whose planting had left a candidate-prepared pin as
  declared damage, into two kills inside the promotion itself.
- **The near-exact family.** Run 3's reason was the recover fixture's P6 hybrid: the creator's marker still
  published beside later events, and an execution root the credited resume re-created. #311 runs each witness's run
  **once first**, as an incarnation that then dies, so the marker is removed for real before the cell is planted.
  Measured per recovery from this run's traces (**F25**, `audit/exactness-per-recovery.txt`): the trace is cut at
  every `recover::run_recovery_order` and the three repairs are listed per recovery. **F25** is the result: in
  **every** one of run 3's near-exact rows the credited recovery enters no `RunDir.RemoveMarker`, and those not
  clean of all three create only the integration ref and the execution root, in each case over a prefix no resume
  of the run had reached, which makes those the prefix's own. Rows 31 and 32 were near-exact for an in-memory integration ref; #311 builds their
  prefix through the production funnels and reads the ref back from Git and from its reflog, and since repair round 5 the
  required audit's two rows say so too (§8.3).

**The planted candidate pin, the question the brief singles out.** `PR311-SHARED-PLANTS-WRITE-A-CANDIDATE-PIN-NO-CRASH-LEAVES`
(P3, deferred) declares that four planting helpers still write a `candidate-prepared` pin production never leaves,
and asserts that nothing credited is inexact for it today. **That assertion is measured here, not read.** The probe
records every `Ref.PinCandidatePrepared` and `Ref.DeleteCandidatePin` funnel consultation, so a deletion with no
matching creation is a planted pin being repaired. **F24**: of the tests that touch either funnel, six delete an
unmatched pin after the last fault of their trace, and **none of the six is a registry citation or a credited
witness of any row** — two of them are the tests #311 created expressly to host the declared pin, which nothing
cites. Where a surplus deletion happens in a credited witness at all, the timeline shows it in the setup resume
**before** that cell's fault (`audit/pin-timeline-credited.txt`, `audit/pin-balance.txt`). **No credited coordinate
is credited to those cells**, and that is an executed answer rather than the finding's word.

### 8.5 The grade

**Clause 2 is established at this range.** Observed execution and the registry's coverage of every observed order are
established on both hosts (§8.2), and ST-07's per-entry execution is established for every executed entry (**F19**; **F20**
is empty). Run 4 graded four coordinates not established; the verify and record lenses of PR #314 each found, separately, a
committed witness that discharges them, and repair round 1 adjudicated both by execution rather than by deference
(`repair1/`, **F54**):

- **Rows 9 and 10, `Worktree.Verify` before and after.** The witness is
  `a_retained_generation_left_by_a_dead_incarnation_is_closed_by_the_next_resume_and_its_worktree_reclaimed`, added by
  #305 for these rows and saying so in its doc comment. It resumes the run once first — the P6-hybrid repair of §8.4, so
  the marker is removed for real and the execution root and integration ref exist — and plants the dead incarnation's retry
  prefix behind it: the dispatch at the recorded base, the attempt, its settlement retained for that incarnation's epoch,
  the attempt's worktree standing at the base with the worker's edit staged in it, registered with Git and its intent
  present. That is the durable state at `Worktree.Verify`/before of `T-RETRY`, because a retry verifies before it appends
  anything and the site is read-only, so the after coordinate holds the same state. The tabled action is the packet's
  `T-RETAINED` disposition — *the retaining incarnation proceeds to T-RETRY; a fresh process closes it in recovery* — and
  the next incarnation's production resume (`resume_as` -> `run_recovery_order` -> `close_retained_idle`) performs it: the
  generation closed with reason `ResumeDiscardsRetainedSession` before `run_resumed`, its worktree and intent reclaimed
  through their own funnels, the staged edit released to Git, and nothing verified, every one asserted; then the log is
  replayed twice and the replays asserted equal, which the probe records. The registry's generic text for the row — the
  observation *repeated* — is an action no next incarnation can perform, since a retained session dies with the incarnation
  that retained it; the packet's fault row governs, which is the reading run 4 itself applied to the creation, husk and
  answer rows. **Why run 4 missed it:** its search listed only the tests whose process consults `Worktree.Verify`, and this
  witness asserts that the resume consults it *not at all*, which is the point. **Executed in repair round 1:** the witness
  passes unchanged in the repair worktree (`repair1/witnesses-in-worktree.log`), and with the production close disabled it
  fails at its close assertion (`repair1/probes/retained-close-disabled.log`). Grades: (i) `P`, (ii) yes, (iii) yes.
- **Rows 88 and 89, the two husk-removal coordinates.** The witness is
  `a_creation_stopped_before_its_private_half_leaves_a_released_run_lock_the_census_reclaims_public_only`, #292's
  witness for row 69 — in the tree since before run 3's range — whose construction is the P2 prefix on the real released lock: the marker published,
  `run.lock` and `cleanup.lock` taken through the production funnel and released, no private half. That is, path for path
  and byte for byte, the state the census's removal of a P3 husk's private half leaves — the public half of a P3 husk is
  exactly the marker and the two released lock files — and it carries the file run 4 faulted the previous witness for
  lacking. **Measured, not argued** (`repair1/probes/husk-prefix-equivalence.log`): an error injected at each of the two
  coordinates inside the production census, over a P3 husk built on the real released lock, leaves a tree whose every path
  and byte equals the witness's construction, with the private half absent and the lock file unheld. The production census
  over that state then performs the tabled action, asserted as `ReclaimedPublicOnly(TargetAbsent)` with the public half
  gone. No committed schema-4 event exists for a P3 husk, so the replay obligation is vacuous, as run 4 already held for
  these rows. **Why run 4 missed it:** its search listed every test consulting a removal site — this witness is in that
  listing — but read only the nine that inject a fault *at* a removal site, and a construction reaching the coordinate's
  durable state by another route was not among them. **Executed in repair round 1, and re-executed in round 2:** the witness and its row-129 sibling
  pass unchanged in the repair worktree (`repair1/witnesses-in-worktree.log`). Round 1's mutation control for them — the
  existing released lock file read as live — was **not** evidence, and its log is kept as it was
  (`repair1/probes/released-lock-called-live.log`): the driver passed `--exact` once per test name, the test harness
  rejected the invocation before running either test, and round 1 counted the resulting exit as a kill; the record and
  verify lenses caught it. Round 2 ran the pair properly in a fresh scratch clone of the range (`repair2/probes/`,
  **F59**): unpatched, both pass; with the same mutation applied, both fail at the census-outcome assertion, `Skipped`
  where `ReclaimedPublicOnly(TargetAbsent)` is asserted; and the driver refuses a kill whose log does not show each named
  test failing at that assertion, or a pass whose log does not show each passing. Grades: (i) `P`, (ii) yes, (iii) `V`.

**The credit that was checked the other way.** The record lens asked whether rows 138 and 139 — the reaper-hold
observation before and after — are entitled to their grade, since neither coordinate has a probe-recorded firing and run 4's
§15.2 described their evidence as a firing and a death. They are entitled to it, and the description was wrong: the
credited witness is `resume_refused_while_reaper_hold_observed_then_succeeds`, graded `=` and `P` by plant — a committed run
and a stand-in hold, the first resume stopped by the observation and the plant removed as a reaper's exit releases it — with
the next resume's observation, both leases and the exclusive cleanup probe asserted, and a probe-recorded replay pair. The
rules require no firing or death of a plant. Repair round 1 ran it unchanged (`repair1/witnesses-in-worktree.log`) and
killed it by making the resume skip the observation (`repair1/probes/cleanup-hold-not-observed.log`). §15.2 now says this.

**No claim is made beyond ST-07's three obligations as the rules grade them**: a construction is a real kill, an injected
error, a plant, or a fault or plant at an adjacent coordinate with nothing durable between, and **F19** counts each kind.

**For the next runner: how run 4's search lost witnesses that were in the tree, and what run 5 must do differently.**
Run 4 bounded each search by how it expected a witness to work. For rows 9 and 10 it listed the tests whose process
consults `Worktree.Verify`, so a witness in which the production recovery *does not* consult the site — because the
disposition for a dead incarnation's retained generation is to close it, never to retry it — could not appear. For rows 88
and 89 it listed every test consulting a removal site but read only those that inject a fault *at* one, so a construction
that reaches the coordinate's durable state by another route (here a plant, with a fault at an unrelated coordinate) was
never read, although it stood in the listing. And it carried run 3's plant-domain file
(`audit/grading/plant-domain-103.json`) as the bound on witnesses that replay twice, when the range had added sixty tests:
a domain file is complete only for the probe that produced it, and a stale one silently shrinks the search. The rules
already say what the bounds are — for a fault-based construction the probe's firing list, for a plant every test that
makes a test-code replay pair, and for a vacuous row every test that runs the row's production recovery — and none of
those is "tests that consult the site". So: regenerate the plant-domain bound from the run's own probe (**F55** is this
range's), search a plant row over that whole domain, search a vacuous row over every test that runs its recovery
component, and never narrow either by the site's own consultations. Run 3 missed the rows 88 and 89 witness the same
way, so the defect predates run 4.

## 9. Clause 3 — the two-crash proof

Both variants are committed tests and both ran green in group 2's invocation (**F37**) and in every suite run. The
**bytes on disk** before and after each simulated loss are printed by this run's own probe
(`probes/projection/probe-run.log`, **F32**), in a scratch clone restored and checked against `HEAD` afterwards:

- **the positive variant** is **F33**: after the kill the log carries `merge_prepared` and the unsynced
  `task_merged`; after the loss `merge_prepared` survives and `task_merged` is gone, one line shorter;
- **the negative variant** is **F34**: the unsynced `merge_prepared` before the barrier, and after the loss it is
  gone, the candidate still queued.

Its witness is one of the six #311 re-ran over the prefix the run's first resume leaves (§8.4), and the credited
recovery enters none of the creation prefix's repairs (**F25**). Two mutations kill the pair (§5).

**The limit, stated.** The loss both variants recover from is simulated: the test discards the unsynced bytes itself.
Whether the physical sync reached the platter is unobservable to any test, which is what `PR5-C-FSYNC-UNOBSERVABLE`
(P2, `deferred`, §13) records, and repair round 2 reproduced that rather than reading it: with the `fsync` call removed
from `sync_log_file` and its durability-ledger entry kept, both variants still pass
(`repair2/probes/two-crash-without-fsync.log`, `repair2/probes/two-crash-without-fsync.diff`). So the proof establishes
the barrier's ordering and the recovery over the bytes that survive, and not that the barrier makes them survive; that
boundary is the finding's and is carried beside the credit, not inside it. The verify lens of repair round 1 executed the
same mutation and asked for this disclosure.

**Grade: established** for the simulated loss, with the physical-sync boundary carried by `PR5-C-FSYNC-UNOBSERVABLE`.

## 10. Clause 4 — the residue-class evidence (required artifact 3, second part)

**The artifact** is `residue/residue-class-evidence-record.md`, generated by `residue/residue-record.py`. Its
declarations and synthetic half cover **every** declared (site, element) pair — the generator counts the pairs and
refuses on a gap, and there is none (**F48**). Its sampling half holds the histograms of
the writing samplers: five Linux runs of each of the five samplers (**F45**, `samplers/linux-loops.log`) and five
guest runs of each (**F46**, `guest/guest-samplers.log`).

**Labels.** Each class entry is labelled recovery-proven, never observed execution, and a residue-class entry
carrying an executed-hook claim is refused by the tree. The conditions the record holds under are named in the
record itself.

**An operational note, because it produced a red that is this run's own and not the range's.** The guest sampler
script's first attempt inherited a different build profile from the guest suite's — it did not carry
`CARGO_PROFILE_TEST_DEBUG=0` — so cargo set out to rebuild the whole lib test with full debug information, and the guest
ran out of disk inside that build: every run of that attempt failed in the compiler and none reached a test. This session
removed **only its own** files to recover the space (its temporary directory, its bundle, its exports and its own target
directory, each listed by path before being removed; nothing belonging to the guest's CI runner was touched) and re-ran
the samplers with the suite's own profile: **F46**, all green. **No log of the failed attempt was kept**, so the failure
is this run's own account and not a saved measurement, and this paragraph quotes nothing from it — not the disk's state
when it failed, not the compiler's error text. What is saved is the re-run's log, whose two free-space readings bracket
it (**F56**, `guest/guest-samplers.log`). Run 4 and repair round 1 quoted the failed attempt's disk figures and its error
text here as if measured, and the record lens caught it. It is a fact about this run's scripting and the guest's disk,
not about the range.

**One residue has no class.** The report-staging leftover a writer killed inside `Report.Write` leaves is outside
the packet's declared classes (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`, P2, deferred, whose own guard text
says Gate 5's residue clause is judged with it outstanding). It is named, not counted.

**Grade: established** for the packet's nine declared classes, under the conditions named.

## 11. Clause 6 — projections equal (required artifact 4)

**The artifact** is `probes/projection/probe-run.log`: the byte snapshots (length, sha256, line counts and the two
event-kind counts) of the durable log at the probe's snapshot points, **F32**, over six tests that pass. **What the
snapshots cover, stated exactly:** the committed test `projections_are_equal_between_live_and_replay_at_every_prefix` makes
two comparisons — a step-level one after each step that succeeds, reading the log from disk and replaying it, and a
per-append one over every prefix from its first append through `run_finished`, from the live projections the hooks
recorded — and the probe printed the bytes at the step-level comparisons only (**F32** names their line counts); the
per-append comparison is asserted by the test and its prefixes are not separately printed. The other five tests are the
two two-crash witnesses and three replay-twice witnesses, and the probe prints snapshots in three of them — the two-crash
pair, whose negative variant also prints one replay-twice point, and the answer-ingestion witness — and none in the other
two, which pass under the probe and print nothing. Run 4's log labels each snapshot by its point and not by its test, so
repair round 2 re-ran the probe with each snapshot labelled by the thread that printed it (**F58**,
`repair2/probes/projection-attributed.log`): the same points, and four of the six tests printing. Run 4 described the
snapshots as covering every state the test compares, and repair round 1 as printed beside every one of the six tests;
neither is so, and the equivalence the clause asks for rests on the test's assertions, not on the snapshots.

**Grade: established.**

## 12. Clause 7 — the per-resource ledgers (required artifact 5)

**The artifact** is the six outcome tables the suite writes under `UPSTROKE_LEDGER_EXPORT`: Complete, Parked,
Halted, BudgetExceeded, no-`run_finished`, and the resumed no-`run_finished`-then-Complete run. **F35**: the two
Linux exports are byte-identical, and the guest's differ from Linux's only in the two `cleanup.lock` R21 lines,
where the file is absent on Windows and the row's expected state is `Any`. **Every row's verdict in every table is
`yes`** (**F36**).

Each table checks the recorded outcome against the derived one. Closure per class is group 4's and finalization's
idempotence group 5's.

**Not established: convergence without an operator, in the filed shapes.** `PR5-RD-002` — the zero-length
`commondir` that made a resume refuse — **is closed in this range** by #308, and its file is gone from the ledger
(§13). What #308 left is filed: `PR5-RD-003-A-PRUNE-STRANDS-A-CHECKOUT-WHOSE-GITDIR-IS-GONE`,
`PR5-RD-002-TORN-REMOVAL-NOT-DURABLE`, `PR5-RD-002-RESUME-DERIVES-THROUGH-A-TORN-ENUMERATION` and
`PR308-R3-SKIPPED-PRUNE-KEEPS-ANOTHER-RUNS-TORN-REGISTRATION`, all P2 and deferred, together with the two
`PR8-CRASH-002` residuals. Their refusals are resumable and lose nothing, so no equation is falsified; but
"converges" holds only for the measured runs. **None of these was re-executed by this run**, and this report does
not claim otherwise: run 3 executed the `PR5-RD-002` and `PR5-RD-003` shapes at its range, and that is history
here, not evidence.

**Grade: established** for the six outcome tables, the derived outcome, closure and finalization as measured, with
the operator shapes named as not established.

## 13. The findings in range, re-verified, and what each does to the verdict

**F39** is the ledger at the range, plus the one this gate files (below). **F40** is what the range opened and
closed. **F41** is the P1 set at both ends.

**No finding is filed `open`.** Clause 9 is graded on disposition, and every file is `deferred` or `accepted-risk`.

**What the range did to run 3's clause-9 sentence**, re-derived rather than carried (`ledger/range-delta.txt`,
each closure traced to the commit that removed the file):

| finding | at run 3's range | at this range |
|---|---|---|
| `PR5-RD-002` (zero-length `commondir`) | P1 deferred, **on a mechanism this gate certifies** | **closed** by `dfab458b` (#308), which filed the two shapes its repair leaves |
| `PR273-R1-DECODER-GUARD-MATCHES-A-SPELLING` | P1 deferred | **closed** by `0a0f6eb2` (#283) |
| `PR5-R1-CFG-TEST-SHRINKS-THE-DOMAIN` | P2, one of the two run 3 gave clause 8 | **closed** by `3dae6a14` (#306) |
| `PR7-SCRATCH-FIXTURE-LEAK` | P2 | **reclassified P2 -> P1** by `92a61661` (#307) — a new P1 in range |
| `PR7-WRAPPERS-EMPTY-DOMAIN` | P1 deferred, the other of clause 8's two | P1 deferred, **narrowed** by #312's fence flip, not closed |
| `PR7-CLASSIFICATION-DOMAIN-READS-FNS-ONLY` | not yet filed | **filed** in range by `409a6138`, P2 `deferred`: a `const` or `static` carrying a closure or function pointer is outside the function-only classification domain, in a module that carries an allowance; reasoned, not executed; it bears on Q5's wrapper limb and is judged in §14. Run 4's accounting omitted it, and the verify lens caught the omission |

**The P1s at this range, all deferred**, and what each does:

| finding | bearing |
|---|---|
| `PR7-WRAPPERS-EMPTY-DOMAIN` | on a mechanism this gate certifies: the wrapper classification (Q5, clause 8). §14 |
| `PR7-SCRATCH-FIXTURE-LEAK` | a fixture hazard to the suite's own measurement under concurrent runs; every export and probe run here used a fresh private `TMPDIR` |
| `PR64-CLEANUP-003-SCRATCH-PRECLEAN` | the same hazard, from the other side |
| `CLASS-INTERMITTENT-SUBPROCESS-KILL-SETTLE-RESIDUE-FAILURES` | the flake class the box's kill-and-settle reds resemble; no red of this run was of it |
| `PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS` | a trust boundary on the ref funnel the CAS uses (Q4); no witness here constructs it |
| `SHAPE-GATE-MISSES-ARITHMETIC-SUBSTITUTION`, `PR286-PROSE-SCANS-READ-THE-WRITTEN-SPELLING` | instruments; outside the tree this gate certifies |
| `LEGACY-WORKSPACE-READS-REPLACEMENT-OBJECTS` | the schema-1..3 workspace; outside the schema-4 topology |

**Carried findings that bear on a clause or a question, judged rather than listed.** The verify lens of repair round 1
found that the ledger export names findings this section never judges, although its title promises what each does to the
verdict. Each is `deferred`, none is P1, so clause 9 is unmoved, and none moves another grade; what each does:

| finding | severity | bears on | what it does to the verdict |
|---|---|---|---|
| `PR5-C-FSYNC-UNOBSERVABLE` | P2 | clause 3; Q1, Q5 | the physical sync's execution is unobservable to any test. The two-crash proof is established for the simulated loss, with this boundary stated beside it (§9, reproduced by execution in repair round 2); the executed evidence at `Event.OpenLog`'s SyncPrefix shows the funnel reaching the sync and recording it, not the platter written (§14). Grade unchanged, limit stated |
| `PR3-REG-001-CONDITIONAL`, `PR4-REG-001-STILL-EQUIVALENT` | P3 | clause 2 (the registry's key); Q5 | "a registry entry for every observed order" is equivalent to the registry's order-free key only while every site exposes zero or one observable order. That condition holds at this range and this run measured it: the inventory export records one order or none on every executed entry (`inventory/site-enums-export.md`, its observable-orders line, which names this finding), and the registry is byte-identical across the range (**F6**). Conditional and carried; its guard is the change that first gives a site two orders, and nothing in the range does |
| `PR3-FRAMEWORK-SILENT-1` to `-5` | P3 | clauses 2 and 4; Q5 | five residue rules the tree's residue authority derives and the packet does not state: non-releasing removals, read-only After phases, the `Referenced` stop and probe, `Event.OpenLog`'s kill against its error return, and the platform containment actions. The audit's tabled actions and the residue record's classes are graded against the authority as the tree states and tests it; the packet's silence is a documentation gap whose guard is the owner's design section, not a disagreement between the tree and the packet, and no grade here rests on the packet stating what the tree does |
| `PR7-STD-PRIVATE-ROOT-NO-CONTAINMENT`, `PR7-STD-OWNERSHIP-PROOF-UNCANONICAL` | P2 | Q1, Q4 | the recorded private-root locator is validated lexically, with no symlink or reparse-point containment, and the private-half ownership proof falls back to an uncanonical spelling when canonicalisation fails. Q1's root derivation and Q4's ownership answer are for the named cases (§14); these two shapes — a linked component in the recorded root, a retargeted spelling with canonicalisation failing — are outside them, no witness here constructs either, and §14 now says so. Both are on a boundary the owner holds |

**The two PR8 deferrals the brief names.** `PR8-R2-SPEND-REPLAY`'s file was removed by `7da321c0` and
`PR8-CRASH-002` was resolved by `1c309d49`, both 2026-09-12 and both ancestors of PR10's merge base; `1c309d49`
filed the two residuals `PR8-CRASH-002-PACKED-REFS-LOCK` and `-PACKED-RUN-REF`, which are still deferred P2s and
bear on clause 7 (§12). Verified at this range in `ledger/deferrals-verify.txt`: neither `PR8-R2-SPEND-REPLAY`
nor `PR8-CRASH-002` has a file, and both residuals do.

**Filed by this gate:** `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT` (P2, deferred), §14's executed
reproduction. It is deferred rather than fixed because this pull request's own diff changes nothing under `src/` — the
merge of the reviewed master in repair round 5 is master's reviewed change, not this report's (§15.2) — and its guard is the
owner pass that already owns `PR7-WRAPPERS-EMPTY-DOMAIN`, whose class it is. **Its file reached master ahead of this report**,
through #316's branch, which filed it as run 4 wrote it and then narrowed it with the fences' measurements; this pull request
carries no copy of its own, since master's is the newer and the copy here had become an add/add conflict that stops GitHub
creating any `pull_request` workflow run for the pull request; and nothing after the range is credited here — the fence is
not in `9bb177ea`, and this report does not rely on it. **Its count is by rows, and this report's is not.** The finding's text
draws the carve-out from the allowlist's forty-five non-empty rows, as run 4 did, and so names two modules; the file-level
reading v17 asks for finds a third, `src/agent/bin.rs` (§14, **F42**, **F43**, **F63**). The finding is master's and is not
amended by this pull request, which carries no finding file; the guard it proposes, in the form that reads a module's
allowance from its row, would not name `src/agent/bin.rs`, and the form that reads the file's own attributes would. That is stated
here for the owner pass the finding names.

**Not filed by repair round 1:** nothing. The four clause-2 coordinates run 4 held back were witnessed in the tree all
along, and the defect was the report's search, which is recorded in §8.5 and in `audit/regrade-repair1.json` rather than
filed against the tree.

**Not filed by repair round 2:** nothing. The rejected invocation counted as a kill was a defect of round 1's record, not
of the tree, and is recorded in §8.5 and in `repair2/`; the log audit found no other among the logs it selected, and repair
round 4's re-run over the whole enumerated directory found no other either (**F57**).

**Not filed by repair round 4:** nothing. Each of the third review's findings was a defect of this report's account of its
evidence — a mutation receipt credited beyond what it shows (§5, §6), an audit's population called the whole directory (§5),
a header claim about two probe logs (the header), a count that took an invocation that ran no test for a survivor (§5), and a
description that took an adjacent construction for a plant (§8.3) — and none is a defect of the tree or of the range.

**Not filed by repair round 5:** nothing. The fourth review's findings were, again, defects of this report's account of its
evidence — the carve-out counted from the allowlist's rows rather than from the files' own attributes, which under-counted
the classified modules the bypass reaches by one (§14); the required audit's rows 31 and 32 describing a ref double the
frozen witness no longer uses, with the like call-path citation in fifteen more rows (§8.3, **F65**); and the header's
provenance sentence that put the range control outside the range — and none is a defect of the tree or of the range. The
third host is a fact about the range that the filed finding's row-based count does not state; it is recorded in §14 and
above for the finding's owner, and a repair round files nothing.

**Not filed by repair round 6:** nothing. The fifth review's findings were, once more, defects of this report's account of
its evidence — a provenance sentence that denied the worktree executions its receipts name (§1.3, **F69**), an integration
receipt cited for more than it records (§1.3, §15.2), an exhaustive count of gate reds written before seven more were added
to **F51** (§2.1, §16), two launcher-caused reds folded into the known-flake list (the header, §15.2), a control total in
the pull request body (**F68**), and two blanket sentences about what every receipt and every path names — and none is a
defect of the tree or of the range. The round's own gate red at its first head (**F71**) is a listed fingerprint, controlled
and counted, and not a finding.

**Not filed by repair round 7:** nothing. The sixth review's findings were defects of this report's account of its own repair
rounds — two rounds' execution summaries that left out the gate attempts and controls their receipts hold (the header, the body,
the evidence index; **F51**, **F67**, **F68**, **F71**, **F72**), two figure restatements in the body left at an earlier cutoff
(**F69**, **F70**), a review history that gave one lens another's findings (the body), and a per-leg claim wider than the
directory (§15.2, the body, the evidence index; **F73**) — and none is a defect of the tree or of the range. The round's own gate
red at its first head (**F75**) is two observed failures on fingerprints the run brief lists, controlled and counted, and not a
finding; the figure of its own that its first head's finalisation found not closed under that head's directories (**F73**) is
corrected here, not filed.

## 14. The six standing questions, scoped to G5's range

The questions are `spec/G5-QUESTIONS.regen`, regenerated from **F1** and byte-identical to the derived file on disk
for all six (**F3**). The definition assigns the portions: the process-local and broker rows (R3, R4, R13, R17,
R22, R28), INV-18 and INV-21 are certified at G6, with R17, R22 and R28's host portions asserted here, and R19/R26
with real Docker at G7. Anything G1–G4 certified is cited, not re-claimed.

**Q1, reconstruction.** *Answered with evidence, partially.*

- **Reconstruction from the durable bytes is replayed, not reasoned.** Projections are equal between the live fold
  and the replay of the bytes on disk at every prefix, and this run printed those bytes — length, sha256, line and
  event-kind counts — at the probe's snapshot points (**F32**, §11), which are the step-level comparisons and not every
  per-append prefix, printed by four of the six tests the probe ran (**F58**). The replay-twice equality at recovered
  coordinates is checked against the probe's own recorded pairs, row by row, for every row graded `(iii) yes` on Linux;
  the five Windows-only rows are held to the Unix twin's pairs, row 145's is read rather than executed (§8.3), and the
  vacuous rows have no pair to check. What is durable here is what survives the simulated loss: the physical sync is
  outside any test's sight (`PR5-C-FSYNC-UNOBSERVABLE`, §9, §13).
- **The barrier comes before any fold-derived effect.** Established at the Event and OpenLog coordinates — rows
  91–121, **all of them at this range**, where run 3 had 96 and 98 near-exact — and by the two-crash proof (§9).
- **The resume derives its root from the recorded locator, and its barrier refuses, appending nothing**: a log whose
  first line disagrees with the commit record's digest, and a run whose recorded plan digest disagrees with the
  frozen inputs. `g5r2r5` mutations kill both (§5). It rebuilds the Runner from the record before its pre-flight
  (ST-20, group 9). That the derivation precedes every lock is **read from the code, not executed separately here**:
  `run_recovery_order` takes the derived root as its input and its first step is `LocksHeld::take`. The locator it derives
  from is validated lexically, without symlink or reparse-point containment: `PR7-STD-PRIVATE-ROOT-NO-CONTAINMENT` (P2,
  `deferred`, §13) names that shape, and no witness here constructs it.
- **"Every durable recovery record reclaimed or repaired before any reuse"** is established at the coordinate level for
  every executed registry entry (**F19**, §8.5), and **not** in the filed torn-registration shapes of §12, which need an
  operator.
- **Process-local holdings empty at start** are G6's, except the observed reaper hold, which is refused until
  released — rows 138 and 139, established by plant and the test's own assertions (§8.5, §15.2).

**Q2, resource accounting.** *Answered with evidence for G5's durable rows.*

- The per-outcome ledgers are **F35** and every row's verdict is `yes` (**F36**), across Complete, Parked, Halted,
  BudgetExceeded, no-`run_finished` and the resumed run. They include R21's answer and staging files, the owner and
  commit records, and R27's unreferenced objects enumerated with `git fsck --unreachable`.
- The derived outcome, closure and finalization are §12's.
- **One residue has no class**: `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED` (§10), whose own guard text says this
  clause is judged with it outstanding.
- Among the findings this question carries are the `PR128-*` rows, where the inspection primitives fold Git
  failures into absence.
- The slot, provisional, invocation, lock-hold and process-handle grants are G6's.

**Q3, stale completions.** *Answered with evidence for G5's portion*, which is the returned append error. Every
error-return point of the three append sites is established at the coordinate level (rows 104, 105, 107, 111, 112,
114, 118, 119, 121). At each, the append-error protocol leaves the fold untouched, retries nothing, reports nothing
from memory and cleans nothing, and the next open decides from the surviving prefix. Stale generations, attempts and
invocations are earlier gates' portions.

**Q4, aliasing.** *Answered with evidence for G5's portion*: private-half ownership and the fast publication's
object.

- The ownership proof refuses a forged, copied, foreign-root, reparse-point, owner-less or disagreeing husk;
  the startup suite's six tests for these pass in every suite run (**F9**, **F11**, **F12**), and two are in
  group 3's invocation.
- The fast publication names the gated candidate object itself: census assertion 8's relations in both directions
  (§7) and the fast no-execution record (§8.1).
- `g5-fast-publication-other-head-accepted` is killed; `g5-fast-publication-other-sha-accepted` survives because
  the wire type refuses that shape before the fold's check runs (§5).
- `PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS` (P1, deferred) is an open trust-boundary finding on the ref funnel, and
  no witness here constructs it.
- `PR7-STD-OWNERSHIP-PROOF-UNCANONICAL` (P2, deferred, §13): when canonicalisation of the public directory fails, the
  ownership proof falls back to an uncanonical spelling. That shape — a spelling retargeted between the marker's write
  and the proof — is outside the six refusals above, and no witness here constructs it.

**Q5, effect and append boundaries.** *Answered with evidence, and the answer is not affirmative.* **Q5's
wrapper-classification limb was amended v16 -> v17 on 2026-09-22 on the project owner's authority, and an
affirmative answer under v17 is a narrower answer than v16 asked for.** That narrowing is recorded here because
recording it is part of answering honestly.

- **Established:** both hook phases observed executed on each host, every parent-executed point on the host that
  has it, and a registry entry for every observed order (**F26**, §8.2) — an entry per order being the registry's
  order-free key only while every site exposes zero or one observable order, which holds at this range and is measured
  by the inventory export (`PR3-REG-001-CONDITIONAL`, §13). At `Event.OpenLog`'s SyncPrefix the executed evidence is
  that the funnel reached the sync and recorded it, not that the platter was written (`PR5-C-FSYNC-UNOBSERVABLE`, §9).
- **Established:** the residue classes' synthetic and sampling evidence (§10).
- **Established:** executed evidence of the tabled recovery action and residue for **every** entry (**F19**; **F20** is
  empty; §8.5).
- **The lists** — the clippy disallowed lists, the allow-placement scan, the funnel-module and frozen legacy
  allowlists — are held against the code by the effects census, which passes (group 8, **F37**). The dependency
  review has nothing new to review: `Cargo.toml` and `Cargo.lock` are unchanged across the range.

- **Four sets are in play here, and this report keeps them apart.** #312 flipped the files that *fence* a governed lint
  from `deny` to `forbid`; `effects::CLASSIFIED_MODULES` and `effects/wrappers.toml` classify a set of *modules*, which
  happens to number the same and is not the same set; the carve-out is drawn over the whole tracked Rust tree, test-only
  files and the example target included, and is a third set (**F42**); and what production reaches is a fourth — the
  classified modules the corrected carve-out leaves unfenced are production modules (`src/agent/bin.rs` is declared
  unconditionally in `src/agent/mod.rs`), and the test-only file the correction removes from the carve-out has no
  production region. Every figure here names which set it counts.

- **The carve-out v17 requires this record to enumerate.** It is the files that carry a *file-level* allowance of a
  governed lint, and **F42** derives it from the source at the range rather than from the allowlist's rows: every tracked
  `.rs` blob read with `git show`, a file in the carve-out exactly when a `#![allow(..)]` among its leading inner attributes
  names a governed lint (`repair5/carveout.py`, its reading `repair5/carveout.txt`). Run 4 and repair rounds 1, 2 and 4
  enumerated the entries of `effects/allowlist.toml` that record any allowance at all and called that the carve-out;
  the fourth review's three lenses each found, independently, that two of those rows record an allowance the file does not
  carry at file level, and this round derived the set afresh and found the same two. `src/agent/bin.rs`'s one allowance is
  an *outer* `#[allow(clippy::disallowed_methods)]` on its inline `#[cfg(test)] mod tests` — its row says so, *limited to
  the `#[cfg(test)]` module* — so its production module carries none. `src/agent/proc/test_support/readiness.rs` *denies*
  all three governed lints at file level and carries six per-site `#[expect]` attributes (its row's `expect_sites`), and
  it is declared inside `#[cfg(test)] mod test_support` of `src/agent/proc.rs`, so it has no production region and is not
  a classified module. The committed `effects::tests::every_allow_of_a_governed_lint_is_module_level_and_in_the_allowlist`
  holds each row's `allows` equal to what the source writes at *any* module level — an outer attribute on an inline module
  and a reasoned per-site expectation under a file-level deny both pass it — so its equality never established file-level
  scope, and citing it for that was this report's error. The amendment's own note counts *the 45 that allow*: that is the
  row count, and its operative wording, the file-level one, governs here. Enumerated in full by the allowlist's own two
  sections, the test-only files marked (†: declared under `#[cfg(test)]` in their parent, or the example target), and the
  enumeration below **checked** against the derivation rather than transcribed, path for path, with none missing and none
  added (`repair5/enumeration-check.txt`; run 4's row-based reading and its check are kept as the receipt of what was
  corrected, `questions/q5-carveout-from-allowlist.txt`, `questions/carveout-enumeration-check.txt`):

  *Funnel (22):*
  `src/agent/proc.rs`, `src/agent/proc/pipe_io.rs`, `src/agent/proc/tests.rs`†, `src/effects/tests.rs`†,
  `src/events/log.rs`, `src/events/log/tests.rs`†, `src/interaction.rs`, `src/rundir.rs`,
  `src/rundir/scratch_tree.rs`†, `src/rundir/tests.rs`†, `src/runner/container.rs`,
  `src/runner/container/census/tests.rs`†, `src/runner/container/exec/tests.rs`†,
  `src/runner/container/resolve/tests.rs`†, `src/runner/container/tests.rs`†, `src/runner/container/view.rs`,
  `src/runner/host.rs`, `src/runner/host/tests.rs`†, `src/util.rs`, `src/workspace_manager.rs`,
  `src/workspace_manager/fixture.rs`†, `src/workspace_manager/tests.rs`†.

  *Legacy, frozen at PR5 (21):*
  `examples/probe.rs`†, `src/agent/claude.rs`, `src/agent/codex.rs`, `src/agent/copilot.rs`, `src/answer.rs`,
  `src/config.rs`, `src/connect.rs`, `src/engine/attempt.rs`, `src/engine/coordinator.rs`, `src/engine/resume.rs`,
  `src/engine/tests.rs`†, `src/events/log/premove.rs`†, `src/events/mod.rs`, `src/export.rs`, `src/gates.rs`,
  `src/main.rs`, `src/review.rs`, `src/route.rs`, `src/status.rs`, `src/validate.rs`, `src/workspace.rs`.

  Fifteen of the forty-three are test-only, and none of the forty-three is a file with no allowlist row; no file at the
  range carries a file-level `#![expect]` of a governed lint. Only three of `GOVERNED_LINTS` are ever allowed at file level
  — `clippy::disallowed_methods`, `clippy::disallowed_types` and `clippy::disallowed_macros`, with the per-lint counts in
  **F42** — and none of these files allows `style`, `all` or `warnings`.

- **The residual bypass inside that carve-out is carried by a filed finding**, as v17 requires:
  `PR7-WRAPPERS-EMPTY-DOMAIN` (P1, `deferred`, under its project-owner guard). It is **not** closed by the
  amendment and is not recorded as closed here.

- **A second filed finding inside the carve-out, omitted by run 4 and judged here:**
  `PR7-CLASSIFICATION-DOMAIN-READS-FNS-ONLY` (P2, `deferred`, filed in this range). It is reasoned, not executed: a `const`
  or `static` whose value is a closure or a function pointer is not a `fn` item, so the classification never asks for it,
  and in a module that carries a recorded allow its body compiles under that allow. Its hosts are, by its own statement,
  modules that carry an allowance — inside the carve-out — so v17 already declines completeness over them, and this finding
  is carried by the same clause that carries `PR7-WRAPPERS-EMPTY-DOMAIN`. It does not on its own reach the modules outside
  the carve-out, it does not change clause 8's grade, and it remains `deferred`.

- **And the carve-out does not bound the class, which is why this limb is still not affirmative.** **F43** is the
  arithmetic over the classification's own domain: how many modules `effects::CLASSIFIED_MODULES` classifies, how many of
  them are in the file-level carve-out, and the remainder v17's affirmative answer would be over — with run 4's row-based
  split beside it, one module apart, since `src/agent/bin.rs` is classified and its row is not a file-level allowance.
  **Three of that remainder carry the bypass.** They carry no file-level allowance — so they are outside the carve-out —
  and no file-level fence either, so #312's flip does not reach them and the three governed lints take their level from
  `-D warnings` alone. Two of the three, `src/capacity.rs` and `src/runner/invocation.rs`, run 4 executed rather than
  reasoned (**F44**, `questions/q5-route/`): with the reviews' own macro shape moved into them and both functions called
  from production `park_question` in a topology module, `cargo clippy --all-targets --all-features -- -D warnings` exits
  **0** and the whole effects governance suite passes, and both controls hold — the same macro emitting `deny` makes clippy
  fail at both writes, and the same function with its name written literally is refused by
  `every_externally_reachable_fn_of_a_legacy_or_shared_module_is_classified`. The third, `src/agent/bin.rs`, run 4 had
  counted inside the carve-out by its row; the fourth review's verify and record lenses executed the same shape in it (the
  receipts are the review's, under `~/orch-pr10/reviews/pr-g5r4r4/`), and repair round 5 executed it again in a fresh
  scratch clone of the range, in run 4's four-step shape (**F63**, `repair5/probes/`): the macro emitting `allow` and the
  function called from `park_question`, clippy exits **0** and the effects suite passes; emitting `deny`, clippy fails at the
  write in `src/agent/bin.rs`; written literally, the classification census refuses the name. What all three executions establish
  is an admitted production **call site** that clippy and the governance suite accept; none claims, and none arranged,
  that the guarded write executed at runtime. The two run 4 executed are filed as
  `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT` (P2, `deferred`), whose text counts the carve-out by rows and so names
  two modules; the third is this report's correction of that count, recorded here and in §13 and filed by no repair round.

  **Completeness over the carve-out is not claimed**, as v17 says; and completeness over the modules outside it
  is **not claimed either**, because of those three. That is the whole of Q5's wrapper limb, and it is negative — it was
  negative on run 4's two hosts alone, and the third strengthens the reason without changing the answer.

**Q6, sequential guarantees.** *Answered with evidence for G5's portion.*

- **The derived run outcome is computed only from durable inputs.** Census assertion 5 establishes the equality in
  both directions over the union (§7), and the ledgers check it per outcome (§12).
- **The precedence is halt > budget > parked > complete**, and pending backoff never blocks Halted or
  BudgetExceeded. The instances are **F31**, and the `st17-*` recipes are killed (§5).
- **`budget_exceeded` is recorded before any budget-driven end** (group 4), and the pre-`budget_exceeded` interval
  classifies `NotEnding` (§7).
- **Closure and finalization** are groups 4 and 5.
- **No answer is ingested after a halt or a budget stop in the same epoch** (group 6).
- **One Runner identity per run.** The fold refuses a `run_resumed` whose Runner differs in any field: census
  assertion 7, `run_resumed_is_accepted_with_an_identical_runner_and_refused_with_any_different_field`, asserted over
  every explored state and passing in every suite run; and the resume rebuilds the recorded Runner before its pre-flight
  probes (ST-20, group 9). Run 4 attributed a count of accepting and refusing states to its own probe, **F31**; that probe
  measures assertion 5's `run_finished` instances and nothing about the Runner, and the attribution is withdrawn.
- **The limits:** the operator shapes of §12 bear on "converges", and none was re-executed by this run.

## 15. The five artifacts, the ten gates, and the evidence directory

### 15.1 The five required artifacts

Each is a file in the evidence directory, listed with its sha256 in `MANIFEST.sha256` once the manifest is finalised
(§15.3). None is a heading with a sentence under it.

| # | required artifact | the file | what makes it the artifact |
|---|---|---|---|
| 1 | the gate report | `reviews/2026-09-22-gate-G5.md` (this file; a byte-identical copy, `report/report-<head>.md` named by the head this file was committed at, is saved after the commit and listed in the manifest, and the copy of each earlier repaired head is kept beside it — run 4 promised that copy and never saved it) | assembled by `report/assemble.py` from `report/template.md` and `figures/figures.md`, which refuses if any `F<n>` the prose cites is missing from the table or any figure in the table is never cited |
| 2 | the bounded census artifact | `census-export2/bounded-census-artifact.md` | generated by its own `.py` from the suite's census summary (**F28**); the members, every bound over the union, the family's contract, the ten coverage assertions with the scope each runs over, and the two limits, every `file:line` resolved at this range |
| 3 | the effect-site inventory, the fault-injection registry with executed evidence per site and order, and the residue-class evidence record | `inventory/site-enums-export.md` with the range's copies of the lists beside it; `audit/per-coordinate-audit.md` and `.tsv`; `residue/residue-class-evidence-record.md` | the inventory tables every site and entry with its order, residue, tabled action, evidence kind and the tests recording it on each host, including the Event kill and error-return points, OpenLog's SyncPrefix point with the barrier's refusal cases, the IdUnread points and the fast no-execution record; the audit carries ST-07's three obligations per entry with resolved cites; the residue record carries synthetic construction per element and the sampling histograms |
| 4 | projection equivalence outputs | `probes/projection/probe-run.log` | the byte snapshots of the durable log at the probe's snapshot points (**F32**; §11 states what they cover and what they do not), including the two-crash proof's four (**F33**, **F34**) |
| 5 | per-resource ledger outputs for Complete, Parked, Halted, BudgetExceeded and no-`run_finished` runs | `ledger-export1/`, `ledger-export2/`, `guest/exports/ledger/` | the six outcome tables of each export, with every row's class, observed state and verdict (**F35**, **F36**) |

### 15.2 The ten gates at this report's own head

Run at this report's own head, from the worktree root, in the order the run brief gives — `~/bin/w1-eight-iso`
invoked bare (it takes a box-wide lock, so another run makes this one wait) with **the worktree under test** as its
argument, then the tenth gate by path. Run 4's attempts named its worktree; repair rounds 1, 2 and 4 name the repair
worktree `/srv/worktrees/repair-g5r4`, repair round 5 its own, `/srv/worktrees/repair-g5r4-r5`, repair round 6 its own,
`/srv/worktrees/repair-g5r4-r6`, and repair round 7 its own, `/srv/worktrees/repair-g5r4-r7`. Every head up to and including repair round 5's pre-merge head `4611b204` has the range's
`src/`; from the merge of the reviewed master `5b16f727` on (below, **F66**) the worktrees' `src/` is master's, and the
attempts at round 5's final head `5744b0aa`, at round 6's two heads `06a2b8a1` and `00a16a64`, at round 7's first head `50c0410b` and at this report's own head validate the
tree this pull request proposes to merge. Each round checked the argument against the worktree it stood in before its run and wrote
the check into its log:

```
~/bin/w1-eight-iso /srv/worktrees/g5-run4          # run 4's attempts, F51
~/bin/w1-eight-iso /srv/worktrees/repair-g5r4      # round 1's at 26d4bc0, round 2's at 5679907e and b3a404dd, round 4's at f1176796, F51
~/bin/w1-eight-iso /srv/worktrees/repair-g5r4-r5   # round 5's seven at its pre-merge head 4611b204, F67, and its one at 5744b0aa, F51
~/bin/w1-eight-iso /srv/worktrees/repair-g5r4-r6   # round 6's two at its first head 06a2b8a1, F71, and its one at its final head 00a16a64, F72
~/bin/w1-eight-iso /srv/worktrees/repair-g5r4-r7   # round 7's two at its first head 50c0410b, F75; then this head
bash .github/scripts/test-pr-ready-audit.sh
```

**F51** is every attempt made at a head up to round 6's first head `06a2b8a1`, its stated cutoff, kept rather than only a passing
one: run 4's six at
its heads (`gates/head-attempts.log`, with `gates/w1-eight-iso-head-<N>.log` and `gates/gate10-head-<N>.log`), repair
round 1's three at `26d4bc0` (`gates/final-head-attempt.log`, with `gates/w1-eight-iso-final.log` and
`gates/gate10-final.log`), and repair round 2's one at each of its heads: `5679907e` (`gates/head-attempt-5679907.log`, with
`gates/w1-eight-iso-5679907.log` and `gates/gate10-5679907.log`) — that same report, pushed before the add/add conflict with
master on the finding file was seen (§13) — and `b3a404dd` (`gates/head-attempt-b3a404d.log`, with
`gates/w1-eight-iso-b3a404d.log` and `gates/gate10-b3a404d.log`), gated again once the duplicate was dropped, repair round 4's one at `f1176796` (`gates/head-attempt-f117679.log`, with
`gates/w1-eight-iso-f117679.log` and `gates/gate10-f117679.log`), and repair round 5's seven at its pre-merge head `4611b204`
(`gates/head-attempt-4611b20.log`, with `gates/w1-eight-iso-4611b20.log` and `gates/gate10-4611b20.log`; every one red on
the test leg, **F67**, each red controlled alone in `gates/repair5-control/`, **F68**), repair round 5's one at its final
head `5744b0aa` (`gates/head-attempt-5744b0a.log`, with `gates/w1-eight-iso-5744b0a.log` and `gates/gate10-5744b0a.log`:
green, on the reviewed master's `src/`), and repair round 6's two at its first head `06a2b8a1` (`gates/head-attempt-06a2b8a.log`,
with `gates/w1-eight-iso-06a2b8a.log` and `gates/gate10-06a2b8a.log`: attempt 1 red on the container-launch-outages
refusal, controlled alone in `gates/repair6-control/`, attempt 2 green, **F71**). Round 6's one attempt at its final head
`00a16a64` (`gates/head-attempt-00a16a6.log`, with `gates/w1-eight-iso-00a16a6.log` and `gates/gate10-00a16a6.log`: green on
the first attempt, on the reviewed master's `src/`) is after that cutoff and is **F72**, frozen the same way, and round 7's two
attempts at its first head `50c0410b` (`gates/head-attempt-50c0410.log`, with `gates/w1-eight-iso-50c0410.log` and
`gates/gate10-50c0410.log`: attempt 1 red on the test leg on two fingerprints, each test controlled alone in
`gates/repair7-control/`, attempt 2 green) are **F75**, after it too. Which attempts keep
their per-leg logs is read from the directory rather than asserted (**F73**): every attempt from run 4's third onward has its
nine legs under `gates/eight-logs-<sha7>-*/`, whose `Compiling upstroke` lines name the worktree tested, and run 4's attempts 1,
2, 5 and 6 — its green attempts at `8b2116c`, `f39abbb`, `b19d89a` and `d1e48bb` — have the runner's summary log and the
gate-ten log alone (`gates/w1-eight-iso-head-<N>.log`, each `ALL 9 PASS` with `w1-eight-iso exit=0`, and
`gates/gate10-head-<N>.log`) and no per-leg log in the evidence directory; round 6's text here said every attempt's per-leg
logs were kept, and the sixth review caught it. Each attempt file is one line per attempt, appended while the attempts were
live, never overwritten, and frozen when the figures table is generated. The run brief's argument for the gate runner
named run 3's worktree; the runner computes its private target base from the worktree it is given, so run 3's tree would
have been the tree tested and this report's head would not have been. That correction is the reason the invocations above
name the tree under test.

**The attempt at this report's own head is recorded after the commit, in files of its own.** The gate run at a head can
only happen once that head exists, so its record cannot be inside the commit it certifies; run 4 appended it to the same
log after committing, and that append is what left the manifest stale (§15.3). Each repair round therefore records the
attempts at its own head in files that no earlier figure reads, and the next round freezes them into **F51**: round 1's
are the `-final` files above, round 2's are the `-5679907` and `-b3a404d` files above, round 4's the `-f117679` files above, round 5's pre-merge head's the
`-4611b20` files above and its final head's the `-5744b0a` files above, round 6's first head's the `-06a2b8a` files above and its final head's the `-00a16a6` files above (**F72**), round 7's first
head's the `-50c0410` files above (**F75**), and
this head's are `gates/head-attempt-<head>.log` (one line per attempt at that head,
naming the head), `gates/w1-eight-iso-<head>.log` and `gates/gate10-<head>.log`, with the per-leg logs under
`gates/eight-logs-<head>-final*/`, all written after the commit and listed in the manifest, which is finalised after them.
The same step re-runs §5's log audit over the directory as it then stands, this head's gate legs included, and writes
`repair7/log-audit-post-commit-<head>.log` beside the frozen pre-commit audits that **F57**, **F64**, **F70** and **F74** read
(rounds 4, 5 and 6's post-commit files, `repair4/log-audit-post-commit-f117679.log`,
`repair5/log-audit-post-commit-5744b0a.log`, `repair6/log-audit-post-commit-06a2b8a.log`,
`repair6/log-audit-post-commit-00a16a6.log` and round 7's at its first head, `repair7/log-audit-post-commit-50c0410.log`, are
frozen with them); this report does not quote it, for the same reason it
quotes no figure for the manifest.
So read it this way: **`gates/head-attempt-<head>.log` names the head this report was committed at, and its last line is
green.** A reader checks that against `git rev-parse HEAD` and against the manifest; the report does not ask to be believed
on it. Every attempt at a head up to `4611b204` is comparable with every other because nothing under `src/` differs between
those heads; the attempts at this report's own head run on the reviewed master's `src/` (**F66**), and what they certify is
the tree this pull request proposes to merge — not the range, whose suite receipts are its own (§2), and not Gate 5's
verdict, which is the range's and unchanged.

**The merge of the reviewed master, and the two source domains.** Repair round 5's seven ten-gate attempts at its
pre-merge head `4611b204` — a head whose `src/` is the range's, byte for byte — went red on the test leg every time (**F67**):
the descriptor-hold test in four, the reaper cleanup-lease assertion in three, the container-launch-outages refusal in two,
and in attempt 1 alone the two terminal-signal tests, a launcher cause demonstrated in `gates/repair5-control/attempt1-reds.log`.
Every other red is a fingerprint on the run brief's known-flake list and among the twelve **F50** searches, with `src/`
unchanged since the range and the test passing alone in its control (**F68**): reasoned, pre-existing, and no cause claimed
beyond the fingerprint. On the orchestrator's decision (`~/orch-pr10/answers/repair_314_r5-2-integration.md`) the round then merged the
reviewed master `5b16f727` — #315, #316 (the file-level fences of `src/capacity.rs` and `src/runner/invocation.rs`, and the
finding file run 4 filed) and #317 (the reaper test's bounded wait for the cleanup lease; merged 2026-09-23T01:11:54Z) — into
this pull request as the merge commit `21b46492`, without a rebase or any rewrite of history. **F66** derives the identities
from git: the merge's tree equals the tree measured before merging, its parents are the pre-merge head and master, the range
and master are both merge bases, the product paths (`src`, `effects`, `Cargo.*`, `.github`, `scripts`, `docs`) at the merge
are master's with an empty diff, the range-to-merge product delta equals the range-to-master delta, and the pull request's
diff against its base is the one report file with the same sha256 before and after. Two source domains follow, kept
obvious: **the historical domain**, the range `9bb177ea`, which every receipt, probe, mutation, census, audit and figure of
this report names — by its full sha or its prefix, or, for the files that carry neither, through the provenance the header
states for each: the clause-2 probe's two summary logs, `audit/probe/run-each.log` and `audit/probe/analyze.log`, carry no
sha and are placed at the range by the probe's build log, the mutation recipe logs by the backups beside them, and the
readings and sweeps by the files they read — and which every generator and checker reads from the exact checkout
`~/repair5-work/range-tree` (`repair5/integration/range-tree.log`: head, cleanliness and `src/` tree proved; the audit, the
figures and the carve-out derivation regenerated there byte-identical; the self-check exiting 1 on the report's own path,
since exempted, and the mutation summary not invoked — the complete regeneration is `repair6/regeneration.log`, §1.3); and
**the proposed domain**, master's `src/` at this report's head, which the ten gates at this head validate and which is not
evidence about the range. The final report-only head's own identity is a post-commit receipt — round 5's,
`repair5/integration/post-commit-identity-5744b0a.log`, certifies `5744b0aa`, and round 6's,
`repair6/post-commit-identity-06a2b8a.log` and `repair6/post-commit-identity-00a16a6.log`, its two heads, and round 7's first
head's `repair7/post-commit-identity-50c0410.log`; this report's own head's is `repair7/post-commit-identity-<head>.log` —
since no figure can name the commit that carries it. The seven red attempts stay
what they are: old-head evidence, not attempts at this head, and no later pass erases them; round 5's green attempt at
`5744b0aa` and round 6's two attempts at its first head `06a2b8a1` — the first red on the container-launch-outages refusal,
controlled, the second green (**F71**) — are frozen in **F51** beside them, on the reviewed master's `src/`, and round 6's one
attempt at its final head `00a16a64`, green, is **F72**, after **F51**'s cutoff, as are round 7's two at its first head
`50c0410b` — the first red on two tests, an observed production cleanup-ownership refusal and an observed count-budget Docker
timeout, each controlled, the second green (**F75**).

**Three reds, one fingerprint, each controlled and attributed — at the heads up to repair round 4's.** Of the attempts
**F51** lists at those heads, three went red, each on
its test leg and each on the same single test: run 4's attempt 3 at `e44385b`, and repair round 1's attempts 1 and 2 at
`26d4bc0`. **F52** reads the three failing legs' own logs and asserts them identical in the test, the panic site and the
assertion text: `a_host_integration_reaper_holds_the_runs_cleanup_lease`, panicking on *"the hold outlived the reaper that
took it"*. Fingerprinted by that assertion text and its line rather than by the test's name, it is
`PR281-CLEANUP-LEASE-HOLD-OUTLIVED-AND-ITS-UNREADABLE-TWIN` (P2, `deferred`, `provenance: pre_existing`), which the
ledger carries at this range and which the run brief names as the likeliest red on this box. Repair round 1 wrote of a
single red here, counted a single attempt among the reds in §16 and one attempt fewer than it made in its pull request
body, while its own finalised logs held three; both lenses caught it.

**The controls are executed, not argued** (**F53**): the same test alone, five times at `e44385b` — the head of run 4's
red attempt, whose `src/` is the range's, as it is at every head of this pull request up to `4611b204` — five
times at the merge base, the frozen range itself, in a separate clean clone with its own target base, and ten times at
`26d4bc0` in two batches, the second under the very load repair round 1's second red ran under
(`gates/pr281-control-final/`): it passes every time, and the fingerprint appears in none of the control logs. Run 4
called its first set a control *at this report's head*; the control's own log names `e44385b`, and **F53** reads every
head from the logs. **F51** is the stronger control: the **whole suite** ran at code identical in every byte but
documentation files across all the attempts, and the red appears in three and not the others. Repair round 1's two reds
ran while the box's Windows CI-runner VM was serving other lanes' CI jobs, and its green attempt after that job ended
(`repair1/final-head-attempts-note.txt` records the load and the tenants at each attempt); run 4's red ran at a lower
load. The finding's own text says it reproduces under the whole suite and not alone, which is the shape observed all
three times.

**What the reds do not touch, stated correctly.** Run 4 wrote here that the failing test is the credited
witness of registry rows 138 and 139 (`Lock.ObserveCleanupHold` before and after) and that their grade rests on the
probe's recorded firing and death at the coordinate. Neither is so, and the record lens caught it. The credited witness of
both rows is `resume_refused_while_reaper_hold_observed_then_succeeds`, which the failing test is not; no row of the audit
cites the failing test; the rows are graded by plant (`=` before, `P` after) — a committed run and a stand-in hold that
stops the first resume right after the observation, the plant removed as a reaper's exit releases it — with the next
resume's observation, both leases and the exclusive cleanup probe asserted, and a probe-recorded replay pair; and the rules
ask no firing or death of a plant. Repair round 1 ran the credited witness unchanged (`repair1/witnesses-in-worktree.log`)
and killed it by making the resume skip the observation (`repair1/probes/cleanup-hold-not-observed.log`). What these
reds bear on is clause 5, and the clause-5 grade is **F50**'s search over the five suite logs; the reds are in gate logs,
are recorded here, and are the reason each of those attempts was followed by another at the same head.

### 15.3 The evidence directory

`~/tactus-artifacts/g5-evidence-9bb177e/`, with `README.md` describing the layout and `MANIFEST.sha256` over every file
in it but two: the manifest itself and `manifest-check.log`, which `make-manifest.sh` writes when it generates and verifies
the manifest. **The manifest is finalised after the gate attempt at this report's own head** — after
`gates/head-attempt-<head>.log`, its two logs, the per-leg logs and the report copy `report/report-<head>.md` exist —
because those files can only be written once the commit exists. That is why this report quotes no figure for the manifest: run 4 quoted a
verification made before its last attempt, and that attempt's append, a figure regeneration and a self-check run then
changed five listed files and added two unlisted ones, so the record did not verify when it was reviewed. The reader's check
is `sha256sum -c --quiet MANIFEST.sha256` from the directory, expected to exit `0`; `manifest-check.log` records the run
made at finalisation, and the repair rounds' own records (`~/orch-pr10/repair-314-evidence/` for round 1,
`~/orch-pr10/repair-314-evidence/round2/` for round 2, `~/orch-pr10/repair-314-evidence/round4/` for round 4, `~/orch-pr10/repair-314-evidence/round5/` for round 5, `~/orch-pr10/repair-314-r6-evidence/` for round 6, `~/orch-pr10/repair-314-r7-evidence/` for round 7, and `repair5/integration/` in this directory for the merge's receipts) record an independent verification of the finalised manifest
against this report's head. Run 1's, run 2's and run 3's directories are separate
and are cited nowhere as this run's evidence; where run 3's numbers appear they are labelled as run 3's and are a
cross-check, never the evidence.

## 16. The verdict against the pass rule, clause by clause

The rule, verbatim from the regenerated definition (`spec/gate4-v17.json`, `pass_fail_rule`):

> pass iff bounded census family complete (the prefix at its stated ceiling with truncation reported, every seeded
> per-dimension census closed, every bound reached by equality over the union) with totality asserted over the union
> and every plan_transition arm covered, site bijection complete with executed evidence for every Topology/Shared
> site, order, and parent-executed point (incl. OpenLog SyncPrefix and the stable-prefix barrier refusal cases), the
> two-crash proof green, residue-class evidence complete and labeled, all rows green, projections equal,
> per-resource ledgers satisfy their outcome equations incl. derived outcome, closure, and finalization, questions
> answered, no open critical/high finding; unbounded closure, effect-phase census, and parent-observed Git-internal
> execution claims are not made

**Nine** positive clauses and **three** prohibitions.

| # | clause | grade | how |
|---|---|---|---|
| 1 | bounded census family complete, totality over the union, every arm covered | **established** | §7: the prefix at its ceiling with truncation reported; the three seeded members closed; every bound of the amended line reached by equality over the union (**F29**, **F30**), plan shapes included; totality and the arms asserted over the union; the summary's hash identical across two Linux exports and the guest (**F28**). The limits it carries are named there: the union reaches the numeric bounds through the prefix, and assertion 5's named instances are this run's own measurement (**F31**) rather than a committed assertion |
| 2 | site bijection complete with executed evidence for every site, order and parent-executed point | **established** | §8. Observed execution of every site, phase and point in every mode, and a registry entry for every observed order, are established on both hosts (**F26**). ST-07's per-entry execution is established for every executed entry (**F19**; **F20** is empty): the four coordinates run 4 held back are credited to committed witnesses its search had excluded, adjudicated by execution in repair round 1 (§8.5, **F54**). **No row is near-exact at this range**: run 3's near-exact family is repaired and the repair is measured per recovery (**F25**), and the planted-pin question its remainder leaves is measured and answered (**F24**) |
| 3 | the two-crash proof green | **established** for the simulated loss, with the physical-sync boundary carried by `PR5-C-FSYNC-UNOBSERVABLE` | §9: both variants green, with the bytes on disk before and after each loss (**F33**, **F34**); two mutations kill them; its witness now runs over the prefix the run's first resume leaves, and the credited recovery repairs none of the creation prefix. The loss is simulated by the test, and the physical sync is unobservable to it: with the `fsync` call removed and its ledger entry kept both variants still pass (`repair2/probes/two-crash-without-fsync.log`), which is the deferred finding's own statement reproduced, stated here beside the credit (§13) |
| 4 | residue-class evidence complete and labeled | **established** for the nine declared classes, under §10's conditions | §10: synthetic construction per element and the sampling histograms of five runs per sampler on each host (**F45**, **F46**). One residue outside the declared classes is named, not counted (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`) |
| 5 | all rows green | **established** | Four full Linux suite runs at this range and one on the guest, all exit 0 with **no red** (**F9**, **F10**, **F11**, **F12**), agreeing with CI's three platforms (**F14**, **F15**, **F16**); the nine group invocations green (**F37**); the ST-07 merge check zero failures on both hosts (**F26**); every ledger row's verdict `yes` (**F36**). No red at this range needed a flake attribution or a merge-base control, and that is searched rather than asserted (**F50**): every `FAILED` and `panicked at` line of all five suite logs accounted for, and each listed flake searched by its own fingerprint, none appearing as a failure. The reds this run saw at the heads before this report's own — **F51**'s population, which closes at round 6's first head, round 6's one attempt at its final head `00a16a64`, green, after it (**F72**), and round 7's two at its first head `50c0410b`, the first red on two tests — an observed production cleanup-ownership refusal and an observed count-budget Docker timeout, each test passing alone in its control — and the second green (**F75**); the attempts at this report's own head are written after its commit and are outside every figure by construction (§15.2) — were outside the range's own suite: two `real_docker_*` tests in the probe's 2,736-process run under a neighbouring session's deliberate load, which did not recur on the cleared box (§1.3); this run's own guest sampler script, whose first attempt set out to rebuild with full debug information the guest had no room for, and whose re-run is green (**F46**, §10); and the test legs of eleven gate attempts, every red attempt **F51** lists: three at the heads up to repair round 4's — run 4's attempt 3 and repair round 1's first two at its own head — each on the one test `a_host_integration_reaper_holds_the_runs_cleanup_lease` under the whole suite, the three sharing one fingerprint (**F52**), attributed to its deferred finding and controlled (**F53**, §15.2); and repair round 5's seven at its pre-merge head `4611b204`, whose `src/` is the range's, red on the descriptor-hold test in four, that same reaper assertion in three and the container-launch-outages refusal in two — each a fingerprint on the run brief's known-flake list, its test passing alone in every control (**F67**, **F68**) — and, in attempt 1 alone, on the two terminal-signal tests that round's own asynchronous launch caused and demonstrated, which are no flake of the range's (§15.2); and repair round 6's attempt 1 at its first head `06a2b8a1`, on the reviewed master's `src/`, red on that same container-launch-outages refusal — production's cleanup-ownership refusal, a listed fingerprint, the test passing alone in its control, which establishes neither the cause of the full-suite failure nor that it is benign (**F71**) — the head then passing all ten on its second attempt. Ten of the eleven are attempts at heads before the merge of the reviewed master, on the range's `src/`; the eleventh is at round 6's first head, on master's. Repair round 1 counted a single attempt here, and both lenses caught it; repair round 5 left three standing after its own seven, and the fifth review caught it |
| 6 | projections equal | **established** | §11: **F32**, the byte snapshots at the probe's snapshot points — the step-level comparisons at three prefixes and the two-crash points, not every compared prefix, printed by four of the six tests that pass under the probe (**F58**) — and the per-append equivalence resting on the committed test's assertions; `pr10`'s three `projection-*` recipes and `g5r2r5`'s are killed (§5). Repair round 1 narrowed §11 and left this cell with the scope §11 had withdrawn, and all three lenses caught it |
| 7 | per-resource ledgers satisfy their outcome equations incl. derived outcome, closure and finalization | **established** for the six outcome tables as measured | §12: **F35**, **F36**. Convergence without an operator is **not established** in the filed shapes `PR5-RD-003-A-PRUNE-STRANDS-A-CHECKOUT-WHOSE-GITDIR-IS-GONE`, `PR5-RD-002-TORN-REMOVAL-NOT-DURABLE`, `PR5-RD-002-RESUME-DERIVES-THROUGH-A-TORN-ENUMERATION`, `PR308-R3-SKIPPED-PRUNE-KEEPS-ANOTHER-RUNS-TORN-REGISTRATION` and the two `PR8-CRASH-002` residuals; none was re-executed here. Their refusals are resumable and lose nothing, so no equation is falsified |
| 8 | questions answered | **asserted** | §14. Q1 partially, Q2, Q3, Q4 and Q6 answered with evidence for G5's portion. **Q5 is answered, and not in the affirmative**, for one reason: its executed-evidence half is clause 2's and is established (§8.5), but its wrapper-classification limb fails even as amended, because the carve-out v17 draws — the files carrying a file-level allowance, **F42**, derived from the source at the range and enumerated in full in §14 — does not bound the class. Three of the classified modules outside it (**F43**) carry the bypass: two executed by run 4 with both controls (**F44**) and filed as `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT`, and a third, `src/agent/bin.rs`, that run 4 had counted inside the carve-out by its allowlist row, found by the fourth review and executed again in repair round 5 (**F63**) |
| 9 | no open critical/high finding | **established on the ledger's own terms, weakened by deferrals** | §13: **no finding is filed `open`** (**F39**). Of the deferred P1s (**F41**), one is on a mechanism this gate certifies — `PR7-WRAPPERS-EMPTY-DOMAIN`, the wrapper classification — and four bear on this gate's conditions or on a boundary it does not certify. `PR5-RD-002`, the other P1 run 3 named here, **is closed in this range**. All are recorded as weakening the pass, not treated as closed |
| 10 | unbounded closure claims are not made | **not made** | §7 claims closure only of the three seeded members, each under its own ceiling and restricted generator |
| 11 | effect-phase census claims are not made | **not made** | §7; effect-phase evidence is the registry's (§8), and the census makes no effect-phase claim |
| 12 | parent-observed Git-internal execution claims are not made | **not made** | §10: every residue class is labelled recovery-proven, never observed execution, and the tree refuses a residue-class entry carrying an executed-hook claim |

### The verdict

**G5 does not pass on range `9bb177ea59e1f97b8f3b3282c19519a31e1253a4`.** **Clause 8 is asserted**; the other eight
positive clauses, clause 2 among them, are established, and the three prohibited claims are not made. The rule is a
conjunction, so the one decides it. **PR11 and PR12 stay blocked by G5.** Run 4 also graded clause 2 not established;
repair round 1 re-derived that grade from the committed suite and it is established (§8.5), so the verdict's reason changed
and its outcome did not. **Gate 5 now fails on one clause, not two**, and the remaining one is Q5's wrapper limb (§14): a
classified module can carry neither an allowance nor a fence, and three do — two found by run 4, the third by the fourth
review after run 4 had counted it by its allowlist row. This report describes the range `9bb177ea` and nothing after it;
whatever may land for those three modules elsewhere is outside the range and is not credited here.

**Repair round 2 moved no grade.** The three lenses that reviewed repair round 1 agreed with the verdict, the clause grades
and the credits; what this round corrected is what the report said about its evidence. Two grades gained a stated limit
without changing: clause 3 carries the physical-sync boundary (§9), and §4's group 1 is graded as the definition words it —
asserted as a literal kill matrix, established under ST-07's standard — which is not a clause of the pass rule. The rule is
decided by clause 8 alone, as before.

**Repair round 4 moved no grade either.** What it corrected is the strength this report gave five of its own statements —
a receipt, an audit's population, a header claim, a count and a description (the header lists them) — and none of the five
is an input to a clause grade: the #311 pin mutation never bore on clause 2, whose credits for rows 41 and 42 are the
unmutated witnesses' own kills (§8.4); the audit's population bears on how many logs were read for a false receipt, and the
re-run over the whole enumerated directory found none new (**F57**); the probe's provenance is in its build log (the header);
the survivor count is the pull request body's (**F62**); and the adjacent-construction description changes no row's grade
(**F60**, §8.3). The rule is still decided by clause 8 alone.

**Repair round 5 moved no grade either.** The fourth review found the carve-out counted from the allowlist's rows where
v17 names the files' own attributes, and the count was wrong by two files and one classified module (§14, **F42**,
**F43**). Clause 8 was asserted because the carve-out does not bound the class, and the corrected carve-out bounds it less:
three classified modules outside it carry the bypass, not two, and the two run 4 executed are among the three (**F44**,
**F63**), so the reason stands strengthened and the grade is the same — an affirmative answer was never on the table at
this range. The required audit's refreshed descriptions and call-path citations change no obligation's grade (**F65**,
§8.3), and the header's provenance sentence bears on no clause. The rule is still decided by clause 8 alone. Nor does the
merge of the reviewed master into this pull request (§15.2, **F66**) move anything here: it is not in the range, no figure of
the verdict reads it, and the gates at this report's head validate the merged tree and not the range.

**Repair round 6 moved no grade either.** The fifth review found five statements of this report's record contradicted by
their own receipts — where the historical executions ran (§1.3, **F69**), what the integration receipt regenerated (§1.3,
§15.2), how many gate attempts went red (§2.1 and this table's clause-5 row, **F51**, **F67**, **F71**), which of round 5's reds were
listed flakes and which its launcher made (the header, §15.2, **F68**), and the body's control total (**F68**) — and two
blanket sentences about what every receipt and every path names; none is an input to a clause grade. The eleven red attempts
**F51** lists are gate-leg reds — ten at heads whose `src/` is the range's, one at round 6's first head on master's — each
controlled alone (the attempts at this report's own head are outside every figure by construction, §15.2), and clause 5's
grade is **F50**'s search over the five suite logs, as before; the execution trees change no credit, since every worktree head up to `4611b204` carries the
range's `src/` (**F69**); and the regeneration receipt's correction changes no figure, since every figure regenerates
byte-identical against the range checkout (`repair6/regeneration.log`). The rule is still decided by clause 8 alone.

**Repair round 7 moved no grade either.** The sixth review found this report's account of its own repair rounds contradicted by
their receipts — two rounds' execution summaries (the header, the body), the body's restatements of **F69** and **F70**, the
body's review history and the per-leg claim (§15.2) — and none is an input to a clause grade: the executions the summaries
omitted are gate attempts and controls already counted (**F51**, **F67**, **F68**, **F71**) and one green attempt (**F72**); the
populations the body misdescribed are the figures' own, unchanged (**F69**, **F70**); the review history is the body's; and which
attempts keep per-leg logs (**F73**) changes no attempt's recorded outcome, every attempt line being its runner's own. Nor does
the round's own red at its first head (**F75**), a gate-leg red on the reviewed master's `src/`, controlled and counted, or the
correction of **F73**'s population, which changes no attempt's outcome either. The rule is
still decided by clause 8 alone.

**What stands in the way, and what it takes.**

1. **Clause 8 — Q5's wrapper limb, and the reason it is not the one run 3 gave.** Run 3 asserted this clause
   because `PR7-WRAPPERS-EMPTY-DOMAIN` was a demonstrated, unfixed bypass of a completeness Q5 then asked for
   unqualified. The packet was amended for exactly that, and the amendment is the right shape: it carves out the
   files that carry an allowance and declines completeness over them. **The carve-out is enumerated here as v17
   requires, and it still does not bound the class**: a classified module can carry neither an allowance nor a
   fence, and three do — the filed finding names two of them, and the third is §14's correction of its row-based count.
   Whether to widen the carve-out, fence those three files, or add the guard that would make the shape fail closed is
   the owner's call; §14 and the filed finding set out the three remedies.

**What the failure does not say.**

- **Clause 2 is established, for the first time in four runs.** Run 3's two failing families are closed — the
  attempt-internal coordinates now recover through the production resume; the staging, pin, candidates-ref and plan-written
  coordinates have real kills; rows 41 and 42 are credited to kills inside the promotion; and **no row is near-exact** — in
  **every** one of run 3's near-exact rows the credited recovery enters no `RunDir.RemoveMarker` (**F25**), and no credited
  coordinate is credited to a cell whose planted candidate pin a resume repairs (**F24**) — and the four coordinates run 4
  held back are credited to witnesses that were in the tree, executed in repair round 1 and controlled by mutation — rows 9
  and 10 in round 1, rows 88 and 89 in round 2, after round 1's control for them proved to be a rejected invocation (§8.5).
- **Clause 1, which failed runs 1 and 2 and passed run 3, is established again here** on this run's own execution.
- **The suite is green everywhere at this range**: four Linux runs, the guest, and CI's three platforms, with no
  red to attribute.
- **The defects the reviews of run 4 and of repair round 1 found in this record are corrected, not argued away**: the
  record is finalised after the last gate attempt (§15.3), the reaper-hold rows are described as they are graded (§15.2),
  the no-red search, the mutation check passes and the control's revision are described as they ran (§2.1, §5, §15.2);
  and repair round 2 corrected what round 1's corrections had left standing in the summaries — the snapshot scope in this
  table and in §14 (§11 had been narrowed), the fault firings behind #305's credit (§6), the count of gate reds (§2.1,
  §15.2, this table), the guest figures with no saved source (§10) and the header's log-provenance claim — replaced the
  one receipt that was not a receipt (§8.5), graded group 1 as the definition words it (§4), and judged the carried
  findings §13 had only listed; and repair round 4 corrected what the third review found still overstated — the #311 pin
  receipt (§5, §6), the log audit's population (§5), the header's probe-log claim, the body's survivor count and §8.3's
  adjacent-construction description — and searched again (`repair4/claim-sweep.txt`); and repair round 5 corrected what
  the fourth review found — the carve-out counted from the allowlist's rows rather than from the files' own attributes
  (§14, **F42**, **F43**, **F63**), the required audit's stale ref-store description and call-path citations (§8.3, **F65**)
  and the header's provenance sentence that put the range control outside the range — and searched again
  (`repair5/claim-sweep.txt`); and repair round 6 corrected what the fifth review found — the execution provenance (§1.3,
  **F69**), the integration receipt's attribution (§1.3, §15.2), the count of gate reds (§2.1, this table), the launcher
  reds folded into the known-flake list (the header, §15.2), the body's control total (**F68**) and two blanket sentences
  (the header, §15.2) — and searched again (`repair6/claim-sweep.txt`); and repair round 7 corrected what the sixth review found —
  the rounds' execution summaries (the header, the body), the body's **F69** and **F70** restatements, the body's review history
  and the per-leg claim (§15.2, the body, the evidence index) — and searched again (`repair7/claim-sweep.txt`). Every narrowed
  claim was searched for across the whole report and the pull request body after each round's edits (`repair2/claim-sweep.txt`,
  `repair4/claim-sweep.txt`, `repair5/claim-sweep.txt`, `repair6/claim-sweep.txt`, `repair7/claim-sweep.txt`).
- **All of it is test-suite execution.** The shipped binary does not reach the schema-4 engine at this range
  (§1.4). The evidence is Linux and the Windows guest; macOS is read only from CI's logs at the range.
