# G3 — cumulative review gate: integration transactions

**Gate:** G3 — *Integration transactions: queue, CAS, all terminal shapes, atomic registration*
**Frozen input range:** merged PR8 on top of G2 — `9cfaf0c885644fe9ce0283624bdfe42194fe0545`
(`origin/master` at the time of the run; PR #247, merged 2026-09-08).
**Blocks:** PR9 and later may not merge until G3 passes.
**Run started:** 2026-09-08. **Reviewer:** Claude Opus 5 session `g3`, worktree `/srv/worktrees/g3`,
branch `gate/g3`.

> **Where this file belongs is the owner's call.** The packet's convention is
> `reviews/<date>-gate-<id>.md` and that is why it is here. Historical review records were moved to
> the private lab repository on 2026-09-04 and the shape of `reviews/` is under reconsideration, so
> this path is the packet's convention, not a claim that it is the right long-term home.

---

## 0. Status of this report

This section is the plan and the progress ledger. It is written before the work and updated as each
piece lands, so that at any commit the record is true. A partial gate honestly reported is worth
more than a complete-looking one that overclaims.

### Reading of the remit

G3 is **not a diff review**. The packet's process clause is binding: *"the integrated tree at the
frozen input range is reviewed as a whole … a gate never reviews only the latest diff."* So the unit
of review is the tree at `9cfaf0c8`, and PR8's diff is evidence about that tree, not the subject.

A gate *"may claim only resources and invariant portions introduced in its frozen range, naming the
portion and the inventory rows."* Every claim below therefore names the portion and the row.

### Planned order, and why

| # | Work | Why here |
|---|---|---|
| A | Baseline: the ten-command gate suite at the frozen sha | Long-running; started first so it overlaps the reading. It is the packet's *"cumulative test suite up to that gate runs on that exact range"*. |
| B | Test inventory: map every packet-named test for T-FAST/T-PROPOSAL/T-VERIFY/T-PREPARED/T-REJECT and PR8's `proof_tests` onto tests in the tree | Cheap, read-only, and it is the skeleton of artifacts #2 and #6. Establishes what is *claimed* before anything is *verified*. |
| C | The fourteen adversarial fault tests, executed | The core of the pass rule. Each verified by running, and where the claim is that a test *detects*, by mutating the code and observing the named test fail. |
| D | Artifacts #3 (ref/object inventory incl. the fast-path no-object proof), #4 (replay-twice), #5 (one-linear-commit-per-task) | These are the concrete proofs the pass rule names by name. |
| E | Q1–Q6 with evidence | The bulk. Ordered Q5, Q4, Q3, Q6, Q2, Q1: Q5 is mostly mechanically checkable and gates the others' vocabulary; Q1 is the widest and most of it belongs to ranges earlier than G3, so it is scoped last. |
| F | Verdict against the pass rule, with reasoning | Last, so it is a conclusion and not a premise. |

### Progress

| Step | State |
|---|---|
| A baseline suite | **done** — ten gates green at `9cfaf0c8`, 2387 tests passed, 0 failed (§1) |
| B test inventory | **done** — 26 packet slugs mapped: 10 verbatim, 14 under prose names, 2 absent by design (§2) |
| C detection by mutation | **done** — 20 mutations, 19 caught, 1 provably-redundant survivor (§3) |
| C fourteen adversarial tests | **done** — all fourteen tabled with results and detection witnesses (§7) |
| D artifacts 3/4/5/6 | **done** — ref and object inventory with the fast-path no-object proof (§4); replay-twice and the measured one-linear-commit-per-task history (§5); the eight-row terminal-shape table (§6) |
| E Q1–Q6 | **done, with one scope limit stated** — Q2, Q3, Q4, Q6 answered with evidence for G3's portion; Q1 for its three G3 portions; **Q5 for G3's portion only** — the whole-inventory effect-site bijection is not claimed and cannot be at this range (§8) |
| F verdict | **done** — G3 passes, with the scope of the pass stated and its dependence on the owner's two deferrals named (§10) |
| — two defects found | **reported, not repaired** — the GitHub Pages Jekyll build of `docs/` fails on master, so upstroke.rs is not publishing (P2, pre-existing, outside PR8: §9 O5); and `pr8-plan.md`, a tracked 100 KB plan still at the repository root, has an R12 paragraph the code contradicts (P3, in range: §9 O6). Neither bears on the pass rule |

**Everything below this line is finished work.** The report is complete: all six required artifacts are
produced, the fourteen adversarial fault tests are run and tabled, Q1–Q6 are answered with the scope of
each answer named, and §10 states the verdict against the pass rule with its reasoning.

### Known deferrals carried into this gate (not re-opened here)

Two gaps are deferred by owner ruling with standing findings in `findings/`:
`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002`. Per the gate brief they are neither reopened, repaired
nor narrowed, and neither fails the gate on its own. They are read in full and reported in **§9** as
the known deferrals they are, and §10 states plainly that this verdict rests on them.

### Method notes that bind the evidence below

- **Nothing is taken on trust from a prior record, and in the event nothing was taken at all.** PR8's
  archived triage (`~/tactus-artifacts/pr8-records/pr8-triage.md`) carries eight rounds of mutation
  tables, and the fourteenth frontier review (`~/review-pr247-70789a8-r8merge.md`) executed five
  mutations. **Neither was used as a source.** The twenty mutations in §3 were designed from this
  reviewer's own reading of `integrate.rs`, `check_integration.rs`, `events.rs`, `apply.rs` and
  `events/log.rs`, and each was applied and measured here. That is deliberate: reusing a prior round's
  mutation catalogue measures the catalogue, not the tree.
- **The frontier review's two open follow-ups were checked independently and neither blocks.** The
  misleading prompt string is real and was met without looking for it: `park_question`
  (`integrate.rs`) builds a `HumanRequired` park with `QuestionKind::Clarify`, whose text asks the
  reader to clarify when what is wanted is a decision. It changes no state, no relation and no
  refusal — the question's `id`, `key`, `options` and `context` are all complete and validated — so it
  is a wording defect in a prompt, not a G3 site. The `pr8-plan.md` misstatement **is** in this tree —
  see O6 below, where checking it turned up something the follow-up did not mention.
- **Mutation is the detection test.** A green suite proves the tests pass, not that they still
  detect. Every "test X guards Y" claim below is backed by a mutation of Y observed to kill X.
- **Verify by hash, not by line count**, and diff against a known-good state before trusting a tree
  a killed worker touched.
- **Every mutation in this report is a Linux mutation.** All 20 were applied and measured on this box
  (`x86_64-unknown-linux-gnu`, kernel 6.8.0); none was re-run on the Windows guest. That is stated
  because a mutation's outcome can be platform-shaped, and a reader should not read these tables as
  three-platform results. The **unmutated** tree, however, is green on all three: at the frozen sha the
  commit's own check runs report `test (ubuntu-latest)`, `test (macos-latest)` and `test (winguest)` all
  success, with `lint` and `msrv` green on ubuntu, macos and windows. What the mutations bound is
  *detection*, and the mechanisms they probe — the fold's refusals, the barrier's byte comparison, the
  ref relations — are platform-independent by construction; the one place platform could matter,
  path-shaped and process-shaped behaviour, is not what any of the 20 touches.

---

## 1. Step A — the cumulative suite on the exact frozen range

The packet requires *"the cumulative test suite up to that gate runs on that exact range."*

**Run:** `w1-eight-iso /srv/worktrees/g3` at `9cfaf0c8` (clean tree; `git status --porcelain` empty),
2026-09-08T13:35:14Z, plus the tenth gate run separately (see below). Logs `~/eight-logs/9cfaf0c/`.

| # | Command | Result | Time |
|---|---|---|---|
| 01 | `cargo fmt --check` | PASS | 1s |
| 02 | `cargo clippy --all-targets --all-features -- -D warnings` | PASS | 19s |
| 03 | `cargo test --all-targets --all-features` | PASS | 62s |
| 04 | `cargo +1.85.0 check --locked --all-targets --all-features` | PASS | 15s |
| 05 | `test-release-record.sh` | PASS | 0s |
| 06 | `test-pr-policy.sh` | PASS | 0s |
| 07 | `test-pr-ledger-evidence.sh` | PASS | 0s |
| 08 | `test-docs-consistency.sh` | PASS | 0s |
| 09 | `test-internals-notes.sh` | PASS | 1s |
| 10 | `test-pr-ready-audit.sh` | PASS | 1s |

**Suite scope, measured not assumed.** `2419` tests declared in the lib binary; `2376` passed, `43`
ignored, `0` failed; `+10` in the `main.rs` binary and `0` in the `probe` example — `2387` passed in
total. The 43 ignored are all `subprocess helper` entry points, invoked as children by their own
tests rather than run directly. **Platform: Linux** (`x86_64-unknown-linux-gnu`, kernel 6.8.0); the
count is per-platform and this number is the Linux one.

**Docker-gated lanes really ran:** `docker ps` succeeds on this box and 213 `runner::container::*`
test lines appear in the log, so the container lanes are evidence, not skips.

**The binary measured *this* tree.** `w1-eight-iso` was used rather than `w1-eight` deliberately.
`repo_root()` bakes `env!("CARGO_MANIFEST_DIR")` at compile time and `upstroke-build`'s slot pool is
shared across source paths, so a shared slot can serve a binary whose baked root is a *different*
worktree and every census then measures the wrong tree while reporting green. Verified rather than
assumed: the test binary
`/mnt/ramtarget/iso-g3/slot1/debug/deps/upstroke-1f05c71869a3a005` contains the string
`/srv/worktrees/g3` and contains neither `/srv/worktrees/pr8` nor `/srv/tactus`.

**One observation, not a finding.** `upstroke-eight` runs nine commands; this head carries ten gates
— `test-pr-ready-audit.sh` is present in `.github/scripts/` and named in `CLAUDE.md`, but the
wrapper does not invoke it. It was run separately here and passes. The wrapper is one gate behind the
head; that is an operator-tooling gap on a box script, outside the repository under review, and is
recorded here only so the tenth gate's result is not mistaken for the wrapper's.

**Re-run at this report's own head.** The same ten gates were run again at each head that added a
section of this report — `811d8d2c`, `e07d76ef`, `f3fb217b`, `5061b060` and `4bc82bd6`, the frozen
tree plus this report — and all ten pass at each (`ALL 9 PASS at <sha>` from the wrapper, plus
`test-pr-ready-audit.sh` separately, `PASS 10`). That is a check on this report, not on the range: it confirms the
report file itself breaks no gate, in particular `test-docs-consistency.sh`, whose rooted-path check
reads every backticked `src/…`-style path in a document. Every such path in this report was
additionally existence-checked by hand.

**Step A verdict: the cumulative suite is green on the exact frozen range.** This is a necessary
condition of the pass rule and not a sufficient one: a green suite proves the tests pass, not that
they still detect. Detection is established by mutation in §3.

---

## 2. Step B — what the tree claims: the packet's test names against the tree

The packet names tests as descriptive slugs; the tree names them as prose. Neither is authority for the
other, so the mapping below was derived by searching the tree rather than by trusting a name match.
Of the 26 distinct test slugs the G3 entry, the five fault rows and PR8's `proof_tests` name, **10
exist verbatim** as functions in the tree, **14 exist under prose names**, and **2 do not exist at
all** — correctly so, for the reason set out after the table.

| Packet slug | In the tree as | File |
|---|---|---|
| `fast_path_publishes_exact_candidate_without_staging_or_proposal_object` | verbatim | `src/engine/topology/integrate/tests.rs:66` |
| `merge_prepared_fast_with_moved_head_or_wrong_proposed_or_pin_refused_live_and_on_replay` | verbatim | `src/engine/topology/integrate/tests.rs:257` |
| `stale_candidate_takes_staging_path_and_publishes_pinned_proposal` | verbatim | `src/engine/topology/integrate/tests.rs:542` |
| `fast_dual_holding_released_once` | verbatim | `src/engine/topology/integrate/tests.rs:215` |
| `infrastructure_failure_defers_then_parks_at_max_defers` | verbatim | `src/engine/topology/integrate/tests.rs:1150` |
| `unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing` | verbatim | `src/engine/topology/recover/tests.rs:6508` |
| `barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` | verbatim | `src/engine/topology/recover/tests.rs` |
| `synthetic_cherry_pick_residue_…_forced_reclaim_converges` | verbatim | `src/engine/topology/recover/tests.rs` |
| `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` | verbatim | `src/engine/topology/recover/tests.rs` |
| `declined_parked_verification_fails_task_consumes_queue_position_releases_lease_and_halts_per_policy` | verbatim | `src/topology/fold/tests.rs` |
| `third_sha_refused` | `third_sha_refused_and_a_ref_already_at_the_proposal_only_records`; `a_resume_of_a_prepared_transaction_whose_ref_moved_elsewhere_refuses_a_third_sha` | `integrate/tests.rs:434`; `recover/tests.rs` |
| `already_present_recovery_no_empty_commit` | `an_already_present_candidate_settles_without_an_empty_commit`; `a_resume_completes_an_already_present_publication_at_the_candidate_commit_and_reclaims_its_staging`; `an_already_present_publication_expects_the_head_its_verification_read` | `integrate/tests.rs:922`; `recover/tests.rs` ×2 |
| `orphan_prepared_pin_reclaimed_only_at_next_sequence` | `a_resume_reclaims_the_orphan_pin_at_the_next_sequence_and_orphan_staging` | `recover/tests.rs` |
| `human_required_verdict_parks_task` | `a_human_required_verdict_parks_the_task` | `integrate/tests.rs:1110` |
| `defer_wait_elapsed_reenables_deferred_candidate` | `deferred_task_woken_by_defer_wait_elapsed_or_resume` | `engine/topology/settle/tests.rs` |
| `kill_during_merge_verification_settles_interrupted_and_reverifies` | `a_resume_settles_an_interrupted_stale_verification_and_reclaims_its_residue` (`recover/tests.rs:6727`); `a_resume_reclaims_an_interrupted_verifications_snapshots_after_settling_it` (`:6812`) | `recover/tests.rs` |
| `staging_residue_reclaimed` | `assert_staging_residue_reclaimed` (a shared assertion helper used by the residue tests) | `recover/tests.rs` |
| `merge_prepared_stale_clean_with_unpinned_proposed_refused` | `only_a_stale_clean_publication_pins_a_proposal` (`fold/check_integration.rs:902`); `unpinned_object_never_adopted_on_resume` (`candidate/tests.rs`) | as named |
| symbolic / checked-out refusals | `a_symbolic_or_checked_out_integration_ref_refuses_before_any_append`; `a_resume_refuses_a_symbolic_or_checked_out_integration_ref`; `an_integration_ref_checked_out_in_a_second_worktree_is_refused`; `a_symbolic_pin_refuses_on_both_the_write_and_the_read` | `integrate/tests.rs:360`; `recover/tests.rs`; `workspace_manager/tests.rs` |

`engine::topology::integrate` alone holds **20 tests, all passing** at the frozen sha.

**Two slugs have no test in the tree, and should not.** T-FAST's `test:` field names
`authorized_publication_completed_at_run_end (ST-17)` and T-PREPARED's names
`prepared_publication_completed_at_run_end (ST-17)`. Neither exists, because their *subject* does not
exist at PR8: `grep -rn 'TopologyEventBody::RunFinished' src/` finds the fold's `check_run_finished`
and `apply`, a census fixture and the wire form — and **no production emitter**. `run_finished`, the
closure procedure and terminal finalization are `pr_sequence[PR10]`'s scope by name, and both slugs
carry the `ST-17` tag that is PR10's own gating. A run cannot end in this build, so a test that a run
end completes an authorized publication has nothing to assert. What *is* in range — completing an
authorized publication at the next **process start** — is `finish_integration`, and it is bound to the
right rows by a census: `the_recovery_order_hands_the_run_on_rather_than_dropping_it`
(`src/engine/topology/recover/tests.rs`) pairs the name `finish_integration` with the text
*"transaction_fault_matrix[T-FAST/T-PREPARED/T-VERIFY].resume_action: complete an authorized
publication after the barrier, or settle a verification interrupted"*.

**One gap in the tree's own coverage table, stated plainly.** The single test that calls itself the
terminal-shape coverage table,
`terminal_shape_coverage_table_drives_every_shape_and_each_converges_on_replay`
(`integrate/tests.rs:732`), iterates `EVERY_INTEGRATE_SHAPE`, a `[Shape; 7]` over
`Fast, StaleClean, AlreadyPresent, Conflict, CodeRejected, Deferred, Parked`. The packet's required
table has **eight** rows: it also requires *Declined-after-park* and *interrupted*. Those two are
covered by tests elsewhere (`declined_parked_verification_fails_task_consumes_queue_position_releases_lease_and_halts_per_policy`
in `src/topology/fold/tests.rs`, and the two interrupted-verification resume tests in
`recover/tests.rs`), so the coverage exists — but it is not in the table the tree names as the table,
and the `Shape` enum has no variant for either. §6 assembles the required eight-row table across the
tests that actually hold each row.

---

## 3. Step C part 1 — detection, established by mutation

A green suite proves the tests pass, not that they still detect. Ten mutations were applied to the
frozen tree, each one asserted to have changed the file (hash before → hash after, a no-op patch
aborts), the full suite run against the mutant, and the file restored and re-hashed. Full logs in the
session scratchpad; the harness refuses to run on a dirty tree so no mutation can be confused with
another.

| # | Mutation | File | Result | Killed by |
|---|---|---|---|---|
| M1 | `decide` always returns `ExactBase::Stale` (the exact-base decision removed) | `engine/topology/integrate.rs` | **caught**, 18 failures | incl. `fast_path_publishes_exact_candidate_without_staging_or_proposal_object`, `fast_dual_holding_released_once`, `terminal_shape_coverage_table_…` |
| M2 | drop `PreparedDefect::FastWithPreparedRef` | `topology/events.rs` | **caught**, 5 failures | `merge_prepared_fast_with_moved_head_or_wrong_proposed_or_pin_refused_live_and_on_replay` |
| M3 | drop the fast `expected_head == candidate base` check | `topology/fold/check_integration.rs` | **caught**, 7 failures | `merge_prepared_fast_with_moved_head_or_wrong_proposed_or_pin_refused_live_and_on_replay` |
| M4 | drop the fast `proposed_sha == candidate commit` check in `check_merge_prepared` | `topology/fold/check_integration.rs` | **SURVIVED**, suite green | nothing — see below |
| M5 | `publish` never issues the CAS | `engine/topology/integrate.rs` | **caught**, 33 failures | broad |
| M6 | drop `PreparedDefect::FastProposesAnotherCommit` | `topology/events.rs` | **caught**, 2 failures | the relation-refusal test |
| M7 | drop the `ThirdSha` refusal in `publish` | `engine/topology/integrate.rs` | **caught**, 3 failures | `third_sha_refused_and_a_ref_already_at_the_proposal_only_records`, `a_resume_of_a_prepared_transaction_whose_ref_moved_elsewhere_refuses_a_third_sha`, `a_resume_keeps_a_prepared_transactions_pin_when_publication_refuses` |
| M8 | drop the `ForeignHead` refusal in `decide` | `engine/topology/integrate.rs` | **caught**, 1 failure | `a_foreign_reset_of_the_integration_ref_refuses_before_any_append_and_keeps_the_merged_task` |
| M9 | drop `assert_publishable` from `decide` | `engine/topology/integrate.rs` | **caught**, 1 failure | `a_symbolic_or_checked_out_integration_ref_refuses_before_any_append` |
| M10 | drop the `PinAtAnotherSha` refusal in `prune_pin` | `engine/topology/integrate.rs` | **caught**, 2 failures | `a_rejected_or_unavailable_terminal_refuses_to_delete_a_pin_another_writer_substituted`, `a_resume_prunes_a_resolved_sequences_pin_at_its_recorded_proposal_and_refuses_it_elsewhere` |

### M4, the one survivor, is a redundancy and not a hole

The check M4 removes is **logically implied by two checks that both run before it** in the same
function, so no test can kill it:

1. `check_merge_prepared` opens by calling `MergePrepared::self_consistency()`, whose `Fast` arm
   refuses `FastProposesAnotherCommit` unless `proposed_sha == candidate_sha`.
2. It then calls `prepared_candidate(…, &prepared.candidate())`, which selects the recorded
   `PreparedCandidate` by `.filter(|prepared| prepared.candidate == *candidate)` — **full**
   `CandidateRef` equality, `commit_sha` included — and `MergePrepared::candidate()` builds that
   `CandidateRef` from `self.candidate_sha`. So reaching the third check at all requires
   `candidate_sha == candidate_record.candidate.commit_sha`.

(1) and (2) give `proposed_sha == candidate_record.candidate.commit_sha`, which is exactly what M4
deletes. The pair is *jointly* guarded — M6 shows that removing (1) alone is caught in 2 tests — so
the guarantee has a live witness; the third statement of it does not, and cannot. This is the
"redundant assertion defeats its own mutation" shape, not a coverage gap. **Not a finding.** It is
worth a one-line note in the source for the next reviewer, who will run the same mutation and reach
the same dead end.

### The second mutation round: the fold's own refusals

Ten more mutations, same harness, same protocol. **All ten were caught.**

| # | Mutation | File | Result | Killed by |
|---|---|---|---|---|
| M11 | `apply_candidate_created` no longer closes the generation before pushing the queue entry | `topology/fold/apply.rs` | **caught**, 63 failures | incl. `kill_after_candidate_prepared_appends_candidate_created_once`, `fast_path_publishes_exact_candidate_without_staging_or_proposal_object`, `fast_dual_holding_released_once`, `promoting_completed_at_run_end` |
| M12 | drop `TransactionAlreadyOpen` (a second unresolved transaction) | `check_integration.rs` | **caught**, 2 | `one_integration_transaction_at_a_time_including_an_authorized_one`, `sequences_are_dense_and_one_transaction_runs_at_a_time` |
| M13 | drop `NotFirstEligible` (overtaking allowed) | `check_integration.rs` | **caught**, 2 | `an_integration_starts_only_for_the_first_eligible_candidate`, `the_queue_is_ordered_by_creation_and_not_by_preparation` |
| M14 | drop `NonDenseSequence` | `check_integration.rs` | **caught**, 3 | `sequences_are_dense_and_one_transaction_runs_at_a_time`, `a_conflict_opens_and_closes_its_own_transaction`, `every_guarded_event_is_refused_the_same_way_live_and_on_a_hostile_replay` |
| M15 | drop the `Parked`-below-`max_defers` half of `check_defer_allowance` | `check_integration.rs` | **caught**, 2 | `an_outage_defers_consecutively_inside_the_allowance_and_parks_at_it`, `a_deferred_verification_is_consecutive_and_within_the_frozen_allowance` |
| M16 | let an `already_present` publication carry a pin | `check_integration.rs` | **caught**, 1 | `only_a_stale_clean_publication_pins_a_proposal` |
| M17 | drop `reread != normalized` from `establish_stable_prefix` (the barrier's byte-stability) | `events/log.rs` | **caught**, 1 | `an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle` |
| M18 | drop `merged_sha != proposed_sha` from `check_task_merged` | `check_integration.rs` | **caught**, 2 | `a_merge_copies_the_authorization_exactly`, `every_guarded_event_is_refused_the_same_way_live_and_on_a_hostile_replay` |
| M19 | `reclaim_staging` no longer removes the staging worktree or its intent | `engine/topology/integrate.rs` | **caught**, 6 | `a_conflicting_candidate_is_rejected_with_an_atomic_repair_before_any_repair_effect`, `a_human_required_verdict_parks_the_task`, `a_rejected_or_unavailable_terminal_refuses_to_delete_a_pin_another_writer_substituted`, three integration-outage resume tests |
| M20 | `unavailable` always parks, never defers | `engine/topology/integrate.rs` | **caught**, 9 | `infrastructure_failure_defers_then_parks_at_max_defers` and every outage-deferral resume test |

**Detection score: 20 mutations, 19 caught, 1 survivor that is provably redundant.** No mutation
revealed an unguarded refusal.

Two of the kills deserve to be named for what they are, because they are the pass rule's own clauses:

- `every_guarded_event_is_refused_the_same_way_live_and_on_a_hostile_replay` (`topology/fold/tests.rs`)
  is the mechanical proof that a refusal is *the same* live and on replay. Over three traces, at every
  position, it builds one-field-invalid variants, asserts `plan_transition` refuses **live**, then
  builds a hostile log that is the trace **with exactly one line replaced** (asserted, so the
  perturbation cannot degenerate), serialises it to the wire, parses it, replays it, and asserts the
  replay's error is **equal** to the live error. Four event kinds are declared deliberately unguarded
  and named. Killed by M14 and M18.

  **Its breadth, stated precisely rather than generously:** `one_field_invalid` perturbs a fixed pair
  of fields per event kind — for `merge_prepared` those are `expected_head` and `candidate_ref`, not
  `proposed_sha`, `prepared_ref` or `disposition`. So this census proves *refusal equality across live
  and replay* for the fields it moves; it does **not** by itself cover the whole T-FAST relation set.
  That set is covered by the dedicated test, next.
- `merge_prepared_fast_with_moved_head_or_wrong_proposed_or_pin_refused_live_and_on_replay`
  (`integrate/tests.rs:257`) is the complete T-FAST relation proof, and it is built the right way
  round. It first asserts the **honest** `merge_prepared(fast)` *is accepted* — a positive control, so a
  broken fixture cannot masquerade as three refusals — then forges the packet's three variants and, for
  each, asserts `plan_transition` refuses **live**, `TopologyFold::replay(log + forgery)` refuses, and
  the two errors are **equal**; then that the ref did not move and no CAS was issued. The middle
  forgery is the sharp one: it moves `proposed_sha` **and** `candidate_sha` together to a sibling's
  commit, which satisfies `self_consistency`'s `FastProposesAnotherCommit` and must therefore be caught
  by `prepared_candidate`'s full-`CandidateRef` equality — the same chain that makes M4 redundant.
  Killed by M2, M3 and M6.
- `a_merge_copies_the_authorization_exactly` is the proof that a `task_merged` cannot record a SHA
  the authorization did not propose. Killed by M18.

---

## 4. Required artifact #3 — ref and object inventory across tests, with the fast-path no-object proof

### The ref namespace the range can write

Every ref the engine writes lives under `refs/upstroke/runs/<run_id>/`
(`RUN_REF_ROOT`, `src/engine/topology/candidate.rs:22`). There are exactly **eight** `RefSite`
variants in the tree (`src/topology/effects/sites.rs`), and they are the whole of the group:

| Ref | Site(s) | Row | Introduced by | Fast sequence? |
|---|---|---|---|---|
| the run's integration ref (recorded in `run_started.integration_ref`) | `Ref.CreateIntegration`, `Ref.CompareAndSwapIntegration` | R21 persistent_output | PR7 / PR8 (the CAS) | **CAS executes**; created zero-old at P8 |
| `…/candidates/<key>/<gen>` | `Ref.CreateCandidates`, `Ref.DeleteCandidatesRef` | R11 | PR7 | untouched by integration; **remains** after a rejection |
| `…/candidate-prepared/<key>/<gen>` | `Ref.PinCandidatePrepared`, `Ref.DeleteCandidatePin` | R23 | PR7 | untouched |
| `…/prepared/<seq>` | `Ref.PinPrepared`, `Ref.DeletePreparedPin` | R12 | **PR8** | **never created** |

There is exactly **one** production call site of `compare_and_swap_ref` in the whole crate —
`src/engine/topology/integrate.rs:471`, inside `publish()` — and exactly one production call site of
`RefSite::CompareAndSwapIntegration`, the same line. Verified by grep over `src/` excluding test
modules. `publish()` is reachable from three places and no others:
`integrate.rs:548` (fast, immediately after `prepare_fast` appended `merge_prepared(fast)`),
`integrate.rs:746` (verified, immediately after `prepare_verified` appended `merge_prepared`), and
`recover.rs:1074` (`finish_integration`, whose `Authorized` comes from `Authorized::from_fold`, which
returns `None` unless the **proven prefix** holds a `TransactionClass::Prepared`). So *no code path
advances the integration ref without a durable `merge_prepared`* — this is a reachability property of
three call sites, not a convention.

### The object inventory

| Object | Created by | Referenced by | Accounting | Fast sequence? |
|---|---|---|---|---|
| candidate commit (+ its tree/blobs) | PR7's `git add` / `write-tree` / `commit-tree` | R23 pin, then R11 candidates ref | R27 → R23 → R11 | pre-exists integration; **the fast publication names this object and creates nothing** |
| proposal commit and the merge objects | `Object.ProposalCherryPick` (`git cherry-pick`) | staging HEAD/index (R10), then the `prepared/<seq>` pin (R12) | R10 → R12 → R27 | **never created** |
| objects a cherry-pick wrote before publishing its reference | the same command, interrupted | nothing | R27, Git/GC-owned, never adopted, never deleted by recovery | **never created** |
| ephemeral snapshot commits (verification) | PR7 | snapshot worktree HEAD | R24 → R27 | integration verification snapshots the **proposal or head commit** and creates **no object** |

`git cherry-pick` is invoked as a bare `cherry-pick` (`PROPOSAL_CHERRY_PICK_ARGV: [&str; 1] =
["cherry-pick"]`, `src/workspace_manager.rs:1612`), so a proposal is a **single-parent** commit; the
"merge objects" of the row are the trees and blobs the merge machinery writes, not a merge commit.

### The fast-path no-object proof, as executed

`fast_path_publishes_exact_candidate_without_staging_or_proposal_object`
(`src/engine/topology/integrate/tests.rs:66`) runs against a **real repository** and proves, in one
test:

1. `git count-objects -v` in the fixture repository, summed over `count` + `in-pack`, is **byte-equal
   before and after** the sequence. (`count-objects` is used rather than `fsck`; it is the stronger
   instrument for "no object was created" because it counts unreferenced loose objects too, which is
   exactly what a killed pick leaves.) The staging worktree, had one been added, would share this
   object store, so the measurement is in the right repository.
2. The hook harness recorded **no execution** of `Worktree.WriteStagingIntent`,
   `Worktree.AddStaging`, `Object.ProposalCherryPick` or `Ref.PinPrepared` inside the sequence, and
   **did** record `Ref.CompareAndSwapIntegration`. That is the packet's three plus the intent site.
3. No `merge/s<seq>` intent exists: `manager.intents()` contains no `Slot::Staging`.
4. The durable kinds appended by the sequence are exactly `["merge_prepared", "task_merged"]`.
5. `merge_prepared(fast)` carries `expected_head == run.base()`, `proposed_sha == candidate.commit_sha`,
   `prepared_ref == None`, `verification == None`, and
   `verification_source == CandidatePrepared{key, generation}` of the candidate itself.
6. The recorded hook order is `Append(merge_prepared)` **<** `CompareAndSwapIntegration` **<**
   `Append(task_merged)`.
7. At the end: task `Merged`, no open transaction, empty queue, `pipeline_held() == 0`, reservations
   balance, no candidate or lineage lease held, and `replay_twice_equal()`.

The independent census `only_the_three_sites_a_fast_sequence_skips_may_record_that_they_did_not_run`
(`src/topology/effects/tests.rs:1733`) pins `skipped_on_fast_path()` to exactly the packet's three
site names, so the *set* the test asserts about cannot silently shrink.

**Detection proven:** M1 (the exact-base decision removed, so every sequence stages) kills this test
along with 17 others. The proof is not vacuous.

---

## 5. Required artifacts #4 and #5 — replay-twice equality, and one linear commit per task

### #4 replay-twice equality

`Run::replay_twice_equal()` (`src/engine/topology/scaffold.rs:1287`) is not a self-oracle. It reads
the **on-disk** log (`durable_events()` = `std::fs::read(log.path())` then `TopologyFold::parse_log`),
replays it twice from `FrozenInputs`, asserts the two folds' `state()` equal, and then asserts the
**live incrementally-folded state equals the replay** — which is the load-bearing third assertion:
the live fold is built by `apply_delta` as events are appended, the replay by `TopologyFold::replay`,
and only agreement between two different code paths over the same bytes is evidence.

It is called at the end of every terminal shape in
`terminal_shape_coverage_table_drives_every_shape_and_each_converges_on_replay` (all 7 shapes),
at the end of the fast-path proof, and in the two-crash proof — where the replay-twice is over the
log that survived a **real** simulated power loss.

### #5 one linear commit per task — measured

Produced by a temporary, **uncommitted** measurement test driving both tasks the frozen scaffold plan
holds through the integration path in one real repository (`git status --porcelain` verified empty
before, both files' hashes verified changed by the patch, verified restored to the same hashes after;
the patch source is in the session scratchpad as `p-MEAS1.py`). Verbatim output:

```
G3-MEAS base=56dc1dd631a7043559a3f61a85630a4861efb8d3
G3-MEAS head=868a074517c65cedbd0ff4b5fd973dd8b1ee32d3
G3-MEAS commits_added=2
G3-MEAS task_merged_events=2
G3-MEAS merged seq=0 sha=399288a904796ca0e88b93dee2e5f993aa33a1e5 satisfies=[TaskKey(0)]
G3-MEAS merged seq=1 sha=868a074517c65cedbd0ff4b5fd973dd8b1ee32d3 satisfies=[TaskKey(1)]
G3-MEAS prepared seq=0 disposition=Fast expected=56dc1dd6… proposed=399288a9… pin=None
G3-MEAS prepared seq=1 disposition=StaleClean expected=399288a9… proposed=868a0745… pin=Some("refs/upstroke/runs/01SCAFFOLD00000000000000AA/prepared/1")
G3-MEAS commit=868a074517c65cedbd0ff4b5fd973dd8b1ee32d3 parents=1
G3-MEAS commit=399288a904796ca0e88b93dee2e5f993aa33a1e5 parents=1
G3-MEAS candidate_alpha=399288a904796ca0e88b93dee2e5f993aa33a1e5 candidate_beta=6e79ee8d07f731c5d6662f6db6b69e4c8575a9dd
```

What that measures, read off the numbers:

- `git rev-list --count base..head` = **2** and `task_merged` events = **2**: the integration ref
  advanced by exactly one commit per publication, with no extra commit and no empty commit.
- `git rev-list --parents` gives **parents=1** for both new commits: the history is linear. No merge
  commit is created on either disposition.
- Sequence 0 is `Fast` and its `proposed` **is** `candidate_alpha` — the fast publication names the
  immutable candidate commit its gates and reviews judged, byte-for-byte, and carries **no pin**.
- Sequence 1 is `StaleClean`: its `expected` is sequence 0's published head (FIFO, no overtaking),
  its `proposed` (`868a0745…`) is **not** `candidate_beta` (`6e79ee8d…`) — it is the cherry-picked
  proposal — and it **is** pinned at `…/prepared/1`.
- `merged_sha == proposed_sha` for both, and each `satisfies` closure names exactly its own task.

**Scope, stated rather than implied.** The frozen scaffold plan holds two tasks (`plan()` in
`scaffold.rs`: `tasks: vec![task_of("alpha"), task_of("beta")]`), so the measured history is two
commits deep — one of each disposition, which is the interesting pair. The property measured is
per-`task_merged`, and the fold makes it inductive rather than a two-sample coincidence:
`check_transaction_start` refuses any sequence that is not `next_sequence` (`NonDenseSequence`), any
start while a transaction is open (`TransactionAlreadyOpen`), and any candidate that is not the
queue's first eligible entry (`NotFirstEligible`); `check_task_merged` refuses a `merged_sha` that is
not the authorization's `proposed_sha`. The v0.1 sequential path's own multi-task linear-history proof,
`happy_path_commits_one_commit_per_task` (`src/engine/tests.rs:874`), is unchanged in range and
passes.

---

## 6. Required artifact #6 — the terminal-shape coverage table

The packet requires eight rows. Seven are driven by one test; two rows (Declined-after-park,
interrupted) live in other tests, and the CodeRejected/Conflict pair both realise "rejected".

| Required row | Realised as | Test holding it | Result |
|---|---|---|---|
| prepared **fast** | `PreparedDisposition::Fast`, `prepared_ref: None`, no staging effect | `terminal_shape_coverage_table_…` (Shape::Fast) + `fast_path_publishes_exact_candidate_without_staging_or_proposal_object` | pass |
| prepared **stale_clean** | `PreparedDisposition::StaleClean`, `proposed_sha` == the pinned proposal | `terminal_shape_coverage_table_…` (Shape::StaleClean) + `stale_candidate_takes_staging_path_and_publishes_pinned_proposal` | pass |
| **already_present** | `PreparedDisposition::AlreadyPresent`, `proposed_sha == expected_head`, no pin | `terminal_shape_coverage_table_…` (Shape::AlreadyPresent) + `an_already_present_candidate_settles_without_an_empty_commit`, `an_already_present_publication_expects_the_head_its_verification_read`, `a_resume_completes_an_already_present_publication_at_the_candidate_commit_and_reclaims_its_staging` | pass |
| **rejected** (conflict) | `merge_rejected{Conflict{paths}}` | `terminal_shape_coverage_table_…` (Shape::Conflict) + `a_conflicting_candidate_is_rejected_with_an_atomic_repair_before_any_repair_effect` | pass |
| **rejected** (code) | `merge_rejected{CodeRejected{verification}}` | `terminal_shape_coverage_table_…` (Shape::CodeRejected) + `a_code_rejected_candidate_registers_a_repair`, `a_failing_gates_own_output_reaches_the_frozen_repair_spec`, `a_rejecting_reviewers_required_change_reaches_the_frozen_repair_spec` | pass |
| unavailable **Deferred** | `merge_verification_unavailable{Infrastructure, Deferred{defers}}` | `terminal_shape_coverage_table_…` (Shape::Deferred) + `infrastructure_failure_defers_then_parks_at_max_defers`, `deferred_task_woken_by_defer_wait_elapsed_or_resume`, `an_outage_defers_consecutively_inside_the_allowance_and_parks_at_it` | pass |
| unavailable **Parked** | `merge_verification_unavailable{HumanRequired, Parked{question}}` | `terminal_shape_coverage_table_…` (Shape::Parked) + `a_human_required_verdict_parks_the_task`, `a_run_that_allows_no_deferral_parks_the_first_outage` | pass |
| unavailable **Declined-after-park** | the parked question Declined → task Failed, queue position consumed, lease released or lineage failed, halt per `decline_halts_run` | `declined_parked_verification_fails_task_consumes_queue_position_releases_lease_and_halts_per_policy` (`src/topology/fold/tests.rs`) — **not** in the in-tree table | pass |
| **interrupted** | `merge_verification_interrupted` appended on resume; pin deleted expected-old; snapshots reclaimed; re-verify under a new sequence | `a_resume_settles_an_interrupted_stale_verification_and_reclaims_its_residue` (`recover/tests.rs:6727`), `a_resume_reclaims_an_interrupted_verifications_snapshots_after_settling_it` (`:6812`) — **not** in the in-tree table | pass |

**The table is complete.** Every one of the eight required shapes has a passing test that drives it,
and every shape the in-tree table drives also proves `replay_twice_equal()` and that the gate and the
reviewer each judged the recorded `proposed_sha` in **its own** snapshot (never the staging worktree —
`run.runner.ran()` is asserted to contain no process whose workspace is the staging path).
The one honest qualification is the one already stated in §2: the in-tree table's `Shape` enum has 7
variants where the packet's table has 8 rows, so two required rows are proven elsewhere and are not
protected by the table's own iteration.

---

## 7. Required artifact #2 — the fault evidence table: the fourteen adversarial fault tests

Read from `.cumulative_review_gates.gates[G3].adversarial_fault_tests`, not from a summary. Each row
names the test(s) that hold the claim, the result of running them at `9cfaf0c8`, and — where one was
run — the mutation that proves the test still detects.

| # | Adversarial claim | Held by | Result | Detection witness |
|---|---|---|---|---|
| 1 | external ref mutation to a third SHA before CAS or between CAS and `task_merged` refuses | `third_sha_refused_and_a_ref_already_at_the_proposal_only_records`; `a_resume_of_a_prepared_transaction_whose_ref_moved_elsewhere_refuses_a_third_sha`; `a_foreign_reset_of_the_integration_ref_refuses_before_any_append_and_keeps_the_merged_task`; `a_resume_refuses_an_integration_ref_at_another_sha_before_touching_anything` | **pass** | **M7** (drop `ThirdSha`) → 3 failures; **M8** (drop `ForeignHead`) → 1 |
| 2 | integration ref checked out refuses; symbolic substitution refuses | `a_symbolic_or_checked_out_integration_ref_refuses_before_any_append`; `a_resume_refuses_a_symbolic_or_checked_out_integration_ref`; `an_integration_ref_checked_out_in_a_second_worktree_is_refused`; `a_symbolic_ref_that_resolves_to_the_expected_object_is_still_refused`; `checked_out_is_byte_equality_with_the_full_refname`; `a_symbolic_pin_refuses_on_both_the_write_and_the_read` | **pass** | **M9** (drop `assert_publishable` from `decide`) → 1 |
| 3 | orphan pin at next sequence reclaimed; others refused; a proposal object whose staging worktree and pin are gone is left to Git | `a_resume_reclaims_the_orphan_pin_at_the_next_sequence_and_orphan_staging`; `a_resume_refuses_a_prepared_pin_outside_the_sequences_the_log_pinned`; `a_prepared_pin_without_a_candidate_record_is_orphan_residue`; `unpinned_object_never_adopted_on_resume`; `a_staging_orphan_is_ignored_by_intents_reported_by_reclaim_and_never_removed` | **pass** | **M10** (drop `PinAtAnotherSha`) → 2 |
| 4 | the full fast-path proof against a real repository: no staging worktree, no intent, no proposal object (`fsck` count unchanged), no pin; hook harness records no `Worktree.AddStaging` / `Object.ProposalCherryPick` / `Ref.PinPrepared`; `expected_head` == candidate base; `proposed_sha` == candidate commit; CAS onto exactly the gated object; forged `merge_prepared(fast)` refused live **and on replay**; a stale candidate takes the staging path and its `stale_clean` `proposed_sha` equals the pinned proposal | `fast_path_publishes_exact_candidate_without_staging_or_proposal_object`; `merge_prepared_fast_with_moved_head_or_wrong_proposed_or_pin_refused_live_and_on_replay`; `stale_candidate_takes_staging_path_and_publishes_pinned_proposal`; `only_the_three_sites_a_fast_sequence_skips_may_record_that_they_did_not_run`; `only_a_stale_clean_publication_pins_a_proposal` | **pass** — see §4 for the full reading, §5 for the end-to-end measurement | **M1** (no exact-base decision) → 18; **M2**, **M3**, **M6** (each relation) → 5/7/2; **M5** (no CAS) → 33 |
| 5 | kill at T-FAST, T-PROPOSAL (**all** sub-prefixes, incl. a cherry-pick killed mid-command leaving unreferenced objects + `CHERRY_PICK_HEAD`/`index.lock` — synthetic **plus** kill sampling), T-VERIFY, T-PREPARED, T-REJECT, **in both orders**; replay twice equal | T-FAST/T-PREPARED both orders: `a_resume_completes_a_prepared_fast_transaction_through_the_barrier_and_cas` (before the CAS) and `a_resume_completes_a_prepared_transaction_whose_cas_already_ran_by_recording_the_merge` (after it). T-PROPOSAL (a′): `synthetic_cherry_pick_residue_…_forced_reclaim_converges` **and** `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered`. (a)/(b): `plant_staging_worktree` + resume, `a_resume_reclaims_the_orphan_pin_at_the_next_sequence_and_orphan_staging`. T-VERIFY: `a_resume_settles_an_interrupted_stale_verification_and_reclaims_its_residue`, `a_resume_reclaims_an_interrupted_verifications_snapshots_after_settling_it`. T-REJECT: the atomic-registration tests. Unsynced-append kills: the two-crash proof and `barrier_sync_failure_…` | **pass**, with the method stated: the durable prefixes are reached by **synthetic construction** (`plant_prepared_fast`, `plant_stale_verification`, `plant_staging_worktree`, `plant_snapshot`) and the **command-internal** residue by **real kills** (`two_crash_kill_child` killed by abort; `KillableGitChild` for the pick). That split is the packet's own: T-PROPOSAL asks for "synthetic construction plus kill sampling", and a synthetic synced prefix is byte-identical to what a kill at that boundary leaves. The one case synthesis cannot reach — a *complete but unsynced* `merge_prepared` — is covered by a real kill in the two-crash proof. Replay-twice holds at every one | **M5**, **M17** (see §3 and the batch-2 table) |
| 6 | stale verification completion for an interrupted sequence refused | `open_transaction`'s `WrongSequence` (a settled sequence clears `self.transaction` via `release_transaction`); `a_resume_refuses_a_substituted_verification_pin_before_settling_it`; `declining_an_embedded_question_closes_a_running_sibling_and_refuses_its_late_result` | **pass** | **M12** (drop `TransactionAlreadyOpen`), **M14** (drop `NonDenseSequence`) |
| 7 | `already_present` with head drifted after start refused / re-verified | `an_already_present_publication_expects_the_head_its_verification_read` (`fold/tests.rs:5473`); `an_already_present_candidate_settles_without_an_empty_commit`; `a_resume_completes_an_already_present_publication_at_the_candidate_commit_and_reclaims_its_staging`; and structurally `PreparedDefect::AlreadyPresentMovesTheHead` plus `check_merge_prepared`'s `expected_head != *expected_head` of the verification | **pass** | **M16** (already_present allowed to pin) |
| 8 | two candidates for one key/generation cannot both hold a queue position | structural: `apply_candidate_created` calls `close_generation` **before** `queue.push`, so a second `task_candidate_created` fails `open_generation` in `check_candidate_created`; `kill_after_candidate_prepared_appends_candidate_created_once`; `a_task_whose_candidate_is_queued_is_not_dispatched_again`; `answering_a_queued_task_preserves_its_candidate_and_prevents_redispatch` | **pass** | **M11** (drop `close_generation`) → **63 failures** |
| 9 | code rejection and conflict terminate atomically in `merge_rejected` with complete frozen repair registration and lease transfer; the rejected candidate's candidates ref remains; a dispatch attempt of that repair refuses before any append | `a_conflicting_candidate_is_rejected_with_an_atomic_repair_before_any_repair_effect`; `a_code_rejected_candidate_registers_a_repair`; `a_failing_gates_own_output_reaches_the_frozen_repair_spec`; `a_rejecting_reviewers_required_change_reaches_the_frozen_repair_spec`; `a_rejection_creates_a_lineage_from_an_ordinary_candidate_and_widens_a_members`; `a_verification_park_answer_is_ingested_at_the_hard_block_and_a_repair_admission_answer_is_refused_before_any_append` | **pass** — `merge_rejected` carries registration, lineage creation/widening and lease transfer in one event, and `reclaim_staging` runs **after** it | — |
| 10 | `human_required` → `Parked{question}` with a complete question; candidate ineligible while parked; `Answered` re-verifies under a new sequence; `Declined` fails the task, consumes the queue position, releases its lease (or fails the lineage), and halts exactly per `decline_halts_run` | `a_human_required_verdict_parks_the_task`; `declined_parked_verification_fails_task_consumes_queue_position_releases_lease_and_halts_per_policy`; `a_decline_cannot_cancel_a_prepared_publication_and_preserves_its_merged_ancestors`; `answering_one_question_keeps_a_queued_task_parked_until_its_last_answer`; `Ineligible::AwaitingInput` in `CandidateQueue::ineligible` | **pass** | **M20** (`unavailable` always Parks) — see the batch-2 table |
| 11 | infrastructure failure → `Deferred{defers}` with candidate retained, ineligible until `defer_wait_elapsed`/`run_resumed`, task `AwaitingMerge`, both entitlements released, pin deleted, snapshots reclaimed; at `max_defers` → `Parked{question}`; **`Deferred` at `max_defers` and `Parked` without a question both refused** | `infrastructure_failure_defers_then_parks_at_max_defers`; `an_outage_defers_consecutively_inside_the_allowance_and_parks_at_it`; `a_run_that_allows_no_deferral_parks_the_first_outage`; `deferred_task_woken_by_defer_wait_elapsed_or_resume`; `a_gate_spawn_failure_during_integration_verification_defers_inside_max_defers`; `a_gate_that_times_out_during_integration_verification_defers_instead_of_registering_a_repair`; `repeated_container_launch_outages_before_start_consume_defers_through_the_production_runner`; `a_git_error_observed_by_the_verification_settles_an_infrastructure_outage` | **pass**. The refusal is **bidirectional** in `check_defer_allowance`: `Deferred` is refused unless `defers == taken+1` **and** `defers < max`, and `Parked` under `Infrastructure` is refused while `taken+1 < max`. So the emitter cannot drift in either direction, and the emitter's own condition (`is_outage && taken+1 < max`) is the same predicate | **M15** (drop the Parked-below-max half), **M20** |
| 12 | provisional integration reservation cancelled on staging failure leaves the candidate queued and no durable trace; residue reclaimed on resume (ST-13) | `integrate_through`'s own cancel-on-error path exercised by the refusal tests; `a_reservation_is_asserted_singly_and_settles_exactly_once`; `cancel_any_releases_an_unnamed_reservation_and_reports_whether_there_was_one`; `an_append_error_during_recovery_cancels_the_reservation_and_every_running_invocation`; `barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` (nothing appended, candidate still queued, integrates under sequence 0 after the loss) | **pass** | — |
| 13 | fast path: provisional pair converts atomically at `merge_prepared(fast)` with no staging effect in between; both derived holdings present at PreCAS and PostCAS; both released exactly once at `task_merged` (ST-13, T-FAST) | `fast_dual_holding_released_once` — 2 provisional entitlements, `pipeline_held() == 0` while queued, converted at `prepare_fast` (`reservations.is_empty()`), `pipeline_held() == 1` and `transaction().is_some()` before the CAS, both zero after `task_merged`, `reservations.balances()` | **pass**. PostCAS is not a distinct fold state (see Q4); it is the same `Prepared` class holding the same pair, and `an_unresolved_verification_leaves_its_entitlements_with_the_open_transaction` covers it across a process boundary | **M5**, **M11** |
| 14 | integration verification snapshots isolate observers and create no object; staging worktree untouched by verification processes; snapshot intents reclaimed after a kill | `verification_snapshots_are_removed_only_after_the_terminal`; the shape table's per-shape assertions (gate head == recorded `proposed_sha`; reviewer `head_at_spawn` == the same; reviewer workspace == its **own** snapshot path; reviewer workspace **≠** gate workspace; **no** process ran with the staging path as its workspace); `snapshots_create_no_object_for_a_commit_and_never_share_a_checkout`; `a_snapshot_ignores_a_replacement_object_and_materialises_the_judged_tree`; `a_resume_reclaims_an_interrupted_verifications_snapshots_after_settling_it`; `the_production_verifier_judges_the_recorded_proposal_and_removes_its_snapshots_after_the_terminal` | **pass** | — |

**All fourteen pass.** Four rows carry no mutation witness of their own (9, 12, 14, and the second half
of 3); each is a positive-behaviour claim asserted by a test that would fail if the behaviour stopped,
rather than a refusal that could rot silently, and rows 9 and 14 are additionally covered by the
collateral of M1 and M5, which killed the whole shape table.

---

## 8. The six standing questions

The gate process says a gate *"may claim only resources and invariant portions introduced in its
frozen range, naming the portion and the inventory rows."* Each answer below therefore opens by
naming what portion of the question is G3's to answer, and closes with a verdict of
**answered with evidence**, **answered in the G3 portion only**, or **not answered**. Nothing is
claimed for a portion belonging to G1, G2, or a later gate.

### Q5 — effect and append boundaries

**G3's portion.** The Object site `Object.ProposalCherryPick` and its command-internal residue class;
the Ref sites `Ref.PinPrepared`, `Ref.DeletePreparedPin` and `Ref.CompareAndSwapIntegration`; the
Worktree sites `Worktree.WriteStagingIntent` and `Worktree.AddStaging`; the integration use of the
Snapshot sites; the Event sites' stable-prefix barrier as it gates a CAS. The clippy/allow/wrapper/
allowlist/dependency half of the question is whole-tree and is answered for the tree at the frozen sha.

**The residue class — answered with evidence, both halves the packet demands.**

*Synthetic construction.* `synthetic_cherry_pick_residue_unreferenced_objects_and_cherry_pick_head_then_forced_reclaim_converges`
plants, with real git in a real repository: an orphan blob (`git hash-object -w`), a dangling proposal
commit (`git commit-tree` parented on the head), and `CHERRY_PICK_HEAD`, `MERGE_MSG`, `index.lock` and
`sequencer/todo` in the staging git dir. It then asserts `classify_object_residue` returns
`ObjectResidue::Internal`, that `observed_residue_elements` contains all four
`ResidueElement`s, that the resume **reclaims rather than refuses**, that after the reclaim both
planted objects still exist (`git cat-file -e` succeeds — R27: unreferenced objects are Git's and the
run never deletes them), and that the candidate then integrates under the sequence the residue held.

*Real-command kill sampling.* `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered`
spawns a real `git cherry-pick` child per sample and kills it on a ladder derived from a measured
probe pick. Its own assertion is a floor (`killed_while_running >= 1`), which proves the mechanism
exists but says nothing about the size of the sample. **So the sample was measured.** Six independent
runs of the sampler (temporary uncommitted `println!`, hashes verified changed and restored):

| run | samples | killed mid-command | residue `None` | residue `Internal` | residue `After` |
|---|---|---|---|---|---|
| 1 | 8 | 7 | 4 | 2 | 2 |
| 2 | 8 | 7 | 3 | 3 | 2 |
| 3 | 8 | 7 | 3 | 3 | 2 |
| 4 | 8 | 7 | 3 | 4 | 1 |
| 5 | 8 | 6 | 2 | 4 | 2 |
| 6 | 8 | 7 | 5 | 1 | 2 |
| **total** | **48** | **41** | **20** | **17** | **11** |

All 48 samples were classified into one of the site's classes and recovered; the classifier refused
none; every run's candidate integrated afterwards under sequence 1; the test passed all six times.
**All three residue classes of the site appeared in every run**, and the `Internal` class — the one
the packet singles out, objects written with their reference unpublished — was observed 17 times in 48.
That is the kill-sampling record the packet asks for, with numbers rather than a floor.

*One honest note on that floor.* The measured behaviour clears the test's own `>= 1` assertion by
between 6× and 7×. A regression that reduced the sampler to one real kill in eight would therefore
still pass. This is the same shape as the standing sampler-ladder observation from PR5 and is a
weakness of the assertion, not of the mechanism; the mechanism is measured above.

**The whole-tree half — answered with evidence.**

| Requirement | State at `9cfaf0c8` | Enforced by |
|---|---|---|
| clippy disallowed list complete for the primitives the crate reaches | `clippy.toml` lists 3 `disallowed-types`, 4 `disallowed-macros` and ~80 `disallowed-methods` across `std::fs`, `std::fs::File`, `io::Write`, symlinks, `std::process::Command`, `libc`, `windows_sys`, the crate's own effectful wrappers and the docker/GitView helpers | `every_declared_effect_denial_refuses_for_the_reason_it_declares` (compiles one fixture per resolution shape — renamed import, re-export, function value — and asserts the emitted lint set); `every_denied_path_this_host_can_resolve_does_resolve` (runs clippy-driver over a probe and fails on the bare `warning:` an unresolvable path produces, which `-D warnings` does **not** escalate, with a positive control); `every_platform_conditional_denial_names_something_real`; `every_libc_item_the_tree_names_is_classified_and_the_effects_are_denied` |
| allow-placement scan | an allow of a governed lint may appear **only** as a module-level attribute in a file listed in `effects/allowlist.toml` | `every_allow_of_a_governed_lint_is_module_level_and_in_the_allowlist`; `every_allowlist_entry_carries_its_justification_and_names_a_real_file`; `every_early_stop_is_at_a_module`; `every_production_region_that_stops_early_stops_at_a_module` |
| wrapper classification | `effects/wrappers.toml`; every row marked `effectful` and not `funnel` also appears on the disallowed list | `every_effectful_wrapper_is_on_the_disallowed_list`; `every_externally_reachable_fn_of_a_legacy_or_shared_module_is_classified`; `every_funnel_classified_fn_names_a_site` |
| funnel-module allowlist | `effects/funnel-modules.json`; each group is confined to its funnel module | `every_group_names_the_funnel_module_the_design_confines_it_to`; `every_file_durability_barrier_in_a_funnel_module_goes_through_one_call` |
| frozen legacy allowlist | exactly two sites are outside the claim: `Event.LegacyOpenLog` and `Event.LegacyAppend` | `the_claimed_inventory_is_every_site_but_the_two_legacy_ones` (claimed == all − 2, and the two missing are named); `only_the_legacy_event_sites_are_outside_the_claim`; `a_legacy_site_carries_no_bijection_requirement_and_a_claimed_one_does` |
| dependency review for the code **and `Cargo.lock`** in range | **the frozen range changes neither.** `git diff --stat 9cfaf0c8^1 9cfaf0c8 -- Cargo.toml Cargo.lock` is empty. The lock holds 64 packages; the 10 direct runtime dependencies plus one test-only (`yaml-rust2 = "=0.12.0"`) are unchanged, and two are pinned exactly (`globset =0.4.19`, `yaml-rust2 =0.12.0`) with the MSRV reason recorded beside each | measurement above; `cargo +1.85.0 check --locked` green (gate 04), which is what proves the lock is the one being reviewed |

**Where Q5 is NOT answered, and why that is the tree's own recorded position.** Q5 also asks whether,
*"for every Topology/Shared site in the generated inventory … both hook phases and every
parent-executed sub-effect point [were] observed executed by the suite [with] a registry entry for
every observable order."* At `9cfaf0c8` that is **false by design and the tree says so**: the
generated inventory is **70** sites, **68** of them claimed, and
`the_bijection_over_the_whole_claimed_inventory_fails_for_this_slice`
(`src/topology/effects/tests.rs`) asserts the whole-inventory bijection produces **more than 100
failures**, with the comment *"PR3 builds the frame and PR7–PR10 fill it"* and a named example of a
site no funnel exists for yet (`RunDir.PublishCommitRecord`). Every `check_bijection` call in the tree
is against a **self-test** harness and registry; none is against the real suite's recorded coverage.

**And the packet assigns that work to PR10 by name, which settles it.** `pr_sequence[PR10]`
("max_parallel = 1 closure") has in its own `scope`: *"complete typed site bijection registry for the
sequential topology incl. Event points in both modes, IdUnread points, residue classes, and the fast
no-execution record, bounded reachability census with totality and the merge_prepared relations,
per-resource ledgers per decisions.resource_accounting incl. R27/R28"*, and in its `gating`:
*"site bijection complete for the range"*. PR10 is **after** the range G3 gates — G3 blocks "PR9 and
later" — so the whole-inventory bijection is not merely unfinished at G3, it is **another slice's
deliverable**. A gate that demanded it would be claiming a portion outside its frozen range, which the
process clause forbids.

G3's own sites are covered: the fast sequence's three-site no-execution record is asserted and pinned
by `only_the_three_sites_a_fast_sequence_skips_may_record_that_they_did_not_run`, the
`Object.ProposalCherryPick` residue class has both required evidence halves above, and
`every_site_carries_the_row_fault_row_scope_and_adjacency_the_design_gives_it` holds over all 70 sites.
**Q5 verdict: answered in the G3 portion. The whole-inventory two-phase bijection is explicitly not
claimed here and is PR10's deliverable by the packet's own assignment.**

### Q4 — aliasing

**G3's portion.** `MergeSequence` (R14), the `prepared/<seq>` pin (R12), the staging worktree and
intent (R10), the queue position (R6), the integration snapshots (R24), the merge and pipeline
entitlements (R2, R1), the provisional integration reservation (R13), and the question of whether a
fast publication can name any object other than the candidate commit.

| Aliasing hazard | Why it cannot happen | Mutation witness |
|---|---|---|
| two transactions on one sequence | `check_transaction_start` refuses any `sequence != next_sequence` (`NonDenseSequence`) and any start while `self.transaction.is_some()` (`TransactionAlreadyOpen`); `apply_verification_started` / `apply_merge_prepared` are the only places `next_sequence` advances | M12, M14 |
| two candidates holding one queue position | `apply_candidate_created` calls `close_generation` **before** pushing the entry, so a second `task_candidate_created` cannot pass `open_generation` in `check_candidate_created`; and a task whose candidate is queued is not dispatched again | M11 (63 failures, incl. `kill_after_candidate_prepared_appends_candidate_created_once`) |
| a candidate overtaking the queue | `check_transaction_start` requires the candidate to be `queue.first_eligible(...)`, with the refusal detail distinguishing "holds no position", "not eligible", and "another is queued ahead and eligible" | M13 |
| two transactions on one `prepared/<seq>` pin | the pin name embeds the sequence (`prepared_pin_ref`), created `create_ref_zero_old` (fails if the ref exists), pruned `delete_ref_expected_old`, and `prune_pin` refuses a pin at any SHA but the recorded proposal rather than deleting it | M10 |
| a fast sequence sharing a stale sequence's staging slot | a fast sequence creates no slot at all: `Authorized.staging` is `None` when `disposition == Fast`, and the no-object proof asserts no `Slot::Staging` intent exists | M1 |
| **a fast publication naming an object other than the candidate commit its gates and reviews judged** | three independent refusals: `MergePrepared::self_consistency` (`FastProposesAnotherCommit` — `proposed_sha` must equal `candidate_sha`), `prepared_candidate` (full `CandidateRef` equality against the recorded `candidate_prepared`, `commit_sha` included), and `check_merge_prepared`'s own third statement of it. Measured end-to-end: sequence 0's `proposed` **is** `candidate_alpha` (§5) | M6 (caught); M4 is the redundant third statement (§3) |
| two entitlements for one transaction, or one released twice | R1 is `open generations holding the pipeline + usize::from(transaction.is_some())` and R2 is `transaction.is_some()` — both are functions of **one** durable fact, so they cannot disagree; the generation's own pipeline hold is released at `task_candidate_created` before the transaction's begins, which `fast_dual_holding_released_once` asserts directly (`pipeline_held() == 0` for a queued candidate, `1` after `merge_prepared(fast)`, `0` after `task_merged`, and `reservations.balances()`) | M5 |
| an observer aliasing the staging worktree | every verification runs in its own snapshot; the shape table asserts `run.runner.ran()` contains **no** process whose workspace is the staging path, and that the reviewer's workspace differs from the gate's | — |

`PreCAS` / `PostCAS` deserve a note, because the packet's R1 granularity names five transaction states
and `TransactionClass` has two variants. That is correct rather than a gap: whether the CAS has run is
**not** a fold-derived fact — it is an observation of the ref, made in `publish()` (`found == expected`
→ CAS; `found == proposed` → record only; anything else → `ThirdSha`). Both PreCAS and PostCAS are the
same `Prepared` class holding the same one entitlement pair, which is exactly what R1/R2's
*"per unresolved integration transaction (incl. fast prepared)"* granularity says.

**Q4 verdict: answered with evidence for the G3 portion.** The portions about container names,
credential volume turns, incarnations, private halves and settlement slots belong to G1/G2's ranges
and are not re-claimed here.

### Q3 — stale, duplicate and out-of-order completions

**G3's portion.** A verification completion arriving for a sequence that is no longer open; a second
terminal for one transaction; a `task_merged` naming a SHA the authorization did not propose; and the
append-error protocol as it applies to an integration append.

- **Every event of an open transaction is keyed to the sequence.** `open_transaction(kind, sequence)`
  refuses with `WrongSequence` if no transaction is open, or if the open one's sequence differs.
  `check_merge_prepared` (stale/already_present), `check_verification_unavailable`,
  `check_verification_interrupted`, `check_merge_rejected` and `check_task_merged` all go through it.
  A completion for an interrupted sequence therefore cannot mutate the newer one: after
  `merge_verification_interrupted`, `release_transaction()` clears `self.transaction`, so the stale
  completion meets "none" and is refused.
- **A closed transaction cannot be reopened.** `check_transaction_start` refuses while one is open
  (M12) and requires the dense next sequence (M14), so a duplicate `merge_verification_started` for a
  resolved sequence is refused on both counts.
- **A `task_merged` cannot name a different object.** `check_task_merged` refuses
  `merged_sha != proposed_sha` of the open `Prepared` transaction (M18).
- **A returned append error cannot let a stale in-memory fold drive anything.** `emit()`
  (`src/engine/topology/emit.rs`) does, on an entered-and-failed append and in this order:
  `state.fold.poison()`, `state.reservations.cancel_any()`, then `establish_stable_prefix` to decide
  `Present` / `Absent` / `Undetermined{step, detail}` **by rereading the file**, never from memory.
  After poisoning, `TopologyFold::plan_transition` returns `FoldError::Poisoned` for **every** event,
  and `ready`, `ready_retry`, `pipeline_reservable` and the other predicates all return `false`. The
  error type is `#[must_use]` and its `Display` states the protocol it followed. There is no retry
  path: `T-RUNSTART`'s refusal list names "a retry of the first append after a returned error" and the
  same rule holds here by the poison.
- Witnessed end-to-end for the CAS specifically by
  `an_append_failure_at_merge_prepared_issues_no_cas_and_leaves_the_integration_ref`
  (`integrate/tests.rs:187`) and
  `an_append_error_during_recovery_cancels_the_reservation_and_every_running_invocation`.

**Q3 verdict: answered with evidence for the G3 portion.** Slot release, cancel and provisional
conversion under *concurrency* belong to PR11's broker and are out of range; what is in range — the
single-process provisional reservation — is covered by the reservation tests and M11/M12.

### Q6 — sequential guarantees preserved

**G3's portion.** One fold, one writer, FIFO integration, no overtaking, exact-commit verification on
immutable snapshots, exact-base fast publication of the gated candidate object itself
(`expected_head == candidate base`, `proposed_sha == candidate SHA`, no staging, no proposal object,
no repeated work), untouched user checkout, atomic settlements, and the identical canonical trace.

| Guarantee | Evidence in range |
|---|---|
| **one fold** | `emit()` is the single append path and calls `plan_transition` before every write; `TopologyLine::round_trip` means the bytes checked are the bytes written; `replay_twice_equal()` asserts the live incremental fold equals a replay of its own on-disk log, at the end of all 7 in-tree shapes, the fast-path proof, and the two-crash proof |
| **one writer** | exactly one production `compare_and_swap_ref` call site (§4); the worktree lock serialises write commands; `decide()` refuses any head the log did not authorize (`ForeignHead`, M8) so a second writer's head is never adopted |
| **FIFO integration, no overtaking** | `check_transaction_start` requires `queue.first_eligible(...)` (M13); `the_queue_is_ordered_by_creation_and_not_by_preparation`; `the_eligible_integration_candidate_is_the_first_the_queue_still_offers` |
| **exact-commit verification on immutable snapshots** | `terminal_shape_coverage_table_…` asserts, per verified shape, that `gate_heads(&run) == [started.proposed_sha]`, that the reviewer's `head_at_spawn == started.proposed_sha`, that the reviewer's workspace is its **own** snapshot path and not the gate's, and that no process ran in the staging worktree; `snapshots_create_no_object_for_a_commit_and_never_share_a_checkout` |
| **exact-base fast publication of the gated candidate itself** | §4 in full, plus the measurement in §5: sequence 0 `Fast`, `expected == base`, `proposed == candidate_alpha`, `pin=None`, object count unchanged, three sites recorded as not executed. **No repeated work**: nothing is cherry-picked, so no diff is re-applied and no verification is re-run — the fast disposition cites `VerificationSource::CandidatePrepared`, the candidate's own record |
| **untouched user checkout** | every integration effect is confined to the `merge/s<seq>` staging slot and snapshot slots under the execution root; the fast path creates neither |
| **atomic settlements** | `merge_rejected` carries the complete frozen repair registration, lineage creation/widening and lease transfer in **one** event — `a_conflicting_candidate_is_rejected_with_an_atomic_repair_before_any_repair_effect` asserts the registration is complete in the event and that no repair effect precedes it; `reclaim_staging` runs **after** the terminal |
| **identical canonical authoritative trace where scheduling must not matter** | the terminal-shape table drives each shape independently and each converges on replay; PR8 is a `max_parallel = 1` slice, so schedule-independence across *concurrent* integrations is PR11's obligation and is not claimed here |
| **derived run outcome, halt > budget > parked > complete; `budget_exceeded` before any budget-driven end; halt/budget-stop semantics** | in range only insofar as integration participates: a verification-deferred candidate never blocks a `Halted` or `BudgetExceeded` closure (`resume_clears_budget_stop_and_wakes_deferred`, `a_decline_cannot_cancel_a_prepared_publication_and_preserves_its_merged_ancestors`), and the closure procedure completes an authorized publication at any run end. The outcome ordering itself is G1/G2's portion |
| **one Runner identity per run, recorded before the first probe, honored by every resume** | G1/G2's portion. In range only as the typestate that gates recovery: `PreflightCertified::certify` requires a `RunnerRebuilt`, which rebuilds the recorded runner and warns on config drift (`resume_rebuilds_runner_from_record_and_warns_on_config_drift`) |

**Q6 verdict: answered with evidence for the G3 portion.** The two clauses that are not answerable in
range are named as such: schedule-independence under real concurrency (PR11) and the run-outcome
ordering (G1/G2).

### Q2 — resource accounting

**G3's portion, by row.** The gate claims these fourteen and no others.

| Row | Resource | Class(es) across its lifecycle | Enforcement domain | State at `9cfaf0c8` |
|---|---|---|---|---|
| R14 | `MergeSequence`; verification defers per candidate | `consumed` only | logical_fold_broker | `next_sequence` advances monotonically in `apply_verification_started` / `apply_merge_prepared` and is never decremented; `check_transaction_start` refuses a non-dense sequence (M14). Defers: `check_defer_allowance` refuses `defers != taken+1` (non-consecutive) and `defers >= max` |
| R12 | `prepared/<seq>` pin | `provisional (pre-append)` → pruned by exact next-sequence orphan reclaim; `active` → pruned expected-old at interrupted/Deferred/Parked; pruned by finalization when prepared | external_physical | created only in the `Picked::Clean` arm, i.e. **stale only** (`create_ref_zero_old`); `prune_pin` refuses a substituted pin (M10); orphan reclaimed only at the next sequence (`a_resume_reclaims_the_orphan_pin_at_the_next_sequence_and_orphan_staging`), refused elsewhere (`a_resume_refuses_a_prepared_pin_outside_the_sequences_the_log_pinned`) |
| R10 | staging worktree + durable synced intent, incl. an interrupted pick's administrative residue | `provisional` → pruned forced; `active` → pruned after the terminal | external_physical | intent **before** add (the funnel refuses an add without a durable intent); reclaimed after every terminal by `reclaim_staging`; forced reclaim of `CHERRY_PICK_HEAD`/`MERGE_MSG`/`index.lock`/`sequencer` proven synthetically and by sampling |
| R24 | integration verification snapshot worktree + intent | pruned on completion of the invocation set; reclaimed as residue at process start after a kill | external_physical | `reclaim_snapshots` after the terminal only — `verification_snapshots_are_removed_only_after_the_terminal`; `a_resume_reclaims_an_interrupted_verifications_snapshots_after_settling_it` |
| R27 | proposal commit and merge objects once unreferenced, and objects an interrupted pick wrote | `released` to Git; never deleted, never adopted | external_physical | the synthetic-residue test asserts `git cat-file -e` still succeeds for both planted objects after the reclaim; `unpinned_object_never_adopted_on_resume`; `a_staging_orphan_is_ignored_by_intents_reported_by_reclaim_and_never_removed`; `both_commit_tree_sites_leave_an_unrecorded_id_at_r27` |
| R21 | the integration ref (and the Topology appends) | `persistent_output` | external_physical | one CAS call site; created zero-old at P8; never recreated by a dispatch that finds it absent (`DispatchHeadAbsent`) |
| R6 | queue position | `resumably_open` while queued/verifying/deferred/parked; `consumed` at `task_merged`, `merge_rejected`, or a Declined park | logical_fold_broker | `apply_candidate_created` pushes exactly one entry per generation and closes the generation first (M11); `declined_parked_verification_fails_task_consumes_queue_position_releases_lease_and_halts_per_policy` |
| R11 | the rejected candidate's candidates ref (repair source) | `persistent_output`, protected while the run can resume | external_physical | `merge_rejected` deletes no candidates ref: `Ref.DeleteCandidatesRef` is reached only at Complete finalization. The shape table's rejection arms assert the ref remains |
| R15 | verification-park question | `resumably_open` while open; `consumed` at `question_answered` | logical_fold_broker | `park_question` always constructs a complete `FrozenQuestion` (id, key, kind, context, options); `check_defer_allowance` + `self_consistency` refuse `Parked` without a question |
| R8 | lineage lease (transfer/widen at rejection) | `resumably_open`; `released` at `task_merged` with root in `satisfies`, or lineage terminal failure | logical_fold_broker | `lease_release()` picks `Lineage{root}` when the registry entry has a lineage and `Candidate{key,gen}` otherwise; `check_lease_release`; `a_publication_releases_the_lease_its_own_candidate_or_its_lineage_holds`; `a_rejection_creates_a_lineage_from_an_ordinary_candidate_and_widens_a_members` |
| R2 | merge entitlement | `held` → released exactly once at the transaction terminal | logical_fold_broker | `transaction.is_some()` — one durable fact |
| R1 | pipeline entitlement during integration | `held` → released at the terminal | logical_fold_broker | `pipeline_held() = open generations holding it + usize::from(transaction.is_some())`; the generation's own hold is released at `task_candidate_created` **before** the transaction's begins, so no double count (asserted directly by `fast_dual_holding_released_once`) |
| R13 | provisional integration reservation | `taken` → released by conversion at first append, or cancelled | logical_fold_broker | `journal.converted(key)` immediately after each first append; `reservations.cancel_any()` in the append-error protocol; `a_reservation_is_asserted_singly_and_settles_exactly_once`; `cancel_any_releases_an_unnamed_reservation_and_reports_whether_there_was_one` |
| R4 | integration `InvocationId`s (gate/review/re-ask) | `registered` → completed or cancelled exactly once | logical_fold_broker | `a_refused_spawn_cancels_its_registration_rather_than_completing_it`; `an_append_error_during_recovery_cancels_the_reservation_and_every_running_invocation` |

**No overlap, and one row each.** The one place two rows could contend for the same object is the
proposal commit, and the packet resolves it as a **sequence** rather than a coincidence:
R10 (staging HEAD/index) → R12 (the pin) → R27 (unreferenced). The code follows that order —
`proposal_cherry_pick` then `create_ref_zero_old(PinPrepared)` then, at any terminal, `prune_pin` and
`remove_worktree`/`remove_intent` — so at every instant exactly one row references the object, and
after both are gone it is R27's. This is exercised by the synthetic-residue test at each of the three
instants.

**The one thing Q2 asks that G3 cannot close.** Q2 also asks whether the grants *"each satisfy the
outcome equations, including the derived total run outcome, the closure procedure, terminal
finalization at Complete/Halted, and repair-source protection on resumable outcomes."* The closure
procedure and finalization are exercised in range for the integration rows
(`a_resume_completes_a_prepared_fast_transaction_through_the_barrier_and_cas`,
`promoting_completed_at_run_end`, `a_pin_left_by_an_interrupted_promotion_is_pruned_by_the_closure_procedure`
— the first completes an authorized publication at the next process start, which is the only run-end
path this build has, since `run_finished` has no production emitter at PR8; see §2),
but the **total run outcome equation over all 28 rows** is assigned to PR10 by the packet itself —
`pr_sequence[PR10].scope` names *"derived total run outcome wired to the loop … closure procedure …
run_finished, terminal finalization … per-resource ledgers per decisions.resource_accounting incl.
R27/R28"*. PR10 is after G3's range. **Q2 verdict: answered with evidence for the fourteen rows G3
owns; the total-run-outcome equation over all 28 rows is PR10's deliverable and is not claimed here.**

### Q1 — reconstruction from durable state

Q1 is the widest of the six and most of it is G1's and G2's. **G3's portion is three things**, and only
these are claimed:

**(a) Is the unresolved transaction — including a fast prepared one — reconstructable from durable
state alone?** Yes, and the reconstruction is a single function. `Authorized::from_fold(fold)`
(`integrate.rs:375`) returns `Some` iff the fold's `transaction` is `TransactionClass::Prepared`, and
derives `expected_head`, `proposed_sha`, `satisfies`, the pin (`prepared_ref`, `None` for fast) and the
staging slot (`Some` iff `disposition != Fast`) from the recorded event; `lease_release` is re-derived
from the registry rather than stored. Nothing is re-decided. Whether the CAS has already run is *not*
reconstructed from the log — it is observed from the ref inside `publish()`, which is the only correct
source. Witnesses: `a_resume_completes_a_prepared_fast_transaction_through_the_barrier_and_cas`
(ref at expected → CAS then `task_merged`), `a_resume_completes_a_prepared_transaction_whose_cas_already_ran_by_recording_the_merge`
(ref at proposed → `task_merged` only, `cas_integration_entries == 0`),
`a_resume_of_a_prepared_transaction_whose_ref_moved_elsewhere_refuses_a_third_sha` (M7), and
`a_recovered_authorization_is_the_live_one_and_completes_through_the_same_publish` — which is the
strongest of the four, because it asserts the recovered authorization is **the same value** the live
path built and that both complete through the same `publish`.

**(b) Do entitlements derive from recorded active generations and unresolved transactions?** Yes:
`pipeline_held()` and the merge entitlement are pure functions of `tasks[].open()` and
`transaction.is_some()`, both of which are fold state. Nothing about an entitlement is stored
separately, so there is nothing to disagree. `an_unresolved_verification_leaves_its_entitlements_with_the_open_transaction`.

**(c) Does every fresh process establish the stable-prefix barrier before any CAS?** Yes, and it is a
**typestate**, not a discipline. To reach `finish_integration` — the only recovery path to `publish` —
a caller must hold a `PreflightCertified`, which is constructible only from a `RunnerRebuilt`, which is
constructible only from a `ResumeCensused`, which holds a `BarrierHeld`, which is constructible only
from a `StablePrefix`. And `StablePrefix` has exactly one constructor in the crate:
`establish_stable_prefix` (`src/events/log.rs:878`), which
(i) opens through `EventLog::open_funnel`, truncating a torn tail and **fsyncing** the surviving
prefix (`sync_log_file`, failures mapped to `BarrierStep::SyncPrefix`);
(ii) rereads the file and refuses unless it ends at a commit marker, is the same **length** as the
prefix synced at open, and is **byte-equal** to it, naming the first differing byte if not;
(iii) refuses unless the first line's sha256 matches the private commit record;
(iv) `parse_log` then `TopologyFold::replay` **from the reread bytes**.
Any failure returns `BarrierError` — so no `StablePrefix`, hence no CAS. Executed proofs:
the two-crash proof (a real child killed by abort after a complete-but-unsynced `merge_prepared`;
the child reports `synced <len>` then `cas` **in that order**, and `<len>` covers the
`merge_prepared` line; a real truncation then discards the unsynced `task_merged`; the log and the ref
still agree and the next resume appends `task_merged`) and
`barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss` (an injected `SyncPrefix`
error: `cas_integration_entries == 0`, the ref unmoved, **nothing appended**, the durability ledger
empty for that log, and after the loss the candidate is still queued and integrates under sequence 0).

**One observation about (c), recorded because a later reviewer will hit it.** `BarrierHeld::from`
(`recover.rs:481`) is the **only** production call site of `StablePrefixBarrier::establish`, and it
passes `PrefixReread { first: m.clone(), second: m.clone() }`, `PrefixReplay { replayed: m }` and
`PrefixSync { synced_len: m.len }` — so all four of that validator's checks are vacuously true and the
call cannot fail. The barrier's substance is enforced upstream in `establish_stable_prefix` as set out
above, and the token's fields (`boundary`, `digest`) are what the census consumes, so **the guarantee
holds**; what does not hold is any claim that `StablePrefixBarrier::establish` is enforcing it at that
call site. Its four checks are exercised only by `src/runner/container/census/tests.rs` unit tests,
which is why a mutation of them would kill nothing else. Filed as an observation in §9, not as a
defect.

**What Q1 asks that G3 does not answer.** Coordinator-local holdings provably empty at process start;
the Windows ambient job and Unix reaper contract; husk reclamation and the bidirectional ownership
proof; the authorized private root derived read-only from `run_started.private_dir`; the Runner rebuilt
from `run_started(4).runner`; `RetainedIdle` with retained session and incarnation; binding overrides;
`halted_at`; the epoch-scoped budget stop. **All of these are G1's and G2's portions, introduced in
earlier frozen ranges, and this gate re-claims none of them.** **Q1 verdict: answered with evidence
for the three G3 portions (a), (b), (c); the remainder is explicitly out of this gate's range.**

---

## 9. Observations recorded, and findings

Nothing in this section is a new critical or high finding. Each item was found by reading or by
mutation, and each says which.

### O1 — `StablePrefixBarrier::establish` is inert at its only production call site

`src/engine/topology/recover.rs:481`, inside `BarrierHeld::from`, is the **only** production call site
of `StablePrefixBarrier::establish`. It is handed
`PrefixReread { first: m.clone(), second: m.clone() }`, `PrefixReplay { replayed: m }` and
`PrefixSync { synced_len: m.len }`, so each of that validator's four refusals — boundary moved, digest
changed, sync shorter than the prefix, replay over other bytes — is vacuously satisfied and the call
cannot fail. Every other call site is a unit test of the validator in
`src/runner/container/census/tests.rs`.

The barrier's substance is enforced upstream in `events/log.rs::establish_stable_prefix` and **M17
proves it detects** (removing its byte-equality check kills
`an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle`). So the guarantee
holds. What does not hold is the impression the call site gives. Class: defence in depth that is inert
where it is read as load-bearing — a "redundant assertion defeats its own mutation" shape.
**Suggested disposition: a tech-debt ledger row, plus one sentence at the call site saying that the
proof is upstream and this is a token.** Not a defect; nothing behaves wrongly.

### O2 — the fault-row test census covers exactly one row and is named "every"

`src/effects/tests/contract_mappings.rs::every_fault_row_name_is_a_test_in_the_tree` is a good census:
it requires each named test to exist as a `#[test]`, to be defined in exactly one file, and it carries a
positive control against a name that cannot exist. Its **domain is the single const array
`T_CONTAINER_TESTS`** — the T-CONTAINER row alone. Grep finds no `T_FAST`, `T_PROPOSAL`, `T_VERIFY`,
`T_PREPARED` or `T_REJECT` transcription anywhere in `src/`, though `FaultRow` carries all 21 rows.

Consequence for exactly this gate: for the five rows G3 is the gate of, nothing mechanically prevents
a named test from being renamed or deleted. The obligation is a review duty and it was discharged by
hand in §2 — 24 of the 26 slugs were located, and the 2 that are absent are absent for a stated
reason (their subject, a run end, does not exist at PR8). Class: coverage-durability; a
measurement whose name over-promises its domain. **Suggested disposition: a ledger row proposing the
five arrays, or a rename of the test to say which row it covers.** Not a defect.

### O3 — the in-tree terminal-shape table is seven rows where the packet's is eight

Stated in §2 and resolved in §6: `EVERY_INTEGRATE_SHAPE` is `[Shape; 7]` and the packet's required
table has eight rows. Declined-after-park and interrupted are proven by tests elsewhere, so the
**coverage** is complete; the table's own iteration does not reach them, so a future shape regression
in those two would not be caught by the test that calls itself the table. Class: same as O2.
**Suggested disposition: a ledger row.**

### O4 — `upstroke-eight` runs nine of this head's ten gates

An operator-tooling gap on a box script, outside the repository under review, recorded in §1 so that
the tenth gate's separately-run PASS is not mistaken for the wrapper's. Not a repository finding.

### O5 — a live defect found by reviewing the integrated tree: the GitHub Pages build of `docs/` fails, so upstroke.rs is not publishing

This is not a G3 site, proof, question or ledger row, and it does not touch the pass rule. It is a
**real, currently-failing check on master**, found because the process clause says the integrated tree
is reviewed as a whole, and it is reported rather than repaired.

**What fails.** At the frozen sha the commit's check runs are: `upstroke-ci` success, `upstroke-pr-policy`
success, `lint` / `lint (macos)` / `lint (windows)` success, `msrv` on all three success,
`test (ubuntu-latest)` / `test (macos-latest)` / `test (winguest)` success — and
**`build` failure**, `deploy` skipped. `build` is not `ci.yml`: it is the dynamic
`pages build and deployment` workflow
(`actions/jekyll-build-pages@v1`, image `ghcr.io/actions/jekyll-build-pages:v1.0.13`).

**Why.** Jekyll runs Liquid over the Markdown *before* Markdown, so a backticked code span does not
protect a `{{`. Two sites, quoted from the run log of
`actions/runs/34222036731`:

```
Liquid Warning:   Liquid syntax error (line 220): Unexpected character ! in "{{ assert!(true); }}"
                  in internals/effects/tests/contract_mappings.md
Liquid Exception: Liquid syntax error (line 1313): Variable '{{\n{body}' was not properly terminated
                  with regexp: /\}\}/ in internals/events/log/tests.md
```

The **Exception** is what fails the build. Its source is a section heading in
`docs/internals/events/log/tests.md:1313` quoting
`fs::write(&source, format!("fn main() {{\n{body}\n}}\n"))` from
`src/events/log/tests.rs:2978`. The `{{` opens a Liquid variable that never closes on that construct,
so Liquid aborts the whole site build.

**Provenance, and PR8's part in it.** The build-breaking line arrived in `f45e76a3`, *"docs(internals):
move event model comments into notes"*, **2026-09-05** — after G2 passed and three days before PR8
merged. `git diff --stat 9cfaf0c8^1 9cfaf0c8 -- docs/internals/events/log/tests.md` is empty: **PR8 did
not touch that file**, and the same `build failure` / `deploy skipped` pair appears on master merges of
2026-09-07, before PR8. So it is **pre-existing to G3's frozen range and not a PR8 regression.**

PR8 does, however, add three more Liquid-openable spans, all in
`docs/internals/runner/container.md`: `{{.Names}}`, `{{.State}}` and `{{.State.Status}}` — Docker
Go-template syntax, which happens to collide exactly with Liquid's. Those are *terminated*, so they
would not fail the build; Liquid would render them as empty variables and silently corrupt that page's
text. They corrupt nothing today only because the build fails before publishing anything. Seven
`docs/` files hold 22 `{{` occurrences in total.

**What the fix cannot be.** Rewording the offending text is the wrong repair: the notes' own preamble
states that *"each code span in a section heading is an exact source fragment. Search it as a fixed
string"*, and that affordance is the point of the headings. (`validate-internals-notes.sh` enforces the
backlink structure rather than fragment equality, so a reword would pass the ten gates — which makes
the wrong repair the easy one, and is why this paragraph exists.) `docs/` has **no `.nojekyll` and no
`_config.yml`**; it holds `CNAME`, `index.html` and `internals/`. The proportionate repairs are
Jekyll-level: add `.nojekyll` to the Pages root, which disables Liquid entirely and is the right answer
for a directory of generated notes; or fence the notes in `{% raw %}`.

**Proposed row for the owner to file** (not filed here — *flag rather than decide*): severity **P2**,
category `documentation`, provenance `pre_existing`, `first_bad: f45e76a3`, location
`docs/internals/events/log/tests.md:1313`, with the observation that any future notes file quoting a
Rust `format!` brace-escape or a Docker Go template reintroduces it, and that the durable guard is a
`.nojekyll` rather than a rule about what may be quoted.

### O6 — `pr8-plan.md` is still tracked at the repository root, and its R12 paragraph disagrees with the code

The fourteenth frontier review's second open follow-up is a misstatement in `pr8-plan.md`. Checking it
produced two facts, the second of which the follow-up does not mention.

**The misstatement is real, and it has two halves.** `pr8-plan.md`'s R12 paragraph says the
verification-park question's kind is *"Clarify for a reviewer's needs-human, **Unblock for an
unjudgeable input** or an outage at max_defers, **context from the coordinator's question builder**"*.
The code (`park_question`, `src/engine/topology/integrate.rs`) maps
`UnavailableCause::HumanRequired` → `QuestionKind::Clarify` and `UnavailableCause::Infrastructure` →
`QuestionKind::Unblock`, and `needs_human()` classifies `ReviewInputTooLarge` and `ReviewInputOpaque`
— *the* unjudgeable inputs — as `HumanRequired`. So an unjudgeable input yields **Clarify**, not
`Unblock`. And the context is built **locally** by `format!` inside `park_question`; only the *options*
come from `crate::engine::coordinator::question_options(kind)`. Both halves confirmed by reading, not
inherited.

**The second fact: the document is still there.** `pr8-plan.md` is a **tracked, 100 KB file at the
repository root**, last written by `70789a8e` — PR8's own eighth-round record commit — and present at
the frozen sha. Its sibling, the triage record, *was* deliberately removed from master and archived
outside the repository. So a stale 100 KB plan for a merged slice sits at the root of the repository,
is not listed in `CLAUDE.md`'s "Where things are", and now contains a paragraph the code contradicts.

That combination is why the misstatement matters more than a wording slip: a plan at the repository
root reads as current, and the next reader of R12 will get the question classification wrong. Which is
exactly what happened — the follow-up exists because a frontier reviewer read it.

**Not a G3 blocker**, and the gate does not treat it as one: no relation, refusal, resource or recovery
action depends on it, and `park_question` itself is correct and complete (the question carries `id`,
`key`, `kind`, `context` and validated `options`; `Parked` without a question is refused in both
directions). **Suggested disposition for the owner: one decision, not two.** Either `pr8-plan.md`
follows the triage record out of master — which retires the misstatement rather than editing it, and
matches the 2026-09-04 decision that moved the review records — or it stays and R12 is corrected on
both halves. Correcting a document that should not be in the tree is the worst of the three options.

### The two known deferrals inside G3's range — reported, not reopened

| id | severity | disposition | what it is | why it does not fail this gate |
|---|---|---|---|---|
| `PR8-R2-SPEND-REPLAY` | P1 correctness | **deferred**, guarded by the owner (a Class C vocabulary decision or an erratum) | an integration verification's review costs are charged live but a `merge_verification_unavailable` terminal carries no review record, so a restart's replay forgets them and the selector can admit an integration the previous incarnation refused. Failure direction is overspending, unbounded across restarts | owner-ruled deferral with a standing finding file; pinned on the branch by `a_paid_review_that_parks_is_charged_live_and_its_replay_loss_is_the_deferred_vocabulary_gap`, which asserts the live charge, the in-incarnation refusal **and** the replayed total — so the gap is measured, not merely noted |
| `PR8-CRASH-002` | P1 crash-consistency | **deferred**, guarded by the owner (a Class C residue-class decision) | a coordinator killed inside `git update-ref`'s lock window leaves `integration.lock`, and every later resume refuses on it until an operator removes the file. Also met as `packed-refs.lock` on macOS | the refusal is **resumable and loses nothing** — no event appended, no effect half-applied — so the cost is liveness, not correctness; pinned by `a_ref_lock_left_by_a_killed_compare_and_swap_refuses_resumably_until_removed`, which asserts both the resumable refusal and the completion after removal |

Both were verified present, both were read in full, and **neither was reopened, repaired or narrowed**,
per the gate brief.

### One pre-existing P1 whose activation set G3's range widens — flagged, not adjudicated

`PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS` (P1 security-trust, **deferred**, provenance `pre_existing`,
guarded by the owner as a design question and queued to `standards/SWEEP.md` row 11) is about
`src/workspace_manager.rs`'s containment table naming nine roles and not naming the checkout's and
base's `.git` file or link, `commondir`, `objects`, `refs`, `packed-refs`, `index` and `config` behind
them. `create_ref_zero_old` is one of the primitives it concerns — and **PR8 adds a new call site of
it**, `Ref.PinPrepared`. Scope follows activation, so G3's range widens the set of paths through which
that pre-existing gap can be reached.

It is not this gate's to re-adjudicate: it is already filed, dispositioned and owner-guarded, and its
provenance is pre-existing. It is named here so that the owner's design answer is taken with PR8's new
activation site in view, and so that no reader of this report concludes G3 examined it and found it
harmless. **Suggested disposition: note the new activation site on the existing row; no new finding.**

### The state of the finding ledger

`findings/` holds 285 files, one of which is `README.md`, the row template — so **284
findings**: 14 at P1, 100 at P2, 169 at P3, 1 at P4. Every one carries a disposition, and counted
after normalising the trailing rationale some rows append: **279 `deferred`, 4 `accepted-risk`, 1
`fixed`**. (286 `disposition:` lines appear in total: 284 findings, plus the README template's
example line, plus a duplicated line in
`P2_security-trust_202609051147_internals-backlink-is-not-a-link.md` — a blemish in one file, not a
finding.) **`grep -l '^disposition: *open'` returns nothing: no finding in the ledger is open.** Of
the 14 at P1, **all 14 are `deferred`** with a named guard. Two are G3's range (above); the other twelve belong
to PR5, PR7, PR64, PR104, PR120, PR125, PR130 and PR137 — earlier frozen ranges or the sweep lane —
and were dispositioned in their own reviews. This gate re-adjudicates none of them.

---

## 10. Verdict against the pass rule

The rule, verbatim:

> pass iff all G3 sites recover as tabled, the fast-path no-object proof and relation refusals green,
> terminal-shape table complete, ref inventory clean, replay-twice equal, isolation proven, questions
> answered, no open critical/high finding

| Clause | Verdict | Where |
|---|---|---|
| all G3 sites recover as tabled | **met.** T-FAST, T-PROPOSAL (a′, a, b), T-VERIFY, T-PREPARED and T-REJECT each have executed recovery evidence; the durable prefixes by synthetic construction and the command-internal residue by real-command kills, which is the packet's own division | §7 rows 3, 5, 6, 7, 11, 12; §8 Q5 |
| fast-path no-object proof green | **met**, and non-vacuous: object count unchanged in a real repository, three sites recorded as not executed, no staging intent, exact `merge_prepared(fast)` relations, hook order prepared < swap < merged — and M1 kills the test | §4 |
| relation refusals green | **met**, and mechanically proven to hold **live and on replay** by `every_guarded_event_is_refused_the_same_way_live_and_on_a_hostile_replay`; each individual relation has its own mutation witness (M2, M3, M6, M7, M8, M9, M10, M16, M18) | §3, §7 rows 1, 2, 4, 7 |
| terminal-shape table complete | **met**, across more than one test: all eight required rows have a passing test. The qualification is O3 — the in-tree table iterates seven of the eight | §6, O3 |
| ref inventory clean | **met.** Four ref families, eight `RefSite` variants, one production CAS call site, `publish()` reachable from exactly three places all of which require a durable `merge_prepared`; `prepared/<seq>` never created for a fast sequence | §4 |
| replay-twice equal | **met**, from the on-disk bytes, against the live incremental fold, at every shape and after a real simulated power loss | §5 |
| isolation proven | **met.** Every verification runs in its own snapshot; the reviewer's workspace differs from the gate's; no process ran in the staging worktree; snapshots create no object and are reclaimed only after the terminal, and after a kill by the next resume | §7 row 14 |
| questions answered | **yes, each with its scope named.** Q2, Q3, Q4, Q6 answered with evidence for G3's portion; Q1 answered for its three G3 portions (a) (b) (c); **Q5 answered for G3's portion, and its whole-inventory ST-07 bijection clause is PR10's deliverable by the packet's own `scope` and `gating`, not G3's** — the tree independently asserts that bijection still fails by >100 links over 68 claimed sites | §8 |
| no open critical/high finding | **met** on the ledger's own terms: 14 P1s, all `deferred` with named guards, none `open`. Two are G3's range and are owner-deferred; per the gate brief neither fails the gate on its own. Two new defects were found: **O5** the failing Pages build (P2, pre-existing, outside PR8) and **O6** `pr8-plan.md`'s R12 paragraph contradicting the code (P3, in range, documentation-only). Neither is critical or high, so neither engages this clause | §9 O5, O6 |

### Verdict: **G3 PASSES**, with the scope of the pass stated.

Every clause of the pass rule is met on the evidence above, with two qualifications that are part of
the verdict and not footnotes to it:

1. **Q5 and part of Q2 are answered for G3's portion only, because the rest is PR10's.** Three
   things the standing questions ask for as whole-tree properties — the complete typed site-bijection
   registry over all 68 claimed sites, the per-resource ledgers over all 28 accounting rows, and the
   run-end closure/`run_finished`/terminal-finalization behaviour — appear verbatim in
   `pr_sequence[PR10].scope` and `pr_sequence[PR10].gating`. PR10 is **after** the range G3 gates.
   Under the process clause — *"a gate may claim only resources and invariant portions introduced in
   its frozen range"* — a G3 that claimed them would be overclaiming, and a G3 that failed for their
   absence would be gating a later slice's work. This is therefore the **correct** scope for G3 and not
   a shortfall; it is stated at length only because the standing questions are written as whole-tree
   questions and a reader could otherwise take this report to have answered them as such. It has not.
2. **The pass rests on the owner's two deferrals.** `PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002` are P1s
   inside G3's range. The gate brief directs that neither fails the gate on its own, and that direction
   is the owner's ruling, not this reviewer's judgement. If the owner withdraws either deferral, this
   verdict does not survive it: `PR8-R2-SPEND-REPLAY` in particular is an unbounded-overspend gap in
   the integration path, which is exactly what G3 is the gate of.

**Consequence: PR9 and later are unblocked by G3**, subject to (2).

### What a successor should do first if this report is reopened

- The whole-inventory bijection (Q5) is the largest single piece of unfinished evidence in the gate
  series. It is **PR10's**, by that slice's own `scope` and `gating`. Whoever takes it up should know
  that at `9cfaf0c8` it fails by >100 links over 68 claimed sites of 70, and that
  `the_bijection_over_the_whole_claimed_inventory_fails_for_this_slice` is the non-vacuity assertion
  they will need to invert.
- O1, O2 and O3 are three ledger rows, not repairs. **O5 is a repair** — one file, `.nojekyll` — and it
  is the only item in this report that anything is currently broken by: upstroke.rs is not publishing.
  It is out of G3's range, which is why this gate reports it instead of fixing it.
- The kill sampler's measured strength is now on record (41 real mid-command kills in 48 samples, all
  three residue classes in every run). If it ever regresses, the test's own `>= 1` floor will not say
  so; §8 Q5's table is the baseline to compare against.

## Addendum, 2026-09-09: what has changed since this report was written

This report is a dated record of the tree at `9cfaf0c8` and is not rewritten. Two of its observations
have moved since, and a reader on `master` should not take either as a present-tense claim.

**O5 is repaired.** The `.nojekyll` this report called "the only item in this report that anything is
currently broken by" landed on `master` as `docs/.nojekyll` in
[#250](https://github.com/sourcemaps/upstroke/pull/250), merged 2026-09-09T00:12:33Z. The Pages build
succeeded at that commit and the result was verified against the live site rather than inferred from
the build status: `https://upstroke.rs/` returns 200, and `/internals/agent/bin.md` returns 200 as
`text/markdown`.

**The outage was longer than this report or #250's body says.** Both describe it as beginning on
2026-09-07. The build history shows the last successful build before it was
**2026-09-05T20:11:16Z**, with every build after that erroring until the fix — so roughly three and a
quarter days and thirteen failed builds, not two days. #250's body carries the same understatement and
is merged; this addendum is the correction of record.

The fix carries a consequence this report did not anticipate, disclosed in #250's body and recorded
here because it changes what the site serves. With Jekyll off, the internals notes are no longer
rendered to HTML. Their `.md` URLs continue to resolve, because Pages already served those directly;
the **generated** forms do not, so `/internals/capacity.html` now returns 404 and
`/internals/runner/host` returns 301 and then 404, while `/internals/capacity.md` returns 200.

The reach of that is small, and smaller than an earlier draft of this addendum claimed. Of 144 notes,
**6 links across 3 notes point at another note**, and none of those is absolute. The eighteen other
Markdown links in the notes — twelve absolute `github.com` URLs and six relative ones — target
`DESIGN.md`, `design/` and `standards/` documents that live outside the notes and are unaffected
either way. An earlier draft counted every Markdown target as a note-to-note link and reported 24
across 16 notes; the frontier review of this pull request disproved that by resolving the targets, and
this is the corrected count.

**O6 still holds.** `pr8-plan.md`, `pr8-body.md` and `pr8-triage.md` remain tracked at the repository
root at the time of writing. [#248](https://github.com/sourcemaps/upstroke/pull/248) exists to move
them and has not landed.

O1, O2 and O3 are unchanged; they were ledger rows rather than repairs when this report was written and
they remain so.
