# G5 — cumulative review gate: complete crash/resume across the max_parallel = 1 topology (second run)

**Gate** G5, *Complete crash/resume across the max_parallel = 1 topology (bounded census + typed site
bijection registry + derived run outcome and finalization)*.
**Frozen input range** `a2fe09c2414996485a95c0b916f057ece8201ed7` — the merge commit that put pull
request #292 on `master` at 2026-09-15T22:18:29Z, confirmed on entry with `git rev-parse HEAD` in
`/srv/worktrees/g5-run2` (`git status --porcelain` empty; branch `gate/g5-run2` was created from it and
carries this report and one finding file, and nothing else). `git diff HEAD -- src effects Cargo.toml
Cargo.lock` is empty at every measurement below; `HEAD:src` is tree `e8ebfe15b1f6385423175db9484054d5829f17ef`
and `HEAD:effects` is `2a5869775c6515e0d3477762481cdd081eb8fde6`.
**This is G5's second run.** The first (`g5_run1`, range `caf6bed0`, report `reviews/2026-09-14-gate-G5.md`
on branch `gate/g5-run1`, draft #288) failed on clauses 1, 2, 7 and 9. This run judges the corrected range
from scratch: every artifact was regenerated, every recipe re-anchored and re-run, and the clause-2 audit
rebuilt from the tree rather than inherited. Run 1's evidence is cited only as history, and labelled.
**Platform of every Linux measurement** `x86_64-unknown-linux-gnu`, kernel 6.8.0-137-generic, cargo 1.97.1
(c980f4866 2026-06-30), rustc 1.97.1 (8bab26f4f 2026-07-14), `cargo +1.85.0` 1.85.0, git 2.43.0, Docker
29.7.2 client and server, shell umask `077`, 32 processors (`spec/platform-versions-host.log`).
**Platform of every Windows measurement** the persistent guest `windowsguest`, in the Gate 5 programme's own
tree `C:\upstroke-g5` fetched to this range, cargo 1.97.1, rustc 1.97.1, git 2.50.1.windows.1
(`guest/guest-fetch-build.log`).
**Evidence** every figure quoted here is in a file this report names, under
`~/tactus-artifacts/g5-evidence-a2fe09c/` on the build box (`README.md` indexes it, `MANIFEST.sha256` lists
every file with its sha256). Paths below of the form `gates/…`, `audit/…` are relative to that directory.

**Verdict: G5 PASSES on this range.** All nine positive clauses of the pass rule are established on
evidence produced at `a2fe09c2`, and the three prohibited claims are not made (§13). Two qualifications are
carried into the verdict rather than hidden: one residue of the run's own making is still outside the
registry's vocabulary (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`, P2, deferred — clause 4 is graded for
the packet's nine declared classes with that exception named), and one deferred P1 in the ledger,
`PR5-RD-002`, sits inside the recovery path this gate certifies and weakens clause 9's pass, which §9.3
states in full. **PR11 and PR12 are unblocked by G5.**

## 0. What this run is, and what the range contains

### The specification, regenerated from the packet

`~/tactus-artifacts/G5-DEFINITION.txt` and `G5-QUESTIONS.txt` were regenerated from
`~/tactus-artifacts/tactus-parallel-design-neutral-v16.json` (sha256
`8b3991c804d74c91a0fe162304b68249991d4104338bed8647da88ec5da50813`) on entry and diffed against the files:
**0 differences** — every field of `cumulative_review_gates.gates[4]` equal in packet order, the six standing
questions equal, and a byte-for-byte regeneration of the whole extract (`spec/regen-definition.log`,
`spec/regen_definition.py`). The gate's entry holds **five** `integrated_invariants`, a **six**-row
`state_resource_ownership_matrix`, **seven** `adversarial_fault_tests`, **five** `required_artifacts`, the
`reviewer_remit` and the `pass_fail_rule`.

Both amendments are in `spec/`: `2026-09-13-AMENDMENT-st14-census-family.md` (the census family) and
`2026-09-15-AMENDMENT-clause1-census-bounds.md` (clause 1's bounds). **Clause 1 is judged against the
amended bounds line**, which the regenerated extract's source line names and which this run read from the
packet itself (`spec/packet-bounds-line.log`).

### What changed since run 1's range

`git log --merges --first-parent caf6bed0..a2fe09c2` (`spec/merges-since-run1.log`) — ten merges, every one
named:

| Merge | Pull request | What it is, and what it does to this gate |
|---|---|---|
| `a2fe09c2` | #292 | clause 2's O coordinates: witnesses that construct and recover them, a per-process observation export, registry citations. Judged in §3.5 |
| `e7c5b082` | #295 | a reproduction note appended to `PR5-RD-002`'s finding file. Bears on §9.3 |
| `60a5ac4f` | #294 | closes the macOS ready-red P1 finding (file deleted) |
| `f9572a73` | #293 | clause 2's R rows: replay-twice assertions at the recovered coordinates. Judged in §3.5 |
| `00856bbe` | #291 | the committed finalization kill matrix (26 Complete + 24 Halted cells, each resumed in a fresh process). Judged in §3.5; filed `PR291-SNAPSHOT-OWNER-READINESS-BOUND-EXPIRED-ON-A-LOADED-GUEST` (P2, deferred) |
| `03ca21f1` | #290 | the o3 attribution vocabulary: `src/events/mod.rs`, `src/interaction.rs`, `src/topology/events.rs`, `effects/wrappers.toml`. Not a G5 subject; its tests ran in the suite |
| `2570e423` | #289 | clauses 7 and 9: the reclaim's observing barrier, `publish_report`'s order, the ST-18 third-resume assertion, the private-record swallow witnesses. Judged in §3.5; closed three findings |
| `aff2b024` | #280 | `.github/scripts/test-pr-policy.sh`, `validate-pr-branch.sh` — instruments, no engine code |
| `0d349480` | #286 | `.github/scripts/test-pr-ready-audit.sh`, `scripts/pr-review-parse.py` — instruments |
| `8b28944f` | #287 | `src/agent/proc.rs`: bounded kill and wait at five sites (closed a P1). Inside the process funnel this gate's §5 audits |

Against run 1's range the engine and its artifacts changed as `range/diff-since-run1.log` records: 30 files
under `src/` and `effects/`, +9,658/−529 lines, `Cargo.lock` **unchanged** (and unchanged since the G4 range
`7e0110a1`; `Cargo.toml`'s only change in that span is a package `exclude`). So no dependency entered the
range, which is what §5.1's dependency-review paragraph turns on.

### Method notes that bind the evidence below

- **Nothing is taken on trust from run 1 or from the fix pull requests' bodies.** Every artifact here was
  produced by a run at `a2fe09c2`; every recipe was re-anchored at this range and re-executed; the
  clause-2 audit was rebuilt from the registry, the tree and this run's own observation exports.
- **Mutation is the detection test.** Where this report says a test *detects* something, a recipe this
  report names was applied to a `cp` backup, the named test run through the wrapper, the file restored and
  `cmp`ed, and the outcome recorded (§3). Where a mutation survived, the survival is reported and explained.
- **Every temporary edit was restored and proven.** The two probes of §6 were applied to a `cp` backup of
  `src/engine/topology/recover/tests.rs`; after the run the file's sha256 equals
  `git show HEAD:src/engine/topology/recover/tests.rs` and `git status --porcelain` is empty
  (`probes/probe-run.log`).
- **One cargo command at a time on a private target.** Every build and test ran as
  `env UPSTROKE_RAMTARGET=/mnt/ramtarget/iso-g5_run2 upstroke-build cargo …`; the gate runs used
  `~/bin/w1-eight-iso /srv/worktrees/g5-run2` bare. The lib test binary bakes this worktree and no other:
  `/srv/worktrees/g5-run2` occurs 9 times in it and `/srv/worktrees/g5-run1`, `/srv/worktrees/pr10`,
  `/srv/tactus`, `/srv/worktrees/review-279` 0 times each (`gates/binary-provenance-range-1.log`).
- **Residue was measured with a fresh TMPDIR, never by name patterns**, with a listing taken mid-run as
  proof the tests allocate there (§1).

## 1. The cumulative suite, the ten gates, and CI

### The ten gates at the frozen range

`~/bin/w1-eight-iso /srv/worktrees/g5-run2`, first attempt, 2026-09-15T23:07:03Z: **`ALL 9 PASS at a2fe09c`**
— fmt 1s, clippy 21s, test 82s, msrv 18s, release-record, pr-policy 13s, pr-ledger, docs-consistency,
internals-notes 1s (`gates/w1-eight-iso-a2fe09c-range-1.log`, the nine logs in
`gates/eight-logs-a2fe09c-range-1/`). The tenth gate, run separately from the worktree root:
`test-pr-ready-audit: ok` (`gates/gate10-a2fe09c-range-1.log`). Box load at the start: in
`gates/box-load-a2fe09c-range-1.log`. `git status --porcelain` empty after.

### The cumulative suite on Linux

From the gate run's own test leg (`gates/eight-logs-a2fe09c-range-1/03-test.log`): `running 2763 tests` in
the lib binary — **2673 passed, 0 failed, 90 ignored** in 82.01s; the `main.rs` binary **10 passed**; the
`probe` example 0. **2683 passed in total, 0 failed.**

The suite was then run twice more with every evidence export set
(`UPSTROKE_HOOK_OBSERVATIONS`, `UPSTROKE_LEDGER_EXPORT`, `UPSTROKE_CENSUS_SUMMARY`) and `TMPDIR` pointed at
a fresh empty directory of this session's own, with a watcher listing that directory every half second
(`suite/export-run.sh`):

| Run | Result | Log |
|---|---|---|
| export 1 (23:12Z) | **2672 passed, 1 failed, 90 ignored**, `cargo exit=101`. The one failure is `runner::container::tests::real_docker_prints_the_transcribed_unreachable_diagnostics`, and **it is an artifact of my own measurement**: `TMPDIR` was this session's 96-character scratchpad, so the socket the test builds exceeded `sun_path` and the live CLI printed *"unix socket path … is too long"*, which the classifier reads as an answered failure (§9.4) | `suite/export1.log`, `suite/export1.cargo.log` |
| export 2 (23:15Z) | **2673 passed, 0 failed, 90 ignored**, `cargo exit=0`; binary 10 passed; 503 observation files, 6 ledger tables, the census summary | `suite/export2.log`, `observations-export2/`, `ledger-export2/`, `census-export2/` |

Export run 2's artifacts are the ones §4–§7 quote. The census summary and all six ledger tables are
byte-identical between the two runs (`cmp`), and the census summary is byte-identical to the guest's.

**Residue, measured rather than pattern-matched.** Both export runs ran with `TMPDIR` at a fresh empty
directory; the watcher's first non-empty listing (23 entries, 18 test-binary processes running) and its
fullest (997–998 entries) are saved, and after the run everything left was listed with no filter: **995
entries, 30,681 paths** under the fresh directory, all of them the suite's own `fixture::scratch` roots
(`suite/export2.after.txt`, `export2.after-tree.txt`, `export2.midrun-*.txt`). **0 new entries appeared in
the system `/tmp`** during either run (`suite/export2.system-tmp-new.txt`), and no test-binary process
survived. That the suite leaves its scratch roots behind is a property of the fixtures, not of the engine;
it is recorded here because a residue claim that is not measured is worth nothing.

### The Windows guest

The guest fetched this range into the Gate 5 programme's own tree `C:\upstroke-g5` (created by run 1;
`git rev-parse HEAD` there is `a2fe09c2…`, `git status --porcelain` empty), rebuilt with
`CARGO_INCREMENTAL=0` (`Compiling upstroke v0.1.0 (C:\upstroke-g5)`, `Finished` in 54.75s) and ran
`cargo test --all-targets --all-features` with the three exports set and `TMP`/`TEMP` at a fresh directory
**under the ordinary CI temporary directory** (`C:\Users\Administrator\AppData\Local\Temp\g5r2suite`, so no
short private path could hide a path-length failure):

**2566 passed, 0 failed, 67 ignored** in 338.24s in the lib binary; the `main.rs` binary **12 passed, 0
failed, 1 ignored**; `suite rc=0` (`guest/guest-suite.log`). Its exports are in `guest/exports/` (497
observation records, 6 ledger tables, the census summary). 963 entries were left in the fresh `TMP`, the
same fixture-scratch class as on Linux.

**The space this cost, and what I removed.** C: had 1.01 GB free when this run started. My first build
filled it and failed at the linker (`LNK1318`, 139 KB free; `guest/guest-fetch-build.log`). I then ran
`cargo clean -p upstroke` in the shared target `C:\cargo-target` — **2,550 files, 8.2 GiB**, all of them
artifacts of *this crate*, which my own build had just overwritten — and C: returned to 8.04 GB free
(`guest/guest-space.log`). Nothing belonging to another session was removed. The suite then cost 0.08 GB
(8,379,973,632 → 8,295,800,832 bytes free).

### CI at the frozen sha, all three platforms

`gh run list --commit a2fe09c2…` (`ci/runs-at-range.json`, each run's jobs in `ci/run-<id>.json`), newest
run per check name:

| Run | Event | Result |
|---|---|---|
| 35029376216 (22:08:01Z) | `merge_group` | **every job success**: lint, lint (macos), lint (windows), test (ubuntu-latest), test (macos-latest), test (winguest), msrv ×3, upstroke-ci |
| 35030295227 (22:18:31Z) | `push` — the newest run per check name | **every job success**, the same ten checks |
| 35029376249 (22:08:01Z) | `merge_group` | upstroke-pr-policy success |

So all three platforms are green at the range in the newest run per check name, and there is no
superseded-run ambiguity to resolve.

### The Docker-gated lanes, run with Docker required

`docker_gate` returns an early success when Docker is unavailable unless `UPSTROKE_REQUIRE_DOCKER` is set,
under which a test that would skip panics. The 23 `DOCKER_GATED_TESTS` were run with that variable set and a
short `TMPDIR` (`docker-required.log`): **21 passed, 2 failed** —
`real_docker_kill_on_an_already_exited_container_is_tolerated` and
`real_docker_returns_both_streams_of_a_container_separately`, each panicking at
`src/runner/container/tests.rs:3436` with *"`upstroke-f1-…` is still running after 200 observations"*, which
is the filed poll flake
`findings/P3_correctness_202609121234_the-docker-termination-poll-counts-yields-not-time.md` by assertion
text and location. **Each is 3 of 3 green alone** with the variable still set (`docker/controls.log`).

## 2. The tests this gate runs by name

The nine group invocations of §8 take their filters from `groups/group-tests.txt`: run 1's lists carried
forward — **every filter re-checked against this range's own `cargo test -- --list`** (2,773 names,
`groups/test-list-a2fe09c.txt`) — plus the 54 tests the four Gate 5 fix pull requests added, each assigned
to the group whose claim it exercises (`groups/new-tests-by-fix-pr.txt`). **228 filters, every one present in
the binary's list**; the check refuses a filter that names no test, which is how run 1's group-4 invocation
had silently run 12 of 13.

## 3. Detection, established by mutation

Three recipe sets ran at this range, all through the same runner shape: each mutation is applied to a `cp`
backup, the named test is run through the wrapper, the file is restored and `cmp`ed, and `rc` is recorded.
Every chunk ended with `git status --porcelain` empty and **no `RESTORE FAILED`**. The runner truncates its
own `summary.tsv` per invocation, so each set is consolidated from its chunk logs (`mutations/summary.md`,
`mutations/summary-*.tsv`, `mutations/chain-*.log`, one `.log`/`.diff`/`.backup` per recipe under
`mutations/run-*/`).

| Set | Recipes | Killed (`rc=101`) | Passed as expected | Survivals |
|---|---|---|---|---|
| PR10's 80 recipes, re-anchored at this range | 80 | 78 | 2 | **0** |
| run 1's 45 G5 recipes, re-run at this range | 45 | 34 | 2 | 9 |
| the fix-judging recipes this run wrote | 16 | 12 | 0 | 4 |

### 3.1 Re-anchoring

Eight of PR10's recipes and one of run 1's no longer applied at this range: #289 gave
`reclaim_report_staging` a `&DurabilityLedger` argument, which moved their anchors. Each was re-anchored by
the one-token change and re-checked; `mutations/mutations-*.sh.run1-anchors` keep the pre-anchor files.
After re-anchoring, **all 125 of run 1's recipes applied** in `CHECK=1` mode before any of them ran
(`mutations/check-pass-pr10-b.log`, `check-pass-g5-b.log`: 80 and 45 `anchor applied`, 0 `patch failed`).
One recipe was retired: `r11-third-resume-leftover-record-only-vs-g5-witness` aimed at run 1's *uncommitted*
witness, which #289 replaced with a committed assertion.

### 3.2 What the survivals are

All nine survivals in run 1's set are the ones run 1 explained, re-observed here, plus one VOID; all four in
this run's set are explained in §3.5. `mutations/summary.md` carries the table; in short: three barrier
recipes name a pure-data test that never calls `establish_stable_prefix` (their re-aimed `-log` twins kill);
`g5-census-question-not-offered-dimension` survives because the `questions` bound is reached by another
route (its `-arms` twin kills); `g5-id-unread-point-skipped` and `g5-residue-frozen-n-changed` name tests off
the mutation's path (their re-aimed twins kill); `g5-retained-idle-not-closed-on-resume-settle` names the
packet-slug settle-level test, which never runs recovery step (e) (the loop-level recipe kills);
`g5-fast-publication-other-sha-accepted` is behaviour-preserving, because `MergePrepared::self_consistency()`
refuses that shape in the wire type before the fold's check runs; and
`g5-answered-question-stays-open-vs-g5-witness` is **VOID** — its filter names run 1's uncommitted witness, so
it matched no test (`0 passed; 0 failed; 2763 filtered out`), while its committed twin kills.

### 3.5 Every fix pull request, judged rather than accepted

**#289 (`2570e423`) — the durable reclaim, the third-resume assertion, the swallow witnesses.** Three of its
claims were put under mutation at this range:

| Recipe | Mutation | Test | Observed |
|---|---|---|---|
| `fix289-reclaim-no-barrier` | the reclaim takes **no** barrier between the directory's deletion and the record's removal — the shape before #289 | `rundir::tests::a_reclaimed_report_stage_remains_reclaimable_after_power_loss` | **killed**, `rc=101` |
| `fix289-reclaim-barrier-not-observing` | the barrier is taken but records no observed entry (`sync_dir` for `sync_dir_observing`) | the same | **killed**, `rc=101` |
| `fix289-publish-record-before-sync` | `publish_report` removes the record before its directory barrier (the pre-fix order) | `rundir::tests::a_refused_public_barrier_leaves_the_record_of_the_staging_directory_it_removed` | **killed**, `rc=101` |
| `fix289-record-removed-before-barrier` | the record removed immediately before the observing barrier | the power-loss witness | **passed** — and correctly: the barrier still follows the deletion and still observes the name absent, so the deletion is durable and nothing is stranded. The faithful pre-fix shape is the first row, which kills |
| `r11-third-resume-leftover-record-only` (run 1's) | the fresh branch removes the record alone | `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` | **killed**, `rc=101` — at run 1's range this recipe *survived*, which is what failed clause 7 |

`r11-staging-record-publish-swallowed` and `-stage-swallowed` still pass against
`a_report_staging_directory_is_recorded_before_it_is_made`; #289 answered that finding with two **new**
tests (`a_refused_public_barrier_leaves_the_record_…`, `a_report_staging_record_failure_stops_before_directory_creation`),
and the third recipe above kills through one of them. **Judgment: #289's claims hold at this range, and the
two facts that failed run 1's clause 7 no longer hold.**

**#291 (`00856bbe`) — the committed finalization kill matrix.** `kill_at_every_finalization_cell`
(`recover/tests.rs:20542`) plants a finished run per cell, kills the child at that cell through
`run_kill_child_within` with a 120-second bound, asserts the child died by abort and that the cell it was
armed at is the last one it consulted, asserts `assert_finalization_order`, then resumes **in a fresh
process** (`finalization_resume_child`), asserts the refusal names the outcome and whether the report was
regenerated, `assert_finalized`, and replays the durable log twice asserting the states equal. 26 Complete
cells and 24 Halted cells, asserted as exact counts. Mutations: `fix291-complete-keeps-candidates-refs`
(finalization stops pruning the candidates refs) **kills** it. `fix291-{complete,halted,matrix}-keeps-staging-leftovers`
(the staging-leftovers step made a no-op) **survive** — because those fixtures plant no staging-intent
leftover, so the step removes nothing in those cells; the dedicated
`a_fault_at_a_staging_leftovers_own_removal_stops_finalization_and_the_next_resume_converges` **kills** the
same mutation (`rc=101`, `mutations/run-g5run2/fix291-staging-leftovers-vs-dedicated-test.log`). **Judgment:
the matrix is a real kill matrix and its convergence is guarded; the one cleanup step its fixtures do not
exercise is guarded elsewhere, and that is stated rather than counted as coverage.**

**#293 (`f9572a73`) — replay-twice at the recovered coordinates.** Five mutations made `TopologyFold::replay`
drop the last event on every second call, and were aimed at one test per assertion helper the pull request
uses: `run.replay_twice_equal` (`dispatch::kill_after_dispatch_recreates_worktree_without_spend`),
`Journal::replay_twice_equal` (`candidate::kill_after_candidate_prepared_appends_candidate_created_once`),
`assert_replays_twice_to_the_next_open` (`create::append_first_sync_error_reports_by_replay_and_never_deletes`
and `emit::every_append_site_reaches_the_protocol_under_its_own_site`) and `assert_log_replays_twice_equal`
(`recover::resume_establishes_stable_prefix_barrier_before_any_fold_derived_effect`). **All five killed**
(`rc=101`). **Judgment: the assertions #293 added are live — a replay that is not deterministic fails them.**

**#292 (`a2fe09c2`) — the coordinate witnesses, the observation export and the registry citations.** Two
mutations at the recovery its witnesses claim: `fix292-root-always-recreated` (the resume creates the
execution root even when one stands) kills
`a_fault_after_the_execution_root_is_recreated_leaves_the_root_and_the_next_resume_adopts_it`;
`fix292-root-never-recreated` kills
`a_resume_after_finalization_pruned_the_execution_root_and_the_pins_recreates_the_root_and_integrates_the_queued_candidate`.
Both `rc=101`. Its registry citations were re-derived rather than trusted: every `evidence.executed.test`
resolves to exactly one `#[test]` in the tree, and the ST-07 merge check passes over this run's own exports
on **both** hosts (§5.2). **Judgment: #292's witnesses do what the registry says they do.**

**The other six merges.** #290, #287, #286, #280, #294 and #295 are judged in §0's table: two are instruments
under `.github/` and `scripts/`, one is `src/agent/proc.rs`'s bounded kill and wait (inside the process
funnel §5 audits, and green in every suite run here), one is vocabulary, and two are finding-ledger changes.

## 4. Required artifact 2 — the bounded census

**The file:** `census-export2/census-summary.json` (sha256
`63d1a783106d9c03f2446d74cccc62d48e50b50ec92261df99b432854765b821`), written by
`topology::census::tests::the_census_summary_names_every_fault_row_and_serializes` during export run 2;
**byte-identical** to export run 1's and to the Windows guest's own export. The artifact, with every member,
every bound and the ten coverage assertions mapped to the test that asserts each, is
`census-export2/bounded-census-artifact.md`.

| Member | Generator | Ceiling | States | Transitions | accepted / refused / truncated | truncated? | closed? |
|---|---|---|---|---|---|---|---|
| `prefix` | full | 20,000 | **20,000** | 5,194,414 | 97,608 / 5,067,348 / 29,458 | **yes, reported** | no |
| `seeded: sequences, repairs, lineages` | restricted, seed of 15 events | 5,000 | **25** | 1,405 | 24 / 1,381 / 0 | no | **yes** |

`the_prefix_reports_its_truncation_and_every_seeded_census_closes` asserts exactly that shape: the prefix at
its stated ceiling reporting truncation; every seeded member closed with its frontier exhausted **under** its
ceiling and no state at the trace ceiling; and the set of dimensions the prefix misses asserted non-empty and
covered. `every_declared_dimension_is_reached_at_its_bound` asserts equality over the union for every
dimension `CensusBounds::dimensions()` declares, and refuses a bound the struct declares that `dimensions()`
does not.

**Every bound, reached by equality over the union** (from the summary's own fields; `union_reaches` equals
`bounds` on all ten, and `every_bound_reached_over_the_union` is `true`): originals 3, repairs 2, lineages 2,
generations per task 2, attempts per generation 2, sequences 4, **defers 1**, questions 2, review passes 1,
resumes 2.

**Clause 1 against the amended bounds line.** The packet now reads *"verification defers per candidate <=
max_defers - 1 (fixture value 1: the outage that would reach max_defers parks the candidate) … plan shapes:
chain, fan-out, join (two independent tasks, then one depending on both)"*. The tree bounds defers at 1 and
asserts `bounds.defers + 1 == run_started().limits.max_defers`, and its `PlanShape` is `Chain`, `FanOut`,
`Join`. **Both dimensions on which run 1 failed clause 1 now agree with the packet**, and the union reaches
the defers bound by equality.

**The plan shapes, stated exactly.** The family explores the fan-out (`MAIN_SHAPE`). The chain and the join
are explored by `every_plan_shape_is_explored` (`src/topology/census.rs:3826`) at a 3,000-state ceiling each,
**truncated**, with no reachable `FoldError` and all four outcomes reached in each, and with the three shapes'
dependency lists asserted against `PlanShape`'s own table — so a fourth shape cannot be smuggled in. Those two
explorations are **not members of the family artifact**, which run 1 recorded and the 2026-09-15 amendment
left to this run to grade. I grade it **established**: the family's obligation in the packet's `family` field
is "one seeded census per dimension of the bounds that the prefix census does not reach", and the equality
obligation is over the bounds `dimensions()` enumerates — ten numeric dimensions, none of them a plan shape;
the bounds line's final clause names the shapes the census explores, and all three are explored by committed
tests at stated ceilings with truncation reported. What would make this reading wrong is a reading in which
"plan shapes" is itself a dimension of the union; on that reading the chain and the join are explored outside
the family and clause 1 would not be established. I state both, and grade on the first.

**Totality and the arms.** `the_derived_outcome_is_total_over_every_explored_state` evaluates
`derived_outcome` at every state of **every family member**, admits only NotEnding or exactly one outcome,
and asserts the `FoldError` arm is never reached; `a_state_with_admissible_work_and_no_budget_exceeded_classifies_not_ending`
is the pre-`budget_exceeded` counterexample. `every_plan_transition_arm_is_executed_by_the_census` asserts the
executed arm set equals `production_arms()` — **24 arms**, read from the production dispatch — over the
family, and that every arm but `run_started` also has an acceptance.

**Detection.** Ten recipes kill a census test at this range (`census-bound-not-reached`,
`-trace-ceiling-silent`, `-defers-not-reached`, `-lineage-collapsed`, `-budget-class-gated`,
`-truncations-counted-refused`, `-truncated-labels-refused`, `-seeded-does-not-close`,
`-dimension-dropped-from-union-check`, `-seeded-row-misattributed`), plus `classifier-drops-beta`,
`matches-row-unconditional`, `g5-derived-outcome-parked-as-fold-error`, `g5-census-run-resumed-not-offered-dimension`
and `-arms`, `g5-run-resumed-runner-difference-ignored` and `g5-fast-publication-other-head-accepted`.

**What is claimed, in the packet's words and no more.** The family completes for the stated bounds; closure of
the space under the bounds is claimed of nothing; **unbounded closure is not claimed**; and the census makes
**no effect-phase claim** — effect-phase distinctions are the registry's alone (§5).

## 5. Required artifact 3 — the effect-site inventory, the fault-injection registry, the residue record

### 5.1 The inventory and its lists

`inventory/site-enums-export.md` is this run's export: **68 sites** (RunDir 14, Worktree 11, Ref 8, Container
8, Object 6, Lock 6, Event 5, Snapshot 4, Answer 3, Process 2, Report 1) and **180 entries** — 171
`execution_observed` and 9 `recovery_proven` by label; 168 `executed`, 9 `recovery_proven` and 3
`not_executed` by evidence kind — each with its order, fault row, tabled action, evidence test and the number
of tests that executed it in this run's exports. The tree copies of the registry, the residue declarations and
synthetic half, `clippy.toml`, the allowlist, the wrapper classification and the funnel-module record are in
`inventory/`.

**Orders per site:** 68 sites, each with exactly one observable order on its executed entries; three
(`Worktree.AddStaging`, `Ref.PinPrepared`, `Object.ProposalCherryPick`) carry the fast no-execution record's
`None` beside it. That is the condition `PR3-REG-001-CONDITIONAL` and `PR4-REG-001-STILL-EQUIVALENT` name as
the guard of the registry's order-free key, **measured inactive at this range**.

**The lists.** The 171 `effects::` census tests passed in one invocation (`groups/group8-effects-censuses-q5.log`):
the clippy disallowed lists, the allow-placement scan, the wrapper classification, the funnel-module allowlist
and the frozen legacy allowlist. **The dependency review, stated as what it is:** no test at this range reviews
the locked dependencies' effectful APIs; the range adds no dependency (`Cargo.lock` unchanged since the G4
range), so there is nothing for such a review to review here, and no claim that one was performed is made.

### 5.2 The bijection, executed on both hosts

The merge check `st07_the_sequential_range_is_a_bijection_over_the_exported_observations` (ignored; reads the
observation export a full suite wrote) was run over this run's own exports:

| Host | Records | Funnel executions | Inventory | Entries | Witnessed claims | Failures |
|---|---|---|---|---|---|---|
| `unix` (Linux, export run 2) | 404 | 398 | 68 | 180 | 163 | **0** |
| `windows` (the guest, its own export) | 401 | 395 | 68 | 180 | **164** | **0** |

`st07/st07-merge-check-export2.log`, `st07/st07-summary-export2.json`, `guest/guest-after.log`,
`guest/exports/st07-summary.json`. The registry pin `the_sequential_registry_is_pinned` passed on both hosts;
`g5-registry-entry-dropped` (one entry removed from the pinned document) kills it. The one extra claim on
Windows is the Windows-only `Process.Spawn` `ambient_job_joined` error-return point.

### 5.3 The per-coordinate audit — ST-07's three obligations, judged separately

The audit is `audit/per-coordinate-audit.md`, with every row's candidate list and flags in
`audit/rows-classed.json`. It was built from the tree at this range, not inherited: for every one of the 180
entries the candidates are (a) the test the registry cites; (b) where that is an `#[ignore]`d kill child,
every committed test that spawns it, resolved through the constant it is named by, up to two hops; (c) **every
test that executed the coordinate at all**, from the observation exports of the full suite on **both hosts**
(the guest's export is what carries the Windows-only `Process.Spawn` points, which no Linux run can reach);
and (d) for every class not settled by (a)–(c), a whole-suite search by the site's own primitive names — which
is what finds the tests that construct a coordinate's state through `NoHooks` and therefore never appear among
its observers. Each candidate carries three flags computed over its body **with its same-file helpers inlined
to three hops**, and the replay flag matches all seven spellings the tree uses, including the inline
`assert_eq!(once.state(), twice.state())`.

| Grade | Rows | What it means |
|---|---|---|
| **F** | **146** | the exact prefix constructed, the tabled action performed by the recovery, the durable log replayed twice and asserted equal |
| **N** | **22** | constructed and performed, replay **vacuous**: a creation husk commits no schema-4 event and the recovery commits none |
| R | 0 | — |
| O | 0 | — |
| — | 12 | the 9 residue classes (§5.4) and the 3 fast no-execution records |

**So every one of the 168 executed coordinates meets ST-07's three obligations**, 146 with the replay
asserted and 22 where there is nothing to replay. The audit states the class behind each grade and names the
witness: the creation prefixes P0–P5b and the husk removals by the create suite's kill child at each
publication prefix and the creator's own armed error paths, with the next census performing the disposition;
the ST-18 finalization cells by #291's kill matrix; the kill-child sequences by their parents; the Event
points by the kill and error drivers and by #292's emit tests, whose `the_next_open_converges` replays the
proven prefix twice; the process funnel by `process_spawn_kill_child` and the helper
`a_kill_in_the_workers_spawn_converges_on_the_next_resume`, which ends with `assert_replays_twice_equal`; the
container intent, view and launch states by #292's fault tests and #293's
`each_container_state_a_dead_incarnations_launch_or_release_leaves_is_reclaimed_by_the_next_resume`; and the
answer-file coordinates by `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step`,
**a witness the registry does not cite**, which plants the staged `.partial`, drives a resume that ingests
nothing, publishes, drives the next incarnation that ingests it once, and asserts replay-twice on both sides.

**One caveat is stated rather than smoothed over**: the fake runtime's liveness vocabulary is
`Running | Exited | Gone`, so "created but never started" and "exited" are one observable state; the container
cells cover the states that runtime can distinguish, and no test arms a fault at `Container.Create` or
`Container.Start` itself.

**The Event sites' points, `OpenLog`'s `SyncPrefix`, the barrier's refusals and `IdUnread`.** Group 1 ran
`every_kill_point_of_the_event_funnels_kills_the_child_under_the_production_adapter` (9 points) and
`every_error_return_point_of_the_event_funnels_fires_under_the_production_adapter` (12 points), the shape
tests `a_kill_at_each_open_point_leaves_the_shape_the_packet_tables` and
`a_kill_at_each_append_point_leaves_the_shape_the_packet_tables`, the barrier's refusal cases
(`an_injected_sync_failure_at_open_names_syncprefix_and_hands_out_no_handle`,
`unstable_reread_after_open_sync_refuses_resumably`,
`an_unstable_reread_refuses_naming_prove_prefix_stable_and_hands_out_no_handle`,
`unsynced_line_made_durable_by_barrier_survives_later_power_loss`), the ordering test
`resume_establishes_stable_prefix_barrier_before_any_fold_derived_effect` and its refusal twin, and both
`IdUnread` kill tests — 69 tests in one invocation, all passed. Detection:
`g5-event-written-kill-point-skipped`, `g5-event-written-error-point-skipped`,
`g5-sync-prefix-point-not-consulted`, `g5-effect-before-barrier`, the three re-aimed `g5-barrier-*-ignored-log`
recipes, `g5-id-unread-point-skipped-kill` and `-record` — every one killed.

**The two-crash proof record** is §6's byte table and `groups/group2-two-crash.log` (2 passed).

### 5.4 The residue-class evidence record

`residue/residue-class-evidence-record.md`: nine declared residue sites, **24 (site, element) pairs**, every
one `constructed`, `classified: internal`, `recovered` in the synthetic half; three histograms produced by
this run's own sampler executions (PR5's four-command sampler, the T-ATTEMPT capture sampler and the ST-07
remaining-sites sampler), **every row `unclassified: 0` and `recovered: true`**, and the declared and sampled
site sets equal (9 = 9). The two samplers that write no file assert their classification inside the test; all
five ran **alone five times each on Linux (25 of 25 green)** and **five times each on the guest (25 of 25
green)** (`samplers/linux-loops.log`, `guest/guest-samplers.log`). Detection: `residue-synthetic-pin` and
`g5-residue-frozen-n-changed-pin` kill the two pins. **Every class is labeled recovery-proven; no
observed-execution claim is made for any Git-internal residue.**

## 6. Required artifact 4 — projection equivalence outputs

`probes/projection-equivalence-outputs.md`. Six committed tests were run `--nocapture` with a temporary probe
that prints, at the head of the two helpers every replay comparison goes through, the durable log being
compared — its length, its sha256 and its `merge_prepared`/`task_merged` counts. **19 comparisons**, each one
a comparison the helper then asserts: `live_vs_replay` parses the bytes on disk, replays them into a fresh
fold, compares the stepped run's own `RunState` with `==`, derives the schema-4 report from both and compares
the digests; `assert_log_replays_twice_equal` replays the same bytes twice and asserts the states equal. All
six tests passed, `cargo exit=0`, and the probe was restored (sha256 equal to `HEAD`, `git status` empty).

**The two-crash proof, with the bytes on disk before and after the simulated loss:**

| Point | Bytes | sha256 | `merge_prepared` | `task_merged` |
|---|---|---|---|---|
| positive: after the kill, before the loss | 4,266 | `58028da64b24fde6…` | 1 | 1 |
| positive: after `lose_unsynced_writes` | 4,057 | `9939eeb746c36875…` | **1** | **0** |
| negative: the unsynced `merge_prepared` before the barrier | 4,066 | `0ff393e0df66f045…` | 1 | 0 |
| negative: after the loss | 3,557 | `101a0a97d305db76…` | **0** | 0 |

The positive test asserts that the child's report reads exactly `synced <prefix including merge_prepared>`
then `cas`, that the ref is at `proposed_sha`, and that the next resume records the completed swap without
swapping again (`merged_sequences == [0]`, zero CAS entries, replay twice equal). The negative variant asserts
the refusal names `SyncPrefix`, **no CAS is issued**, the ref did not move, nothing was appended, the ledger
records no sync, and after the loss the candidate is still queued and integrates under sequence 0.
Detection: `g5-open-sync-skipped` kills the positive, `g5-sync-prefix-point-not-consulted` the negative.

## 7. Required artifact 5 — per-resource ledger outputs

`ledger-export2/`: `ledger-complete.md`, `ledger-parked.md`, `ledger-halted.md`, `ledger-budget-exceeded.md`,
`ledger-no-run-finished.md` and `ledger-no-run-finished-then-complete.md`, written through
`UPSTROKE_LEDGER_EXPORT` by the five ledger tests during export run 2; byte-identical to export run 1's. Each
table has **54 verdict rows** — R1–R28 and the named parts of R14 (9), R21 (13) and R27 (4) — and **every one
reads `yes` in every one of the six tables: 324 of 324**. The guest's six tables read `yes` in every row too,
differing from Linux in exactly one line each: `R21 › cleanup_lock_file`, `Present` on Unix and `Absent` on
Windows, where the Unix cleanup lease file does not exist.

`ledger-export2/packet-at-run-end-mapping.log` places **every `decisions.resource_accounting.rows[*].at_run_end`
cell against the row the ledger observed, per outcome**: 28 rows × 6 tables, **168 of 168 with `Holds = yes`**,
with each row's class, expected, before, after and the evidence sentence. Detection: `ledger-report-skipped`,
`ledger-prunes-unreachable`, `ledger-verification-defers-zero`, `ledger-verification-defers-from-queue`,
`finalize-removes-answers-at-complete`, `settle-scrub-removed`, `g5-candidates-refs-kept-at-complete`,
`g5-candidates-refs-pruned-at-halted`, `st18-fresh-branch-prunes-objects` and `-real-kill` — all killed.

## 8. The seven adversarial fault-test groups

Counted from the definition: **seven**. Each row says what ran, what was reasoned, and the recipe that shows
the named test still detects. Every group ran as one `cargo test` invocation by name
(`groups/group<N>-*.log`): **69, 2, 45, 13, 29, 16, 37, 171 and 16 tests passed, 0 failed, `cargo exit=0`**
in the nine invocations.

| # | The definition's claim (abridged) | What ran | Grade |
|---|---|---|---|
| 1 | the exhaustive kill matrix over every Topology/Shared site × order × resume, incl. kills during recovery, run-end closure and terminal finalization, Event append points (kill and error return), OpenLog's SyncPrefix (both modes), and kills inside Git commands (sampled) | the registry's 168 executed coordinates with the bijection checked on both hosts (§5.2) and **the per-coordinate audit on ST-07's three obligations (F 146, N 22)**; #291's committed kill matrix — a **real kill** at each of 26 Complete and 24 Halted cells, each resumed in a fresh process; `kill_during_recovery_repeats_recovery`, `kill_inside_closure_recovers`, `kill_after_run_started_creates_integration_ref`, the create suite's kills at all 13 publication prefixes; the Event points in both modes and the barrier's refusals; #292's kills and faults at the worktree lease, lock file, spawn, terminate, git view, candidate sequence, question payload and answer ingestion; the five samplers alone on both hosts | **established**, and the kill-equivalence gap run 1 recorded (50 cells as error returns, one real kill) is **closed** by #291 |
| 2 | the two-crash proof, positive and negative | both tests, with the bytes on disk before and after each simulated loss (§6) | **established** |
| 3 | recovery of damaged or leftover state: two coordinators refused by locks; edited config warns; deleted worktrees and pruned non-authoritative refs recovered; Retained generations closed before `run_resumed`; snapshot, container, husk and stale-marker residue | 45 tests in one invocation, including `a_second_process_is_refused_the_run_lock`, `a_surviving_reaper_hold_refuses_the_next_coordinator_until_released`, `resume_rebuilds_runner_from_record_and_warns_on_config_drift`, the husk family at every prefix, the container census family, the stale-marker repairs, **#292's `a_resume_after_finalization_pruned_the_execution_root_and_the_pins_recreates_the_root_and_integrates_the_queued_candidate`** (the pruned non-authoritative refs run 1 could only reason) and **#293's `each_container_state_…`** | **established**; the two checks run 1 carried on uncommitted witnesses are now committed tests |
| 4 | ST-17 run-end closure per class and outcome, every derived-outcome refusal, and the pre-`budget_exceeded` counterexample | 13 tests in one invocation, plus the census's outcome equality in both directions | **established** |
| 5 | ST-18 terminal finalization: kills after `run_finished`, after the report write and between every cleanup effect converge; resume of Complete/Halted finalizes then refuses; the report lists retained candidate refs at Halted; answer files and `.partial` untouched and never ingested; a late answer writes an inert file | 29 tests in one invocation, including #291's two kill matrices, #289's three new rundir witnesses, `late_answer_after_finalization_is_inert_and_reported_not_live`, `answer_files_untouched_by_finalization`, `halted_report_lists_candidate_refs` and the report-barrier family | **established** |
| 6 | outcome-specific resume behaviour: Halted retains and lists; Complete prunes and leaves objects to Git; BudgetExceeded resume continues queued candidates, parked repairs and woken deferred items; answers on disk ingested only by the resume and only for still-open questions | 16 tests in one invocation, including `resuming_with_a_higher_ceiling_continues_the_run_the_budget_stopped`, `resume_clears_budget_stop_and_wakes_deferred`, `a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume`, `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch` and **`a_kill_at_the_answer_ingestion_converges_on_the_next_incarnation`'s two committed tests**, whose helper asserts a further resume with the file still on disk ingests nothing because its question is closed | **established**; the sequence run 1 executed with an uncommitted witness is now committed |
| 7 | the packet's seam tests in range: ST-07 full; ST-09 durable rows incl. R21, the owner and commit records, R27 with the fsck inventory; ST-10; ST-11; ST-12; ST-13 incl. the fast no-object proof; ST-14 incl. totality, live-versus-replay equality and `merge_prepared` relations; ST-15; ST-19; ST-20 | 37 + 16 tests in two invocations, plus §4 (ST-14), §5 (ST-07), §7 (ST-09) | **established**, with ST-13's provisional-reservation lifecycle beyond the fast no-object proof named as G3's evidence: `fast_path_publishes_exact_candidate_without_staging_or_proposal_object` and R13 `Zero` in all six ledgers are what this range carries |

## 9. Invariants, findings, deferrals and flakes

### 9.1 The five integrated invariants

The first is §7 (every durable row `yes` in six tables on two hosts; the process-local rows observed and
named as G6's). The second is §5 (0 bijection failures on both hosts; 180 entries; one observable order per
site; the residue record; the 171 effects tests) **with the per-coordinate audit now meeting ST-07's three
obligations at every executed coordinate**. The third is §6. The fourth is §7 and §8 rows 4–6. The fifth is
§4.

### 9.2 The findings in range

The ledger at this range holds 348 files (`README.md`, `PROCESS.md` and 346 findings): **9 P1, 139 P2, 198
P3**, and **every one carries `disposition: deferred` or `accepted-risk`** — no file carries `open`
(`findings/g5-term-hits.tsv` lists the 27 whose text names a G5 term or a registry site).

**Run 1's three blocking findings are closed.** `PR10-ST18-THIRD-RESUME-LEFTOVER-ASSERTION` (the P1 that
failed run 1's clause 9), `PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION` and
`PR10-PRIVATE-RECORD-BARRIERS-UNWITNESSED` were deleted by #289 (`git diff --name-status 2570e423^1
2570e423 -- findings`), and this run's mutations show the committed tests that replaced them detect the
shapes they name (§3.5).

**Still open, and bearing on this gate:**

| Finding | What it says | Bearing |
|---|---|---|
| `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED` (P2) | the record and directory a report writer leaves between `Report.Write`'s two hook phases has no residue class | **clause 4's named exception**: the packet's residue clause is about the Object sites' command-internal classes, which are complete and labeled; this leftover is not one of them. Its reclamation is executed (the matrix's third resume, #289) and its durability window is now closed (§3.5) |
| `PR291-SNAPSHOT-OWNER-READINESS-BOUND-EXPIRED-ON-A-LOADED-GUEST` (P2) | a 15-second readiness bound in `src/workspace.rs` expires on a loaded guest | a test-bound defect, not an engine one; the guest suite here was green (§1) |
| `PR292-MACOS-ZOMBIE-ONLY-GROUP-EPERM-FAILS-KILL-TREE` (P3) | on macOS `kill_tree` can report a group it killed as not gone when the group holds only zombies | macOS is outside this run's measurements; CI's macOS leg is green at the range |
| `PR10-LEGACY-REPORT-REWRITE-DROPS-ITS-POSIX-ACL` (P2) | rewriting a schema-3 report drops its POSIX ACL | v0.1-path; no pass-rule clause reads it |
| `PR3-REG-001-CONDITIONAL`, `PR4-REG-001-STILL-EQUIVALENT` (P3) | the registry's order-free key is equivalent only while every site has ≤ 1 observable order | **measured inactive at this range** (§5.1); no effect on clause 2 |
| `PR3-FRAMEWORK-SILENT-1..5` (P3, docs-contract) | the design does not state five rules the residue authority derives in code | every action the authority takes is executed here; a missing design sentence, not a missing witness |

**One finding is filed by this gate**: `G5RUN2-DOCKER-DIAGNOSTIC-TEST-DEPENDS-ON-A-SHORT-TMPDIR` (P3,
`findings/P3_correctness_202609160140_…`), the long-`TMPDIR` sensitivity of
`real_docker_prints_the_transcribed_unreachable_diagnostics`, executed both ways (§9.4). It is a test-robustness
defect, not an engine one, and bears on no pass-rule clause.

### 9.3 The deferrals, and what they do to the verdict

- **`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002` are not deferrals of this range**: no finding file carries
  either id at `a2fe09c2`, and the commits that removed them (`7da321c0` and `1c309d49`) are ancestors of
  PR10's merge base `231c1aad` as well as of the range (`git merge-base --is-ancestor`, both `yes`). Verified
  in `findings/` and in `git log`, not taken from a record.
- **`PR5-RD-002` (P1, crash-consistency, deferred) is inside the path this gate certifies and weakens its
  pass.** A kill inside `git worktree add`'s registration window can leave a zero-length `commondir`, after
  which `git worktree list --porcelain -z` fails and `WorkspaceManager` calls that revalidate propagate the
  error — including the reclaim loops a resume runs. #295 appended a reproduction to its file **inside this
  range**. No test at this range constructs that shape inside a topology resume, so nothing here contradicts
  it and nothing here establishes that a resume converges through it. The gate reports it as a deferral that
  weakens clause 9 rather than treating it as closed (§13).
- The other eight P1s are outside this range's subject (two prose-scan and shape-gate findings in
  `.github`/`scripts`, the legacy workspace's replacement objects, the empty classification domain, the
  Git-discovery table, the scratch pre-clean, the decoder guard, and the intermittent kill/settle class) and
  are deferred with named guards, as at G4.
- **`SWEEP-CHECK_INTEGRATION-003`** (P3, deferred) records that `ladder::next_step` allows `max_defers`
  deferrals where the fold allows `max_defers − 1`. The census's bound is the fold's reading, which is the
  reading the packet's dispositions, refusals and the 2026-09-15 amendment all take, so it bears on no
  pass-rule clause here; it is the design's sentence to settle, not this gate's.
- **The two packet mentions the clause-1 amendment left unchanged** (`decisions.task_registry.validation_at_fold[11]`'s
  `defers <= the frozen max_defers`, and `v11_verification_dispositions[0].rationale`) bear on **no**
  pass-rule clause: the census clause reads `decisions.bounded_census.bounds`, which is corrected, and both
  mentions carry their own refusal of a `Deferred` at `max_defers` beside them.

### 9.4 The flakes and measurement artifacts on this run, fingerprinted

| Fingerprint | Where | Control | Finding |
|---|---|---|---|
| `real_docker_prints_the_transcribed_unreachable_diagnostics`, `src/runner/container/tests.rs:4114`, *"unix socket path … is too long"* | export run 1 (1 of 3 Linux full runs) | **executed both ways**: the same test alone fails under a 96-character `TMPDIR` (`rc=101`) and passes under a 29-character one (`rc=0`) — my own measurement environment, not the range | filed here as `G5RUN2-DOCKER-DIAGNOSTIC-TEST-DEPENDS-ON-A-SHORT-TMPDIR` (P3) |
| `real_docker_kill_on_an_already_exited_container_is_tolerated` and `real_docker_returns_both_streams_of_a_container_separately`, `container/tests.rs:3436`, *"still running after 200 observations"* | the Docker-required run (2 of 23) | 3 of 3 alone each, Docker still required | `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME` (P3, deferred) |
| `a_host_integration_reaper_holds_the_runs_cleanup_lease`, `src/engine/topology/recover/tests.rs:8841`, *"the hold outlived the reaper that took it"* | the **first** gate attempt at this report's head `5baf00c` (§11) | **3 of 3 alone** at that head (`flakes/alone-5baf00c.log`); the second attempt green | `PR281-CLEANUP-LEASE-HOLD-OUTLIVED-AND-ITS-UNREADABLE-TWIN` (P2, deferred), which names this test and this assertion |
| `real_docker_returns_both_streams_of_a_container_separately`, same location and message as above | the same attempt, beside the reaper red | **3 of 3 alone** at that head (`flakes/alone-5baf00c.log`) | `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME` (P3, deferred) |

Both reds at the report's head are tests this branch does not touch — `git diff --name-only a2fe09c2 HEAD`
is this report and one finding file, and no code — and both are filed by assertion text and location. No
other red occurred anywhere on this run: not in the gate run at the frozen range, the guest suite, any group
invocation, any sampler loop, any probe or any mutation run.

## 10. The six standing questions

Each is answered for **G5's portion** of the question, with the earlier gates' portions named and not
re-claimed.

**Q1 — reconstruction from durable state alone.** *Answered by replaying the bytes on disk*, not by reading
the code: 19 comparisons in which the durable log is parsed from disk, replayed into a fresh fold and compared
against the live fold (state by `==` and by report digest), and replayed twice with the states asserted equal
(§6, each with the log's length and sha256); the census's classification equal live and on replay over every
explored state of every family member (§4); the stable-prefix barrier established before any fold-derived
effect and its four refusal predicates each guarded by a killed mutation (§5.3); the two-crash proof (§6);
the husk, marker, intent and container reclaims of §8 row 3; the Runner rebuilt from `run_started(4).runner`
with the drift warning. The process-local holdings (R3, R4, R13, R17, R22, R28) are observed `Zero`/`Absent`/
`Balanced` at every outcome in six ledgers and are **certified at G6, not here**. **Answered with evidence for
G5's portion.**

**Q2 — resource accounting.** Six ledgers × 54 verdict rows, **324 of 324 `yes`**, on Linux and on the guest,
with every packet `at_run_end` cell placed against its observed row (168 of 168, §7); `every_outcome_equation_names_every_row_once`;
R21's answer and `.partial` files retained at Complete; R11 retained at Parked and BudgetExceeded and pruned
at Complete; R27 balanced with the `fsck`-inventoried unreachable objects monotone. **The negative run 1
recorded is gone**: the reclaimed staging directory the power-loss window stranded is now reclaimable, and the
witness that proves it is committed (§3.5). What remains outside the accounting is the *classification* of
that leftover, not its reclamation (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`). **Answered with evidence
for G5's portion, with that one exception named.**

**Q3 — stale completions.** The append-error protocol tests at `Written`, `WrittenFull` and the undetermined
case; `append_error_inside_closure_ends_command_and_resume_completes_closure`; the NoRunFinished ledger
(nothing derived from the poisoned fold, R17 released with the process); `every_error_return_point_of_the_event_funnels_fires_under_the_production_adapter`.
`closure-swallows-close-append-error` and `closure-swallows-undetermined-close-append` kill their tests.
**Answered with evidence for G5's portion**; the stale-completion, slot-release and provisional-conversion
halves are G3's and G4's.

**Q4 — aliasing.** The husk family (private halves reclaimed only under the ownership proof and only without
a commit record; the unprovable and the possibly committed retained and reported), the create suite's kills at
all 13 publication prefixes converging to ST-19's disposition, the fast publication's exact-base relations in
both directions with the wire type's own refusal beside the fold's, and the report staging directory named
uniquely per write and reclaimed only by its record — now with its deletion made durable before the record is
retired (§3.5). **Answered with evidence for G5's portion.**

**Q5 — effect/append boundaries.** Both hook phases and every parent-side point of the 68 sites observed
executed, the bijection with **0 failures on both hosts**, 180 entries with one observable order per site, and
— this is the part run 1 could not establish — **executed evidence in ST-07's own sense at every executed
coordinate**: 146 with the prefix constructed, the tabled action performed and replay twice asserted, 22 where
replay is vacuous (§5.3). The residue classes are recovery-proven per element and sampled on both hosts, never
claimed as parent-observed execution. The clippy disallowed list, the allow-placement scan, the wrapper
classification, the funnel-module allowlist and the frozen legacy allowlist are held by 171 tests in one
invocation. The dependency review is a change-time obligation and no dependency entered the range, which is
stated rather than claimed as a review. **Answered with evidence.**

**Q6 — sequential guarantees.** One fold and one writer (the barrier is the only way a log becomes a fold; the
run lock refuses a second process, `g5-run-lock-shared` killed); the untouched user checkout (two acceptance
mutations killed); exact-base fast publication of the candidate object itself; atomic settlements; the derived
outcome from durable inputs with halt > budget > parked > complete and pending backoff never blocking Halted
or BudgetExceeded (`st17-budget-outranks-halt`, `st17-halted-with-backoff-refused` killed); `budget_exceeded`
recorded before any budget-driven end (`g5-breach-ends-without-budget-exceeded` killed); closure and
finalization (§8 rows 4–5); no answer ingestion after a halt or budget stop in the same epoch, and ingestion
only by the resume and only for still-open questions — now by committed tests; one Runner identity per run
honoured by every resume. **At max_parallel = 1 there is one schedule**, so "identical canonical authoritative
trace where scheduling must not matter" has nothing to vary and **is not measured**, which is said rather than
implied. **Answered with evidence for G5's portion.**

## 11. The ten gates at this report's own head

The ten gates ran at the frozen range before anything else (§1) and again at the commits that carry this
report. Every attempt is kept, with its box load beside it (`gates/`).

| Head | What it is | Result | Logs |
|---|---|---|---|
| `a2fe09c2…` | the frozen range | `ALL 9 PASS at a2fe09c` **first attempt**; `test-pr-ready-audit: ok` | `gates/w1-eight-iso-a2fe09c-range-1.log`, `gates/eight-logs-a2fe09c-range-1/`, `gates/gate10-a2fe09c-range-1.log` |
| `5baf00cebb748e8b0fe52585acf5b7eb74e10ba4` | this report and the finding file | **two attempts.** Attempt 1 (01:53:49Z) **FAIL on the test leg alone** — 2671 passed, **2 failed**, 90 ignored: `a_host_integration_reaper_holds_the_runs_cleanup_lease` (*"the hold outlived the reaper that took it"*) and `real_docker_returns_both_streams_of_a_container_separately` (*"still running after 200 observations"*), the two filed box flakes of §9.4, **each 3 of 3 green alone** between the attempts; the other eight legs passed. Attempt 2 (01:57:23Z) **`ALL 9 PASS at 5baf00c`** — fmt 1s, clippy 0s, test 115s, msrv 0s, the five scripts. The tenth gate: `test-pr-ready-audit: ok` at both attempts | `gates/w1-eight-iso-5baf00c-head-1.log` (failed), `-head-1-attempt2.log`; `gates/eight-logs-5baf00c-head-1/`, `eight-logs-5baf00c-head-1-attempt2/`; `gates/gate10-5baf00c-head-1*.log`; `gates/box-load-5baf00c-*.log`; `flakes/alone-5baf00c.log` |
| `8b8acc0153ec99a53692c79a5a0d3102c975b358` | `5baf00ce` plus §9.4's two flake rows and this table's second row | **two attempts.** Attempt 1 (02:00:52Z, load average 5.89) **FAIL on the test leg alone** — 2672 passed, **1 failed**, 90 ignored: `a_host_integration_reaper_holds_the_runs_cleanup_lease` again, same assertion and same line, **3 of 3 green alone** between the attempts (`flakes/alone-8b8acc0.log`). Attempt 2 (02:03:00Z) **`ALL 9 PASS at 8b8acc0`** — fmt 1s, clippy 0s, test 106s, msrv 0s, the five scripts; `test-pr-ready-audit: ok` at both | `gates/w1-eight-iso-8b8acc0-head-2.log` (failed), `-head-2-attempt2.log`; `gates/eight-logs-8b8acc0-head-2/`, `-head-2-attempt2/`; `gates/gate10-8b8acc0-head-2*.log`; `gates/box-load-8b8acc0-head-2*.log` |
| the head this branch pushes | that commit plus this table's own last two rows, and nothing else (`git diff --stat` between them is this file alone) | gated the same way — ten gates — before the push, every attempt kept with its box load measured, and green at the attempt that pushed | `gates/` with the label `head-3`: `w1-eight-iso-<the pushed sha7>-head-3*.log`, `eight-logs-<the pushed sha7>-head-3*/`, `gate10-<the pushed sha7>-head-3.log`, `box-load-<the pushed sha7>-head-3*.log` |

## 12. The evidence directory

`~/tactus-artifacts/g5-evidence-a2fe09c/`, indexed by its `README.md`, with `MANIFEST.sha256` listing every
file and its sha256. The five required artifacts are:

| # | Artifact | File |
|---|---|---|
| 1 | the gate report | this file, copied in as `report-<sha7>.md` |
| 2 | the bounded census artifact | `census-export2/census-summary.json` and `census-export2/bounded-census-artifact.md` |
| 3 | the effect-site inventory, the fault-injection registry with executed evidence per site/order, and the residue-class evidence record | `inventory/site-enums-export.md`, `inventory/sequential-registry.json`, `audit/per-coordinate-audit.md`, `residue/residue-class-evidence-record.md`, `st07/`, `guest/exports/` |
| 4 | projection equivalence outputs | `probes/projection-equivalence-outputs.md` |
| 5 | per-resource ledger outputs | `ledger-export2/` (six tables) and `ledger-export2/packet-at-run-end-mapping.log` |

## 13. The verdict against the pass rule, clause by clause

The rule, verbatim from the regenerated definition (`spec/G5-DEFINITION.txt`, equal to the packet's):

> pass iff bounded census family complete (the prefix at its stated ceiling with truncation reported, every
> seeded per-dimension census closed, every bound reached by equality over the union) with totality asserted
> over the union and every plan_transition arm covered, site bijection complete with executed evidence for
> every Topology/Shared site, order, and parent-executed point (incl. OpenLog SyncPrefix and the stable-prefix
> barrier refusal cases), the two-crash proof green, residue-class evidence complete and labeled, all rows
> green, projections equal, per-resource ledgers satisfy their outcome equations incl. derived outcome,
> closure, and finalization, questions answered, no open critical/high finding; unbounded closure,
> effect-phase census, and parent-observed Git-internal execution claims are not made

Counted here: **nine** positive clauses and **three** prohibitions. Each is graded in the verification lens's
vocabulary — **established**, **asserted** or **silent**.

| # | Clause | Grade | How |
|---|---|---|---|
| 1 | bounded census family complete, with totality over the union and every `plan_transition` arm covered | **established** | §4: the prefix at 20,000 of 20,000 states with `truncated: true`; the seeded member closed at 25 states under its 5,000 ceiling with no truncated offer and no state at the trace ceiling; `union_reaches` equal to the declared bound on all ten dimensions and `every_bound_reached_over_the_union: true`, on the **amended** bounds line; totality asserted over every member with the `FoldError` arm never reached and the pre-`budget_exceeded` counterexample by name; the 24 production arms all executed. The three plan shapes are explored, two of them outside the family at a stated ceiling with truncation reported — stated in §4 with the alternative reading that would fail this clause |
| 2 | site bijection complete with executed evidence for every site, order and parent-executed point, incl. OpenLog SyncPrefix and the barrier's refusal cases | **established** | §5.2 and §5.3: 0 bijection failures on **both** hosts over this run's own exports, 180 entries, one observable order per site; and the per-coordinate audit, rebuilt from the tree with a four-source search per row: **every one of the 168 executed coordinates meets ST-07's three obligations** — 146 with replay twice asserted, 22 where replay is vacuous. `OpenLog`'s `SyncPrefix` is executed in both modes with the barrier's four refusal predicates each guarded by a killed mutation; both `IdUnread` points are executed and guarded |
| 3 | the two-crash proof green | **established** | §6: both variants green with the bytes on disk before and after each simulated loss; `g5-open-sync-skipped` and `g5-sync-prefix-point-not-consulted` kill them |
| 4 | residue-class evidence complete and labeled | **established for the packet's nine declared classes, with one residue outside them named** | §5.4: 24 (site, element) pairs constructed, classified and recovered; three histograms from this run with `unclassified: 0` and `recovered: true` on every row; declared and sampled sets equal; every class labeled recovery-proven with no observed-execution claim; both pins killed by mutation. The report-site leftover (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`) is a residue of the run's own making that is not an Object site's command-internal residue and has no class; it is stated, not counted as covered |
| 5 | all rows green | **established** | §7: 324 of 324 `yes` across six tables on Linux and the same on the guest; the cumulative suite green on the exact range on both hosts; CI green on all three platforms in the newest run per check name |
| 6 | projections equal | **established** | §6: 19 printed comparisons, each asserted; three projection mutations killed |
| 7 | per-resource ledgers satisfy their outcome equations incl. derived outcome, closure and finalization | **established** | §7 and §8 rows 4–5: every equation satisfied at all five outcomes plus the resumed table, with every packet `at_run_end` cell mapped to its observed row; the derived outcome equal to the recorded one with every listed refusal executed; finalization idempotent across the third resume **by a committed assertion** (#289), and the power-loss window that stranded a directory outside every equation at run 1's range **closed and witnessed** (§3.5) |
| 8 | questions answered | **established** | §10: all six answered with evidence and scoped to G5's portion; Q1 by replaying the bytes on disk; Q2 now without the negative run 1 recorded, with the unclassified leftover named |
| 9 | no open critical/high finding | **established on the ledger's own terms, weakened by one deferral** | §9.2–§9.3: no finding at this range carries `disposition: open`; the P1 on G5's own subject that failed run 1 is closed by #289 and its file is gone; the nine remaining P1s are deferred with named guards. **`PR5-RD-002` is a deferred P1 inside the recovery path this gate certifies**, re-confirmed as reproducing inside this range by #295's own note; this gate reports it as weakening the pass rather than treating it as closed |
| 10 | unbounded closure claims are not made | **not made** | §4 says closure of the space under the bounds is claimed of nothing and unbounded closure is not claimed; the prefix is a truncated breadth-first prefix and says so |
| 11 | effect-phase census claims are not made | **not made** | §4 and §5: no effect-phase distinction is claimed of the census; they are the typed registry's alone |
| 12 | parent-observed Git-internal execution claims are not made | **not made** | §5.4: every residue class is labeled recovery-proven — synthetic construction plus kill sampling — and no sentence in this report claims the parent observed a Git child's internal execution |

### The verdict

**G5 passes on this range.** All nine positive clauses are established on evidence produced at `a2fe09c2`,
and the three prohibited claims are not made. **PR11 and PR12 are unblocked by G5.**

Three things the pass does **not** say, so they are not read into it:

- **Clause 4 holds for the packet's nine declared residue classes, with one residue of the run's own making
  outside the registry's vocabulary** (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`, P2, deferred). Its
  reclamation and its durability are executed here; its *classification* is not.
- **Clause 9 holds on the ledger's own terms and is weakened by `PR5-RD-002`**, a deferred P1 whose subject —
  Git's worktree enumeration after a torn registration — sits inside the reclaim loops a resume runs, and
  which no test at this range exercises inside a topology resume.
- **The evidence is Linux and the Windows guest.** Nothing was measured on macOS beyond reading CI's runs at
  the sha; the samplers, the merge check, the full suite and the ledgers ran on both hosts, and the
  Windows-only `Process.Spawn` points are established from the guest's own observation export.
