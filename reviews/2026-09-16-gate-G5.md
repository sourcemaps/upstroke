# G5 — cumulative review gate: complete crash/resume across the max_parallel = 1 topology (second run)

**Gate** G5, *Complete crash/resume across the max_parallel = 1 topology (bounded census + typed site
bijection registry + derived run outcome and finalization)*.
**Frozen input range** `a2fe09c2414996485a95c0b916f057ece8201ed7` — the merge commit that put pull
request #292 on `master` at 2026-09-15T22:18:29Z, confirmed on entry with `git rev-parse HEAD` in
`/srv/worktrees/g5-run2` (`git status --porcelain` empty; branch `gate/g5-run2` carries this report and one
finding file, and nothing else). `git diff HEAD -- src effects Cargo.toml Cargo.lock` is empty at every
measurement below; `HEAD:src` is tree `e8ebfe15b1f6385423175db9484054d5829f17ef` and `HEAD:effects` is
`2a5869775c6515e0d3477762481cdd081eb8fde6`.
**This is G5's second run, after five review rounds of its own.** Run 1 (`g5_run1`, range `caf6bed0`,
`reviews/2026-09-14-gate-G5.md`, draft #288) failed on clauses 1, 2, 7 and 9. This run judged the corrected
range from scratch. Its heads `31c5fbd8`, `2f388103`, `1bdd6d7c`, `7cde80cb` and `17980022` were each reviewed and each
returned CHANGES_REQUIRED (rounds 1 to 5; §0 records every finding and what the head after it did). The first three
rounds found claims wider than their evidence; round 4 found claims narrower than their evidence; round 5, the first
round whose three lenses could execute, found both kinds again, a wrong table size among them, and found that each
earlier repair had corrected the instances it was shown and left others of the same kind. This head is the sixth,
written by a third fresh session, and it works round 5's findings as classes: §0's round-5 table says, for each, what
was searched and the count the search returned.
**Platform of every Linux measurement** `x86_64-unknown-linux-gnu`, kernel 6.8.0-137-generic, cargo 1.97.1
(c980f4866 2026-06-30), rustc 1.97.1 (8bab26f4f 2026-07-14), `cargo +1.85.0` 1.85.0, git 2.43.0, Docker
29.7.2 client and server, shell umask `077`, 32 processors (`spec/platform-versions-host.log`).
**Platform of every Windows measurement** the persistent guest `windowsguest`, in the Gate 5 programme's own
tree `C:\upstroke-g5` fetched to this range, cargo 1.97.1, rustc 1.97.1, git 2.50.1.windows.1
(`guest/guest-fetch-build.log`).
**Evidence** every figure quoted here is in a file this report names, under
`~/tactus-artifacts/g5-evidence-a2fe09c/` on the build box (`README.md` indexes it, `MANIFEST.sha256` lists
every file with its sha256). Paths below of the form `gates/…`, `audit/…` are relative to that directory.

**Verdict: G5 does NOT pass on this range.** **Clause 1 is not established** — the census family's union
explores one of the three plan shapes the amended bounds line names — and that alone decides a conjunction.
**Clause 2 is asserted** and **clause 8 is asserted**. Two groups' universal claims are graded *asserted*
rather than established (group 1's exhaustive kill matrix, group 5's "between every cleanup effect"). The
other six positive clauses are established and the three prohibited claims are not made (§13). **PR11 and
PR12 stay blocked by G5.**

**What the review rounds changed, plainly, head by head.** Review round 2 found the rebuilt per-coordinate audit
unsound in both directions and showed it with committed witnesses, two of which it executed. **That audit's partition —
`F 107 / R 20 / O 41` — is withdrawn in full** (§5.3, since `71aede1b`), and stays withdrawn. It is replaced by what this
gate can actually show at this range: a per-coordinate certification of the 32 parent-executed point coordinates, where
the harness records a `Point` observation **only when the armed fault fired**, on top of the merge check's
machine-checked claim table; and, for the 136 before/after coordinates, **no partition and no absence claim at
all**, because this gate did not produce one that a reader can re-derive. Review round 3 found that the replacement
measurement counted an adapter unit test's record as a constructed crash prefix; `b95f03fe` corrected the generator
with the merge check's own adapter-test exclusion and re-derived its figures — the fault fired, in a funnel-execution
record, at **27** points in the Linux export and **28** in the guest's, **32** in their union and 23 in both — and
began assessing every filed finding for its bearing (§9.2). Review round 4 found the report understating its
evidence; `17980022` credited row 14's committed witness, so ST-07's three obligations are certified at **29** of the
32, the other three (rows 1, 2 and 23) carrying a stated gap. Review round 5 found, among other things, that the claim
table holds 168 claims, not the 172 this report had said since `1bdd6d7c`, that row 14's replay-twice check has no
power over the line its own coordinate leaves, and that the assessment had re-read only part of its `none` class.
**This head** measures that power for every row whose replay-twice assertion is established and whose witness runs on
Linux, and states the limit where it is nil
(rows 8, 9 and 14), states that rows 14 and 18 rest on arming and an observed death rather than on an export record,
re-reads every one of the 347 finding rows, carries two more operator-only convergence shapes into clause 7, Q1 and Q6
(`PR5-RD-002`'s, which review round 5 named, and `PR5-RD-003`'s, which the re-read found), and re-derives each
corrected figure from a saved file (`repair-r5/figures.out`). So **clause 2 is *asserted***: the
bijection and the claim table are established, executed, on both hosts; the certified layer is stated at its size
and with its limits; and no absence is claimed anywhere. The verdict does not change, and **its ground does not change
either**: G5 fails on clause 1, whose failure is established by reading `family()`.

## 0. What this run is, what the range contains, and what the review rounds changed

### The specification, regenerated from the packet

`~/tactus-artifacts/G5-DEFINITION.txt` and `G5-QUESTIONS.txt` were regenerated from
`~/tactus-artifacts/tactus-parallel-design-neutral-v16.json` (sha256
`8b3991c804d74c91a0fe162304b68249991d4104338bed8647da88ec5da50813`) on entry and diffed against the files:
**0 differences** — every field of `cumulative_review_gates.gates[4]` equal in packet order, the six standing
questions equal, and a byte-for-byte regeneration of the whole extract (`spec/regen-definition.log`,
`spec/regen_definition.py`). The gate's entry holds **five** `integrated_invariants`, a **six**-row
`state_resource_ownership_matrix`, **seven** `adversarial_fault_tests`, **five** `required_artifacts`, the
`reviewer_remit` and the `pass_fail_rule`. Both amendments are in `spec/`; **clause 1 is judged against the
amended bounds line**, read from the packet itself (`spec/packet-bounds-line.log`).

### What changed since run 1's range

`git log --merges --first-parent caf6bed0..a2fe09c2` (`spec/merges-since-run1.log`) — ten merges, every one
named:

| Merge | Pull request | What it is, and what it does to this gate |
|---|---|---|
| `a2fe09c2` | #292 | clause 2's O coordinates: witnesses that construct and recover them, a per-process observation export, registry citations. Judged in §3.5 |
| `e7c5b082` | #295 | a reproduction note appended to `PR5-RD-002`'s finding file. Bears on §9.3 |
| `60a5ac4f` | #294 | closes the macOS ready-red P1 finding (file deleted) |
| `f9572a73` | #293 | clause 2's R rows: replay-twice assertions at the recovered coordinates. Judged in §3.5 |
| `00856bbe` | #291 | the committed finalization kill matrix (26 Complete + 24 Halted cells, each resumed in a fresh process). Judged in §3.5; filed `PR291-SNAPSHOT-OWNER-READINESS-BOUND-EXPIRED-ON-A-LOADED-GUEST` (P2, deferred) |
| `03ca21f1` | #290 | the o3 attribution vocabulary. Not judged by a recipe of its own, but not outside the subject either: it adds an `attribution` field to the `design_defect` topology event the fold replays (a no-op in `apply`, `src/topology/fold/apply.rs:64`), changes how the census generator's `design_defect` candidate is built (`src/topology/census.rs:1627-1636`), and adds 11 names to `effects/wrappers.toml`'s `effect_free` lists, the classification Q5 asks about. It is measured here only through the suite, the census and the Q5 censuses that ran (§1, §4, §5.1), all green |
| `2570e423` | #289 | clauses 7 and 9: the reclaim's observing barrier, `publish_report`'s order, the ST-18 third-resume assertion, two new private-record witnesses. Judged in §3.5; closed three findings |
| `aff2b024` | #280 | `.github/scripts/` — instruments, no engine code |
| `0d349480` | #286 | `.github/scripts/`, `scripts/` — instruments |
| `8b28944f` | #287 | `src/agent/proc.rs`: bounded kill and wait at five sites (closed a P1). Inside the process funnel §5 audits |

`Cargo.lock` is unchanged across the range and since the G4 range `7e0110a1`; `Cargo.toml`'s only change in
that span is a package `exclude` (`range/diff-since-run1.log`). No dependency entered the range.

### Review round 1 of this report, and what this head does about each finding

Both lenses returned CHANGES_REQUIRED at `31c5fbd8` (`~/orch-pr10/reviews/rg5r2-1/`, posted on #300). Each
finding is answered by producing the evidence, or by narrowing the claim and re-grading with it:

| Finding | What this head does |
|---|---|
| verify P1 1 — the coordinate audit credits witnesses whose durable prefix is not the coordinate's (row 56; rows 69/129) | **The audit is rebuilt on a different rule** (§5.3): obligation (i) is decided by *where a committed test arms its fault*, never by which tests passed through the site. Every row's witness is re-derived and the search is stated in the artifact. The result was **F 107, R 20, O 41**, not the first head's 146/22/0. **Review round 2 refuted that too, and this head withdraws it** — see the round-2 table below and §5.3 |
| verify P1 2 — clause 1 passes by substituting the implementation's `dimensions()` for the packet's bounds | **Clause 1 is re-graded not established** (§4, §13). The amended bounds line names three plan shapes; the family's union explores one. The tree change that would fix it is named; none is made |
| verify P1 3 — synthetic recovery plus 50 finalization cells do not establish an *exhaustive* kill matrix | **Group 1's exhaustiveness is re-graded asserted** (§8), with what *is* established stated separately: the 50 real kill cells, the Event points, the barrier, the samplers. Group 5's "between every cleanup effect" is re-graded asserted for the same reason |
| verify P1 4 / record P1 2 — the `fix289-record-removed-before-barrier` survival was excused at the wrong loss point; class rule 4 wants both sides | **Executed**: the same mutation against the committed witness that observes the **earlier** loss point, `a_refused_public_barrier_leaves_the_record_of_the_staging_directory_it_removed`, is **killed** (`rc=101`). The "nothing is stranded" sentence is withdrawn and replaced by the two-sided evidence (§3.5) |
| record P1 3 — the private-record swallow mutations were never run against #289's replacement test | **Executed**: both swallows against `a_report_staging_record_failure_stops_before_directory_creation` are **killed** (`rc=101` each). The old recipes, aimed at the test they were written for, still pass, and that is said (§3.5) |
| verify P1 5 / record P1 5 — `PR7-WRAPPERS-EMPTY-DOMAIN` is wrongly placed outside the gate's subject | **Q5 is re-graded asserted** and the deferral is carried into §9.3 and the verdict: at this head it is a demonstrated bypass of the wrapper classification Q5 certifies |
| record P1 1 / verify P3 — "19 comparisons" | **Recounted**: the probe printed **19 records — 8 live-versus-replay comparisons, 7 replay-twice calls and 4 byte snapshots**; the asserted comparisons are **15** (§6). Every figure of that kind in the report and the body was recounted with it |
| record P2 4 — nine claimed `cargo exit=0` with no capture | **Re-executed**: the nine group invocations were re-run with the status captured (`groups-r1/`, nine `cargo exit=0` lines). The first head's logs, which carried none, are kept beside them |
| record P3s and verify P3 6 | the census assertion (`assert!(unreached.is_empty())`), the `Container.Create`/`Start` arming (`a_launch_that_fails_after_a_committed_effect_still_releases_everything` arms both phases of both sites), the `TMPDIR` character counts (**102** in the export, **105** in the isolated control), the guest ledgers' **two** differing lines, the `cargo clean` ownership sentence, the `/tmp` comparison's limit, the "no other red" sentence and the body's rollback count are all corrected below |

### Review round 2 of this report, and what this head does about each finding

All three lenses returned CHANGES_REQUIRED at `2f388103` (`~/orch-pr10/reviews/rg5r2-2/`, posted on #300).
The blocking finding was the same object in all three: the rebuilt coordinate audit.

| Finding | What this head does |
|---|---|
| verify P1 1 / record P1 1 — the rebuilt audit still credits witnesses for crash states they do not construct (row 56 repeats round 1's wrong witness; row 69; rows 16 and 18) | **The partition is withdrawn in full** (§5.3). Not re-cut, not narrowed — withdrawn. Each false credit is confirmed at source and named, and so is the method that produced them. In its place: an **executed** certification of the 32 parent-executed points, where the harness records a fault only when it fired — **all 32 fired in the two-host union**, **28 carried all three obligations at that head**, four a stated gap (`audit/point-coordinate-certification.md` as it stood then; the file has since been revised, and now says 29 and three). Review round 3 found that measurement counted an adapter unit test on Linux; corrected below |
| record P1 2 / regression P2 1 — the claimed missing-witness population is also false: committed, passing witnesses exist for rows 36, 53, 117, 120, 129, 143, `Answer.StageWrite/after`, `Answer.PublishRename/{before,after}` and `Lock.AcquireRun/after`; the regression lens **executed two of them at `2f388103`, exit 0, 2 passed**; and the audit's own `rows-classed.json` says **37**, not 41, O rows have an empty `armed_by` | **Every absence claim is withdrawn, and none is made at this head** (§5.3). The 37-versus-41 contradiction is confirmed here by recount and reported. Clause 2 is re-graded **asserted**, not failed: a claim of absence needs the evidence a claim of presence needs, and this gate does not have it |
| verify P2 2 — two open deferred findings listed by filename and never assessed | **Both assessed** against the tree at this range, with the clause each bears on and the mechanism (§9.2). One of them corrects this report: `Lock.ProbeCleanupExclusive` is not a read-only site |
| verify P3 3 — the resumed ledger block maps the wrong packet outcome, 24 of 28 cells | **Confirmed independently and regenerated** (§7): the generator selected the packet key from the table's label; it now looks the key up in an explicit per-table map (§7), and the corrected log is 28 of 28 in all six blocks. The generator is saved beside its output |
| record P2 3 / verify P3 4e — `kill_after_run_started_creates_integration_ref` is called a kill | **Fixed as a class, not an instance** (§8, §3.5): every test this report calls a kill was re-checked against its body. **Four** are mislabelled, not one — that test is synthetic recovery, `kill_after_run_finished_before_report` and `kill_after_report_before_each_cleanup_step` are error-return witnesses, and `a_kill_at_id_unread_leaves_a_gc_owned_object_nothing_adopts` is a plant |
| regression P3 3 — the repair deleted the four citations for the two-host bijection measurement | **Restored** (§5.2), with what the merge check actually enforces stated beside them |
| verify P3 4a — the required projection artifact still says "19 comparisons" | **The artifact is corrected** (§6), with the `grep -c` counts that re-derive 15 + 4 |
| verify P3 4b / record — the socket length | **Recounted** (§1, §9.4, and the finding file): `TMPDIR` **102**, socket path **141**; control `TMPDIR` **105**, socket **144**. The unsupported "29-character" figure is withdrawn from the finding: the control log records that run's status, not its path |
| verify P3 4c — "the other seven positive clauses" | **Six**, as §13's own table says |
| verify P3 4d / record — the red-attempt arithmetic and the isolated-control claim | **Recounted** (§9.4): **eight** failed tests across **five** red attempts, and the blanket "green 3 of 3 alone at the head that saw it" is withdrawn — `real_docker_kill_on_an_already_exited_container_is_tolerated` has no control at `16aea26` |
| record P3 — "54" new tests from the four fixes | **64**, by two independent derivations (§2) |
| verify P3 4f — the body's claim that each failing clause needs a tree change | **Withdrawn** (§13): clause 1 does; whether clause 2 does is not known, and §13 now says so rather than asserting the opposite |

### Review round 3 of this report, and what this head does about each finding

All three lenses returned CHANGES_REQUIRED at `1bdd6d7c` (`~/orch-pr10/reviews/rg5r2-3/`, posted on #300). Two
lenses graded the same replacement measurement; every finding is answered as a class, not as the instance named:

| Finding | What this head does |
|---|---|
| verify P2 1 / record P1 1 — the point measurement counted an adapter unit test as a constructed crash prefix: `every_family_of_the_harness_bundle_records_into_the_same_harness` (`seams.rs:296`) arms `Process.Spawn` / `ambient_job_joined` / `error_return`, calls the adapter directly and executes no production funnel, and the generator counted its record | **The generator is corrected and every figure it produced is re-derived** (§5.3; `audit/point-coordinates-fired.py`). It now applies the merge check's own exclusion — `is_funnel_execution` over `ADAPTER_UNIT_TEST_MODULES`, read from `coverage.rs` at the range — and prints the raw and the filtered counts side by side: raw 28 / 28 / 24; **funnel-only 27 Linux, 28 guest, 23 both, 32 in the union, 0 in neither**. The one record removed, on either host, is that test at that coordinate; the coordinate's three production witnesses are all `#[cfg(windows)]`, so its certification row now reads **guest only**. The required artifact's "24 Linux / 12 guest", which matched neither the raw nor the filtered counts, is replaced; round 2's generator and output are kept under `audit/superseded-round2/`. Provenance is now stated as a rule in the artifact and here: a `Point` record establishes that the harness *returned* the armed injection; the funnel filter, and the row's named test with the argument that selects the point, establish that a production operation consumed it |
| record P1 2 / verify P3 3 — partial certification stated as complete: "at the 32 parent-executed points" where the table certifies 28 and names four gaps | **The claims listed here were made to carry the certified count and the gaps at that head** (28 and four — rows 1, 2, 14 and 23): the header, §5.3, §9.1, Q5, §13's row 2 and item 2, and the body. §8's group-7 row was not on that list: it carried the count and not the gaps, and when round 4 changed the count to 29 and three that row kept 28. Round 5 found it and this head corrects it (the round-5 table below). The gaps are gaps in this certification, not findings about the tree |
| verify P2 2 — two filed recovery/finalization deferrals unassessed (`PR8-CRASH-002-PACKED-REFS-LOCK`, `PR8-CRASH-002-PACKED-RUN-REF`), and the class with them | **The whole class is assessed** (§9.2; `findings/assess-findings.py`, `findings/every-finding-assessed.tsv`): every finding file at the range is placed by a generator that refuses if a file lacks a decision or a decision a file, with the clause or question each bears on and its effect. The two named bear on clause 7, Q1 and Q6 as a resumable refusal that needs an operator, in two shapes this head said no committed test constructs (round 4 found two committed tests that construct the packed-run-ref shape, and the claim was narrowed at `17980022`); clause 7's row and Q1/Q6 carry it. The same pass found three deferred P1s the previous heads called "outside this range's subject" that touch the gate's conditions or a boundary its clauses do not certify (§9.3), and several findings that narrow a clause's conditions (clause 4's guest sampling, clause 3's loss model) |
| verify P3 3 / regression P3 2 / record P3 5 — §3.5 still says §5.3 "counts where" #292 supplied armed faults "and where it did not" | **Removed** (§3.5). The sentences in §5.3, §9.1, Q5 and §13 stating a coordinate count or a certification scope were rechecked against what §5.3 then claimed; §8's group-7 row was not among them (see the row above) |
| record P3 3 — the six parser exceptions are not "closures inside match arms" | **Corrected** (§5.3): re-derived from `audit/fns2.pickle`, all six are `fn` signatures inside string literals — `src/effects.rs:808` and `:815`, `src/effects/tests.rs:2102` and `:2103`, `src/effects/tests/source_oracles.rs:909`, `src/runner/mod.rs:1728` — which the line regex matches because it runs over raw lines, so those six spans are invalid; none is in a file the certification's searches read |
| record P3 4 — the ledger generator is described more strongly than implemented | **Corrected** (§7): the outcome each block is placed under is an explicit six-entry map in `regen-packet-mapping.py`, the resumed table mapped to `Complete` as the suite's own `assert_ledger` call fixes it; nothing is read from the table's rendering |
| regression P3 3 — the body lost round 1's provenance (`31c5fbd8` appeared nowhere; both validators pass) | **Restored** in the body's Review evidence: all three rounds' SHA-bound verdicts with their comment ids |
| regression P3 1 — 14 gate legs with eight green; the evidence holds 15 and nine | **Recounted** (§9.4): 15 legs and nine green through `1bdd6d7`'s own run, 16 and ten through `b95f03f`'s, with the cutoff stated |
| verify P3 3 — §13 says clause 2 "does not need a tree change" | **Withdrawn** (§13): round 2's executed witnesses refute *necessity*; they do not establish that every undecided coordinate has sufficient evidence, so whether a tree change is needed is not known, and the point gaps (four at that head, three now) are named beside the 136 phases |
| the previous session's own handover — §5.3 says "both `IdUnread` kill tests" | **Corrected** (§5.3): group 1's list holds four `IdUnread` filters — three real kills and one plant |

### Review round 4 of this report, and what this head does about each finding

Two lenses ran at `7cde80cb` (`~/orch-pr10/reviews/rg5r2-4/`, posted on #300); the record lens, which audits figures
and provenance, had no model capacity and did not run. Three of its findings were the report understating evidence it
has (verify P2 1, verify P2 2, and verify P3 3 with regression P3 1); regression P3 2 was a misattribution, and verify
P3 4 was a sentence claiming more certainty than the evidence gives. The column below says what `17980022` did; where
round 5 found that it did less than it said, the cell says so:

| Finding | What this head does |
|---|---|
| verify P2 1 — `Event.Append` / `synced` / kill kept among the gaps although the committed `synced_line_recovery_matches_after_append_row` (`events/log/tests.rs:2511`) carries (ii) and (iii) | **Credited** (§5.3; certification row 14): the log suite's kill helper runs `append_topology_hooked(EventSite::Append, …)` under `KillAt { point: Synced, shape: Complete }` (`events/log/tests.rs:1903-1911`), the parent asserts the child did not exit cleanly and, on Unix, died by `SIGABRT` (`kill_at`, `:1841-1885`), runs `establish_stable_prefix` — the tabled next open through the barrier — asserting the synced line is in the surviving prefix (`:2540-2552`), and calls `assert_replays_twice_to` (`:2553` → `:2420`), two replays compared and the recovered fold compared with them. `ok` in the range's gate leg (`gates/eight-logs-a2fe09c-range-1/03-test.log:1270`), export run 2 (`suite/export2.cargo.log:1280`) and the guest suite (`guest/guest-suite.log:1165`). **29 of 32 certified, three gaps (rows 1, 2 and 23)**; the sentences in the header, §5.3, §9.1, Q5 and §13 changed with it, and §8's group-7 row did not (round 5 found it). Round 5 also measured that this row's replay-twice check cannot see the synced line (§5.3). Then the class: the three remaining gaps were searched again, every test whose funnel-execution record shows the fault firing listed from the two exports and its body checked for the missing obligation's spelling, with the search and its limit stated in the artifact; none closes |
| verify P2 2 — `PR136-PASS3-P1-CENSUS-EXTENT-FORGED-BY-A-DECOY` and `PR136-P3-STRUCTURAL-ARGV-COMPARISON-NOT-AVAILABLE-HERE` classed as bearing on nothing | **Reclassified** (§9.2; `findings/every-finding-assessed.tsv`): both bear on clause 4 and Q5, since they concern whether the sampled Git command is the funnel's; `PR136-PASS3-P2-SLOT-PRIMITIVE-CENSUS-IS-SPELLING-BASED`, the same family, to Q4 and Q5. Then the subject rows classed `none` — those only — were re-read, and two more moved — `PR3-CONTAINER-START-ROW` to clause 2 and `SWEEP-TESTS-SUBSTITUTION-CASE-READS-METADATA-WITH-OK` to Q4. At that head **126** of the 347 files bore on a clause or question and **221** on none; the generator's refusal on an orphan decision or an undecided file is unchanged. Round 5 found `none` rows outside the subject class that bear by the same rules, and the round-5 repair re-reads all 347 (the round-5 table, §9.2) |
| verify P3 3 / regression P3 1 — "no committed test constructs either" packed-ref shape is false | **Narrowed** (§9.2, clause 7, Q1, Q6): two committed tests construct the packed-run-ref shape, assert the resumable refusal, remove the lock and verify the write or publication then completes — `a_stale_lock_on_a_packed_ref_is_left_and_the_write_refuses` (`workspace_manager/tests.rs:13215`; the regression lens executed it, exit 0) and `a_ref_lock_on_a_packed_integration_ref_is_left_and_refuses_resumably_until_removed` (`recover/tests.rs:11017`), both `ok` in all three saved suite logs. What is not established is convergence without an operator, which the design forbids in that shape. For the `packed-refs.lock` shape no committed test constructs a killed deletion; the cherry-pick sampler's parent removes a `packed-refs.lock` its killed children leave (`recover/tests.rs:10710`, `:10827`) |
| regression P3 2 — Q1 places every killed barrier mutation in `src/events/log.rs` | **Restored** (Q1, §9.2, the TSV's `G3-O1` row): `g5-effect-before-barrier` changes `src/engine/topology/recover.rs`, inserting `remove_marker` before `establish_stable_prefix` in `run_recovery_order` (`mutations/run-g5/g5-effect-before-barrier.diff`), and is killed by `resume_establishes_stable_prefix_barrier_before_any_fold_derived_effect` — `rc=101` (`mutations/chain-g5-chunk-02.log:9`), *"Event.OpenLog (12) before the census (10)"* (`mutations/run-g5/g5-effect-before-barrier.log:14`); the five refusal mutations are the ones in `src/events/log.rs` (`+++ b/src/events/log.rs` in each saved diff). That shows the file, not the function, and `17980022`'s attribution by function was wrong for two of the five, which change `EventLog::open_funnel`; round 5 found it (the round-5 table) |
| verify P3 4 — §13 item 2 still opens "not evidence in the tree but" | **Removed** (§13) |
| the record lens did not run | `17980022` said every figure it touched names the saved file it is re-derived from; round 5's record lens, the first figure pass since round 3, found figures this head kept that no file supports (the round-5 table). The figures the two round-4 lenses reproduced, at `7cde80cb`: 27 / 28 / 32 / 23, 17 gate legs with eleven green, 4,348 manifest files, and the assessment's 347 rows before `17980022`'s reclassification |

### Review round 5 of this report, and what this head does about each finding

All three lenses returned CHANGES_REQUIRED at `17980022` (`~/orch-pr10/reviews/rg5r2-5/`, posted on #300): one P1, six
P2s and many P3s, every one a defect in this report or its artifacts and none in the tree or the verdict. The lenses
could execute for the first time, and they showed that each earlier repair had corrected the instances it was shown
and left others of the same kind. So each row below is worked as a class, says what was searched, and gives the
count the search returned. The phrase searches are `repair-r5/searches.sh`, run over this report, the body this head
applies and the evidence directory's own text (the published report copies excluded); their output is
`repair-r5/searches.out`, and every hit that remains in this report is inside a sentence saying the value was
corrected — except `three hostile-input`, whose count is right now that the fourth such row bears on none (§9.2
names the three).

| Finding | What this head does |
|---|---|
| record P1 1 / verify P3 3 — `CLAIMS` holds 168 claims, not 172 (§5.2) | **Corrected** (§5.2, §13 row 2), with the tree's own assertion of 168 and its split, and the arithmetic that closes: 159 + 4 = 163 on Unix, 159 + 5 = 164 on Windows. The class is every count of a tree table or list this report states; each one this round touched is re-derived by `repair-r5/figures.py` (output `figures.out`), and the ones found wrong are the rows below |
| record P2 2 — row 14's replay-twice check cannot see the line its coordinate leaves | **Measured as a class**, not for row 14 alone (§5.3; the certification's new section): the saved drop-the-last-event mutation, run alone in a scratch copy against each of the 21 Linux witnesses of the rows whose (iii) is established (the 24 certified rows that run there, and row 23, whose gap is (ii)), is killed at the row's own replay-twice assertion in 14 of those tests, killed earlier in three, killed at a later comparison in one (row 16), and **survives in rows 8, 9 and 14**, whose recovered logs end in a line that changes no `state()`. **Choice: the limit is stated**, and the rows stay certified, for the reason §5.3 gives — the obligation as defined is met and has no power over that one line; a reader holding (iii) to power at the coordinate's own line should not count those three. Row 18, which the brief asked about, is killed at its (iii) assertion: it is not the same class |
| verify P2 1 — `PR5-RD-002` is a third operator-only convergence shape, omitted from clause 7 and Q6 | **Executed on the tree's own loops** in a scratch copy (§9.3): finalization's `scrub_slots` and the resume's `reclaim_intents` each refuse three of three passes when the torn registration is not first, and converge when it is. **Carried into** §9.2's clause-7 row, §13's clause-7 row, Q1, Q6 and the assessment TSV (`Q1; C7; Q6; C9`). The class — every finding whose shape needs an operator before finalization or recovery converges — was part of the re-read below, and **it found a fourth**: `PR5-RD-003`'s torn `gitdir` registration, which a committed test shows the plain removal funnel refusing for every slot, and which the resume's reclaims call. Both are carried: clause 7 and Q6 now name four shapes |
| verify P2 2 / record 9 — the 126 / 221 bearing split: round 4 re-read only the `subject` rows | **All 347 rows re-read** against the finding files: the 221 classed `none` by five readers applying the assessment's written rules (`findings/reread-r5/RULES.md`), and the 126 bearing rows by three more for an over- or under-stated bearing and a stale or false reason (`RULES-BEARING.md`); every proposed change was read against its finding file, and each that changes what this report says was checked at source, before it was taken. **100 rows decided afresh**: of the 221 classed `none`, 45 — **14 now bear** (five of the six the verify lens named and all seven the record lens named, `PR172-REAPER-ROUNDS-STUB-NEVER-RAN` among both, and three more the re-read found: `PR149-ARRANGED-OCCUPIED-REFUSAL-IN-SETTLE-KILL-TESTS`, `PR262-DOCKER-OBSERVE-READS-RUNNING-AFTER-PROCESSGONE` and `SWEEP-TESTS-REFUSALS-ASSERTED-BY-MESSAGE-SUBSTRING`) and 31 keep `none` with a corrected reason; of the 126 bearing, 55 — **12 now bear on none**, their filed mechanism not present at the range, 15 bear on a changed set, and 28 keep their bearing with a corrected reason. The sixth row the verify lens named, `PR10-RECORD-STALE-SENTENCES-AFTER-ROUND-11`, whose bearing the owner's 2026-09-14 decision left to this gate, is decided explicitly: none (§9.2). Regenerated: **128 bear, 219 none**, 256 decided explicitly and 91 by a location rule, every rule-placed row read as well. The generator still refuses a decision with no file and a file with no decision (`findings/reread-r5/refusal-check.log`). The re-read also corrected what the report said of those rows: clause 5's conditions (the gate legs ran under the shared `/tmp`), Q6's halted-answer limit (the loop never offers one), Q2's snapshot commit (unreferenced, not reachable) and `RefSite::CreateIntegration`'s order (backwards in value, not only in its documentation) |
| record P2 3 / regression 1 / verify P3 1 — §8 still said "certified at 28 of the 32"; the round-3 row, and the body, claimed every claim was made to carry the count | **Corrected** (§8 row 7), and the completeness claims themselves: the round-3 and round-4 rows of this table now say which sentences were changed and which were missed, and the body no longer claims more than it can show. Searched for `certified at 28`, `28 of the 32`, `28 and four`, `four carry a stated gap`, `every claim now says`, `every sentence`: counts in `repair-r5/searches.out` |
| record P2 4 / verify P3 7 — §9.2 and the TSV called certification row 23 certified | **Corrected** in §9.2 and in the TSV reason (`PR5-R2-IDUNREAD-BEFORE-THE-PARSE`). Searched for `(22 and 23)` and `rows 22 and 23` |
| record P2 5 / regression 3 / verify P3 2 — the clause-4 sampler caveat was cited to three places that did not carry it; the Q4 row's two findings had no stated effect | **The caveat is now in all three places** — §5.4, §13 row 4 and `residue/residue-class-evidence-record.md` — **and narrowed**: the sampled command's fixed arguments are the funnel's by construction, its run-time arguments are transcribed, and that it is the funnel's command is not established. The Q4 row's findings now have their effect in §9.2 and in §10's Q4 answer. Then the class: every §9.2 row was checked finding by finding against the generator's index, its own effect cell, and its §10 or §13 answer. The Q1, Q3, Q6, clause 2 and clause 4 rows gained effects for findings they listed without one, clause 9 and §8's groups 1 and 3 gained a row, the Q1, Q3, Q4 and Q6 answers now name every finding their rows carry — by id, or for the fourteen `PR128-*`, the four `PR7-STD-*` and the three hostile-input rows by the group §9.2 names — while the Q2 and Q5 answers carry theirs by kind, as before; and §13's rows 1, 2, 5, 7 and 8 now point to the findings that condition them |
| regression 4 / verify P3 10 / record 7 — two "barrier refusal" mutations attributed to `establish_stable_prefix` change `EventLog::open_funnel`; a quote cited to a log that does not hold it; "four refusal predicates" named none | **Every one of the 145 recipes attributed by the function its changed lines sit in** (`repair-r5/mutation-attribution/`), and every attribution the report makes checked against that table: the two `open_funnel` recipes are corrected in Q1, §6, the round-4 row and the TSV's `G3-O1` reason; the quote now cites `run-g5/g5-effect-before-barrier.log:14`. The barrier's refusals are named, each with its line, and the four no recipe had mutated — the first-line digest, mismatched and missing, and the checked replay's parse and fold — were mutated this round and each is killed (`repair-r5/barrier-refusals/`) |
| regression 5 / record 6 / verify P3 12 — load figures and a "CI guest at 370% CPU" that no saved file holds | **Every load figure in the report and the body re-derived** from the box-load logs (`repair-r5/figures.out`), given as the one-minute load at the start (and after) of each attempt, and no longer attributed to other sessions, which the logs cannot show; the CPU figure is removed from the body. Searched for `370%`, `11.09`, `average 9.3`, `6–11`, `6–10`, `9–11`, `load average 6-16`, `from 1 to 12` |
| regression 6 / record 8 — rows 14 and 18 rest on no export record, and the report said the export carries (i) | **Disclosed** in §5.3, §13 row 2 and the certification, after a class check over all 29 certified rows (`repair-r5/witness-provenance.tsv`): 14 and 18 are the only two whose named witness has no export record of its own |
| regression 2 / verify P3 4 / record 6 — Q5's "sixteen further filed residuals" | **Recounted** from the regenerated index (§10 Q5, §9.2's Q5 row) |
| verify P3 5 / record 7 — the census assertion described as two assertions; the required census artifact's bounds table omitted plan shapes; "would establish it" stated as fact; two closing join censuses unmentioned | **Corrected** (§4, §13 row 1 and item 1, `census-export2/bounded-census-artifact.md`, which now leads its bounds table with the plan-shape row, not reached) |
| verify P3 6 / record 7 — the inventory export described with columns it lacks and a count it is not; "every site carries exactly one order" | **Corrected** (§5.1): the table's real columns, its count as the `reached` set for all 32 points, and 58 / 3 / 7 sites with one, one-plus-`None` and no order |
| verify P3 9 / record 7 — "dies by abort" for two tests that do not assert it; tests credited to the wrong invocation | **Corrected** (§8 rows 1 and 3). Every "abort" claim in the report was checked against its test: #291's matrix does assert `died_by_abort`, and the log suite's `kill_at` asserts `SIGABRT` on Unix only, which §5.3 now says |
| verify P3 11 — clause 6's cited kills never reach the digest comparison | **Executed** one that does, in a scratch copy: killed at `recover/tests.rs:21579`, `cargo exit=101` (`repair-r5/projection-mutation/`; §13 row 6) |
| verify P3 12 / record 6 — figures against their files: 18 processes, #292's 77 / 13, "sixteen" census kills, "four minutes", the guest build lines' file, 1.01 GB, a `git status` claim with no record, unnamed "ten" and "two" recipes | **Each re-derived and corrected** (§1, §2, §3, §4, §7, §9.4, §10; `mutations/summary.md`); the figures are in `repair-r5/figures.out` and the #292 recount in `repair-r5/recount/` |
| verify P3 13 / record 8 — the gap re-search could not see a witness that exports nothing | **Widened** (§5.3, the certification): the site-blind drivers searched, and every firing test searched with helpers inlined three calls deep for all nine spellings; the three gaps stand |
| verify P3 14 — Q2 not scoped to G5's portion | **Scoped** (§10 Q2), and `PR272-R2-DESIGN-26-OVERPROMISES-UNAVAILABLE` named there |
| record 7 — bare `effects.rs`, `effects/tests.rs` and `registry.rs` citations naming the wrong file | **Every backticked source citation resolved against the range** (`repair-r5/cite-check.py`): the four ambiguous ones (three bare paths, cited six times) now carry their full path, each checked against the range's text; no citation names a line beyond its file |
| record 7 — "171 census tests", "228 filters, every one present"; the withdrawn files "labelled"; #290 "not a G5 subject"; the §9.4 flake table and "two fingerprints"; "every mutation run green"; the Docker controls' environment; "the fix pull requests do what their records say" | **Each corrected or narrowed where it stands** (§2, §5.1, §5.3, §0's merge table, §9.4, §1, §13) |
| record 8 — the certification's wrong rows and file in its caveat, row 25's bare ✅, the "eight spellings", the "two" weaker claim rows, row 16's empty-prefix comparison | **Corrected in the artifact** and in §5.3: rows 14 and 18 in the caveat, `src/events/log.rs:958`, row 25's lines, nine spellings, **eight** weaker claim rows, and row 16's two comparisons |
| record 6 — "executed … at this head" for round 2's executions; round 4 described as understatement only; "five decisions changed" with no earlier table kept; the body's "125 re-anchored"; "sampler-census" for a slot census; "three hostile-input rows" where the TSV had four | **Corrected** (§5.3, the round-4 row, §12, §9.2 and the body); §9.2 now names the three hostile-input rows, and the fourth, `PR128-REVIEW2-STORE-SCAN-HANGS-ON-A-FIFO`, bears on none at the range, its scan reverted before it |

### Method notes that bind the evidence

- **Mutation is the detection test**; a survival is reported with the loss point it actually covers, and
  re-aimed where the aim was wrong (§3).
- **Every temporary edit was restored and proven**: the probes' file sha256 equals
  `git show HEAD:src/engine/topology/recover/tests.rs`, `git status --porcelain` empty afterwards. This head's
  executions (repair round 5) ran in a `git archive` copy of the range on a target base of their own, never in the
  worktree; every mutated file was restored from its backup and compared with the range's blob, and the copy's `src`
  tree was compared with a fresh archive afterwards (`repair-r5/copy-provenance.log`: identical).
- **One cargo command at a time on a private target**, through `upstroke-build`; the gate runs used
  `~/bin/w1-eight-iso` bare. The lib test binary bakes this worktree and no other
  (`gates/binary-provenance-range-1.log`).

## 1. The cumulative suite, the ten gates, and CI

### The ten gates at the frozen range

`~/bin/w1-eight-iso /srv/worktrees/g5-run2`, first attempt, 2026-09-15T23:07:03Z: **`ALL 9 PASS at a2fe09c`**
— fmt 1s, clippy 21s, test 82s, msrv 18s, and the five scripts (`gates/w1-eight-iso-a2fe09c-range-1.log`,
the nine logs in `gates/eight-logs-a2fe09c-range-1/`). The tenth gate, run separately:
`test-pr-ready-audit: ok` (`gates/gate10-a2fe09c-range-1.log`).

### The cumulative suite on Linux

From the gate run's own test leg (`gates/eight-logs-a2fe09c-range-1/03-test.log`): `running 2763 tests` in
the lib binary — **2673 passed, 0 failed, 90 ignored** in 82.01s; the `main.rs` binary **10 passed**; the
`probe` example 0.

The suite ran twice more with every evidence export set and `TMPDIR` at a fresh empty directory of this
session's own, with a watcher listing that directory every half second (`suite/export-run.sh`):

| Run | Result | Log |
|---|---|---|
| export 1 (23:12Z) | **2672 passed, 1 failed, 90 ignored**, `cargo exit=101`. The one failure is `runner::container::tests::real_docker_prints_the_transcribed_unreachable_diagnostics`, and it is an artifact of my own measurement: `TMPDIR` was this session's scratchpad, **102 characters**, so the socket path the test built under it is **141 characters**, which exceeds `sun_path`'s 108-byte array (107 usable) and the live CLI printed *"unix socket path … is too long"*, which the classifier reads as an answered failure (§9.4) | `suite/export1.log`, `suite/export1.cargo.log` |
| export 2 (23:15Z) | **2673 passed, 0 failed, 90 ignored**, `cargo exit=0`; binary 10 passed; 503 observation files (502 per-process records and the residue histogram), 6 ledger tables, the census summary | `suite/export2.log`, `observations-export2/`, `ledger-export2/`, `census-export2/` |

Export run 2's artifacts are the ones §4–§7 quote. The census summary and all six ledger tables are
byte-identical between the two runs, and the census summary is byte-identical to the guest's.

**Residue, measured rather than pattern-matched.** After export run 2, everything left under the fresh
`TMPDIR` was listed with no filter: **995 entries, 30,681 paths**, every one a scratch entry of the suite's own tests —
986 named `upstroke-*` by the suite's many scratch helpers (30 of them `workspace_manager::fixture::scratch`'s
`upstroke-wm-*`) and nine named `<tag>-1583293-ThreadId(<n>)`, 1583293 being export run 2's test process
(`suite/export2.after.txt`, `export2.after-tree.txt`; the split is in `repair-r5/figures.out`). The mid-run listings
— 23 entries with 17 test-binary processes running at the first, 997 at the fullest — are saved beside them. The system `/tmp`
was listed before and after and **no new entry remained**; that comparison cannot see an entry created and
removed between the two listings, and is reported as the bounded check it is.

### The Windows guest

The guest fetched this range into the Gate 5 programme's own tree `C:\upstroke-g5`, rebuilt it
(`Compiling upstroke v0.1.0 (C:\upstroke-g5)` and `Finished` in 54.75s, `guest/guest-build2.log:4-5`; the run's
brief asked for `CARGO_INCREMENTAL=0`, and no saved log records that variable, although until repair round 5 this
sentence said the rebuild used it) and ran the full
suite with the three exports set and `TMP`/`TEMP` at a fresh directory **under the ordinary CI temporary
directory**: **2566 passed, 0 failed, 67 ignored** in 338.24s; the `main.rs` binary **12 passed, 0 failed, 1
ignored**; `suite rc=0` (`guest/guest-suite.log`). Its exports are in `guest/exports/`.

**The space this cost.** C: had 1.08 GB free when this run started (1,081,356,288 bytes,
`guest/guest-fetch-build.log:2`); my first build filled it and failed at
the linker (`LNK1318`, 139 KB free). I then ran `cargo clean -p upstroke` in the shared target
`C:\cargo-target` — **2,550 files, 8.2 GiB, `clean rc=0`** — and C: returned to 8.04 GB free
(`guest/guest-space.log`). That command removes the artifacts of **this crate** in that target and nothing
else; the guest sets `CARGO_TARGET_DIR=C:\cargo-target` for every tree, so those artifacts are shared between
sessions, and my own build had just rewritten the ones it touched. **No inventory of which file belonged to
which session was taken, and none is claimed.** The suite then cost 0.08 GB.

### CI at the frozen sha, all three platforms

`ci/runs-at-range.json`, each run's jobs in `ci/run-<id>.json`; newest run per check name:

| Run | Event | Result |
|---|---|---|
| 35029376216 (22:08:01Z) | `merge_group` | **every job success**: lint ×3, test (ubuntu-latest), test (macos-latest), test (winguest), msrv ×3, upstroke-ci |
| 35030295227 (22:18:31Z) | `push` — the newest per check name | **every job success**, the same ten checks |
| 35029376249 (22:08:01Z) | `merge_group` | upstroke-pr-policy success |

### The Docker-gated lanes, run with Docker required

The 23 `DOCKER_GATED_TESTS` ran with `UPSTROKE_REQUIRE_DOCKER=1` and a short `TMPDIR`
(`docker-required.log`): **21 passed, 2 failed** — `real_docker_kill_on_an_already_exited_container_is_tolerated`
and `real_docker_returns_both_streams_of_a_container_separately`, each at
`src/runner/container/tests.rs:3436` with *"`upstroke-f1-…` is still running after 200 observations"*, the
filed poll flake `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME`. **Each is 3 of 3 green alone**
(`docker/controls.log`). One limit on every Docker control in this report, these and those in §9.4: the control
logs do not record `UPSTROKE_REQUIRE_DOCKER=1` (only `docker-required.log` does), and without it a Docker-gated test
that finds no daemon skips and passes (`docker_gate`, `src/runner/container/fake.rs:469-482`). The daemon answered on
this box throughout — the Docker-required run above, and the container listings saved before each retry — and each
control of the two poll-flake tests finished in 0.21–0.31 s, the time of a run rather than of a skip, but no control
log proves by itself that its test ran.

## 2. The tests this gate runs by name

The nine group invocations take their filters from `groups/group-tests.txt`: run 1's lists carried forward —
every filter re-checked against this range's own `cargo test -- --list` (2,773 names,
`groups/test-list-a2fe09c.txt`) — plus tests the four fix pull requests added, each assigned to the group
whose claim it exercises. **228 filters: 227 exact test names, every one present in the binary's list, and one
prefix, `effects::`, which selects 171 tests** — 61 in `effects::tests` and 110 in `topology::effects::`
(`repair-r5/figures.out`; until repair round 5 this sentence counted the prefix as a name).

**The count of added tests is corrected.** The first two heads said the four fixes added **54** non-ignored
tests. Recounted at `71aede1b` two independent ways — a name-set difference between each merge and its first
parent (`git show <merge>:<path>` over every `*.rs`, counting a `fn` iff its attribute run holds `#[test]`
and not `#[ignore]`), and a count of added `#[test]` and `#[ignore]` lines in `git diff <merge>^1 <merge>`
— both give **64**: 3 from #289, 2 from #291, 1 from #293 and 58 from #292 (70 new `#[test]` functions, 12 of
them `#[ignore]`d, nine of those named `*_kill_child`; the "77 of which 13" this sentence gave until repair round 5
are the four pull requests' totals — `repair-r5/recount/newtests.json`, re-derived this round by name-set difference
against each merge's first parent). 54 was the size of an intersection, not a count: the working file
`groups/new-tests-by-fix-pr.txt` holds 82 names, of which exactly 54 appear in the range's test list. 53 of the 82
are among the 64; the other 29 are not — 26 are functions that are not tests at the range (helpers), two are tests
that predate their pull request (`finalization_kill_child` and
`a_kill_inside_finalization_after_the_execution_root_is_removed_converges_on_the_next_resume`) and one is a new
`#[ignore]`d child (`finalization_resume_child`) — and the file misses 11 of the 64 (re-derived against
`repair-r5/recount/newtests.json`; until repair round 5 this sentence called all 29 helpers or names renamed away). Three of the 64 are absent from the Linux list because they are platform- or cfg-gated, so **61** of
them are runnable there; the group filters are unaffected, since each of the 227 exact names resolves in the
binary's own list and the 228th is the prefix.

The nine invocations were **re-run with the process status captured**, after review round 1 found the first
head's logs carried none (`groups-r1/group<N>-*.log`, each ending in a captured `cargo exit=0`):
**69, 2, 45, 13, 29, 16, 37, 171 and 16 tests passed, 0 failed, nine `cargo exit=0`**. The first head's logs
are kept in `groups/` beside them.

## 3. Detection, established by mutation

Three recipe sets ran at this range. Each mutation is applied to a `cp` backup, the named test is run through
the wrapper, the file restored and `cmp`ed, and `rc` recorded. No chunk log contains `RESTORE FAILED`, which the
runner prints when a restored file differs from its backup, and all 145 backups equal the range's blob of the path
their diff names (`mutations/summary.md`, `mutations/summary-*.tsv`, `mutations/chain-*.log`, `repair-r5/figures.out`;
one `.log`, `.diff` and `.backup` per recipe under `mutations/run-*/`). The chunk logs do not record `git status`;
until repair round 5 this sentence said every chunk ended with it empty, which no saved file shows.

| Set | Recipes | Killed (`rc=101`) | Passed as expected | Survivals |
|---|---|---|---|---|
| PR10's 80 recipes, re-anchored at this range | 80 | 78 | 2 | **0** |
| run 1's 45 G5 recipes, re-run at this range | 45 | 34 | 2 | 9 |
| the fix-judging recipes this run wrote | **20** | **15** | 0 | **5** |

Eight of PR10's recipes and one of run 1's had to be re-anchored (#289 gave `reclaim_report_staging` a ledger
argument); after re-anchoring all 125 applied in `CHECK=1` mode before any ran. One recipe was retired because
it aimed at run 1's uncommitted witness. The nine survivals in run 1's set are the ones run 1 explained,
re-observed here, one of them **VOID** (its filter names an uncommitted witness, so it matched no test);
`mutations/summary.md` carries the table with each explanation.

### 3.5 Every fix pull request, judged rather than accepted

**#289 (`2570e423`) — the durable reclaim, the third-resume assertion, the private-record witnesses.**

| Recipe | Mutation | Test | Observed |
|---|---|---|---|
| `fix289-reclaim-no-barrier` | the reclaim takes **no** barrier between the deletion and the record's removal — the shape before #289 | `a_reclaimed_report_stage_remains_reclaimable_after_power_loss` | **killed**, `rc=101` |
| `fix289-reclaim-barrier-not-observing` | the barrier records no observed entry (`sync_dir` for `sync_dir_observing`) | the same | **killed**, `rc=101` |
| `fix289-publish-record-before-sync` | `publish_report` removes the record before its barrier | `a_refused_public_barrier_leaves_the_record_of_the_staging_directory_it_removed` | **killed**, `rc=101` |
| `fix289-record-removed-before-barrier` | the record removed immediately **before** the observing barrier | the power-loss witness | **passed**, `rc=0` — see below |
| `fix289-record-removed-before-barrier-vs-refused-barrier` | the same mutation against the witness that observes the **earlier** loss point | `a_refused_public_barrier_leaves_the_record_of_the_staging_directory_it_removed` | **killed**, `rc=101` |
| `fix289-record-publish-swallowed-vs-new-test` | the private record's publish error swallowed | `a_report_staging_record_failure_stops_before_directory_creation` | **killed**, `rc=101` |
| `fix289-record-stage-swallowed-vs-new-test` | the private record's stage error swallowed | the same | **killed**, `rc=101` |
| `fix289-record-publish-swallowed-vs-refused-barrier-test` | the publish swallow against the refused-barrier witness | `a_refused_public_barrier_leaves_the_record_of_the_staging_directory_it_removed` | **passed**, `rc=0` — mis-aimed: that test exercises the public barrier's refusal, not the private record's publish; the correctly aimed pair above kills |
| `r11-third-resume-leftover-record-only` (run 1's) | the fresh branch removes the record alone | `kill_after_report_before_each_cleanup_step` | **killed**, `rc=101` — at run 1's range this survived, which is what failed clause 7 |

**On the one survivor, corrected.** The first head of this report called `fix289-record-removed-before-barrier`'s
survival correct because "the deletion is durable and nothing is stranded". That sentence is **withdrawn**. The
power-loss witness simulates its loss *after* the moved barrier has run, so it cannot see the interval the
mutation opens: the record retired while the public deletion is still unsynced, which is the sequence
`PR10-RECLAIM-RECORD-DROPPED-BEFORE-DURABLE-DELETION` describes and `src/rundir.rs`'s own rustdoc warns of.
Class rule 4 asks for controls on **both** sides of the barrier, and the suite has one at the earlier point:
`a_refused_public_barrier_leaves_the_record_of_the_staging_directory_it_removed`, against which the same
mutation is **killed**. So the two sides are bounded by two different committed tests, neither alone.

`r11-staging-record-publish-swallowed` and `-stage-swallowed`, aimed at the test they were written for, still
pass (`rc=0`); the same two mutations against the test #289 **added** are killed. That is what discharges
`PR10-PRIVATE-RECORD-BARRIERS-UNWITNESSED` — an execution, not a reading of the new test's code.

**#291 (`00856bbe`) — the committed finalization kill matrix.** `kill_at_every_finalization_cell` plants a
finished run per cell, kills the child there through `run_kill_child_within`, asserts it died by abort and
that the armed cell was the last one it consulted, asserts `assert_finalization_order`, resumes **in a fresh
process**, and replays the durable log twice asserting the states equal. 26 Complete and 24 Halted cells,
asserted as exact counts. `fix291-complete-keeps-candidates-refs` **kills** it.
`fix291-{complete,halted,matrix}-keeps-staging-leftovers` **survive** (`rc=0` each): those fixtures plant no
staging-intent leftover, so the step removes nothing in them. The dedicated
`a_fault_at_a_staging_leftovers_own_removal_stops_finalization_and_the_next_resume_converges` **kills** the
same mutation (`rc=101`) — but it arms an **injected error** at that step, not a kill, so what the suite
establishes for that cleanup effect is the error-return mode.

**And that is not the only one.** Review round 2's class rule 5 check was widened at `71aede1b` to every test
this report calls a kill, and two of the ST-18 witnesses whose *names* carry group 5's claim are error-return
witnesses too: `kill_after_run_finished_before_report` (`recover/tests.rs:20647`) and
`kill_after_report_before_each_cleanup_step` (`recover/tests.rs:18723`) both build
`ArmedFinalization::new(...)`, which is `Self::answering(harness, at, Injection::Error)`
(`recover/tests.rs:18334`), and both drive in-process and `expect_err`. Neither launches a child; neither
kills. The **real**-kill evidence after `run_finished`, at the report write and between the cleanup effects is
#291's two matrices, which do spawn children and assert `died_by_abort`. §8's groups 1 and 5 are graded
accordingly.

**#293 (`f9572a73`) — replay-twice at the recovered coordinates.** Five mutations made `TopologyFold::replay`
non-deterministic (`src/topology/fold.rs:521`, `TopologyFold::replay` itself, in every saved diff), aimed at one test
per assertion helper: `run.replay_twice_equal`, `Journal::replay_twice_equal`,
`assert_replays_twice_to_the_next_open` in two suites, and `assert_log_replays_twice_equal`. **All five killed.** #293
also added a sixth helper, `assert_replays_twice_to` (`src/events/log/tests.rs:2420`, `git diff f9572a73^1 f9572a73`),
which no recipe here aimed at until repair round 5: the saved drop-the-last-event mutation, run in a scratch copy
against the Linux witness of every row whose (iii) is established, is killed at that helper in row 18's witness and survives it in row 14's,
where the recovered log's last line changes no state (§5.3; `repair-r5/iii-power/`).

**#292 (`a2fe09c2`) — the coordinate witnesses and the registry citations.** `fix292-root-always-recreated`
and `fix292-root-never-recreated` both **kill** their tests; its citations resolve to exactly one `#[test]`
each; the ST-07 merge check passes over this run's exports on both hosts (§5.2). Whether #292 gives every
coordinate an armed fault is not something this report counts: §5.3 withdrew both partitions and makes no absence
claim, so what #292 establishes per coordinate is what §5.2's claim table holds it to, and no more is said.

**The other six merges** are judged in §0's table: two are instruments under `.github/` and `scripts/`, one is
`src/agent/proc.rs`'s bounded kill and wait (green in every suite run here), one is vocabulary, two are
finding-ledger changes.

## 4. Required artifact 2 — the bounded census

**The file:** `census-export2/census-summary.json` (sha256 `63d1a783…`), byte-identical to export run 1's and
to the guest's. The artifact is `census-export2/bounded-census-artifact.md`.

| Member | Generator | Ceiling | States | Transitions | accepted / refused / truncated | truncated? | closed? |
|---|---|---|---|---|---|---|---|
| `prefix` | full | 20,000 | **20,000** | 5,194,414 | 97,608 / 5,067,348 / 29,458 | **yes, reported** | no |
| `seeded: sequences, repairs, lineages` | restricted, seed of 15 events | 5,000 | **25** | 1,405 | 24 / 1,381 / 0 | no | **yes** |

`the_prefix_reports_its_truncation_and_every_seeded_census_closes` asserts the prefix at its ceiling with
truncation reported, every seeded member closed with its frontier exhausted **under** its ceiling and no state
at the trace ceiling, each seeded member reaching the dimensions it names at their declared bounds
(`src/topology/census.rs:1969-1976`), and that the set of declared numeric dimensions the **prefix** does not reach at
its bound is empty (`assert!(unreached.is_empty())`, `:2000`, over `unreached` built from the prefix's own reach,
`:1983-1988`) — so the prefix alone reaches every numeric bound. That is one assertion, stronger than the two this
sentence described until repair round 5 (a family-level one and a separate prefix-level one): there is no separate
family-level assertion in that test, and reaching over the union is the census summary's
`every_bound_reached_over_the_union`, below.

**Every numeric bound is reached by equality over the union** (`union_reaches` equals `bounds` on all ten and
`every_bound_reached_over_the_union` is `true`): originals 3, repairs 2, lineages 2, generations per task 2,
attempts per generation 2, sequences 4, **defers 1**, questions 2, review passes 1, resumes 2. Totality is
asserted over every member (the `FoldError` arm never reached; the pre-`budget_exceeded` counterexample by
name), and `every_plan_transition_arm_is_executed_by_the_census` asserts the executed arm set equals
`production_arms()` — **24 arms** — over the family. Nineteen recipes kill a test of `topology::census::tests` at this
range, 13 of PR10's and 6 of run 1's (§3; `repair-r5/figures.out` — this sentence said sixteen until repair round 5).

**The plan shapes, and why clause 1 is not established.** The amended bounds line reads *"… plan shapes: chain,
fan-out, join (two independent tasks, then one depending on both)"*, and the packet's `family` field requires
that "every bound is reached by equality over that union" and "every coverage assertion holds over the union
of explored states". `family()` (`src/topology/census.rs:1828`) has two members, both of the **fan-out** shape.
The chain and the join are explored by `every_plan_shape_is_explored` (`:3826`) at a 3,000-state ceiling each,
**truncated**, with no reachable `FoldError` and all four outcomes reached in each; the join is also explored by two
restricted censuses with their own candidate classes, which close (`:2379-2386`, `:3677-3684`). All of these are
**outside the family whose union the clause certifies**, so no coverage assertion runs over them. The required
census artifact said "every bound, reached by equality over the union" of the ten numeric dimensions alone until
repair round 5; it now states the plan-shape row, not reached (`census-export2/bounded-census-artifact.md`).

The first head of this report passed the clause by reading the bounds as the ten numeric dimensions
`CensusBounds::dimensions()` enumerates. Review round 1 rejected that, and it is right to: the packet's
`bounds` leaf is the contract, `dimensions()` is an implementation choice, and the 2026-09-15 amendment
expressly left this question to this run rather than settling it. **Clause 1 is not established on this
range.** The change the failure points to is in the tree — a seeded census per plan shape, admitted to `family()`, so
that the union explores all three and every coverage assertion runs over it. That such a change would establish the
clause is a hypothesis this gate has not executed (class rule 3), and this gate makes no tree changes.

**What is claimed, and no more.** Closure of the space under the bounds is claimed of nothing; **unbounded
closure is not claimed**; the census makes **no effect-phase claim**.

## 5. Required artifact 3 — the inventory, the registry, the audit, the residue record

### 5.1 The inventory and its lists

`inventory/site-enums-export.md`: **68 sites**, **180 entries** — 171 `execution_observed` and 9
`recovery_proven` by label; 168 `executed`, 9 `recovery_proven` and 3 `not_executed` by evidence kind — each
with its order, label, fault row, evidence kind, evidence test and a count of the tests whose Linux export records
it. Those are the table's columns; the tabled actions and residues are in `inventory/sequential-registry.json`, which
the table is generated from, and not in the table. The count is the size of the export's `reached` set, the tests that
passed through the entry: for all 32 point entries it is that, not the tests whose fault fired (`Event.Append` /
`synced` / kill shows 246 where two fired; the fired sets are `audit/point-coordinates-fired.md`'s). Until repair round
5 this sentence said the table carries the tabled action and "the number of tests that executed it"
(`repair-r5/figures.out` re-derives the columns and the counts). **Observable orders, by site** (the same file): 58
of the 68 sites carry exactly one order on every entry; three carry one order on their executed entries and the fast
no-execution record's `None` beside it, which is the condition `PR3-REG-001-CONDITIONAL` names, **measured
inactive**; and seven carry **no** order (`order: null` on every entry) — the five Event sites and the two
husk-removal sites, whose adjacency is `Adjacent::None`, for which `observable_orders()` returns an empty list
(`src/topology/effects.rs:338-344`). Until repair round 5 this said every site carries exactly one order on its
executed entries. Where a site carries one, the order is **vacuous at three sites**, and the report says so rather
than letting the count stand alone: `Worktree.Verify`, `Answer.Ingest` and `Lock.ObserveCleanupHold` are read-only —
their `after_effect()` is `NoEffect` and their semantics carry `rows: []` — yet each is keyed at `EffectBeforeEvent`,
documented as "the effect is durable, the adjacent append is not", because the order is derived from adjacency with no
read-only carve-out (`src/topology/effects.rs:338-344`). `SWEEP-SITES-READ-ONLY-ORDER` is the finding; §9.2 assesses
it.

The 171 tests the filter `effects::` selects passed in one invocation with the status captured
(`groups-r1/group8-effects-censuses-q5.log`, `cargo exit=0`): 61 in `effects::tests` — the census tests, over the
clippy disallowed lists, the allow-placement scan, the wrapper classification, the funnel-module allowlist and the
frozen legacy allowlist — and 110 in `topology::effects::` (63 in its own `tests` module, the rest in `vocab`, `registry`, `harness`,
`residue_authority`, `parent_tests`, `export` and `sites`), which the substring filter also matches. Until repair round 5 this sentence called all 171 census tests. **What those
tests do not show is that the classification domain is complete.** `PR7-WRAPPERS-EMPTY-DOMAIN` (P1, deferred)
records that `effects::externally_reachable_fns` consults `production_region`, which truncates a module at its
first `#[cfg(test)]` item, and demonstrates a `pub(super) fn` below that cut reaching an effect with clippy and
the whole suite green. At this head `externally_reachable_fns` still calls `production_region`
(`src/effects.rs:693-694`). §9.3 and Q5 carry it. **The dependency review** is a change-time obligation and no
dependency entered the range; no claim that one was performed is made.

### 5.2 The bijection, executed on both hosts

| Host | Records | Funnel executions | Inventory | Entries | Witnessed claims | Failures |
|---|---|---|---|---|---|---|
| `unix` (Linux, export run 2) | 404 | 398 | 68 | 180 | 163 | **0** |
| `windows` (the guest, its own export) | 401 | 395 | 68 | 180 | **164** | **0** |

Linux from `st07/st07-merge-check-export2.log` and `st07/st07-summary-export2.json`, the guest from
`guest/guest-after.log` and `guest/exports/st07-summary.json` (the first head carried these four citations,
the repair dropped them, and review round 2 asked for them back). The registry pin passed on both hosts;
`g5-registry-entry-dropped` kills it.

**What the merge check enforces, exactly.** `st07_the_sequential_range_is_a_bijection_over_the_exported_observations`
(`coverage/tests.rs:555`) loads the export a full suite run wrote, asserts `check_bijection` returns **no
failures** over the whole inventory for that host, asserts the export's fast sequences are the ones the
no-execution record names, asserts the frozen N of every recovery-proven class, and — the part that matters
here — walks `CLAIMS`, the tree's own coordinate→test table (`coverage.rs:367-1304`), and asserts for
every claim the host requires that **the named test's own observation record shows it executing that
coordinate**. The table holds **168** claims, and the tree asserts that number itself, with its split: *"159 shared,
four Unix-only and five Windows-only points"*, beside *"163 Unix and 164 Windows coordinates, none excused"*
(`coverage/tests.rs:238-246`; the 168 rows of `st07/claims-table.tsv` agree, and `repair-r5/figures.out` recounts
all three). A host therefore requires 159 + 4 = **163** claims on Unix and 159 + 5 = **164** on Windows, and the
`witnessed_claims` figures above are exactly those: every claim each host requires is witnessed. **This sentence
said 172 claims from `1bdd6d7c` until repair round 5**; with 172, the 163 and 164 would have implied four claims no
host checks, and there are none. **This is observation, per coordinate,
machine-checked against an executed export, on both hosts.** It is not, by itself, ST-07's three obligations:
it does not say the coordinate's exact durable prefix was constructed or its tabled recovery driven. That is
§5.3, and the two must not be conflated.

### 5.3 The per-coordinate audit — the partition withdrawn, and what replaces it

**Both partitions this report has published are withdrawn.** The first head graded 146 of 168 coordinates `F`;
review round 1 refuted it. The second head replaced it with `F 107 / R 20 / O 41`; **review round 2 refuted
that too, in both directions**, and this head withdraws it rather than cutting it a third way. It is kept in
the evidence directory as `audit/per-coordinate-audit.md` and `audit/rows-classed.json`, withdrawn,
because a reader checking this history should be able to see what was claimed. The evidence README labels them
withdrawn; the two files themselves carry no notice, and are kept byte for byte as they were published.

**The false credits, verified here at source.** Review round 2 named four rows, and each is confirmed by
reading the test at this range:

- row 56 credited `kill_after_candidate_prepared_appends_candidate_created_once` to
  `Object.CandidateCommitTree` / `IdUnread` / kill. It kills at `"after-prepared"` and then asserts
  `journal.count("candidate_prepared") == 1` and a `Promoting` generation with the pin already standing
  (`candidate/tests.rs:1004-1021`). `IdUnread` is strictly earlier. **This is the same wrong witness review
  round 1 identified**, and the rebuild carried it over;
- row 69 credited the first-line-synced marker-repair witness to `RunDir.CreatePrivateDir` / before — it kills
  at `AppendFirst/Synced`, with the private records already written;
- rows 16 and 18 credited
  `a_resume_over_a_stale_queued_candidate_with_nothing_staged_takes_the_staging_path_and_publishes_the_proposal`
  to faults *after* the staging intent and *after* the staging worktree. Its own assertion reads *"the durable
  prefix stages nothing: no staging intent, no staging worktree"* (`recover/tests.rs:13697`). It is an ordinary
  execution of the staging path.

**The false absences, also verified here.** The audit reported coordinates as unwitnessed where committed,
passing witnesses exist. `Lock.AcquireRun` / after: `startup/tests.rs:2088` constructs the coordinate exactly —
`let coordinate = (EffectSiteId::Lock(LockSite::AcquireRun), HookPhase::After); let mut erring =
ExportedErrorAt::new(coordinate);` — asserts the acquisition returns the injected error, and drives a
public-only reclamation. `Answer.StageWrite` / after and `Answer.PublishRename` / before and after:
`an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step`
(`recover/tests.rs:12815`) calls `rundir::stage_answer` at `:12854`, resumes and asserts the unpublished
`.partial` is *not* ingested and stays byte-identical (`:12889-12893`), calls `rundir::publish_answer` at
`:12895`, resumes again to the ingestion, and asserts `assert_log_replays_twice_equal` at `:12905`. Review round 2's
regression lens executed two of these at `2f388103`, whose `src` is the range's: **exit 0, 2 passed, 0 failed**
(`~/orch-pr10/reviews/rg5r2-2/regression.md`). And the partition
contradicted its own data: recounted here, **37** of the 41 `O` rows have an empty `armed_by`, not 41.

**Why both attempts failed, so the next one need not.** A lexical detector over test bodies cannot decide a
coordinate that is selected by an *argument*. The tree names its fault coordinates four different ways — a
tuple bound to a local (`ExportedErrorAt::new(coordinate)`), a string tag resolved inside an `#[ignore]`d kill
child's `match` (`"commit-tree-id-unread" => (EffectSiteId::Object(ObjectSite::CandidateCommitTree),
HookPhase::Point { point: SubEffectPoint::IdUnread, mode: InjectionMode::Kill })`,
`recover/tests.rs:13894-13915`), a bespoke hook type whose identity *is* the coordinate, and a direct call to
the site's own production function. Only the first is visible where the detector looked. Two further defects
compounded it, both measured here: the detector's arming verbs matched anywhere in a body, so a test that
merely mentions a site near a kill was credited; and its function parser mis-matched braces, giving thirty-seven
functions — 33 of them `#[test]`s, 25 in `src/engine/tests.rs` — bodies that ran to the end of their file, so
those tests "contained" every site name and arming verb below them. (Recounted here, that second defect reached
the candidate list of exactly one of the 180 rows, so it is a contributor, not the cause.) The parser was rewritten at `71aede1b` with a real Rust lexer
over comments, raw and normal strings, char literals and lifetimes, and it is self-checked: **8,506 of 8,512
function spans end on a `}` at the signature's own indentation** (`audit/parse_fns2.py`, `audit/rustlex.py`;
re-derived here from `audit/fns2.pickle`). The heads from `71aede1b` to `1bdd6d7c` called the six exceptions
"closures inside match arms"; review round 3 found that false, and it is. All six are `fn` signatures **inside string literals** — test
fixtures that quote Rust source: `src/effects.rs:808` and `:815`, `src/effects/tests.rs:2102` and `:2103`,
`src/effects/tests/source_oracles.rs:909`, `src/runner/mod.rs:1728`. The signature regex runs over raw lines while
only the brace matcher uses the lexer's offsets, so a quoted signature is matched and paired with later code
braces, and those six spans are invalid. None of the four files is one the certification cites — its rows cite the `create`, `emit`, `recover`,
`settle`, `attempt`, `coverage` and `engine` test files, `events/log/tests.rs`, `seams.rs` and `fold/apply.rs` — so no row and no gap statement
in it rests on an invalid span.

**No absence claim is made at this head.** Not for one coordinate. A claim that a coordinate has no witness
needs exactly the evidence a claim that it has one needs — the whole committed suite searched, all four naming
channels followed, the plant channel included — and this gate has not done that search in a form a reader can
re-derive. What follows is positive only.

**The executed layer, machine-checked.** §5.2's merge check is the tree's own per-coordinate enforcement, and
it passed on both hosts: 0 bijection failures over the whole inventory, and every claim the host requires held
to its named test's own observation record (163 of the 168 on Linux, 164 on the guest).

**The certified layer: the 32 parent-executed points.** `HookHarness::hook`
(`src/topology/effects/harness.rs:259-288`) records a `Point` observation **only when that exact (site, point,
mode) was armed and the injection therefore fired**; `Before` and `After` are recorded on every call and are
reachability. Its own rustdoc says why: *"a mode is executed when its fault fired — not when a funnel walked
past the place it would have fired"*. So for the 32 point coordinates a saved `Point` record is an **executed
record that the harness returned the armed injection at that coordinate, in that test**. **That is not yet
obligation (i).** An adapter unit test can call the adapter directly, receive the armed `Injection::Error` and
assert on it, and its record is indistinguishable in the export from a production funnel's:
`every_family_of_the_harness_bundle_records_into_the_same_harness` (`src/engine/topology/seams.rs:296`) does
exactly that at `Process.Spawn` / `ambient_job_joined` / `error_return`, on both hosts, and executes no production
funnel. The tree's own merge check drops such records through `is_funnel_execution`
(`src/engine/topology/coverage.rs:174`) over the three `ADAPTER_UNIT_TEST_MODULES`; `71aede1b`'s generator
omitted that exclusion and review round 3 found it. The generator now applies the same exclusion, read from
`coverage.rs` at the range, and prints both counts (`audit/point-coordinates-fired.py`, which reads only saved
JSON and the frozen source; output `audit/point-coordinates-fired.md`): raw, every record counted, **28 Linux /
28 guest / 24 both**; **funnel-execution only, 27 Linux / 28 guest / 23 both / 32 in the two-host union / 0 in
neither**. The Linux-only four are the `#[cfg(unix)]` spawn parent's kill points and the guest-only five are the
`#[cfg(windows)]` parent's four kill points plus `ambient_job_joined` / `error_return`, whose three production
witnesses (`recover/tests.rs:15282`, `engine/tests.rs:9421`, `coverage/tests.rs:1174`) are all Windows-only — the
split read from the source and the split measured from the filtered exports agree. The one record the exclusion
removes, on either host, is the seams adapter test at that one coordinate. What the filtered record establishes
is that a test outside the adapter modules consumed the injection; **which production operation consumed it is
read from the row's (i) column, which names that test and the argument that selects the point.** The exclusion is
the merge check's module list, not a per-test proof, and the artifact says so. **At two certified rows the named
witness has no export record of its own**, and review round 5 found that this section said otherwise: rows 14 and 18
are certified on the log suite's kill helper, whose `KillAt` hook is not a `HookHarness` and exports nothing, so their
(i) rests on that suite's arming and on its parent's exit check — on Unix a `SIGABRT` death, on Windows only a
non-success status that is not the harness's panic status 101 (`events/log/tests.rs:1841-1885`) — and not on an
export record (the records at those coordinates are other tests'). Every other certified row's named witness has its
own funnel-execution record at its coordinate on the host it runs on (`repair-r5/witness-provenance.tsv`, from
`repair-r5/witness-provenance.py` over the two saved exports).

Obligations (ii) and (iii) are then read per coordinate, and every row of
`audit/point-coordinate-certification.md` cites `file:line`. Where the fault fires inside an `#[ignore]`d kill
child, the child carries (i) and the committed parent that launches it carries (ii) and (iii); both are named,
with **the argument that selects the point** — the string tag and the `match` arm it resolves to, or the point
list a `for` loop walks. The result:

| | Coordinates | |
|---|---|---|
| ST-07's three obligations **established** | **29** of 32 | a fired fault at the coordinate, the tabled action performed by a later process or open, replay-twice asserted — with the limit below at rows 8, 9 and 14 |
| (i) and (ii), **no replay-twice assertion reachable** | 2 | `Event.AppendFirst` / `written` / {kill, error_return} |
| (i) and (iii), (ii) **partial** | 1 | `Object.SnapshotCommitTree` / `id_unread` / kill — settled and reclaimed, but nothing asserts the tabled before-phase action is performed, where its candidate twin does (`recover/tests.rs:14363-14371`) |

**(iii)'s power, measured.** Obligation (iii), in the run brief's words, is "the durable log replayed twice and the
replays asserted equal". Review round 5 executed the saved mutation that makes `TopologyFold::replay` drop the log's
last event on every second call against row 14's witness: it **survived**, because the line row 14's kill leaves is a
`defer_wait_elapsed` over the log suite's empty plan and changes no `state()`. So the same mutation was run here
against the witness of every row whose (iii) is established and that runs on Linux — the 24 certified rows there
and row 23, whose gap is (ii), in 21 tests, since rows 12 and 13 share one and rows 29–32 another — each alone, in a
scratch copy (`repair-r5/iii-power/`; unmutated, all 21 pass). It is **killed at the row's own replay-twice assertion** in rows 3,
4, 5, 6, 7, 10, 11, 15, 17, 18, 20, 21, 23 and 29–32; **killed earlier**, before (iii) is reached, in rows 12, 13, 19
and 22; killed in row 16 at the comparison after the first line is appended (`emit/tests.rs:3420`), the cited
`:3394` being a comparison of two empty replays; and it **survives** in rows **8, 9 and 14**, whose recovered logs end
in a line the fold applies without changing state (a `pool_exhausted` for rows 8 and 9, `fold/apply.rs:64`). Rows
24–28 are guest-only and were not run. **The choice made, and why:** rows 8, 9 and 14 stay certified, with the limit
stated in the artifact and here, rather than being re-graded "(iii) insensitive". The obligation as the brief defines
it is an assertion over the recovered log, and each of these witnesses makes it, over exactly the log its
coordinate's recovery leaves; the mutant does not violate the asserted equality there, because at that prefix a
replay that drops the last line and one that keeps it reach the same state. What the assertion has there is no power
over a replay defect confined to that one line, and that is what the certification now says of those three rows. A
reader who holds (iii) to detection power at the coordinate's own line should not count rows 8, 9 and 14 — and
should note that the "killed earlier" rows and the guest-only rows were not measured at their own line either; clause
2's grade, *asserted*, and the verdict are the same on either reading.

Three corrections that came out of the reading, all kept in the artifact. **The tree has at least nine
replay-twice spellings, not the seven the heads before `71aede1b` used, nor the eight the heads from `71aede1b` to
`17980022` listed**: `assert_replays_twice_to`
(`events/log/tests.rs:2420`, ending `assert_eq!(once.state(), twice.state(), "two replays disagree")`) carries
`Event.OpenLog`/`sync_prefix`/kill on its own, and the census's `assert!(again.state() == replayed.state(), …)`
(`src/topology/census.rs:2563-2564`) is a ninth, which review round 5 found; adding it to the gap searches changes
nothing. **Eight `CLAIMS` rows name a weaker witness than the tree holds**, not the two this section said until
round 5: at rows 2, 3, 5 and 10 the claimed test is the error-return driver that fires each point and stops
(`coverage/tests.rs:907-936`); at row 18 it is a kill child whose parent asserts the abort and nothing after
(`coverage/tests.rs:982-1003`); at row 14 a kill child whose parent replays once (`settle/tests.rs:1650`); and at rows
13 and 15 an emit test that asserts the append error and stops (`emit/tests.rs:1479`, `:1906`). Each is read from the
named body, helpers not inlined. At the seven of these that are certified, a better committed witness is the one
certified; row 2 is a gap whichever is read. That is not a defect in the merge check, which asks only that the named
test execute the coordinate; it is the reason the claim table alone does not discharge ST-07.

**The three gaps above are gaps in this certification, not findings about the tree.** Each says what was
searched and where it stopped. Review round 4 showed the caution was needed: `Event.Append` / `synced` / kill,
graded "(i) only" at `7cde80cb` on its settlement witness, has a committed witness in the log suite —
`synced_line_recovery_matches_after_append_row` (`events/log/tests.rs:2511`), whose kill helper runs the `Append`
funnel under a `Synced` kill, whose parent runs `establish_stable_prefix` and asserts the synced line survives, and
which calls `assert_replays_twice_to` — and it is credited (certification row 14). So the three remaining gaps were
searched again as a class at `17980022`: every test whose funnel-execution record shows the fault firing was listed
from the two exports and its body checked, from `audit/fns2.pickle`, for the missing obligation's spelling. Review
round 5 pointed out what that search cannot see, and it is the shape that hid row 14: a witness that exports nothing.
So this head searched for that shape too — the log suite's site-blind `KillAt` and `FailAt` drivers, and any
`SnapshotCommitTree` `IdUnread` arming outside `attempt/tests.rs` (there is none): `KillAt` never runs
`AppendFirst`, and the one `FailAt` test that fires row 2 (`events/log/tests.rs:1467`) neither reopens nor replays.
Every test found was then searched with helpers inlined three calls deep, an over-approximation of 556 to 1,748
function bodies per test, for all nine spellings and for `observed(` (`repair-r5/gap-search.py`, output
`repair-r5/gap-search.out`). None closes. A trait-dispatched or macro-generated assertion is what the search would
still miss, which is why these stay gaps in this certification and not claims about the tree.

**The 136 before/after coordinates.** No partition, no grade, no count. What is established for them is §5.2's
executed layer and nothing more. What a future certification needs is a reading per coordinate that follows
each candidate witness's *argument* to the tabled residue — through all four channels the tree uses to name a
coordinate, the plant and the direct production call included — and prints the arming, the recovery and the
replay with a `file:line` each. The two that review round 2's regression lens executed at `2f388103` show such
witnesses exist; how many of the 136 they cover is exactly what this gate does not know.

**The Event points, `SyncPrefix`, the barrier and `IdUnread`.** Group 1 ran both production-adapter drivers (9
kill points, 12 error-return points), the shape tests, the barrier's refusal cases, the ordering test and its
refusal twin, and the four `IdUnread` filters — three real kills, each launching and killing a child
(`a_kill_at_id_unread_aborts_before_the_id_is_recorded`, `kill_at_commit_tree_id_unread_point_leaves_gc_owned_object`,
`kill_at_snapshot_commit_id_unread_point_leaves_gc_owned_object`), and the plant
`a_kill_at_id_unread_leaves_a_gc_owned_object_nothing_adopts` (§8) — 69 tests, all passed, `cargo exit=0`. Detection:
`g5-event-written-kill-point-skipped`, `g5-event-written-error-point-skipped`,
`g5-sync-prefix-point-not-consulted`, `g5-effect-before-barrier`, the three re-aimed `g5-barrier-*-ignored-log`
recipes, `g5-id-unread-point-skipped-kill` and `-record` — every one killed (`mutations/summary-g5-recipes.tsv`). Repair
round 5 added four for the barrier refusals no recipe had mutated — the committed first line's digest, mismatched and
missing, and the checked replay's parse and fold refusals — each killed by its log-suite test, `cargo exit=101`, in a
scratch copy (`repair-r5/barrier-refusals/summary.tsv`; Q1 lists which refusal each guards).

### 5.4 The residue-class evidence record

`residue/residue-class-evidence-record.md`: nine declared residue sites, **24 (site, element) pairs**, every
one constructed, classified `internal` and recovered in the synthetic half; three histograms from this run's
own sampler executions, **every row `unclassified: 0` and `recovered: true`**, declared and sampled sets equal.
All five samplers ran alone five times on Linux (25 of 25 green) and five times on the guest (25 of 25). **Three
caveats from the filed findings, carried into the record** (§9.2; the first two since `b95f03fe`, the third since
repair round 5): on the guest the census sampler's
kills end Git for Windows' launcher rather than git — `SampledChild::spawn` spawns `git` by name at this range
(`src/workspace_manager/tests.rs:11865`; `PR249-KILL-SAMPLER-WINDOWS-WRAPPER`) — so its four sites' guest runs
(`Object.CandidateStage`, `Object.CandidateWriteTree`, `Object.ProposalCherryPick`, `Worktree.Add`) are green runs
and not Git-internal residue evidence on that host, while the other four samplers spawn the real binary through
`KillableGitChild::spawn` and the Linux histograms are unaffected; and for the two commit-tree sites the synthetic
record's `recovered` is initialised true and never computed, since their tabled action deletes nothing
(`SWEEP-TESTS-COMMIT-TREE-RECOVERED-IS-TRUE-BY-CONSTRUCTION`), with the fsck assertion carrying the claim; and **that
the census sampler kills the command the production funnel runs is not established**: `sampled_command`
(`src/workspace_manager/tests.rs:11449`) takes each funnel's fixed arguments from the funnel's own `*_ARGV`
constants, so those are shared by construction, but adds the run-time arguments by hand (`:11465`, `:11470-11471`),
and the only census relating the two is a literal-counting tripwire that says of itself that it "does not establish
that the sampled child runs the funnel's argv" (`:11116-11117`) and can be walked around by a decoy (`:11103-11106`;
`PR136-PASS3-P1-CENSUS-EXTENT-FORGED-BY-A-DECOY`, `PR136-P3-STRUCTURAL-ARGV-COMPARISON-NOT-AVAILABLE-HERE`). So those
four sites' histograms are evidence for the command `sampled_command` builds, and not, argument for argument, for the
one the funnel builds. Detection: `residue-synthetic-pin` and `g5-residue-frozen-n-changed-pin`. **Every class is labeled
recovery-proven; no observed-execution claim is made for any Git-internal residue.**

## 6. Required artifact 4 — projection equivalence outputs

`probes/projection-equivalence-outputs.md`. Six committed tests ran `--nocapture` with a temporary probe at
the head of the two helpers every replay comparison goes through, and at the two-crash proof's own byte reads.
The probe printed **19 records: 8 live-versus-replay comparisons, 7 replay-twice calls and 4 byte snapshots**
— so **15 asserted comparisons**, not the 19 the first head claimed. Review round 1 recounted them; review
round 2 found the correction had reached the report and the body but **not the required artifact**, which
still said "Comparisons printed: 19" and called every record asserted. The artifact was corrected at `71aede1b`:
it now reads 19 records = 15 asserted comparisons + 4 byte snapshots, names which rows are the snapshots
(3, 4, 18, 19) and gives the four `grep -c` counts over `probes/probe-run.log` that re-derive the split
(19 / 8 / 7, the remaining four being `negative variant:` and `two-crash:`). Each comparison is one the
helper then asserts: `live_vs_replay`
parses the bytes on disk, replays them into a fresh fold, compares `RunState` with `==`, derives the schema-4
report from both and compares the digests; `assert_log_replays_twice_equal` replays the same bytes twice and
asserts the states equal. All six tests passed, `cargo exit=0`, and the probe was restored (sha256 equal to
`HEAD`, `git status` empty).

**The two-crash proof, with the bytes on disk before and after the simulated loss:**

| Point | Bytes | sha256 | `merge_prepared` | `task_merged` |
|---|---|---|---|---|
| positive: after the kill, before the loss | 4,266 | `58028da64b24fde6…` | 1 | 1 |
| positive: after `lose_unsynced_writes` | 4,057 | `9939eeb746c36875…` | **1** | **0** |
| negative: the unsynced `merge_prepared` before the barrier | 4,066 | `0ff393e0df66f045…` | 1 | 0 |
| negative: after the loss | 3,557 | `101a0a97d305db76…` | **0** | 0 |

Detection: `g5-open-sync-skipped` kills the positive
(`unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing`),
`g5-sync-prefix-point-not-consulted` the negative (`barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss`),
`rc=101` each (`mutations/summary-g5-recipes.tsv`); both mutations are in `EventLog::open_funnel`
(`src/events/log.rs:274-375`), whose sync step the barrier runs (`repair-r5/mutation-attribution/`).

## 7. Required artifact 5 — per-resource ledger outputs

`ledger-export2/`: six tables, each with **54 verdict rows**, **every one `yes`: 324 of 324** on Linux and 324
of 324 on the guest. Each guest table differs from its Linux twin in **two** lines — R21's own row and its
`R21 › cleanup_lock_file` subrow, both because the Unix cleanup lease file does not exist on Windows (the
first head said one line; review round 1 recounted).

**The packet mapping was wrong at `2f388103` and was regenerated at `71aede1b`.** Review round 2 found that
`ledger-export2/packet-at-run-end-mapping.log` selected each block's packet text by
`outcome.split(' ')[0]` on the *table's label*, so the sixth block — `ledger-no-run-finished-then-complete.md`,
a run that resumed and finished `Complete` — was handed the `NoRunFinished` cells: **24 of its 28 cells
carried the wrong outcome's text**, the other four only because the two outcomes' text is identical there.
Confirmed independently here (the sixth block's 28 cells equal `at_run_end["NoRunFinished"]` 28 times and
`at_run_end["Complete"]` 4 times), and the table's own outcome is fixed in the suite:
`assert_ledger(&before, &after, LedgerOutcome::Complete, "ledger-no-run-finished-then-complete")`
(`recover/tests.rs:22573-22578`). The generator reads only `spec/packet-resource-accounting.json` and the six
tables — no cargo, no suite run — so it was corrected and re-run, and it is saved beside its output as
`ledger-export2/regen-packet-mapping.py`. **What the correction is, exactly** (`71aede1b` described it more
strongly than it is, and review round 3 said so): the packet key is looked up in an explicit six-entry map from each table to the outcome its
equation was rendered under (`keys` at `regen-packet-mapping.py:13-15`), the resumed table mapped to `Complete` as
the `assert_ledger` call above fixes it. The map is handwritten and checked by a reader against that call; the
generator reads no outcome from the table's rendering and performs no outcome check of its own. **Every block now carries its
own outcome's cells: 28 of 28 in each of the six blocks, 168 of 168**, re-derivable by comparing each printed
cell against `at_run_end[<that block's outcome>]`. The log's `Holds` column is **the ledger table's own
verdict, copied beside the packet cell** — not a comparison of the packet text against the row — and the log
now says so; the mapping's claim is placement, and the reader makes the comparison. Detection: the six recipes whose
named test is a ledger test — `ledger-report-skipped`, `ledger-prunes-unreachable`,
`finalize-removes-answers-at-complete` and `g5-candidates-refs-kept-at-complete` against
`the_ledger_balances_at_complete`, `settle-scrub-removed` against `the_ledger_balances_at_parked`, and
`ledger-verification-defers-zero` against `ledger::tests` — all killed, `rc=101` (`mutations/summary-pr10-recipes.tsv`,
`summary-g5-recipes.tsv`); the finalization recipes are §3.5's. Until repair round 5 this sentence said "ten ledger and
finalization recipes" and named none, and no saved file gives ten.

## 8. The seven adversarial fault-test groups

Counted from the definition: **seven**. Every group ran as one `cargo test` invocation by name with the status
captured (`groups-r1/`): **69, 2, 45, 13, 29, 16, 37, 171 and 16 passed, 0 failed, nine `cargo exit=0`**.

| # | The definition's claim (abridged) | What ran | Grade |
|---|---|---|---|
| 1 | the **exhaustive** kill matrix over every Topology/Shared site × order × resume, incl. kills during recovery, run-end closure and terminal finalization, Event append points, OpenLog's SyncPrefix, and kills inside Git commands (sampled) | **In group 1's own invocation (69 tests):** `kill_during_recovery_repeats_recovery` and `kill_inside_closure_recovers`, each spawning a child the parent checks died before finishing — a non-zero status, no test result printed, its `unreachable!` not reached (`recover/tests.rs:4080-4096`, `:18121-18129`), which is not an abort check; the Event points in both modes and the barrier's refusals; #292's kills and faults at the worktree lease, lock file, spawn, terminate, git view, candidate sequence, question payload and answer ingestion; the five samplers. **In other invocations, and counted here:** #291's committed matrix — a **real kill** at each of 26 Complete and 24 Halted cells, each asserted to die by abort (`recover/tests.rs:20418-20422`) and resumed in a fresh process — in group 5's; the create suite's kills at all 13 publication prefixes (`KILL_PREFIXES`, 13 entries; `kill_at_each_prefix_p0_to_p8_converges`) in group 9's; and the five samplers alone, five times each, on both hosts (§5.4) (`groups/group-tests.txt`; until repair round 5 this cell listed all of these as group 1's and called the two recovery kills deaths by abort) | **asserted.** The real kills named here, the Event points, the barrier and the sampled Git-internal residues are **established**. Three corrections of class rule 5, made at `71aede1b`: `kill_after_run_started_creates_integration_ref` is **not a kill** — it plants the P6/P7 prefix with `Fixture::healthy` and resumes it (`recover/tests.rs:3697-3711`, and its own assertion says *"the fixture is the P6/P7 prefix"*), so it is synthetic recovery and is struck from this list; `kill_after_run_finished_before_report` and `kill_after_report_before_each_cleanup_step` are **error-return** witnesses (§3.5); and `a_kill_at_id_unread_leaves_a_gc_owned_object_nothing_adopts` constructs the `IdUnread` residue through `NoHooks` without aborting (`workspace_manager/tests.rs:9439`), which makes it a **plant** witness, not a kill. That the matrix is *exhaustive over every site × order × resume* is not established: `finalization_sites()` derives from a hand-listed table of finalization effects rather than the generated inventory, and §5.3 withdraws the per-coordinate partition that the first two heads cited here |
| 2 | the two-crash proof, positive and negative | both tests, with the bytes on disk before and after each loss (§6) | **established** |
| 3 | recovery of damaged or leftover state | 45 tests in one invocation: the lock refusals, the config-drift warning, the husk family at every prefix, the container census family, the stale-marker repairs and #293's container-state witness; #292's pruned-root-and-pins witness ran in group 5's invocation (`groups/group-tests.txt:164`; this cell listed it here until repair round 5) | **established** for the named checks |
| 4 | ST-17 run-end closure per class and outcome, every derived-outcome refusal, and the pre-`budget_exceeded` counterexample | 13 tests in one invocation, plus the census's outcome equality in both directions | **established** |
| 5 | ST-18 terminal finalization: kills after `run_finished`, after the report write, and **between every cleanup effect** | 29 tests in one invocation: #291's two kill matrices, #289's three rundir witnesses, the late-answer and answer-file tests, the report-barrier family | **asserted.** Finalize-then-refuse, idempotence across the third resume, the Halted listing, the untouched answer files and the inert late answer are **established**. "Between every cleanup effect" is not: the two tests whose *names* state the claim — `kill_after_run_finished_before_report` and `kill_after_report_before_each_cleanup_step` — arm `Injection::Error` through `ArmedFinalization::new` and drive in-process, so they are **error-return** witnesses, not kills (§3.5). The real-kill coverage at those coordinates is #291's two matrices, whose cells come from a hand-listed table of finalization effects rather than the generated inventory |
| 6 | outcome-specific resume behaviour | 16 tests in one invocation: the raised-budget resume, the woken deferred items, the answer-ingestion witnesses | **established** |
| 7 | the packet's seam tests in range | 37 + 16 tests in two invocations, plus §4 (ST-14), §5 (ST-07), §7 (ST-09) | **ST-07 asserted** — the bijection and the claim table executed with 0 failures on both hosts, all three obligations certified at 29 of the 32 parent-executed points, with three stated gaps (rows 1, 2 and 23 of the certification) and, at rows 8, 9 and 14, a replay-twice check that has no power over the coordinate's own line, and the 136 before/after coordinates undecided by this gate rather than graded (§5.3; this cell said 28 of 32 and named no gaps until repair round 5); **ST-14 not established** on the plan-shape clause (§4); ST-09, ST-10, ST-11, ST-12, ST-15, ST-19 and ST-20 **established** for the named cases, with ST-13's provisional-reservation lifecycle beyond the fast no-object proof named as G3's evidence |

## 9. Invariants, findings, deferrals and flakes

### 9.1 The five integrated invariants

The first is §7; the second is §5, **with its "executed evidence" clause established as observation over the
whole inventory on both hosts, its fault fired in a funnel-execution record at all 32 parent-executed points in
the two-host union (27 Linux, 28 guest), certified on ST-07's three obligations at 29 of those 32 with three stated
gaps (rows 1, 2 and 23 of the certification) and, at rows 8, 9 and 14, a replay-twice check without power over the
coordinate's own line, and undecided at the 136 before/after coordinates**; the third is §6; the fourth is §7 and §8 rows 4–6; the fifth
is §4, **not established on the plan-shape clause**.

### 9.2 The findings in range

348 files under `findings/` (`README.md`, `PROCESS.md` and 346 findings): **9 P1, 139 P2, 198 P3**, every one
`deferred` or `accepted-risk`; no file carries `open`. Run 1's three blocking findings are closed by #289, and
this run's mutations show the committed tests that replaced them detect the shapes they name (§3.5).

**Every row was read at this head, and every finding is placed.** Review round 2 found two omitted findings; the
repair added those two and stopped, and review round 3 found two more (`PR8-CRASH-002-PACKED-REFS-LOCK`,
`PR8-CRASH-002-PACKED-RUN-REF`). So `b95f03fe` placed every finding with a generator:
`findings/assess-findings.py` in the evidence directory reads every finding file's front matter at the report's
head and joins it to a decision table — the finding's class, the pass-rule clauses (C1–C9) and standing questions
(Q1–Q6) it bears on or `none`, and the reason — and **refuses if any file lacks a decision or any decision lacks a
file** (still so: `findings/reread-r5/refusal-check.log`, exit 1 each way). Review round 4 found rows classed `none`
that bear; that head re-read the subject rows only, and review round 5 found more in the other classes. **So this
head re-read all 347 rows against their finding files**: the 221 classed `none` against the assessment's written
rules, the 126 bearing rows for an over- or under-stated bearing and a stale or false reason, and every proposed
change was read against its finding file — and, where it changes what this report says, checked at source — before
it was taken (`findings/reread-r5/`, whose README says how). One policy was applied throughout: a row whose filed
mechanism is not present at the range bears on no clause or question, and its reason names the lines that show it.
The outputs are `findings/every-finding-assessed.tsv` (one row per file, 347 rows) and
`findings/every-finding-assessed.md` (the counts, every bearing row with its reason, and the ids per clause and
question). Of the 347 files at this head — the 346 at the range plus the one §9.4 files — the classes are: the
topology subject **194**; records and notes **55**; instruments (gate scripts, workflows, off-tree wrappers) **31**;
CI-platform and box flake fingerprints **20**; modules outside the topology (config, plan, validate, status, review,
gates, capacity, connect, export) **19**; process-local and reaper internals certified at G6 **19**; the legacy
schema-1..3 engine path **6**; real Docker, G7's **2**; and this gate's own **1**. **128 bear on a clause or a
question** — 103 of the subject's, 19 of the flake fingerprints, and six others — and **219 bear on none**, each with
its reason in the TSV. This head decided 45 of the rows classed `none` at round 4 afresh (14 of them now bear, among
them five of the six the round-5 verify lens named — the sixth, `PR10-RECORD-STALE-SENTENCES-AFTER-ROUND-11`, is
decided none below — and all seven the record lens named) and 55 of the rows bearing at round 4 (12 of them now bear
on none, their filed mechanism not present at the range); the other 247 rows kept their decision.
256 are decided explicitly and 91 by a location rule the row names, and every rule-placed row was read too.

What the bearing findings do to the grades, beyond what the heads before `b95f03fe` carried:

| Bears on | Findings | Effect at this range |
|---|---|---|
| **clause 7, Q1, Q6** — finalization and recovery need an operator in four filed shapes | `PR5-RD-002` (P1) and its sibling `PR5-RD-003` (P2), with `RESIDUE-UNBINDABLE-TASK-REGISTRATION-HAS-NO-DESIGN-SENTENCE`; `PR8-CRASH-002-PACKED-REFS-LOCK` and `PR8-CRASH-002-PACKED-RUN-REF` (P2, crash-consistency; the separately filed residuals of the closed `PR8-CRASH-002`, which closing did not close) | **Torn worktree registrations.** A kill inside `git worktree add`'s registration window can leave a zero-length `commondir` (`PR5-RD-002`): every call that revalidates through `git worktree list` then fails, and repair round 5 drove the tree's own loops over it — finalization's `scrub_slots` and the resume's `reclaim_intents` refuse three of three passes when the torn registration is not the first slot reached (§9.3). The same kill can leave `gitdir` absent, empty or partial (`PR5-RD-003`): the classifier reads that add as nothing (`src/workspace_manager/tests.rs:5496`), and the plain removal funnel — the one the resume's snapshot and staging reclaims call (`src/engine/topology/recover.rs:1151`, `:1164`, `:1216`) — refuses for every slot while an empty or absent entry stands (`src/workspace_manager.rs:4794`, `:4810`), which the committed `an_add_killed_before_it_wrote_gitdir_is_unlisted_and_refuses_forced_cleanup` asserts (`src/workspace_manager/tests.rs:5440`, `ok` on both hosts); finalization holds the no-writer proof and passes those two shapes over (`src/engine/topology/finalize.rs:251-255`), and a partial `gitdir` refuses its removal under either proof (`src/workspace_manager.rs:4817`; `malformed_gitdir_refuses_before_removal`, `src/workspace_manager/tests.rs:5242`). **Stale ref locks.** A kill inside a ref deletion leaves `packed-refs.lock`, and finalization's `delete_ref_expected_old` (`src/engine/topology/finalize.rs:270`) then refuses on every later resume until an operator removes the file, which the reclaim leaves alone by design (`src/workspace_manager.rs:3246-3248`: it is the repository's, every Git process can hold it, and a wrong removal would let a concurrent `pack-refs` publish an empty packed file over every packed ref; the finding adds that a live holder's and a dead holder's lock are byte-identical). A run ref packed by a `gc` during the run and then left with a stale lock is refused reclamation by design, since a live prune cannot be excluded; two committed tests construct that shape — pack the ref, plant its empty lock, run the CAS or the resume — assert the refusal, then remove the lock and verify the write or the publication completes: `a_stale_lock_on_a_packed_ref_is_left_and_the_write_refuses` (`src/workspace_manager/tests.rs:13215`) and `a_ref_lock_on_a_packed_integration_ref_is_left_and_refuses_resumably_until_removed` (`src/engine/topology/recover/tests.rs:11017`), both `ok` in the range's gate leg, export run 2 and the guest suite (`gates/eight-logs-a2fe09c-range-1/03-test.log:2664` and `:963`, `suite/export2.cargo.log:2669` and `:963`, `guest/guest-suite.log:2551` and `:874`). Every one of these refusals is resumable and loses nothing, so the outcome equations are not falsified and the six ledgers stand as measured — on repositories with none of these shapes. **What is not established is convergence without an operator**: for the torn registrations the tree's loops refuse until one acts; for the packed run ref the design forbids it and both witnesses converge only after the lock is removed by hand; for `packed-refs.lock` no committed test constructs a killed deletion — the cherry-pick sampler's parent removes a `packed-refs.lock` if one is present after each killed sample and discards whether it removed one (`src/engine/topology/recover/tests.rs:10710`, `:10827`), so no saved output shows the file arising, and that it arises under a kill is the finding's own observation (a killed `git cherry-pick` on the macOS runner). Clause 7's row, Q1 and Q6 say exactly that. (This row named the two ref-lock shapes only until repair round 5: the round-5 verify lens found `PR5-RD-002`'s place here, and this head's re-read found `PR5-RD-003`'s.) |
| **clause 4** — the residue sampling and the synthetic record | `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` (P2), `PR136-PASS3-P1-CENSUS-EXTENT-FORGED-BY-A-DECOY` (P2), `PR136-P3-STRUCTURAL-ARGV-COMPARISON-NOT-AVAILABLE-HERE`, `PR136-PASS3-P3-KILL-PAIRING-IS-A-TOCTOU-ORACLE`, `SWEEP-TESTS-COMMIT-TREE-RECOVERED-IS-TRUE-BY-CONSTRUCTION`, `SWEEP-EFFECTS-REGISTRY-DUPLICATE-SYNTHETIC-RECORD`, `SWEEP-TESTS-RECOVER-SAMPLE-COMPARES-TWO-DISCARDED-RESULTS`, `PR5-VERIFY-CLAUSE-NARROWER-THAN-STATED`, `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED` (P2), the torn-`gitdir` rows of the first row (`PR5-RD-003`, `RESIDUE-UNBINDABLE-…`), and two flake rows that also bear here (`PR247-SAMPLER-REFUSED-A-LOCKED-INDEX-ON-WINDOWS`, `PR80-MACOS-WORKSPACE-SAMPLER-COLD-PROBE-RECURRENCE`); with the third row's fourteen, the generator's "C4 (27)" | On the guest the census sampler kills Git for Windows' launcher, not git (`SampledChild::spawn` spawns `git` by name at this range, `src/workspace_manager/tests.rs:11865`), so its four sites' guest runs are green runs and not Git-internal residue evidence there; the other four samplers spawn the real binary. For the two commit-tree sites the synthetic `recovered` is by construction. **That the census sampler kills the command the production funnel runs is not established**: `sampled_command` shares the funnel's fixed `*_ARGV` constants (`src/workspace_manager/tests.rs:11449`) but transcribes the run-time arguments (`:11465`, `:11470-11471`), the literal-counting tripwire says of itself that it "does not establish that the sampled child runs the funnel's argv" (`:11116-11117`) and a decoy walks around it (`:11103-11106`), and the cherry-pick and repair-materialization samplers spell their commands as literals; so the sampling record is evidence for the commands the samplers build. The census sampler's per-shape kill oracle was withdrawn, so a shape none of whose kills landed still passes. A torn add — `gitdir` absent or empty — is read as no residue at all (`src/workspace_manager/tests.rs:5496`, pinned as a known hole), a shape no sampler here observed. The recorded report-staging leftover has no class (§13 row 4's named exception). §5.4, `residue/residue-class-evidence-record.md` and clause 4's row carry these conditions (until repair round 5 this cell said so while the three places did not carry the sampled-command caveat, and called that command "established by construction"); the Linux histograms are unaffected. The other five move nothing measured here: `recover_sample` can misdiagnose a sample but can only redden the census sampler, never green it (`SWEEP-TESTS-RECOVER-SAMPLE-COMPARES-TWO-DISCARDED-RESULTS`); the record's format does not enforce one synthetic record per element, and the 24 pairs were counted from the record (`SWEEP-EFFECTS-REGISTRY-DUPLICATE-SYNTHETIC-RECORD`); the proof test's Verify sentence is narrower than it states, and §5.4 does not rest on it (`PR5-VERIFY-CLAUSE-NARROWER-THAN-STATED`); and two sampler flakes on hosts where this run's samplers were not red — the guest's 25 of 25, and macOS, which this run did not sample (`PR247-SAMPLER-REFUSED-A-LOCKED-INDEX-ON-WINDOWS`, `PR80-MACOS-WORKSPACE-SAMPLER-COLD-PROBE-RECURRENCE`). (Until repair round 5 this cell gave no effect for these five) |
| **clause 4, Q1, Q2** — the classifier's inspection primitives | the fourteen `PR128-*` rows that bear on clause 4 (five more, bearing here until this head, now bear on none or on Q1 alone, their filed mechanism absent or narrower at the range) | Each reads a Git failure or a Git refusal as something it is not — absence, emptiness, a difference, a populated worktree, or an error where it should classify: an unreadable pack, a dangling `gitdir` link, a `gitdir:` pointer Git will not open, a failed `rev-parse`, `cat-file` or `diff`, a common-directory failure, an unreadable store, a non-UTF-8 path. Every synthetic construction and sampler here ran on a healthy repository with UTF-8 paths, so clause 4's "established for the declared classes" holds **under those conditions**; `unreachable_objects` never reads `fsck`'s exit status (`src/workspace_manager.rs:4984-5001`), and the R27 inventory the ledgers cite is read through it |
| **clause 3, clause 2** — the loss model | `PR5-C-FSYNC-UNOBSERVABLE` (P2) | Deleting `sync_all` is undetectable on this machine. The two-crash proof's loss is the durability ledger's simulated `lose_unsynced_writes`, which is the loss the packet specifies, and clause 3's row names the model |
| **Q1** — recovery evidence | `SWEEP-CLASSIFY-009` (P2), `SWEEP-CLASSIFY-010`, `SWEEP-NAMES-005`, `SWEEP-WORKTREE-013` (P2), the four `PR7-STD-*` ownership and private-root rows (P2), `PR7-STEP-D-LINEAGE-ARM-UNWITNESSED`, `G3-O1-BARRIER-INERT-AT-CALL-SITE`, `PR4-R28-NEXT-COORDINATOR-UNWITNESSED` (P2), `REFUSED-KILL-UNBOUNDS-THE-BOUNDED-DOCKER-REAP`, `PR128-PARENT-GIT-TRACE-MAKES-ABSENCE-AN-ERROR`, `PR7-P3A-CREATOR-RETAINS`, `SWEEP-FOLD_PARSE-002`, three hostile-input liveness rows (`PR128-PARENT-UNBOUNDED-READS-OF-REPOSITORY-CONTROLLED-FILES`, `SWEEP-CLASSIFY-003`, `SWEEP-CLASSIFY-012`), and the findings of this table's first and third rows, all of which name Q1 (the generator's index: "Q1 (37)") | Q1 carries each as a named exclusion: `read_dir_names` (`src/rundir.rs:1751` at the range) returns `[]` on a listing failure, the unbound husk's reclaiming answer, so a committed run's public half can be deleted on a transient failure no witness here provokes; the husk classifier folds five I/O failures into absence and reports such a run directory as a husk (`src/rundir/classify.rs:298`); a non-UTF-8 entry leaves a husk unreclaimed; the ownership proof falls back to lexical spelling when canonicalization fails, and the private-root locator is checked lexically only (`src/engine/topology/recover.rs:207-236`); step (d)'s `LineageHeld` arm is driven by a committed test (`src/engine/topology/recover/tests.rs:3104`) whose power over the finding's mutation this gate did not measure; R28's next-coordinator refusal has a committed witness (`src/rundir/tests.rs:4512`), and a refused kill leaves the reaper's hold unbounded on its docker-reclaim arm, which only tests arm at this range; under an inherited `GIT_TRACE` one lookup turns a silent absence into an error, failing closed; the barrier call at `recover.rs:482` is handed the same measurement three times and cannot refuse — the refusal mutations this run killed change `establish_stable_prefix` (the three `g5-barrier-*-ignored-log`) and the open it calls first, `EventLog::open_funnel` (`g5-open-sync-skipped`, `g5-sync-prefix-point-not-consulted`), both in `src/events/log.rs`, and the ordering mutation `g5-effect-before-barrier`, killed in `src/engine/topology/recover.rs`, shows production calls that barrier before any fold-derived effect; a user-written lock reason `initializing` makes a populated worktree read unpopulated and be force-removed and re-added (`SWEEP-WORKTREE-013`); a creator that errs exactly at P3a leaves both halves, retained and reported, for an operator (`PR7-P3A-CREATOR-RETAINS`); a log rewritten to CRLF parses to the same events and is folded rather than refused, which the barrier's first-line digest catches wherever a commit record exists and the engine never does to its own log (`SWEEP-FOLD_PARSE-002`); and a FIFO, an unbounded file or an unexplained I/O failure in a hostile repository or run directory can stall or misclassify a recovery no witness here constructs (the three hostile-input rows). (Until repair round 5 this cell gave no effect for those four, said "three hostile-input liveness rows" where the TSV then had four, placed all five refusal mutations in `establish_stable_prefix`, and called step (d)'s arm unwitnessed; the fourth hostile-input row, `PR128-REVIEW2-STORE-SCAN-HANGS-ON-A-FIFO`, now bears on none, its scan reverted before the range) |
| **Q2** — accounting | `ATTEMPT-PAID-REVIEW-CHARGED-NOWHERE-WHEN-THE-JUDGEMENT-FAILS` (P2), `PR272-R2-DESIGN-26-OVERPROMISES-UNAVAILABLE`, `PR7-R3-ATTEMPT-003-RESIDUE-DISCARD-UNREACHED` (P2), `SWEEP-RESIDUE-AUTHORITY-002`, `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED` (P2), `PR7-P3A-CREATOR-RETAINS`, and the findings of this table's first and third rows that name Q2 (the generator's index: "Q2 (22)") | A paid review pass whose judgement then fails is charged nowhere, live or replayed — on the attempt path, and on the integration path, where `src/engine/topology/run.rs:170` settles only a Git error as unavailable and `:180` propagates every other judgement error, dropping `charged`; a snapshot worktree's pre-intent ephemeral commit stays in the object store, unreferenced, after a coordinator death, and nothing discards it, the packet classing it R27; the recorded report-staging leftover has no class; a creator that errs at P3a leaves both halves, retained and reported, which stay until an operator acts; and `cleanup.lock` is accounted under R21 while the site that creates it says R17, a second statement of its row that nothing cross-checks (`SWEEP-RESIDUE-AUTHORITY-002`, below). No ledger row measures spend, so no measured figure changes; Q2 names them. (`G4B-O9-EVENT-LOG-ANSWER-READER-NEVER-PRUNES-UNWITNESSED`, which this row named until repair round 5, now bears on none: its missing witness is committed at the range) |
| **Q3** | `SWEEP-FOLD-003`, `PR7-APPEND-REPORT-READABLE-UNDISCHARGED` | `apply_delta` does not refuse a poisoned fold, and the two production poisoning sites (`src/engine/topology/create.rs:1999`, `src/engine/topology/emit.rs:272`) apply no delta on their poisoning arms: Q3's answer holds by call-site discipline, not by the type. The append-error report's prose leaks without a discharge; the count and the discharge still bind |
| **Q4** — aliasing and containment | `PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS` (P1), the four `PR7-STD-*` ownership and private-root rows and the two unvalidated path components (P2), `SWEEP-WORKTREE-015` (P2), `SWEEP-WORKTREE-016` (P2), `PR5-R2-WIN-NON-SURROGATE-REPARSE` (P2), `PR5-WORKSPACE-003` (P2), `PR136-PASS3-P2-SLOT-PRIMITIVE-CENSUS-IS-SPELLING-BASED` (P2), `SWEEP-TESTS-SUBSTITUTION-CASE-READS-METADATA-WITH-OK`, `SWEEP-TESTS-REFUSALS-ASSERTED-BY-MESSAGE-SUBSTRING` | Q4's answer excludes by name: a link planted at `A/.git` at the Before hook sends `git update-ref` into another repository (the ref funnel's containment table covers `HooksPath` alone); the ownership proof's lexical fallbacks and the private-root locator's lexical-only check; two write boundaries, answer staging and the question payload, that take a path component unvalidated; a linked worktree that checks out the run ref between `assert_publishable` and the CAS has its HEAD moved (reproduced on Git 2.43 by that finding's reviewer); ref identity by exact bytes on a case-insensitive backend; Windows non-surrogate reparse points; a `repo_key` mutation that survives on the guest; the hostile-slot-name refusal grid's primitive set, matched by spelling, so a primitive spelt otherwise is outside the grid; and two test-strength caveats on containment witnesses — one reads a permission failure as absence when choosing its case, and the workspace manager's refusals are asserted by message substring. None is a crash shape and no witness here constructs one. (`PR110-CONTAINMENT-COMMENT-STATES-A-FALSE-GUARANTEE`, `SWEEP-PARSERS-008` and `SWEEP-NAMES-005`, listed here until repair round 5, no longer bear on Q4: the first two are not present at the range and the third aliases nothing) |
| **Q5**, and **clause 8** for the first two — the classification and census tooling | `PR7-WRAPPERS-EMPTY-DOMAIN` (P1) and 22 more: `PR5-R1-CFG-TEST-SHRINKS-THE-DOMAIN`, the four `PR73-*` residuals, `PR101-CFG-ATTR-…`, `PR103-CENSUS-DOMAIN-…`, `PR103-CONTAINER-SUBSTRATE-…`, `PR107-CONTAINER-LINT-CENSUS-…`, `PR110-SITE-CENSUS-…`, `W1-CLASSIFIED-MODULES-…`, `CLASS-GATE-STATED-DOMAIN-…`, `PR5-R2-OBJECT-GROUP-TAKES-NO-SITE`, `PR5-ANSWER-MODULE-COLUMN`, `G3-O2-FAULT-ROW-CENSUS-…`, `REVIEW212-VARIANT-CENSUS-…`, `SWEEP-SITES-READ-ONLY-ORDER`, the three `PR136` rows (two on the census sampler's command, one on the hostile-slot grid's primitive census), `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` and `PR119-POISON-INVARIANT-CLAIMED-FOR-SHARED-HARNESS` (the generator's index: "Q5 (23)") | Q5 was already asserted on the first, and clause 8 with it (§13); `PR5-R1-CFG-TEST-SHRINKS-THE-DOMAIN` is the same mechanism, open for its third part (`src/effects.rs:694`); the rest are further stated reasons of the same kind — censuses whose domain is a hand list, a substring match, a spelling or a directory walk, or that count less than their names say (one fault row under "every"; enum variants counted without their explicit discriminants), a funnel group that takes no site, an inventory module column that names `src/interaction.rs` for three Answer sites whose literals are in `src/rundir.rs`, a declared read-only boundary no test crosses with the observable orders, a sampling record that is not shown to be the funnel's command and on the guest is not Git-internal evidence for four sites, and an export four harness adapters can still record into after a panic poisoned it — and move no grade. (`PR5D-FUNNEL-RETURNS-A-COMMAND` and `PR5D-PROCESS-FUNNEL-TAKES-NO-SITE`, listed here until repair round 5, now bear on none: neither mechanism is present at the range) |
| **Q6** | `SWEEP-WORKTREE-016` (P2), `SWEEP-WORKTREE-015` (P2), `SWEEP-CHECKEND-001`, `REVIEW212-BUDGET-CEILING-NOT-POSITIVE` (P2), `G3-O3-INTEGRATE-SHAPE-TABLE-SHORT-BY-TWO`, `SWEEP-FOLD-OUTCOME-ADMISSIBILITY-NOT-REFUSED`, `PR3-RUNNER-DIGEST` (ST-20), and the five findings of the first row (the generator's index: "Q6 (11)" and "Q6 (ST-20) (1)") | The fold's refusal of `question_answered` after a halt is epoch-scoped (`src/topology/fold/check_end.rs:73` at the range), so after a resume the fold — and a replay of such a log — would admit an answer into a halted run before `run_finished`; the loop never offers one, because `select` answers `Closure` and `ingest_answers` returns early while `run_is_ending()`, which reads `halted_at` in every epoch (`src/engine/topology/select.rs:228-230`, `src/engine/topology/run.rs:792`, `src/topology/fold.rs:617-618`). So Q6's "no ingestion after a halt in the same epoch" is what is measured, and beyond it the limit is the checker's, not the engine's (until repair round 5 this cell said a halted run can ingest one). A Container run whose runtime reports no image digest is accepted under INV-23's reading and refused under the packet's `validation_at_fold` reading, and the range takes INV-23's (`PR3-RUNNER-DIGEST`). Ref identity by exact bytes can miss a case-variant checkout and let the CAS move it (`SWEEP-WORKTREE-015`), and a linked worktree that checks out the run ref after `assert_publishable` has asked has its HEAD moved by the CAS (`SWEEP-WORKTREE-016`). The fold's admission checks accept what no caller here offers: a zero budget ceiling breached by zero spend, which `design/17` refuses at load (`REVIEW212-BUDGET-CEILING-NOT-POSITIVE`), and a dispatch or a transaction start the loop's eligibility predicate would refuse — `check_transaction_start` (`src/topology/fold/check_integration.rs:15`) reads neither the entitlement nor the run's ending (`SWEEP-FOLD-OUTCOME-ADMISSIBILITY-NOT-REFUSED`) — so the fold-side guarantees are measured over the logs the loop writes. The integrate shape table has seven rows where the packet's terminal-shape table has eight, a naming gap and not a coverage one (`G3-O3-INTEGRATE-SHAPE-TABLE-SHORT-BY-TWO`). And convergence without an operator is not established in the four shapes of the first row. Q6 states each limit (until repair round 5 this cell gave no effect for the window, the two admission findings or the shape table) |
| **clause 1** | `SWEEP-FOLD-OUTCOME-CENSUS-BACKOFF-MIRROR`, `SWEEP-FOLD-OUTCOME-ADMISSIBILITY-NOT-REFUSED`, `SWEEP-CHECK_INTEGRATION-002` | The census's `backoff_pending` mirror is narrower than the code, so one outcome-side oracle can pass for the wrong reason (the fold's own test catches the mutation; the census's independence is what is weaker); `merge_prepared` is offered whenever a sequence remains, so a transaction start the fold admits and the eligibility predicate would refuse can lie inside the explored set (the census offers `task_dispatched` only where `ready` holds, `src/topology/census.rs:1125`); three integration grids assert only `is_err`. Clause 1 fails on the plan-shape ground regardless; a repair should carry these |
| **clause 2** | `PR5-R2-IDUNREAD-BEFORE-THE-PARSE`, `PR7-CREATEINTEGRATION-ORDER-BACKWARDS`, `PR3-REPORT-DOUBLE-NAME`, `PR3-FRAMEWORK-SILENT-3`, the two `REG-001` rows and `SWEEP-EFFECTS-PARENT-001` (measured inactive, §5.1), `SWEEP-RESIDUE-AUTHORITY-002`, `SWEEP-SITES-READ-ONLY-ORDER`, `G3-O1-…`, `G3-O2-…`, `PR110-SITE-CENSUS-…`, `PR5-C-FSYNC-UNOBSERVABLE`, `PR3-CONTAINER-START-ROW`, `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`, `PR119-POISON-INVARIANT-CLAIMED-FOR-SHARED-HARNESS` and `REVIEW212-VARIANT-CENSUS-IGNORES-DISCRIMINANTS` (the generator's index: "C2 (17)") | The `IdUnread` witnesses run a child that succeeds, so the point's position relative to the id parse is not what they distinguish — a caveat on certification row 22, which is certified, and on row 23, which is one of the three stated gaps (until repair round 5 this cell called both certified). `RefSite::CreateIntegration`'s adjacency value is backwards, not only its documentation: `Adjacent::Before` means the effect precedes the append (`src/topology/effects/vocab.rs:384`), the site answers `Before(RunStarted)` (`src/topology/effects/sites.rs:370`), and creation appends `run_started` before it creates the ref (`src/engine/topology/create.rs:14-15`), so the registry keys both entries `effect_before_event` and holds no entry for the order production has — the bijection passed against the derived key. The report sites' before phase tables no row where a recorded staging leftover can be durable. Four harness adapters still record into a poisoned harness, a condition on the export clause 2 reads. A vocabulary variant written with an explicit discriminant could fall out of `InjectionMode::ALL`, which fixes six points' modes, with the census green (none carries one at the range). `Container.Start`'s row is pinned as tabled, its coordinates among the 136 this gate leaves undecided. The rest move nothing the bijection measured: `report.json` named by two sites (`PR3-REPORT-DOUBLE-NAME`); the one-order condition, measured inactive (§5.1); two censuses the bijection does not rest on, the fault-row census covering one row (`G3-O2-…`) and the site census matching names by substring (`PR110-SITE-CENSUS-…`); the barrier call that cannot refuse, whose refusals are measured where they live (`G3-O1-…`, the Q1 row); the loss model (`PR5-C-FSYNC-UNOBSERVABLE`, the row above); and the read-only and cleanup-lock rows below (`SWEEP-SITES-READ-ONLY-ORDER`, `SWEEP-RESIDUE-AUTHORITY-002`, and `PR3-FRAMEWORK-SILENT-3`, whose two tabled rows were measured as written). (Until repair round 5 this cell called all of these documented-order and naming caveats) |
| **clause 5** | the 19 flake fingerprints — `CLASS-INTERMITTENT-SUBPROCESS-KILL-SETTLE-RESIDUE-FAILURES` (P1) among them — and ten condition rows: `PR64-CLEANUP-003-SCRATCH-PRECLEAN` (P1) with its pid-keyed and predictable-name species (`PR7-SCRATCH-FIXTURE-LEAK`, `PR104-PRELOCK-SCRATCH-NAME-REPRODUCIBLE-ACROSS-RUNS`, `PREDICTABLE-SCRATCH-ROOTS-BEYOND-VALIDATE`, `PR149-ARRANGED-OCCUPIED-REFUSAL-IN-SETTLE-KILL-TESTS`), `PR245-WINGUEST-IMAGE-SHIPS-FIXTURE-RESIDUE`, `PR262-WARM-BUILD-SLOT-SERVES-ANOTHER-WORKTREE`, `PR262-UNREACHABLE-PATH-ENTRY-ABORTS-PROGRAM-RESOLUTION`, `PR287-R2-STAND-IN-DRIVERS-REQUIRE-A-REAPING-ANCESTOR`, and §9.4's own | No row was red at the range on any platform after the fingerprinted reruns of §9.4. **The conditions were held unevenly, and that is the limit.** The two export runs used a fresh private `TMPDIR` and the guest suite a fresh `TMP`/`TEMP`, which exclude the pre-clean and scratch-name hazards there; **the gate legs ran under the build box's shared `/tmp`**, beside other sessions' suites — the gate wrapper sets no `TMPDIR`, and the range's own leg shows `/tmp/upstroke-scratch-*` paths (`gates/eight-logs-a2fe09c-range-1/03-test.log`) — where those hazards are not excluded, and every red those legs showed matched one of §9.4's filed fingerprints. The warm build slot is excluded by the private target and the binary-provenance check. The long `TMPDIR` is this gate's own finding, measured both ways. Every suite passed the two drivers that need a reaping ancestor, and which ancestor collected their stand-ins was not recorded. The winguest image's residue and the UNC `PATH` fixture belong to CI's winguest leg, green at the range, and the guest suite here passed that fixture (`guest/guest-suite.log:1859`). (Until repair round 5 this cell said the conditions were avoided here by a fresh private `TMPDIR`, which held for the export runs only) |
| **clause 7**, in addition | `SWEEP-CHECKEND-002`, `REVIEW212-BUDGET-CEILING-NOT-POSITIVE`, `PR7-R3-ATTEMPT-003-RESIDUE-DISCARD-UNREACHED` (P2), `PR128-RESIDUE-UNREACHABLE-OBJECTS-IGNORES-THE-EXIT-STATUS` | `check_run_finished` checks the outcome and the halt attribution, not the `merged`/`parked` counts, and accepts a zero ceiling that `design/17` refuses at load so no producer makes; clause 7 certifies the outcome equality, which is the checked half. The ledgers' R27 rows read the object store through an `fsck` whose exit status is never read, and count an unreferenced ephemeral commit as the packet classes it |
| **Q1 (ST-12)** | `G4-O2-OVERRIDE-SINGLE-RUNG-EXHAUSTION-UNWITNESSED` | An unwitnessed exhaustion; ST-12 stays graded for named cases. (`SWEEP-FOLD-PREDICATES-OVERRIDE-READER`, carried here until repair round 5 with its mechanism not present as filed, now bears on none, with the lines that show it) |
| **clause 9**, and §8's **groups 1 and 3** | clause 9: the five deferred P1s §9.3 names — `PR7-WRAPPERS-EMPTY-DOMAIN`, `PR5-RD-002`, `PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS`, `CLASS-INTERMITTENT-SUBPROCESS-KILL-SETTLE-RESIDUE-FAILURES` and `PR64-CLEANUP-003-SCRATCH-PRECLEAN`; group 1: `CLASS-INTERMITTENT-…`; group 3: `SWEEP-CLASSIFY-009`, `SWEEP-WORKTREE-013` and `PR7-P3A-CREATOR-RETAINS` (the generator's index: "C9 (5)", "group 1 (1)", "group 3 (3)") | Clause 9 is established on the ledger's own terms and weakened by these five, each carried where it bears (§9.3, §13 row 9). Group 1's exhaustive kill matrix is graded asserted already, and its intermittent species is the flakes §9.4 fingerprints. Group 3's recovery of damaged state is established for the checks it names, and these three are shapes none of its witnesses constructs — a transient listing failure, a user-written lock reason, a creator erring exactly at P3a — so the Q1 row names them and group 3's grade stands. (Until repair round 5 this table had no row for clause 9 or the two groups; clause 8's two are in the Q5 row) |

Also carried: `PR291-SNAPSHOT-OWNER-READINESS-BOUND-EXPIRED-ON-A-LOADED-GUEST` (a 15-second readiness bound in a
legacy-workspace test on a loaded guest, not an engine defect; clause 5), `PR292-MACOS-ZOMBIE-ONLY-GROUP-EPERM-FAILS-KILL-TREE`
(macOS, outside this run's measurements; CI's macOS leg was green at the range with the witness that exposed it compiled
out there, so that green says nothing about it) and `PR10-LEGACY-REPORT-REWRITE-DROPS-ITS-POSIX-ACL` (the report writer's
staged publication drops a POSIX ACL; `rundir::write_report` is also the schema-4 finalization's writer,
`src/engine/topology/finalize.rs:110`, and no clause certifies ACL preservation; it bears on none). The five framework
docs-contract P3s state no measured figure (the actions they name are executed here) and bear on nothing, except
`PR3-FRAMEWORK-SILENT-3`, read with `SWEEP-RESIDUE-AUTHORITY-002` below. **Twelve rows bear on none at this head because
their filed mechanism is not present at the range**, each with the lines that show it in the TSV:
`SWEEP-FOLD-PREDICATES-OVERRIDE-READER`, `G4B-O9-EVENT-LOG-ANSWER-READER-NEVER-PRUNES-UNWITNESSED`, `SWEEP-PARSERS-008`,
`PR110-CONTAINMENT-COMMENT-STATES-A-FALSE-GUARANTEE`, `PR5D-FUNNEL-RETURNS-A-COMMAND`, `PR5D-PROCESS-FUNNEL-TAKES-NO-SITE`,
`PR128-REVIEW2-STORE-SCAN-HANGS-ON-A-FIFO`, `PR128-RESIDUE-RECORD-FOR-FOLDS-A-FAILED-WORKTREE-LIST`,
`PR128-REVIEW-RECORD-FOR-READS-AN-UNLISTABLE-REGISTRATION-AS-ABSENT`, `PR128-REVIEW2-SYMLINKED-GIT-DIR-RECLASSIFIED`,
`PR128-COMMONDIR-UNWRITTEN-READ-AS-AN-INSPECTION-FAILURE` (its remaining enumeration half is `PR5-RD-002`'s) and
`PR128-NON-UTF8-TEST-CANNOT-RUN-ON-MACOS`; their files stay open. And `PR10-RECORD-STALE-SENTENCES-AFTER-ROUND-11`,
whose bearing the owner's 2026-09-14 decision leaves to this gate, is decided here: **none** — none of its five stale
sentences is a figure or mechanism this gate reads (this report cites none of them, checks the staging leftover's
ownership against `src/rundir.rs`, runs the four checkout-barrier guards by name in group 5, and fingerprints the
reaper-lease flake from its own logs). What no finding does is move a grade that §13 did not already carry: clause 1
stays not established on its own ground, clauses 2 and 8 asserted, and the established clauses gain the conditions
named above rather than losing their grade.

**Two more, which the first two heads listed by filename only.** Review round 2 asked for an assessment, and
a filename is not one:

- **`SWEEP-RESIDUE-AUTHORITY-002`** (P3, docs-contract, deferred) — `cleanup::take` creates a durable
  `<public>/cleanup.lock` with `create(true)` (`rundir.rs:2446-2452`) inside the `Lock.ProbeCleanupExclusive`
  funnel, whose `row()` is **R17**, "the coordinator's own lock holds (OS lock state only)"
  (`sites.rs:1190`, `vocab.rs:141`), and whose after-effect is `MomentaryHold` → `rows: []`
  (`residue_authority.rs:974`). The registry cannot say otherwise: `validate_entry` rejects any entry whose
  rows differ from `semantics()` (`src/topology/effects/registry.rs:671-679`), so the coordinate's tabled residue reads "no row is
  left holding it" while the site leaves a file. **It bears on clause 2 and on Q2**, and the mechanism is
  exact: this run's own class rule says obligation (i) means *every durable file the coordinate leaves,
  including lock files*, and at these coordinates that cannot be checked against the registry table at all —
  only against the packet's R21 text, which is a different document. `Lock.AcquireRun` has the same shape for
  `run.lock`. It does **not** weaken clause 5: the ledger does account for the file, as `R21 › cleanup_lock_file`
  (`ledger.rs:702-705`), which is exactly the subrow §7 counts. **It also forces a correction in this report**:
  `Lock.ProbeCleanupExclusive` is **not** a read-only site — `sites.rs:1241-1243` returns `false` for it, the
  committed test pins the read-only set to `Worktree.Verify`, `Event.ProvePrefixStable`, `Answer.Ingest` and
  `Lock.ObserveCleanupHold` (`src/topology/effects/tests.rs:1327`), and `residue_authority.rs:1551` asserts
  `!LockSite::ProbeCleanupExclusive.is_read_only()` under the comment *"the probe creates the lock file it
  probes"*. The first two heads called it read-only in §5.3; that is withdrawn.
- **`SWEEP-SITES-READ-ONLY-ORDER`** (P3, correctness, deferred) — three read-only sites (`Worktree.Verify`,
  `Answer.Ingest`, `Lock.ObserveCleanupHold`) carry `ObservableOrder::EffectBeforeEvent`, documented as "the
  effect is durable, the adjacent append is not" (`residue_authority.rs:195`), while the same authority answers
  `AfterEffect::NoEffect` → `rows: []` for them (`src/topology/effects.rs:538-543`); the order is derived from adjacency
  with no read-only carve-out (`src/topology/effects.rs:338-344`). **It bears on clause 2 and on Q5, and moves neither
  grade.** Six of the 168 coordinates are keyed by an order their own site denies — without it they would still be
  required, keyed `None` (`src/topology/effects/bijection.rs:562-564`) — so at those three sites the order axis §5.1
  counts is vacuous rather than substantive (until repair round 5 this sentence said the six exist because of the
  order). For Q5 it is a second instance of the species `PR7-WRAPPERS-EMPTY-DOMAIN` names: a declared boundary that
  nothing cross-checks — the finding's own prescription, a test relating `is_read_only()` to `observable_orders()`
  across all sites, does not exist at this range (`src/topology/effects/tests.rs:1327` and `:1193` each assert one side
  and never cross them).

Neither is a defect in a measured figure, and neither changes the verdict.

**One finding is filed by this gate**: `G5RUN2-DOCKER-DIAGNOSTIC-TEST-DEPENDS-ON-A-SHORT-TMPDIR` (P3,
deferred), executed both ways (§9.4).

### 9.3 The deferrals, and what they do to the verdict

- **`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002` are not deferrals of this range**: no finding file carries
  either id at `a2fe09c2`, and the commits that removed them (`7da321c0`, `1c309d49`) are ancestors of PR10's
  merge base `231c1aad` as well as of the range. Verified in `findings/` and in `git log`.
- **`PR7-WRAPPERS-EMPTY-DOMAIN` (P1, security-trust, deferred) bears on Q5 and on the verdict.** It records a
  demonstrated bypass: production functions below a module's first `#[cfg(test)]` item fall outside the
  classification domain `effects::externally_reachable_fns` computes, and a `pub(super) fn` there reached an
  effect with clippy and the suite green. At this head that code path is unchanged. Q5 certifies exactly that
  classification, so **Q5 is asserted, not established**, and clause 8 with it. The first head placed this
  finding outside the gate's subject; that was wrong.
- **`PR5-RD-002` (P1, crash-consistency, deferred) is inside the path this gate certifies.** A kill inside
  `git worktree add`'s registration window can leave a zero-length `commondir`, after which enumeration fails
  and every `WorkspaceManager` call that revalidates propagates the error — including the reclaim loops a
  resume runs and finalization's slot scrub. #295 appended a reproduction inside this range. No committed test
  constructs that shape inside a topology resume. **Repair round 5 constructed it and drove the tree's own loops
  over it**, in a scratch copy: with two task intents whose second registration is torn as the finding records
  (`locked` reading "initializing", `commondir` zero-length), finalization's `scrub_slots`
  (`src/engine/topology/finalize.rs:240-260`, made `pub(crate)` in the copy so a test could call it) and the resume's
  `reclaim_intents` (`src/workspace_manager.rs:2070-2089`) each fail on all three of three passes, at the first
  slot's `remove_intent`, whose `revalidate` enumerates (`:1992`), and both intents stay; with the torn registration
  first, both converge on the first pass (`repair-r5/pr5-rd-002-probe/probe.log`; review round 5's verify lens drove the
  same shape through the manager calls in `scrub_slots`' order). So a torn registration that the loop does not reach
  first stops finalization and recovery from converging until an operator removes it — the finding's manual
  recovery is `rm -rf <common-git-dir>/worktrees/<slot>` — and that is an **operator-only convergence shape**, beside
  its sibling `PR5-RD-003`'s (a torn `gitdir`, which the plain removal funnel refuses for every slot) and the two
  ref-lock shapes of §9.2; clause 7, Q1 and Q6 carry all four. The finding's reachability note holds at
  this range too: `WorkspaceManager::derive` is called only from test code, so the shipped binary does not reach this
  path; the topology this gate certifies is the one its tests drive.
- **Of the other seven P1s**, three are instruments (`SHAPE-GATE-MISSES-ARITHMETIC-SUBSTITUTION`,
  `PR273-R1-DECODER-GUARD-MATCHES-A-SPELLING`, `PR286-PROSE-SCANS-READ-THE-WRITTEN-SPELLING`), one is the legacy
  path (`LEGACY-WORKSPACE-READS-REPLACEMENT-OBJECTS`: its own text says the schema-4 path sets
  `NO_REPLACEMENT_OBJECTS` at its boundaries), and **three touch this gate where the heads before `b95f03fe` said "outside"**:
  `PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS` is a trust boundary on the ref funnel the CAS uses (Q4: an adversarial
  repository, not a crash shape); `CLASS-INTERMITTENT-SUBPROCESS-KILL-SETTLE-RESIDUE-FAILURES` is the species the
  box flakes of §9.4 resemble (clause 5); `PR64-CLEANUP-003-SCRATCH-PRECLEAN` is a test-fixture hazard to the
  suite's own measurement under concurrent runs (clause 5's conditions, not in play here). None of the three is a
  crash/resume mechanism the positive clauses certify; clause 9 names all five (§9.2, §13).
- **`SWEEP-CHECK_INTEGRATION-003`** (P3) records that `ladder::next_step` allows `max_defers` deferrals where
  the fold allows `max_defers − 1`. The census's bound is the fold's reading, which the amendment adopted, so
  it bears on no pass-rule clause here. **The two packet mentions the clause-1 amendment left unchanged** bear
  on none either: the census clause reads `decisions.bounded_census.bounds`, which is corrected.

### 9.4 The flakes and measurement artifacts on this run, fingerprinted

| Fingerprint | Where | Control | Finding |
|---|---|---|---|
| `real_docker_prints_the_transcribed_unreachable_diagnostics`, `container/tests.rs:4114`, *"unix socket path … is too long"* | export run 1 (1 of 3 Linux full runs); my `TMPDIR` was **102 characters** and the socket path the test built under it **141** (`suite/export1.cargo.log:2823`), against `sun_path`'s 108-byte array (107 usable) | **executed both ways**: alone under a **105**-character `TMPDIR` (socket path **144**) it fails (`cargo exit=101`), and under a short `TMPDIR` it passes (`cargo exit=0`, `docker/controls.log:179-201`) — my measurement environment, not the range | filed here as `G5RUN2-DOCKER-DIAGNOSTIC-TEST-DEPENDS-ON-A-SHORT-TMPDIR` (P3) |
| `real_docker_kill_on_an_already_exited_container_is_tolerated` and `real_docker_returns_both_streams_of_a_container_separately`, `container/tests.rs:3436` | the Docker-required run (2 of 23) | 3 of 3 alone each | `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME` (P3) |
| `a_host_integration_reaper_holds_the_runs_cleanup_lease`, `recover/tests.rs:8841`, *"the hold outlived the reaper that took it"* | gate attempt 1 at `5baf00c`, attempt 1 at `8b8acc0`, attempts 1 and 3 at `16aea26`, attempt 3 at `1798002` (§11) — five failures | **3 of 3 alone** at `5baf00c`, `8b8acc0`, `16aea26` and `1798002` (`flakes/alone-*.log`) | `PR281-CLEANUP-LEASE-HOLD-OUTLIVED-AND-ITS-UNREADABLE-TWIN` (P2) |
| `real_docker_returns_both_streams_of_a_container_separately`, `container/tests.rs:3436` | beside it at `5baf00c` attempt 1, and at `16aea26` attempts 1 and 2 — three gate failures, besides the Docker-required run | 3 of 3 alone at `5baf00c` and at `16aea26` | `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME` (P3) |
| `real_docker_kill_on_an_already_exited_container_is_tolerated`, `container/tests.rs:3436` | `16aea26` attempt 3, `e9074f0` attempt 1, `1798002` attempts 1 and 2 — four gate failures, besides the Docker-required run | per head, below | `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME` (P3) |

**Three fingerprints account for every red this run saw** — the table's: the socket-path one in export run 1 (my own
long `TMPDIR`); the Docker poll-count one at `container/tests.rs:3436`, for both Docker tests, in the Docker-required
run and at gate attempts; and the reaper-lease one at `recover/tests.rs:8841`. (Until repair round 5 this sentence
said "those two fingerprints", under a table of three.) Through `1798002`'s four attempts the gate test legs number
**21** — one per `gates/eight-logs-*` directory — of which **12** were green and **9** red, with **12** failed tests:
`5baf00c` attempt 1 (2), `8b8acc0` attempt 1 (1), `16aea26` attempts 1, 2 and 3 (2, 1, 2), `e9074f0` attempt 1 (1)
and `1798002` attempts 1, 2 and 3 (1, 1, 1), each a `---- … stdout ----` block in its `03-test.log`
(`repair-r5/figures.out`). By test: the reaper-lease flake 5, `real_docker_kill_on_an_already_exited_container_is_tolerated`
4 and `real_docker_returns_both_streams_of_a_container_separately` 3. Through `7cde80c`'s own run the figures were 17
legs, eleven green and 9 failures (15 and nine through `1bdd6d7`'s run; the head before that said 14 and eight, having
not counted its own run); the first two heads said "seven", which contradicted §11's own table. **The box's one-minute
load average at the start of those nine red attempts ranged from 0.23 to 12.03**, and at the start of every attempt at
every head from 0.23 to 16.07 (`gates/box-load-*.log`, one per attempt; `repair-r5/figures.out`). Those logs record the
load, memory and disk — not what produced the load — and a figure taken after an attempt includes the gate's own
build. Until repair round 5 this paragraph said the red attempts ran "under a box at load average 6–11 from other
sessions", which neither the figures nor the logs support.

**The isolated-control claim is stated per head, not as a blanket.** The reaper-lease flake and
`real_docker_returns_both_streams_of_a_container_separately` were each run **3 of 3 green alone at the head
that saw them** (`flakes/alone-5baf00c.log`, `alone-8b8acc0.log`, `alone-16aea26.log`), and the reaper-lease flake
again at `1798002` (`flakes/alone-1798002.log`). `real_docker_kill_on_an_already_exited_container_is_tolerated`
failed at four attempts, and they are not all alike:

- at `16aea26` attempt 3 it has **no isolated control at that head** — `flakes/alone-16aea26.log` does not
  contain it. Its controls for that occurrence are at the frozen range, taken hours earlier
  (`docker/controls.log:16-84`, `cargo exit=0` each), and it passed in the full suite at attempt 4 and at
  `2f38810`;
- at `e9074f0` attempt 1 it failed with the same fingerprint — *"`upstroke-f1-already-exited` is still running
  after 200 observations"*, `container/tests.rs:3436`, in an attempt that started at a one-minute load of 5.41 and
  ended at 9.85 (`gates/box-load-e9074f0-head-7.log`; this bullet said "load average 11.09" until repair round 5, a
  figure no saved file holds) — and here the control **was** taken at that head: **3 of 3 green alone**,
  `cargo exit=0` each (`flakes/alone-e9074f0.log`). Before the retry I removed the one exited
  `upstroke-f1-already-exited` container my own failed attempt had created, at 04:40:02Z, about two minutes before
  the 04:42:18Z listing (this bullet said "four minutes"), and nothing else — the other 20 containers on the box
  belong to other sessions and were left alone (`docker/leftover-containers-before-head7-retry.log`). Attempt 2 was
  `ALL 9 PASS at e9074f0`;
- at `1798002` attempts 1 and 2 it failed with the same fingerprint, and the control was taken at that head both
  times: **6 of 6 green alone**, `cargo exit=0` each (`flakes/alone-1798002.log`); the exited container each red
  attempt left was removed before the retry, and other sessions' containers were left alone
  (`docker/leftover-containers-before-head11-retry.log`). Attempt 4 was `ALL 9 PASS at 1798002`.

The first two heads' blanket "every one of them was green 3 of 3 alone at the head that saw it" is withdrawn;
the per-head statement above is what the logs hold. The gate run at the frozen range, the guest suite, every group
invocation, every sampler loop and every probe were green, and every mutation run ended with the status its row
records — a killed recipe exits 101 by design (§3; until repair round 5 this sentence called every mutation run
green).

## 10. The six standing questions

**Q1 — reconstruction from durable state alone.** Answered by replaying the bytes on disk: **15 asserted
comparisons** (8 live-versus-replay, 7 replay-twice), each printed with the log's length and sha256 (§6); the
census's classification equal live and on replay over every explored state; the stable-prefix barrier before any
fold-derived effect, with each of the refusals named below guarded by a killed mutation; the two-crash proof;
the husk, marker, intent and container reclaims; the Runner rebuilt from `run_started(4).runner`. The
process-local holdings are observed and **certified at G6, not here**. **Answered with evidence for G5's
portion**, with the exclusions §9.2's Q1 rows name — 37 findings in the generator's index, each named here or in
its group — which are: the torn
worktree registrations, which stop the resume's reclaims from converging until an operator acts (`PR5-RD-002`'s
zero-length `commondir`, executed this round, §9.3; `PR5-RD-003`'s absent, empty or partial `gitdir`, with
`RESIDUE-UNBINDABLE-TASK-REGISTRATION-HAS-NO-DESIGN-SENTENCE`, which the plain removal funnel the resume calls refuses
for every slot); the two ref-lock shapes in which a resume refuses resumably
until an operator acts (`PR8-CRASH-002-PACKED-REFS-LOCK`, `-PACKED-RUN-REF`); a refused kill that leaves the reaper's
R28 hold unbounded on its docker-reclaim arm, which only tests arm at this range
(`REFUSED-KILL-UNBOUNDS-THE-BOUNDED-DOCKER-REAP`); the classifier's inspection primitives, which read a Git failure
or refusal as something it is not (the fourteen `PR128-*` rows of §9.2); the husk classifier's folds of a listing or
I/O failure into absence (`SWEEP-CLASSIFY-009`, `-010`) and its unreclaimed non-UTF-8 husks (`SWEEP-NAMES-005`); the
ownership proof's lexical fallbacks when canonicalization fails and the private-root locator's lexical-only check
(the four `PR7-STD-*` ownership and private-root rows); three hostile-input liveness limits; recovery step (d)'s
`LineageHeld` arm (`PR7-STEP-D-LINEAGE-ARM-UNWITNESSED`), which a committed test now drives (`src/engine/topology/recover/tests.rs:3104`) and whose power
over the finding's mutation this gate did not measure (this answer called the arm unwitnessed until repair round 5);
the barrier call at `recover.rs:482`, which is handed the same measurement three times and cannot refuse
(`G3-O1-BARRIER-INERT-AT-CALL-SITE`); R28's next-coordinator refusal, which has a committed witness whose power this
gate did not re-measure (`PR4-R28-NEXT-COORDINATOR-UNWITNESSED`); under an inherited `GIT_TRACE`, a silent absence
that becomes a Git error at three lookups, failing closed (`PR128-PARENT-GIT-TRACE-MAKES-ABSENCE-AN-ERROR`); a
user-written lock reason that makes a populated worktree read unpopulated (`SWEEP-WORKTREE-013`); a creator that errs
exactly at P3a, whose two halves wait for an operator (`PR7-P3A-CREATOR-RETAINS`); and a log rewritten to CRLF, folded
rather than refused where no commit record's digest guards it (`SWEEP-FOLD_PARSE-002`). For ST-12, a one-off
binding's single-rung exhaustion has no loop-level witness, so ST-12 is graded for its named cases
(`G4-O2-OVERRIDE-SINGLE-RUNG-EXHAUSTION-UNWITNESSED`, §9.2's Q1 (ST-12) row). (Until repair round 5 this answer
named only some of these.) Two things are enforced elsewhere and were
measured here. **The barrier's refusals** are `establish_stable_prefix` (`src/events/log.rs:871-970`) and the open it
calls first (`EventLog::open_funnel`, `:274-375`, called at `:879`), and the following refusals each have a killed
mutation, located by the function its changed lines sit in (`repair-r5/mutation-attribution/`): the open's sync step
(`g5-open-sync-skipped`, in `open_funnel`, `:359-360`), with `g5-sync-prefix-point-not-consulted`, also in
`open_funnel`, guarding the SyncPrefix fault point itself (`:351-357`); a torn tail that reappears on reread, a
length change and a byte change (the three `g5-barrier-*-ignored-log` recipes, in `establish_stable_prefix` at
`:894`, `:903` and `:914`); the committed first line's digest, mismatched and missing (`:926-948`); and the checked
replay's parse and fold refusals (`:953-962`). The last four were mutated in repair round 5, in a scratch copy, and
each is killed by its log-suite test, `cargo exit=101` (`repair-r5/barrier-refusals/summary.tsv`); the missing-digest
mutation is also killed by `a_kill_while_the_first_line_is_written_leaves_a_retained_husk_whose_next_open_truncates_it`,
and the replay mutation by two resume tests. No recipe mutates the reread's own I/O failure (`:888-892`), the open's
directory sync (`:362-363`) or its create and truncate steps. Until repair round 5 this paragraph said the barrier has
"four refusal predicates each guarded by a killed mutation" and that all five killed refusal mutations live in
`establish_stable_prefix`; two of them are in `open_funnel`, and "four predicates" is the name of a container-census
test (`the_stable_prefix_barrier_refuses_each_of_its_four_predicates_independently`) that exercises the
`StablePrefixBarrier` abstraction `recover.rs:482` calls, not `establish_stable_prefix`. **The ordering** is enforced
at the production call site itself, where the killed mutation `g5-effect-before-barrier` inserts `remove_marker`
before `establish_stable_prefix` in `run_recovery_order` (`src/engine/topology/recover.rs:912-1046`,
`mutations/run-g5/g5-effect-before-barrier.diff`), and
`resume_establishes_stable_prefix_barrier_before_any_fold_derived_effect` fails with *"Event.OpenLog (12) before
the census (10)"* (`mutations/run-g5/g5-effect-before-barrier.log:14`; `rc=101`, `mutations/chain-g5-chunk-02.log:9`).

**Q2 — resource accounting.** Six ledgers × 54 rows, 324 of 324 `yes` on both hosts, with every packet
`at_run_end` cell placed against its observed row — a mapping that was wrong for the resumed table at
`2f388103` and was regenerated at `71aede1b`, 168 of 168, each block against its own rendered outcome (§7). One gap
is named rather than measured: `SWEEP-RESIDUE-AUTHORITY-002` shows the accounting has **two** statements of
where `cleanup.lock` lives — the ledger and packet say R21, the site's own `row()` says R17 — and only the
ledger side is measured here; nothing in this gate cross-checks site rows against the packet's row text
(§9.2). The negative run 1 recorded is gone: the reclaimed
staging directory is reclaimable, and the witness that proves it is committed, with both sides of its barrier
bounded by two committed tests (§3.5). What remains outside the accounting is the *classification* of that
leftover. More are named rather than measured (§9.2's Q2 rows, 22 findings in the generator's index): a paid review
pass whose judgement then fails is charged nowhere (`ATTEMPT-PAID-REVIEW-CHARGED-NOWHERE-WHEN-THE-JUDGEMENT-FAILS`; no
ledger row measures spend); a snapshot worktree's pre-intent ephemeral commit that stays in the object store,
unreferenced, after a coordinator death, which the packet classes R27 (`PR7-R3-ATTEMPT-003-RESIDUE-DISCARD-UNREACHED`;
this answer called it reachable until repair round 5); the classifier's inspection primitives and the torn `gitdir`
registrations, through which a residue can be read as none; and a creator that errs at P3a, whose two halves stay
retained until an operator acts. The production answer reader's never-prunes, which this answer named as unwitnessed
until repair round 5, has a committed byte-identity witness after the loop's ingestion at the range
(`src/engine/topology/recover/tests.rs:13273-13277`, `:13305-13309`), and its finding now bears on none. Repair
round 5 also added the integration-path twin of the first: `src/engine/topology/run.rs:170` settles only a Git error as unavailable and
`:180` propagates every other judgement error, dropping `charged`, so an integration review charged live and then
followed by a ledger refusal is lost on replay (`PR272-R2-DESIGN-26-OVERPROMISES-UNAVAILABLE`, whose own probe read
`live=3.7, replay=1.2`). **Answered with evidence for G5's portion, with those exceptions named**: the durable rows,
and the entitlement grants per generation and transaction. The per-process grants Q2 also asks about — slot,
provisional, invocation, lock-hold and process-handle — are process-local holdings the definition certifies at G6,
and none is certified here. (Until repair round 5 this answer did not scope itself.)

**Q3 — stale completions.** The append-error protocol at `Written`, `WrittenFull` and the undetermined case;
the NoRunFinished ledger; the error-return driver; the closure recipes `closure-swallows-close-append-error` and
`closure-swallows-undetermined-close-append` each kill
`an_append_error_at_the_run_ending_close_ends_the_command_and_the_next_resume_closes_the_generation`, `rc=101`
(`mutations/summary-pr10-recipes.tsv`). Two limits (§9.2's Q3 row, 2 findings in the generator's index): a poisoned
fold is kept from applying a stale delta by its callers, not by its type — `apply_delta` does not read the poison
(`src/topology/fold.rs:547-559`), and the two production poisoning sites apply no delta on their poisoning arms
(`src/engine/topology/create.rs:1999`, `src/engine/topology/emit.rs:272`; `SWEEP-FOLD-003`); and the append-error
report's prose can be read without discharging it, while its count and its discharge still bind
(`PR7-APPEND-REPORT-READABLE-UNDISCHARGED`). Neither lets a stale fold drive a mutation in anything measured here
(this answer named neither until repair round 5). **Answered with evidence for G5's portion, by call-site
discipline where the type does not enforce it.**

**Q4 — aliasing.** The husk family under the ownership proof, the 13 publication prefixes, the fast
publication's exact-base relations in both directions, and the report staging directory named uniquely per
write and reclaimed only by its record. Excluded by name (§9.2's Q4 row, 14 findings in the generator's index), none
constructed by a witness here and none a crash shape: a link planted at `A/.git` at the Before hook that sends
`git update-ref` into another repository (`PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS`, a deferred P1); the ownership
proof's lexical fallbacks, the private-root locator's lexical-only check and two unvalidated path components (the
`PR7-STD-*` rows); the window between `assert_publishable` and the CAS in which a linked worktree's checkout is moved
(`SWEEP-WORKTREE-016`); ref identity by exact bytes on a case-insensitive backend (`SWEEP-WORKTREE-015`); Windows
non-surrogate reparse points (`PR5-R2-WIN-NON-SURROGATE-REPARSE`); a `repo_key` mutation the guest's suite does not
detect (`PR5-WORKSPACE-003`); the hostile-slot-name refusal grid, whose set of slot-taking primitives is matched by
spelling (`PR136-PASS3-P2-SLOT-PRIMITIVE-CENSUS-IS-SPELLING-BASED`); and two test-strength caveats on the containment
witnesses — one reads a permission failure as absence when choosing its case
(`SWEEP-TESTS-SUBSTITUTION-CASE-READS-METADATA-WITH-OK`), and the workspace manager's refusals are asserted by message
substring (`SWEEP-TESTS-REFUSALS-ASSERTED-BY-MESSAGE-SUBSTRING`). Until repair round 5 the hostile-slot grid and the substitution-case caveat were in §9.2's Q4 row with no answer
here, the refusals-by-substring caveat was classed as bearing on nothing, and `SWEEP-PARSERS-008` was named here
although its mechanism is not present at the range.
**Answered with evidence for G5's portion, with those exclusions.**

**Q5 — effect/append boundaries. Asserted, not established.** Both hook phases and every parent-side point of
the 68 sites are *observed* executed with 0 bijection failures on both hosts, and every claim the host
requires is held to its named test's own record (§5.2); but the question also asks whether the wrapper
classification and the allowlists are complete for the code in range, and `PR7-WRAPPERS-EMPTY-DOMAIN` is a
filed, unfixed, demonstrated bypass of exactly that classification at this head (§9.3).
`SWEEP-SITES-READ-ONLY-ORDER` is a second declared boundary nothing cross-checks: the test it asks for,
relating `is_read_only()` to `observable_orders()`, does not exist at this range (§9.2). In ST-07's own sense
the executed evidence is certified on all three obligations at **29** of the 32 parent-executed points, with three
stated gaps (rows 1, 2 and 23) and, at rows 8, 9 and 14, a replay-twice check without power over the coordinate's own
line, and undecided at the 136 before/after coordinates (§5.3) — undecided, not absent; the fault fired at all 32 in
the two-host union (27 Linux, 28 guest) under the merge check's own adapter-test exclusion. Besides
`PR7-WRAPPERS-EMPTY-DOMAIN`, **22 filed findings** bear on Q5 (§9.2's Q5 row; the generator's index reads "Q5 (23)"):
censuses whose domain is a hand list, a substring match, a spelling or a directory walk, a funnel group that takes no
site, a sampling record not shown to be the funnel's command, and an export four harness adapters can still record into
after a panic poisoned it; they move no grade because Q5 is asserted already. (This sentence said "sixteen" until
repair round 5, a count of the row's comma-separated entries that no file supports.) The residue classes are
recovery-proven and sampled on both hosts — on the guest the census sampler's four sites are not Git-internal residue
evidence (`PR249-KILL-SAMPLER-WINDOWS-WRAPPER`) — and never claimed as parent-observed execution.

**Q6 — sequential guarantees.** One fold and one writer; the untouched user checkout; exact-base fast
publication; atomic settlements; the derived outcome with halt > budget > parked > complete; `budget_exceeded`
before any budget-driven end; closure and finalization as §8 grades them; no answer ingestion after a halt or
budget stop in the same epoch; one Runner identity per run. At max_parallel = 1 there is one schedule, so the
scheduling-invariance clause **is not measured**, and is said not to be. The limits are named (§9.2's Q6 row, 12
findings in the generator's index): the four shapes in which finalization or recovery refuses resumably until an
operator acts — the two torn worktree registrations (`PR5-RD-002`; `PR5-RD-003`, with
`RESIDUE-UNBINDABLE-TASK-REGISTRATION-HAS-NO-DESIGN-SENTENCE`) and the two ref-lock shapes
(`PR8-CRASH-002-PACKED-REFS-LOCK`, `-PACKED-RUN-REF`) — so convergence without intervention
is not established there (until repair round 5 this answer named the two ref-lock shapes only); the
`assert_publishable`-to-CAS window in which a linked worktree's checkout is moved (`SWEEP-WORKTREE-016`), and ref
identity by exact bytes, which can miss a case-variant checkout of the run ref (`SWEEP-WORKTREE-015`) — the "untouched
user checkout" guarantee under shapes no witness here constructs; one Runner identity per run under INV-23's reading of
when a Container run's image digest is required, which the range takes and the packet's `validation_at_fold` does not
(`PR3-RUNNER-DIGEST`); the fold's admission checks, which accept a zero budget ceiling no producer makes
(`REVIEW212-BUDGET-CEILING-NOT-POSITIVE`) and a dispatch or a transaction start the loop's eligibility predicate
would refuse (`SWEEP-FOLD-OUTCOME-ADMISSIBILITY-NOT-REFUSED`), so "one fold" is measured over the logs the loop
writes; the integrate shape table's seven rows against the packet's eight, a naming gap and not a coverage one
(`G3-O3-INTEGRATE-SHAPE-TABLE-SHORT-BY-TWO`); and the fold's refusal of `question_answered` after a halt, which is epoch-scoped
(`src/topology/fold/check_end.rs:73`): after a resume the fold, and a replay of such a log, would admit an answer into a
halted run before `run_finished`, though the loop never offers one, since `select` answers `Closure` and
`ingest_answers` returns early while `run_is_ending()`, which reads `halted_at` in every epoch
(`src/engine/topology/select.rs:228-230`, `src/engine/topology/run.rs:792`, `src/topology/fold.rs:617-618`) — so "no
answer ingestion after a halt in the same epoch" is what is measured, and beyond it the limit is the checker's (this
answer said a later epoch can ingest one until repair round 5, and named neither admission finding nor the shape
table; `SWEEP-CHECKEND-001`). **Answered with evidence for
G5's portion, with those limits.**

## 11. The ten gates at this report's own heads

| Head | What it is | Result | Logs |
|---|---|---|---|
| `a2fe09c2…` | the frozen range | `ALL 9 PASS at a2fe09c` **first attempt**; `test-pr-ready-audit: ok` | `gates/w1-eight-iso-a2fe09c-range-1.log`, `gates/eight-logs-a2fe09c-range-1/`, `gates/gate10-a2fe09c-range-1.log` |
| `5baf00ce…` | this report's first commit | **two attempts**: attempt 1 FAIL on the test leg alone (2671 passed, **2 failed**) on the two filed box flakes of §9.4, each 3 of 3 green alone; attempt 2 **`ALL 9 PASS at 5baf00c`**; `test-pr-ready-audit: ok` at both | `gates/w1-eight-iso-5baf00c-head-1*.log`, `gates/eight-logs-5baf00c-head-1*/`, `flakes/alone-5baf00c.log` |
| `8b8acc01…` | its gate rows | **two attempts**: attempt 1 FAIL (2672 passed, **1 failed**) on the reaper-lease flake, 3 of 3 green alone; attempt 2 **`ALL 9 PASS at 8b8acc0`** | `gates/w1-eight-iso-8b8acc0-head-2*.log`, `flakes/alone-8b8acc0.log` |
| `31c5fbd8…` | the head reviewed in round 1 | **`ALL 9 PASS at 31c5fbd`** first attempt; `test-pr-ready-audit: ok`; CI green on every job at the pushed head (`ci/ci-run-35046849547.json`) | `gates/w1-eight-iso-31c5fbd-head-3.log`, `gates/gate10-31c5fbd-head-3.log` |
| `16aea265…` | the repaired report and the corrected finding, after review round 1 | **four attempts**, with the box's one-minute load between 4.46 and 12.10 at their starts and ends (`gates/box-load-16aea26-*.log`; this cell said "6–10 from other sessions" until repair round 5, and the logs attribute the load to nothing): attempt 1 FAIL on the test leg alone (2671 passed, **2 failed**: the reaper-lease flake and `real_docker_returns_both_streams_of_a_container_separately`); attempt 2 FAIL (2672 passed, **1 failed**: the same Docker test); attempt 3 FAIL (2671 passed, **2 failed**: the reaper-lease flake and `real_docker_kill_on_an_already_exited_container_is_tolerated`); **attempt 4 `ALL 9 PASS at 16aea26`**. Both tests ran **3 of 3 green alone** between attempts 1 and 2 (`flakes/alone-16aea26.log`), and before attempt 4 I removed one exited `upstroke-f1-already-exited` container my own attempt 3 had left (`docker/leftover-containers-before-attempt4.log`). `test-pr-ready-audit: ok` at every attempt | `gates/w1-eight-iso-16aea26-head-4*.log`, `gates/eight-logs-16aea26-head-4*/`, `gates/gate10-16aea26-head-4*.log`, `flakes/alone-16aea26.log` |
| `2f388103…` | the head reviewed in round 2 | **`ALL 9 PASS at 2f38810`** first attempt; `test-pr-ready-audit: ok`; CI green on every job at the pushed head (`ci/ci-run-35051489433.json`) | `gates/w1-eight-iso-2f38810-head-5.log`, `gates/gate10-2f38810-head-5.log` |
| `71aede1b…` | the withdrawn partition, the certification of the 32 points, the regenerated ledger mapping and every recount, after review round 2 | **`ALL 9 PASS at 71aede1` first attempt**, at a one-minute load of 6.07 when it started and 6.79 after (`gates/box-load-71aede1-head-6.log`; the fifteen-minute figure was 9.03, and this cell said "9.3 from other sessions" until repair round 5); test leg 84s, 0 failed; `test-pr-ready-audit: ok`; `git status --porcelain` empty after | `gates/w1-eight-iso-71aede1-head-6.log`, `gates/eight-logs-71aede1-head-6/`, `gates/gate10-71aede1-head-6.log` |
| `e9074f0d…` | that head plus its own gate row | **two attempts**, with the one-minute load between 5.41 and 14.31 at their starts and ends (`gates/box-load-e9074f0-head-7*.log`; this cell said "9–11 from other sessions" until repair round 5): attempt 1 FAIL on the test leg alone (2672 passed, **1 failed**: `real_docker_kill_on_an_already_exited_container_is_tolerated`, the filed poll-count flake); **3 of 3 green alone at this head** between the attempts (`flakes/alone-e9074f0.log`); attempt 2 **`ALL 9 PASS at e9074f0`**. `test-pr-ready-audit: ok` at both | `gates/w1-eight-iso-e9074f0-head-7*.log`, `gates/eight-logs-e9074f0-head-7*/`, `gates/gate10-e9074f0-head-7*.log`, `flakes/alone-e9074f0.log` |
| `1bdd6d7c…` | the head reviewed in round 3: `e9074f0`'s two attempts and its control recorded | **`ALL 9 PASS at 1bdd6d7` first attempt**, at a one-minute load of 16.07 when it started (`gates/box-load-1bdd6d7-head-8.log`); test leg 90s, 0 failed; `test-pr-ready-audit: ok`; CI green on every job at the pushed head (`ci/ci-at-pushed-head-1bdd6d7.json`) | `gates/w1-eight-iso-1bdd6d7-head-8.log`, `gates/eight-logs-1bdd6d7-head-8/`, `gates/gate10-1bdd6d7-head-8.log` |
| `b95f03fe…` | the repair after review round 3 | **`ALL 9 PASS at b95f03f` first attempt**, at a one-minute load of 0.44 when it started (`gates/box-load-b95f03f-head-9.log`); test leg 69s, 2673 passed, 0 failed; `test-pr-ready-audit: ok`; `git status --porcelain` empty after | `gates/w1-eight-iso-b95f03f-head-9.log`, `gates/eight-logs-b95f03f-head-9/`, `gates/gate10-b95f03f-head-9.log` |
| `7cde80cb…` | the head reviewed in round 4: `b95f03f`'s row plus two figure corrections | **`ALL 9 PASS at 7cde80c` first attempt**, at a one-minute load of 1.48 when it started (`gates/box-load-7cde80c-head-10.log`); test leg 73s, 2673 passed, 0 failed; `test-pr-ready-audit: ok`; CI green on every job of the surviving run at the pushed head (`ci/ci-run-35064848788.json`, `upstroke-ci` success; `ci/ci-run-35064848944.json`, `upstroke-pr-policy` success). The push's own runs were cancelled by the body edit three seconds later and their `upstroke-ci` reads `failure` (`ci/ci-at-pushed-head-7cde80c.json`): the superseded-run trap, read by newest run per name | `gates/w1-eight-iso-7cde80c-head-10.log`, `gates/eight-logs-7cde80c-head-10/`, `gates/gate10-7cde80c-head-10.log` |
| `17980022…` | the head reviewed in round 5: the repair after review round 4 | **four attempts**: attempts 1 and 2 FAIL on the test leg alone (2672 passed, **1 failed** each: `real_docker_kill_on_an_already_exited_container_is_tolerated`, the filed poll-count flake), attempt 3 FAIL on the test leg alone (2672 passed, **1 failed**: the reaper-lease flake), attempt 4 **`ALL 9 PASS at 1798002`** (test leg 77s, 2673 passed, 0 failed), after a bounded wait for the one-minute load to fall below 4; the one-minute load was 0.97, 3.15, 5.43 and 3.91 when the four started (`gates/box-load-1798002-head-11*.log`). Controls at this head: the Docker test **6 of 6** and the reaper-lease test **3 of 3** green alone (`flakes/alone-1798002.log`); the exited container each red Docker attempt left was removed before the retry, and no other (`docker/leftover-containers-before-head11-retry.log`). `test-pr-ready-audit: ok` at all four; `git status --porcelain` empty after each. CI green on every job of the surviving runs at the pushed head (`ci/ci-run-35071116564.json`, all ten CI jobs and `upstroke-ci` success; `ci/ci-run-35071116570.json`, `upstroke-pr-policy` success); the push's own runs, 35071114930 and 35071114800, were superseded by the body edit that followed the push, and read `cancelled` — with `upstroke-ci` reading `failure`, the superseded-run trap (`ci/ci-at-pushed-head-1798002.json`) | `gates/w1-eight-iso-1798002-head-11*.log`, `gates/eight-logs-1798002-head-11*/`, `gates/gate10-1798002-head-11*.log`, `gates/box-load-1798002-head-11*.log`, `flakes/alone-1798002.log` |
| the head this branch pushes | the repair after review round 5, and this table's own last row | gated the same way — ten gates — before the push, every attempt kept with its box load measured | `gates/` with the label `head-12` |

## 12. The evidence directory

`~/tactus-artifacts/g5-evidence-a2fe09c/`, indexed by its `README.md`, with `MANIFEST.sha256` over every file.
The five required artifacts: this report (copied in as `report-<sha7>.md`); the bounded census artifact; the
effect-site inventory with the registry, the per-coordinate certification and the residue-class evidence
record; the projection equivalence outputs; and the six per-resource ledger tables with the packet mapping.
**At this head (repair round 5), new:** `repair-r5/` — every execution this round ran, all in a `git archive` copy of
the range and never in the worktree (`copy-provenance.log`): the replay-twice power of the Linux witness of
every row whose replay-twice obligation is established (`iii-power/`), the torn-registration probe over finalization's own `scrub_slots` and the resume's `reclaim_intents`
(`pr5-rd-002-probe/`), a projection mutation that reaches the digest comparison (`projection-mutation/`) and the four
barrier-refusal mutations (`barrier-refusals/`), each with its diff, log and the script that ran it (`scripts/`);
beside them the mutation attribution by function (`mutation-attribution/`), the witness-provenance check, the gap
search with helpers inlined, the #292 recount (`recount/`), the citation checker (`cite-check.py`), `figures.py` with
`figures.out` (every figure this round corrected, re-derived from saved files) and `searches.sh` with `searches.out`
(where each corrected phrase still appears); and `findings/reread-r5/`, the readings of all 347 finding rows, the
decisions taken from them, and the generator and its outputs as round 4 left them (`before-round-5/`), against which
`figures.py` finds exactly the 100 decided rows changed. **Changed:** `audit/point-coordinate-certification.md` (the replay-twice power table and the
limits at rows 8, 9, 14, 16 and 18, row 25's citations, nine spellings, eight weaker claim rows, the widened gap
search, the caveat's rows); `audit/point-coordinates-fired.py` and its output (one label, files rather than records);
`findings/assess-findings.py` with both outputs (a round-5 block of 100 decisions; regenerated at 128 bearing, 219
none); `census-export2/bounded-census-artifact.md` (the plan-shape bound, not reached); `residue/residue-class-evidence-record.md`
(the sampled-command caveat); `mutations/summary.md` (the restoration sentence); and `README.md` (the new rows, the
export record count, and the withdrawn files' label). **At repair round 4** (`17980022`):
`audit/point-coordinate-certification.md`, row 14 credited and the class-wide second search recorded in its Result;
`findings/assess-findings.py`, five bearings moved from `none` and `G3-O1`'s reason rewritten, with both outputs
regenerated (126 bearing, 221 none) — that count is the round-4 session's own record, since no copy of the generator
from before that round was kept (this section said "five decisions changed" until repair round 5); and `README.md`'s
`audit/` row. **At repair round 3** (`b95f03fe`), new or replaced: the corrected generator
`audit/point-coordinates-fired.py` and its output `audit/point-coordinates-fired.md` (round 2's pair kept under
`audit/superseded-round2/` with a note saying why); `audit/point-coordinate-certification.md`, corrected in its header,
its figures and row 25; `findings/assess-findings.py` with `findings/every-finding-assessed.tsv` and
`findings/every-finding-assessed.md`; one added paragraph in `residue/residue-class-evidence-record.md`; and the
`README.md` rows for `audit/` and `findings/`. **From `71aede1b`**, unchanged since: the readings
`audit/points-event.md` and `audit/points-spawn.md`, the function parser `audit/parse_fns2.py` with `audit/rustlex.py`,
the regenerated `ledger-export2/packet-at-run-end-mapping.log` with `ledger-export2/regen-packet-mapping.py`, and
`probes/projection-equivalence-outputs.md`. The withdrawn `audit/per-coordinate-audit.md` and `audit/rows-classed.json`
are kept byte for byte; the README labels them withdrawn.

## 13. The verdict against the pass rule, clause by clause

The rule, verbatim from the regenerated definition:

> pass iff bounded census family complete (the prefix at its stated ceiling with truncation reported, every
> seeded per-dimension census closed, every bound reached by equality over the union) with totality asserted
> over the union and every plan_transition arm covered, site bijection complete with executed evidence for
> every Topology/Shared site, order, and parent-executed point (incl. OpenLog SyncPrefix and the stable-prefix
> barrier refusal cases), the two-crash proof green, residue-class evidence complete and labeled, all rows
> green, projections equal, per-resource ledgers satisfy their outcome equations incl. derived outcome,
> closure, and finalization, questions answered, no open critical/high finding; unbounded closure,
> effect-phase census, and parent-observed Git-internal execution claims are not made

**Nine** positive clauses and **three** prohibitions.

| # | Clause | Grade | How |
|---|---|---|---|
| 1 | bounded census family complete, totality over the union, every arm covered | **not established** | §4: the numeric bounds, the prefix's reported truncation, the seeded member's closure, totality over every member and the 24 arms are established. The bounds line names three plan shapes and the family's union explores one; the chain and the join are explored outside the family — both truncated in `every_plan_shape_is_explored`, and the join also in two restricted censuses that close — so no coverage assertion runs over them. **The change the failure points to: a seeded census per plan shape admitted to `family()`** — a tree change this gate does not make, and whose sufficiency this gate has not executed. Three filed findings make the census's outcome-side oracle less independent than it reads and name what such a repair should carry (§9.2's clause-1 row); they do not change the grade |
| 2 | site bijection complete with executed evidence for every site, order and parent-executed point | **asserted** | §5.2–§5.3. **Established, executed, on both hosts:** `check_bijection` reports **0 failures** over the whole inventory, and the merge check holds every claim the host requires to its named test's own observation record (**163** claims on Linux, **164** on the guest, of the table's **168**: 159 shared, four Unix-only and five Windows-only, as `coverage/tests.rs:238-246` asserts; §5.2 said 172 until repair round 5). **Fired at all 32 parent-executed points in the two-host union** — 27 in the Linux export and 28 in the guest's, 23 in both, under the merge check's own adapter-test exclusion (§5.3): the harness records a `Point` observation only when the armed fault fired, so a filtered record establishes that a test outside the adapter modules consumed the injection, and the row's named test and selecting argument establish which production operation did. **All three obligations certified at 29 of those 32**, each row citing the argument that selects the point, the recovery and the replay-twice assertion by `file:line`, with two limits stated: at rows 8, 9 and 14 the replay-twice assertion has no power over the coordinate's own line, which changes no `state()` (executed, §5.3), and at rows 14 and 18 the fired fault rests on the log suite's arming and its child's observed death rather than on an export record; **the other three (rows 1, 2 and 23) carry a stated gap** and are not certified, after a class-wide search that repair round 5 widened to witnesses that export nothing. **Undecided by this gate:** the 136 before/after coordinates, where the export records reachability only. It is *asserted*, not failed: **no claim is made that any coordinate lacks a witness** — review round 2 named several that have them and executed two. **What would let it hold: a per-coordinate certification of the 136 on the same standard as the 32 — the argument followed to the tabled residue, every row checkable.** §9.2's clause-2 row names 17 findings that condition the executed layer without moving this grade — among them `RefSite::CreateIntegration`'s adjacency, backwards in value, against which the bijection passed, and the `IdUnread` witnesses' caveat at certification row 22 |
| 3 | the two-crash proof green | **established** | §6: both variants green with the bytes on disk before and after each loss; two mutations kill them. The loss is the packet's simulated one, the durability ledger's `lose_unsynced_writes`; that the physical fsync happens is unobservable on this machine (`PR5-C-FSYNC-UNOBSERVABLE`, §9.2) |
| 4 | residue-class evidence complete and labeled | **established for the packet's nine declared classes, under the conditions sampled, with one residue outside them named** | §5.4; the report-site leftover has no class and is stated, not counted. The conditions: healthy repositories (the classifier's inspection primitives fold Git failures into absence, §9.2); on the guest the census sampler's kills end the launcher rather than git (`PR249-KILL-SAMPLER-WINDOWS-WRAPPER`), so its four sites' guest runs are not residue evidence there, while the Linux histograms carry every class; and that the census sampler kills the command the production funnel runs, argument for argument at run time, is not established — its fixed arguments are the funnel's by construction, its run-time arguments are transcribed, and the only census relating them is a tripwire that says it does not establish this (`PR136-PASS3-P1-CENSUS-EXTENT-FORGED-BY-A-DECOY`, `PR136-P3-STRUCTURAL-ARGV-COMPARISON-NOT-AVAILABLE-HERE`; §5.4 and `residue/residue-class-evidence-record.md` carry the same caveat); and a torn add — `gitdir` absent or empty — is read as no residue at all, a shape no sampler here observed (`PR5-RD-003`, §9.2) |
| 5 | all rows green | **established, under the conditions §9.2 names** | §7: 324 of 324 on both hosts; the suite green at the range on both hosts; CI green on all three platforms. Every red this run saw is one of §9.4's three fingerprints, each controlled. The conditions were held unevenly: the export runs and the guest suite ran on a fresh private temporary directory, and the gate legs under the box's shared `/tmp`, where the filed pre-clean and scratch-name hazards are not excluded (§9.2's clause-5 row; this cell named no condition until repair round 5) |
| 6 | projections equal | **established** | §6: 15 asserted comparisons, printed with the bytes they read. A mutation that keeps the live snapshot sequence intact and makes the live fold stale is killed at the digest comparison itself (`recover/tests.rs:21579`, `cargo exit=101`; `repair-r5/projection-mutation/`, repair round 5); the three saved `projection-*` recipes are killed too, but earlier, at the snapshot-sequence assertion (`:21553`), before any digest is compared — this cell cited only those until round 5 |
| 7 | per-resource ledgers satisfy their outcome equations incl. derived outcome, closure and finalization | **established for the six outcome tables and the 50 committed finalization cells** | §7, §3.5: every equation satisfied at all five outcomes plus the resumed table; the derived outcome equal to the recorded one; finalization idempotent across the third resume by a committed assertion; the reclaim's durability bounded on both sides of its barrier by two committed tests. The *universal* form of "between every cleanup effect" belongs to group 5 and is graded asserted there. **Not established in four filed shapes: convergence without an operator.** A torn worktree registration with a zero-length `commondir` that finalization's slot scrub does not reach first makes `scrub_slots` fail on every resume until an operator removes it — executed in repair round 5 on the tree's own `scrub_slots` and `reclaim_intents`, three of three passes refused (`PR5-RD-002`, P1, §9.3; the verify lens of review round 5 found it missing from this row); a torn registration whose `gitdir` is absent, empty or partial makes the plain removal funnel the resume's reclaims call refuse for every slot, as a committed test asserts, and a partial one refuses finalization's removal too (`PR5-RD-003`, P2, with `RESIDUE-UNBINDABLE-TASK-REGISTRATION-HAS-NO-DESIGN-SENTENCE`; this head's re-read found it, §9.2); a stale `packed-refs.lock` left by a killed ref deletion makes finalization's `delete_ref_expected_old` refuse on every resume until an operator removes it (no committed test constructs that killed deletion); and a packed run ref with a stale lock is refused reclamation by design — a shape two committed tests construct, whose refusal and whose convergence after the lock's removal they witness (`PR8-CRASH-002-PACKED-REFS-LOCK`, `-PACKED-RUN-REF`, §9.2). All four refusals are resumable and lose nothing, so the equations are not falsified, but "converges without an operator" holds for the measured runs only. Four more findings condition the equations without falsifying them (§9.2's clause-7 row in addition): `check_run_finished` checks the outcome and the halt attribution but not the counts, it accepts a zero budget ceiling no producer makes, and the R27 rows read the object store through an `fsck` whose exit status is never read |
| 8 | questions answered | **asserted** | §10: Q1–Q4 and Q6 are answered with evidence for G5's portion. **Q5 is asserted**: its wrapper-classification half is contradicted by a filed, unfixed, demonstrated bypass (`PR7-WRAPPERS-EMPTY-DOMAIN`, with `PR5-R1-CFG-TEST-SHRINKS-THE-DOMAIN`, the same mechanism's open third part), and its executed-evidence half rests on clause 2's audit |
| 9 | no open critical/high finding | **established on the ledger's own terms, weakened by deferrals** | §9.2–§9.3: no finding carries `disposition: open`; the P1 on G5's own subject is closed by #289. Of the nine deferred P1s at the range, **two are on mechanisms this gate certifies** (`PR7-WRAPPERS-EMPTY-DOMAIN`, `PR5-RD-002`) and **three touch its conditions or a boundary its clauses do not certify** (`PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS`, `CLASS-INTERMITTENT-SUBPROCESS-KILL-SETTLE-RESIDUE-FAILURES`, `PR64-CLEANUP-003-SCRATCH-PRECLEAN`), all five recorded as weakening the pass rather than treated as closed; the other four are instruments and the legacy path |
| 10 | unbounded closure claims are not made | **not made** | §4 |
| 11 | effect-phase census claims are not made | **not made** | §4, §5 |
| 12 | parent-observed Git-internal execution claims are not made | **not made** | §5.4 |

### The verdict

**G5 does NOT pass on this range.** **Clause 1 is not established**; clauses 2 and 8 are **asserted**; the
other six positive clauses are established, and the three prohibited claims are not made. The rule is a
conjunction, so one unestablished clause decides it. **PR11 and PR12 stay blocked by G5.**

**The verdict is unchanged from the previous head, and so is its ground, which last changed at `71aede1b`.** At
`2f388103` this report failed G5 on clauses 1 *and* 2, the second on the count `F 107 / R 20 / O 41`. That count is
withdrawn (§5.3), so clause 2 no longer carries the failure; clause 1 does, and clause 1's failure is established by
reading `family()` at this range, not by a partition.

What stands in the way, and what it takes:

1. **Clause 1 — not established.** The census family's union explores one of the three plan shapes the amended
   bounds line names. The change this points to is a seeded census per shape, admitted to `family()`, with the
   coverage assertions run over the union; that it would establish the clause is a hypothesis this gate has not
   executed. The chain and the join are already explored outside the family — the chain only truncated, the join
   truncated and in two closing restricted censuses. This is a tree change, and a gate does not make one.
2. **Clause 2 — asserted.** What is missing is a *certification this gate can defend*: a per-coordinate reading
   of the 136 before/after coordinates that follows each candidate witness's
   arming or plant to the coordinate's exact tabled residue, performs the tabled action, asserts replay-twice,
   and prints all four with a `file:line` a reader can check. Two attempts at it have now been withdrawn.
   **Whether it needs a tree change is not known.** Review round 2 named committed, passing witnesses for
   coordinates the withdrawn audit called uncovered and executed two of them, which refutes the earlier claim that
   a tree change was *necessarily* required; it does not establish that every undecided coordinate already has
   sufficient evidence, and the heads from `71aede1b` to `1bdd6d7c` said "it does not need a tree change", claiming
   that certainty without it. The missing certification covers the 136 phases and the three point gaps (rows 1, 2 and 23 of
   `audit/point-coordinate-certification.md`) alike.
3. **Clause 8 — asserted.** Q5 cannot be answered in the affirmative while `PR7-WRAPPERS-EMPTY-DOMAIN` stands:
   the classification whose completeness it asks about has a demonstrated bypass at this head.

Three things the failure does **not** say:

- **Where a named execution establishes it, the tree's crash/resume behaviour at width 1 is as the packet
  states it.** The 50 finalization cells are real kills resumed in fresh processes; the two-crash proof, the
  ledgers, the projections, the residue classes and the barrier's refusals are established here.
- **The four fix pull requests do what §3.5 judged them on**, each by mutation at this range, including the two
  shapes review round 1 found unexecuted; for #292, whether every coordinate gained an armed fault is what §3.5
  declines to count (§5.3 withdrew both partitions).
- **The evidence is Linux and the Windows guest.** Nothing was measured on macOS beyond reading CI's runs at
  the sha.
