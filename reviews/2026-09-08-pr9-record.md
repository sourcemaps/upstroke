# PR9 — repair execution, retries, lineages: the working record

The record of the ninth slice of upstroke v0.2, kept on the branch so that a successor session
inherits what was decided and why. Written before the work and updated as each piece lands, so
that at any commit on this branch the record is true. It is **not** a design document:
`DESIGN.md` and the packet stay the authority, and a sentence here that disagrees with either is a
defect in this file.

**Branch:** `feature/pr9-repair-execution`, cut from master at `9cfaf0c8` — the exact tree G3
certified on 2026-09-08 (`reviews/2026-09-08-gate-G3.md` on branch `gate/g3`).

**Where this file lives, and why.** The owner ruled on 2026-09-08 (PR #248) that PR8's three
root-level working records do not belong on `master`, and the brief for this slice forbids any new
file at the repository root. `docs/internals/` cannot hold it either: `validate-internals-notes.sh`
(N2) requires every file there to mirror a Rust module. So the record sits beside the gate report's
convention in `reviews/`, dated. Where it belongs long-term is the owner's call, as the G3 report
says of itself.

## 0. Status

| Step | State |
|---|---|
| A read the packet, errata, G3 report, DESIGN §4/§21/§26, MAINTAINING, standards | **done** |
| B verify and extend the abandoned attempt's refusal inventory against the code | **done** — §4 |
| C record the readings taken on every packet ambiguity | **done** — §3 |
| D fold layer: errata E2 override binding, lineage terminal failure, the override-aware reader | **done** — `c7223367` |
| E workspace manager: materialization observed, unresolved-conflict read, pick state cleared, held lock forms read | **done** — `d23d6bdb`, `9eff29d5`, `0cebabec` (§11) |
| F engine: repair dispatch, continuation, retry, answer ingestion, recovery step (g) | **done** — `d23d6bdb` |
| G tests: the packet's gating and proof tests, the G4 adversarial list | **done** — §10 names each test; two defects found by the tests and fixed (§9) |
| H notes under `docs/internals/` brought back into agreement with the code | **done** — `848a36b3` |
| I ten gates green on this box; Windows guest for anything platform-shaped | **done** on this box — `ALL 9 PASS at 8362f9f` plus `test-pr-ready-audit: ok` (Linux, 2399 passed) at the first head, and again at each round's head (the body records the latest); the platform-shaped bits were CI's Windows leg to confirm until the second round's sampler change, which was run by hand on the persistent guest at `77c0111f` (§13 finding 3), the third round's manifest and case-alias changes, run there at `a6f3898f` (§14), the fourth round's two Windows-established defects, run there at `f97ea340` and `7cda5dde` (§15), and the fifth round's manifest-spelling change with three guest-only mutations, run there at `d394d52c` (§16) |
| J draft pull request with a validated body | **done** — draft #249, body validated by `validate-pr-body.sh` before and after publication; not marked ready, not enqueued, not reviewed |
| K first repair round: three `gpt-6-astra` reviews at `48017a43` found eight things | **done** — `a23ce25c` (findings 1–4, code), `7f7aa8fc` (findings 5–7, tests), then this record; §12 records each finding, its witness reproduced on the unrepaired tree, the repair, and what now dies |
| L second repair round: three scoped reviews at `3bce2c6a` found four must-fix things and one P3; the owner ruled the P1's design answer (Option 4) | **done** — `19a464bb` (code, tests, notes, `design/26` §26.4), `56ea88c9` (this record's §13), `77c0111f` (the kill sampler after CI at `56ea88c9` failed it on the winguest and macOS legs: its child the real git on Windows, its budget a warm pick), then this record again; §13 records each finding, its witness re-run on the unrepaired tree (except the immediate-kill mutation, which finding 3 records as not re-run; the fifth round's record review found this row saying "each"), the repair, and what now holds; the ten gates on this box at the head the body records |
| M third repair round: four scoped reviews at `698777b0` — adequacy **PASS**, regression, record and manifest-contract **CHANGES_REQUIRED** — found two capture defects, a false verification claim, four prose corrections and seven manifest-contract findings | **done** — `a6f3898f` (code, tests, notes, `design/26` §26.4, the finding files), then this record's §14; every witness re-run on the unrepaired tree and again on the repaired one; the ten gates on this box at the head the body records; the platform-shaped tests run on the persistent Windows guest |
| N fourth repair round: four scoped reviews at `b2946956` — regression, manifest-contract, adequacy and record, each **CHANGES_REQUIRED** — found five capture defects with executed witnesses, two of them native to Windows, two prose claims the code contradicts, an overstated closure claim, a claimed compile-time guard, and a `design/26` sentence this slice made false | **done** — `f97ea340` (code, tests), `7cda5dde` (the argument-limit witness made to run on Windows), `5302cfc6` (notes, `design/26` §26.4, the finding file), then this record's §15; the reviewers' seven witness tests run unchanged on the repaired tree, seven mutations killed in nine runs, the two Windows defects verified on the persistent guest; the ten gates on this box at the head the body records |
| O fifth repair round: four scoped reviews at `6448262e` — adequacy, manifest-contract, regression and record, each **CHANGES_REQUIRED** — found the unread-manifest defect in its third round, two capture aborts (one native to Windows), three record claims still false and a stale planner note | **done** — `d394d52c` (code, tests), `a664c6f5` (notes, `design/26` §26.4, the worker's text, two `CHERRY_PICK_HEAD` corrections), then this record's §16; the reviewers' three witnesses run on the repaired tree, six mutations killed in twelve runs, three of them on the persistent guest; the ten gates on this box at the head the body records |
| P sixth repair round: four scoped reviews at `697c1ac8` — regression and adequacy **PASS**, manifest-contract and record **CHANGES_REQUIRED** — found one contract sentence and six record sentences false, and no defect | **done** — `d05462b1` (the dispatch test note, `design/26` §26.4, three doc comments, the run note and one assertion's text), then this record's §17 and the filed successor `PR249-REFUSED-MANIFEST-HANDOFF`; no code, no test, no mutation, no witness, no sweep; `src/topology/**` untouched; the ten gates on this box at the head the body records |

## 1. What PR9 is, from the packet

`decisions.pr_sequence[10]`, `.id == "PR9"`, title *repair execution, retries, lineages*,
`production_effect: none`, `cumulative_gate_after: G4`.

Scope, verbatim: repair dispatch (`InheritedLineage`) from protected source candidates,
materialization plan+observed (`Object.RepairMaterialize`; objects behind the repair index, R9;
interrupted materialization recreated after `Worktree.Verify` fails), `LineageHeld` dispositions
incl. same-generation retry without re-materialization, one-off binding activation, over-limit
activation, lineage terminal failure, lineage candidates through the queue with `satisfies`,
two-lineage order at runtime, `generation_closed` for repair generations; T-REPAIR-DISPATCH,
T-ANSWER, T-RETAINED, T-RETRY (repair).

Non-goals: the empty no-worker path; the production writer. Invariants introduced: INV-06 (repair
generations incl. retry), INV-11 (execution), INV-16 (complete lifecycle incl. terminal failure),
INV-19 (overrides). Preserved: every PR7/PR8 invariant and R11's protection of source candidates.

## 2. What the tree already had at `9cfaf0c8`, measured by reading

Most of the vocabulary and most of the primitives landed in PR3–PR8; what was missing was the
production path through them, plus the three refusals PR8 stood where that path goes.

Present: `LeaseGrant::InheritedLineage`, `TaskDispatched.source_candidate`, `Materialization`,
`AttemptStarted4.materialization_observed`, `LeaseDisposition::LineageHeld`, `BindingOverride`,
`SpawnAdmission::{HumanRequired, HumanBinding}`; `engine::topology::dispatch`'s
`DispatchKind::Repair`, `refuse_absent_source`, `materialize_repair`, `verify_or_recreate`,
`resume_open_no_attempt`; `workspace_manager::repair_materialize` (the funnelled
`Object.RepairMaterialize` site with its `ObjectInternal` residue class); the fold's
`check_dispatched` repair arms, `check_attempt_started`'s materialization-parity arm,
`check_question_answered` with override validation, `apply_answer` (override state, `fail_lineage`
on a decline), `satisfies_closure`, the queue's `BehindOlderLineage` ordering,
`repair::merge_rejected` with `repair_ladder` and `admission_for`; `settle::retry` already carries
a `materialization` field; `close_generation` already takes its disposition from the generation's
lease kind.

## 3. Readings taken on packet ambiguities

Each is a place where the packet, the errata and the shipped vocabulary together do not determine
the answer. The reading, the alternative, and why. Where the abandoned attempt
(`~/tactus-artifacts/pr9-opus-wip/pr9-plan-opus.md`) reached the same reading, that is noted, but
each was re-derived here from the packet and the code rather than adopted.

### R1 — tier and pin of a one-off binding (errata E2)

E2 amends `decisions.task_registry.binding_override`: the validation binds *"the complete resulting
rung binding: agent, model and effort from the override payload, and tier and pinned from the
frozen option selected by option_index"*. The shipped frozen option is a bare agent name
(`SpawnAdmission::HumanBinding { options: Vec<String> }`, filled from `allowed_agents`), so tier
and pin cannot come from the option literally.

**Reading.** `tier` is the repair ladder's frozen `floor` and `pinned` is `true`. Both are
derivable from frozen registry state alone, which replay stability requires: the fold re-derives
them on every replay of every log. `repair_ladder` sets `floor = max(root floor, mid)` on exactly
the empty-intersection path that raises a `HumanBinding`, so the floor is the tier the options were
offered *at*; "the frozen option" is read as "the frozen option set", whose tier is that floor.
`pinned: true` because a human naming one agent for one task is the definition of a pin, and it is
the safer value: a build that forgets to mark it is refused rather than admitted.

**Alternatives rejected.** Leaving tier and pin unbound (the shipped half-rule, which E2 exists to
close). Deriving the tier from the model catalogue, which is a compile-time constant and would make
the replay of an old log depend on the binary. Widening the frozen option to a rung — a Class C
change to `SpawnAdmission` (§6).

The abandoned attempt reached the same reading.

### R2 — where a one-off binding's `model` and `effort` come from

`BindingOverride` carries `{key, question, option_index, agent, model, effort}`; the answer source
yields an option *text*, an agent name. Nothing in the packet says who chooses the model.

**Reading.** The model is the named agent's catalogued model at the lowest tier at or above the
ladder's floor (`crate::catalog::CATALOG`, first entry at that tier in catalogue order), and the
effort is the entry's frozen effort policy at the floor. The derivation runs **once, live, at
ingest**, and what replay reads is the recorded payload; the fold never re-derives the model, so a
compile-time catalogue is safe here and would not be inside the fold. An agent with no catalogued
model at or above the floor refuses the activation before any append, naming the agent and the
floor.

**Alternatives rejected.** `run_started.chains` (frozen run identity, but the empty intersection
means the root's chain has nothing at or above the floor, and another task's chain is not the
right authority for this task). A new seam on `AttemptPlans` for the assembler to choose — a
reasonable future shape when PR12 wires production config, recorded here as a follow-up, not
built: the production `FrozenPlans` has no config today either.

### R3 — the ceiling applies to a repair dispatch

`select` returned `Step::RepairDispatch` *before* the ceiling check, because the checkpoint refused
the step anyway. `decisions.sequential_substrate.loop` states the branch as *"else if a ready task
exists: ceiling check, provisional dispatch reservation, dispatch"*, and a repair is a ready task.
**Reading:** a repair dispatch checks the ceiling exactly as an ordinary dispatch does, and a breach
appends `budget_exceeded` naming the repair's key. Nothing exempts repairs, and `repairs.limits`
bounds them by count, not cost.

### R4 — which settlement folds a lineage (`decisions.repairs.terminal_failure`)

*"a lineage member Failed folds every AwaitingRepair member of that lineage to
Failed{lineage_failed} and releases the lineage lease"*. **Reading:** the fold folds the lineage
when a task **that has a `lineage` in the registry** settles `attempt_finished{Closed{Failed}}` —
a repair, never an ordinary task. A lineage root is an ordinary task and is `AwaitingRepair` with no
open generation for as long as its lineage lives, so it cannot be the settling task; testing
membership rather than `lineage_root(key)` keeps an ordinary task's terminal failure exactly as PR7
left it. `Failed{lineage_failed}` is a derived-state annotation, not a wire field: `TaskState::Failed`
carries no payload, and adding one would be Class C.

### R5 — classifying the materialization (measured, not assumed)

`attempt_started.materialization_observed` is `Clean | Conflict | Empty | Retained`, and nothing
says how a `git cherry-pick --no-commit` maps onto the first three. Measured on this box (git 2.43.0,
`x86_64-unknown-linux-gnu`) on purpose-built repositories, 2026-09-08:

| case | exit | `ls-files --unmerged` | `diff --cached --quiet` | git-dir residue left |
|---|---|---|---|---|
| the change applies | 0 | empty | 1 (a staged change) | `AUTO_MERGE`, `MERGE_MSG`, `ORIG_HEAD` |
| the change is already present | 0 | empty | 0 (nothing staged) | `AUTO_MERGE`, `COMMIT_EDITMSG`, `MERGE_MSG`, `ORIG_HEAD` |
| the change conflicts | 1 | three stage entries | — | `AUTO_MERGE`, `COMMIT_EDITMSG`, `MERGE_MSG`, `ORIG_HEAD` |
| the commit is not an object | 128 | — | — | — |

Two facts from the table bind the code. First, **`CHERRY_PICK_HEAD` is not left behind by
`--no-commit` in any of the three cases** — `git rev-parse --verify CHERRY_PICK_HEAD` fails in all
three worktrees — which contradicts the pre-existing doc comment on `repair_materialize` ("`--no-commit`
deliberately leaves `CHERRY_PICK_HEAD` behind"). `MERGE_MSG` is left by the bare command, and it is
one of the names `administrative_residue_at` reads; the funnel therefore removes `MERGE_MSG` and
`AUTO_MERGE` once the pick has ended, in all three outcomes (PR9-F2, §9), so a *completed*
materialization is quiescent at its base and `Worktree.Verify` reuses it (`Reuse::Verified`). A
pick killed before that removal leaves whatever its kill point left — nothing, `index.lock`, a
merged index with no state file, `MERGE_MSG.lock`, or `MERGE_MSG` (the five states §8 lists) — and
the verify fails on the two lock forms and on `MERGE_MSG`; the merged index with no state file (the
documented K2 boundary: the index published and unlocked, the process dead before
`MERGE_MSG.lock`) verifies `Reuse::Verified` exactly as a completed pick does, which the
deterministic late-kill test asserts for a clean and a conflicting materialization alike, and it
converges because the continuation restores the base's tree before it picks (R6). (The first
version of this paragraph said a completed materialization fails the verify "exactly as an
interrupted one does" — true of the bare command in the table, false of the funnel since PR9-F2;
the second said a pick killed before the removal "leaves `MERGE_MSG` or its held `.lock` and fails
the verify", false of the K2 state; PR #249's second- and third-round record reviews caught them,
§13 finding 4 and §14 finding 3.) Second, exit 0 with an unchanged index is the already-present
case, not an error.

**Reading.** Exit 0 with a staged change is `Clean`; exit 0 with none is `Empty`; a non-zero exit
**with unmerged index entries** is `Conflict` and is not an error — a conflicted cherry-pick is the
normal shape of repairing a `Conflict` rejection, and leaving the conflict in the worktree is what
gives the agent something to resolve. Any other non-zero exit stays the Git error it is today.
`Retained` is recorded by a same-generation retry, which re-materializes nothing.

### R6 — when a repair generation's worktree is re-materialized

`T-REPAIR-DISPATCH.resume_action`: Worktree.Verify at the recorded base; on failure remove with
force and recreate; re-run the cherry-pick deterministically; the observed kind is recorded when
`attempt_started` is appended. `recovery_order` (g): "recreate OpenNoAttempt worktrees at their
bases (through Worktree.Verify or forced recreate)". Neither says whether step (g) or the loop's
continuation performs the cherry-pick.

**Reading.** Recovery step (g) recreates the worktree **at its base only** — verify, or forced
recreate — for ordinary and repair generations alike. The materialization is reproduced by the
loop's continuation of the generation, immediately before the `attempt_started` that records what
it observed. Two reasons. The observation recorded must be the one made by the process that
appends it; an interrupted process's observation is never carried. And one materialization per
resume: a step (g) that materialized would hand the loop a worktree the continuation materializes
again — two cherry-picks per resume for one observation. What the code does, in sequence, when a
coordinator dies after a completed materialization and before `attempt_started`: (g) verifies the
existing generation and gets `Reuse::Verified`, because the funnel cleared `MERGE_MSG` and
`AUTO_MERGE` when the pick ended (R5 as corrected); the already-materialized index is retained as
it is; the continuation restores the base's tree and picks once. So every resume of a repair
generation performs exactly one cherry-pick, on a worktree that verified at its base or was just
recreated there, and the three prefixes a kill can leave (before the pick, inside it, after it)
converge on the same tree — because the funnel restores the base's tree before it picks (§12,
finding 3), not because a pick onto an index that already holds it is a no-op: measured, it is not.
`a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces` asserts
`Reuse::Verified` on both of its resumes.

**Alternative rejected.** Materializing in step (g) as well (the abandoned attempt's shape).
Correct and deterministic, but it performs the effect twice per resume. (The first version of this
paragraph said the alternative would report a healthy worktree as `Recreated { Residue(MergeMsg) }`;
that premise died with PR9-F2's clearing of the state files — §13, finding 4.)

### R7 — which candidate a repair is materialized from, and where that is read

`repairs.dispatch`: "task_dispatched(repair) records base and source candidate; ... git cherry-pick
--no-commit <candidate> from the protected candidates ref". `merge_rejected`: "the rejected
candidate's candidates ref remains as the protected repair source". DESIGN §26.4 says of a
semantic rejection that the engine "materializes the clean proposal against that current head".

**Reading.** The source is always the **rejected candidate** — the `candidate` the `merge_rejected`
that registered the repair names — because that is the object R11 protects: the proposal's pin is
pruned at the rejection terminal and the staging worktree reclaimed, so the proposal is Git's, not a
protected source. Cherry-picking the candidate onto the current head reproduces the proposal for a
code rejection and the conflict for a textual one, which is what §26.4 describes in both arms.

For a **fresh** dispatch the source is read from the `merge_rejected` event whose `repair.key` is
the task (the log is the durable pairing). For a **continuation** of an open generation — live, or
after a resume — the source is the `task_dispatched.source_candidate` of that generation, read from
the log. Both reads are Class A: `integrate::authorized_head` and `IntegrationRequest::from_log`
already read the log for exactly this kind of fact, and it avoids retaining a second copy in the
frozen fold (the abandoned attempt's Class B row 1, not taken here).

### R8 — the materialization kind on a same-generation retry

ST-15: "a repair generation retry records `LineageHeld` and does not re-materialize". **Reading:**
the retry records `Materialization::Retained` and runs no `Object.RepairMaterialize`; the fold keeps
checking only presence parity (a lineage member records a kind, an ordinary task records none) and
does not gain a refusal on the kind itself. The kind is the engine's observation, like `pool`, and
the packet's refusal list for this slice names no refusal for it.

### R9 — what "unresolved index entries fail capture before gates" can mean

`repairs.dispatch` and DESIGN §26.4: "the worker may edit but never commit; the engine refuses a
result with unresolved index entries and then runs the ordinary gates and reviews." Under
*agents edit files; the engine owns git*, an agent cannot resolve an index entry: only the engine's
own staging (`git add -A` at capture) does, and staging resolves every unmerged path
unconditionally — an untouched conflicted file is staged **with its markers inside**. Read
literally after staging the rule is vacuous; read literally before staging it fails every conflict
repair, because no agent may run `git add`.

**Reading (revised three times: §12 finding 2, §13 finding 1, §14 findings 1 and 2).** The rule is
read where the design states it, in the index: before the capture stages anything, the engine
lists the index's unmerged paths, and every one of them is conflicted — whatever the working-tree
file looks like, so a `conflict-marker-size=8` or `-merge` conflict is read like any other. What
resolves one is a declaration, not a git command: the worker resolves the file with its file tools
and records the path in the resolution manifest, `.upstroke-resolved` at the root of its worktree,
as `resolved <path>` (the working-tree content, or its absence, is the resolution) or
`deleted <path>`; at capture the engine stages what the manifest declares
(`git add -- :(literal)<path>` or `git rm --quiet --force -- :(literal)<path>`, inside
`Object.CandidateStage`, before the `add -A`, which excludes the worker's manifest — an untracked
regular file of that name the ignore rules do not already keep out — and nothing else) and refuses
the capture if any unmerged path is undeclared, the manifest does not parse, a path is declared
both ways, or a path is declared in another case (beside the index's spelling with the other
keyword, or alone) — staging nothing, returning the base's tree, and failing the attempt as the
worker's failure (`AgentError`, naming the entries) before any gate or review runs. §4's "the
engine owns git" holds literally: the worker runs no git command at all, and `design/20`'s "gates
are the only commands they may run" is untouched. A path that does not decode is reported as one
unresolved entry naming the reason. The manifest is read from the durable worktree at capture time
like the diff; it is not an event field, so no wire vocabulary changes (Class C: none).

Two boundaries the third round drew (§14), and one the fourth corrected (§15). The manifest
governs the unmerged entries **and** the entries a previous capture of the same generation
resolved that the index still holds — Git's resolve-undo record, which staging over unmerged
stages writes — so a retained retry's manifest can revise a resolution, the `deleted` a worker
whose tools cannot delete corrects itself to; a resolved entry the retry's manifest does not name
is left to the ordinary `add -A`, which stages its edits or its deletion like any path's (this
sentence once said the entry "stays as it was", which no capture guarantees and the suite
contradicts — §15 finding F); and when the index holds nothing the manifest governs it is not
read at all. The manifest does not outlive the capture that finds it: the capture that acts on it
removes it, and one that finds nothing for it to govern removes it unread, so a declaration is
applied once, by the capture of the attempt that wrote it, and a settled deletion is not reapplied
to a file the worker later recreates (§15 finding A; §16 finding 1, which found the unread case
after the fourth round had covered the acted-on one); a refused manifest, or one whose capture does
not reach the removal, is left standing, and the driver never captures that worktree again — a
refusal is not resumable, a capture error interrupts the attempt, and either closes the generation,
so the next attempt is a fresh generation and worktree (§17 item 2; this sentence said "the next
capture reads it" until the sixth round). And the name is reserved for the worker's file only, under
the spelling the checkout lists it by — a case variant, on a checkout that folds case (§16 finding
3): a repository that tracks
`.upstroke-resolved` — as spelt, or in another case, which a checkout that folds case reads as
the same file (§15 finding D) — or holds a directory there, gets its ordinary captures unchanged
— that path staged like any other — and its conflict repairs refused before anything is staged,
naming what holds the name, because the repository's bytes are not declarations; a worker's file
standing where a tracked directory was is the worker's, and what the index still held under the
name is staged first, deletions included (§15 finding C).

**The readings it replaces, and why.** The first reading inferred from "the engine owns git" that a
worker cannot resolve an index entry, and so read the working-tree files for `<<<<<<< ` and
`>>>>>>> ` markers instead, calling a file without them resolved and a missing file resolved by
deletion. PR #249's conformance review materialized a conflict under `conflict-marker-size=8` and
another under `-merge`: the scan read both as resolved, the capture staged everything, and
unresolved work reached the gates. The second reading (§12 finding 2) kept the index as the record
and told the worker to stage its resolution with `git add`/`git rm`, "exactly Git's own hint" —
and no supported worker can: PR #249's regression review assembled the production Claude Code
permissions (`Read, Glob, Grep, Edit, Write, NotebookEdit, Bash(<gate>)` under `dontAsk`) and
Copilot's (`write`, `shell(<gate>)`), and the owner's design-staging session measured that Codex's
`workspace-write` sandbox keeps `.git` read-only and that its one switch admitting `git add` admits
`git commit` too. A binary keep-ours resolution is, in the file bytes, identical to abandonment, so
no content read can replace the worker's signal either; the declaration is the signal, and the
engine — which owns staging — acts on it. Option 4 of
`~/tactus-artifacts/DESIGN-STAGING-DECISION.md`, ruled by the owner; §13 finding 1.

### R10 — the answer-ingestion branch, and why it needs a non-blocking read

`loop`: "after recovery: ingest answers (never after budget_exceeded or a halting settlement in
this epoch) -> if an eligible integration exists ...". PR8 assigned this branch to PR9 by name
(`LoopBranch::IngestAnswers => Disposition::NotThisSlice { slice: "PR9", .. }`) and ingested only
verification-park answers, at the hard block. DESIGN §4 (6): "Questions never stop the runnable
frontier ... the run hard-blocks only when nothing remains runnable."

**Reading.** Every `step()` begins by polling the answer source for each open question, in id
order, while the run is not ending; the first answer or decline is ingested as `question_answered`
and the step returns `Progress::Answered`. The hard block keeps its blocking read. The poll must be
**non-blocking**: `TerminalAnswers::resolve` prompts and reads stdin, and `EventLogAnswers::resolve`
sleeps up to its budget — both are the hard block's "attached-terminal prompt or wait_on_block",
and neither may run while other work is runnable. So `AnswerSource` gains `poll`, defaulting to
`Unanswered`; `EventLogAnswers` answers it from the answer file without waiting. That is the change
this reading asks of `interaction.rs`; the first repair round changed the file again — an option is
a decline by what it says (`DECLINE_OPTIONS`, `is_decline_option`, `answer_for_option`), with its
tests — §12 finding 1. (This sentence once called the poll "the whole change to `interaction.rs`",
which it stopped being at `a23ce25c`; §13, the P3.)

### R11 — this record's location

See the preamble. `reviews/`, dated, beside the gate report's convention; not the root, not
`docs/internals/`.

### R12 — the worker's prompt does not say what the materialization observed

`PlanRequest` carries `materialization_observed` and the assembler could tell the worker that its
worktree holds conflict markers. Not done here: the frozen repair spec body already names the
conflicting paths and the rejecting head, the packet does not ask for it, and the prompt builder is
shared with the released path. Recorded as a possible follow-up.

## 4. The PR8 checkpoint refusals this slice replaces, and the placeholders beside them

`decisions.sequential_substrate.checkpoint_refusals` let PR8 refuse *"repair execution and
repair-admission answers"* before any append. The abandoned attempt's inventory listed three
refusal sites; each was verified against `9cfaf0c8` by grep and by reading, and the inventory was
extended with the eleven places that were written for ordinary tasks only and would have let a
repair through wrongly once the refusals fell. Every one is replaced by an implementation; none is
deleted and forgotten.

**The three refusals (verified; the abandoned inventory had all three):**

| # | Site at `9cfaf0c8` | What it refused | What stands there now |
|---|---|---|---|
| 1 | `src/engine/topology/select.rs`, `checkpoint`, `Step::RepairDispatch` arm | dispatch of a Repair-origin task, before any append | `Admitted::RepairDispatch { key, generation, continuing }`; the loop dispatches or continues the repair and runs its first attempt |
| 2 | `src/engine/topology/run.rs`, `hard_block`, `origin != VerificationPark` arm | ingesting a repair-admission or attempt-park answer | every origin is ingested: an activation, a one-off binding (with the validated `binding_override`) or a decline |
| 3 | `src/engine/topology/recover.rs`, `open_no_attempt`, `InheritedLineage` arm | recovering a repair generation, because the source was not retained | the source is read from the generation's `task_dispatched` in the proven prefix; step (g) verifies or recreates at base and the loop re-materializes (R6, R7) |

**The eleven placeholders (extension of the inventory; each written for ordinary tasks only):**

| # | Site | Ordinary-only shape | Now |
|---|---|---|---|
| 4 | `select.rs`, `select` | `RepairDispatch` selected without the ceiling check | ceiling checked (R3) |
| 5 | `run.rs`, `LoopBranch::IngestAnswers` | `NotThisSlice { slice: "PR9" }` | `Performed` (R10) |
| 6 | `run.rs`, `continue_open` | `source: None`, `DispatchKind::Ordinary` | kind from the registry, source from the log |
| 7 | `run.rs`, `dispatch_request` | `DispatchKind::Ordinary` unconditionally | `Repair { root, source }` for a lineage entry |
| 8 | `run.rs`, `retry_ready` | `materialization: None` | `Some(Retained)` for a lineage member (R8) |
| 9 | `run.rs`, `attempt` | `materialization_observed: None` | what the dispatch or continuation observed |
| 10 | `run.rs`, `promote_candidate` | `CandidateLeaseEffect::ReplacesPredicted` unconditionally | `WidensLineage { root }` for a lineage member (the fold refuses the other) |
| 11 | `run.rs`, `attempt` / `retry_ready` / `implementer_binding` | `frozen_rung_binding` only | the override-aware `rung_binding` reader |
| 12 | `run.rs`, `ladder_policy` | `rungs` from the frozen ladder (0 for an empty-ladder repair, which `next_step` reads as `Fail`) | 1 when an override is recorded: "a single-rung ladder with the kind's attempts_per; exhaustion escalates to the human rung" |
| 13 | `src/topology/events.rs`, `RungBinding::matches_override` | agent, model and effort only | all five fields (E2; Class B row B1) |
| 14 | `src/topology/fold/apply.rs`, `apply_settlement`, `Failed` arm | closes the generation and fails the task | also folds the lineage for a member (Class B row B2, R4) |

## 5. Class B / Class C ledger

`src/topology/**` is frozen. Class A (no vocabulary change) is free; Class B (fold behaviour, no
wire change) needs a row here whose description matches the code; Class C (wire vocabulary) is an
approval owed and is not made in this slice.

**No Class C change is made.** §6 records the one place a Class C change would have been simpler.

| # | Change | Where | Description (verified against the code at each commit that touches it) |
|---|---|---|---|
| B1 | `attempt_started`'s override check binds all five fields of the rung binding | `src/topology/events.rs` (`RungBinding::from_override`, `matches_override`), `src/topology/fold/check_attempt.rs` | Errata E2. The check compared agent, model and effort and said nothing about tier or pin. It now builds the whole expected binding — agent, model and effort from the recorded override, tier from the repair ladder's frozen floor, `pinned: true` (R1) — and compares it whole; an override recorded on a ladder with no floor is a mismatch, because there is then no tier the binding runs at. Strictly narrows what is accepted; no wire form changes. |
| B2 | A repair task's terminal `Failed` settlement folds its lineage | `src/topology/fold/apply.rs` (`apply_settlement`) | `decisions.repairs.terminal_failure`. The fold folded a lineage on a recorded decline and not on a member's own terminal failure, so such a settlement left the lineage lease held and the root `AwaitingRepair` with nothing admissible — the `derived_outcome` arm the census asserts unreachable. It now runs the same `fail_lineage` the decline path runs, for a settling task whose registry entry has a `lineage` (R4), and records the halt afterwards as before. Ordinary tasks are untouched. |

Class A changes in the frozen layer, listed so nobody hunts for an undeclared one: the read-only
reader `TopologyFold::rung_binding` in `src/topology/fold/predicates.rs` (the override-aware
composition of `binding_override` and `frozen_rung_binding`, the same rule `check_attempt_started`
applies); the constructor `RungBinding::from_override`.

## 6. Where a Class C change would have been simpler, and was not made

R1: the errata's prose says tier and pin come "from the frozen option", and the shipped option is
an agent name. Widening `SpawnAdmission::HumanBinding`'s options to frozen rungs would make the
sentence literal. It is a wire change, in every log forever, and the ladder's frozen floor answers
the same question from state the fold already holds — so the change is not made and no approval
is owed for it.

## 7. Deferred findings this slice must not disturb

`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002` are deferred by owner ruling with standing findings, and
G3's pass rests on the first staying deferred. Neither is reopened, repaired or narrowed. Whether
this slice makes either newly reachable is checked in §9 as the work lands, not assumed.

## 8. Measurements

- git 2.43.0 `cherry-pick --no-commit`: the table in R5, including the absence of
  `CHERRY_PICK_HEAD` in every case and the presence of `MERGE_MSG` and `AUTO_MERGE`.
- Resolution by edit: after a conflicting `--no-commit` pick, rewriting the file without markers
  and running `git add -A` leaves zero unmerged entries and `write-tree` succeeds (R9).
- Second repair round, git 2.43.0 (§13 finding 1): `git rm -- <path>` on an unmerged path removes a
  present file without `-f`, for a both-modified and for a delete/modify entry alike; `git add --
  <path>` on a tracked path whose file is gone stages the deletion; `git add` of a never-tracked
  missing path exits 128; `git add -A -- . ':(exclude,top)<name>'` keeps only the root-level `<name>`
  out of the index and stages a `sub/<name>`; a pathspec's literal spelling matches an existing file
  before any glob reading (`git add -- 'a[1].txt'` staged `a[1].txt`), and `:(literal)` makes that
  the rule rather than a fortune; `git rm -q -- ':(literal)sp ace.txt'` exits 0.
- Third repair round, git 2.43.0 (§14): `git rm --quiet -- <path>` on a path with changes staged
  in the index — a retained retry's staged resolution — exits 1 ("the following file has changes
  staged in the index") and needs `--force`; `git add -- :(literal)<path>` over unmerged stages
  writes the index's resolve-undo extension, which `git ls-files --resolve-undo -z` lists as
  `<mode> <object> <stage>\t<path>` records for the three stages, survives `add -A` and
  `write-tree`, is left in place by a later `git rm` of the resolved path, and is cleared by
  `read-tree --reset -u HEAD`; `git rm` over unmerged stages writes it too; `git add -A -- .
  ':(exclude,top)<name>'` with `<name>` an untracked file ignored by `.gitignore` exits 1 ("The
  following paths are ignored by one of your .gitignore files") because the exclusion item is
  matched against the ignored paths `add` collects; that exclusion also keeps `<name>/data.txt`
  out (a pathspec is a directory prefix); `git ls-files --others --exclude-standard --
  ':(top,literal)<name>'` lists `<name>` exactly when it is untracked and not ignored, nothing when
  it is ignored or tracked, and a directory's files by their own names; `git ls-files --stage --
  ':(top,literal)<name>'` lists a tracked `<name>` at any stage and nothing for an untracked one.
- Fourth repair round, git 2.43.0 (§15): `git ls-files --stage -- ':(top,literal,icase)<name>'`
  lists a tracked case variant of `<name>` (`.UPSTROKE-RESOLVED` for `.upstroke-resolved`) and the
  entries under `<name>/` in any case, and a longer name sharing the prefix (`.UPSTROKE-RESOLVED-x`)
  not at all; `git add -A -- . ':(exclude,top)<name>'` with the index holding `<name>/data.txt` and
  a regular file at `<name>` on disk leaves `data.txt` in the index; `git add -A --
  ':(top,literal,icase)<name>/'` then removes it and does not add the file (a pathspec longer than
  the name cannot match it), restages a `<NAME>/` directory present on disk harmlessly, and with
  nothing under the name in the index or on disk exits 128 "did not match any files";
  `git clean --quiet --force -x -- ':(top,literal)<name>'` removes an untracked `<name>` ignored or
  not, exits 0, and leaves a tracked `<name>` alone; `git ls-files --stage --resolve-undo -z` lists
  the stage-0 entries and then the resolve-undo records in one output; `git ls-files` has no
  `--pathspec-from-file`; `git bundle create` refuses a bare sha as its head. On the Windows guest
  (NTFS, the manifest reviewer's `windows-cases.log`): `ΟΣ` and `οσ` are one file, `Σ` and `ς`
  are two, `Σ` and `σ` one; `checkout-index` of a 180-character name fails "Filename too long".
- Second repair round, the sampler (§13 finding 3): ten runs of the repaired sampler on this box,
  each collecting its eight kills in ten or eleven spawns with one or two of them landing while the
  pick was writing; the immediate-kill mutation fails three times in three with 32 of 32 spawns
  killed before the first write. CI at `56ea88c9`: winguest 30 kills in 32 spawns, ten `None`,
  twenty `After` with `MERGE_MSG`, none between (the kill reached Git for Windows' launcher, 46 KB
  at `cmd\git.exe`, not the 4.2 MB `mingw64\bin\git.exe` it starts); macOS 6 kills in 32 spawns,
  four of them `Internal`, 26 picks complete before their kill.
- Kill points of `cherry-pick --no-commit` (git 2.43 source order, confirmed by the N=8 kill
  sampler `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`):
  `index.lock` is taken at the start (`write_index_as_tree`) and again for the whole merge and
  working-tree write (`do_recursive_merge`), released by `write_locked_index`; `AUTO_MERGE` is
  written plainly while that lock is held; `MERGE_MSG` is written afterwards through its own
  `MERGE_MSG.lock`. So a kill leaves one of: nothing; `index.lock`; a merged index with no state
  file; a merged index with `MERGE_MSG.lock`; a merged index with `MERGE_MSG`. Sample 5 of the
  first sampler run was the fourth state.
- Re-running the pick onto an index that already holds it is **not** a no-op merge, whatever the
  first measurement of it said: it is a three-way merge, and PR #249's crash review measured it
  applying its hunk a second time (`a a y c b c a` picked with `c b -> c c b x` gives
  `a a y c c b x c a`; picked again, `a a y c c c b x c a`; one line more per resume). Reproduced
  on this box, git 2.43, by hand and by
  `a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces`. The funnel
  now restores the base's tree first (`git read-tree --reset -u HEAD`: index and checkout to
  `HEAD`'s tree, unmerged entries discarded, no ref and no `ORIG_HEAD` written, untracked files
  kept — measured), so every materialization is one pick onto the base
  (`a_materialization_killed_after_its_index_write_converges_from_both_of_its_states` drives the
  clean and the conflicting index).
- The catalogue's lowest `claude-code` model at or above `Mid` is `claude-sonnet-4-5` (first of
  two `Mid` entries, `min_by_key` returning the first minimum); the policy's `Mid` effort in the
  recover fixture is `High`. Both are what the one-off binding test pins.

## 9. Findings raised

**Fixed in this slice (both found by this slice's own tests, both in the path it adds):**

- **PR9-F1 — a held `MERGE_MSG.lock` passed `Worktree.Verify`.** Found by the kill sampler on its
  first run: a pick killed inside `write_message` leaves `MERGE_MSG.lock`, a merged index and no
  `MERGE_MSG`; the verifier read only the committed name, reused the worktree, and the re-run pick
  died on `could not lock 'MERGE_MSG': File exists`. Fixed as the class in
  `src/workspace_manager/residue.rs`: every administrative state file is read in its committed
  form and in its held `.lock` form (`CHERRY_PICK_HEAD`, `MERGE_HEAD`, `MERGE_MSG`, `REVERT_HEAD`),
  in the verifier and in the residue classifier alike; `index.lock` was already the held form of
  `index`. Within the packet's quiescence rule ("no cherry-pick/merge/revert/sequencer/rebase
  state exists"): a held lock is that state, in progress. Pinned by
  `worktree_verify_answers_every_non_quiescence_by_name` and the K3 half of the late-kill test.
- **PR9-F2 — the materialization left `MERGE_MSG` for the life of the generation.** Found by the
  ST-15 (repair) test: the retry's `HoldsTree` verification read the `MERGE_MSG` the pick had
  left and closed the generation, so no repair generation could ever be retried in place, which
  the packet's own `ST-15` for repairs requires. Fixed in `WorkspaceManager::repair_materialize`:
  the funnel removes `MERGE_MSG` and `AUTO_MERGE` once the pick has ended, in all three outcomes.
  Consequence recorded in R6's terms: a *completed* materialization is now quiescent at its base,
  so recovery (g) reuses it (`Reuse::Verified`) and the continuation's re-run pick starts from the
  restored base tree and reports the same observation; a kill before the removal leaves one of the
  five states §8 lists, of which the two lock forms and `MERGE_MSG` fail the verify and are
  recreated, while a merged index with no state file (K2) verifies like a completed pick and
  converges through the restore (R5 as corrected in §14 finding 3; this paragraph once said every
  such kill "still fails the verify"). `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once`
  drives all three prefixes.
- **Ordering of the unresolved-conflict verdict.** First written after `evaluate_outcome`, which
  reported a completed worker's empty diff (`EmptyDiff`) ahead of the conflict it left. Now the
  unresolved verdict pre-empts the diff-shaped verdicts but not the worker's own end (error exit,
  timeout, question). Pinned by `an_unresolved_conflict_fails_the_capture_before_any_gate_and_a_declared_one_is_staged_by_the_engine` (named `…_a_resolved_one_is_staged` until the second round; the fifth round's record review found the old name here).

**Recorded, not fixed (each is outside this slice's authority or beyond the packet's rule):**

- **PR9-N1 — the frozen residue row for `Object.RepairMaterialize` names `CHERRY_PICK_HEAD`,**
  which `--no-commit` never leaves (R5, and the pre-existing workspace_manager residue grid
  already says so); what it leaves is `MERGE_MSG`, which the row does not name. The element is
  still constructed synthetically because PR3 froze the row. Amending the row is a change to
  `ObjectSite::residue_elements()` in `src/topology/effects.rs` and to `effects/residue-classes.json`
  — frozen; **approval owed**, not made.
- **PR9-N2 — `Quiescence::AtBase` does not compare the index with the base's tree.** The packet's
  rule reads HEAD, the index lock and the state files, and this slice keeps to it. The one
  interrupted state it therefore passes — a merged index with no state file — converges because
  the materialization funnel restores the base's tree before it picks (§8, §12 finding 3); the
  earlier justification, that the re-run pick was idempotent, was false. A stricter quiescence
  rule would be a strengthening beyond the packet (`hardening-vs-defect`), so it is recorded here
  rather than made.
- **PR9-N3 — `ORIG_HEAD.lock` is not read** for the same reason `ORIG_HEAD` is not (ordinary
  commands write it); no engine funnel writes `ORIG_HEAD` in a task worktree, measured by the
  sampler's residue histogram.
- **PR9-N4 — the human guidance text of an admission question is not on the schema-4 wire**
  (`FrozenQuestion.context` carries a fixed sentence; the worker's prompt is not told what the
  materialization observed, R12). No change; the observation is in `attempt_started`.
- **PR9-N5 — `TaskRegistry::originals_with_agents` and `Damage.small_only`:** a Small-only chain is
  accepted and yields a `HumanBinding` repair; nothing in `registry.rs` refuses a chain whose every
  rung is below the repair floor at ingest, so the first repair of such a task always asks a
  person. That is the packet's rule (`repairs.human_binding`) and is recorded as the behaviour a
  plan author gets, not as a defect.
- **PR249-KILL-SAMPLER-WINDOWS-WRAPPER — PR5's census sampler kills Git for Windows' launcher, not
  git** (§13 finding 3; `findings/`, P2, deferred). The PR9 and T-ATTEMPT samplers' shared
  fixture is fixed at `77c0111f`; the census sampler's spawn is PR5's evidence, and kills that
  reach git on Windows for the first time need the guest to show all four of its commands'
  recoveries — a slice of its own. `effects/residue-classes.json` freezes its declarations and
  sampling N, not its histogram, which that file calls machine-varying; the finding file said
  "frozen contract" once and was corrected (§14 finding 4).
- **PR249-MANIFEST-NORMALIZATION-ALIAS — a normalization-form respelling of a conflicted path is
  not read as an alias** (§14 finding 5; `findings/`, P3, deferred). `Declaration::names`
  compares components as written and `names_in_another_case` folds case for refusing — per
  character since the fourth round (§15 finding E; the string fold was contextual); a path
  respelt in another Unicode normalization form matches nothing and is not refused as a
  contradiction beside the index's spelling, though a normalization-insensitive volume (macOS)
  reads the two as one file. The standard library has no normalization tables; the fold needs a
  dependency or a filesystem oracle, neither taken here or in the fourth round, whose case
  repair does not reach normalization. Pinned as the boundary by the case-alias test, which
  asserts on each CI platform what the checkout does with the two spellings.
- **PR9-N6 — the resolution manifest's grammar tolerances are chosen, not measured** (§13 finding
  1). A leading list bullet, a colon after the keyword, backticks or quotes around the path and a
  `./` prefix are accepted because a model is likely to write them; no live worker was run against
  the grammar in this round. A worker that writes anything else loses one attempt to a refusal that
  quotes the line and feedback that spells the grammar, bounded by the ladder. A follow-up to
  measure, not a defect.
- **PR9-N7 — a refused manifest is corrected in a fresh generation, not in place** (§17 item 2;
  `findings/`, `PR249-REFUSED-MANIFEST-HANDOFF`, P3, deferred). A refusal fails the attempt
  as the worker's with `capture.unresolved` non-empty, which `run.rs` excludes from `resumable`, so
  the settlement closes the generation and the next attempt is a fresh generation and worktree
  whose worker resolves and declares afresh; `design/26` §26.4 promised until the sixth round that
  the refused manifest stays "for the worker to correct, and the next capture reads it", and the
  worker-facing texts (`classify::unresolved_conflict_failure`, `repair::repair_body`) still say it
  "stays for you to correct" — production strings, out of a documentation-only round, named there
  for the change that takes this up. Whether the in-place correction is worth its retained retry is
  the owner's call; the cost today is one attempt and the resolution work, bounded by the ladder.

**The two deferred findings (§7):** neither `PR8-R2-SPEND-REPLAY` nor `PR8-CRASH-002` is made newly
reachable. The first concerns the replay of a parked review's cost, which this slice does not
touch (`Spend::replay` is unchanged and `a_runs_spend_is_the_same_live_as_on_replay` still holds).
The second concerns a Git ref lock left by a coordinator killed inside a ref write during
authorized publication — `integration.lock` on the integration ref's compare-and-swap, or, in its
common-directory variant, `packed-refs.lock` — which every later resume's retry of that
publication refuses on until an operator removes the file. A lineage candidate is published
through the same `integrate` compare-and-swap as an ordinary one, so a repair adds one more
publication a run performs and no new ref write, no new lock and no new window: the finding's
shape and its remedy (a residue-class decision the owner holds) are unchanged. (The first version
of this paragraph described the finding as a window in `promote_candidate` with `WidensLineage`
the difference; PR #249's conformance review corrected it — §12, finding 8.)

## 10. Tests against the packet's `gating` and `proof_tests`

Every row names the test that holds it at the head of this branch. Where the test is narrower
than the packet's sentence, the row says how.

| Packet item | Test |
|---|---|
| rejection kind tests (invalid or duplicate override; non-override `attempt_started` on an overridden task; `LineageHeld`/predicted disposition mismatch) | `fold::tests::an_override_replaces_the_frozen_binding_for_every_later_attempt` (E2: exact five-field binding accepted; every single-field move, the tier either side of the floor and the pin refused; a floorless ladder refused naming the floor); `a_closing_generation_admits_exactly_the_disposition_its_holding_implies` (PR8, unchanged); `a_rejection_already_folded_is_refused_when_it_arrives_again` |
| limit tests (over-limit registers `AwaitingInput`; nothing spends until the answer; the answer activates) | `recover::tests::an_over_limit_repair_spends_nothing_until_its_answer_activates_it`, `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue` |
| ST-12 one-off binding authority | `recover::tests::a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run` (the five-field override on the wire and the attempt run under it, pinned, at the floor), `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`, `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` (both `halts_run` values); the fold half in the row above |
| ST-11 repair | `recover::tests::a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` |
| ST-15 repair | `recover::tests::a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` (one `Object.RepairMaterialize` execution across both attempts, counted at the hook harness) |
| two-lineage order test; lineage candidates through the queue with `satisfies` | `recover::tests::two_lineages_publish_in_lineage_order_and_the_younger_candidate_waits_behind_the_older`. **Narrower than the sentence:** the rejections are `Conflict` dispositions on the record and the repairs' picks apply cleanly; the fake worker resolves nothing, so the conflict-resolving half is the attempt-level `an_unresolved_conflict_fails_the_capture_before_any_gate_and_a_declared_one_is_staged_by_the_engine`, where the resolution and its manifest are written by the test. The lineage-order claim itself — the younger candidate queued first and ineligible, publication in lineage order with `satisfies` and the lineage release — is held whole. |
| source-protection test across Parked/BudgetExceeded resume | `recover::tests::a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume` (BudgetExceeded); the Parked half is the over-limit test, whose run is parked on the question across the resume |
| T-ANSWER | `recover::tests::an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step` (the production `EventLogAnswers` reader, `via: "event-log"`) |
| already-present-source repair | `attempt::tests::an_already_present_source_proceeds_as_an_ordinary_attempt_whose_empty_diff_fails_honestly`; the dispatch-level `Empty` observation in `dispatch::tests::a_repair_dispatch_records_what_its_materialization_observed` |
| kills incl. mid-materialization, synthetic and sampled | `dispatch::tests::repair_materialization_synthetic_residue_recreated_after_forced_removal`, `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` (N=8), `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states`, `repair_materialization_objects_released_to_git_on_scrub`; the existing `repair_materialization_reproduced_after_kill` |
| T-REPAIR-DISPATCH across the process boundary | `recover::tests::a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` (no worktree; held `MERGE_MSG.lock`; completed pick) |
| T-RETAINED / T-RETRY (repair) | the ST-11 and ST-15 rows; an interrupted retry's `LineageHeld` settlement is `fold::tests::a_closing_generation_admits_exactly_the_disposition_its_holding_implies` plus the loop's `settle_interrupted` through `closing_disposition` (PR8) |
| ST-05 lease subset | `fold::tests::a_lineage_lease_is_released_exactly_once_and_a_second_release_is_refused` |
| unresolved index entries fail capture before gates (R9) | `attempt::tests::an_unresolved_conflict_fails_the_capture_before_any_gate_and_a_declared_one_is_staged_by_the_engine`, `a_declared_deletion_resolves_a_conflict_and_an_undeclared_missing_file_does_not`, `a_conflict_rendered_with_a_longer_marker_size_is_unresolved_at_capture`, `an_untouched_binary_conflict_is_unresolved_at_capture_and_a_declared_keep_ours_is_staged`, `four_conflict_shapes_declared_at_once_are_staged_by_the_engine_into_one_tree`, `four_conflict_shapes_undeclared_are_all_refused_before_any_gate_and_nothing_is_staged`, `an_edit_only_worker_completes_a_conflict_repair_through_file_writes_alone`, `a_declared_path_is_staged_literally_whatever_characters_it_holds`, `a_resolution_manifest_written_where_nothing_conflicted_is_ignored_and_stays_out_of_the_candidate`, `the_resolution_manifest_grammar_reads_what_a_worker_writes_and_refuses_the_rest` (§13 finding 1: every worker action a file write); §14: `a_declaration_in_another_case_is_refused_and_the_checkout_says_whether_it_named_the_file`, `a_quoted_declaration_names_an_entry_exactly_whitespace_included`, `a_declared_resolution_reaches_the_configured_checks_which_decide_what_they_detect`; §15: `a_case_alias_is_read_per_character_so_a_final_sigma_hides_no_contradiction` |
| the manifest's name: the worker's file excluded, a taken name the repository's (R9 as revised in §14 and §15) | `attempt::tests::a_tracked_file_of_the_manifests_name_is_the_repositorys_and_a_conflict_repair_there_is_refused`, `an_ignored_manifest_is_read_and_kept_out_of_the_candidate_by_the_ignore_rules_alone`, `a_directory_of_the_manifests_name_is_the_repositorys_and_its_contents_are_captured`; §15: `a_tracked_file_of_the_manifests_name_in_another_case_is_the_repositorys_on_every_platform`, `a_manifest_standing_where_a_tracked_directory_was_hides_none_of_its_deletions`; `workspace_manager::tests::the_manifests_name_is_read_by_what_holds_it_and_excluded_only_when_it_is_the_workers` (every state of the name, the symbolic link, the case variant and the displaced directory included), `the_resolved_conflict_read_passes_no_recorded_path_as_an_argument` |
| a same-generation retry revises a declared resolution (R9 as revised in §14 and §15; ST-15 repair) | `attempt::tests::a_retained_retry_revises_a_declared_resolution_to_a_deletion_and_a_settled_deletion_is_not_reapplied`, `a_retained_retry_reads_the_manifest_while_the_index_holds_what_a_capture_resolved`; §15: `a_settled_deletion_is_not_revived_by_the_manifest_that_made_it_once_the_path_is_recreated` |
| lineage terminal failure | `fold::tests::a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay`, `an_ordinary_tasks_terminal_failure_leaves_a_live_lineage_exactly_as_it_was` |
| the checkpoint split | `select::tests::every_step_variant_is_admitted_or_refused_and_the_split_is_seven_two`, `a_repair_origin_task_crosses_the_checkpoint_and_the_ceiling_binds_it_like_any_dispatch` |

## 11. The funnel layer's changes (`src/workspace_manager.rs`, `src/workspace_manager/residue.rs`)

Not frozen, but the layer G3 certified, so listed:

- `repair_materialize` restores the worktree's index and checkout to `HEAD`'s tree
  (`git read-tree --reset -u HEAD`) before it picks, so every materialization is one pick onto the
  base (§12, finding 3); returns `Materialized::{Clean, Conflict, Empty}` (R5); and, once the pick
  has ended, removes `MERGE_MSG` and `AUTO_MERGE` (`clear_pick_state`; PR9-F2). Its doc's claim
  that `CHERRY_PICK_HEAD` is left was corrected against the measurement.
- New reads: `staged_against_head`, `unmerged_records` (shared with `proposal_state`),
  `unresolved_conflicts` (every path the index holds unmerged, R9 as revised; a record that does
  not decode is one unresolved entry naming the reason). `effects/wrappers.toml` lists
  `unresolved_conflicts` as effect-free.
- Second repair round (§13 finding 1): `candidate_stage` takes the worker's declared resolutions
  and stages each with its own `git add -- :(literal)<path>` or `git rm --quiet -- :(literal)<path>`
  before the `add -A`, inside the one `Object.CandidateStage` funnel invocation;
  `CANDIDATE_STAGE_ARGV` ended in `:(exclude,top).upstroke-resolved` until the third round (the
  bullet below); `RESOLUTION_ADD_ARGV` and `RESOLUTION_RM_ARGV` are the fixed words. New: `RESOLUTION_MANIFEST`, `RESOLVED_KEYWORD`,
  `DELETED_KEYWORD`; `ResolutionKind`, `Declaration` (`names`, a `std::path` comparison),
  `DeclaredResolution` (`argv`), `ResolutionManifest` (`parse`); the effect-free read
  `resolution_manifest`. `effects/wrappers.toml` lists the four functions effect-free.
- Third repair round (§14): `CANDIDATE_STAGE_ARGV` is the bare `add -A -- .` again and
  `CANDIDATE_STAGE_MANIFEST_EXCLUSION` is the pathspec `candidate_stage` appends to it exactly when
  `manifest_name` reads the worker's manifest (`ManifestName::Manifest { ignored: false }`); the
  argv tripwire's `candidate_stage` row declares that one dynamic argument. `ManifestName`
  (`Absent`, `Manifest { ignored }`, `Tracked`, `Other { what }`) is the one reading both
  `candidate_stage` and `resolution_manifest` decide from, and `resolution_manifest` refuses a
  taken name (`UpstrokeError::Refused`). New read: `resolved_conflicts` (`ls-files --resolve-undo
  -z`, then `ls-files --stage` for the paths the index still holds). `RESOLUTION_RM_ARGV` gains
  `--force`. `Declaration::names_in_another_case`; `ResolutionManifest::parse` strips one colon
  and keeps a quoted path exact. `parsers` gains `stage_record_paths`, `plain_record_paths` and
  `decode_index_path`. `effects/wrappers.toml` lists the new functions effect-free.
- Fourth repair round (§15): `candidate_stage` takes a `ManifestDisposal` (until the fifth round;
  the bullet below) and, for a manifest the
  capture acted on, removes it after the `add -A` (`CANDIDATE_STAGE_MANIFEST_CLEAN_ARGV`, a `git
  clean` of the one untracked path); when the worker's file displaces a tracked directory it
  stages what the index held under the name first (`CANDIDATE_STAGE_DISPLACED_DIRECTORY_ARGV`);
  `ManifestName::Manifest` carries `displaces_directory` and `Tracked` carries the index's
  `spelling`, read with `icase` so a case variant is the repository's on every platform;
  `resolution_manifest` refuses the variant naming it; `resolved_conflicts` reads the whole index
  and intersects in process; `Declaration::names_in_another_case` folds per character. The argv
  tripwire's `candidate_stage` row is unchanged: the two new lists are whole children of the funnel,
  not arguments beside the sampled one.
- Fifth repair round (§16): `candidate_stage` takes no disposal — `ManifestDisposal` is gone — and
  removes the worker's manifest after the `add -A` whenever the name holds it, read or unread;
  `CANDIDATE_STAGE_DISPLACED_DIRECTORY_ARGV` is `add -u`; `ManifestName::Manifest` carries the
  `spelling` the checkout lists the file by, `CANDIDATE_STAGE_MANIFEST_EXCLUSION` is the magic
  `:(exclude,top,literal)` completed by it, `CANDIDATE_STAGE_MANIFEST_CLEAN_ARGV` is five words
  completed by `CANDIDATE_STAGE_MANIFEST_CLEAN_PATHSPEC` and the same spelling, and
  `resolution_manifest` reads the file by it; `name_spelling` is the one rule that picks the entry
  out of the index's records and the directory's; `manifest_name` makes four reads (the
  `ls-files --others` listing with an `icase` pathspec is new) and reports a Git error when the
  directory walk lists no spelling of a file the working tree holds. The argv tripwire's
  `candidate_stage` row declares two dynamic arguments and no literal.
- `administrative_residue_at` and `observed_residue_elements` read each state file's held
  `.lock` form as that state's residue (PR9-F1); `state_file_present` is the one rule.
- `quiescence()` is otherwise unchanged: HEAD, the index lock, the state files (PR9-N2).

## 12. First repair round — the three reviews at `48017a43`

Three `gpt-6-astra` passes at max effort on 2026-09-08, each with its own scope, found eight
things: two P1s (conformance), two P2s with reproductions (crash consistency), three P2s with
mutation witnesses (refusal replacement and test honesty), and one P3 about this record. Seven
carry an executed witness, so `MAINTAINING.md` step 5 fixes them whatever their labels; the eighth
is a correction to §9 and a ledger row. Every witness was reproduced on the unrepaired tree before
its repair — the tree at `48017a43` plus the witness tests and nothing else — and each repair was
then shown to make the witness pass; the mutation findings' repairs were shown to make the named
mutation die. Each row says what was found, what reproduced, what changed, and what now holds it.

### Finding 1 (conformance, P1) — selecting the last offered agent was a decline

`answer_for_option` read the last option of any non-`Clarify` list with two or more options as the
decline, which is where the legacy coordinator puts it; a `HumanBinding` question's options are
agents. **Reproduced:** `interpret` on `["claude-code", "copilot"]` with `2` gave `Declined`
(`picking_the_last_agent_offered_by_number_names_that_agent_and_declines_nothing`), and driven end
to end with two allowed agents the repair's lineage failed
(`picking_the_last_of_two_offered_agents_binds_the_repair_to_it_rather_than_declining`).
**Repaired as the class, not the instance:** an option is a decline by what it says. The decline
texts are two constants in `interaction` (`DECLINE_OPTIONS`), the legacy `question_options` builds
its lists from them, and `answer_for_option` asks `is_decline_option` — never a position. Every
site that maps a person's answer onto an option or a decline was swept: `interpret` (typed
`skip`/`decline`/`fail`/`abandon`, a number, or free text), `answer_for_option` (the fix),
`answer::answer` (`--option N`, through the same function), the schema-4 `answer_for` and
`chosen_index` (exact option text; a one-off binding refuses anything else; other origins record
index 0 for free text, which is the continue option), and the fold's `check_question_answered`
(frozen; validates the index against the frozen list and is untouched). **The PR8 half of the
class:** the schema-4 `Clarify` question offered "answer in your own words (typed free text is sent
back to the agent)" while `answer_for` records an option index and no text. The schema-4 sites
(`repair::admission_for`, `integrate::park_question`, `run::park_question`) now take their lists
from `coordinator::topology_question_options`, worded for what that engine does — "retry this task
(typed text un-parks it and is not passed to the agent)" and the shared give-up constant; the
legacy list is unchanged because the legacy engine does deliver typed text (`answer_question`,
`Feedback { human: true }`). The gap underneath — carrying the text needs a field the frozen
`Answer4::Answered` lacks — is filed as `PR249-ANSWER-TEXT-NOT-CARRIED` (`findings/`,
P2, deferred as beyond reach). **Now holds:** the two tests above, the constants' round trip in
`a_number_picks_an_option_and_anything_else_is_free_text`, and the existing binding tests.

### Finding 2 (conformance, P1) — the unresolved-conflict refusal read one marker format

`unresolved_conflicts` scanned each unmerged path's file for `<<<<<<< `/`>>>>>>> `. **Reproduced:**
a conflict under `conflict-marker-size=8` and one under `-merge` both captured as resolved
(`a_conflict_rendered_with_a_longer_marker_size_is_unresolved_at_capture`,
`an_untouched_binary_conflict_is_unresolved_at_capture`: `unresolved` was `[]`). **Repaired:** the
read is the index's — every unmerged entry is unresolved until the worker stages its resolution
(R9 as revised in §3), `carries_conflict_marker` is gone, and the worker's feedback and the repair
spec say to `git add`/`git rm` the resolved path. The existing tests follow the rule: a file
rewritten without markers is unresolved until staged, and a deletion resolves when staged
(`a_staged_deletion_resolves_a_conflict_and_an_unstaged_one_does_not`, since renamed). **Now
holds:** the tests §10's R9 row names. **Superseded in part by §13 finding 1:** the index half of
this repair stands; the "worker stages" half could be met by no supported adapter, and the
resolution manifest with the engine's staging replaces `git add`/`git rm` in the feedback, the
repair spec and the tests.

### Finding 3 (crash, P2, reproduction) — re-materialization duplicated source content

A pick onto an index that already held the pick applied its hunk again. **Reproduced:** the
reviewer's line-token witness, on this box by hand (§8) and by
`a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces` (index
`a a y c c c b x c a` after one resume). **Repaired:** `repair_materialize` restores the base's
tree first. The body's deterministic-convergence claim and PR9-N2's idempotence justification were
false and are corrected (§9, the body). **Now holds:** that test, over two resumes, on the index
and the working-tree bytes, and the late-kill test's two index states.

### Finding 4 (crash, P2, witness) — closed repair generations kept their worktrees and intents

Recovery (e) appended `generation_closed(LineageHeld)` and reclaimed nothing. **Reproduced:** the
retained-repair test with its worktree planted kept `worktree=true, intent=true` after the resume;
so did an ordinary retained generation (`retry_refused_after_resume`), and so did an interrupted
attempt's generation after step (d)
(`an_interrupted_attempts_worktree_and_intent_are_reclaimed_by_recovery`) — the class is
"recovery closes a generation and leaves what it owned". **Repaired:** (d) and (e) scrub the closed
generation's worktree and intent after the append (`reclaim_closed_generation`), under
`workspace_candidates.cleanup`'s "task worktree scrubbed only after … the generation is Closed"
and T-ATTEMPT's "the task worktree scrubbed with force". The one arm left: a live retry whose
verification fails closes the generation `WorktreeMissing` and does not scrub, because the
attempt-level test pins that a retry itself removes nothing and a live run's closed worktree is
run-end closure's (PR10) — stated here so nobody hunts for it. **Now holds:** the three tests
above, the repair one across a later resume as well. **Completed in §13 finding 2:** the reclaim is
derived from the durable closed state by a sweep after (e), not performed by the closing step,
because a recovery that died between the durable append and the scrub left a generation neither
step would select again.

### Finding 5 (refusals, P2, mutation M6) — materialization tests accepted corrupted checkouts

M6 kept the pick's index right and overwrote every changed file in the checkout with
`MUTATED REPAIR WORKTREE`; all 2,399 library tests passed, because every oracle read `write-tree`
(the index) or compared SHAs with each other. **Repaired:** the dispatch tests read the checkout
from disk (`checkout_bytes`) and compare it with what one pick of the source onto the base leaves,
derived from the two commits (`expected_checkout`), and require the index to agree with the files
(`assert_checkout_is`); the publication test compares the worker's checkout and the published blobs
with the protected source's bytes and with the content already merged at the base; the
continuation and fresh-generation tests read the worker's checkout. **M6 on the repaired tree
(`7f7aa8fc`):** 9 of the 29 tests in the round's set fail —
`a_materialization_killed_after_its_index_write_converges_from_both_of_its_states`,
`a_repair_dispatch_records_what_its_materialization_observed`,
`a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces`,
`repair_materialization_synthetic_residue_recreated_after_forced_removal`,
`sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`,
`repair_materialization_reproduced_after_kill` (dispatch), and
`a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`,
`a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once`,
`a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again`
(recover). The unmutated control is the committed tree, on which all 29 pass.

### Finding 6 (refusals, P2, mutations M4, M5, M11) — the answer tests could not tell the paths apart

The mock's `poll` called `resolve`, and the admission fixtures had no other runnable work, so
removing the pre-step ingestion (M4), replacing the non-blocking `poll` with the blocking `resolve`
(M5) and restoring PR8's hard-block refusal for non-verification-park origins (M11) each passed all
396 topology tests. **Repaired:** `DriveSeams::answer_delivery` — `Polled` (the default: `poll`
answers, `resolve` refuses) or `Blocking` (terminal-style: `poll` finds nobody, `resolve` answers);
the admission, one-off binding and decline tests run under both deliveries; and
`a_delivered_answer_is_ingested_before_unrelated_runnable_work_dispatches` puts an open question
beside genuinely runnable work with a halting decline already delivered. **On the repaired tree
(`7f7aa8fc`):** M4 kills 7 of 29 (the frontier test — beta settled `spent_attempt: true` before
the decline — and every polled-delivery answer test, whose `resolve` refuses); M5 kills the same 7;
M11 kills 3, exactly the blocking arms
(`a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`,
`a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`,
`declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`). Control:
all 29 pass on the committed tree. M4's build also warns `ingest_answers` unused, as the reviewer
noted; the kills are the tests'.

### Finding 7 (refusals, P2, mutation M3) — the sampler accepted one kill of eight

M3 killed only the first child, at spawn, and let seven picks finish; the floor (`observed.len()
== 8`, `killed_while_running >= 1`) accepted `killed=1/8, observed=[None, After ×7]`.
**Measured before the change,** three runs on this box: 5/8, 7/8 and 7/8 kills, with `None`,
`Internal` and `After` all observed and `Internal` in two runs of three. **Repaired:** the sampler
collects `SAMPLING_N` children that actually died by the kill over at most `4 × SAMPLING_N`
spawns, verifies a pick the kill missed converges but counts it as a control, and requires at
least one kill to have landed before the index was published; `Internal` is reported in the
histogram, not required, because the synthetic half constructs it deterministically and a floor
the box hits two times in three would be a flake. **M3 on the repaired tree (`7f7aa8fc`):** the
sampler fails at "1 children died by the kill in 32 spawns"; control: passes. **Strengthened in §13
finding 3:** a kill of a child that had not begun is a sample of nothing, and the floor now asks for
a kill that landed while the pick was writing.

### Finding 8 (conformance, P3, no witness) — the record misdescribed PR8-CRASH-002

§9 described the deferred finding as a `promote_candidate` window with `WidensLineage` the
difference; the standing finding is a Git ref lock left inside an authorized publication's ref
write (`integration.lock`; `packed-refs.lock` in the common-directory variant) that every later
resume refuses on. §9 now says what the finding is and why a lineage candidate — published through
the same compare-and-swap — adds a publication and no new window. Ledger row `PR249-R1-008`; no
code change.

### The round's evidence

The witness run on the unrepaired tree (`48017a43` plus the witness tests): eight failures, each
at the assertion its finding predicts, recorded in `a23ce25c`'s message. The mutation runs above
were made on `7f7aa8fc` with each mutation applied by script and the file restored from the commit
afterwards (`git checkout -- src/`, the tree verified clean), one compile per mutation. The
mutation texts are the reviewer's, re-anchored where the round's own edits moved a line (M3 on the
rewritten sampler loop kills only the first spawn, as the original killed only sample 0).

## 13. Second repair round — the three reviews at `3bce2c6a`

Three scoped frontier reviews at `3bce2c6a` on 2026-09-08 — a regression review of the repair range
`48017a43..3bce2c6a`, a repair-adequacy review of the eight first-round findings, and a record
review — each returned `CHANGES_REQUIRED` (`~/review-pr249-3bce2c6-{regression,adequacy,record}.md`;
the verdict files do not name the model, and their review worktrees are the `astra-20260905` harness
of the first round's passes). Four findings carry an executed witness and are fixed whatever their
label (`MAINTAINING.md` step 5); a fifth is stale prose, corrected. The owner ruled the P1's design
answer before this round began (`~/tactus-artifacts/DESIGN-STAGING-DECISION.md`, Option 4): the
worker is granted no staging permission, and the engine stages what the worker declares. Every
witness below but one was re-run on this box on the unrepaired tree — `3bce2c6a` plus the reviewers'
witness tests and nothing else, in a scratch clone with its own target directory — before the
repair, and each repair was then shown to make the witness pass or the mutation die; the exception
is finding 3's immediate-kill mutation, which was not re-run here and whose evidence is the
reviewer's retained log, as that finding says. The code, tests, notes and
design change are `19a464bb`; this record follows it. `src/topology/**` is untouched by the round
(`git diff --stat 3bce2c6a..19a464bb -- src/topology` is empty), no wire word was added, and the two
deferred findings (§7) are untouched: this round adds no publication, no ref write and no replay of
spend.

### Finding 1 (regression, P1) — conflict repairs required a staging command no worker can run

The first round's repair read the index for unresolved conflicts (right) and required the worker to
`git add`/`git rm` its resolution (impossible): an edit profile has file tools and gate commands and
nothing else. **Reproduced:** the regression review's witness — the production Claude Code worker
command assembled through `WorkerAssembly`, its generated `permissions.allow` read back as
`Read, Glob, Grep, Edit, Write, NotebookEdit, Bash(cargo test)` under `--permission-mode dontAsk`,
Copilot's `permission_args` as `write` and `shell(cargo test)`, a real conflict, the file edited,
production `capture` — fails at `3bce2c6a` with `capture refuses ["c.txt"]`; the tree's own test
passed only because it staged through an unrestricted `git(&worktree, &["add", "--", "c.txt"])`
helper, a privilege production lacks. The design-staging session measured the rest: Claude denies
`git add -- c.txt` under production settings; Codex's `workspace-write` sandbox keeps `.git`
read-only in a clone and in a linked worktree alike, and adding the gitdir to `writable_roots` lets
`git commit` land; a `-merge` binary conflict resolved keep-ours is byte-identical to one abandoned,
so no content read can stand in for the worker's signal; `git add -A` collapses every unmerged
entry, markers included.

**Repaired (Option 4, as ruled).** The worker declares; the engine stages. `WorkspaceManager` gains
the resolution manifest, `RESOLUTION_MANIFEST` = `.upstroke-resolved` at the root of the worktree —
a root-level file because the Claude adapter denies every write under `.upstroke/`, `.git/` and
`.claude/` — with the grammar `resolved <path>` / `deleted <path>`, one per line, `#` comments, and
the tolerances `ResolutionManifest::parse` documents (a list bullet, a colon after the keyword,
backticks or quotes around the path, `./`, CRLF; backslashes read as separators on Windows by
`Declaration::names`'s `std::path` comparison). `resolution_manifest(slot)` is an effect-free read
beside `unresolved_conflicts`. `capture` reconciles the two (`plan_resolutions`): every unmerged
path the manifest declares is staged by the engine — `git add -- :(literal)<path>` or
`git rm --quiet -- :(literal)<path>` (`DeclaredResolution::argv`; `:(literal)` because `a[1].txt`
is a glob to a pathspec and a path beginning with `:` is magic) — inside the one
`Object.CandidateStage` funnel invocation, before the `add -A`, whose `CANDIDATE_STAGE_ARGV` this
round made end in `:(exclude,top).upstroke-resolved` so that the manifest would never be part of a
candidate (unconditional, it matched more than the manifest, and the third round moved it beside
the list and made it conditional — §14 finding 1); any unmerged entry undeclared, a manifest that
does not parse, or a path declared both ways refuses the whole capture before staging anything — the base's tree, the entries named, `AgentError` of worker
origin before any gate or review — and after staging the index is read again, an unmerged entry
left being a Git error. The feedback (`classify::unresolved_conflict_failure`) and the repair body
(`repair::repair_body`) instruct the manifest and "run no git command"; the worker prompt's
"NEVER run git commit, branch, merge, push, or reset — the engine owns git" is unchanged and now
literally complete. `design/26` §26.4's "At actual dispatch" paragraph carries the owner's exact
replacement wording: the quoted text to replace matched the file verbatim, and the two sentences
the brief's ellipsis covered (the semantic rejection, the payload's rejecting head) follow the new
text. `effects/wrappers.toml` classifies the four new externally reachable functions of
`workspace_manager.rs` (`argv`, `names`, `parse`, `resolution_manifest`) effect-free; the argv
tripwire's `candidate_stage` row still reads 0/0 because the declared-resolution children build
their argv from `RESOLUTION_ADD_ARGV`/`RESOLUTION_RM_ARGV` outside the funnel body, which the row's
description now says.

**Now holds:** in `attempt::tests`, with the `git add` helper gone and every worker action a file
write (`declare` writes the manifest) —
`an_unresolved_conflict_fails_the_capture_before_any_gate_and_a_declared_one_is_staged_by_the_engine`,
`a_declared_deletion_resolves_a_conflict_and_an_undeclared_missing_file_does_not`,
`a_conflict_rendered_with_a_longer_marker_size_is_unresolved_at_capture`,
`an_untouched_binary_conflict_is_unresolved_at_capture_and_a_declared_keep_ours_is_staged`,
`four_conflict_shapes_declared_at_once_are_staged_by_the_engine_into_one_tree` (default markers,
`conflict-marker-size=8`, a `-merge` binary kept as ours, a delete/modify resolved by deletion: one
`Object.CandidateStage` execution, zero unmerged, the tree carrying the three kept paths and neither
`d.txt` nor the manifest, `d.txt` removed from disk by the engine's `git rm`),
`four_conflict_shapes_undeclared_are_all_refused_before_any_gate_and_nothing_is_staged` (nothing
declared: four refused, named in the `AgentError`, no gate, no reviewer; three of four declared: the
fourth refused and nothing staged, the declared deletion removing nothing; a malformed line: all four
refused and the line quoted back as a fifth entry),
`an_edit_only_worker_completes_a_conflict_repair_through_file_writes_alone` (the reviewer's witness
shape, passing), `a_declared_path_is_staged_literally_whatever_characters_it_holds`,
`a_resolution_manifest_written_where_nothing_conflicted_is_ignored_and_stays_out_of_the_candidate`,
`the_resolution_manifest_grammar_reads_what_a_worker_writes_and_refuses_the_rest`. The four
conflict shapes needed one candidate commit carrying all four changes (`Run::commit_changing`): a
cherry-pick applies one commit against its parent, and the first draft's chain of one-file source
commits conflicted in `d.txt` alone. The reviewer's literal witness is not among the tests by
design: it resolves the file and declares nothing, which Option 4 refuses on purpose. The full
suite on this box: 2414 library tests passed (2406 before the round, eight added), 43 ignored, 10
binary tests passed.

### Finding 2 (adequacy, P2, witness) — a recovery interrupted after its durable close leaked

(d) and (e) scrubbed after each closing append; a recovery that died between the durable append and
the scrub left a `Closed` generation neither step would ever select again. **Reproduced:** both
adequacy witnesses — `Event.Append` made to return an error at its `Synced` point on the first
resume, the close durable, the worktree and intent present, a clean second resume — fail at
`3bce2c6a` with `worktree=true, intent=true` (2 failed, 5 passed of the seven `pr249_` witnesses in
the scratch clone; the five are the first round's, still passing). **Repaired:** the reclaim is
derived from the durable state. `reclaim_closed_generations` runs once after (e): every `Closed`
generation whose task slot still has an intent in the execution root is scrubbed
(`dispatch::scrub`, forced, idempotent), whichever incarnation appended the close; (d) and (e) no
longer scrub themselves (their `manager` parameter went with it), and a resume with nothing to
reclaim executes no `Worktree.Remove`. The live retry close (`WorktreeMissing`) still scrubs nothing
in the live run (PR10's run-end closure), and the next resume's sweep reclaims it like any other
closed generation's. **Now holds:**
`an_interrupted_attempts_reclaim_the_closing_recovery_never_reached_is_finished_by_the_next` and
`a_retained_generations_reclaim_the_closing_recovery_never_reached_is_finished_by_the_next` (the
first recovery stops at its close append with the close durable and the checkout present; the second
closes nothing and reclaims it; a third executes no `Worktree.Remove` at all), beside the first
round's three; and the reviewers' own two witnesses, applied unchanged to the repaired tree, pass
(4 of 4 `pr249_` recover witnesses). One count is deliberately not asserted: the second recovery's
`Worktree.Remove` executions are three in this fixture, because it also finishes the promotions the
interrupted first never reached (step (f)), and the site harness cannot attribute a scrub to a
slot; the on-disk reclaim and the third recovery's zero are the claims.

### Finding 3 (adequacy, P2, mutation) — the sampler accepted eight kills of nothing

The floor "at least one kill before the index was published" was satisfied by `None`: a child
killed before its first write. **Reproduced by the reviewer** (`M3_immediate_eight_kills.log`: the
variant passes at `3bce2c6a` with `killed=[None ×8]`; not re-run here, the mutation being the
reviewer's and its log retained). **Repaired:** each kill records
`KilledSample { class, merge_msg_written }`, read before recovery; `while_writing` is `Internal`
(after `index.lock`, so after the first write, before the publish) or `After` without `MERGE_MSG`
(the publish done, the pick's last write not — a completed `--no-commit` pick always leaves
`MERGE_MSG`, R5); the loop keeps sampling, within `MAX_SPAWNS = 32`, until it holds `SAMPLING_N`
kills *and* one that landed while writing, and the test fails when none did. **Measured, repaired
tree, this box, ten runs:** every run collected its eight kills in ten or eleven spawns with one or
two while-writing kills — `Internal` in nine runs, `After` without `MERGE_MSG` in the tenth — the
rest `None`, and two kills in all found a pick that had finished. **M3-immediate on the repaired
tree (`19a464bb`), three runs:** fails each time at "none of the 32 kills in 32 spawns landed while
the pick was writing". **M3 (one kill, the rest completing) on the repaired tree:** fails at "1
children died by the kill in 32 spawns; 31 picks completed". The floor is the reviewer's ("at least
one"), and the measurement says the loop reaches it two or three spawns beyond the eight; `Internal`
alone was hit in two runs of three before the first round and would have been a flake to require,
so an after-phase kill that had not yet written `MERGE_MSG` counts too — it interrupted a pick that
had not finished.

**What CI at `56ea88c9` then showed, and the sampler's second change (`77c0111f`).** Nine checks
green; `test (winguest)` and `test (macos-latest)` red, both at this sampler and at nothing else
(2337 and 2404 library tests passed on those legs). The winguest histogram: 30 kills in 32 spawns,
ten `None` and twenty `After` with `MERGE_MSG` in place, none between — no kill had ever landed
mid-write on that lane, and on inspection none could: `git` on the guest's `PATH` is Git for
Windows' `cmd\git.exe`, a 46 KB launcher that starts the real `mingw64\bin\git.exe` (4.2 MB, its
DLLs beside it) as its own child and waits, so `Child::kill` ended the launcher and the pick ran
on to completion. Every kill any sampler had delivered on that lane was a kill of the launcher.
What the log measured is the histogram: of the 30 kills, nine `None` with `MERGE_MSG` absent,
twenty `After` with it present, and one `None` with it present — a sample whose residue read found
nothing written and whose message read, taken separately afterwards, found the pick's last write
done, which is a git process still running after the kill had returned. What is inferred from the
launcher mechanism is why the histogram looks so: a kill of the launcher lands before git has
started, or leaves git to finish, and the first round's floor "a kill before the publish" was met
on that lane by the `None`s. (This paragraph first gave the inference as the measurement — "kills
of a process that had not yet started git" — and counted ten `None` against twenty `After` without
the `None`-with-`MERGE_MSG` sample; PR #249's third-round record review read the log, §14 finding
4.) So `KillableGitChild::spawn` — the fixture of this sampler and of
the T-ATTEMPT two-command sampler — now runs the real binary, resolved once through
`git --exec-path` as `<prefix>/mingw64/bin/git.exe` (`fixture::sampled_git`; `git` where that
layout is absent), and the mid-write floor binds on Windows too. PR5's four-command census sampler
(`SampledChild::spawn`) still spawns `git` by name; that is the reasoned half, filed as
`PR249-KILL-SAMPLER-WINDOWS-WRAPPER` (deferred: the census is PR5's evidence, and kills that reach
git on Windows for the first time need the guest to show all four of its commands' recoveries, a
slice of its own; `effects/residue-classes.json` freezes its declarations and sampling N and calls
its histogram machine-varying — the finding file said "frozen contract" once, §14 finding 4). The
macOS leg
failed the first round's own kill-count floor: 6 kills in 32 spawns, 26 picks complete before
their kill — four of the six mid-write, so the new floor was reached there — because the budget
was one cold probe pick, the first in a fresh worktree, and a schedule spread over it lands most
kills after a warm pick has finished; the budget is now the shorter of two probe picks (the
worktree restored between them) and `MAX_SPAWNS` is `8 × SAMPLING_N`. Linux, five runs after the
change: pass each time. Verified on the persistent Windows guest (`windowsguest`, git
2.50.1.windows.1, global `core.autocrlf=true`) at `77c0111f`: `engine::topology::dispatch::tests`
18 passed and `engine::topology::attempt::tests` 31 passed, this sampler's mid-write floor and the
T-ATTEMPT sampler among them — the first kills of git rather than of its launcher those samplers
have delivered on Windows. macOS is CI's to confirm.

### Finding 4 (record, P2) — R5 and R6 asserted a verification the code does not perform

R5 said a completed materialization "fails `Worktree.Verify` at its base exactly as an interrupted
one does"; R6 repeated it and said the rejected alternative would report
`Recreated { Residue(MergeMsg) }`. Both were true of the bare command in R5's table and false of
the funnel since PR9-F2 cleared `MERGE_MSG` and `AUTO_MERGE`; the continuation witness asserts
`Reuse::Verified` on both resumes and passes. **Corrected in §3:** R5 now says what the funnel does
with what the bare command leaves; R6 states the sequence the code performs (finish the
materialization, die before `attempt_started`, (g) verifies `Reuse::Verified`, the index is
retained, the continuation restores the base and picks once) and keeps the alternative rejected for
the double effect alone; each keeps its first wording as labelled history. No code change.

### The P3 — stale inventory prose

The body's validation paragraph listed "a byte scan for conflict markers" among the platform-shaped
additions; that scan left at `a23ce25c`. R10 said the non-blocking poll was "the whole change to
`interaction.rs`" while `a23ce25c` also changed its decline interpretation (`DECLINE_OPTIONS`,
`is_decline_option`, `answer_for_option`) with tests. Both corrected; the platform-shaped additions
now read: the manifest read and its `std::path` comparison, `-z`-delimited path decoding, state
files read by name in a worktree's git dir.

### What this round did not do, and why

- **The reviewer's literal P1 witness is not adopted as a test.** It resolves the file and declares
  nothing, and Option 4 refuses exactly that; the reviewer's *shape* — production permissions, then
  only what they admit — is `an_edit_only_worker_completes_a_conflict_repair_through_file_writes_alone`.
- **No staging permission, on any adapter.** Ruled by the owner; the analysis and its measurements
  are the design-staging decision's, cited rather than re-derived.
- **The manifest grammar's tolerances are chosen, not measured against a live model** (PR9-N6, §9).
- **`Recovered` gains no count of reclaimed generations.** The witnesses assert the reclaim on disk
  and through the hook harness; a field would be observability, not a repair.
- **A declared subset is never staged beside a refused path.** The capture is all or nothing; a
  worker that declared three of four paths is told the fourth and stages nothing this attempt. The
  alternative — staging the three — would leave a partly resolved index for a retry that the
  settlement rules never make `Retained` (R9); refusing whole keeps "an unresolved capture stages
  nothing" as the one invariant the tests pin.

## 14. Third repair round — the four reviews at `698777b0`

Four scoped frontier reviews at `698777b0` on 2026-09-09
(`~/review-pr249-698777b0-{adequacy,regression,record,manifest}.md`; the verdict files do not name
the model). The adequacy review of the second round's four findings returned **PASS**: every
remedy executed, seven Linux mutants killed, and the Windows launcher repair confirmed by
reverting only the child selection on the guest (three runs, 64 spawns, zero mid-write kills;
restored, three passes with one, two and one). The regression review of `3bce2c6a..698777b0`
returned **CHANGES_REQUIRED** with two executed capture defects; the record review
**CHANGES_REQUIRED** with a false verification claim and four prose corrections, having checked all
15 ledger rows, both Class B rows, R6 and R10 as holding; the manifest-contract review
**CHANGES_REQUIRED** with seven findings against the promises the tree itself makes, twelve probes
run, six controls passing and six documented-contract assertions failing. Every finding carrying an
executed witness is fixed whatever its label (`MAINTAINING.md` step 5); the three that were
reasoned or prose are corrected in the same round. Their evidence is copied out of `/tmp` into
`~/tactus-artifacts/pr249-manifest-contract-a1jplejz/` (the witness patch and its log),
`~/tactus-artifacts/pr249-r3-regression-zc3q78_p/` (the two capture witnesses and the recovery and
sampler controls) and `~/tactus-artifacts/pr249-record-honesty-dprytzha/` (the manifest witness and
its log).

**The witnesses, before and after.** The regression review's two tests and the manifest review's
twelve were run together on the unrepaired tree in a scratch clone with its own target directory —
`698777b0` plus those fourteen tests and nothing else — with the reviewers' own result: **8 failed,
6 passed**, the six controls passing and every assertion of the documented contract failing at the
line its finding names (`witness-698777b0-unrepaired.log`, kept beside the artifacts). The same
fourteen on the repaired tree (`a6f3898f` plus the fourteen; one two-argument call of
`plan_resolutions` in the reviewer's alias control given the planner's new second argument, and
nothing else changed): **9 passed, 5 failed** (`witness-a6f3898f-repaired.log`, beside the other).
The nine are the two regression witnesses, four of the six contract assertions (the directory's
ordinary edits, the extra colons, the ignored manifest with its required filter, the quoted
whitespace) and three of the six controls; the five that fail each assert what this round retired
— the recapture probe's ignored deletion and staged malformed manifest, the malformed-without-
unmerged probe's unqualified promise, the alias control's accepted case pair, the
directory-in-conflict control's I/O error, and the judging probe's "caught" — and a test of this
round asserts the replacement in each case (below). The code, tests, notes, `design/26` change and
finding files are `a6f3898f`; this record follows it. `src/topology/**` is untouched by the round
(`git diff --stat 698777b0..a6f3898f -- src/topology` is empty), no wire word was added, the two
deferred findings (§7) are untouched, and nothing this round touched is near the reviews' confirmed
holdings: the index's spelling is still what is staged on both planner branches, a declaration
naming a path the manifest does not govern still does nothing, and the new parser and planner code
holds no denied call and no input-dependent index or slice.

### Finding 1 (regression P2, record P2, manifest 5.1 and 5.2) — the manifest exclusion was unconditional, and the name was not the engine's to take

`CANDIDATE_STAGE_ARGV` ended in `:(exclude,top).upstroke-resolved` under a doc claiming "an
exclusion that matches nothing excludes nothing, so an ordinary capture is unchanged by it". Four
witnesses said otherwise, three reviewers landing on the area independently. **Reproduced (all
four, unrepaired tree):** an ordinary schema-4 worker in a repository that tracks a file of that
name edits it and another file, and the captured tree holds the file's *old* content with
`unresolved=[]` — the worker's work silently dropped, no refusal, no conflict, no repair
(`pr249_r3_ordinary_capture_keeps_an_existing_tracked_protocol_named_file_edit`); a source
candidate that carries a file of the name, materialized against a conflicting base, puts it in the
index, and the exclusion — which stops `add -A` *updating* the path, not an entry already there —
leaves it in the captured tree beside `c.txt`, so "the manifest is never part of a candidate" was
false (the record review's `manifest-witness.patch`); a committed `.gitignore` naming the manifest
makes `git add -A -- . ':(exclude,top).upstroke-resolved'` exit 1 "The following paths are
ignored" after the declared resolution was already staged
(`pr249_manifest_probe_ignore_and_attributes_controls`; §8: the exclusion item is matched against
the ignored paths `add` collects); and the pathspec is a directory prefix, so a tracked
`.upstroke-resolved/data.txt` edited beside an ordinary file keeps its old content in the tree while
the other edit is staged, an untracked directory's contents vanish from the candidate, and with a
conflict the directory makes the manifest read an `IsADirectory` I/O error
(`pr249_manifest_probe_directory_does_not_hide_ordinary_edits`, `_directory_in_conflict_is_io_error`).

**Decided:** the guarantee is made real for the file it is about, and the boundary is stated and
enforced for everything else that can hold the name. The worker's manifest is an untracked regular
file at the root; `ManifestName` is the one reading of what holds the name (`Absent`, `Manifest {
ignored }`, `Tracked` — the index has an entry at any stage — or `Other { what }`, a directory, a
symbolic link, or neither), and both deciders read it. `candidate_stage` runs the bare
`add -A -- .` (`CANDIDATE_STAGE_ARGV` is four words again, what the sampler always sampled) and
appends `CANDIDATE_STAGE_MANIFEST_EXCLUSION` in exactly one state, `Manifest { ignored: false }`:
an absent name needs no exclusion; an ignored manifest is kept out by the ignore rules and an
exclusion exactly naming it is what broke; a tracked file of the name is the repository's data and
its edit is staged like any other; a directory of the name holds ordinary paths. So the worker's
manifest is never part of a candidate, and no path of the repository's own is ever silently
omitted. `resolution_manifest` — called only when the index holds an entry the manifest governs —
refuses a taken name before anything is staged, `UpstrokeError::Refused` naming what holds it ("the
repository tracks `.upstroke-resolved` ... a conflict repair cannot be declared in this repository
while a tracked file holds that name"; "... is a directory, not the worker's resolution manifest
..."), because the repository's bytes are not declarations and a repository that has taken the
name cannot run the protocol; the refusal is the run's error, not the worker's failure, since no
attempt can fix it. The record's "never part of a candidate" and the body's "excluded from every
capture" now say which file they are about. The argv tripwire's `candidate_stage` row declares the
one dynamic argument and says why the sampler's child still runs the funnel's argv for the
worktree it samples (no manifest there).

**Now holds:** `a_tracked_file_of_the_manifests_name_is_the_repositorys_and_a_conflict_repair_there_is_refused`
(the regression witness's shape passing: the edit in the tree; the record witness's shape refused
with nothing staged and the index still unmerged; the same candidate picked cleanly captured as
data), `an_ignored_manifest_is_read_and_kept_out_of_the_candidate_by_the_ignore_rules_alone` (the
reviewer's control, a required clean filter on the manifest beside the ignore rule: the
declaration read, `c.txt` and `.gitattributes` staged, the manifest not, no `add` reaching it),
`a_directory_of_the_manifests_name_is_the_repositorys_and_its_contents_are_captured` (untracked
contents captured; a tracked edit captured; with a conflict, refused naming "a directory"), and in
`workspace_manager::tests`,
`the_manifests_name_is_read_by_what_holds_it_and_excluded_only_when_it_is_the_workers` (every
state, the symbolic link included, through `resolution_manifest` and `candidate_stage` with the
index read afterwards). The reviewers' witnesses on the repaired tree: the regression one passes;
the ignore-and-attributes control passes; the directory witness passes; the directory-with-conflict
control fails, because it asserted the I/O error this round replaced with the refusal.

### Finding 2 (regression P2, manifest 5.6) — a retained retry could not revise a resolution, because its manifest went unread

`capture` read the manifest only when something was still unmerged. Attempt 1 declares `resolved
c.txt`, the capture stages it and nothing is unmerged any more; a gate fails and the generation is
retained; attempt 2 corrects itself to `deleted c.txt`; the capture sees nothing unmerged, never
reads the manifest, and reports success with `c.txt` in the tree and on disk — for a worker whose
file tools cannot delete, the very case `deleted` exists for, the correction could never take
effect in that generation and every further attempt would spend allowance without applying it.
**Reproduced** through production materialization, capture, retry verification and second capture
with a retained `GateFailed` settlement between (`pr249_r3_retained_retry_can_revise_a_conflict_resolution_to_deletion`,
the reviewer's; the event sequence replays), and the manifest review's `_recapture_after_staging`
probe, which pinned the same behaviour beside the malformed-manifest half of it.

**Repaired.** What records that `c.txt` was conflicted in this generation is Git's own index:
staging a resolution over unmerged stages writes the resolve-undo extension (the record `git
checkout -m <path>` recreates a conflict from), written by `add` and `rm` alike, kept by `add -A`
and `write-tree`, and cleared by the `read-tree --reset` every fresh materialization runs first
(§8) — so it holds exactly the entries a capture of this generation resolved. `resolved_conflicts`
reads it (`ls-files --resolve-undo -z`) and keeps the paths the index still holds (`ls-files
--stage` for those paths), because a path resolved by deletion has no entry left to re-stage or
remove and a declaration naming it again would make `add` or `rm` fail on a pathspec that matches
nothing: a settled deletion governs nothing, and a file the worker recreates there is an ordinary
addition. `capture` reads the manifest when either read names anything, and `plan_resolutions`
takes both lists: an unmerged path must be declared; a resolved path may be revised — re-staged
from the working tree, or removed — and, undeclared, is left to the ordinary `add -A`; a malformed manifest
refuses every governed path, in the retry as in the first attempt, which is the condition the
manifest review asked to be stated (5.6): "a malformed manifest stages nothing" is a promise about
a capture that reads one, and a capture with nothing to govern does not. (This paragraph said a
resolved path "is left as it was when undeclared", and its running sentence still said so after
the fourth round's correction was added beside it, which the fifth round's record review found —
§16 finding 8; the `add -A` stages its edits or deletion like any path's, and the fourth round's
record review found the test in this tree that asserts exactly that — §15 finding F.) `RESOLUTION_RM_ARGV`
gains `--force`, because `git rm` refuses a path with changes staged in the index — the state a
retained retry's staged resolution is in (§8, measured). `design/26` §26.4 now says a
same-generation retry may revise a resolution an earlier attempt declared, and why. The retained
generation is process-local (T-RETAINED), so nothing crosses a process boundary that the index
does not already carry; no event changes.

**Now holds:** `a_retained_retry_revises_a_declared_resolution_to_a_deletion_and_a_settled_deletion_is_not_reapplied`
(`resolved_conflicts` empty before the first capture and `["c.txt"]` after; attempt 2's `deleted
c.txt` removes the entry and the file, one `Object.CandidateStage` execution, the tree without it;
attempt 3 with the manifest untouched stages the worker's other edit and runs no `rm` against
nothing; the log replays), `a_retained_retry_reads_the_manifest_while_the_index_holds_what_a_capture_resolved`
(the malformed manifest in the retry refused with nothing staged and the base's tree; corrected,
the new content staged; removed, an ordinary capture), and in the grammar test the planner over
both lists. The reviewer's retained witness passes on the repaired tree; the recapture probe fails,
because it asserted the deletion ignored and the malformed manifest staged.

### Finding 3 (record P2) — R5 still asserted a verification the code does not perform

R5 said "a pick killed before that removal leaves `MERGE_MSG` or its held `.lock` and fails the
verify". The counterexample is the project's own documented K2 boundary: git publishes and unlocks
the merged index, the process dies before creating `MERGE_MSG.lock`, `HEAD` stays at the base, and
there is neither index lock nor message residue, so verification returns `Reuse::Verified` — which
the exact-head deterministic late-kill test constructs and asserts for a clean and a conflicting
materialization (`src/engine/topology/dispatch/tests.rs`, the K2 assertions). The second round had
rewritten R5 for a different false claim and left this generalisation standing, and the same
sentence stood in PR9-F2's paragraph and in `docs/internals/engine/topology/run.md`'s continuation
note, which still called the re-run pick "a no-op onto an index a completed pick already left" when
production restores the base's tree first. **Corrected in all three:** R5 lists the five states a
kill can leave and says which fail the verify and which verify and converge through the restore;
PR9-F2 says the same in R6's terms; the continuation note says the pick runs afresh in either case
because the funnel restores the base's tree, and that a second pick onto the merged index is not a
no-op. R6's rewritten account was already right and is unchanged. Each keeps its earlier wording as
labelled history. No code change. Ledger row `PR249-R3-003`.

### Finding 4 (record P3 ×4, manifest 5.7) — four sentences that overstated their evidence

- **`design/26` §26.4** said a wrong declared resolution "is caught by the ordinary gates and
  review". A declaration only authorizes staging; the manifest review left the markers in `c.txt`,
  declared it resolved, and ran production capture, assessment and judgment with no gates and no
  reviewers in the plan — judgment accepted it. The sentence now says a wrong declaration *reaches*
  the ordinary gates and review, whose configured checks decide what is detected; the body's
  formulation already said "reaches".
  `a_declared_resolution_reaches_the_configured_checks_which_decide_what_they_detect` is the
  reviewers' shape with the assertion the new sentence makes (the reviewer's own probe asserted the
  retired one, and fails on the repaired tree for that reason).
- **The body said "nothing in this slice was run on the Windows guest by hand"** while §13's
  addendum recorded the sampler verified on the persistent guest at `77c0111f`; the status table's
  row I said it too. Both now say what was run there and when, and the body's validation paragraph
  names this round's guest run as well.
- **§13's introduction said every witness was re-run on the unrepaired tree** while its finding 3
  says the immediate-kill mutation was "not re-run here". The introduction now carries the
  exception.
- **The sampler addendum gave an inference as a measurement.** "Kills of a process that had not yet
  started git" is the launcher explanation; what the winguest log at `56ea88c9` holds is nine
  `None` without `MERGE_MSG`, twenty `After` with it, and one `None` *with* it — a residue read that
  found nothing written and a message read, taken separately afterwards, that found the pick's last
  write done, which is a git process still running after the kill returned. The addendum now
  separates the histogram from the mechanism inferred from it. The finding file
  `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` invoked "PR5's frozen contract" as the barrier to routing the
  census sampler through `sampled_git`; `effects/residue-classes.json` freezes the census's
  declarations and sampling N and calls its histogram machine-varying, so that route changes no
  frozen thing. The file, §9 and the ledger row now state the actual scope boundary — the census is
  PR5's evidence and kills that reach git on Windows need the guest to show all four commands'
  recoveries — without implying the observed Windows counts are frozen; the finding stays deferred
  and unfixed, as the brief directs.

### Finding 5 (manifest 5.3, 5.4, 5.5) — the grammar and the planner against their own promises

- **5.4 (P2) — quoting trimmed inside the quotes.** `unquoted` stripped the quotes and then
  trimmed, so the documented quoted form could not name an entry whose name begins or ends with
  whitespace: with the index path ` c.txt `, `resolved " c.txt "` parsed to `c.txt` and the capture
  refused the real entry (`pr249_manifest_probe_quotes_preserve_filename_whitespace`, reproduced).
  What is inside the quotes is now the path, exactly; the grammar doc says so and says when to
  quote. `a_quoted_declaration_names_an_entry_exactly_whitespace_included` (Unix, where such a
  name can exist): unquoted, trimmed away and refused as undeclared; quoted, staged. The reviewer's
  witness passes on the repaired tree.
- **5.5 (P3) — repeated colons.** `trim_end_matches(':')` accepted `resolved::: c.txt`; the grammar
  documents one colon. `strip_suffix(':')` reads one, and `resolved:::`, `resolved::` and a
  `deleted:` with no path are malformed lines in the grammar test. The reviewer's witness passes on
  the repaired tree.
- **5.3 (P2) — filesystem aliases evaded the contradictory-declaration refusal.**
  `Declaration::names` compares lexical components; for the index entry `Dir/C.txt`, `resolved
  Dir/C.txt` beside `deleted dir/c.txt` staged the resolution and ignored the deletion, and
  `resolved dir/c.txt` alone was refused as undeclared. On a case-insensitive filesystem the two
  spellings name one file, so the promised both-kinds refusal failed there; the reviewer executed
  the planner and reasoned the filesystem half, unable to reach the guest. **Repaired for case, on
  every platform:** `Declaration::names_in_another_case` folds case (`str::to_lowercase`, no tables
  the standard library lacks) for one purpose, refusing — never matching, so staging keeps the
  index's spelling. `plan_resolutions` refuses a governed path declared one way exactly and the
  other way in another case ("declared `resolved`, and `deleted` as `dir/c.txt`, a spelling that
  differs only by case"), and a path declared only in another case ("declared `resolved` only as
  `dir/c.txt`, which differs from the index's spelling by case alone; spell the path as the index
  does"), with the guard that a respelling which is itself a governed path is a different file (two
  index entries that differ only by case are two files, and each takes its own declaration). The
  rule is the same on every platform so that a manifest means one thing wherever it is read; on a
  case-sensitive filesystem it refuses a pair that would have been half-ignored, costing one
  attempt with the index's spelling in the feedback. **Established natively rather than
  inherited:** `a_declaration_in_another_case_is_refused_and_the_checkout_says_whether_it_named_the_file`
  materializes the `Dir/C.txt` conflict, asserts what the checkout's filesystem says of `dir/c.txt`
  — the file on Windows and macOS, nothing on Linux, asserted per platform so the record's
  statement is measured — and then the three refusals and the staging above; the suite ran on this
  box (Linux) and on the persistent Windows guest at `a6f3898f` (below), and macOS is CI's leg.
  **Not repaired, stated as the boundary:** Unicode normalization forms. `café.txt` composed and
  `cafe` + U+0301 decomposed are one file on a normalization-insensitive volume (macOS) and two
  names to the engine everywhere: the pair is not refused and the composed declaration is staged.
  The standard library carries no normalization tables; the fold needs a dependency
  (`unicode-normalization`, a supply-chain decision the owner takes) or a filesystem oracle
  (`fs::canonicalize` of both spellings, exact for files that exist), and neither was taken in a
  repair round. Filed as `PR249-MANIFEST-NORMALIZATION-ALIAS` (`findings/`, P3, deferred)
  with the remedy named; stated in `RESOLUTION_MANIFEST`'s grammar doc; and pinned by the same
  test, whose second half asserts per platform what the checkout does with the two spellings and
  that the engine does not refuse the pair, so that folding normalization moves the test. The
  reviewer's alias control fails on the repaired tree at its first case, the case pair it recorded
  as accepted.

### What this round did not do, and why

- **No normalization fold**, as above; a boundary with a finding, not a fix.
- **The reviewers' witnesses are adopted in shape, not verbatim.** Five of the fourteen assert the
  behaviour this round retired (the recapture probe's ignored deletion, the malformed-without-
  unmerged probe's unqualified promise, the alias control's accepted case pair, the
  directory-in-conflict control's I/O error, and the judging probe's "caught"); each has a test
  above asserting what the tree now promises, and the record says which reviewer test fails on the
  repaired tree and why.
- **No detection of the collision at run start.** A repository that tracks the manifest's name is
  refused at the first conflict repair's capture, with the run's error naming the file; detecting
  it at ingestion would be a better shape and is a follow-up, not this round's.
- **No change to what the reviews confirmed:** staging uses the index's spelling on both planner
  branches; a declaration naming a path the manifest does not govern does nothing, contradictory or
  not (the reviewer's `_non_unmerged_declarations_are_ignored` control passes on the repaired
  tree); the §7 panic policy in the parser and planner (no denied call, no input-dependent index
  or slice; `strip_suffix`, `get(tab + 1..)` and `find_map` are the new reads).
- **`PR249-KILL-SAMPLER-WINDOWS-WRAPPER` stays filed and unfixed;** only its justification changed.
  `PR8-R2-SPEND-REPLAY`, `PR8-CRASH-002` and `PR249-ANSWER-TEXT-NOT-CARRIED` are untouched.

### The Windows guest

The round's platform-shaped changes — the reserved name read through `symlink_metadata` and
`ls-files`, the resolve-undo read, the case fold, and the case-alias test's per-platform
assertions — were run by hand on the persistent guest (`windowsguest`, git 2.50.1.windows.1,
global `core.autocrlf=true`, the branch cloned from GitHub and the round's commits shipped as
bundles). The full suite at `a6f3898f` (`cargo test --all-targets --all-features`, 706 s): **2346
passed, 1 failed, 40 ignored** — the one failure
`workspace_manager::tests::every_slot_taking_primitive_refuses_a_hostile_slot_name`, which derives
its grid from the module's slot-taking `pub fn`s and had no arm for the new `resolved_conflicts`,
the same failure the Linux gate run produced at that commit (a tripwire doing its job; the arm is
`83b83f64`, four lines, test-only). At `83b83f64` the grid test, the reserved-name test, the fifteen
parser tests and the attempt tests (38 passed, 1 ignored: the quoted-whitespace test is Unix-only,
so Windows runs one fewer than Linux's 39) pass on the guest, the case-alias test's assertion that
`dir/c.txt` names the conflicted `Dir/C.txt` on that checkout and that the decomposed `café.txt`
does not among them. macOS is CI's leg: its `test (macos-latest)` run at the head the body records
is where the record's macOS statements (both spellings name one file) are established, and this
record says so rather than claiming them measured here.

## 15. Fourth repair round — the four reviews at `b2946956`

Four scoped frontier reviews at `b2946956` on 2026-09-09
(`~/review-pr249-b2946956-{regression,manifest,adequacy,record}.md`; the verdict files do not name
the model), each **CHANGES_REQUIRED**. The regression review of `698777b0..b2946956` executed two
defects the third round introduced; the manifest-contract review executed four against the tree's
own promises, two of them natively on the Windows guest; the adequacy review closed ten of the third
round's eleven findings by mutation and found the case-alias repair partial, with a mutation that
survived every scoped test; the record review found three claims the code contradicts and a P3.
Several findings were reached independently by two or three reviewers, and where their accounts
differ in detail both are in the packets
(`~/tactus-artifacts/r4-evidence/pr249-r4-regression-tu2q_r56/`,
`~/tactus-artifacts/r4-evidence/pr249-b2946956-manifest-4d4j0ldp/`,
`~/tactus-artifacts/pr249-closure-b2946956-l3ghu4r7/`); this round's own evidence — the guest logs, the mutation logs, the reviewers' witnesses run on the repaired tree — is `~/tactus-artifacts/pr249-r4-repair-b2946956/`. Every finding carrying an executed witness
is fixed whatever its label (`MAINTAINING.md` step 5); the prose findings are corrected in the same
round. The code and tests are `f97ea340`; the argument-limit witness made to run on Windows is
`7cda5dde`; the notes, `design/26` change and finding file are `5302cfc6`; this record follows them.
`src/topology/**` is untouched by the round (`git diff --stat b2946956..5302cfc6 -- src/topology` is
empty), no wire word was added, and the five deferred findings (§7, §9) are untouched and stay
deferred.

**The witnesses, before and after.** The reviewers' seven witness tests — the regression review's
two (`pr249_r4_recreated_file_survives_later_capture_with_a_spent_deletion_manifest`,
`pr249_r4_resolved_index_read_does_not_overflow_the_process_argument_limit`), the manifest
review's four (`review_b294_manifest_exclusion_must_not_hide_indexed_descendant_deletions`,
`review_b294_unicode_case_contradiction_must_be_refused`,
`review_b294_settled_deletion_must_stay_an_ordinary_path_after_recreation`, and the
`review_b294_ordinary_manifest_text_variations` control) and the adequacy review's one
(`pr249_closure_contextual_case_alias_contradiction_must_refuse_before_staging`) — were **not**
re-run on the unrepaired tree here: their retained logs are the evidence
(`head-two-witnesses.log`: 2 failed of 2 at `b2946956`, the same recreated-file witness passing at
`698777b0` in `baseline-witness.log`; `probes.log`: 3 failed, 17 passed, the three at the lines
their findings name; `contextual-case-capture.log`). All seven were applied unchanged to the
repaired tree (`5302cfc6`, whose `src/` is `7cda5dde`'s, plus the seven; the argv witness's one
three-argument `candidate_stage` call given the fourth argument, the regression review's scaffold
helper with them) and run on this box: **7 passed, 0 failed**, in three filtered runs
(`reviewer-witnesses-on-repaired-tree.log` in the packet). None is adopted verbatim; each has a test of
this round in its shape (below). Seven mutations of the repaired code, in nine runs — two of them
run against two suites — each applied to the committed tree, run against the scoped suite named,
and restored with `git checkout` before the next: every run killed the mutation (below, per
finding). Linux gate counts are the body's; the Windows guest's are in
"The Windows guest" at the end of this section.

### Finding A (regression P2, manifest 5.3) — a settled deletion revived when its path was recreated

Attempt 1 declares `deleted c.txt` and the capture removes it; a gate fails and the generation is
retained; attempt 2 recreates `c.txt` with a file write, the manifest untouched, and the capture
stages the ordinary addition; attempt 3 edits an unrelated file. Git's resolve-undo record still
names `c.txt`, the addition put an index entry back beside it, so the third capture's intersection
governed the path again, reread the standing declaration and ran `git rm --force`: the capture
succeeded and the replacement was gone from the disk and the candidate. Two reviewers reached it
independently; the same witness passes at `698777b0` and fails at `b2946956`, so the third round's
repair (§14 finding 2, governing the paths a capture resolved) introduced it. It contradicted
`resolved_conflicts`' own doc — a deleted path "an ordinary path from then on", a recreation "an
ordinary addition".

**Decided.** Nothing in the index tells that path from one a retry is revising to a deletion: after
`resolved` then `deleted` the index holds a stage-0 entry beside a resolve-undo record, and after
`deleted` then a recreation it holds exactly the same. What distinguishes them is whether the
declaration has been acted on, and the index cannot carry that (`git update-index
--clear-resolve-undo` is all-or-nothing, and a selective clearing would drop the records that let
a retry revise the other paths). So the manifest's presence is made that record: **the capture
that reads a manifest and stages what it declares removes it** (`ManifestDisposal::Consumed`,
passed by `capture` to `candidate_stage`, which runs `CANDIDATE_STAGE_MANIFEST_CLEAN_ARGV` — `git
clean --quiet --force -x -- ':(top,literal).upstroke-resolved'`, `-x` so an ignored manifest goes
the same way — after the `add -A`, inside the same `Object.CandidateStage` funnel, so that the
engine still speaks only git in a worktree it owns). A declaration is applied once; a
same-generation retry that revises a resolution writes the manifest again, one that revises
nothing writes nothing; a refused manifest stays (this said "for the worker to correct" until the
sixth round, whose record review traced the settlement: the driver dispatches a fresh generation
instead — §17 item 2), and one the capture
did not read (nothing governed) is removed with the capture too, since the fifth round (this
paragraph said it "stays where it was — it can never be read later, since only the first capture
of a generation can find unmerged entries and a generation whose first capture governed nothing
has no resolve-undo record to govern anything with"; false, and two reviewers executed the
counterexample: a generation whose first capture *did* govern something has the record, a settled
deletion re-declared while nothing was governed left the manifest standing, and the capture after
a recreation read it — §16 finding 1). The "still holds" filter
stays: a settled deletion governs nothing, a re-declared deletion does nothing, and a recreated
file is an ordinary addition — after which the path is governed once more, by whatever the *next*
manifest says, which is the worker's current intent. The worker-facing texts
(`classify::unresolved_conflict_failure`, `repair::repair_body`) say the manifest is read at capture
and removed once acted on; `design/26` §26.4 says it is read once. Rejected: a process-local memo
of applied declarations (state beside the index that every `AttemptContext` construction would
thread, for a fact the worktree can carry), and consuming at the start of a retained retry (which
would also remove a *refused* manifest the worker was told to correct).

**Now holds:** `a_settled_deletion_is_not_revived_by_the_manifest_that_made_it_once_the_path_is_recreated`
(the reviewers' three-attempt shape: the deletion consumed and `resolved_conflicts` empty; the
recreation captured and the read naming the path again — the governed-once-more state the
reviewers' third capture deleted in; the third capture one staging, the file on disk and in the
tree with attempt 2's bytes; the log replays), the retained-retry tests asserting the consumption
after each acting capture and the refused manifest kept, the revision test's attempt 3 now
re-declaring `deleted c.txt` for the settled path (not read, no `rm`; kept until the fifth round,
removed since), and
`the_manifests_name_is_read_by_what_holds_it_and_excluded_only_when_it_is_the_workers` asserting
the plain and the ignored manifest excluded and removed (it drove `Kept` and `Consumed` through
`candidate_stage` until the fifth round removed the type and the argument — §16 finding 1; this
sentence said so in the present tense until the sixth round, §17 item 6).
**Mutations:** the consumption made unreachable (`Consumed && Manifest` → `Consumed && Absent`):
6 of 43 attempt tests fail, the recreated-file test among them; the "still holds" filter removed:
2 fail (the recreated-file test at its empty-read assertion, the revision test at its own
empty-read assertion after attempt 2 — `tests.rs:3392` at `f97ea340`, before attempt 3 starts;
this sentence first said "at attempt 3's `git rm` against nothing", which the retained
`mut-M7_no_still_holds_filter.log` contradicts, as the fifth round's record review found). The
regression review's witness passes on the repaired tree.

### Finding B (regression P2) — the resolve-undo read exceeded the process argument limit

`resolved_conflicts` passed every recorded path to `git ls-files --stage` as one argument list.
The reviewer built a valid index of 13,000 resolved paths of 180 characters with `update-index
--index-info` over stdin: production `candidate_stage`, `candidate_write_tree` and both unfiltered
`ls-files` reads succeeded, and the new reader failed `Argument list too long (os error 7)`,
aborting the capture before any gate. Windows caps a command line at 32K characters, lower still.
**Repaired:** the paths still held are read from the whole index (`ls-files --stage -z`,
unfiltered) and intersected in process (a `HashSet` of the index's paths); no path is passed as an
argument. `git ls-files` has no `--pathspec-from-file` (measured); a stdin-driven `cat-file
--batch-check` of `:<path>` would need a new stdin-writing git helper and a git version for its
`-z`; the whole index is what `add -A` walks anyway. **Now holds:**
`the_resolved_conflict_read_passes_no_recorded_path_as_an_argument` (the reviewer's fixture: 13,000
three-stage records then 13,000 stage-0 records by index-info, `write-tree` and the unmerged read
as controls, the resolve-undo read counting 39,000 records, the production read succeeding with
all 13,000 paths in order). The files are not checked out: on the guest, `checkout-index` of a
180-character name fails "Filename too long" (the first version of the test did, and failed there;
`7cda5dde`), and the read consults the index. **Mutation:** the argument-list form restored: the
test fails with the reviewer's exact error, before and after the test's own repair. The reviewer's
witness (which does check the files out) passes on the repaired tree on Linux.

### Finding C (manifest 5.1, P2) — the exclusion still hid a deletion across a directory-to-file transition

The base tracks `.upstroke-resolved/data.txt`; the worker deletes the file and its directory with
file operations, writes a regular file at the name, and edits another file. `manifest_name` looked
for an exact tracked entry, found none, read the worker's file; `candidate_stage` appended the
exclusion — a directory prefix as well as a name — and the `add -A` never considered the deleted
descendant: the captured tree still held `data.txt`. The third round's static-directory tests never
performed the transition. **Repaired as the classification:** the tracked query already lists the
entries under the name (a pathspec is a directory prefix); `manifest_name` now remembers them
(`ManifestName::Manifest { displaces_directory }`), and `candidate_stage` stages what the index held
under the name first, by its own pathspec (`CANDIDATE_STAGE_DISPLACED_DIRECTORY_ARGV`: `add -A --
':(top,literal,icase).upstroke-resolved/'` — `add -u` since the fifth round, the `-A` having
collected an ignored manifest at the name and exited 1, §16 finding 2 — the trailing slash matches the index's entries under the
name and never the regular file at it, `icase` matches the directory in another case on a checkout
that folds case and restages harmlessly on one that does not, and the command runs only in that
state because with nothing under the name it exits 128; §8), then the excluded `add -A`. No pathspec
excludes a name without its descendants (`match_pathspec_item`'s prefix rule runs whatever the
magic), and staging the manifest then `rm --cached` would leave an unreferenced blob outside the
site's documented residue. A worker's file standing where a tracked directory was is the worker's
manifest: a conflict repair reads it, and the directory's deletion is captured beside the
resolution. **Now holds:** `a_manifest_standing_where_a_tracked_directory_was_hides_none_of_its_deletions`
(the reviewer's ordinary shape: the edit and a nested `sub/.upstroke-resolved` in the tree, the
worker's file and the deleted `data.txt` not, the idle manifest removed — the test asserts "a
manifest the capture did not read is removed all the same"; this said "kept", the fourth round's
behaviour, until the sixth round, §17 item 3; then the conflict-repair
shape: read, consumed, the resolution and the deletion in one staging) and the displaced state of
the manifest-name test. **Mutations:** the displaced staging removed: the attempt test fails and
the manifest-name test fails, run separately. The reviewer's witness passes on the repaired tree.

### Finding D (manifest 5.2, P2, native Windows) — a tracked file in another case was read as the manifest

The repository tracks `.UPSTROKE-RESOLVED` holding application data; a real conflict in `c.txt`;
the worker resolves it and writes `resolved c.txt` to lowercase `.upstroke-resolved`. On Windows
that write **is** a write to the tracked file. Both exact-spelling reads (`ls-files --stage` and
`--others --exclude-standard` with the literal lowercase pathspec) answered nothing while the
lowercase path existed, so the name was classified `Manifest { ignored: true }`, the repository's
file was read as declarations, no exclusion was made, and the candidate carried
`.UPSTROKE-RESOLVED` holding the declaration text — the taken-name refusal bypassed and the very
file it consumed captured. Confirmed by the reviewer's native git execution on the guest
(`windows-native.log`). **Repaired:** the index is read by name in any case
(`ls-files --stage -- ':(top,literal,icase)<name>'`; `icase` folds ASCII case, which is all the
name has), and an entry at the name or a case variant of it is `ManifestName::Tracked { spelling }`
— the repository's on **every** platform, so that a repository means one thing wherever it is
checked out: on a case-sensitive checkout a lowercase file the worker writes beside a tracked
variant is a second file, staged as one, and the protocol is refused there too rather than running
for a plan that cannot run on Windows or macOS. `resolution_manifest` refuses naming the index's
spelling and "by case alone". The taken-name refusal is still not invoked by an ordinary capture
with nothing governed — by design; such a capture stages the tracked file's edit like any other.
**Now holds:** `a_tracked_file_of_the_manifests_name_in_another_case_is_the_repositorys_on_every_platform`
(the reviewer's shape; the test first reads the tracked file back and records per platform whether
the lowercase write landed in it — true on Windows and macOS, false on Linux — then the refusal
with nothing staged and the index still unmerged; then an ordinary attempt in that repository
capturing the tracked file's edit) and the case-variant state of the manifest-name test (the same
per-platform assertion; the lowercase file staged as a second file where it is one). **Established
on the guest, not inherited:** both tests pass there with the folding branch taken (below).
**Mutations:** `icase` dropped from the query: the attempt test fails and the manifest-name test
fails, run separately, on Linux.

### Finding E (manifest 5.4, adequacy P2, native Windows) — the case fold was contextual

`names_in_another_case` lowercased whole components with `str::to_lowercase`, which is contextual:
`ΟΣ` becomes `ος` with a final sigma while `οσ` is unchanged, so `resolved ΟΣ` beside
`deleted οσ` was not read as a contradiction and production capture staged the resolution with the
deletion ignored; the same for `AΣ` against `aσ`. Three reviewers reached it independently; the
guest's filesystem reads `ΟΣ` and `οσ` as one file (`windows-cases.log`, U+039F U+03A3 against
U+03BF U+03C3; the adequacy review's native planner probe likewise), and both names are unchanged
by NFC and NFD, so this is the *case* half of the third round's alias finding, inside what that
round claimed closed — not the deferred normalization boundary. The adequacy review's
Unicode-to-ASCII mutation of the fold survived all 57 scoped tests. **Repaired:** the fold is per
character (`chars().flat_map(char::to_lowercase)`), which has no context. `ς` against `σ` is not
an alias under it, and the guest's filesystem says the same of `Σ` against `ς` (two files). **Not
closed by this:** the normalization half — `café.txt` composed against decomposed is still not read
as an alias, and `PR249-MANIFEST-NORMALIZATION-ALIAS` stays deferred with its guard test unchanged;
its file now says the case half folds per character. **Now holds:**
`a_case_alias_is_read_per_character_so_a_final_sigma_hides_no_contradiction` (the contextual fold
shown unequal on the pair; per platform whether `οσ` names the conflicted `ΟΣ`, true on Windows and
macOS; the three outcomes of the `Dir/C.txt` test: the pair refused, the lone respelling refused
naming the index's spelling, the index's spelling staged) and the grammar test's new assertions on
both Greek pairs and the `ς` boundary. **Mutations:** the ASCII fold (the adequacy review's M11 in
this round's shape): 2 fail, the sigma test and the grammar test; the contextual string fold
restored: the same 2 fail. The manifest and adequacy reviews' witnesses pass on the repaired tree.

### Finding F (record P2) — R9 promised a preservation the code does not provide

R9 said "a resolved entry the retry's manifest does not name stays as it was", §14 finding 2 said
"left as it was when undeclared", and the capture note said "stays as the previous capture left
it". The planner stages nothing for an undeclared resolved path, and the `add -A` then captures its
edits or its deletion like any path's; `a_retained_retry_reads_the_manifest_while_the_index_holds_what_a_capture_resolved`
asserts exactly that — the third resolution's bytes in the tree — and every gate stayed green,
because no gate compares prose with tests. **Corrected:** R9, §14 and the capture note (and the
planner's doc, the test notes) say what the manifest does not name is left to the ordinary
`add -A`; each keeps its earlier wording as labelled history; the test says so in its own words. No
code change; the assertion the record contradicted is the guard.

### Finding G (record P2) — the retired K2 claim had more homes than the three the third round fixed

`repair_materialize`'s doc still said "a kill before the removal leaves `MERGE_MSG` (or its held
`MERGE_MSG.lock`), which the quiescence check reads and recreates from"; the dispatch note's
`resume_open_no_attempt` paragraph listed only residue-bearing outcomes for a kill inside the pick;
the recovery test's doc called the continuation "a no-op onto the index a completed pick already
left". All present tense, none labelled history, and the deterministic K2 test asserts
`Reuse::Verified` for the merged index with no state file, clean and conflicting sources alike.
The third round's brief listed three locations and the round fixed exactly those; the list read as
the scope. **Swept, not spot-fixed.** The tree was searched by pattern, case-insensitively, over
`src/`, `docs/`, `design/`, this record and `findings/` (the frozen `src/topology/**`
excluded from correction, and its one hit read): every mention of `MERGE_MSG` (91 lines, each
read); `no-op`/`noop`/`no op` near `pick`, `materializ`, `continuation`, `index`, `K2` or
`cherry`; `quiescence check reads`, `reads and recreates`, `recreates from`, `recreated from`,
`fails the verify`, `fail(s) its verif`, `verify fails`, `fails verif`, `before the removal`,
`before that removal`, `killed before`, `kill before`, `killed inside`, `kill inside`, `Recreated {
Residue`, `Residue(MergeMsg)`, `interrupted one does`, `all of which`, `only residue`, `leaves
MERGE_MSG`, `left behind`, `state files by name`, `no state file`; and every line mentioning
`materializ` beside `verif`, `reuse`, `recreat` or `quiesc`. Hits that stated the claim, corrected:
`src/workspace_manager.rs` (`repair_materialize`'s doc: the five kill states, which two verify, and
the restore), `docs/internals/engine/topology/dispatch.md` (the resume paragraph: the same, with the
K2 state named as indistinguishable from a completed pick), `src/engine/topology/recover/tests.rs`
(the `T-REPAIR-DISPATCH` test's doc: onto the restored base tree, a second pick onto the merged
index not a no-op), and two "the three states a kill leaves" phrasings in
`docs/internals/engine/topology/recover.md` and `recover/tests.md` ("three of the states", the
completed pick named as the shape the K2 kill leaves). Hits read and found correct: the K2/K3 test's
own doc and the dispatch test notes (both describe K2 verifying), the run note's continuation
paragraph (corrected in the third round), the sampler test's comment (the bare command leaves
`MERGE_MSG`), `residue.rs`'s held-lock paragraph (K3), this record's R5, R6, PR9-F2, PR9-N2, §8,
§12 finding 3, §13 finding 4 and §14 finding 3 (the corrected accounts and their labelled
history), the sampler addendum's histogram, the findings' histogram line, and
`residue_authority.rs`'s frozen row. Each correction keeps the sentence it replaces as labelled
history. No code change. (The fifth round's record review found two copies this sweep missed,
both saying `cherry-pick --no-commit` leaves `CHERRY_PICK_HEAD`: `CHERRY_PICK_HEAD` was not among
the patterns above. Corrected under §16 finding 5, whose sweep adds it and is kept as a
re-runnable script.)

### Finding H (record P2) — the closure claim overstated itself

The body said the third round's findings were "all closed at this head" and its review evidence
that "every witnessed finding and every prose correction is repaired"; the third round's alias
finding 5.3 covered case aliasing *and* normalization, the case half was only partly repaired
(finding E), and the normalization half is deferred — accurately described, but deferred is not
closed. **Corrected:** the body's summary paragraphs, review evidence and risk section, and this
section, say what is repaired and what stays deferred, naming each deferred finding
(`PR249-MANIFEST-NORMALIZATION-ALIAS`, `PR249-ANSWER-TEXT-NOT-CARRIED`,
`PR249-KILL-SAMPLER-WINDOWS-WRAPPER`, `PR8-R2-SPEND-REPLAY`, `PR8-CRASH-002`); the summary
sentences no longer contradict the detail beneath them. (Not quite: the body's third-round
paragraph still said all seven manifest-contract findings "were repaired at `a6f3898f`" beside
its own sentence that the normalization half is not repaired, and its review-evidence paragraph
said §15 carries each witness "re-run on the unrepaired tree" while §15 says they were not; the
fifth round's record review found both — §16 finding 6.)

### Finding I (record P3) — a claimed compile-time guard does not exist

The dispatch note said `observed_kind` matches "three variants each way, matched exhaustively so a
fourth on either side is a compile error here". `Materialization` already has four variants
(`Retained`); the match is exhaustive over the three-variant funnel enum only, and a fifth wire
variant would not break it. **Corrected:** the note says which side the match guards and that the
wire's fourth variant is a retry's record no materialization observes; a guard the other way was
not added, since a dead exhaustive match over a frozen enum would be code for a sentence. No code
change.

### Finding J (record, outside scope; P2) — `design/26` §26.4 described the driver this slice replaced

§26.4 said "the current topology checkpoint driver still refuses human answer ingestion before
append. It does not yet cancel running agent processes on a decline." PR9's `ingest_answers`
(`d23d6bdb`) polls every open question before each step and the hard block waits on the source;
three tests name the ingestion; the function does not exist on `master`. `CLAUDE.md`'s rule is that
a decision changing the design changes the section in the same pull request. **Corrected:** the
paragraph says the driver ingests answers before each step (non-blocking) and at the hard block,
appends `question_answered` and folds it before any further work is selected, and runs one attempt
at a time so no agent process is running when an answer is ingested; the concurrent-driver
requirement stays. The rest of §26.4 was read against what the slice does — the rejection's one
fold and its admission field, the question rule and the decline's consequences, the dispatch and
capture paragraph (now also saying the manifest is read once), the semantic-rejection arm, the
lease and limit paragraphs — and no other sentence was found false.

### What this round did not do, and why

- **No normalization fold** (finding E): the case repair does not reach it; the finding stays
  deferred and its file is updated, not retired.
- **The five deferred findings stay deferred**, untouched in substance.
- **The reviewers' witnesses are adopted in shape, not verbatim**, each with a test of this round;
  all seven pass unchanged on the repaired tree, and none was re-run on the unrepaired tree here.
- **No process-local record of applied declarations** — the manifest's presence is the record
  (finding A); and **no consumption at the start of a retry**, which would remove a refused
  manifest.
- **No detection of a taken name at plan ingestion**, as in §14.

### The Windows guest

The two defects established natively on the guest were verified there before the push, on the
persistent guest (`windowsguest`, git 2.50.1.windows.1, rustc 1.97.1, global `core.autocrlf=true`;
the third round's clone `C:\pr9r3` and its target directory, the round's commits shipped as bundles
and checked out detached). At `f97ea340` (the code and tests): `engine::topology::attempt::tests`
**42 passed, 0 failed, 1 ignored** (Windows runs one fewer than Linux's 43: the quoted-whitespace
test is Unix-only), the four new tests among them with their folding branches taken — the
lowercase write read back from the tracked `.UPSTROKE-RESOLVED`, and `οσ` a file beside the
conflicted `ΟΣ`; `the_manifests_name_is_read_by_what_holds_it_and_excluded_only_when_it_is_the_workers`
passed with its case-variant state on the folding branch; the hostile-slot grid, the argv tripwire
and the fifteen parser tests passed; `the_resolved_conflict_read_passes_no_recorded_path_as_an_argument`
**failed** at its `checkout-index -a`, "Filename too long" for the 180-character names, which is
why `7cda5dde` builds the index without checking the files out. At `7cda5dde` the same six runs: the attempt tests **42 passed, 0 failed, 1 ignored**, the manifest-name test, the argument-limit test, the hostile-slot grid, the argv tripwire and the fifteen parser tests all passed (`guest-pr9r5.log`, `guest-pr9r5b.log` in the packet). macOS is CI's leg:
the per-platform assertions of the two new tests (and of the manifest-name test) are where the
record's macOS statements are established, and this record says so rather than claiming them
measured here.

## 16. Fifth repair round — the four reviews at `6448262e`

Four scoped frontier reviews at `6448262e` on 2026-09-09
(`~/tactus-artifacts/r5-evidence/review-pr249-6448262e-{adequacy,manifest,regression,record}.md`;
the verdict files do not name the model), each **CHANGES_REQUIRED**. The adequacy review closed
seven of the fourth round's eight findings by mutation — all fifteen production mutations caught,
64 controls and the seven original witnesses passing — and found finding A's closure partial; the
manifest-contract review executed one P2 natively on the Windows guest and reached the partial
closure independently, ran the parser and planner against sixteen native Windows filename pairs
(Turkish i, long s, Kelvin sign, micro sign, sharp s, the ligatures, the sigmas) and found no
missed case alias, so finding E stays closed; the regression review of `b2946956..6448262e`
executed one P2 the fourth round introduced; the record review found three of §15's claims still
false and four imprecisions. Where the accounts differ in detail both are in the packets
(`~/tactus-artifacts/pr249-closure-6448262e-71okmsv9/`,
`~/tactus-artifacts/pr249-6448262e-manifest-42np1f0p/`, and the regression review's
`/tmp/pr249-r5-regression-jwhhjxkk/`, copied to this round's packet as
`regression-review-evidence/`); this round's own evidence — the guest logs, the mutation logs, the
reviewers' witnesses run on the repaired tree, the sweep and its hits — is
`~/tactus-artifacts/pr249-r5-repair-6448262e/`. Every finding carrying an executed witness is fixed
whatever its label (`MAINTAINING.md` step 5); every prose finding, the P3s included, is corrected
in the same round. The code and tests are `d394d52c`; the notes, the `design/26` change, the
worker's text and the two `CHERRY_PICK_HEAD` corrections are `a664c6f5`; this record follows them.
`src/topology/**` is untouched by the round (`git diff --stat 6448262e..a664c6f5 -- src/topology`
is empty), no wire word was added, and the five deferred findings (§7, §9) are untouched and stay
deferred.

**The witnesses.** The reviewers' three witness tests were **not** re-run on the unrepaired tree
here; their retained logs are the evidence (`A-unread-declaration-reactivation.log` and its causal
control in the adequacy packet, `linux-review-probes.log` in the manifest packet, `head.log` and
`base.log` in the regression evidence: 2 failed at `6448262e`, 2 passed at `b2946956`). All three
were applied to the repaired tree and run on this box (`reviewer-witness-*-on-repaired-tree.log`):
the regression review's two tests pass verbatim; the adequacy review's and the manifest review's
each **fail verbatim** — at their attempt-2 assertion that the unread manifest is *kept*
(`unread manifest kept: … NotFound`; `assertion failed: …RESOLUTION_MANIFEST).is_file()`), the
very state this round's repair removes, not at the deletion they were written to catch — and pass
with exactly those two intermediate assertions flipped to "removed" and nothing else changed
(`…-adjusted-on-repaired-tree.log`; the adjusted texts are in the logs' headers). None is adopted
verbatim; each has a test of this round in its shape (below). Six mutations of the committed code
in twelve runs — three on this box (`mut-M*.log`), three on the persistent guest
(`guest-pr9r6-mutations.log`) — each applied to `d394d52c`, run against the scoped suites named,
and restored (on this box with `git checkout`; on the guest each mutation is a scratch commit built
by plumbing, never pushed, the guest restored to `d394d52c` afterwards): every run killed the
mutation (below, per finding). Linux gate counts are the body's; the Windows guest's are in "The
Windows guest" at the end of this section.

### Finding 1 (adequacy P2, manifest 1, record 1) — a manifest no capture read was read later

Attempt 1 declares `deleted c.txt`; the capture removes the file and consumes the manifest.
Attempt 2 declares `deleted c.txt` again while the path has no entry, and edits another file: the
governed set is empty, the manifest is not read, and — the fourth round's `ManifestDisposal::Kept`
— it stays. Attempt 3 recreates `c.txt` with a file write, the manifest untouched: an ordinary
addition, after which the index holds the path beside the resolve-undo record that survived the
deletion. Attempt 4 edits only another file, and its capture finds `c.txt` governed, reads
attempt 2's declaration, runs `git rm --force`, and reports success with the replacement gone from
the disk and the candidate; the log replays. Two reviewers executed it independently through
production capture and verified retries; the adequacy review's causal control — the same sequence
with only the stale manifest removed after attempt 3 — passes, which identifies the `Kept` arm of
`capture` (`attempt.rs:464` at that head). Two documented claims were false because of it:
`ManifestDisposal::Kept`'s doc (an unread manifest "governs nothing in any later capture of the
generation either") and §15 A's (it "can never be read later", on the argument that a generation
whose first capture governed nothing has no resolve-undo record — true, and beside the point: this
generation's first capture *did* govern something). The committed retry test preserved the
repeated-deletion state and stopped before the recreation, which is why the suite was green. This
is the defect's third round: the third round made capture govern resolved entries, so a settled
`deleted` reactivated on recreation; the fourth consumed the manifest a capture *acted on*, which
protected a consumed declaration and left an unread one live.

**The class, and what was decided.** An empty current intersection says nothing about the next
capture's: the resolve-undo record survives a deletion, and a recreation puts the path back under
it. Whether the manifest was read, consumed or ignored is a separate axis, and the last two rounds
each fixed one value of it. So the axis is removed: **the worker's manifest does not outlive the
capture that finds it.** `candidate_stage` takes no disposal — `ManifestDisposal` is deleted, so no
caller can pass the wrong one — and removes the worker's file after the `add -A` whenever the name
holds it (`ManifestName::Manifest`), whether the capture read it and staged what it declared or
found nothing for it to govern and did not read it. A declaration is therefore applied once, by the
capture of the attempt that wrote it: the manifest's presence is the record of "written since the
last capture" exactly when no capture leaves one behind. The one manifest a capture does leave is
one it *refused*, and a refusal never reaches the funnel — it stages nothing, so the next capture
of the generation governs the same non-empty set and reads the manifest again; a refused manifest
cannot lie dormant. In an ordinary attempt a stray manifest is removed the same way, which the
reservation of the name already implied. Rejected: reading the manifest whenever it exists (an
ordinary capture's stray malformed manifest would then be a refusal or an explicit ignore, moving
the third round's "a malformed manifest stages nothing *in a capture that reads it*" boundary for
no gain); governing every resolve-undo record whether or not the index still holds the path
(`git rm` against nothing again, the third round's defect, plus a rule for `resolved` on a path with
no entry); a process-local memo of applied declarations (rejected in §15 A, still). **Both false
sentences are gone:** the `Kept` doc with its type, the lifetime rule stated once in
`RESOLUTION_MANIFEST`'s doc and once in `candidate_stage`'s; §15 A corrected above with its wording
kept as history. The worker-facing texts (`classify::unresolved_conflict_failure`,
`repair::repair_body`) say the manifest is read at capture and removed by it unless refused;
`design/26` §26.4 says the manifest does not outlive the capture that finds it; the capture note
and `resolved_conflicts`' doc say the same.

**Now holds:** `a_declaration_no_capture_read_does_not_outlive_it_so_a_recreated_path_is_governed_by_no_stale_one`
walks the whole four-attempt sequence including the recreation: attempt 1 consumed and
`resolved_conflicts` empty; attempt 2's other edit captured, `c.txt` still absent, and the unread
manifest **absent** — the assertion the reviewers' witnesses fail without; attempt 3's replacement
captured and the read naming `c.txt` again; attempt 4 one staging, the replacement on disk and in
the tree with attempt 3's bytes, the other edit beside it; the log replays. With it: the
manifest-name test's first state now writes the manifest twice and asserts it excluded *and*
removed each time (the funnel neither knows nor asks whether the caller read it); the revision
test's attempt 3, the displaced-directory test's ordinary shape and the idle-manifest test assert
the unread manifest removed where they asserted it kept. **Mutation** M2, the removal made
conditional on something having been staged (the fourth round's rule in the new code's shape): 5
of 46 attempt tests fail — the four-attempt test at attempt 2's absence assertion, the revision
test, the idle-manifest test, the displaced-directory test and the ignored displaced-directory
test (the retained `mut-M2_remove_only_when_acted_on-engine__topology__attempt__tests.log`; this
list named the settled-deletion test in place of the ignored displaced-directory test until the
sixth round, and the log marks that test `ok` — §17 item 4) — and the
manifest-name test fails. The adequacy and manifest reviews' witnesses pass on the repaired tree
once their two kept-assertions are flipped, as above.

### Finding 2 (regression P2) — the displaced-directory staging exited 1 when the manifest was ignored

The repository tracks `.upstroke-resolved/data.txt` and its `.gitignore` names
`.upstroke-resolved`; the worker deletes the file and its directory with file operations, writes
the manifest at the name and edits another file. `manifest_name` classifies the file
`Manifest { ignored: true, displaces_directory: true }`, and the fourth round's
`git add -A -- ':(top,literal,icase).upstroke-resolved/'` stages the descendant's deletion **and
exits 1**, "The following paths are ignored by one of your .gitignore files: .upstroke-resolved",
so `git_ok(…)?` aborts the capture before the ordinary staging, `write-tree` and the gates — in the
repair shape with the declared resolution already staged. Introduced by `f97ea340`; the reviewer's
two witnesses pass at `b2946956` and fail at `6448262e`. **Why `add -A` did it** (git's `dir.c`,
`exclude_matches_pathspec`): `-A` walks the directory collecting the ignored paths its pathspec
names, and an item is taken to name an ignored path when it equals the path or when it is longer
and has `/` at the path's length — so `<name>/` names the ignored regular file at `<name>` as a
path under it, and `add` reports it. **Repaired:** `CANDIDATE_STAGE_DISPLACED_DIRECTORY_ARGV` is
`add -u`, which walks the index alone and never the directory; what the command stages is what the
index held under the name — deletions and modifications of tracked entries — which is all `-u`
does. Measured on git 2.43 in the reviewer's state: `-A` exits 1 after staging the deletion, `-u`
exits 0 with the same index; with nothing under the name in the index `-u` exits 128 like `-A`
did, so the command still runs only in the displaced state; against a tracked variant directory
(`.UPSTROKE-RESOLVED/data.txt` deleted, `new.txt` added beside it, the worker's file at the
lowercase name) `-u` stages the deletion and the later `add -A -- .` with the spelled exclusion
stages `new.txt`. **The trap the brief named** — what else on the capture route treats a nonzero
exit as failure when the work succeeded — was checked command by command: the per-resolution
`add -- :(literal)<path>` and `rm --quiet --force -- :(literal)<path>` act on index entries, to
which ignore rules do not apply, and `rm --force` was measured in the third round; the bare
`add -A -- .` reports no ignored path, since `.` names none exactly and nothing under it (the
third round's measurement), and the exclusion is appended only when the file is not ignored (the
third round's repair); `clean` exits 0 whether or not its pathspec matches (measured); `write-tree`
fails only on unmerged entries, which the capture checks first. **Now holds:**
`an_ignored_manifest_standing_where_a_tracked_directory_was_captures` (the reviewer's two shapes:
an ordinary attempt and a conflict repair whose declaration is read from the ignored file; one
staging, the edit, the resolution and the `.gitignore` in the tree, `data.txt` and the worker's
file not, the ignored manifest removed) and the ignored-and-displaced state of the manifest-name
test. **Mutation** M1, `add -A` restored: the attempt test fails and the manifest-name test fails,
run separately. The regression review's two witnesses pass verbatim on the repaired tree.

### Finding 3 (manifest P2, native Windows) — an untracked case variant was captured and not consumed

On the guest, in a real conflict: the worker creates an untracked `.Upstroke-Resolved`, then writes
`resolved c.txt` through `.upstroke-resolved`; both names address the same file, and the directory
lists it by the first spelling. The tracked-name query (`icase`) correctly returned nothing; the
untracked query (`ls-files --others --exclude-standard -- ':(top,literal).upstroke-resolved'`)
returned nothing too, while its `icase` counterpart listed `.Upstroke-Resolved`. So the file was
classified `Manifest { ignored: true }`, no exclusion was appended, the bare `add -A` staged
`.Upstroke-Resolved` holding the declaration text into the candidate, and the `clean` — spelt as
written — left it on disk (`windows-untracked-case.log`: `candidate-names=[.Upstroke-Resolved
c.txt]`, `manifest remains=True`). The reviewer established that the query alone was not the fix:
the exclusion spelt as written also missed the file, and with an `icase` exclusion substituted the
cleanup spelt as written still left it behind (`windows-exclusion-controls.log`). **Repaired as one
predicate, three sites.** `ManifestName::Manifest` carries `spelling`, the name as the checkout
lists the file: `manifest_name` reads `ls-files --others -z -- ':(top,literal,icase)<name>'` —
every untracked entry at the name in any case, ignored or not — and `name_spelling` picks the
entry spelt as written when it is listed and the one case variant otherwise, the same rule the
tracked lookup already applied to the index's records (exact first: on a checkout that does not
fold case an entry at the name and one at a variant are two files and the one spelt as written is
the worker's; on one that does, the two cannot both exist). `ignored` is whether that spelling is
in the `--exclude-standard` listing. The exclusion is `CANDIDATE_STAGE_MANIFEST_EXCLUSION`, now the
magic `:(exclude,top,literal)`, completed by the spelling; the removal is
`CANDIDATE_STAGE_MANIFEST_CLEAN_ARGV`, now five words, completed by
`CANDIDATE_STAGE_MANIFEST_CLEAN_PATHSPEC` (`:(top,literal)`) and the spelling; `resolution_manifest`
opens the file by the spelling. A regular file at the name that the directory walk lists under no
spelling is Git and the filesystem disagreeing and is a Git error, not a guess. Rejected: `icase`
in the exclusion and the removal (on a case-sensitive checkout they would keep a second file of the
repository's out of the candidate and delete it from disk — "never silently omits a path of the
repository's own"). **On Linux** the variant is a second file, captured as one; a candidate that
carries it makes the repository track a case variant of the name, and finding D's rule then
refuses the protocol there on every platform, which is the worker's doing and a repository meaning
one thing everywhere. The argv tripwire's `candidate_stage` row declares two dynamic arguments and
no literal (the two spelled pathspecs, each an `OsString::from(<const>)` with the spelling pushed);
`no_sampled_funnel_builds_its_argv_from_a_literal` passes with the new numbers and its controls
pass. **Now holds:**
`an_untracked_file_of_the_manifests_name_in_another_case_is_the_workers_where_the_checkout_folds_case`
(the reviewer's shape; the test first writes the variant, then declares through the lowercase name
and records per platform whether the variant's bytes are the declaration — true on Windows and
macOS, false on Linux; then the capture: the resolution staged, no spelling of the name in the
candidate and none on disk where the two are one file, the variant captured as a second file and
the manifest removed where they are two), the two untracked-variant states of the manifest-name
test (unignored, and ignored through `core.ignorecase`), and
`the_manifests_spelling_is_the_exact_one_when_listed_and_the_case_variant_otherwise`, which pins
the rule on every platform. **Established on the guest, not inherited** (below). **Mutations:** M4,
the variant preferred over the exact spelling: both manifest tests and the attempt test fail on
Linux. M3, `icase` dropped from the `--others` read; M5, the removal's pathspec spelt as written;
M6, the exclusion spelt as written: none can fail on Linux, where the exact spelling is always the
one listed — each was run **on the guest**, and each fails both the attempt test and the
manifest-name test there: M3 at the capture's Git error ("lists under no spelling of the name")
and the classification's; M5 at "and removed under that spelling"; M6 at the candidate holding the
variant.

### Finding 4 (record P2) — §15 A's impossibility claim

Corrected in place under finding 1, with the wording kept as history; the omissions discussion
below records the boundary the fourth round asserted impossible.

### Finding 5 (record P2) — the verification-claim sweep was incomplete

§15 G listed its patterns and claimed the sweep complete; the record review found two surviving
false descriptions, both saying `cherry-pick --no-commit` leaves `CHERRY_PICK_HEAD`: the dispatch
test note's kill-after-materialization paragraph ("the merge objects **and** `CHERRY_PICK_HEAD`,
which `Worktree.Verify` reads as administrative residue and refuses";
`docs/internals/engine/topology/dispatch/tests.md:224` at `6448262e`) and `residue.rs`'s
discriminator comment ("`CHERRY_PICK_HEAD` survives a *successful* `--no-commit`";
`src/workspace_manager/residue.rs:308`). `CHERRY_PICK_HEAD` was not among §15 G's patterns.
Measured again here on git 2.43 before correcting: `cherry-pick --no-commit` leaves `AUTO_MERGE`
and `MERGE_MSG` and no `CHERRY_PICK_HEAD`, on a clean pick and on a conflicting one (R5 said so).
**Both corrected** with the earlier wording kept as history. (The dispatch note's replacement was
itself false: it said the after-phase kill leaves `MERGE_MSG` and `AUTO_MERGE` "not yet cleared",
while `repair_materialize` clears them inside the funnel's primitive, before the after phase
fires; the sixth round's record review executed the case. Corrected again in §17 item 1, which
claims no completeness for this sweep.) **Swept again, with the patterns
stated and the sweep kept as a script** (`sweep-verification-claims.sh` in the packet; GNU
`grep -rniE` over `src`, `docs`, `design`, this record, `findings` and `effects`; its hit
files are beside it). Pattern 1, the fourth round's list verbatim:
`MERGE_MSG|no-op|noop|no op|quiescence check reads|reads and recreates|recreates? from|recreated from|fails the verify|fails? its verif|verify fails|fails verif|before (that |the )removal|kill(ed)? (before|inside|after)|Recreated \{ ?Residue|Residue\(MergeMsg\)|interrupted one does|all of which|only residue|leaves MERGE_MSG|left behind|state files? by name|no state file|three states`
— 353 hits, 72 of them in this record. Pattern 2, the words a claim about the pick's state file
is made of:
`CHERRY_PICK_HEAD|survives? a|administrative residue|--no-commit|Reuse::Verified|HoldsTree|quiescence`
— 299 hits, 32 in this record. And every `CHERRY_PICK_HEAD` hit with one line before and two
after, for a claim broken across lines (190 lines). Every hit outside this record was read (281
and 267 lines); this record's own hits are the corrected accounts and their labelled history, read
in the fourth round and again here. Disposition, by file: the two above, false, corrected.
`src/workspace_manager.rs` (`repair_materialize`'s doc, the five kill states; `proposal_state`'s
doc, whose `CHERRY_PICK_HEAD` claims are about the *committing* pick of `Object.ProposalCherryPick`,
which does leave it on a conflict and on an empty pick, as `proposal_state`'s test asserts),
`src/workspace_manager/residue.rs` (the K3 paragraph, the element table),
`src/engine/topology/dispatch.rs`, `src/engine/topology/dispatch/tests.rs` (the K2/K3 tests, the
sampler's `MERGE_MSG` reads), `src/engine/topology/recover/tests.rs` (the three-prefix test, the
synthetic residue construction), `docs/internals/engine/topology/{dispatch,recover,run,settle}.md`
and the `recover/tests.md` and `dispatch/tests.md` notes otherwise, `src/workspace_manager/tests.rs`
(fixtures that plant `CHERRY_PICK_HEAD` synthetically; the assertion that a successful `--no-commit`
sets none), `src/topology/effects/residue_authority.rs` (the frozen row that names
`CHERRY_PICK_HEAD`, PR9-N1, recorded and left as PR3 froze it), `design/26`, `effects/` and
`findings/`: read and found correct. Hits in the runner, agent, run-directory, fold, emit,
create and settle subsystems (containers and coordinators killed, no-ops of the fold and the CLI):
none mentions the pick, the materialization, `MERGE_MSG`, `CHERRY_PICK_HEAD`, the verify or the
quiescence check — checked mechanically as well as read. **What this sweep cannot claim:** a false
description phrased without any of these words, or spread over more than the context lines, is not
found by it; the reader can re-run the script and extend the patterns.

### Finding 6 (record P2) — the summaries still overstated closure

§15 H said the summaries no longer contradict their detail; the body's third-round paragraph still
said the seven manifest-contract findings "were repaired at `a6f3898f`" beside its own sentence
that the normalization half is not repaired, and its review-evidence paragraph said §15 carries
"each witness re-run on the unrepaired tree" while §15 says the seven were not re-run there
(their retained logs are the evidence); status row L said §13 records each witness re-run while
§13 finding 3 excepts the immediate-kill mutation. **Corrected:** the body's paragraphs say six
were repaired at `a6f3898f` and the alias finding in its case half, and distinguish the reviewers'
retained executions from this branch's reruns; row L carries the exception; §15 H above records
what it missed. A summary that disagrees with its own detail is the defect, and each summary
sentence here was read against the section it summarizes.

### Finding 7 (manifest P3) — the planner's note described the previous contract

`docs/internals/engine/topology/attempt.md:491` (at `6448262e`) headed the planner with the
two-argument signature and said "No manifest: everything refused" and "a declaration of a path
that is not unmerged is nothing", three rounds after the resolved list was added; the retained-retry
tests show an already-resolved `c.txt` accepting `deleted c.txt` and an absent manifest permitting
an ordinary capture. **Corrected:** the heading is the current first line, the paragraph states the
three inputs and the rule per governed path (declared one way, both ways, in another case, only in
another case, undeclared — unmerged refused, resolved left to the `add -A`), with the old wording
as history; the `ResolutionPlan` paragraph beside it says "governed", not "unmerged".

### Finding 8 (record, three P3s and finding 2) — four imprecisions

§14 finding 2's running sentence still said a resolved path "is left as it was when undeclared"
before its parenthetical correction; it now says "left to the ordinary `add -A`" and the
parenthesis records both rounds. §15 A said mutation M7 died at "attempt 3's `git rm` against
nothing"; the retained log shows it dying at the revision test's empty-read assertion after
attempt 2 (`tests.rs:3392` at `f97ea340`), attempt 3 never starting — the mutation was killed, the
depth was wrong, corrected with the wording kept. §9 cited the test by a name it lost in the
second round; the current name is cited with the old one as history. The witness-provenance
sentences are finding 6's.

### What this round did not do, and why

- **No change to the refused-manifest rule.** A refused manifest still stays for the worker to
  correct: it is read by the next capture by construction, so it is not the class this round
  closes, and removing it would take the worker's correction away. (The sixth round's record
  review: "by construction" holds of the capture API and not of the driver, which never makes that
  next capture — a refusal is not resumable and closes the generation; the design no longer
  promises the handoff, and the successor is filed — §17 item 2.)
- **No read of a manifest nothing governs.** Its content has no effect either way; removing it
  unread is the whole repair, and reading it would move the malformed-manifest boundary.
- **The five deferred findings stay deferred**, untouched in substance; finding E of the fourth
  round stays closed on the manifest reviewer's sixteen native pairs and is not reopened.
- **The reviewers' witnesses are adopted in shape, not verbatim**, each with a test of this round;
  the two whose intermediate assertions pin the removed behaviour are recorded as failing verbatim
  and passing adjusted, and none was re-run on the unrepaired tree here.
- **No `icase` in the exclusion or the removal** (finding 3), for the reason given there.
- **The omission the fourth round asserted impossible is recorded:** a manifest no capture read
  could be read by a later capture once the worker recreated a path the resolve-undo record still
  named. It cannot now, because no capture leaves a manifest it did not refuse.

### The Windows guest

The defect established natively on the guest, and the three mutations Linux cannot kill, were
verified there before the push, on the persistent guest (`windowsguest`, git 2.50.1.windows.1,
rustc 1.97.1, global `core.autocrlf=true`, `core.ignorecase=true`; the third round's clone
`C:\pr9r3` and its target directory, the round's commit shipped as a bundle and checked out
detached). At `d394d52c` (the code and tests): `engine::topology::attempt::tests` **45 passed, 0
failed, 1 ignored** (Windows runs one fewer than Linux's 46: the quoted-whitespace test is
Unix-only), the three new tests among them with the folding branch taken — the variant's bytes are
the declaration, the candidate holds no spelling of the name and none is on disk;
`the_manifests_name_is_read_by_what_holds_it_and_excluded_only_when_it_is_the_workers` and
`the_manifests_spelling_is_the_exact_one_when_listed_and_the_case_variant_otherwise` passed, the
former on the folding branch (`spelling: ".Upstroke-Resolved"`, one file, excluded and removed;
the ignored variant read as ignored under `core.ignorecase`); the argv tripwire, the hostile-slot
grid, the argument-limit test and the fifteen parser tests passed (`guest-pr9r6.log`). Then the
three scratch commits, each `d394d52c` plus one mutation, checked out in turn and the two scoped
tests run: M3 (`e1b4369e`), M5 (`63b6d794`) and M6 (`1c0f1b2d`) each **fail both**
(`guest-pr9r6-mutations.log`), at the assertions finding 3 names; the guest was then restored to
`d394d52c`. macOS is CI's leg: the per-platform assertions of the new attempt test and of the
manifest-name test are where the record's macOS statements are established, and this record says
so rather than claiming them measured here.

## 17. Sixth repair round — the four reviews at `697c1ac8`

Four scoped frontier reviews at `697c1ac8` on 2026-09-09
(`~/tactus-artifacts/r6-evidence/review-pr249-697c1ac8-{manifest,record,regression}.md`, and the
adequacy review's packet `~/tactus-artifacts/pr249-closure-697c1ac-ccgx_xs3/README.md`, which
carries its verdict — no `review-pr249-697c1ac8-adequacy.md` exists; the verdict files do not name
the model): regression **PASS**, adequacy **PASS**, manifest-contract and record
**CHANGES_REQUIRED**. The regression review of `6448262e..697c1ac8` executed the capture against a
held `index.lock` and against a required clean filter failing the `add -A`, nested deletions and
directory renames, the deletion and renaming of a tracked `.upstroke-resolved`, and an interruption
boundary constructed after an unread manifest's removal with only `attempt_started` durable, and
found no regression. The adequacy review closed all fourteen of the fifth round's items — eighteen
Linux mutations and the three native ones killed by committed tests, the 13,000-path witness and
the seven fourth-round witnesses passing, 29 native Windows filename pairs checked with every
observed guest alias refused — and presents the normalization remainder as open, not closed. The
manifest-contract review found eleven of twelve promises holding and one not, a lifetime sentence;
the record review found three P2s and three P3s, every one a sentence. **No defect: this round
changes documentation and nothing else.** No code, no test beyond one assertion's text, no
mutation, no witness, no sweep. The corrections are `d05462b1` (the dispatch test note,
`design/26` §26.4, three doc comments, the run note and one assertion's text); this record and the
filed successor follow. `src/topology/**` is untouched (`git diff --stat 697c1ac8..HEAD --
src/topology` is empty), no wire word was added, and the five deferred findings (§7, §9) are
untouched and stay deferred; a sixth is filed (item 2). The round's evidence — the validators'
outputs and the gate logs — is `~/tactus-artifacts/pr249-r6-repair-697c1ac8/`; the ten gates on
this box at the head the body records.

### Item 1 (record P2) — the after-kill test note asserted the opposite of what the test observes

**The false sentence.** `docs/internals/engine/topology/dispatch/tests.md:218` at `697c1ac8`, §16
finding 5's replacement: killed after the materialization, the worktree carries the merged index
"and, in this test's after-phase kill, the funnel's `MERGE_MSG` and `AUTO_MERGE` not yet cleared,
which `Worktree.Verify` reads as administrative residue and recreates from". **What the code does.**
`repair_materialize` clears both files inside the funnel's primitive (`clear_pick_state`, on each
outcome the materialization reports), and `funnel` consults `HookPhase::After` only after the
primitive has returned (`src/workspace_manager/hooks.rs`), so the after-phase kill leaves the
merged index and no state file — the state `repair_materialize`'s own doc names as this site's
after phase. The reviewer added presence assertions to the existing kill test's `after_materialize`
case in an isolated copy, production code unchanged: `MERGE_MSG present = false`, `AUTO_MERGE
present = false`, `CHERRY_PICK_HEAD present = false`, `reuse = Verified`. Read here against
`clear_pick_state` and `funnel`, not re-run. **The new sentence.** The note says the after-phase
kill leaves the merged index and no state file, which `Worktree.Verify` reads as a completed
materialization (`Reuse::Verified`) and the re-run materialization converges from by restoring the
base's tree before it picks — the same state and route as the mid-write kill the other test drives
— with both earlier wordings kept as history. **No completeness is claimed.** The fifth round
corrected two sites and re-ran its sweep with the patterns stated; the replacement it wrote at one
of them was itself false, and the reviewer's re-run of both patterns found it (361 and 320 hits at
this head) — the sweep finds the words, and interpreting a replacement is what went wrong. No sweep
was run this round. The known sites are corrected; others may remain, and the next reviewer should
read every description of the verify's outcomes as a claim to check, not as swept. §16 finding 5
and `PR249-R5-005` say so.

### Item 2 (manifest P2, record P2) — the design promised a handoff the driver does not perform

**The false sentences.** `design/26` §26.4 at `697c1ac8`: "only a manifest the capture refuses
stays, for the worker to correct, and the next capture reads it"; `RESOLUTION_MANIFEST`'s doc
(`src/workspace_manager.rs:775`): "Nothing else leaves a manifest standing"; the same promise in
`candidate_stage`'s doc, the capture comment (`attempt.rs:463`), the capture note
(`docs/internals/engine/topology/attempt.md:919`), this record's R9, §15 A and §16's omissions
bullet, and the body's fifth-round paragraph ("the next capture reads it by construction").
**What the code does — the lifetime.** The removal is the funnel's last step, after the declared
staging, the displaced staging and the `add -A` (`candidate_stage`), each of which returns a Git
error through `git_ok(…)?` before it; so a manifest also survives a capture that fails. The
manifest review executed a required clean filter failing the `add -A` after `resolved c.txt` was
staged (the index holding the resolution, the valid manifest standing), the resolution's own
staging failing, and an ordinary capture against a held `index.lock` (the unread manifest standing,
nothing staged); a capture of that worktree after the fault is removed reads it again, which the
regression review executed and which is what makes recapture work; a failure immediately before
`write-tree` leaves the manifest removed and the resolution staged. Deleting the manifest before
staging would lose the declaration the reviewers asked about, so the code is right and the
sentences claimed more. **What the code does — the handoff.** A refusal returns `Capture` with
`unresolved` non-empty and the base's tree (`attempt.rs`); `assess` reports it as the worker's
failure with feedback naming the entries and spelling the grammar; the settlement's `resumable`
is `plan.session_resume && session_id.is_some() && capture.unresolved.is_empty()` (`run.rs:1376`
to `1378`), false; `next_step` then yields no resumed retry (`resume: state.resumable`), and
`settle_failed` records `Retained` only for `RetrySameRung { resume: true }` with a session
(`settle.rs:50`), so the settlement is `Closed`, the fold closes the generation, and the next
attempt is dispatched into a fresh generation and worktree that never rereads the previous one's
manifest. A capture error propagates out of the step with no settlement; the next resume's recovery
settles the attempt interrupted (`settle_interrupted`), which the fold closes too, and closed
generations' worktrees are reclaimed. The retained-retry test corrects a refused manifest by
calling `capture` again directly, without the settlement between, which establishes the capture
API and not the handoff. Reasoned from the code, as the reviewer did; no witness executed.
**The new sentences.** §26.4 says the manifest does not outlive a capture that completes with it;
that a refusal and a capture that does not reach the removal leave it standing; that a further
capture of the worktree would read it and the driver makes none, since either outcome closes the
generation and the next attempt is a fresh generation and worktree whose worker declares afresh;
and that the in-place correction is a recorded successor, not a promise — the same in
`RESOLUTION_MANIFEST`'s and `candidate_stage`'s docs, the capture comment, the capture note, R9,
§15 A, §16's bullet and the body, each with the old wording as history; and
`docs/internals/engine/topology/run.md`'s `resumable` paragraph, which said "both halves" of a
three-conjunct expression, names the third (checked against the two tests that bake that note,
which pin its `Settled {` and `disposition` sections and not this one). **Not changed:** the
driver — making a refused capture resumable is new behaviour, outside this slice — and the
worker-facing texts (`classify::unresolved_conflict_failure`, `repair::repair_body`), production
strings that still say a refused manifest "stays for you to correct"; both are named in
`PR249-REFUSED-MANIFEST-HANDOFF` (P3, deferred, `findings/`; §9 PR9-N7), filed as the
successor because the design's reason for keeping a refused manifest was that correction, and
without it the retention serves nothing the driver reads.

### Item 3 (record P2) — §15 C said the idle manifest is kept

**The false sentence.** §15 C's "Now holds" (`reviews/2026-09-08-pr9-record.md:1462` at
`697c1ac8`) described the displaced-directory test's ordinary shape as "the idle manifest kept",
current wording beside the fifth round's `add -u` update. **What the test does.** It asserts the
manifest absent — "a manifest the capture did not read is removed all the same"
(`src/engine/topology/attempt/tests.rs:4235`) — as §16 finding 1 says it does. **The new
sentence.** "The idle manifest removed", with the assertion quoted and the old word kept as history.

### Item 4 (record P3) — §16's M2 account named the wrong fifth test

**The false sentence.** §16 finding 1 (`record.md:1745`) listed "the settled-deletion test" among
M2's five failures. **What the log shows.**
`~/tactus-artifacts/pr249-r5-repair-6448262e/mut-M2_remove_only_when_acted_on-engine__topology__attempt__tests.log`
marks `a_settled_deletion_is_not_revived_by_the_manifest_that_made_it_once_the_path_is_recreated`
`ok`; the five `FAILED` are the four-attempt test, the revision test, the idle-manifest test, the
displaced-directory test and the ignored displaced-directory test
(`an_ignored_manifest_standing_where_a_tracked_directory_was_captures`). Read from the retained
log, not re-run; the total of five and the mutation's kill stand. **The new sentence.** The five
named from the log, the wrong name kept as history.

### Item 5 (P3) — a retired assertion text survived

**The false sentence.** `src/engine/topology/attempt/tests.rs:3176`: "a resolved path with no
manifest stays as the previous capture left it". **What the code does.** An empty `ResolutionPlan`
promises no explicit resolution operation, and the `add -A` then captures the path's edits or its
deletion like any path's (§15 finding F;
`a_retained_retry_reads_the_manifest_while_the_index_holds_what_a_capture_resolved` captures the
newer bytes). **The new sentence.** "A resolved path with no manifest is left to the ordinary
add -A, the plan naming nothing" — the assertion's text only; what it asserts is unchanged.

### Item 6 (record P3) — §15 A cited an API that is gone

**The false sentence.** §15 A's "Now holds" (`record.md:1410`) said the manifest-name test drives
"`Kept` and `Consumed` through `candidate_stage`". **What the code does.** `ManifestDisposal` and
the argument were removed in the fifth round (§16 finding 1); the test asserts the plain and the
ignored manifest excluded and removed. **The new sentence.** Says that, with the old wording as
history.

### The ledger

Six rows, `PR249-R6-001` to `PR249-R6-006`, each bound to `697c1ac8` and the reviewed location;
`PR249-R5-005` reworded — its passages are corrected at this head, the dispatch note's a second
time, and it claims no completeness for the sweep (item 1); `PR249-REFUSED-MANIFEST-HANDOFF`
added, deferred, with its file. The "three deferred findings" the body's scope named are four.
One body sentence found while trimming the body to GitHub's length limit is withdrawn as well:
Risk's compatibility paragraph said "a resume replays capture from the same worktree", and a
resume does no such thing — recovery settles every in-flight attempt interrupted and closes every
retained generation (`settle_interrupted`, `close_retained_idle`), so the attempt after a resume is
a fresh generation; the manifest is read from the worktree at capture and is never an event field,
which is all the sentence had to say.

### What this round did not do, and why

- **No code, no driver change.** The handoff §26.4 promised would be new behaviour — a refused
  capture made resumable — and is filed, not built.
- **No sweep, and no completeness claim** (item 1).
- **The worker-facing strings are untouched** — production text, out of a documentation-only
  round; named in the filed finding.
- **The reviewer's added assertions are not adopted.** The kill test's oracle is the independent
  materialization, and the state files' absence after the primitive is `repair_materialize`'s
  documented after phase; the executed observation is recorded here and in the note.
- **The five deferred findings stay deferred**, untouched.
- **No Windows guest run:** nothing platform-shaped changed; CI's legs re-run the tree.
