# Gate 5 — complete crash/resume across the max_parallel = 1 topology (sixth run)

**Frozen input range: `d724fb162f76d86b88077cf51011013f1cfbefb8`** — master carrying #318 (the clause-8 remedy) and #320,
on top of merged PR10 on G4 and every fix run 4's report judged. Proved on entry in `/srv/worktrees/g5-run6`: `HEAD` is
the range, the index and the worktree clean, the range is `origin/master` after a fetch, and the range's merge commit has
exactly the tree `git merge-tree` gives its two parents (**F8**, `range/identity.log`, `range/merges-in-range.log`).
This report's own later commits change no product byte; they are not the gate input.

**Verdict: G5 passes on this range.** All nine positive clauses of the pass rule are established and none of the three
prohibited claims is made (§16); the limits each grade carries are printed beside it. This is the report after its one repair round (§17), and it awaits
its final independent review.

**This text is the report after its one repair round.** Its first head, `e0f9a1cd`, had three independent reviews (VERIFY
PASS; RECORD and REGRESSION changes required, REGRESSION with one executed P1, R6-REG-01: ST-18's kills between the
execution-root step's cleanup effects, with the staged-intent leftovers planted, had never been executed). This round
executed those kills at the range, with every leftover kind at every occurrence position the step takes, for Complete and
Halted, then a fresh resume after each (**F60**), and a first-bad control that makes every leftover cut fail
(**F61**); it measured the stale adapter exclusion behind ST-07's counters on both hosts (**F59**) and
folded in every other finding. §17 maps each review item to the place and the receipt that answer it.

**How to read this report.** Every count a grade rests on is a figure in §0's table, generated by `figures/figures.py`
from the saved file named beside it, and the prose cites the figure (**F**_n_) rather than retyping it. The prose states
other counts in two ways only: in tables and lists a generator printed and the report embeds verbatim (§5's per-set table,
`mutations/summary-table.md`; §14's carve-out list and its † count, `report/fills.py` over `q/q5-domains.json`), and in the
few counts §0's closing note names, each with the file it was checked against. F1–F58 are run 6's figures, with the ids the
reviews cite; the repair round appended F59 onward. Paths are relative to the evidence directory `~/tactus-artifacts/g5-evidence-d724fb1/` unless they begin `src/`,
`effects/`, `findings/`, `reviews/`, `design/`, `docs/`, `examples/` or `.github/`, which are repository paths at the range
(`report/selfcheck.py` checks both kinds, with one stated exemption: this report's own path exists at its head and not at
the range). "Executed" means this run ran it and kept the receipt; "reasoned" means it was read, and the sentence says so.
Historical runs' numbers appear only as labelled cross-checks, never as this range's evidence.

**Where the evidence was made.** On the build box every cargo command went through `upstroke-build` on a private target
base, never with `CARGO_TARGET_DIR` set; the Windows guest has no such wrapper, and there cargo ran on this run's own target,
named by `CARGO_TARGET_DIR` (§2.2). On the build box every suite, merge check, probe, group invocation, sampler loop, mutation
campaign and gate attempt ran through `run.py` (kept in the evidence as `scripts/run6/run.py`), which starts it in its own
session with default dispositions for SIGHUP, SIGINT, SIGQUIT, SIGTSTP, SIGTTIN, SIGTTOU and SIGPIPE and writes the tree
identity, clock and load into the log header. Its header's `child SigIgn` line does **not** record the command's mask: it is
read inside run.py's `bash -c` wrapper, and bash ignores SIGQUIT for itself, so the line reads `0x4` while the command the
wrapper `exec`s runs with every one of those signals at its default, `0x0` (run.py header `child SigIgn` 0000000000000004 (the `bash -c` wrapper's), the command's own 0000000000000000; run-r1.py header 0000000000000000 (the exec'd command's); across this round's 150 run-r1.py logs in the evidence: 0000000000000000 in 150 (**F70**)). This round's runner,
`scripts/repair-r1/run-r1.py`, execs the command directly and records the command's own mask after the exec. The
boundary preflight's lanes are the one exception: their driver (`preflight/run-route.py`) ran Clippy, the effects censuses and
the reviewers' named tests directly, and none of those tests reads a signal disposition. Each receipt names the tree it ran in, and every tree is proved at the range:

| tree | what ran there | target base |
|---|---|---|
| `/srv/worktrees/g5-run6` (the worktree) | the Linux cumulative suites S1–S3, the ST-07 merge checks, the group invocations, the samplers; the ten gates, at this report's own head (§15.2) | `/mnt/ramtarget/iso-g5-run6` (a symlink to a disk-backed directory) |
| `~/g5r6-work/range-tree` | read-only checkout every generator and checker reads (`range/range-tree.log`) | none |
| `~/g5r6-work/probe-tree` | the instrumented clause-2 probe (`audit/probe/instrument-receipt.log`) | `iso-g5r6-probe` |
| `~/g5r6-work/pf-tree`, `pf-tree2` … `pf-tree7` | the boundary preflight's routes, one lane per clone (each receipt names its clone), and the population probe | `iso-g5r6-pf`, `-pf2` … `-pf7` |
| `~/g5r6-work/mut-tree`, `mut-tree2` | the mutation campaign; `mut-tree2` ran the `pr10` set in parallel with its own `TMPDIR` | `iso-g5r6-mut`, `-mut2` |
| `~/g5r6-work/aux-tree` | the projection and census-instance probes | `iso-g5r6-aux` |
| `~/g5r6-work/r1-cut-tree` (the repair round) | the ST-18 kill-cut probe, its first-bad control and restored run (`st18-cuts/`); the Linux ST-07 checks over raw and filtered export copies (`st07/adapter-exclusion/`) | `iso-g5r6r1-cut` |

Every scratch tree is a `git clone` of the range with its remote removed, checked out detached at the range with a clean
status before its first use, and every mutation or probe in it is restored and proved against the range's blobs.
The Windows guest ran from its own tree `C:\upstroke-g5-run6`, cloned from a git bundle of the range whose sha256 was verified on both sides and checked out detached at the range with a clean status, with its own target `C:\upstroke-g5-run6-target` (§2.2). The repair round's native ST-07 checks ran in a tree of their own, `C:\upstroke-g5r6r1`, cloned from the same bundle and
removed afterwards (**F73**).

**Deviations from the run brief, stated.** None of the stale-path traps earlier briefs carried recurs in this one: the
handover, the target base, the gate runner's argument and CI's commit all name this run. Two instructions were read
narrowly on purpose: the flake rule's one re-run was used once, for S1 (§2.1), and nothing else was re-run under it;
and the historical grading rules and run 4's reviewed table were read only after this run's own 168-row audit was complete
and mechanically checked, as a cross-check (§8.3). The guest steps waited for the owner's approval to free space on the shared
guest, which root obtained and carried out (§2.2). This run's own errors are kept with the evidence and stated where they bear:
the preflight's defective first setups and the out-of-memory kills its driver caused (§3), a guest step log overwritten by the
log it fetched (`guest/guest-st07.NOTE.md`), and one resource snapshot written into the worktree by swapped arguments and moved,
verbatim, into `logs/resources.log`. The repair round's own: the kill-cut probe's first version resumed twice into one
report file its reader expects to hold one line (`st18-cuts/setup-failed/NOTE.md`; every assertion before that read had
passed), and its driver once refused to start over its own earlier scratch; the first Linux ST-07 attempt ran in a clone
that no sampler had yet written PR5's residue histogram into (`st07/adapter-exclusion/setup-failed/NOTE.md`). Three
intermediate probe versions ran in full and are kept (`st18-cuts/prelim-v2/`, `st18-cuts/prelim-v3/`, `st18-cuts/prelim-v4/`);
only the last version's runs are counted.

## 0. The figures

| id | figure | value | source (evidence directory) | derivation |
|---|---|---|---|---|
| **F1** | the design packet v17 read by this run | sha256 `a34417d6248edbd80ecdabd7fb9a4b8bcd3ff1d98270c67bd43c25f9fb0b712d` | `spec/copies.sha256` | sha256sum over spec/copies/ (byte copies of ~/tactus-artifacts/*); equal to the run brief's digest |
| **F2** | v16 -> v17: gates[4] equal; leaves whose values differ | gates[4] equal: True; 2 leaves differ (`/cumulative_review_gates/standing_questions/4`, `/packet_version`) | `spec/regen-spec.log` | spec/regen-spec.py: full leaf-set walk of both packets |
| **F3** | derived files on disk vs this regeneration | definition: every section byte-identical but `source` (provenance line, differs by design), `adversarial_fault_tests` (differs in text, equal parsed as json); questions: 6 of 6 identical | `spec/regen-spec.log` | spec/regen-spec.py, section by section |
| **F4** | counts from the regenerated entry | 5 integrated invariants, 6 ownership rows, 7 adversarial groups, 5 required artifacts | `spec/gate4-v17.json` | len() of each field |
| **F5** | first-parent merges from run 4's range 9bb177ea to this range | 5: #315, #316, #317, #320, #318 | `range/merges-in-range.log` | git log --first-parent 9bb177ea..d724fb16 |
| **F6** | src/ changed from 9bb177ea to this range | 34 files changed, 15267 insertions(+), 3732 deletions(-) | `range/merges-in-range.log` | git diff --stat 9bb177ea d724fb16 -- src |
| **F7** | the registry blob at 9bb177ea and at the range | 1aeee56fc73ee033072fad814f9733347faca6f0 at both | `range/merges-in-range.log` | git rev-parse <commit>:effects/sequential-registry.json |
| **F8** | the range merge commit's tree against git merge-tree of its two parents | equal (`419005f6…` both) | `range/merges-in-range.log` | git merge-tree --write-tree 9c5c2e93 6c5e093f |
| **F9** | schema-4 engine symbols in the shipped debug binary | 0 for each of 11 entry points (recover::run_recovery_order, create::create_run, startup::startup_census, recover::settle_interrupted, candidate::complete_promotion, attempt::AttemptContext::settle_interrupted, finalize::finalize, run::TopologyRun, integrate, fold::TopologyFold::replay, log::EventLog::open); `engine::topology::` symbols: 2, of which legacy-engine trait impls 2 | `range/shipped-binary-symbols.log` | nm -C over pf-tree's debug/upstroke, grep per symbol |
| **F10** | production files changed since 9bb177ea, compared without comments and the lint prologue | 22 identical in code; code changed: `src/effects.rs`; moves: `src/agent/mod.rs` → `src/agent/adapter.rs` (0 code lines only before, 5 only after, each listed); `src/runner/mod.rs` → `src/runner/contract.rs` (3 code lines only before, 7 only after, each listed) | `range/production-diff.txt` | range/production-diff.py (git show at both commits) |
| **F11** | files by the builds that read them (rustc dep-info) | bin-only 1, example-only 1, production-lib 140, test-only (lib test harness) 47 (of 189 tracked .rs) | `q/domains/domains.json` | q/domains/domains.py over the four .d files in q/domains/depinfo/ |
| **F12** | Linux S1 (clean run) | exit 101; lib 2810 passed / 2 failed / 92 ignored | `suite/linux-s1-clean.log` | libtest result lines; run.py footer |
| **F13** | Linux S1, its one permitted re-run | exit 0; lib 2812 passed / 0 failed / 92 ignored; bin 10/0/0 | `suite/linux-s1-rerun.log` | libtest result lines; run.py footer |
| **F14** | Linux S2 (export 1) | exit 0; lib 2812 passed / 0 failed / 92 ignored; bin 10/0/0 | `suite/linux-s2-export1.log` | libtest result lines; run.py footer |
| **F15** | Linux S3 (export 2) | exit 0; lib 2812 passed / 0 failed / 92 ignored; bin 10/0/0 | `suite/linux-s3-export2.log` | libtest result lines; run.py footer |
| **F16** | S1's failures, from libtest's closing list | `a_resume_over_a_creation_that_stopped_after_creating_its_integration_ref_adopts_it` at `src/engine/topology/recover/tests.rs:17727:10` (cleanup-lease refusal); `real_docker_kill_on_an_already_exited_container_is_tolerated` at `src/runner/container/tests.rs:3436:5` (docker 200-observation) | `suite/linux-s1-red-classification.json` | classify-red.py over the S1 log |
| **F17** | the two Linux exports | 552 and 552 observation files; census summaries identical (sha256 `10991b391643ee97cd0a04ad121bb379109c7d96d1da6f3251be9e7b0e5a388f`); the six ledger tables byte-identical | `observations-export{1,2}/, census-export{1,2}/, ledger-export{1,2}/` | ls | wc -l; sha256; cmp per table |
| **F18** | ST-07 merge check over Linux export 1 (the unchanged checker's raw counters) | failures 0; entries 180, witnessed claims 163, records 434, funnel executions 429 as the checker counts them, which includes the one record of the adapter test #318 moved to `runner::contract::tests` (production-funnel records without it: 428), inventory 68 | `st07/st07-summary-export1.json` | the ignored test over a copy of the export (st07/merge-check.sh); one test selected; the population without the moved module from st07/adapter-exclusion/copies.json |
| **F19** | ST-07 merge check over Linux export 2 (the unchanged checker's raw counters) | failures 0; entries 180, witnessed claims 163, records 434, funnel executions 429 as the checker counts them, which includes the one record of the adapter test #318 moved to `runner::contract::tests` (production-funnel records without it: 428), inventory 68 | `st07/st07-summary-export2.json` | the ignored test over a copy of the export (st07/merge-check.sh); one test selected; the population without the moved module from st07/adapter-exclusion/copies.json |
| **F20** | CI merge_group run 36091055988 at the range | conclusion success; jobs lint success, lint (macos) success, lint (windows) success, msrv (Rust 1.85, macos-latest) success, msrv (Rust 1.85, ubuntu-latest) success, msrv (Rust 1.85, windows-latest) success, test (macos-latest) success, test (ubuntu-latest) success, test (winguest) success, upstroke-ci success | `ci/run-36091055988.json` | gh run view --json |
| **F21** | CI push run 36091947216 at the range | the newest of the SHA's 2 CI runs (created 2026-09-25T03:49:51Z); conclusion failure; failed jobs: test (winguest), upstroke-ci | `ci/run-36091947216.json` | gh run view --json |
| **F22** | CI suite counts at the range (lib passed/failed/ignored) | merge_group: ubuntu 2812/0/92, macOS 2747/0/70, winguest 2638/0/67; push: ubuntu 2812/0/92, macOS 2747/0/70, winguest 2637/1/67 | `ci/ci-*-job-*.log` | the lib harness's unfiltered result line in each job log |
| **F23** | the clause-2 probe: tests run one process each, non-zero exits | 2812 tests; 2 non-zero (`every_unix_containment_point_is_gated_on_unix_and_not_on_one_unix`, `every_unclassified_production_file_states_each_governed_lint_or_inherits_its_forbid`) | `audit/probe/results.jsonl` | run-each.py over the instrumented probe tree |
| **F24** | rows with a firing; with firing, recovery and a replay pair | 123 of 168; 89 | `audit/probe/analyze.log` | analyze.py |
| **F25** | probe completeness against the Linux observation export | before/after 136/136; points fired 27/32; exported but missed 0; Windows-only 5 | `audit/probe/reach-check.log` | reach-check.py over observations-export1 |
| **F26** | the plant domain from this run's probe | 124 tests | `audit/grading/plant-domain-d724fb1.json` | plant-domain.py |
| **F27** | the per-coordinate audit (grade-check.py) | 168 rows: established 168; (i) K 75, E 35, P 37, = 21, none 0; (ii) yes 168; (iii) yes 137, V 31, no 0; checker failures 0 | `audit/grade-check.log` | audit/grade-check.py over graded-B1..B6.json |
| **F28** | rows carrying an adjudicated limit (no grade changed) | 79 | `audit/review/adjudication.json` | audit/review/adjudication.py |
| **F29** | cross-check against run 4's reviewed table (never evidence) | all three obligations credited: this run 168/168, run 4 168/168; same witness on 159/168 rows; construction or replay label differs on 7 rows | `audit/review/cross-check-run4.txt` | audit/review/cross-check-run4.py |
| **F30** | the census family | `prefix`: 20,000 states, truncated (reported); `seeded chain: sequences, repairs`: 25 states, closed; `seeded fan-out: sequences, repairs, lineages`: 31 states, closed; `seeded join: sequences, repairs, lineages`: 75 states, closed; every bound reached over the union: true | `census-export2/census-summary.json` | the suite's census summary export |
| **F31** | coverage assertion 5's named instances over the union (this run's probe) | a be refused under halt=409 b halted after declined park=94 c parked refused with backoff=1167 c complete refused with backoff=1167 d halted accepted with backoff=14 d budget accepted with backoff=312 states accepting some outcome=4947 unfinished states=17392 | `probes/census-instances/probe-run.log` | the temporary probe test in aux-tree, restored after |
| **F32** | projection equivalence: live snapshots vs replay of the same durable bytes; replay twice per prefix | 6/6 live snapshots equal (prefixes 2–7); 7/7 prefixes replay twice equal | `probes/projection/probe-run.log` | grep G5R6PROJ |
| **F33** | two-crash proof, positive: the log after the kill / after the simulated power loss | 8 lines, 4701 bytes, merge_prepared 1, task_merged 1 (sha256 `3665169f9a2cc64c…`) / 7 lines, 4492 bytes, merge_prepared 1, task_merged 0 (sha256 `9bc0316d6346e0c1…`) | `probes/projection/probe-run.log` | grep G5R6BYTES |
| **F34** | two-crash proof, negative: the unsynced merge_prepared before the barrier / after the loss | 7 lines, 4501 bytes, merge_prepared 1, task_merged 0 (sha256 `b32b761b5eb7379f…`) / 6 lines, 3992 bytes, merge_prepared 0, task_merged 0 (sha256 `3d6ec82d729f46f1…`) | `probes/projection/probe-run.log` | grep G5R6BYTES |
| **F35** | the per-resource ledger tables (Linux export 1) | `ledger-budget-exceeded.md` 54 rows, 54 `yes`; `ledger-complete.md` 54 rows, 54 `yes`; `ledger-halted.md` 54 rows, 54 `yes`; `ledger-no-run-finished-then-complete.md` 54 rows, 54 `yes`; `ledger-no-run-finished.md` 54 rows, 54 `yes`; `ledger-parked.md` 54 rows, 54 `yes` | `ledger-export1/*.md` | rows beginning `| R`; the Holds column |
| **F36** | the finding ledger at the range | 357 files; {"P1": 8, "P2": 146, "P3": 203}; {"deferred": 351, "accepted-risk": 6}; `open`: [] | `ledger/ledger-at-range.txt` | ledger/ledger-at-range.py (git show at the range) |
| **F37** | every finding placed by bearing | on-G5-evidence-conditions 62, on-a-mechanism-G5-certifies 142, outside-G5 153 (of 357) | `ledger/assess-findings.json` | the assessment over every file |
| **F38** | the carve-out v17 names (file-level allowance of a governed lint) | 43 files: example-only 1, production-lib 27, test-only (lib test harness) 14, bin-only 1 | `q/q5-domains.json` | q/prologue.py + q/q5-domains.py |
| **F39** | allowlist rows (not v17's wording) | 51 rows, 45 with allows; not a file-level allowance: `src/agent/bin.rs`, `src/agent/proc/test_support/readiness.rs` | `q/q5-domains.json` | effects/allowlist.toml at the range |
| **F40** | classified modules split by the carve-out | 54: 29 inside, 25 outside; outside, forbidding all three lints in production: 25 | `q/q5-domains.json` | CLASSIFIED_MODULES at the range |
| **F41** | production hosts and their fences | 141 hosts; in the carve-out 28; outside 113, of which 109 may hold code; non-forbid pairs among those: 0 | `q/q5-domains.json` | q/q5-domains.py |
| **F42** | carve-out files allowing each governed lint at file level; group statements in any prologue | `clippy::disallowed_macros` 19, `clippy::disallowed_methods` 42, `clippy::disallowed_types` 23; statements of `clippy::all`, `clippy::style` or `warnings` in any prologue: 0 | `q/q5-domains.json` | q/q5-domains.py over q/prologue-at-d724fb1.json |
| **F43** | unstated classified pairs (the pin) | 29 in 20 files, all inside the carve-out: 20 deny from the command line's `-D warnings`; 6 forbid from src/engine/mod.rs; 3 deny from src/agent/mod.rs | `q/q5-domains.json` | q/q5-domains.py |
| **F44** | Clippy over a macro-generated allowance of each lint in every production host | 423 pairs over 141 hosts: E0453 338, accepted 85 (10 in the 4 declaration-only roots, 75 in carve-out files, 0 elsewhere); agreement with the prologue reading 423/423 | `preflight/population/population-result.json` | preflight/population/population-probe.py |
| **F45** | the boundary preflight | 167 executed specs and 2 `--nocapture` re-runs (of `r5-main/08-batch-versus-individual`, `r5-main/09-batch-diagnostic-controls`), covering 242 of the 502 catalogued records; verdicts: 152 as expected; 4 legitimate for its own named checks, which pass; another in-range guard refuses the shape; 13 an instrument experiment whose outcome matches its original record; restorations proven 169/169 | `preflight/summary.json` | preflight/summarize.py over preflight/routes/*/receipt.json |
| **F46** | Cargo.lock from G4's range 7e0110a1 to this range | packages at G4: 64; added none, removed none, changed none | `q/dependency-review.log` | tomllib over git show <commit>:Cargo.lock |
| **F47** | entries left in each Linux suite's private TMPDIR after it ended | suite-s1 1014, suite-s1-rerun 1014, suite-s2 1014, suite-s3 1014 entries; not named `upstroke-*`: suite-s1 0, suite-s1-rerun 0, suite-s2 0, suite-s3 0 | `logs/tmpdir-leftovers.txt` | ls -A | wc -l per TMPDIR; the entries not named upstroke-* counted per TMPDIR |
| **F48** | the mutation campaign | 168 recipes in 7 sets: 148 killed, 19 survived (each explained in the table), 1 VOID; kills on a flake fingerprint 0; backups checked against the range 169; problems 0 | `mutations/summary.json` | mutations/summarize.py over the chain logs and every run-*/ log |
| **F49** | the group invocations (passed/failed/ignored; exit) | group1 86/0/0 exit 0 (green, every one of 86 exact filters selected); group2 2/0/0 exit 0 (green, every one of 2 exact filters selected); group3 55/0/0 exit 0 (green, every one of 55 exact filters selected); group4 13/0/0 exit 0 (green, every one of 13 exact filters selected); group5 36/0/0 exit 0 (green, every one of 36 exact filters selected); group6 17/0/0 exit 0 (green, every one of 17 exact filters selected); group7 40/0/0 exit 0 (green, every one of 40 exact filters selected); group8 199/0/0 exit 0 (green); group9 16/0/0 exit 0 (green, every one of 16 exact filters selected) | `groups/summary.json` | groups/summarize.py over groups/logs/*.log |
| **F50** | the Linux sampler loops (five runs of each sampler alone) | `sampled_git_add_and_write_tree_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` 5/5 ok; tree dirty after a run: 0 | `samplers/summary.json` | samplers/summarize.py over samplers/linux-loops.log |
| **F51** | the residue-class evidence record | 9 declared residue sites, 24 (site, element) pairs, 24 constructed, classified and recovered in the synthetic half; 110 histogram site rows, 0 unclassified or unrecovered; runs with no histogram 0; runs observing no internal residue 0 | `residue/residue-record-summary.json` | residue/residue-record.py |
| **F52** | S-guest-1, the Windows guest's plain suite | exit 0; lib 2638/0/67 (passed/failed/ignored); harnesses 2638/0/67, 12/0/1, 0/0/0; closing list empty; beside the harness lines, 6 one-test result lines printed by child processes the tests started | `guest/guest-suite-s1.log` | guest/summarize.py (CARGO-EXIT, harness result lines, closing list, child result lines) |
| **F53** | S-guest-2, the Windows guest's export suite | exit 0; lib 2638/0/67 (passed/failed/ignored); harnesses 2638/0/67, 12/0/1, 0/0/0; closing list empty; beside the harness lines, 6 one-test result lines printed by child processes the tests started | `guest/guest-suite-s2.log` | guest/summarize.py (CARGO-EXIT, harness result lines, closing list, child result lines) |
| **F54** | ST-07 merge check over a copy of the guest export (the unchanged checker's raw counters) | one test selected, exit 0; entries 180, failures 0, funnel_executions 423, host windows, inventory 68, records 428, witnessed_claims 164; funnel_executions as the checker counts them includes the one record of the adapter test #318 moved to `runner::contract::tests` (production-funnel records without it: 422) | `guest/exports/export/st07-summary.json` | the ignored test over a copy of the guest export (guest/guest-run.sh st07); the population without the moved module from st07/adapter-exclusion/copies.json |
| **F55** | the guest sampler loops (five runs of each sampler alone) | `sampled_git_add_and_write_tree_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` 5/5 ok | `guest/summary.json` | guest/summarize.py over guest/guest-samplers.log |
| **F56** | C: free on the Windows guest, bytes, before → after each step | prepare: 1,738,297,344 → 1,677,172,736; s1: 1,677,221,888 → 1,293,717,504; s2: 1,349,840,896 → 1,278,910,464; st07: 1,331,216,384; samplers: 1,331,146,752 → 1,332,187,136; fetch: 1,333,456,896 → 1,329,639,424; cleanup: 1,334,136,832 → 1,719,189,504 | `guest/guest-*.log` | the driver's Get-PSDrive C measurement before and after each step (st07's "before" line was overwritten: guest/guest-st07.NOTE.md) |
| **F57** | this run's guest TEMP after each guest suite, before it was emptied | after s1: 966 entries (0 not named `upstroke-*`), 39,086,012 bytes; after s2: 966 entries (0 not named `upstroke-*`), 39,085,950 bytes | `guest/guest-tmpclean-after-*.log` | Get-ChildItem count and summed length |
| **F58** | the guest's export | 543 observation files; ledger tables: `ledger-budget-exceeded.md` 54 rows, 54 `yes`; `ledger-complete.md` 54 rows, 54 `yes`; `ledger-halted.md` 54 rows, 54 `yes`; `ledger-no-run-finished-then-complete.md` 54 rows, 54 `yes`; `ledger-no-run-finished.md` 54 rows, 54 `yes`; `ledger-parked.md` 54 rows, 54 `yes` | `guest/exports/export/` | ls; rows beginning `| R` and their Holds column |
| **F59** | ST-07's stale adapter exclusion, measured: the unchanged checker over a raw copy of each export and a copy without the moved adapter module (records/funnel executions/witnessed claims) | `ADAPTER_UNIT_TEST_MODULES` at the range: `workspace_manager::hooks::tests::`, `engine::topology::seams::tests::`, `runner::tests::`; the adapter test at `src/runner/mod.rs:2198` at 9bb177ea is at `src/runner/contract.rs:2200` at the range (module `runner::contract::tests::`), moved by `8f1c133a` of #318; its record, the only one the filtered copies drop, is in each export; Linux export 1 raw 434/429/163, without it 433/428/163; export 2 raw 434/429/163, without it 433/428/163; guest (native) raw 428/423/164, without it 427/422/164; failures 0 over the 6 runs, each exit 0 with one test selected: True, every copy unchanged: True; problems 0 | `st07/adapter-exclusion/summary.json` | st07/adapter-exclusion/summarize.py over run-linux.py's and guest-st07-r1.sh's receipts |
| **F60** | ST-18: process death at every cleanup-effect boundary of terminal finalization, then a fresh resume (the counted probe run; cells killed per configuration, Complete/Halted) | 14 tests, one invocation each, exactly its named test selected, exit 0: 14/14; 386 cuts, 386 pass; first finalization no leftover: 24/22 of 24/22 cells; all kinds after ordinary intents: 30/28 of 30/28 cells; two ordinary slots and pins of each kind with every leftover kind: 44/42 of 44/42 cells; task leftover first occurrence: 22/20 of 22/20 cells; snapshot leftover first occurrence: 22/20 of 22/20 cells; staging leftover first occurrence: 22/20 of 22/20 cells; two of each kind after ordinary intents: 36/34 of 36/34 cells; the sequence derived from the source equal to the recorded finalization 14/14; problems 0 | `st18-cuts/summary.json` | st18-cuts/summarize.py over st18-cuts/positive/ (receipt.json and the probe's G5R6R1_SEQ/CUT/SUMMARY lines) |
| **F61** | ST-18 cuts, first-bad control: the REGRESSION reviewer's inert-cleanup mutation of `finalize.rs`, then the file restored | mutated file equal to the reviewer's: True, and to run 6's 4 `fix291-*` recipes': True; tests planting a leftover exit 101: 12, their cuts failing 340 of 340 (the fresh resume left staged-intent leftovers standing 232, the kill child never reached the armed occurrence and did not die there 60, the killed child had consulted a sequence other than the derived one 48); the tests planting none exit 0: 2 (46 cuts pass); restored to the range blob: True, then 14/14 tests exit 0, 386/386 cuts pass | `st18-cuts/summary.json` | st18-cuts/summarize.py over control-inert-cleanup/ and restored/; st18-cuts/same-mutation-check.sh |
| **F62** | the committed kill and error-return matrices' hand-listed cells (`finalization_effects`) against the cells the probe recorded for the same plant shape (the first finalization) | complete: 24 table cells, 24 recorded, equal as sets: True, no recorded cell repeated: True; halted: 22 table cells, 22 recorded, equal as sets: True, no recorded cell repeated: True | `st18-cuts/summary.json` | st18-cuts/summarize.py: finalization_effects parsed at the range against the first-finalization sequence |
| **F63** | #318's deferred residual R5-MAIN-01 at the range (the boundary preflight's two routes of it) | `r5-main/10-undecodable-literals`: at d724fb16, Clippy exit 0, effects exit 101; 63 undecodable literals measured, the checker credits 21, rustc refuses 63 of 63 with `E0539`; `r5-main/13-invalid-any-guard-witness`: Clippy exit 0, effects exit 101, the named witness fails at `src/effects/tests.rs:7540:5` ("a literal rustc refuses excused the deny", 6 excusals), 9 fixtures, rustc refuses 9; restorations proven: True | `preflight/routes/r5-main__10-undecodable-literals/, preflight/routes/r5-main__13-invalid-any-guard-witness/` | receipt.json steps; the R5_BAD_LITERAL and R5_INVALID_ANY lines and the panic of each 02-effects.log |
| **F64** | run 6's raw clause-2 probe traces, kept in the evidence and re-derived from it | `audit/probe/probe-out.tar.xz` (sha256 `1f01a545d664e0b8…`): 7011 files, each as `audit/probe/probe-out.sha256` lists it; from the extracted traces: analyze.py exit 0, reach-check.py exit 0, plant-domain.py exit 0; outputs byte-identical 4, byte-identical after the input-path substitution 2, differing 0 | `audit/probe/rederive-from-archive.log` | audit/probe/rederive-from-archive.sh over a fresh extraction and the range checkout |
| **F65** | the fault-injection registry at the range | 180 entries over 68 sites: executed 168, recovery_proven 9, not_executed 3 | `effects/sequential-registry.json` | git show <range>:effects/sequential-registry.json (a repository path at the range); entries by evidence kind |
| **F66** | the fresh grading | 6 graders, 28/28/28/28/28/28 rows each (168 in all) | `audit/grading/packet-B*.json` | len() of each grading packet |
| **F67** | rows witnessed at their exact coordinate only inside terminal finalization | 11: rows 12, 13, 14, 19, 20, 21, 22, 27, 28, 29, 30 | `audit/review/adjudication.json` | the rows whose adjudicated limit begins "witnessed at the exact coordinate by a real kill inside terminal finalization's cleanup" |
| **F68** | the mutation recipes' test selection | 44 recipes run their named test with `--exact` (sets `g5run6`, `g5run4`, `g5run3`, `g5run2`, `g5r2r5`), 124 with the full path and no `--exact` (`g5` 45, `pr10` 79); library harness per log: 0 test(s) run in 1, 1 test(s) run in 167; logs running more than one: 0 | `mutations/exact-and-selection-check.json` | mutations/exact-and-selection-check.py over the recipe scripts and run-*/ logs |
| **F69** | preflight routes whose patch creates a file, against `applied.diff` | 5 of 169 routes create a file; `applied.diff` (tracked files only) omits it in 5; the created file's bytes are in `route.patch` for 4 and in the route's spec (`added_files`) for 1 | `preflight/applied-diff-coverage.json` | preflight/applied-diff-coverage.py over preflight/routes/*/ and preflight/specs/ |
| **F70** | the `SigIgn` receipts: run 6's run.py header against the command's own mask, and this round's run-r1.py | run.py header `child SigIgn` 0000000000000004 (the `bash -c` wrapper's), the command's own 0000000000000000; run-r1.py header 0000000000000000 (the exec'd command's); across this round's 150 run-r1.py logs in the evidence: 0000000000000000 in 150 | `scripts/sigign-check.log` | scripts/sigign-check.sh; every run-r1.py header line under the evidence directory |
| **F71** | the Windows guest window of run 6 | C: free 1,738,297,344 before its first step (2026-09-25T08:23:13Z) and 1,719,189,504 after its cleanup (2026-09-25T08:45:06Z): 19,107,840 bytes lower; 21 minutes 53 seconds between the two | `guest/summary.json` | the first "before" and the last "after" C: measurement of guest/guest-run.sh's steps |
| **F72** | boundary-preflight receipts that are not the result | first executions with a defective setup of this run's making, kept and re-run: 8; routes whose lane the kernel's OOM killer stopped before applying anything: 4 (`r5-reg__R03-real-test-only`, `r5-reg__R08-R3-main-B4`, `r5-reg__R01-X4-raw`, `r5-reg__R02-equivalent-literals`) | `preflight/routes-setup-failed/, preflight/round-b-oom.md` | directories under routes-setup-failed/; the rc=137 lane lines of round-b-oom.md |
| **F73** | this round's guest steps (the native ST-07 checks, in their own tree at the range) | prepare: bundle sha256 both sides 771a3dd8…/771a3dd8…, HEAD d724fb16, tree 419005f6; C: free 1,734,856,704 before prepare, 1,717,903,360 after cleanup (16,953,344 bytes lower); own paths left: 0; files written under the shared cargo registry: 0 | `st07/adapter-exclusion/guest-step-*.log` | guest-st07-r1.sh's prepare and cleanup logs |

**Counts in the prose that are not figures.** Tables and lists a generator printed and the report embeds verbatim: §5's
per-set table (`mutations/summary-table.md`, from `mutations/summarize.py`) and §14's carve-out list with its † count
(`report/fills.py` over `q/q5-domains.json`), and the guest's §2.2 text (`guest/section.py`), which cites the figures for its
values. Counts of what the same sentence names or lists, checked by reading it: the pass rule's nine positive clauses and three
prohibitions (counted from the rule §16 quotes), the three amendment records (`spec/copies.sha256`), the P1 table's rows and
their bearings in §13 (`ledger/assess-findings.json`). Everything else numeric in the prose is an identifier — a row, section,
line, pull-request, run or job number, or a hash.

## 1. The run

### 1.1 The specification

`spec/regen-spec.py` reads the packet (`spec/copies/tactus-parallel-design-neutral-v17.json`, sha256 `a34417d6248edbd80ecdabd7fb9a4b8bcd3ff1d98270c67bd43c25f9fb0b712d` (**F1**), the digest the
run brief names) and writes `cumulative_review_gates.gates[4]`, the process clause and the six standing questions, as JSON
(`spec/gate4-v17.json`, `spec/process-v17.txt`, `spec/standing-questions-v17.json`) and in the derived files' own format
(`spec/G5-DEFINITION.regen`, `spec/G5-QUESTIONS.regen`). Against v16 and against the derived files on disk: gates[4] equal: True; 2 leaves differ (`/cumulative_review_gates/standing_questions/4`, `/packet_version`) (**F2**);
definition: every section byte-identical but `source` (provenance line, differs by design), `adversarial_fault_tests` (differs in text, equal parsed as json); questions: 6 of 6 identical (**F3**). The three amendment records are copied beside the packet with their hashes (`spec/copies.sha256`):
2026-09-13 (the census family), 2026-09-15 (the census bounds line) and 2026-09-22 (Q5). **Q5 is graded from v17's
wording**, which is narrower than v16's; §14 says so where it answers it. The seam statements ST-07 to ST-20 the seventh group
names are extracted verbatim to `spec/seam-tests-in-range.txt`, and the decisions the clauses point at to
`spec/decisions-extract.txt`. Counted from the regenerated entry: 5 integrated invariants, 6 ownership rows, 7 adversarial groups, 5 required artifacts (**F4**); the pass rule has nine positive clauses and
three prohibitions (§16). No disagreement between the brief and the packet arose that the packet did not settle.

### 1.2 What entered at this gate

The definition assigns G5 its portions, and this report claims only those: INV-01 to INV-17 in all their portions, INV-19,
INV-20 at fold level, INV-22's durable rows **R1, R2, R5–R12, R14–R16, R18, R21, R23, R24, R25, R27**, plus **R19/R26** with
the fake container runtime, the outcome equations, the derived outcome, closure and finalization, and INV-23 for the host and
the fake container runtime, all at max_parallel = 1. INV-18, INV-21 and the process-local and broker rows **R3, R4, R13, R17,
R22, R28** are G6's; R17, R22 and R28's host portions are asserted here, not certified; R19/R26 with real Docker are G7's. The
six ownership-matrix rows are the census and the inventory/registry (gate artifacts, §7, §8), the coordinator lock holds and
the observed reaper hold (R17/R28, host portion, §8.4 rows 138–139), `report.json` and the integration ledger (R21, §12), the
execution root (R18, §12) and the per-resource ledgers (§12).

### 1.3 What the range contains since run 4

G5's input is merged PR10 on G4 with its fix pull requests; run 4's range `9bb177ea` carried all of those through #311, and
run 4's report judged each. Since then: 5: #315, #316, #317, #320, #318 (**F5**), changing 34 files changed, 15267 insertions(+), 3732 deletions(-) (**F6**) under `src/`. **Measured rather than read**
(`range/production-diff.py`): 22 identical in code; code changed: `src/effects.rs`; moves: `src/agent/mod.rs` → `src/agent/adapter.rs` (0 code lines only before, 5 only after, each listed); `src/runner/mod.rs` → `src/runner/contract.rs` (3 code lines only before, 7 only after, each listed) (**F10**). So every production path the crash/resume system runs is the code run 4
measured, and what the range adds is test code (#317's and #320's bounded waits and the lease fixtures, `src/rundir/tests.rs`
and `src/engine/topology/recover/tests.rs`), lint fences (#316, #318), the governance instruments (#318's reader in
`src/effects.rs` and its censuses in `src/effects/tests.rs`) and two module moves with their re-exports (#318). The
fault-injection registry is unchanged (1aeee56fc73ee033072fad814f9733347faca6f0 at both (**F7**)), so the audit's subject is the same 168 entries, numbered the same
way. Nothing in this paragraph is used as evidence for a clause: every clause below is re-derived at this range.

| merge | PR | what it is |
|---|---|---|
| `958d3aa4` | #315 | docs only: files `PR311-NEW-RECOVER-PROSE-OUTSIDE-NOTES` |
| `de6d6434` | #316 | `#![forbid]` of all three governed lints in `src/capacity.rs` and `src/runner/invocation.rs`, run 4's two executed hosts; files `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT` |
| `5b16f727` | #317 | test side: a bounded wait for the run cleanup lease in the recovery tests |
| `9c5c2e93` | #320 | test side: narrows the cleanup-lease flake (`PR281-CLEANUP-LEASE-HOLD-OUTLIVED-AND-ITS-UNREADABLE-TWIN` stays deferred); `src/rundir.rs` changes a doc comment only |
| `d724fb16` | #318 | the clause-8 remedy: the lint reader and its censuses, fences in the silent production files, the declaration-only roots with their bodies moved to `src/agent/adapter.rs` and `src/runner/contract.rs`; reviewed through five rounds, final MAIN and REGRESSION PASS at `6c5e093f` (§3) |

### 1.4 What production reaches at this range

Re-derived here, and executed where it can be: `src/topology/schema.rs` pins `TOPOLOGY_ACTIVATION` to `Inactive` behind a
compile-time assertion, and `src/engine/topology.rs` carries `#![cfg_attr(not(test), allow(dead_code))]`. **Measured on the
binary**: 0 for each of 11 entry points (recover::run_recovery_order, create::create_run, startup::startup_census, recover::settle_interrupted, candidate::complete_promotion, attempt::AttemptContext::settle_interrupted, finalize::finalize, run::TopologyRun, integrate, fold::TopologyFold::replay, log::EventLog::open); `engine::topology::` symbols: 2, of which legacy-engine trait impls 2 (**F9**) (`range/shipped-binary-symbols.log`: the shipped debug binary is linked with section garbage
collection, so a function absent from it is not reachable from `main`). The legacy engine's resume and coordinator, the host
runner, the process funnel and the run-directory funnels are present. So **every recovery this report credits ran through the
test suite's calls of the engine's own entry points**; the shipped binary does not reach the schema-4
engine at this range. The compiler's own dep-info gives the file domains the report uses: bin-only 1, example-only 1, production-lib 140, test-only (lib test harness) 47 (of 189 tracked .rs) (**F11**)
(`q/domains/`).

## 2. The cumulative suite, CI and the gates, at the range

### 2.1 The suite on Linux

Four full runs of `cargo test --all-targets --all-features` at the range in the worktree, each with a fresh private TMPDIR
and a resources snapshot before it (`logs/resources.log`):

- **S1** (`suite/linux-s1-clean.log`): exit 101; lib 2810 passed / 2 failed / 92 ignored (**F12**). **A red, and it stays counted as one.** Its failures, read from libtest's
  closing list rather than from interleaved output: `a_resume_over_a_creation_that_stopped_after_creating_its_integration_ref_adopts_it` at `src/engine/topology/recover/tests.rs:17727:10` (cleanup-lease refusal); `real_docker_kill_on_an_already_exited_container_is_tolerated` at `src/runner/container/tests.rs:3436:5` (docker 200-observation) (**F16**) — each on one of the two fingerprints the run brief permits a
  single re-run for (the production cleanup-ownership refusal "refusing overlapping engine ownership", filed as
  `PR281-CLEANUP-LEASE-HOLD-OUTLIVED-AND-ITS-UNREADABLE-TWIN`; the Docker "still running after 200 observations", filed as
  `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME` and `TESTS-CONTAINER-WAIT-HAS-NO-TIME-BOUND`, which #319 bounds
  outside this range). Matching a filed fingerprint is a reasoned classification, not an established cause. Root recorded,
  and `suite/s1-context.md` keeps, that another lane's crate compiles overlapped S1 on this box (03:58 to 04:05Z); that is a
  fact of the schedule, not a cause.
- **S1's one permitted re-run** (`suite/linux-s1-rerun.log`): exit 0; lib 2812 passed / 0 failed / 92 ignored; bin 10/0/0 (**F13**). This is the only re-run this run made under the
  flake rule; it establishes neither S1's cause nor that it is benign.
- **S2 and S3**, the export runs (`suite/linux-s2-export1.log`, `suite/linux-s3-export2.log`): exit 0; lib 2812 passed / 0 failed / 92 ignored; bin 10/0/0 (**F14**); exit 0; lib 2812 passed / 0 failed / 92 ignored; bin 10/0/0 (**F15**). Their
  exports agree: 552 and 552 observation files; census summaries identical (sha256 `10991b391643ee97cd0a04ad121bb379109c7d96d1da6f3251be9e7b0e5a388f`); the six ledger tables byte-identical (**F17**).

The lease refusal that reddened S1 is `PR281`'s class: production refusing a resume while a sibling fork of the test process
still holds a copy of the run's cleanup-lease descriptor. #320 bounds the recovery tests' wait for it from a fixture's second
resume on; S1's refused resume is a fixture's first (§6). **That test is the registry's cited test and this audit's witness for row 32**
(`Ref.CreateIntegration/after`, a resume over a creation stopped after its integration ref, adopting the ref); what it
establishes rests on its passing executions at the same source — S1's re-run, S2, S3 and its own process in the clause-2 probe
(`audit/probe/results.jsonl`) — and not on S1, where it was refused. The Docker test is not a registry row, a credited witness
or a group member.

**The same lane overlapped the clause-2 probe too, and the overlap is bounded** (`suite/s1-context.md`, corrected in the
repair round: the re-run's start is 04:06:01Z by its own header, not the 04:05:25Z the note first gave). That lane ran a full
gate from 04:12:42 to 04:17:02Z while this run's `audit/probe/run-each.py` ran every test in its own process from 04:12:39 to
04:15:48Z. The one cross-lane effect is a host-wide one:
`runner::container::exec::tests::real_docker_a_container_contains_a_daemonised_descendant` puts fixed markers (`sleep 903111`,
`sleep 903222`) in a container and scans the whole host's `/proc` for them, so two runs of it on one box see each other's
processes. The other lane's copy went red on this run's; this run's copy (it passed) may have met its vacuity guard partly on
the other lane's (`~/orch-pr10/questions/findings_gate5_deferred-2.md`). So that pass is no evidence of containment, and this
gate uses none of it: the test is no registry row, credited witness or group member; every probe record is written by this
run's instrumented binary into a per-test trace directory of its own; and the probe's target directory and every test's
`TMPDIR` were this run's own, so the fixed container pre-clean key was not shared. The fixed markers and the host-wide scan are
a test defect of G7's real-Docker territory, recorded in §13 and being filed by that lane.

### 2.2 The Windows guest

The guest ran from its own tree `C:\upstroke-g5-run6`, cloned from a git bundle of the range (sha256 `771a3dd8…` on both sides, `guest/bundle.sha256`, `guest/guest-prepare.log`), checked out detached at the range with the range's tree id and a clean status, into its own target `C:\upstroke-g5-run6-target`, with a private `TEMP`/`TMP` (`C:\Users\Administrator\AppData\Local\g5r6`, as long as the ordinary `TEMP`, so Git for Windows' 220-character `.git` limit is met as it is under the ordinary one). The guest has no `upstroke-build`, so there cargo ran with `CARGO_TARGET_DIR` naming that target, `CARGO_INCREMENTAL=0`, `-Cdebuginfo=0 -Clink-arg=/DEBUG:NONE` and debug off in both profiles (the shape run 5 measured the guest's linker needing, which the run brief carries; this run used it and did not re-measure the need), and with `UPSTROKE_TEST_TEMP_FOLDS_CASE=1` as CI's `test (winguest)` step sets it (`guest/guest-run.sh`; its two changes before the first run, the `UPSTROKE_TEST_TEMP_FOLDS_CASE=1` setting and the `core.autocrlf` read in its prepare step, are `guest/guest-run.changes-before-first-run.diff`). The guest's system Git configuration sets `core.autocrlf=true`, recorded in the prepare log, so the checkout's text files carry the line endings Git gives them there. The owner approved, and root carried out, the removal of two finished runs' directories to make room (`~/orch-pr10/answers/g5_run6-2.md`); this run removed nothing on the guest but its own files. C: free before and after each step: prepare: 1,738,297,344 → 1,677,172,736; s1: 1,677,221,888 → 1,293,717,504; s2: 1,349,840,896 → 1,278,910,464; st07: 1,331,216,384; samplers: 1,331,146,752 → 1,332,187,136; fetch: 1,333,456,896 → 1,329,639,424; cleanup: 1,334,136,832 → 1,719,189,504 (**F56**).

- **S-guest-1, the plain suite** (`guest/guest-suite-s1.log`): exit 0; lib 2638/0/67 (passed/failed/ignored); harnesses 2638/0/67, 12/0/1, 0/0/0; closing list empty; beside the harness lines, 6 one-test result lines printed by child processes the tests started (**F52**); `Compiling upstroke` names `C:\upstroke-g5-run6`. Green.
- **S-guest-2, the export suite** (`guest/guest-suite-s2.log`): exit 0; lib 2638/0/67 (passed/failed/ignored); harnesses 2638/0/67, 12/0/1, 0/0/0; closing list empty; beside the harness lines, 6 one-test result lines printed by child processes the tests started (**F53**); no `Compiling upstroke` line: it ran the library test binary S-guest-1 compiled, the same path in both logs (`upstroke-2a74f6670646457b.exe`). Green.
- **ST-07's merge check over a copy of the guest export** (`guest/guest-st07.log`, whose lines `guest/guest-st07.NOTE.md` accounts for): `st07_the_sequential_range_is_a_bijection_over_the_exported_observations` ok; one test selected, exit 0; entries 180, failures 0, funnel_executions 423, host windows, inventory 68, records 428, witnessed_claims 164; funnel_executions as the checker counts them includes the one record of the adapter test #318 moved to `runner::contract::tests` (production-funnel records without it: 422) (**F54**) (`guest/exports/export/st07-summary.json`).
- **The five samplers, each alone, five times** (`guest/guest-samplers.log`): `sampled_git_add_and_write_tree_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` 5/5 ok (**F55**); the histograms each run copied are `guest/exports/samplers/`, read by the residue record (§10).
- **The export** (`guest/exports/export/`): 543 observation files; ledger tables: `ledger-budget-exceeded.md` 54 rows, 54 `yes`; `ledger-complete.md` 54 rows, 54 `yes`; `ledger-halted.md` 54 rows, 54 `yes`; `ledger-no-run-finished-then-complete.md` 54 rows, 54 `yes`; `ledger-no-run-finished.md` 54 rows, 54 `yes`; `ledger-parked.md` 54 rows, 54 `yes` (**F58**).
- **This run's `TEMP` after each guest suite** (`guest/guest-tmpclean-after-*.log`): after s1: 966 entries (0 not named `upstroke-*`), 39,086,012 bytes; after s2: 966 entries (0 not named `upstroke-*`), 39,085,950 bytes (**F57**) — the scratch fixtures `PR7-SCRATCH-FIXTURE-LEAK` names — each then emptied before the next step.
- **Cleanup** (`guest/guest-cleanup.log`): this run's tree, target, `TEMP`, exports, bundle and command files removed, each path tested absent afterwards and no `g5r6` or `g5-run6` entry left on C:; C: free 1,738,297,344 before its first step (2026-09-25T08:23:13Z) and 1,719,189,504 after its cleanup (2026-09-25T08:45:06Z): 19,107,840 bytes lower; 21 minutes 53 seconds between the two (**F71**), both measurements in **F56**; none of this run's paths remains and its builds wrote nothing to the guest's shared cargo registry (a read-only check, recorded in the same log); what else changed on a shared guest in that window was not identified.


### 2.3 CI at the range

The range is `master`'s head, so CI ran it twice, and both runs are this range's platform receipts (`ci/runs-at-range.json`;
each run's jobs `ci/run-<id>.json`; the test jobs' own logs `ci/ci-*-job-*.log`), read by conclusion per run and never by a
job's colour alone:

- **the merge-queue run**, conclusion success; jobs lint success, lint (macos) success, lint (windows) success, msrv (Rust 1.85, macos-latest) success, msrv (Rust 1.85, ubuntu-latest) success, msrv (Rust 1.85, windows-latest) success, test (macos-latest) success, test (ubuntu-latest) success, test (winguest) success, upstroke-ci success (**F20**);
- **the push run**, the newest of the SHA's 2 CI runs (created 2026-09-25T03:49:51Z); conclusion failure; failed jobs: test (winguest), upstroke-ci (**F21**) — one test of the Windows leg:
  `runner::host::tests::every_path_entry_this_runner_searches_names_a_location_on_its_own` panicked at
  `src\runner\host\tests.rs:5634` with "`\\server\share\bin` names a location and was not searched: failed to stat … The
  specified network name is no longer available. (os error 64)". That is the fingerprint, test, message and errno of
  `PR262-UNREACHABLE-PATH-ENTRY-ABORTS-PROGRAM-RESOLUTION` (P2, `deferred`): a fixture whose `PATH` entry is a UNC share, whose
  result the guest's network stack decides, recorded there as "one source, twice green and once red". This run did not re-run
  CI; the merge-queue run at the same SHA is green on that leg. **It is a red at the range, disclosed as one.** The pass rule's
  "all rows green" is read as the packet's matrix and ledger rows — the fault-injection registry's entries this gate audits
  and the per-resource ledgers' rows — together with the named tests that witness them (§16, clause 5), and that test is none
  of them: it is in no registry entry, audit witness or group filter, and no ledger row names it. VERIFY put the reading to
  root (its O-1), and root directed that it be stated; a reader who takes the phrase as "every suite run at the range green"
  would count this red against clause 5.
- **Per-platform counts**: merge_group: ubuntu 2812/0/92, macOS 2747/0/70, winguest 2638/0/67; push: ubuntu 2812/0/92, macOS 2747/0/70, winguest 2637/1/67 (**F22**). The Linux counts equal S1's re-run, S2's and S3's exactly, and the merge-queue run's Windows count equals both guest suites' (**F52**, **F53**).

### 2.4 The ten gates

§15.2, at this report's own head.

## 3. The boundary preflight: the reviewers' routes, re-established at this range

Run 5 was stood down before certification because a production route outside v17's carve-out survived the Clippy and effects
boundary; #318 is the remedy, reviewed through five rounds of independent MAIN and REGRESSION review and PASS in both lanes at
its final head `6c5e093f` (its pull request's review comment and instrument statement; the executed receipts under
`~/orch-pr10/reviews/pr-318r1` … `pr-318r5`). A reviewed head's rejection of a payload does not certify this range, so the
boundary was re-established here, in scratch clones of the range, before the rest of the gate was trusted.

**What was taken from the reviews, and how.** A read-only catalogue of every executed route and control in the five rounds'
records and in the repair lanes' own reproductions (`preflight/catalog/routes.{json,md}`, with byte copies of every patch and
their sha256; `preflight/catalog/patches/`) keeps each record's original source identity: the round, the lane, the head it
ran at, its patch, its commands and its recorded outcome. From it (`preflight/build-specs.py`, `preflight/selection.json`,
selection and reasons written before anything ran): every record of the final round R5 whose patch applies at the range, every
record the catalogue marks as a class's original route whose patch applies at the range, with its negative and
legitimate-allowance controls, run 4's and run
5's own routes, and the controls the run brief names that those do not reach — the agent and runner root routes and the
relocated children (R3's `C5`/`C6`), the declaration-only checks, the composite-test-`cfg` route (R2 REG), and the
fence-restore, unfence, new-host, pin and inherited-forbid controls. One execution per distinct patch; the records that carry
the same patch are listed with it. **What was not re-run**: the other catalogued records — intermediate rounds' routes and the
repair lanes' reproductions at earlier heads — were not re-executed here, and the catalogue keeps each with its round, head and
recorded outcome (**F45** counts both). Three of them are marked original routes: the repair lane's
`rep3/C5-agent-root-host` and `rep3/C5-runner-root-host`, whose patches do not apply to the roots #318 made declaration-only
(R3 MAIN's own `C5` routes on the same roots, and R3 REG's closures of them, ran), and `rep4/measure-compiler-probe`, which has
no patch and whose measurement this run's population probe makes afresh (below). One final-round record has no patch at all, R5 REGRESSION's `B10`, an
unmodified-source effects run under `strace`, and this run's own unmodified-range baseline stands in for it without the spawn
tally (`preflight/baseline/`: Clippy over all targets exit 0, the effects selection green). Every patch was applied **unchanged** but for one substitution, recorded per route: every
absolute path it names under `/dev/shm`, `/tmp` or `~/orch-pr10` points instead at a private copy under this run's work
directory, so no route writes into a reviewer's evidence, and each route proves those originals unchanged afterwards.

**How each ran** (`preflight/run-route.py`): refuse unless the clone is at the range, clean, and every file the patch touches
is its range blob; apply the patch; save the applied diff and its sha256 — a `git diff` of tracked files, so for a route
whose patch creates a file the created file is not in it, and its bytes stay where the route recorded them, in the byte copy
`route.patch` or in the route's spec (5 of 169 routes create a file; `applied.diff` (tracked files only) omits it in 5; the created file's bytes are in `route.patch` for 4 and in the route's spec (`added_files`) for 1 (**F69**)); run Clippy over all targets with `-D warnings`
(JSON diagnostics, so every error's code and primary span is parsed), the effects selection (`effects::`), the record's own
named tests with `--exact` and their selection proved from the log, and, where the record ran it, a plain `cargo build --lib`;
restore, and refuse unless every touched file is its range blob, every added file is gone and the tree is clean. Each step's
log is removed before it runs, so the bytes read are that run's.

**How each was judged** (`preflight/summarize.py`, written before the results were read): a route is refused only by a
required check refusing the route's own code — a Clippy `E0453` or disallowed-API diagnostic whose primary span is in a file
the route touched, or a test that exists at the range failing — never by a reviewer's runtime-witness test the patch itself
added, and never by a plain `rustc` build, which enforces no Clippy level. A legitimate-allowance control must be refused by
nothing, or, where the range's wider selection refuses its shape, its own original named checks must pass here; every other
route must be refused by at least one required check. A bypass-like route that nothing refuses would be a **survivor**, and a
survivor would have stopped this gate at an orchestration question.

**The result**: 167 executed specs and 2 `--nocapture` re-runs (of `r5-main/08-batch-versus-individual`, `r5-main/09-batch-diagnostic-controls`), covering 242 of the 502 catalogued records; verdicts: 152 as expected; 4 legitimate for its own named checks, which pass; another in-range guard refuses the shape; 13 an instrument experiment whose outcome matches its original record; restorations proven 169/169 (**F45**) (`preflight/summary.{json,md}`, one receipt per route under `preflight/routes/<route>/`).
**No survivor**: every bypass-like route this run re-ran is refused at this range by a required check — Clippy's `E0453` or disallowed-API error at the route's own file, or an effects census naming the file — every negative control and guard- or reader-sensitivity control is refused by the check it targets, every legitimate control passes its own original checks, and every instrument experiment of R5 has the outcome its record holds (`preflight/class-table.md`). Every clone was restored to the range's blobs and every reviewer original the route's paths named was proved unchanged.

4 legitimate-allowance controls are refused here by a wider in-range guard than the one they ran, and each one's own original checks pass at the range, so the refusals are those guards doing their work, not bypasses:

- `r1-main__F6-readiness-enrolled` — refused by `every_externally_reachable_fn_of_a_legacy_or_shared_module_is_classified` (R1's enrolment puts the readiness module in one record of the classified domain and not in the other, and the census refuses the disagreement ('the record and CLASSIFIED_MODULES disagree about the domain')); own filtered checks (classified_module) passing: `every_classified_module_that_declares_a_visible_fn_has_a_domain`, `every_classified_module_carries_a_file_level_fence_or_allowance_of_a_governed_lint`, `the_governed_lint_pairs_classified_modules_leave_unstated_only_shrink`
- `r3-main__G1-root-declaration-legitimate` — refused by `every_unclassified_production_file_states_each_governed_lint_or_inherits_its_forbid` (R3's new host states no level for a governed lint and inherits no `forbid`); own named checks passing: `a_declaring_module_holds_declarations_and_re_exports_and_nothing_else`
- `r4-main__X3-uninvoked-legitimate` — refused by `the_whole_file_test_modules_are_resolved_from_the_declarations_not_the_file_names` (R4's uninvoked macro phantom holds a module-shaped token sequence, which the whole-file module scan refuses because it cannot read whether the expansion declares a module); own check (clippy over all targets, the only command the original control ran) passes
- `r4-main__killed-X3-discarded-legitimate` — refused by `the_whole_file_test_modules_are_resolved_from_the_declarations_not_the_file_names` (the same phantom in R4's discarded variant); own check (clippy over all targets, the only command the original control ran) passes

**Receipts that are not this result, kept and named** (first executions with a defective setup of this run's making, kept and re-run: 8; routes whose lane the kernel's OOM killer stopped before applying anything: 4 (`r5-reg__R03-real-test-only`, `r5-reg__R08-R3-main-B4`, `r5-reg__R01-X4-raw`, `r5-reg__R02-equivalent-literals`) (**F72**)). The first executions with defective setups of this run's own making are kept as they ran under `preflight/routes-setup-failed/`, each with its reason in `NOTE.md` (double path substitution and unfilled sibling fixtures in one sweep replay, R1's untracked added file omitted from its enrolment route, and runtime witnesses run before their private scratch directories existed); each is re-run with the setup corrected, and only the re-runs are counted. The routes the OOM killer stopped died before applying anything, because this run's driver read whole files under a reviewer's scratch directory to prove it unchanged (`preflight/round-b-oom.md`); the driver was corrected to hash only the reviewers' evidence tree, streamed, and those routes and every route their lanes had not reached re-ran in a fourth round.

**Which "instrument experiments" are which.** Two of them are #318's own deferred residual reproducing at this range,
R5-MAIN-01: `r5-main__10-undecodable-literals` and its minimal case `r5-main__13-invalid-any-guard-witness` (`r5-main/10-undecodable-literals`: at d724fb16, Clippy exit 0, effects exit 101; 63 undecodable literals measured, the checker credits 21, rustc refuses 63 of 63 with `E0539`; `r5-main/13-invalid-any-guard-witness`: Clippy exit 0, effects exit 101, the named witness fails at `src/effects/tests.rs:7540:5` ("a literal rustc refuses excused the deny", 6 excusals), 9 fixtures, rustc refuses 9; restorations proven: True (**F63**)).
The checker credits an undecodable literal inside a true `any()`; rustc refuses every such source, so no compiling route
reaches the credit (§6, §14). Three more are R4's compiler-parity sweeps (`r5-main__07-original-sweeps__main_r4_additional_compiler_parity`,
`r5-reg__R05-main-parity48`, `r5-reg__R06-regression-sweeps__REG30-extended-parity`), which still exit 101 at the range as they
did at #318's final head: their harness counts the repaired reader's conservative "undecided" answer as a miss (the
catalogue's record of `rep5/after-MAIN-X2-parity48`, `preflight/catalog/routes.md`).

**What the routes show, class by class** (the receipts are the evidence; the reading is below):

- **`src/agent/bin.rs`** — the original macro-generated allowance and wrapper reached from the production
  `engine::topology::integrate::prepared_pin_ref` (R1 MAIN), and the hand-written spelling: at the range bin.rs carries
  `#![cfg_attr(not(test), forbid(clippy::disallowed_methods))]`, so Clippy refuses the generated `allow` with `E0453` at the
  lint gate. Every later bin.rs route first lowers that fence to `deny` and then adds an allowance meant to read as test-only or
  never-active (R2 REG's composite `cfg(all(test, unix))`, R3 MAIN's B4 nested `cfg_attr`, R3 REG's nested activation, R4's
  X-series, R4 REG's equivalent string literals, R5's final X4-raw and equivalent-literal routes): **Clippy accepts those at
  the range**, and the effects census `no_deny_of_a_governed_lint_is_excused_by_test_code_alone` refuses each, naming
  `src/agent/bin.rs`. That test runs in every required test leg, so the boundary for this class is the effects census, not
  the lint gate, and it is re-established here route by route.
- **`src/plan/mod.rs`** — R2 MAIN's ordinary production-library route: at the range the file forbids all three lints, and
  Clippy refuses the route with `E0453`; lowering that fence is refused by
  `every_unclassified_production_file_states_each_governed_lint_or_inherits_its_forbid`.
- **The agent and runner roots and their relocated children** — a wrapper in `src/agent/mod.rs` or `src/runner/mod.rs` passes
  Clippy (the roots `deny`) and is refused by the declaration guard; the same wrapper in `src/agent/adapter.rs` or
  `src/runner/contract.rs` meets the child's `forbid` and is `E0453`; the `-deny` and no-attribute variants show the fence is
  what refuses (Clippy then reports the disallowed call itself). A plain `cargo build --lib` of such a tree succeeds, which
  records the boundary above, not a bypass.
- **Run 4's hosts, `src/capacity.rs` and `src/runner/invocation.rs`** — the macro route and the hand-written spelling are
  `E0453` at the range; the `deny` control is refused at the write.
- **The guards' own sensitivity** — restoring a `deny` where #318 put a production `forbid`, unfencing a root or a leaf, adding
  an unfenced new host or an unfenced classified module, moving the 29-pair pin, and a body in a declaration-only root are
  each refused by the guard written for it; the reader and rule mutations of R5 (`M01`–`M23`) and the batch-protocol mutations
  each fail the named tests that hold their rules.

**Population, not only routes** — 423 pairs over 141 hosts: E0453 338, accepted 85 (10 in the 4 declaration-only roots, 75 in carve-out files, 0 elsewhere); agreement with the prologue reading 423/423 (**F44**). And a probe the brief asked for that no review ran as such: the compiler's
own dep-info gives the production domain (bin-only 1, example-only 1, production-lib 140, test-only (lib test harness) 47 (of 189 tracked .rs) (**F11**)), so "every production host" above is the set rustc compiles into the
non-test library and the binary, not a list anyone typed.

## 4. The seven adversarial fault-test groups

Counted from the definition: **seven** (`spec/G5-DEFINITION.regen`, `adversarial_fault_tests`). Each ran by name, as one
`cargo test` invocation per group with its exit captured, in the worktree at the range, after the suites and outside any
other load of this run's (`groups/run-groups.sh`, `groups/logs/`): run 4's filter lists carried, #318's moved test paths
mapped, the tests this range added appended to the groups they belong to, and **every exact filter checked against this
range's own `--list`** before anything ran (`groups/build-groups.py`, `groups/filters-check.log`: every filter matches).
Groups 8 and 9 are run 1's supplementary invocations (the effects censuses and Q5; the ST-12/13/15/19/20 seams). group1 86/0/0 exit 0 (green, every one of 86 exact filters selected); group2 2/0/0 exit 0 (green, every one of 2 exact filters selected); group3 55/0/0 exit 0 (green, every one of 55 exact filters selected); group4 13/0/0 exit 0 (green, every one of 13 exact filters selected); group5 36/0/0 exit 0 (green, every one of 36 exact filters selected); group6 17/0/0 exit 0 (green, every one of 17 exact filters selected); group7 40/0/0 exit 0 (green, every one of 40 exact filters selected); group8 199/0/0 exit 0 (green); group9 16/0/0 exit 0 (green, every one of 16 exact filters selected) (**F49**). No invocation recompiled: each ran the test binaries S1 built from the worktree at the range, named by path and hash in `groups/binary-identity.txt` (the only `Compiling upstroke` line on the worktree's private target since entry is S1's), and `groups/summarize.py` judged each log by its per-test lines, not its exit.
Where a group's claim is that something is *detected*, the mutation that removes it is §5; where it is a per-coordinate claim,
§8's audit is the grade's evidence and the invocation shows the named tests pass.

| # | the definition's claim (abridged) | what ran, and how it was judged | grade |
|---|---|---|---|
| 1 | an **exhaustive kill** matrix over every Topology/Shared site × order × resume, incl. kills during recovery, run-end closure and terminal finalization, Event append points (kill and error return), OpenLog's SyncPrefix (kill and error return), and sampled kills inside Git commands with every residue classified and recovered | group 1's invocation (the kill matrices, the Event and OpenLog kill and error-return witnesses, the barrier refusals, the five samplers, the IdUnread kills); per coordinate, §8's audit against the generated inventory; the samplers, §10 | **asserted** as the definition words it; **established** under ST-07's construction standard. Every coordinate of the generated inventory has a constructed prefix, a production recovery performing the tabled action and a replay-twice or vacuous credit (**F27**), but a real kill stands at fewer than half of them — the rest are injected errors, plants and adjacent constructions — so the matrix is not a literal kill at every cell. Clause 2 asks for executed evidence, not a kill, and keeps its grade (§16) |
| 2 | the two-crash proof, positive and negative | group 2's invocation; the bytes on disk before and after each loss (§9) | **established** for the simulated loss (§9) |
| 3 | two coordinators refused by locks; resume with edited config warns; deleted worktrees and pruned non-authoritative refs recovered; Retained generations closed before `run_resumed`; snapshot, container, husk (every prefix, the ownership proof, the commit record, the creator-error shapes) and stale-marker residue reclaimed, repaired, or retained and reported | group 3's invocation (with #320's two lease-refusal recoveries appended); per coordinate, §8 | **asserted**, for one reason: the named cases are established (the lock refusals, the drift warning, the Retained closure, the husk at every prefix, the creator-error shapes, the container states, the stale-marker repair), but the filed torn-registration shapes need an operator (§12), so "deleted worktrees … recovered" is not established in them |
| 4 | ST-17 run-end closure per class and outcome, every listed derived-outcome refusal, and the pre-`budget_exceeded` counterexample | group 4's invocation; the census's outcome equality in both directions and the measured named instances (§7) | **established** |
| 5 | ST-18: kills after `run_finished`, after the report write and **between every cleanup effect** converge; resume of Complete/Halted finalizes then refuses; the Halted report lists retained candidates refs; answer files and `.partial` residue untouched and never ingested; a late answer after finalization writes an inert file | group 5's invocation: the two kill matrices, the error-return matrix, the declared-pin tests, the late-answer and answer-file tests; and, in the repair round, the ST-18 kill-cut probe at the range (14 tests, one invocation each, exactly its named test selected, exit 0: 14/14; 386 cuts, 386 pass; first finalization no leftover: 24/22 of 24/22 cells; all kinds after ordinary intents: 30/28 of 30/28 cells; two ordinary slots and pins of each kind with every leftover kind: 44/42 of 44/42 cells; task leftover first occurrence: 22/20 of 22/20 cells; snapshot leftover first occurrence: 22/20 of 22/20 cells; staging leftover first occurrence: 22/20 of 22/20 cells; two of each kind after ordinary intents: 36/34 of 36/34 cells; the sequence derived from the source equal to the recorded finalization 14/14; problems 0 (**F60**)), with its first-bad control (**F61**) | **established**. A process killed at every cell of the finalization of each constructed run (**F60**), then resumed in a fresh process, twice: the resume finalizes and refuses, the durable prefix the death left is exactly the effects whose `before` cell came before the kill, every effect is done afterwards, the outcome equations hold (`assert_finalized`: slots, intents, pins and the root pruned, candidates refs pruned at Complete and retained and listed at Halted, answer files byte-identical, nothing appended, no object the run referenced deleted), the log replays twice equal and the repeat rewrites nothing. The cells are derived from `CleanupStep::ORDER`, the plant's residue and the funnels' own loops, and are the cells the finalization consulted, recorded unarmed. The constructed runs are a first finalization (so the first cell is a kill after `run_finished` with no report staging begun), every cleanup step with one ordinary slot of each kind and one prepared pin and again with two of each, and every staged-intent leftover kind at every occurrence position the execution-root step takes — its site's first consultation, after an ordinary intent's, and after another leftover of its kind. Removing the leftovers' cleanup fails every leftover cut, where the committed matrices stayed green; the committed matrices' own cells are exactly the recorded first finalization's (**F62**). The candidate-prepared pin's two cells, which no finished plant holds, are the declared-pin kill test's; finalize-then-refuse, the Halted listing, the untouched answers and the inert late answer are their named tests', in group 5's invocation |
| 6 | Halted retains and lists candidates refs, Complete prunes all and leaves objects to Git; a BudgetExceeded resume with a raised budget continues queued candidates, parked repairs with sources present, and woken deferred items; answers left during a budget-stopped epoch are ingested only by the resume and only for still-open questions | group 6's invocation | **established** |
| 7 | the seams in range: ST-07 full; ST-09 incl. R21, the owner and commit records, and R27 with the fsck inventory; ST-10, ST-11, ST-12; ST-13's sequential subset incl. the fast no-object proof; ST-14 incl. totality, live-versus-replay classification equality and `merge_prepared` relations; ST-15, ST-19, ST-20 | groups 7 and 9; §7 (ST-14), §8 (ST-07), §12 (ST-09); the statements verbatim in `spec/seam-tests-in-range.txt` | **ST-07** as §8.5 grades it; **ST-14 established** (§7); ST-09, ST-10, ST-11, ST-12, ST-15, ST-19 and ST-20 **established for the named cases**; ST-13's provisional-reservation lifecycle beyond the fast no-object proof is G3's evidence and not claimed here |

## 5. Detection, established by mutation

Every claim this report makes that a test *detects* something rests on a mutation that removes the thing and the named test
observed to fail, in a saved log. The campaign ran in the scratch clone `mut-tree` at the range (`mutations/run-campaign.sh`),
with the `pr10` set in a second clone, `mut-tree2`, with its own target and `TMPDIR`, at the same time
(`mutations/chain-pr10-parallel.log`; `mutations/rebinding-pr10p.diff`): run 4's six recipe sets carried with only their paths
re-bound (`mutations/rebinding.diff`: recipe bodies unchanged), and one set of this run's own. A pass that applied every anchor and ran nothing came first (`mutations/check-pass/`): every carried
anchor applied at the range but one, `pr10`'s `process-phases-not-recorded`, whose code #318 moved unchanged from
`src/runner/mod.rs` to `src/runner/contract.rs`; that failed patch is kept, and this run's set re-anchors the same patch and
the same named test to the new file (`mutations/mutations-g5run6-recipes.sh`). Each recipe: back the file up with `cp`, apply
the patch (its anchor must occur exactly once), run the named test by its full path — with `--exact` in five of the seven
sets, and in `g5` and `pr10` as a full-path filter without it (44 recipes run their named test with `--exact` (sets `g5run6`, `g5run4`, `g5run3`, `g5run2`, `g5r2r5`), 124 with the full path and no `--exact` (`g5` 45, `pr10` 79); library harness per log: 0 test(s) run in 1, 1 test(s) run in 167; logs running more than one: 0 (**F68**)), so each log's own lines are what prove which
one test ran — restore with `cp`, and
refuse unless the file equals the range's blob and the tree is clean.

**An exit code is not a kill until the log is read** (`mutations/summarize.py`): a kill is exit 101 with the named test's own
`FAILED` line, and its panic site and message are recorded (`mutations/killed-sites.json`) so a failure on a flake fingerprint
is never counted as a kill; a survivor is exit 0 with the named test's `ok` line; an exit 0 in which no test ran is VOID,
neither. Every recipe's log was read for cargo's `Compiling upstroke` line naming its own clone, so no recipe is credited
with a binary another tree built (`mutations/compile-receipts.{py,md}`: one recipe, whose patch is to a file the test reads at
run time, has no such line, and the binary it ran is shown to be built from the range's Rust sources). **The result**: 168 recipes in 7 sets: 148 killed, 19 survived (each explained in the table), 1 VOID; kills on a flake fingerprint 0; backups checked against the range 169; problems 0 (**F48**).

| set | recipes | run | killed (`rc=101`, the named test FAILED) | survived (`rc=0`, the named test ok) | VOID (`rc=0`, no test ran) | log |
|---|---|---|---|---|---|---|
| `g5run6` | this run's own: PR10's `process-phases-not-recorded`, re-anchored where #318 moved its code | 1 | 1 | 0 | 0 | `mutations/chain-g5run6.log` |
| `g5run4` | run 4's seven recipes against #305's and #311's properties | 7 | 6 | 1 | 0 | `mutations/chain-g5run4.log` |
| `g5run3` | run 3's seven recipes against #302 | 7 | 7 | 0 | 0 | `mutations/chain-g5run3.log` |
| `g5run2` | run 2's fix-judging recipes (#289, #291, #292, #293) | 20 | 15 | 5 | 0 | `mutations/chain-g5run2.log` |
| `g5` | run 1's G5 recipes | 45 | 34 | 10 | 1 | `mutations/chain-g5.log` |
| `pr10` | PR10's recipes | 79 | 77 | 2 | 0 | `mutations/chain-pr10-parallel.log` |
| `g5r2r5` | run 2's review-round-5 recipes (barrier refusals, projection) | 9 | 8 | 1 | 0 | `mutations/chain-g5r2r5.log` |

Across the 7 sets: 168 recipes: 148 killed, 19 survived, 1 VOID (an exit `0` whose invocation ran no test is not a survivor).
Not counted above: `pr10`'s `process-phases-not-recorded` did not apply, as the check pass found before anything ran: #318 moved the code it patches, unchanged, out of `src/runner/mod.rs`; set `g5run6` applies the same patch where the code now is (`process-phases-not-recorded-reanchored`, exit 101).

Every recipe whose named test passed, or that ran no test, and why:

| set | recipe | why |
|---|---|---|
| `g5run4` | `g5r4-reclaim-after-creation-keeps-the-pin` | mis-aimed: that witness's first resume completes no promotion, so `reclaim_after_creation` never runs in it and the pins its plant leaves are pruned by test code. The re-aimed `-vs-promotion-kill` recipe fails its named test (exit 101), but not at the recovery: removing the deletion removes the hook the kill child is armed at, so the child runs through to its own panic instead of dying at `before-candidate-pin-delete`, and the parent refuses the missing kill (`recover/tests.rs:14551`) before the next resume or any pin assertion runs (run 4's explanation, re-observed at this range) |
| `g5run2` | `fix289-record-publish-swallowed-vs-refused-barrier-test` | mis-aimed: that test exercises the public barrier's refusal, not the private record's publish; the correctly aimed `-vs-new-test` kills |
| `g5run2` | `fix289-record-removed-before-barrier` | the power-loss witness simulates its loss after the moved barrier, so it cannot see the interval the mutation opens; the earlier loss point is bounded by the refused-barrier witness, which kills the same mutation (`fix289-record-removed-before-barrier-vs-refused-barrier`) |
| `g5run2` | `fix291-complete-keeps-staging-leftovers` | the same, at Complete |
| `g5run2` | `fix291-halted-keeps-staging-leftovers` | the finalization matrices plant no staging-intent leftover, so the step removes nothing in those cells; `fix291-staging-leftovers-vs-dedicated-test` kills the same mutation against the error-return witness; the ST-18 kill cuts of the repair round plant the leftovers, and every leftover cut fails under this same mutation (§4, `st18-cuts/`) |
| `g5run2` | `fix291-matrix-keeps-staging-leftovers` | the same, against the committed error-return matrix |
| `g5` | `g5-answered-question-stays-open-vs-g5-witness` | **VOID**: its filter names run 1's uncommitted witness and matches no test (`0 passed; 0 failed`); the committed twin kills |
| `g5` | `g5-barrier-length-mismatch-ignored` | the same; its `-log` twin kills |
| `g5` | `g5-barrier-reread-mismatch-ignored` | the named test is a pure-data test of the container census's barrier abstraction and never calls `establish_stable_prefix`; the re-aimed `-log` recipe kills the event log's own test (run 2's explanation, re-observed) |
| `g5` | `g5-barrier-torn-tail-reappearance-ignored` | the same; its `-log` twin kills |
| `g5` | `g5-census-question-not-offered-dimension` | the `questions` bound is reached by another route (a rejection's repair admission opens a question); the `-arms` twin kills |
| `g5` | `g5-fast-publication-other-sha-accepted` | `MergePrepared::self_consistency()` refuses that shape in the wire type before the fold's check runs, so the mutation is behaviour-preserving; the head relation's recipe kills |
| `g5` | `g5-id-unread-point-skipped` | the named test is a classifier test that spawns no child and consults no hook; the re-aimed `-kill` and `-record` recipes kill |
| `g5` | `g5-residue-frozen-n-changed` | the named test constructs its own entry against a site's declaration; the re-aimed `-pin` recipe kills the declarations' pin |
| `g5` | `g5-retained-idle-not-closed-on-resume-settle` | the settle-level test never runs recovery step (e), where the mutation is; the loop-level recipe kills |
| `g5` | `r11-staging-record-publish-swallowed` | expected to pass (declared so in the recipe): its original aim does not exercise the private record's publish; #289's added test kills the same mutation (`fix289-record-publish-swallowed-vs-new-test`) |
| `g5` | `r11-staging-record-stage-swallowed` | expected to pass, as above; `fix289-record-stage-swallowed-vs-new-test` kills it |
| `pr10` | `sampler-kills-leader-only` | declared `pass` in the recipe, which is PR10's expected outcome; re-observed. This gate did not re-derive why the remaining-sites sampler is indifferent to it |
| `pr10` | `torn-registration-both-without-gitdir` | declared `pass` in the recipe: the lens's reproduction, which failed before PR10's fix and passes after it; re-observed |
| `g5r2r5` | `barrier-first-line-digest-missing-ignored--commit_record_present_without_committed_` | the named test plants its P5b husk without a log (it asserts the log is absent), so the census never opens a log and the barrier's first-line check is never reached; the same mutation is killed against the event log's own test and against the torn-first-line kill witness (run 2 round 5 observed the same) |

All 169 `.backup` files equal the range's blob of the path their `.diff` names (for the one patch that did not apply, the file its recipe names), and no chain log records a refused restore or a patch that failed to apply other than that one (`mutations/summarize.py`).

## 6. The fix pull requests since run 4's range, judged by what this run executed

Run 4's report judged every fix pull request up to its range `9bb177ea`; this run does not carry those judgements as evidence
— every clause is re-derived at this range — but it does not repeat them either, because §1.3 measured that none of the
production code they judged has changed. The five merges since, and what this run did about each rather than accepted:

| merge | PR | judged by |
|---|---|---|
| `958d3aa4` | #315 | docs only (a finding file); the ledger at the range carries it (§13) |
| `de6d6434` | #316 | the fences it added to `src/capacity.rs` and `src/runner/invocation.rs` are measured by the population probe (every pair in both files `E0453`) and by run 4's own two routes, re-run here (§3) |
| `5b16f727` | #317 | test side only; its bounded wait is part of the recovery tests every suite here runs, and it does not repair the production cleanup-ownership refusal, which S1's red shows still occurs (§2.1) |
| `9c5c2e93` | #320 | test side, plus one doc comment in `src/rundir.rs` (§1.3); it narrows and does not close `PR281-CLEANUP-LEASE-HOLD-OUTLIVED-AND-ITS-UNREADABLE-TWIN`, and S1's lease refusal carries no note of an expired fixture wait: the recovery tests' wait (`await_previous_incarnations_release`, reached through `resume_as_certified_by`) runs only from a fixture's second resume on, and the refused resume there is its fixture's first (the fixture is planted and its prefix built through the funnels, `a_resume_over_a_creation_that_stopped_after_its_marker_was_removed_converges`) (§2.1) |
| `d724fb16` | #318 | its instruments by the boundary preflight (§3), its fences by the population probe, its declaration-only roots by the declaration guard's routes, and its moves by the line-multiset comparison (§1.3). Its deferred residual `R5-MAIN-01` (P3: an undecodable literal inside a true `any()` earns checker credit) **was re-executed here**, by the boundary preflight's two routes of it: `r5-main/10-undecodable-literals`: at d724fb16, Clippy exit 0, effects exit 101; 63 undecodable literals measured, the checker credits 21, rustc refuses 63 of 63 with `E0539`; `r5-main/13-invalid-any-guard-witness`: Clippy exit 0, effects exit 101, the named witness fails at `src/effects/tests.rs:7540:5` ("a literal rustc refuses excused the deny", 6 excusals), 9 fixtures, rustc refuses 9; restorations proven: True (**F63**). So the checker's credit is reproduced at this range, and so is the premise the deferral leans on — rustc refuses every such source, so no compiling route reaches the credit. It is a credit imprecision of #318's checker on sources the compiler rejects, not a route past the lint boundary; it is a deferred row of #318's own ledger, whose finding file is filed after the range (§13), and Q5 carries it (§14) |

`#319` (the Docker termination poll budget) is reviewed-pending and **not** in the range; S1's Docker red is its fingerprint
(§2.1).

## 7. Clause 1 — the bounded census family (required artifact 2)

**The artifact** is `census-export2/bounded-census-artifact.md`, generated by `census-export2/bounded-census-artifact.py` from
the census summary this run's export suite wrote, the packet's `decisions.bounded_census` (`spec/packet-bounded-census.json`)
and this run's census-instance probe; every `file:line` in it is resolved at the range by `audit/anchors.py` (the generator
reports unresolved anchors, and there are none). The summaries of the two Linux export runs are byte-identical (552 and 552 observation files; census summaries identical (sha256 `10991b391643ee97cd0a04ad121bb379109c7d96d1da6f3251be9e7b0e5a388f`); the six ledger tables byte-identical (**F17**)), and the guest's export summary is byte-identical to them (§2.2).

Against the amended clause — *the prefix at its stated ceiling with truncation reported, every seeded per-dimension census
closed, every bound reached by equality over the union, totality asserted over the union and every `plan_transition` arm
covered*:

- **The members**: `prefix`: 20,000 states, truncated (reported); `seeded chain: sequences, repairs`: 25 states, closed; `seeded fan-out: sequences, repairs, lineages`: 31 states, closed; `seeded join: sequences, repairs, lineages`: 75 states, closed; every bound reached over the union: true (**F30**). The prefix is explored to its ceiling and reports its truncation; each seeded member closes
  under its own ceiling and restricted generator with no state at the trace ceiling (the artifact's table, and the committed
  assertions it cites).
- **Every bound over the union.** Every numeric bound of the amended bounds line is reached by equality over the union, and
  the three plan shapes (chain, fan-out, join) are the closing members' own `plan_shape` fields, which
  `src/topology/census.rs` asserts one per shape of `PlanShape::ALL`. Defers are bounded at `max_defers - 1`, as the
  2026-09-15 amendment corrected them. **The limit, stated**: the union reaches the numeric bounds through the prefix; the
  seeded members reach sequences, repairs and lineages at their bounds, and no claim is made that each shape reaches every
  numeric bound.
- **Totality and the arms**: asserted by committed tests over every member's explored states and over the union (the
  artifact's table of the ten coverage assertions names the test and the scope of each), and all of them passed in every
  Linux suite this run kept green (**F13**, **F14**, **F15**).
- **Coverage assertion 5's named instances are measured, not assumed**: a be refused under halt=409 b halted after declined park=94 c parked refused with backoff=1167 c complete refused with backoff=1167 d halted accepted with backoff=14 d budget accepted with backoff=312 states accepting some outcome=4947 unfinished states=17392 (**F31**) — a temporary test appended to the census
  test module in a scratch clone, run once, the file restored to its range blob and the clone proved clean
  (`probes/census-instances/`). `src/topology/census.rs` is unchanged since run 4's range and the figures equal run 4's; that
  equality is a cross-check, not the evidence.

**Prohibitions.** Closure is claimed only of the three seeded members, each under its own ceiling and restricted generator;
no unbounded closure and no effect-phase claim is made of the census.

**Grade: established.**

## 8. Clause 2 — the inventory, the bijection, and the per-coordinate audit (required artifact 3, first part)

### 8.1 The inventory and its lists

`inventory/site-enums-export.md` is the site enums export: every site, every registry entry with its order, residue, tabled
action and evidence kind, and how many tests on each host record it. Beside it are the range's own copies of the lists —
`clippy.toml` (the disallowed methods, types and macros), `allowlist.toml` (funnel and frozen legacy sections),
`wrappers.toml`, `funnel-modules.json`, the registry and the residue declarations. The registry is the one run 4 audited
(1aeee56fc73ee033072fad814f9733347faca6f0 at both (**F7**)): 180 entries over 68 sites: executed 168, recovery_proven 9, not_executed 3 (**F65**); the `recovery_proven` entries are residue classes and the `not_executed` ones
fast-path entries. The observable-orders line of the export records one order or none on every executed
entry, which is the condition under which "a registry entry for every observed order" is the registry's order-free key
(`PR3-REG-001-CONDITIONAL`, §13).

### 8.2 The bijection, executed

ST-07's merge check (the ignored test `st07_the_sequential_range_is_a_bijection_over_the_exported_observations`) ran over a
copy of each Linux observation export, the copy proved unchanged afterwards and exactly one test selected
(`st07/st07-merge-check-export{1,2}.log`): failures 0; entries 180, witnessed claims 163, records 434, funnel executions 429 as the checker counts them, which includes the one record of the adapter test #318 moved to `runner::contract::tests` (production-funnel records without it: 428), inventory 68 (**F18**); failures 0; entries 180, witnessed claims 163, records 434, funnel executions 429 as the checker counts them, which includes the one record of the adapter test #318 moved to `runner::contract::tests` (production-funnel records without it: 428), inventory 68 (**F19**). The same check over a copy of the Windows guest's export (`guest/guest-st07.log`): one test selected, exit 0; entries 180, failures 0, funnel_executions 423, host windows, inventory 68, records 428, witnessed_claims 164; funnel_executions as the checker counts them includes the one record of the adapter test #318 moved to `runner::contract::tests` (production-funnel records without it: 422) (**F54**); that export carries the Windows-only points. This establishes observed
execution of every site and phase, and of every parent-executed point on the host that has it, with a registry entry for
every observed order. It does not establish ST-07's per-entry obligations; that is §8.3's work.

**Those counters are the unchanged checker's raw ones, and one record in each is not a funnel execution.** The checker counts
as a funnel execution every record whose test is not under `ADAPTER_UNIT_TEST_MODULES` (`src/engine/topology/coverage.rs`),
which still names `runner::tests::`; #318 moved the one adapter test there,
`harness_hooks_consult_every_mode_a_point_declares`, to `runner::contract::tests::`, so its record — `HarnessHooks::point`
called directly on two of the Windows-only points, recording three point-and-mode coordinates and executing no containment
primitive (`src/runner/contract.rs`, read at the range) — is counted as one.
The production-funnel population is therefore one smaller on each host than the raw counter. The repair round measured it
rather than reasoning it, with the checker unchanged (it is product code): over a raw copy of each export and a copy without
that module's records, on Linux in a clone at the range and on the guest natively (`ADAPTER_UNIT_TEST_MODULES` at the range: `workspace_manager::hooks::tests::`, `engine::topology::seams::tests::`, `runner::tests::`; the adapter test at `src/runner/mod.rs:2198` at 9bb177ea is at `src/runner/contract.rs:2200` at the range (module `runner::contract::tests::`), moved by `8f1c133a` of #318; its record, the only one the filtered copies drop, is in each export; Linux export 1 raw 434/429/163, without it 433/428/163; export 2 raw 434/429/163, without it 433/428/163; guest (native) raw 428/423/164, without it 427/422/164; failures 0 over the 6 runs, each exit 0 with one test selected: True, every copy unchanged: True; problems 0 (**F59**)). Every check stays
green without the record, and the witnessed claims do not move, so no credited witness rested on it: §8.3's audit credits no
`runner::contract::tests` witness, and the Windows-only points are reached by genuine funnel executions on the guest. The
stale exclusion is recorded in §13.

### 8.3 The per-coordinate audit on ST-07's three obligations, derived afresh

**The subject** is the 168 `executed` entries, numbered in file order (`audit/registry-rows.tsv`). **The rules** are this
run's own (`audit/grading/RULES-run6.md`): three obligations graded separately — (i) the exact durable prefix and residue
constructed, every durable file included (K a real kill at the coordinate, E an injected error, P a plant, `=` a fault or
plant at an adjacent coordinate with nothing durable between); (ii) the tabled action performed by a recovery production
runs, and asserted — a plan, a classification or a function production never calls does not count; (iii) the recovered
run's durable log replayed twice and the two states asserted equal, or vacuous (V) where no committed schema-4 event exists.

**Executed first: this run's own probe.** Every non-ignored library test ran in its own process in an instrumented scratch
clone of the range (2812 tests; 2 non-zero (`every_unix_containment_point_is_gated_on_unix_and_not_on_one_unix`, `every_unclassified_production_file_states_each_governed_lint_or_inherits_its_forbid`) (**F23**); both non-zero exits are source censuses reading the instrumented tree, neither a registry
witness, and both pass in the clean suites). The probe records every production hook consultation and its answer, entry into
each recovery component, and every `TopologyFold::replay` with its call site and digests (`audit/probe/`): 123 of 168; 89 (**F24**).
Its completeness against the suite's own observation export: before/after 136/136; points fired 27/32; exported but missed 0; Windows-only 5 (**F25**) (`audit/probe/reach-check.log`). **The search
domains were regenerated from this probe, never carried**: the firing list per coordinate for fault constructions, the plant
domain (124 tests (**F26**), `audit/grading/plant-domain-d724fb1.json`) for plants, and, for rows whose replay obligation is vacuous,
every test whose process entered the row's recovery component (`audit/grading/recovery-index.json`). No search was narrowed to
"the tests that consult the site" — the narrowing that lost run 4 four witnesses.

**Then graded, fresh, and machine-checked.** The graders (6 graders, 28/28/28/28/28/28 rows each (168 in all) (**F66**)) read their rows at the range from the packets
`audit/grading/packet-B*.json` (the registry entry, the packet's fault-row text for its transaction, and every probe
firing at the coordinate, with adapter unit tests flagged and never credited), under a brief that forbade reading any
historical grade (`audit/grading/GRADER-BRIEF.md`); their readings are `audit/grading/graded-B*.json` and their own
summaries `audit/review/grader-summaries.md`. `audit/grade-check.py` then refuses unless every row appears once with the
registry's coordinate, every witness is exactly one `#[test]` in the file its module path names, every cite's anchor resolves
to exactly one line of its named function at the range, every `K` has a probe-recorded death at that coordinate in the
witness, every `E` an injected error, and every `(iii) yes` a probe-recorded replay pair after the firing (for K/E) or in the
witness (for P/=). For the five Windows-only rows (144–148) the K/E check reads the Windows guest's observation export instead of the probe, which
ran on Linux; before that export existed the checker refused exactly those five rows, and that refusal is kept
(`audit/grade-check-before-guest-export.log`). **The result**: 168 rows: established 168; (i) K 75, E 35, P 37, = 21, none 0; (ii) yes 168; (iii) yes 137, V 31, no 0; checker failures 0 (**F27**) (`audit/per-coordinate-audit.md` and `.tsv`,
`audit/grade-check.log`).

**Adjudicated, and every limit printed.** The graders raised caveats rather than silently crediting; each was ruled on, and
the ruling either keeps the grade with its limit printed beside the row in the audit table or replaces a field with a reason
(`audit/review/adjudication.py`; 79 (**F28**); no grade was replaced). The limits a reader most needs:

- **Rows witnessed at their exact coordinate only inside terminal finalization** (11: rows 12, 13, 14, 19, 20, 21, 22, 27, 28, 29, 30 (**F67**)) — `Worktree.Remove` and
  `RemoveIntent` (rows 12–14), `Snapshot.Remove` and `RemoveIntent` (27–30), mapped to T-SCRUB, and `Worktree.RemoveStaging`
  and `RemoveStagingIntent` (19–22), mapped to T-PROPOSAL. The packet keys a registry entry by site × phase × order × mode,
  with the fault-matrix row an attribute of the entry, so a real kill at the entry's exact coordinate discharges it. The
  recovery that follows is not the same in both contexts, though, and the adjudicated limit's "transaction-independent" says
  too much (`audit/review/v3-correction.md`, read at the range): after a kill in the live sequence, production recovers these
  slots through `WorkspaceManager::remove_worktree` (T-SCRUB's `reclaim_closed_generations` → `dispatch::scrub`, T-PROPOSAL's
  `finish_integration`, the snapshots' `reclaim_snapshot_residue`), which proves nothing about writers (`WriterProof::Unknown`),
  where terminal finalization's `scrub_slots` removes through `remove_worktree_proving(…, NoWriterAlive)`. They share the
  funnel, its sites and phases, and the adoption of an already-absent checkout, and differ on a registration that names no
  checkout, which `Unknown` refuses and `NoWriterAlive` passes over. **The live path's recovery at these coordinates is not
  executed** by any witness in the search domains, and the audit says so row by row. Row 11 (`Worktree.Remove/before`) has a
  live T-SCRUB witness at the adjacent `Ref.DeleteCandidatePin/after`.
- **Rows 81 and 83** (the report's before-phases) are killed, in their audit witnesses, over a planted finished run that also
  carries a dead report writer's staging leftover; T-FINALIZE's own boundary ("report.json missing or stale; cleanup steps
  partially applied … repeated on every resume until converged") contains that state, which is a prefix of a run whose
  earlier finalization was killed inside the report write. The repair round's probe kills the same two coordinates in a
  first finalization as well, with no report staging begun, and the fresh resume converges (**F60**, §4).
- **Rows 5–8 and 59–60** recover through `dispatch::resume_open_no_attempt` called by the test directly; its production caller
  is `TopologyRun::continue_open` (`src/engine/topology/run.rs:1097`), reached from `TopologyRun::step`, not
  `run_recovery_order`.
- **Rows 144–148** are §8.4's.

**What was carried, and what was not.** Nothing of run 3's or run 4's readings was an input. **After** the fresh audit passed
its checker, it was compared with run 4's reviewed table as a cross-check only (`audit/review/cross-check-run4.txt`):
all three obligations credited: this run 168/168, run 4 168/168; same witness on 159/168 rows; construction or replay label differs on 7 rows (**F29**). Every difference is a construction label or the choice between two witnesses of the same obligations, listed
row by row in that file.

### 8.4 The Windows-only rows

Rows 144–148 are `Process.Spawn`'s Windows-only points — the ambient job join (kill, and error return), the suspended creation,
the private job's assignment and the resume — whose production path is `#[cfg(windows)]` and never fires on Linux. They are
graded by the same rules as every other row and checked by the same checker, against the guest's export instead of the
probe (`audit/grade-check.log`: all checks pass; the refusal before the export existed is `audit/grade-check-before-guest-export.log`):

- **(i).** Rows 144, 146, 147 and 148 are real kills: `a_kill_at_each_windows_point_of_the_workers_spawn_leaves_no_process_and_the_next_resume_converges`
  runs, for each of the four points, a kill child armed at exactly that point, asserts it died by abort and asserts the durable
  prefix it left (nothing at the ambient join; `run_resumed`, `task_dispatched`, `attempt_started` after it), that the spawned
  worker is gone and the run lock released. The guest export records each point reached in that kill child
  (`process_spawn_kill_child`). Row 145 is an injected error: `an_error_at_the_ambient_job_join_refuses_the_write_command_and_the_next_resume_converges`
  arms the error return at the ambient join, asserts the write command is refused, the armed point observed and nothing
  appended, and the guest export records the point reached in that witness.
- **(ii).** Each witness then resumes through production recovery (`resume_as`, whose step (d) settles the attempt the kill
  interrupted, asserted by count: none at the ambient join, which precedes the attempt, one at the three later points) and drives the next attempt to acceptance (row 145: the resume converges and appends only
  `run_resumed`).
- **(iii).** Both witnesses end in `assert_replays_twice_equal`, which passed in S-guest-1, S-guest-2 and both CI Windows legs
  (§2.2, §2.3). The probe, which ran on Linux, saw no replay on Windows; for rows 144 and 146–148 the checker credits the probe's
  replay pair in the Unix twin that calls the same driver
  (`a_kill_at_each_unix_point_of_the_workers_spawn_is_settled_by_the_reaper_and_the_next_resume_converges`), and row 145, which
  has no twin, carries its replay-twice credit on the guest's passing executions of that assertion, labelled *read* in the
  audit table because no probe recorded it.

So every obligation of these rows is executed on Windows, by this run on the guest and by CI on its Windows legs. **What rests on
the witnesses' own assertions rather than on a record the checker reads is more than one pair, and it holds for all five rows**
(corrected in the repair round, VERIFY V-2): the guest export's records carry `test`, `observed`, `reached` and
`fast_sequences` only — no injection answer and no death — so the kill of rows 144 and 146–148 is the witness's own
`died_by_abort` and row 145's error its own assertion that the write command was refused with the armed point observed; and
on Windows the replay-twice of all five is the witnesses' own `assert_replays_twice_equal`, the probe pair the checker
credits for 144 and 146–148 being the Unix twin's, recorded on Linux at other points. Those assertions executed and passed
on the guest in both suites and on CI's two Windows legs, and the grades rest on them.

### 8.5 The grade

**Clause 2 — site bijection complete with executed evidence for every Topology/Shared site, order and parent-executed point
(incl. OpenLog SyncPrefix and the stable-prefix barrier refusal cases): established.**

- **The bijection** is ST-07's merge check over both Linux exports and the guest's (**F18**, **F19**,
  **F54**): no failure on either host, every site and phase observed, every parent-executed point observed on the host
  that has it, and a registry entry for every observed order (§8.2), under the condition §8.1 measures for an order-free key.
  Those figures are the unchanged checker's raw counters; without the one adapter record its stale exclusion lets in, the
  production-funnel population is one smaller on each host and every check stays green with the same witnessed claims
  (**F59**).
- **Executed evidence per entry** is §8.3's audit: all 168 executed entries carry ST-07's three obligations, derived afresh from
  this run's probe and checked mechanically (**F27**); the five Windows-only rows are checked against the guest's export
  and executed on Windows by this run and by CI (§8.4).
- **`Event.OpenLog`'s SyncPrefix and the barrier's refusal cases.** SyncPrefix's kill and error return are audit rows 97–98 and
  the barrier, `Event.ProvePrefixStable`, rows 99–100; the refusal cases are committed named tests, green in every green suite here, of which group 1 carries the re-read and
  four-predicate refusals and the barrier's ordering (§4). Their detection is
  mutation-proved: removing the barrier's sync or the funnel's consultation of the SyncPrefix point fails the two-crash witnesses (§9); the
  barrier's refusals — a first-line digest that disagrees with the commit record or is missing from it, an invalid terminated
  line, a replay whose digest or Runner identity disagrees, an unstable re-read (content, length, a torn tail reappearing) — and
  its ordering before any fold-derived effect are each killed by a mutation that removes them (§5, sets `g5` and `g5r2r5`).
  What SyncPrefix's executed evidence shows is the funnel reaching the sync and recording it, not the platter written (§9's
  limit).
- **Held to the limits §8.3 prints**, none of which removes an obligation's credit: the rows witnessed at their exact
  coordinate only inside terminal finalization (**F67**), whose recovery in the live T-SCRUB and T-PROPOSAL sequences —
  through the plain removal funnel rather than the proving one — is not executed; the report-before cells' audit witnesses
  killed over a planted leftover T-FINALIZE's own boundary contains (the repair round's probe also kills them in a first
  finalization, **F60**); rows 5–8 and 59–60 recovered through the dispatch helper `TopologyRun::continue_open` calls
  rather than through `run_recovery_order`; and, on Windows, the kills, the error and the replay-twice of rows 144–148 resting on
  the witnesses' own assertions (§8.4). The cross-check with run 4's reviewed table agrees on every obligation (**F29**); it is not evidence.
- **The prohibition.** No row credits a parent-observed execution inside a Git child: residue inside Git commands is clause 4's,
  labelled recovery-proven (§10).

## 9. Clause 3 — the two-crash proof

Both variants are committed tests (`unsynced_merge_prepared_two_crash_barrier_before_cas_then_power_loss_keeps_log_and_ref_agreeing`
and `barrier_sync_failure_before_cas_issues_no_cas_and_converges_after_loss`, in group 2, §4) and both passed in every green
Linux suite at the range. **The bytes on disk** before and after each simulated loss were printed by this run's projection
probe (`probes/projection/probe-run.log`), in a scratch clone restored and proved against its range blob:

- **positive variant**: 8 lines, 4701 bytes, merge_prepared 1, task_merged 1 (sha256 `3665169f9a2cc64c…`) / 7 lines, 4492 bytes, merge_prepared 1, task_merged 0 (sha256 `9bc0316d6346e0c1…`) (**F33**) — after the second simulated power loss `merge_prepared` survives and the unsynced
  `task_merged` is gone, one line shorter; the next resume records the merge (the task is `Merged`) without a second CAS (`cas_integration_entries` is 0).
- **negative variant**: 7 lines, 4501 bytes, merge_prepared 1, task_merged 0 (sha256 `b32b761b5eb7379f…`) / 6 lines, 3992 bytes, merge_prepared 0, task_merged 0 (sha256 `3d6ec82d729f46f1…`) (**F34**) — with the barrier's sync failing, no CAS is issued, and after the loss the unsynced
  `merge_prepared` is gone and the candidate is still queued.

The lines and the event counts are the proof's; the byte lengths and hashes are this run's only. The log embeds the fixture's
absolute paths, so the same probe elsewhere prints other bytes: VERIFY's re-run at the range gave every length 13 bytes
longer, with identical line and event counts.

**Detection, by mutation at this range** (§5): removing the barrier's sync of the log in `src/events/log.rs` (`g5-open-sync-skipped`, `mutations/run-g5/g5-open-sync-skipped.log`) fails the positive variant at `src/engine/topology/recover/tests.rs:7386` ("a sync was recorded"), and removing the funnel's consultation of the `SyncPrefix` point (`g5-sync-prefix-point-not-consulted`, `mutations/run-g5/g5-sync-prefix-point-not-consulted.log`) fails the negative variant at `src/engine/topology/recover/tests.rs:7755` (the barrier whose sync fails no longer ends the command). Each named test is the only one its invocation ran.

**The limit, stated.** The loss is simulated by the test, which discards the unsynced bytes itself; whether a physical sync
reached the platter is invisible to any test (`PR5-C-FSYNC-UNOBSERVABLE`, P2, `deferred`, §13). The proof establishes the
barrier's ordering and the recovery over the surviving bytes, not that the barrier made them survive.

**Grade: established** for the simulated loss, with the physical-sync boundary carried by that finding.

## 10. Clause 4 — the residue-class evidence (required artifact 3, second part)

**The artifact** is `residue/residue-class-evidence-record.md`, generated by `residue/residue-record.py` from the range's
residue declarations and synthetic half (`effects/residue-classes.json`, `effects/residue-synthetic.json`) and from the
histograms this run's own sampler executions wrote: 9 declared residue sites, 24 (site, element) pairs, 24 constructed, classified and recovered in the synthetic half; 110 histogram site rows, 0 unclassified or unrecovered; runs with no histogram 0; runs observing no internal residue 0 (**F51**).

**The sampling half**: the five Git-child kill samplers, each alone and five times, on Linux in the worktree at the range
(`samplers/linux-loops.{sh,log}`, judged by `samplers/summarize.py`; `sampled_git_add_and_write_tree_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` 5/5 ok; tree dirty after a run: 0 (**F50**)); on the Windows guest the same loops, five runs of each sampler alone (`guest/guest-samplers.log`; `sampled_git_add_and_write_tree_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered` 5/5 ok; `sampled_git_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` 5/5 ok; `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` 5/5 ok (**F55**)). Like the groups, every run ran the test binary S1 built from the worktree at the range (`samplers/linux-loops.log` names it; `groups/binary-identity.txt`), and each writing sampler's five histograms differ from one another, so each run wrote its own. A sampler passes only when every residue it
observes classifies into exactly one of the declared classes and recovers by the classified action; because a descheduled
sampler can observe nothing and still pass (`CLASS-INTERMITTENT-SUBPROCESS-KILL-SETTLE-RESIDUE-FAILURES`), the record reads the
histograms' landed-kill counts, not only the exit. On the guest the census sampler spawns `git` by name, so its kills end Git for
Windows' launcher rather than git (`PR249-KILL-SAMPLER-WINDOWS-WRAPPER`, P2, `deferred`; `SampledChild::spawn` in
`src/workspace_manager/tests.rs`): its guest rows are green runs, not residue evidence on that host, and the record says so; the
other four kill a `fixture::KillableGitChild`, which spawns the binary `sampled_git` resolves — on Windows the real
`mingw64\bin\git.exe`, found from `git --exec-path`, falling back to `git` by name only when that layout is absent
(`src/workspace_manager/fixture.rs`).

**Labels.** Every class entry is labelled recovery-proven, never observed execution, and the tree refuses a residue-class entry
that carries an executed-hook claim (the bijection's completeness rule).

**One residue has no class.** The report-staging leftover a writer killed inside `Report.Write` leaves is outside the declared
classes (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`, P2, `deferred`, whose own guard text says Gate 5's residue clause is
judged with it outstanding). It is named, not counted.

**Grade**: **established**, with its limits stated. Every declared (site, element) pair is constructed, classified and recovered in the
synthetic half, and every histogram row this run's sampler executions wrote on Linux and on the guest is classified and
recovered, no run observing nothing inside a Git command (**F51**); every class is labelled recovery-proven, and no
parent-observed execution inside a Git child is claimed. The limits: the report-staging leftover a writer killed inside
`Report.Write` leaves has no declared class (`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`, named and not counted); the guest's
census-sampler rows are not residue evidence on that host (`PR249-KILL-SAMPLER-WINDOWS-WRAPPER`); and a sampler's pass says
what the kills it landed found, not that every interleaving was sampled.

## 11. Clause 6 — projections equal (required artifact 4)

**The artifact** is `probes/projection/probe-run.log`. The committed test
`projections_are_equal_between_live_and_replay_at_every_prefix` compares, after every append the resumed process makes, the
report derived from the live fold with the report derived from a replay of the durable prefix, and replays every prefix of
the final log twice. This run's probe added print statements only (no assertion and no control flow) inside that comparison,
so that for **every** prefix the test compares, the durable bytes holding exactly that prefix (their length and sha256), the
live digest and the replay digest are on disk: 6/6 live snapshots equal (prefixes 2–7); 7/7 prefixes replay twice equal (**F32**), and for every one of them the two digests the committed
`assert_eq!` compares are equal. The same log carries the two-crash bytes (§9) and the byte snapshots at the other replay
witnesses the probe instruments.

**What this covers, stated exactly**: the per-append equality inside one resumed run of a two-task plan under a spending
ceiling (the run defers, waits, stops for budget and ends), every prefix of its log, and replay-twice equality at each; the
live-versus-replay classification over every census trace is clause 1's assertion 9 (§7). **Detection, by mutation at this range** (§5): the committed test fails when the live fold is snapshotted before the append's delta is applied (`r2r5-projection-folded-before-apply`, `mutations/run-g5r2r5/r2r5-projection-folded-before-apply.log`: at `src/engine/topology/recover/tests.rs:24069`, prefix 2's live report differs from its replay), when no live snapshot is taken (`projection-no-live-snapshot`, `mutations/run-pr10/projection-no-live-snapshot.log`), when the snapshot is handed a prefix one event short (`projection-stale-prefix`, `mutations/run-pr10/projection-stale-prefix.log`) and when the resume's own `run_resumed` is skipped (`projection-skips-run-resumed`, `mutations/run-pr10/projection-skips-run-resumed.log`); the last three fail at `src/engine/topology/recover/tests.rs:24043`, the check that the first live snapshot is the resume's `run_resumed`. Each invocation ran that one test.

**Grade: established.**

## 12. Clause 7 — the per-resource ledgers (required artifact 5)

**The artifact** is the six outcome tables the suite writes under `UPSTROKE_LEDGER_EXPORT` — Complete, Parked, Halted,
BudgetExceeded, no-`run_finished`, and the resumed no-`run_finished`-then-Complete run — in `ledger-export1/` and
`ledger-export2/` (byte-identical to the first, **F17**) and the guest's `guest/exports/export/ledger/` (every row `yes` there as well, **F58**). `ledger-budget-exceeded.md` 54 rows, 54 `yes`; `ledger-complete.md` 54 rows, 54 `yes`; `ledger-halted.md` 54 rows, 54 `yes`; `ledger-no-run-finished-then-complete.md` 54 rows, 54 `yes`; `ledger-no-run-finished.md` 54 rows, 54 `yes`; `ledger-parked.md` 54 rows, 54 `yes` (**F35**): every row, and every sub-row (R14's counters, R21's run-directory
contents, R27's object store), holds in every table. Each table checks the recorded outcome against the one derived from
durable inputs; closure per class is group 4's (§4) and finalization's idempotence group 5's.

**Finalization under process death, executed.** ST-18 asks that a kill after `run_finished`, after the report write and
between every cleanup effect converge to the outcome equations, for Complete and for Halted. The repair round killed a real
finalization at every cell of each constructed run and resumed it in a fresh process, twice: 14 tests, one invocation each, exactly its named test selected, exit 0: 14/14; 386 cuts, 386 pass; first finalization no leftover: 24/22 of 24/22 cells; all kinds after ordinary intents: 30/28 of 30/28 cells; two ordinary slots and pins of each kind with every leftover kind: 44/42 of 44/42 cells; task leftover first occurrence: 22/20 of 22/20 cells; snapshot leftover first occurrence: 22/20 of 22/20 cells; staging leftover first occurrence: 22/20 of 22/20 cells; two of each kind after ordinary intents: 36/34 of 36/34 cells; the sequence derived from the source equal to the recorded finalization 14/14; problems 0 (**F60**). After every
cut the durable prefix is exactly the effects whose `before` cell preceded the kill, and after the resume every row the
cleanup acts on holds its Complete or Halted class — task, snapshot and staging worktrees and intents pruned (R9, R10, R24),
the staged-intent leftovers removed, prepared and candidate-prepared pins deleted, candidates refs pruned at Complete and
retained and listed in the report at Halted, the execution root pruned (R18), the answer files and `.partial` residue
byte-identical (R21), nothing appended to the log, and no object the run referenced deleted (R27) — the report is current and
names the recorded runner, the log replays twice equal, and a repeated resume rewrites nothing. Removing the leftovers'
cleanup makes every leftover cut fail (mutated file equal to the reviewer's: True, and to run 6's 4 `fix291-*` recipes': True; tests planting a leftover exit 101: 12, their cuts failing 340 of 340 (the fresh resume left staged-intent leftovers standing 232, the kill child never reached the armed occurrence and did not die there 60, the killed child had consulted a sequence other than the derived one 48); the tests planting none exit 0: 2 (46 cuts pass); restored to the range blob: True, then 14/14 tests exit 0, 386/386 cuts pass (**F61**)). This is the evidence REGRESSION's R6-REG-01 found missing: the
committed kill matrices plant no staged-intent leftover, and the one witness that does injects an error rather than a death.

**Not established: convergence without an operator in the filed torn-registration shapes.**
`PR5-RD-003-A-PRUNE-STRANDS-A-CHECKOUT-WHOSE-GITDIR-IS-GONE`, `PR5-RD-002-TORN-REMOVAL-NOT-DURABLE`,
`PR5-RD-002-RESUME-DERIVES-THROUGH-A-TORN-ENUMERATION`, `PR308-R3-SKIPPED-PRUNE-KEEPS-ANOTHER-RUNS-TORN-REGISTRATION` and the
two `PR8-CRASH-002` residuals are all P2 and `deferred` (§13). Their refusals are resumable and lose nothing, so no equation
is falsified; "converges" holds for the measured runs, and none of those shapes was re-executed here.

**Grade: established** for the six outcome tables, the derived outcome and closure as measured, and finalization under a
kill at every cleanup-effect boundary of each constructed finalization, for Complete and Halted (**F60**); the filed operator shapes
are named as not established, and no equation is falsified by them.

## 13. The findings in range, and what each does to the verdict

**The ledger at the range**, read with `git show` at the range rather than from any worktree: 357 files; {"P1": 8, "P2": 146, "P3": 203}; {"deferred": 351, "accepted-risk": 6}; `open`: [] (**F36**)
(`ledger/ledger-at-range.{py,json,txt}`). **No finding is filed `open`**, and this report files none. What the range did to the
ledger since run 4's range is `ledger/range-delta.txt`: three files added and none of run 4's removed — `PR311-NEW-RECOVER-PROSE-OUTSIDE-NOTES`
(P3, #315), `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT` (P2, #316, narrowed through #318), and
`PR320-R7-PRODUCTION-LEASE-PROBE-RETRIES-AN-INTERRUPTED-OPEN` (P3, #320) — while #318 filed and then closed
`GUARD-DECISION-SILENT-PRODUCTION-FILES-OUTSIDE-THE-ROLL-CALL` inside the range. The deferred rows #318 and #320 merged with
(`R5-MAIN-01`, `PR320-R8-*`) are rows in those pull requests' ledgers; their files are being filed by another lane after the
range and are not in it.

**Every file was placed, not a selection** (`ledger/assess-findings.{json,md}`, one record per file with its bearing and the
clauses it bounds; none undecided): on-G5-evidence-conditions 62, on-a-mechanism-G5-certifies 142, outside-G5 153 (of 357) (**F37**). Closures that earlier records rely on were verified at the range rather
than carried: `PR8-R2-SPEND-REPLAY` (removed by `7da321c0`) and `PR8-CRASH-002` (resolved by `1c309d49`), both ancestors of
the range, with `PR8-CRASH-002`'s two residuals still filed; `PR5-RD-002` has no file at the range (removed by `dfab458b`,
#308).

**The P1s at the range, all `deferred`**, and what each does:

| finding | bearing |
|---|---|
| `PR7-WRAPPERS-EMPTY-DOMAIN` | on a mechanism this gate certifies: the wrapper classification. v17's Q5 names it as the carrier of the residual bypass inside the file-level carve-out (§14). Its text still says the route is open in production `src/runner/container/exec.rs` at `deny`; at the range #318 fences that file (and `census.rs`, `resolve.rs`) with `cfg_attr(not(test), forbid(..))`, so that sentence is stale, and the finding stays `deferred` for the carve-out's residue |
| `PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS` | on a mechanism this gate certifies: the workspace manager's containment table covers nine roles and not Git's discovery paths (Q4, Q5's platform-containment sub-effects). No witness here constructs the shape; the trust boundary is the owner's design question |
| `PR7-SCRATCH-FIXTURE-LEAK`, `PR64-CLEANUP-003-SCRATCH-PRECLEAN` | on this gate's evidence conditions: the suite's scratch fixtures leak (each Linux suite here left suite-s1 1014, suite-s1-rerun 1014, suite-s2 1014, suite-s3 1014 entries; not named `upstroke-*`: suite-s1 0, suite-s1-rerun 0, suite-s2 0, suite-s3 0 (**F47**) in its private TMPDIR, `logs/tmpdir-leftovers.txt`) and a predictable scratch root can be pre-cleaned under another process. Every Linux suite here ran with a fresh private TMPDIR, and every probe, preflight lane and mutation set with a private one |
| `CLASS-INTERMITTENT-SUBPROCESS-KILL-SETTLE-RESIDUE-FAILURES` | on this gate's evidence conditions: intermittent kill/settle/residue reds with no established cause, and samplers that observe nothing when descheduled (§10 reads the landed kills, not only the pass) |
| `SHAPE-GATE-MISSES-ARITHMETIC-SUBSTITUTION`, `PR286-PROSE-SCANS-READ-THE-WRITTEN-SPELLING` | instruments outside the tree this gate certifies (the PR-policy gate and the merge-readiness parser) |
| `LEGACY-WORKSPACE-READS-REPLACEMENT-OBJECTS` | the schema-1..3 workspace; outside the schema-4 topology |

**The deferred findings that bound a grade**, judged rather than listed (each is P2 or P3 unless marked; none is `open`; none
falsifies a clause's property as measured; each is stated beside the grade it bounds):

- **Clause 3**: `PR5-C-FSYNC-UNOBSERVABLE` — the physical sync is invisible to any test (§9).
- **Clause 4**: `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED` — one residue outside the declared classes, named and not counted
  (§10); the sampler-side hazards `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` and `PR247-SAMPLER-REFUSED-A-LOCKED-INDEX-ON-WINDOWS`
  bear on the guest's sampling half.
- **Clause 5**: the fingerprints of this run's own reds — `PR281-CLEANUP-LEASE-HOLD-OUTLIVED-AND-ITS-UNREADABLE-TWIN` (S1's
  lease refusal), `PR274-DOCKER-TERMINATION-POLL-COUNTS-YIELDS-NOT-TIME` and `TESTS-CONTAINER-WAIT-HAS-NO-TIME-BOUND` (S1's
  Docker poll; #319, which bounds it, is not in the range), and `PR262-UNREACHABLE-PATH-ENTRY-ABORTS-PROGRAM-RESOLUTION` (the
  red Windows CI leg at the range, §2.3). A reclassified fingerprint is a reasoned classification, not an established cause.
- **Clause 7**: the torn-registration family (§12).
- **Clause 8** (Q5): `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT` carries the pinned residue inside the carve-out, and
  `PR7-CLASSIFICATION-DOMAIN-READS-FNS-ONLY` (reasoned, not executed; no such item exists at the range) is inside it too;
  `PR3-REG-001-CONDITIONAL` and `PR4-REG-001-STILL-EQUIVALENT` bound "an entry per observed order" to the condition §8.1
  measures; `PR7-STD-PRIVATE-ROOT-NO-CONTAINMENT` and `PR7-STD-OWNERSHIP-PROOF-UNCANONICAL` bound Q1's and Q4's root and
  ownership answers to the shapes constructed; the source-text recogniser family (`PR73-*`, `PR101-*`, `PR103-*`, `PR107-*`,
  `CLASS-GATE-STATED-DOMAIN-EXCEEDS-COUNTED-DOMAIN`, `W1-CLASSIFIED-MODULES-IS-A-HAND-MAINTAINED-ROLL-CALL`) bounds what the
  effects censuses can read, which is why §3's boundary evidence is the compiler's, not the readers'. And #318's own
  deferred residual, `R5-MAIN-01` (a row of #318's ledger; its finding file is filed after the range), is re-executed here:
  the checker credits an undecodable literal inside a true `any()`, and rustc refuses every such source (`r5-main/10-undecodable-literals`: at d724fb16, Clippy exit 0, effects exit 101; 63 undecodable literals measured, the checker credits 21, rustc refuses 63 of 63 with `E0539`; `r5-main/13-invalid-any-guard-witness`: Clippy exit 0, effects exit 101, the named witness fails at `src/effects/tests.rs:7540:5` ("a literal rustc refuses excused the deny", 6 excusals), 9 fixtures, rustc refuses 9; restorations proven: True (**F63**)), so
  it bounds the effects checker's reading, not the lint boundary Q5's answer rests on (§6, §14).
- **Clause 9**: graded on disposition. Two deferred P1s are on mechanisms this gate certifies (above) and three on its
  evidence conditions; they weaken the pass and are not treated as closed.

**Filed by this report:** nothing. **Observed and recorded, not filed**:

- the packet's T-PROPOSAL text orders the staging reclaim "intent then worktree" where production removes the worktree first
  (a grader's note; no grade depends on it);
- **ST-07's adapter exclusion is stale** since #318: `ADAPTER_UNIT_TEST_MODULES` (`src/engine/topology/coverage.rs`) names
  `runner::tests::`, and the adapter test it meant is now under `runner::contract::tests::`, so the checker's
  `funnel_executions` counts one adapter record as a funnel execution on each host. Measured, not reasoned, with the checker
  unchanged (it is product code): every check stays green without that record (**F59**, §8.2). REGRESSION's
  R6-REG-02 found it; it is a `deferred` row of this pull request's review ledger, for a product change this lane does not
  make;
- **a test defect of G7's real-Docker territory**: `real_docker_a_container_contains_a_daemonised_descendant` puts fixed
  markers in its container and scans the whole host's `/proc` for them (`src/runner/container/exec/tests.rs`, the markers at
  lines 4604–4605 and the scan at 4607), so two runs on one box fail it or pass its guard vacuously; it collided with another
  lane during this run (§2.1). For G5 it is an evidence condition only; that lane is filing it.

## 14. The six standing questions, scoped to G5's range

The questions are `spec/G5-QUESTIONS.regen`, regenerated from the packet and identical to the derived file on disk for all six
(**F3**). The definition assigns the portions (§1.2): the process-local and broker rows R3, R4, R13, R17, R22 and R28,
INV-18 and INV-21 are G6's, with R17, R22 and R28's host portions asserted here, and real-Docker R19/R26 are G7's. What G1–G4
certified is cited, not re-claimed.

**Q1, reconstruction.** *Answered with evidence for G5's portion.*

- **The durable bytes are replayed and compared with the live fold**, not reasoned about: at every prefix the resumed process
  appends in the projection witness, the report derived from the live fold equals the report derived from a replay of exactly
  the durable bytes holding that prefix, whose length and sha256 are on disk beside both digests, and every prefix of the final
  log replays twice equal (6/6 live snapshots equal (prefixes 2–7); 7/7 prefixes replay twice equal (**F32**), §11). At every recovered coordinate of the audit whose replay obligation is not vacuous,
  the recovered run's log is parsed from disk and replayed twice, and the probe recorded the pair (§8.3).
- **No fold-derived effect precedes the stable-prefix barrier of a fresh process**: the Event and OpenLog coordinates (rows
  91–121) are credited at the coordinate level (§8.3), and the two-crash proof shows the integration ref never outruns a
  durable `merge_prepared` for the simulated loss (§9). The physical sync is outside any test's sight
  (`PR5-C-FSYNC-UNOBSERVABLE`).
- **The resume derives its authorized root from the recorded locator and rebuilds the recorded Runner before its pre-flight**:
  the barrier's refusals of a disagreeing commit record and of a disagreeing plan digest are killed by mutation (§5, `g5r2r5`),
  and ST-20's witnesses are in group 9 (§4). That the derivation precedes every lock is **read from the code** (the recovery
  order's first step takes the locks over the root it was handed), not executed separately. The locator is validated lexically
  (`PR7-STD-PRIVATE-ROOT-NO-CONTAINMENT`, deferred); no witness here constructs a linked component in it.
- **Durable recovery records reclaimed or repaired before reuse**: at the coordinate level for every executed registry entry
  (§8.3), and **not** in the filed torn-registration shapes, which need an operator (§12).
- **Process-local holdings empty at start** are G6's, except the observed reaper hold, which is refused until released: rows
  138–139, by plant, with the next resume's observation asserted (§8.3).

**Q2, resource accounting.** *Answered with evidence for G5's durable rows.* The six outcome tables (`ledger-budget-exceeded.md` 54 rows, 54 `yes`; `ledger-complete.md` 54 rows, 54 `yes`; `ledger-halted.md` 54 rows, 54 `yes`; `ledger-no-run-finished-then-complete.md` 54 rows, 54 `yes`; `ledger-no-run-finished.md` 54 rows, 54 `yes`; `ledger-parked.md` 54 rows, 54 `yes` (**F35**), §12) check
every durable row's class at each outcome — including R21's answer and staging files and the private owner and commit records,
and R27's unreferenced objects enumerated with `git fsck --unreachable` — and the recorded outcome against the one derived from
durable inputs. Closure per class and finalization are groups 4 and 5 (§4); finalization's rows under a process killed
between any two of its cleanup effects, for Complete and Halted, are the repair round's kill cuts (**F60**, §12). One residue has no class
(`PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED`, §10). The slot, provisional, invocation, lock-hold and process-handle grants are
G6's.

**Q3, stale completions.** *Answered with evidence for G5's portion*, which is the returned append error: every error-return
point of the three append sites is credited at the coordinate level (rows 104, 105, 107, 111, 112, 114, 118, 119, 121; §8.3),
each with the append-error protocol's outcome asserted — the fold untouched, no retry, no report from memory, no cleanup, and
the next open decides from the surviving prefix. Stale generations, attempts and invocations are earlier gates' portions.

**Q4, aliasing.** *Answered with evidence for G5's portion*: private-half ownership and the fast publication's object. The
ownership proof refuses a forged, copied, foreign-root, reparse-point, owner-less or disagreeing husk and deletes nothing of it
(`engine::topology::startup::tests`: `forged_marker_naming_foreign_run_never_deletes_private_half`,
`copied_husk_from_other_repository_retained_repo_key_mismatch`, `locator_outside_authorized_private_root_retained`,
`locator_through_reparse_point_retained`, `private_dir_without_owner_record_retained_and_reported`,
`owner_record_disagreement_retained`, `runner_digest_mismatch_retained` and `every_retain_reason_kind_deletes_nothing`, each
passing in S1's re-run, S2 and S3); the fast publication names the gated candidate object itself (census assertion 8's
relations in both directions, §7, and the fast no-execution record, §8.1). `PR120-TABLE-OMITS-GIT-DISCOVERY-PATHS` (P1,
deferred) and `PR7-STD-OWNERSHIP-PROOF-UNCANONICAL` (P2, deferred) name shapes no witness here constructs.

**Q5, effect and append boundaries.** *Answered with evidence, and affirmatively under v17's wording, for G5's portion, with the limits below stated beside it* —
the first report of this gate to do so: every earlier one that reached a verdict graded clause 8 asserted
(`reviews/2026-09-16-gate-G5.md`, `reviews/2026-09-16-gate-G5-run3.md`, and run 4's draft #314), and run 5 stopped before
certification. That history is context, not evidence. **Q5 was amended v16 -> v17 on 2026-09-22 on the project owner's
authority, and an affirmative answer under v17 is narrower than v16 asked for: the wrapper classification is answered over
the modules it classifies, with the files that carry a file-level allowance of a governed lint excluded as a carve-out this
record enumerates, the residual bypass inside that carve-out carried by filed findings, and completeness over the carve-out
not claimed.**

- **Hook phases, points and orders**: both hook phases of every site and every parent-executed point are observed executed,
  with a registry entry for every observed order (§8.2). At `Event.OpenLog`'s SyncPrefix the executed
  evidence is the funnel reaching the sync and recording it, not the platter written (§9).
- **Executed evidence of the tabled action and residue per entry**: §8.3's audit, **F27**, with rows 144–148 as §8.4
  states.
- **Residue classes**: synthetic construction per element and the sampling records (§10).
- **The lists and the dependency review**: the Clippy disallowed lists, the allow-placement scan, the funnel-module allowlist
  and the frozen legacy allowlist are held against the code by the effects censuses, green in every green suite and in group 8
  (§4); Clippy over all targets with `-D warnings` passes at the range. The dependency review: packages at G4: 64; added none, removed none, changed none (**F46**)
  (`q/dependency-review.log`), so no new crate needs a disallowed-list entry or a funnel.
- **Four domains, kept apart** (`q/q5-domains.{py,json,md}`): the carve-out v17 names is the files carrying a *file-level*
  allowance (a leading `#![allow]`/`#![expect]`) of a governed lint, read by this run's own prologue reader from every tracked
  file at the range (`q/prologue.py`): 43 files: example-only 1, production-lib 27, test-only (lib test harness) 14, bin-only 1 (**F38**). The allowlist's rows are a different count: 51 rows, 45 with allows; not a file-level allowance: `src/agent/bin.rs`, `src/agent/proc/test_support/readiness.rs` (**F39**) — `src/agent/bin.rs`'s
  row records its test module's outer allowance and `src/agent/proc/test_support/readiness.rs`'s row records per-site
  expectations under a file-level `deny`, so neither is a file-level allowance. The classified modules: 54: 29 inside, 25 outside; outside, forbidding all three lints in production: 25 (**F40**). The
  file domains are rustc's own dep-info (§1.4), and the four declaration-only roots (`src/lib.rs`, `src/agent/mod.rs`,
  `src/runner/mod.rs`, `src/engine/mod.rs`) are held to declarations and re-exports by
  `a_declaring_module_holds_declarations_and_re_exports_and_nothing_else`.
- **The carve-out, enumerated as v17 requires** (every path read at the range; † marks a file no production build reads — the
  library's test harness or the example):

  *Funnel (22):* `src/agent/proc.rs`, `src/agent/proc/pipe_io.rs`, `src/agent/proc/tests.rs`†, `src/effects/tests.rs`†, `src/events/log.rs`, `src/events/log/tests.rs`†, `src/interaction.rs`, `src/rundir.rs`, `src/rundir/scratch_tree.rs`†, `src/rundir/tests.rs`†, `src/runner/container.rs`, `src/runner/container/census/tests.rs`†, `src/runner/container/exec/tests.rs`†, `src/runner/container/resolve/tests.rs`†, `src/runner/container/tests.rs`†, `src/runner/container/view.rs`, `src/runner/host.rs`, `src/runner/host/tests.rs`†, `src/util.rs`, `src/workspace_manager.rs`, `src/workspace_manager/fixture.rs`†, `src/workspace_manager/tests.rs`†.
  *Legacy, frozen at PR5 (21):* `examples/probe.rs`†, `src/agent/claude.rs`, `src/agent/codex.rs`, `src/agent/copilot.rs`, `src/answer.rs`, `src/config.rs`, `src/connect.rs`, `src/engine/attempt.rs`, `src/engine/coordinator.rs`, `src/engine/resume.rs`, `src/engine/tests.rs`†, `src/events/log/premove.rs`†, `src/events/mod.rs`, `src/export.rs`, `src/gates.rs`, `src/main.rs`, `src/review.rs`, `src/route.rs`, `src/status.rs`, `src/validate.rs`, `src/workspace.rs`.

  15 of the carve-out's files are read by no production build (†); every carve-out file has an allowlist row; per lint and against every prologue: `clippy::disallowed_macros` 19, `clippy::disallowed_methods` 42, `clippy::disallowed_types` 23; statements of `clippy::all`, `clippy::style` or `warnings` in any prologue: 0 (**F42**).

- **Outside the carve-out, measured by the compiler, not by a reader**: 141 hosts; in the carve-out 28; outside 113, of which 109 may hold code; non-forbid pairs among those: 0 (**F41**). Every production host outside the
  carve-out that may hold code carries a production `forbid` of all three governed lints, its own or inherited. **Executed**:
  423 pairs over 141 hosts: E0453 338, accepted 85 (10 in the 4 declaration-only roots, 75 in carve-out files, 0 elsewhere); agreement with the prologue reading 423/423 (**F44**) (`preflight/population/`): a macro-generated `#[allow]` of each lint, the shape no source reader can see,
  appended to every production host and checked by Clippy over the production targets, is refused with `E0453` everywhere but
  the carve-out's own files and the four declaration-only roots, and agrees pair by pair with the file-level reading. The roots
  accept the attribute at the lint gate, and the declaration guard refuses any code in them (§3).
- **The boundary routes the reviews executed, as far as this run re-ran them at this range (§3 says which)**: 167 executed specs and 2 `--nocapture` re-runs (of `r5-main/08-batch-versus-individual`, `r5-main/09-batch-diagnostic-controls`), covering 242 of the 502 catalogued records; verdicts: 152 as expected; 4 legitimate for its own named checks, which pass; another in-range guard refuses the shape; 13 an instrument experiment whose outcome matches its original record; restorations proven 169/169 (**F45**) (§3).
- **The residual bypass inside the carve-out is carried by filed findings**, as v17 requires: `PR7-WRAPPERS-EMPTY-DOMAIN` (P1,
  `deferred`, under its project-owner guard; not closed by the amendment or by #318) and
  `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT` (P2, `deferred`), whose pin holds 29 in 20 files, all inside the carve-out: 20 deny from the command line's `-D warnings`; 6 forbid from src/engine/mod.rs; 3 deny from src/agent/mod.rs (**F43**); and
  `PR7-CLASSIFICATION-DOMAIN-READS-FNS-ONLY` (P2, `deferred`, reasoned) inside the carve-out as well. **Completeness over the
  carve-out is not claimed.** And a boundary no gate here claims: Clippy is the only enforcer of these lint levels — plain
  `rustc` compiles an `allow` under a `forbid` of a Clippy lint, so a `cargo build` or `cargo test` of a bypass writes, and that
  is not a bypass of the lint gate (§3 records it).
- **#318's own deferred residual, `R5-MAIN-01`, re-executed at this range** (`r5-main/10-undecodable-literals`: at d724fb16, Clippy exit 0, effects exit 101; 63 undecodable literals measured, the checker credits 21, rustc refuses 63 of 63 with `E0539`; `r5-main/13-invalid-any-guard-witness`: Clippy exit 0, effects exit 101, the named witness fails at `src/effects/tests.rs:7540:5` ("a literal rustc refuses excused the deny", 6 excusals), 9 fixtures, rustc refuses 9; restorations proven: True (**F63**)): #318's checker credits an
  undecodable literal inside a true `any()` as an allowance, and rustc refuses every such source with `E0539`. It is a credit
  imprecision of the effects checker on sources the compiler rejects, not a route past the lint boundary: this answer's claim
  outside the carve-out rests on the compiler's own refusals (**F44**), not on that checker's reading. It is a
  deferred row of #318's ledger, whose finding file is filed after the range, so the ledger at the range does not hold it.

**So Q5's wrapper limb, the one runs 3 to 5 could not answer, is answered here in the affirmative v17 permits**: over the
modules the classification classifies and outside the file-level carve-out this record enumerates, no production host that
may hold code accepts a governed-lint allowance at the lint gate (**F44**), the four declaration-only roots hold no
code and a guard refuses any, every production file outside the roll-call states each governed lint or inherits a `forbid`,
and every route and control this run re-ran from the five review rounds of #318 is refused or behaves as recorded at this
range by a required check (**F45**; §3 says which records were selected and which were not). Inside the carve-out the residual bypass is carried by filed findings and completeness is not
claimed; and #318's checker carries its own deferred residual, `R5-MAIN-01`, re-executed here, on sources the compiler refuses
(**F63**). That answer is narrower than v16 asked for, and this record says so. The hook-phase and per-entry halves of Q5 are
clause 2's (§8), the residue half clause 4's (§10).

**Q6, sequential guarantees.** *Answered with evidence for G5's portion.* The derived run outcome is computed only from
durable inputs: census assertion 5 establishes the equality in both directions over the union (§7), with the named instances
measured (**F31**), and the ledgers check it per outcome (§12). The precedence halt > budget > parked > complete, with
pending backoff never blocking Halted or BudgetExceeded, and `budget_exceeded` recorded before any budget-driven end, are
group 4's and the census's (§4, §7). That no answer is ingested after a halt or a budget stop in the same epoch is asserted by group 6's named cases (§4). One
Runner identity per run: the fold refuses a `run_resumed` whose Runner differs in any field (census assertion 7, over every
explored state), and the resume rebuilds the recorded Runner before its pre-flight (ST-20, group 9). Finalization converges
to the same derived outcome after a kill at every cleanup-effect boundary, for Complete and Halted (**F60**, §12). The
operator shapes of §12 bound "converges", and none was re-executed here.

## 15. The five artifacts, the ten gates, and the evidence directory

### 15.1 The five required artifacts

Each is a file in the evidence directory, listed with its sha256 in `MANIFEST.sha256` once the manifest is finalised (§15.3).

| # | required artifact | the file(s) | what makes it the artifact |
|---|---|---|---|
| 1 | the gate report | `reviews/2026-09-25-gate-G5.md` (this file); a byte copy named by the head it was committed at, `report/report-<head>.md`, is saved after the commit | assembled by `report/assemble.py` from `report/sections/` and `figures/figures.json`, refusing an unresolved placeholder or an uncited figure; checked by `report/selfcheck.py` |
| 2 | the bounded census artifact | `census-export2/bounded-census-artifact.md` | the members, every bound over the union, the family's contract, the ten coverage assertions with the scope each runs over and the named test that asserts it, and the limits, every `file:line` resolved at the range (§7) |
| 3 | the effect-site inventory and fault-injection registry with executed evidence per site and order, and the residue-class evidence record | `inventory/site-enums-export.md` with the range's copies of the lists beside it; `audit/per-coordinate-audit.md` and `.tsv`; `residue/residue-class-evidence-record.md` | the inventory tables every site and entry with its order, residue, tabled action, evidence kind and the tests recording it on each host, the Event kill and error-return points, OpenLog's SyncPrefix with the barrier's refusal cases, the IdUnread points and the fast no-execution record; the audit carries ST-07's three obligations per entry with resolved cites and the probe's check; the residue record carries synthetic construction per element and the sampling histograms (§8, §10). The two-crash proof record is §9's bytes in `probes/projection/probe-run.log` |
| 4 | projection equivalence outputs | `probes/projection/probe-run.log` | the durable bytes, the live digest and the replay digest at every prefix the committed test compares, and replay twice at every prefix (§11) |
| 5 | per-resource ledger outputs for Complete, Parked, Halted, BudgetExceeded and no-`run_finished` runs | `ledger-export1/`, `ledger-export2/`, `guest/exports/export/ledger/` | the six outcome tables of each export, every row's class, observed state and verdict (§12) |

### 15.2 The ten gates at this report's own head

The ten gates are the nine `w1-eight-iso` runs (formatting, Clippy over all targets with `-D warnings`, the full suite, the
MSRV check with `--locked`, and the five gate scripts it runs) plus `bash .github/scripts/test-pr-ready-audit.sh`, which that
wrapper does not run, all from the worktree root through `upstroke-build` on this run's private target. They run **after**
this report is committed, at the commit that carries it, because a file cannot record the verification of the commit that
contains it; so this report states no gate result. The logs are `gates/` in the evidence directory, named by that head, and
the pull request's body records the passing head. The report's commit changes no product byte, so the gates' suite is the
range's suite with this one Markdown file added. A red there is a result: it is recorded in `gates/` and the pull request's
body and put to orchestration, not re-run on this run's own authority, because the flake rule's one re-run was S1's (§2.1).

### 15.3 The evidence directory

`~/tactus-artifacts/g5-evidence-d724fb1/`, allocated for this range and written by no other run; `README.md` describes its
layout. `MANIFEST.sha256` covers every file in it but the manifest itself and `manifest-check.log`; it is generated and
verified by `make-manifest.sh` **after** the gate attempt at this report's own head and the report copy exist, because those
files can only be written once the commit exists, so this report quotes no figure about the manifest. The reader's check is
`sha256sum -c --quiet MANIFEST.sha256` from the directory, expected to exit 0. Since the repair round the directory also
holds two things the record's derivations need that were outside it before (RECORD P3-4): the working scripts whose output the receipts
carry — run 6's `run.py`, `classify-red.py`, `resources.sh` and `guest-ps.sh` under `scripts/run6/`, this round's runner and
drivers under `scripts/repair-r1/`, with `scripts/SHA256SUMS` — and the clause-2 probe's raw per-test traces,
`audit/probe/probe-out.tar.xz`, with every file's sha256 in `audit/probe/probe-out.sha256`, from which
`audit/probe/rederive-from-archive.sh` re-derives the probe's outputs: `audit/probe/probe-out.tar.xz` (sha256 `1f01a545d664e0b8…`): 7011 files, each as `audit/probe/probe-out.sha256` lists it; from the extracted traces: analyze.py exit 0, reach-check.py exit 0, plant-domain.py exit 0; outputs byte-identical 4, byte-identical after the input-path substitution 2, differing 0 (**F64**). The repair round's own evidence is
`st18-cuts/` (the kill-cut probe: its appended text, its receipts per phase, its preliminary versions and its failed setup,
`summarize.py`), `st07/adapter-exclusion/` (the raw and filtered ST-07 checks on both hosts) and the receipts its figures
name. The scratch trees under `~/g5r6-work/` are named in the header. Run 1's to run 5's directories are separate and are cited
nowhere as this run's evidence.

## 16. The verdict against the pass rule, clause by clause

The rule, verbatim from the regenerated definition (`spec/gate4-v17.json`, `pass_fail_rule`):

> pass iff bounded census family complete (the prefix at its stated ceiling with truncation reported, every seeded
> per-dimension census closed, every bound reached by equality over the union) with totality asserted over the union and
> every plan_transition arm covered, site bijection complete with executed evidence for every Topology/Shared site, order,
> and parent-executed point (incl. OpenLog SyncPrefix and the stable-prefix barrier refusal cases), the two-crash proof
> green, residue-class evidence complete and labeled, all rows green, projections equal, per-resource ledgers satisfy their
> outcome equations incl. derived outcome, closure, and finalization, questions answered, no open critical/high finding;
> unbounded closure, effect-phase census, and parent-observed Git-internal execution claims are not made

**Nine** positive clauses and **three** prohibitions, counted from the rule.

| # | clause of the rule | grade | the evidence (section, figure) | carried beside the grade |
|---|---|---|---|---|
| 1 | bounded census family complete, totality over the union, every `plan_transition` arm covered | **established** | §7; **F30**, **F31**, `census-export2/bounded-census-artifact.md` | the numeric bounds are reached over the union through the prefix; no claim that each shape reaches each bound |
| 2 | site bijection complete with executed evidence for every Topology/Shared site, order and parent-executed point, incl. OpenLog SyncPrefix and the barrier's refusal cases | **established** | §8; **F18**, **F19**, **F54**, **F59**, **F27** | the row-level limits §8.3 prints (the finalization-only rows' live-path recovery is not executed; the report-before audit witnesses run over a planted leftover, and the repair round also kills those cells in a first finalization; rows 5–8 and 59–60 recover through the helper `TopologyRun::continue_open` calls); on Windows, rows 144–148's kills, error and replay-twice rest on the witnesses' own assertions (§8.4); ST-07's counters are the checker's raw ones, the production-funnel population one smaller on each host and every check green without the adapter record (**F59**); SyncPrefix evidenced as the funnel's sync, not the platter; group 1's literal "exhaustive kill" is asserted, since a real kill stands at fewer than half the coordinates, and the clause asks for executed evidence, not a kill (§4) |
| 3 | the two-crash proof green | **established** for the simulated loss | §9; **F33**, **F34**, detection by mutation (§5) | `PR5-C-FSYNC-UNOBSERVABLE`: a physical sync is invisible to any test |
| 4 | residue-class evidence complete and labeled | **established** | §10; **F51**, **F50**, **F55** | `PR10-RECORDED-STAGING-LEFTOVER-UNCLASSIFIED` named, not counted; the guest's census-sampler rows are not evidence there (`PR249-KILL-SAMPLER-WINDOWS-WRAPPER`) |
| 5 | all rows green | **established**, reading "rows" as the packet's matrix and ledger rows — the registry entries audited per coordinate, the finalization cells killed, and the per-resource ledgers' rows on both hosts — with the named tests that witness them, as the two merged G5 reports graded it on the ledger rows and the suites (`reviews/2026-09-16-gate-G5.md`, `reviews/2026-09-16-gate-G5-run3.md`) | §2, §4, §8, §12; **F27**, **F60**, **F35**, **F58**, **F49**; the suites that carry them: **F13**, **F14**, **F15**, **F52**, **F53**, **F20** | S1 was red on the two permitted fingerprints and is counted as a red (**F16**); its one re-run was green; the push CI run's Windows leg is red on `PR262-UNREACHABLE-PATH-ENTRY-ABORTS-PROGRAM-RESOLUTION` (**F21**), a test in none of those rows — disclosed, and the reading is stated at root's direction (§2.3); read as "every suite run at the range green", that red would count against this clause |
| 6 | projections equal | **established** | §11; **F32**, detection by mutation (§5) | the per-append equality is measured in one resumed run; the classification equality over every census trace is clause 1's |
| 7 | per-resource ledgers satisfy their outcome equations incl. derived outcome, closure and finalization | **established** for the six outcome tables as measured, and for finalization under a kill at every cleanup-effect boundary of each constructed finalization, Complete and Halted (**F60**) | §12, §4; **F35**, **F58**, **F60**, **F61**, **F62**, groups 4 and 5 | convergence without an operator in the filed torn-registration shapes is **not** established (§12, §13); their refusals are resumable and lose nothing, so no equation is falsified, and they are also why group 3 is asserted |
| 8 | questions answered | **established** | §14; Q5 affirmatively under v17's wording (**F38**, **F44**, **F45**, **F63**) | v17's answer is narrower than v16's: the carve-out is enumerated and excluded, its residual bypass carried by filed findings, completeness over it not claimed; #318's checker keeps its deferred residual `R5-MAIN-01`, re-executed here: a credit on sources rustc refuses, not a route past the lint boundary (**F63**) |
| 9 | no open critical/high finding | **established** | §13; **F36** | eight P1s are `deferred`, not closed; they bound the grades they are stated beside |

**The three prohibitions.** No unbounded closure is claimed: closure is claimed of the three seeded members only, each under
its own ceiling (§7). No effect-phase census is claimed: the census explores fold states, and the effect phases are clause 2's
executed evidence (§7, §8). No parent-observed Git-internal execution is claimed: residue inside a Git command is
recovery-proven, and the samplers' histograms record what the parent found after its kill, not what the child executed (§10).

### The verdict

**G5 passes on the range `d724fb162f76d86b88077cf51011013f1cfbefb8`.** All nine positive clauses of the rule are established on
this run's own evidence at the range — the census family, the bijection with executed evidence per coordinate on both hosts,
the two-crash proof for the simulated loss, the residue-class record, every row green, projections equal, the per-resource
ledgers with finalization's convergence after a kill at every cleanup-effect boundary, the six questions (Q5 affirmatively
under v17's wording) and no open critical or high finding — and none of the three prohibited claims is made. The repair round
executed what the first review found unexecuted, ST-18's kills between the execution-root step's cleanup effects with the
staged-intent leftovers planted (**F60**, **F61**), and every cut converged; no grade needed lowering.

**What the pass does not say.** It is a pass at `max_parallel = 1` for G5's portions (§1.2). It rests on grades whose limits are
printed beside them, not on their absence: the physical sync is outside any test's sight; one residue has no declared class;
convergence without an operator in the filed torn-registration shapes is not established; the carve-out v17 excludes is
enumerated, its residual bypass carried by `PR7-WRAPPERS-EMPTY-DOMAIN` and `G5RUN4-RESIDUAL-BYPASS-OUTSIDE-THE-Q5-CARVE-OUT`,
and completeness over it is not claimed; #318's checker credits undecodable literals rustc refuses (`R5-MAIN-01`, re-executed
here and deferred with #318); ST-07's funnel counters include one adapter record a stale exclusion lets in, and the population
without it is one smaller with every check still green; the finalization-only rows' recovery in the live transactions is not
executed; on Windows, rows 144–148 rest on the witnesses' own assertions; eight P1 findings stay `deferred` and weaken the
pass without being treated as closed. S1's red on the two permitted fingerprints stays counted as a red, and the push CI run
at the range is red on one Windows test outside G5's rows (§2).

**What changed since run 4** is the clause-8 remedy #318 and its re-established boundary: no route or control this run re-ran from its five
review rounds survives at this range (§3), the compiler refuses a governed-lint allowance in every production host outside the
carve-out and the declaration-only roots (§14), and the rest of the evidence was re-derived here rather than carried (§1.3).
This is the report after its one repair round (§17); its final independent review, triage and merge are orchestration's,
and PR11 and PR12 remain blocked until they are done.

## 17. The repair round: each review finding, and where it is answered

The report's first head, `e0f9a1cd`, was reviewed in three lenses (REGRESSION on gpt-6-astra, VERIFY and RECORD on Claude
Opus 5.5, both at max; the canonical verdict is on the pull request). Under the owner's round cap this is the report's one
repair round. Every finding is answered below by a change and a receipt; none is left open. The one that asks for a product
change — the stale exclusion behind R6-REG-02 — is a `deferred` row of the pull request's ledger, because this lane changes
no product byte.

| finding | lens, severity | what it found | what this round did | where, and the receipt |
|---|---|---|---|---|
| R6-REG-01 | REGRESSION, P1, executed | no process-death cut between the execution-root step's cleanup effects with the staged-intent leftovers planted, for Complete or Halted, although clause 7 and the verdict were graded established; with that cleanup removed, both kill matrices stayed green | executed them at the range, without a product change: append-only tests in a private clone kill a child at every cell of each constructed finalization (**F60**) — the leftover kinds at every occurrence position the step takes, plus a first finalization and one with two ordinary slots of each kind — and resume it in a fresh process twice; every cut converges. The reviewer's own mutation fails every leftover cut. Group 5 is graded established on this evidence, and clause 7's finalization limb rests on it | §4 group 5, §12, §16 rows 5 and 7; **F60**, **F61**, **F62**; `st18-cuts/` |
| R6-REG-02 | REGRESSION, P2, executed | ST-07's counters count the adapter test #318 moved (the exclusion still names `runner::tests::`) | disclosed; the raw counters and the production-funnel population kept apart in F18, F19 and F54; measured with the unchanged checker over raw and filtered copies, natively on both hosts, every check green without the record | §8.2, §8.5, §13; **F18**, **F19**, **F54**, **F59**; `st07/adapter-exclusion/` |
| R6-REG-03, RECORD P2-1, VERIFY V-4 | REGRESSION P3; RECORD P2; VERIFY P3 | §6 said `R5-MAIN-01` was not re-executed; the preflight had re-executed it at the range; Q5, clause 8 and the verdict's limits did not carry it | corrected, and carried everywhere RECORD named | §3, §6, §13, §14 Q5, §16 row 8, the verdict; **F63** |
| RECORD P3-1 | P3 | constant text inside figure values; counts typed in the prose | `figures/figures.py` derives F2, F3, F8, F9, F10, F21, F43, F46 and F47 from their logs (F46's all-or-nothing test fixed); the counts RECORD listed are figures (**F65**, **F66**, **F67**, **F71**, **F72**, and F52/F53 carry the guest's child result lines); the header's claim is narrowed to what holds, and §0's closing note names the counts left in prose | the header, §0, §2.2, §3, §8 |
| RECORD P3-2 | P3 | the report omitted the probe overlap and the `/proc` marker collision; `suite/s1-context.md` gave one wrong time | §2.1 gives the overlap and its bound; the note's time is corrected in place, the correction stated; the defect is recorded in §13 | §2.1, §13; `suite/s1-context.md` |
| RECORD P3-3, VERIFY V-5 | P3 | run.py's `child SigIgn` line is the `bash -c` wrapper's mask, not the command's | the header says what the line measures; a reproduction records both masks; this round's runner records the command's own after the `exec` | the header; **F70**; `scripts/sigign-check.log` |
| RECORD P3-4 | P3 | the probe's raw traces and the working scripts were outside the manifest | the traces are archived in the evidence with every file's sha256 and re-derived from the archive; the scripts are copied in | §15.3; **F64**; `audit/probe/rederive-from-archive.log`, `scripts/SHA256SUMS` |
| RECORD P3-5 | P3 | clause 5 did not state its reading of "rows" or cite the ledger rows | the reading is stated and the ledger rows cited, with the audit rows and the kill-cut cells | §16 row 5 |
| VERIFY V-1 | P3 | §5 said every recipe ran `--exact`; the `g5` and `pr10` sets did not | the sentence says what ran, and a check over every log proves one test ran in each | §5; **F68**; `mutations/exact-and-selection-check.json` |
| VERIFY V-2 | P3 | §8.4 said only row 145's replay rests on a test's own assertion | on Windows, the kills, the error and the replay-twice of all five rows rest on the witnesses' own assertions; stated | §8.4, §8.5, §16 row 2 |
| VERIFY V-3 | P3 | the justification for the finalization-only rows said their recovery does not depend on the transaction | the two recovery paths are named, the live one stated unexecuted, and the credit put on the packet's keying; the adjudicated text is corrected beside the audit | §8.3; `audit/review/v3-correction.md` |
| VERIFY V-6 | P3 | "the whole applied diff" omits a file a route creates | the sentence says what `applied.diff` holds and where a created file's bytes are | §3; **F69**; `preflight/applied-diff-coverage.json` |
| VERIFY O-1 | root's direction | whether the push CI red counts against "all rows green" | the reading is stated and the red disclosed beside the merge-queue run's green | §2.3, §16 row 5 |
| VERIFY O-2 | observation | the two-crash byte figures embed this run's paths | stated | §9 |
| RECORD's minor notes | none required | the guest driver's pre-run diff holds two edits; three instrument experiments are R4 parity sweeps failing as at #318's head | both stated | §2.2, §3 |

VERIFY's O-3 and O-4 are observations, not defects: group 5 is no longer among the groups graded "asserted" in their literal
form (§4), and the remaining-sites sampler's indifference to a leader-only kill stands as §5 states it, not re-derived.
