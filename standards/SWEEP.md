# Standards sweep

§6 (shared ownership, locks, clones) and §7 (the `?` operator) were tightened on 2026-09-03, and
§7's panic surface (indexing, slicing, `unreachable!`) on 2026-09-04. They bind new and materially
changed code immediately. The existing tree predates them and is being brought up to them one file
at a time: each file gets a deep review by a frontier model, then a pull request that lands the
cleanup and adds the file to the table below.

**Activation rule.** In a file not yet listed here, §6 and §7 apply to the code a change adds or
rewrites: every line inside a hunk the change introduces or modifies, and the whole body of any
function the change modifies. A pure formatting, renaming or comment change activates nothing.
An existing `Rc`, `Arc`, `Mutex`, `RwLock`, `clone()`, `?`, `v[i]`, `&v[a..b]` or `unreachable!`
outside that scope is not a review finding. Once a file is listed, §6 and §7 apply to it in full,
and a reviewer may cite them against any line.

**The activation rule is temporary.** It exists only because the tree predates the rules. When
every Rust file the standards govern (§1: all Rust in the repository, which today is `src/` and
`examples/probe.rs`) is listed in the swept table, the transitional wording goes in one pull
request, every sentence of it: this file's opening paragraph ("bind new and materially changed
code immediately ... one file at a time"), this paragraph and the activation rule above; §6's
paragraph "These three rules bind the code a change adds or rewrites, now" and its "Enforced by"
line's reference to this file; §7's sentence "This rule is transitional in the same way as
§6's" and, in its panic-surface paragraph, the sentence beginning "This rule is transitional in
the same way as §6's and the `?` rule above"; §1's sentence "Some standards are newer than the
tree; `standards/SWEEP.md` says which"; §16's sentence "A §6 or §7 finding in an unswept file is
in scope only under the activation rule"; `MAINTAINING.md`'s triage clause "or against an unswept
file under a transitional standard"; the `CODING_STANDARDS.md` index paragraph that points here;
and the hard-conventions bullet in `CLAUDE.md` and `AGENTS.md` that says these rules "bind the
code a change adds or rewrites" and points at the activation rule. That same pull request adds
`clippy::indexing_slicing` and `clippy::unreachable` to `[lints]`, taking
`allow-indexing-slicing-in-tests` and no allowance for `unreachable!`, which has none to take (see
§7). That is what makes the prose removable: the panic surface §7 governs stops being a review duty
**for these two constructs** at the commit the build starts catching them. It does not end the
review duty for the panic surface as a whole — `assert!`, `split_at`, `expect`-shaped helpers and
arithmetic overflow all still terminate, and a local macro that expands to `unreachable!` resolves
elsewhere and is not caught — so §7's paragraph on what may panic survives these lints. After that §6 and §7 bind the whole tree with no
scoping, and this file is the record of how the tree got there. Errors are handled where they
arise; a `?` that survives a sweep is one the reviewer agreed was deliberate; an index or an
`unreachable!` that survives one is under an `#[expect]` that says why.

## Review queue

The queue is the W2 decomposition wave: the seven large modules split into per-concern child
modules by the pull requests that merged on 2026-09-03 (#110, #107, #106, #108, #111, #117,
#123), one pull request per parent. For each family, every Rust file under its
directory is queued (the children the split produced, and the test, fixture and support files
that were extracted there earlier), followed by the parent, which keeps whatever the split did
not move. Families are listed in the order their splits merged, and a family of this wave that
merged after the last edit of this table joins it in the next pull request that touches the
table, whichever that is; a stale queue is a queue with work owed, not a broken one. Each file is
swept in queue order: one session, one file, one review by Claude Fable 5.1 whose only subject
is that file, then the pull request that lands the cleanup and moves the row to the swept table.
Files outside `src/` that the standards govern (today `examples/probe.rs`) close the wave.

The tree has older per-concern families that no pull request of this wave produced, among them
`src/plan/markdown/` (`cd612c3`, 2026-08-30), `src/engine/topology/`, `src/runner/container/`,
`src/effects/`, `src/events/log/`, `src/validate/`, `src/status/` and `src/connect/`, and flat
modules no split has touched. They are not on this queue until named. They join it, family by
family, after the wave above or earlier when the owner names one. The owner named four on
2026-09-04; the status family (#67) and connect family (#68), both split on 2026-08-30,
have rows below. The merge-order rule applies within a wave, not across the history of the tree.
The validate family (#69), split on 2026-08-30, has allocated rows 58–60; graph.rs is swept below.

| # | File | Lines | Family | Merged |
|---|---|---|---|---|
| 9 | `src/workspace_manager/fixture.rs` | 386 | #110 | 2026-09-03 |
| 11 | `src/workspace_manager.rs` | 2,535 | #110 | 2026-09-03 |
| 12 | `src/rundir/classify.rs` | 280 | #107 | 2026-09-03 |
| 13 | `src/rundir/discovery.rs` | 331 | #107 | 2026-09-03 |
| 15 | `src/rundir/ownership.rs` | 344 | #107 | 2026-09-03 |
| 16 | `src/rundir/retention.rs` | 232 | #107 | 2026-09-03 |
| 17 | `src/rundir/scratch_tree.rs` | 1,281 | #107 | 2026-09-03 |
| 18 | `src/rundir/tests.rs` | 4,079 | #107 | 2026-09-03 |
| 19 | `src/rundir.rs` | 1,792 | #107 | 2026-09-03 |
| 43 | `src/runner/host/probe.rs` | 133 | #111 | 2026-09-03 |
| 44 | `src/runner/host/tests.rs` | 7,391 | #111 | 2026-09-03 |
| 45 | `src/runner/host.rs` | 777 | #111 | 2026-09-03 |
| 48 | `src/agent/proc/hooks.rs` | 113 | #117 | 2026-09-03 |
| 49 | `src/agent/proc/test_support/readiness.rs` | 583 | #117 | 2026-09-03 |
| 50 | `src/agent/proc/tests.rs` | 3,893 | #117 | 2026-09-03 |
| 51 | `src/agent/proc.rs` | 5,239 | #117 | 2026-09-03 |
| 54 | `src/config.rs` | 2,875 | #123 | 2026-09-03 |
| 59 | `src/validate/render.rs` | 257 | #69 | 2026-08-30 |
| 60 | `src/validate.rs` | 838 | #69 | 2026-08-30 |
| 57 | `src/status.rs` | 735 | #67 | 2026-08-30 |
| 62 | `src/connect.rs` | 785 | #68 | 2026-08-30 |
| 65 | `src/plan/markdown/drafts.rs` | 263 | `cd612c3` | 2026-08-30 |
| 66 | `src/plan/markdown/hints.rs` | 51 | `cd612c3` | 2026-08-30 |
| 67 | `src/plan/markdown/sections.rs` | 108 | `cd612c3` | 2026-08-30 |
| 68 | `src/plan/markdown.rs` | 456 | `cd612c3` | 2026-08-30 |

**Row 12 has had two passes and is not swept.** PR #137 landed correctness repairs in
`src/rundir/classify.rs` — a first-line re-read that carried the scan's proof onto bytes the scan
never saw, an unreachable `?`, an unclamped short read, and a module comment that stated a safety
property the code does not have — with three tests and their mutations. It also **found a P1 it
could not repair**: the probe does not terminate on a source answering `Interrupted`, the census
holds the physical worktree lock across it, and both repairs attempted were wrong — the second
because an exhausted retry bound has to answer `Husk`, which is the reclaiming classification, so
it traded a hang for a deletion.

**`SWEEP-CLASSIFY-001` is closed** by the pull request that made
`classify_run_dir` three-valued: `RunDirClass::Indeterminate` is what a bound answers when it runs
out, every reader treats it as retaining, and the census retains the directory with
`RetainReason::ClassificationIncomplete` rather than offering it to the reclaim path. Both unbounded
doors are gone, and there is one `Interrupted` retry site in the file rather than two. That change
did **not** discharge §7 for the file and this row stays open.
**Seven** inspections still fold an I/O failure the filesystem declined to explain into the same
`Husk` as an honest absence: the `symlink_metadata` guard, the `open`, the `fstat` that takes the
bound, the window read's `Err`, the scan read's own `Err(_)` arm, the seek and the re-read's `Err`.
Seven is derived rather than counted -- PR #137's body carries the command that enumerates the
sites, after two frontier passes corrected the number twice. **The `SWEEP-CLASSIFY-001` repair
folds every one of those seven exactly as master did**; what it moved out of the set is a read that
did not *finish*, which was never one of them.

**One sub-case did move, and it is named here rather than left inside that sentence.** An
allocation the host refuses while the first line is materialised reached master through
`read_to_end`'s `Err`, inside the window read and the re-read, and folded to `Husk` with
everything else. The repair's own loop grows that buffer through `Vec::try_reserve` in
`classify::append`, and a refused reservation answers `RunDirClass::Indeterminate`. It is a site
the repair created rather than one of the seven -- the seven are failures the filesystem named,
and every one of them still folds -- and the alternative measured was not `Husk` but a process
abort, which is what a bare `extend_from_slice` did for one round of that pull request. What it did change is the number of *arms* those
seven reach, from seven to six: the window read and the re-read share one `Step::Failed` arm in
`read_up_to` now, where they were two `read_to_end(..).ok()?` calls before. Six arms, seven
inspections, by

```
grep -n 'Step::Failed\|is_ok_and\|let Ok(.*) = \(File::open\|file.metadata\)\|seek(SeekFrom::Start(0)).is_err()' src/rundir/classify.rs
```

which prints seven lines, of which `Err(_) => return Step::Failed` is where the failure is
classified rather than where it is folded, and `read_up_to`'s arm is reached from two call sites.
Making those errors is what `RunDirClass::Indeterminate` is now available for, and it no longer
needs the signature change: `classify_run_dir` already answers three ways, and `list_runs`,
`list_husks` and `RunDirEntry`'s class field already handle all three (queue row 13).
Listing the file here would activate §6 and §7 over it in full, and recording a violation does not
satisfy a standard, so the row stays open and a successor takes the folds. The findings are
`reviews/FINDINGS.md` §56, and the ones still open are files in `findings/`.


**Row 29 (`src/topology/fold/apply.rs`) is swept, in the table below.** It was left open when
PR #152 merged, because seven independent frontier passes over that pull request had each
returned `CHANGES_REQUIRED` — the last of them on first-attempt continuation selection, repaired
there by a separate crate-private fold eligibility reader — and no pass had judged the result.
PR #152's ledger is the record of every one of those findings, of the repairs that were withdrawn
(the `parked_from` snapshot shapes among them) and of the claims that were rejected. What a sweep
of the merged file found is its row's Notes cell.

Line counts are as of the family's split merge and are a guide to session sizing, not a
contract. "Family" is the pull request whose split defines the family the file belongs to, and
"Merged" is when that split landed; neither says which pull request first created the file at
its path (several of the test and support files were extracted earlier: by #98, #100 and #102,
and `readiness.rs` by `1cd4b1e` on 2026-08-30), and `git log --follow` is the record for that.
`examples/probe.rs` belongs to no family.

Baseline at the tightening (master `cfec136`, 114 Rust files under `src/`):

| Construct | Sites | Files |
|---|---|---|
| `Arc<` | 81 | 20 |
| `Mutex<` | 145 | 32 |
| `Rc<` | 4 | 2 |
| `.clone()` | 1,941 | 84 |
| `?` (propagation) | ≈1,200 | 71 |

Baseline at the panic-surface tightening, measured at master `93d6337` over 166 Rust files under
`src/` and `examples/` by running the two lints themselves —
`cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing`,
counting each warning's primary span once:

| Construct | Sites | Files | In a `tests.rs` | Elsewhere |
|---|---|---|---|---|
| `unreachable!` | 65 | 20 | 43 | 22 |
| indexing and slicing | 1,034 | 72 | 442 | 592 |

**This is a measurement at a named head, not a property of the tree, and the sweep moves it.**
Merging master in twice while this section was being written took `unreachable!` from a claimed 77
to a claimed 79 and indexing from 1,030 to 1,034 — the second of those from one merge, `93d6337`,
which added four indexing sites in a suite. The command above is recorded so the number can be
taken again rather than argued about; a stale figure here is a re-run, not a finding.

**Neither construct can be counted by text, and the earlier figures in this section were a text
census that was wrong.** `grep` finds 79 mentions of `unreachable!` across 23 files at this head;
14 of them are comments, doc comments and string literals discussing the construct, several of them
in the very tests whose helper ends in one. The count above is what the compiler resolves, and it
supersedes both the 77 recorded at `44dc06f` and the 79 that replaced it. The suite
`src/workspace_manager/tests.rs` holds **one** invocation, at line 7390, and it was there at
`44dc06f`: PR #136 added two mentions and no invocations.

Two limits of these numbers, both stated rather than hidden. They are a **Linux** run, so code
behind a platform gate is invisible to them: `src/runner/host/tests.rs:5161` is a real
`unreachable!` inside `#[cfg(windows)] fn windows_ambient_coordinator_helper`, which makes the
across-platform count 66 across 21 files, and the msvc leg is what would report the Windows half of
the indexing count. And the `tests.rs` split is by file name, so an inline `#[cfg(test)] mod tests`
inside a production file counts under "elsewhere": 592 and 22 are upper bounds on the production
figure, not the figure.

Of the nine files in the swept table, eight contain no `unreachable!` at all and the ninth,
`src/workspace_manager/tests.rs`, contains the single site above — which it inherited rather than
introduced, and which its sweep already reasoned about.

## Swept files

| File | Swept at (commit) | Date | Notes |
|---|---|---|---|
| `src/topology/effects/bijection.rs` | `4a4a9bd3` | 2026-09-05 | The ST-07 bijection check, first file of the `#106 effects` family; five legacy frontier passes (PR #146). §6: no shared ownership, lock or clone written by this file; the two copies are a resume action's text moving into the failure that compares it with its before-phase counterpart, stated at the site. §7: no `?`; the `.expect()` recovering a hook phase from `EntryPhase::hook_phase()`'s `Option` and its `#[expect(clippy::expect_used)]` are gone, the required-coordinate list carrying both spellings so the `Option` is never produced; `entries[..index]` is `entries.iter().take(index)`; `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports zero sites of either construct in this file. **Fixed:** a begun fast sequence with no hook observation was accepted as exercised, now reported once by name as `EmptyFastSequence` independently of inventory and entry coverage; a trace observation does not establish that a sequence ended or an integration completed. The sampling accounting summed with `saturating_add`, so `n = u32::MAX` with a histogram one sample over passed (pass 1); a site whose hook the harness recorded inside an exercised fast sequence the no-execution record does not name was reported as a gap with the observation withheld -- `UnwitnessedFastSequence` now carries `observed`, an observation and not an inference, and `ExecutedInFastSequence` is the contradiction between a record that names a sequence and a harness that recorded the site's hook in it, as at `943ae61`; recovery-proven evidence on a hook phase answers `MissingEvidence` and leaves records of a class the entry is not about unread; a failing synthetic record names its element and predicate for every failing element, witnessed by a test that breaks four of seven elements and asserts the exact `(variant, element)` set; the expected classification is read from `ResidueClass::classified_as` rather than written in. **§5:** `BijectionFailure` carries `EffectSiteId`, `HookPhase` and `EntryPhase` where it carried `String`, the typed `RegistryError` where it carried that error's text, and `SamplingUnaccounted::counted` is `u64`; the `String` fields left are two categories, a resume action and a fast sequence's name, both a document's own words. `check_bijection` is `#[must_use]`; one order-enumeration spelling serves hook and residue phases. **§13:** the contract this file enforces had no design sentence -- `grep -rniE 'bijection|effect site|every effect' design/` returned nothing at `323beb0` -- and is now the verification-contract subsection of `DESIGN.md` §26, recording what the code does, grounded in §26's own fast-path item; the module comments cite it in place of the packet. Fifteen legacy mutations run on the box through the wrapper under the gate lock, thirteen killed by a named test and **two recorded as unwitnessed with the reason** (one residue class and at most one observable order per site leave nothing for a test to distinguish; neither is scheduled to change). The parent re-export was verified rather than trusted. The open-trace diagnostic is pinned by `an_empty_inventory_still_checks_fast_traces_without_claiming_they_ended`; M16 restores the false completion claim and fails that test, with source restoration verified by hash. Deferred: the sampling count `n` is the registry's own number and "frozen" is a property across runs no single-document check can hold (`SWEEP-BIJECTION-005`, beyond reach -- escalate rather than re-defer if a pass labels it). |
| `src/workspace_manager/containment.rs` | `2dd1350` | 2026-09-03 | the `?` sites in `refuse_reparse_points` are kept: each propagates a typed refusal or `Io` error, path attached; the run id is refused at `derive` unless it is the canonical ULID of `DESIGN.md` §15 (`Refusal::RunId`), the anchor itself is re-examined and canonically pinned on every revalidation, the walk reports a regular file on the chain where it stands, and every funnel primitive re-checks the chain down to its own target after the `Before` hook; `refuse_unreal_directory` and `canonical_prefix` decide absence (`NotFound`, `NotADirectory`) from failure instead of `?` or a discarded `Err`; the walk refuses a chain that is not plain components below the anchor (new `Refusal::RootOutsidePrivateRoot`); no `Rc`, `Arc` or lock, the peel's per-step parent clone replaced by `PathBuf::pop`; the `strip_verbatim` and "every deletion" doc claims corrected |
| `src/workspace_manager/hooks.rs` | `f58747a` | 2026-09-03 | `Arc<Mutex<HookHarness>>`, the lock and the two ledger clones kept with their lifecycle, protected invariant and handle semantics stated; the three `?` in `funnel` kept as deliberate and documented at the site; `point` now applies a refusal at the mode that answered (it named `kill` whatever fired); a test module for the protocol itself. After review (PR #119): a poisoned harness refuses wherever a refusal is legal and proceeds unrecorded at a `Kill`-only point (`c265536`). |
| `src/workspace_manager/naming.rs` | `5a0ae59` | 2026-09-04 | First pass `51feba7`, reviewed at `9f83b09` (PR #118, six findings, fixed in `a7b7c98`); second pass at `8d25472` (five findings: three fixed in `bc07f05`, the stale queue is PR #122's, the body claims the PR's); third pass at `cff812d` (four findings, fixed in `ecddff8`); fourth pass at `3482ba1` (five unlabelled findings, classified P2/P2/text/docs-contract/text and all fixed in `5a0ae59`; the persisted intent record's wire contract now has a design sentence in `design/15`). `reviews/FINDINGS.md` §45 has every row. §6: no shared ownership or lock, and no clone: none written, and none in expanded serde code, since the record's `Serialize` and `Deserialize` are written by hand (each earlier pass had found one: `slot.clone()`, `keys().cloned()`, then the derive's `into = "String"`). §7: `from_intent_name` has three `?` sites and `Slot::from_parts`, which it shares with `SlotId`'s parser, five more; each returns the parser's `None` and is dispositioned on `from_intent_name` in terms of what "not an intent name" and "malformed intent name" mean to the directory walk, which has one action for both. Fixed: `from_intent_name` accepted non-canonical integers (`g03`, `g+3`) and so was not `intent_name`'s inverse; it now round-trips, pinned at the parser and, in `tests.rs`, through `reclaim_intents`. `safe_component` returns `Result` (§5); `validate` reuses `kind()`; `Slot::parts` is the one rendering that `relative` (a `PathBuf`), `intent_name` and the record's `SlotId` spell. The intent record derives `Serialize` and `Deserialize`; underneath, a hand-written wire reader accepts a key only by finding it in `IntentRecord::FIELDS`, `IntentKind` is read only by matching `IntentKind::WORDS` (derived from `as_str`), and `slot` is `SlotId`, an identifier mirroring the relative path and never a path, parsed back into a validated `Slot` and required to agree with `kind`; the record's fields are private behind `IntentRecord::new`, so what it writes it reads back. Unit tests in the file for all of it. Deferred to row 9 (the parent): `write_synced` stages an intent as `<stem>.tmp` in the intents directory, and `intents()` refuses that residue after an interrupted write. |
| `src/workspace_manager/object.rs` | `af382fa` | 2026-09-04 | No shared ownership, lock or clone: a refusal copies the ref name and the offered value into the error value it returns. The two refusals return `Refusal` rather than the parent's flattened `UpstrokeError`, so a caller or a test matches the variant, and the two `?` are same-type propagation stated at the site. The hash lengths are named. The null id is now refused on the new side of a create or compare-and-swap too (`Refusal::NullNew`; the parent's two call sites and import moved with it): measured on git 2.43 it deletes the ref through the CAS and creates nothing with exit 0. A test module in the file drives both predicates and the three refusals at both hash lengths and the length and alphabet boundaries; each test was witnessed failing under a mutation. After review (PR #126, the pass on `6a54b65`): the null-new refusal is witnessed through both public primitives against a real repository with the raw measurement executed, `design/26` step 5 states the rule, and the changed `# Errors` contracts and the diagnostic say exactly what was measured (`6e7604e`). After the second pass (on `def9320`, the last): the fixture pins its object format and the witness runs in a repository of each format, the design sentences distinguish the compare-and-swap from the delete, `refuse_new` is crate-visible and applied by `ensure_integration_ref` and both `IntegrationRefs` doubles, and the SemVer assessment of the added variant is in the body (`df494c8`). |
| `src/workspace_manager/parsers.rs` | `196f641` | 2026-09-04 | §6: no shared ownership, lock or clone; the one copy is the bytes of a Git path becoming the owned `PathBuf`, stated at the site. §7: the two `.ok()` are gone (one strict `decode_path` per platform family returns the `Utf8Error`, and every refusal says from which byte a path stops being UTF-8); `status_endpoints`'s `?` on `Option` is kept as a membership test's own verdict, stated at the site; the `--name-status -z` refusals are a `NameStatusError` at their source, naming the field, and `decode_changed_paths` is the one fold to `RepoWide`, spelling every variant. Fixed: `registration_checkout` trimmed leading whitespace and a trailing form feed that Git keeps as part of the path (measured, Git 2.43.0), so it bound a registration to a checkout Git does not read from it; `decode_git_path` was lossy on Windows while the path is identity in the parent, so `parse_worktree_records` returns `Result` and refuses, and its two callers propagate; both NUL-delimited grammars require their terminator and refuse an empty field, a doubled terminator, an unclosed final record and an attribute before any record, where before a cut-short answer was read as complete; the line-oriented `trim_end` on a NUL-terminated attribute is gone. Unit tests in the file, each hostile shape asserting which refusal it saw; eleven mutations run on the box, each killed by the named case. `reviews/FINDINGS.md` §48 has every row. Deferred to the parent (row 11) and `worktree.rs` (row 8): recording why a region became repo-wide, and the lossy `branch` attribute that `assert_publishable` compares with a refname. After review (PR #127, pass 1 on `3ef06f2`, five findings all P2, fixed in `8cf2d90`): a worktree record ends only at the empty attribute and an empty answer refuses, where a header had closed the open record; `HEAD`, `branch`, `detached` and `bare` are held to their own grammars while reasons stay verbatim; a relative `gitdir` is Git's own form (2.48's `worktree.useRelativePaths`) and resolves against its registration directory instead of refusing; `record_for` propagates a failed enumeration instead of answering absence; a changed path that is not one normalised repository path is repo-wide. Coordinator read of `8cf2d90` (two rows): the relative join is resolved lexically and never hands out `..` (`59fc2c6`), and the refname claim is kept to the byte set `can_be_refname` applies. |
| `src/workspace_manager/snapshot_ref.rs` | `dda65ab` | 2026-09-04 | Two types and no functions at the base, so §6/§7 literally found nothing and the sweep was §5 and §12, with the owner's amendment 7 passes (correctness, better implementation) applied. `ObjectId`: a full hexadecimal object id of either hash length that is not the null id, validated once at `ObjectId::new` through the parent's `is_object_id` and `is_null_object_id`, spelt lowercase so `Eq` and `Hash` are equality of the object; `SnapshotInput`'s three ids and `Snapshot`'s HEAD are this type, so neither can be built from a ref name, a short id or an option-shaped string (the funnel handed every value to `git commit-tree` and `git worktree add` unchecked). `Snapshot`: private fields behind one parent-visible constructor that builds the slot from a `SnapshotName` and holds the HEAD as one `SnapshotHead`, so the ephemeral commit is the HEAD rather than a second field that has to agree with it; accessors `slot`, `path`, `head`, `ephemeral`; `Clone` dropped (a copy is two values for one checkout). Parent moved with it: `Refusal::NotAnObjectId`, `add_snapshot` builds through the constructor and checks the `git commit-tree` line is an id before checking anything out at it, `remove_snapshot` reads the slot through the accessor; the engine's one construction site and two path reads, and the read and construction sites in the two suites. §6: no shared ownership or lock; the clones in `add_snapshot` are two of the name and one of the input's commit, each a small owned value stated at the site. §7: the funnel's three `?` are same-type propagation; the commit-tree line's refusal becomes a Git error naming the command. Seven tests in the file, each witnessed under at least one of ten mutations. Deferred to row 11 (the parent): consolidating `ObjectId` beside its predicates in `object.rs` and giving the ref primitives and the engine's `CommitSha` the same type. After review (PR #130, the pass on `be201d8`, one P2): the newtype is a spelling and cannot know the repository's object format, so a ref spelt as hexadecimal of the other format's length passed it and `git worktree add` followed the ref (measured, git 2.43); `add_snapshot` now resolves each input once against the repository, peeled to its role's object type, and accepts only an answer equal to the input (`Refusal::SnapshotInputResolvesElsewhere`, `SnapshotObject`), witnessed in a repository of each format (`a1c8d2f`). After the second pass (on `891cd7a`, one P1): a replacement object defeated that check, since `rev-parse` prints the replaced id while Git reads the replacement everywhere and the checkout materialised it; every command the manager runs now carries `GIT_NO_REPLACE_OBJECTS=1`, set where those commands are built, and the refusal names the object type each role requires rather than calling a full id malformed (`8a2b0bd`). After the third pass (on `8c5dbc3`): that isolation reached only the children this manager spawns, not a gate or reviewer running in the snapshot (the host runner composes its own environment) nor `read_only_git`, so every claim was narrowed to manager-run commands and the open half was deferred; both directions of the wrong-type mistake are now the typed refusal and the engine's own malformed ids are Git errors (`3e00568`). That open half is closed: the pair is one constant, `NO_REPLACEMENT_OBJECTS`, named by the builder, by `read_only_git`, and by both runners' environment composition, where it is upserted after the overlay so no base and no adapter decides it; `design/15` now says what an exact snapshot is exact against (the objects the repository holds, not configurable), and the witnesses are a role process run through the production `HostRunner` over a snapshot of a replaced tree and a `verify_worktree` over a worktree whose recorded tree carries a replacement. |
| `src/workspace_manager/residue.rs` | `a17b8c5` | 2026-09-04 | §6: no shared ownership or lock, and no clone; `ResidueTarget` is four borrows and is now `Copy`, the reason on the line. §7: every `?` propagates the parent's `UpstrokeError` unchanged and each is kept as deliberate, with the module doc saying why and the three `# Errors` contracts saying what the error names. Fixed in the file: every residue name was read with `Path::exists`, which answers `false` for a permission failure, a symlink loop and a transient I/O error alike, so a git dir the process could not search classified `None` and `administrative_residue_at` called it quiescent; every name now goes through `name_present` (`symlink_metadata`, only `NotFound` is absence, and a link is not followed because a name Git takes with `O_EXCL` is the fact whatever it points at), and `index_lock_present` moved into the file from the parent, its only caller — the one line this sweep changes outside the file. The adds' after phase and their residue element are the two states of one `add_state` reading. Three tests, each witnessed under a stated mutation. **Three frontier passes (`b5c12e0`, `f161c9e`, `dfc238c`) went to a programme this sweep attempted and then reverted at `59f2bd99c95c80f1a2a011a78ebe34b43fbf4555`**: making the parent's inspections report failure instead of folding it into absence. Each round's repair introduced the next defect — an object-store readability scan that missed alternates and could block on a FIFO, then a registration scan with the same unbounded-read class, and a fold-into-absence defect one level deeper each pass. The parent is master's again, and `reviews/FINDINGS.md` §51 carries a row per case for row 11's sweep: the dangling `gitdir` symlink Git omits from its listing, `GIT_TRACE` making absence an error, `direct_ref_target` and `quiescence` folding, the gitfile grammar Git parses and this does not, and bounds on every read of a repository-controlled file. |
| `src/workspace_manager/worktree.rs` | `5e60c5e` | 2026-09-04 | §6: no shared ownership, lock or clone; `Clone` dropped from the record (nothing copies one; the parent moves the path into its refusal through `into_path`), kept on `VerifyFailure` and `Quiescence` with the reason stated on each; the copies in `from_porcelain` are borrowed porcelain bytes becoming the owned record. §7: no `?`; the constructor returns a typed `MalformedAttribute` and the parser joins it after the record number. Fixed: `WorktreeRecord` holds its attribute grammar by construction (private fields; `from_porcelain` the one constructor, applying object.rs's `is_object_id` to `HEAD` and `can_be_refname`, moved here from parsers.rs, to `branch`; the producer moved with it in `219e9e0`); `branch` is the bytes Git printed and `has_checked_out` compares them with the refname exactly, closing `SWEEP-PARSERS-008`; `lock_reason` and `prunable_reason` say that a label printed with no value is `Some("")` (measured, Git 2.43.0) and `is_initializing` is the one spelling of the word, adopted by the parent's `quiescence`; `HeadMismatch` displays as a lowercase fragment. Seven unit tests in the file, nine mutations run on the box and each killed by a named test. `reviews/FINDINGS.md` §52 has every row. Deferred: `locked` stays `pub(super)` until residue.rs (row 6, PR #128) can move its two sites to `is_initializing`, in the base merge-in that follows; the engine renders no `VerifyFailure` (`run.rs` drops it from `RetryOutcome::Close`); a `Display` for `ResidueElement` (row 24); typed object ids and a typed tree difference for `Quiescence` and `VerifyFailure` (object.rs and the parent's `index_differs_from`, row 11). After review (PR #131, pass 1 on `ea4fd74`, five findings, three P2 and two text; four fixed in `5e19848` and one deferred as `SWEEP-WORKTREE-013`): every rule of the record is applied by `OpenRecord` as the parser feeds it each attribute, so a refusal names the first attribute outside the grammar in attribute order and `detached` or `bare` twice refuses like the rest; `is_initializing` is documented as Git's word compared exactly and not as provenance, a writer's `git worktree lock --reason initializing` being indistinguishable from Git's, with the engine-written lock token proposed to row 11 (`SWEEP-WORKTREE-013`). After the second pass (on `c0eb8c5`, four findings, three P2 and one text, all fixed in `1720d91`): `OpenRecord::close` refuses a set of attributes that is not a worktree -- bare, or a HEAD with exactly one of `branch` and `detached`, the rule Git 2.43.0 was measured printing over twelve shapes -- so `branch()` answering `None` is a fact about the worktree and `assert_publishable` no longer reads malformed evidence as absence; `VerifyFailure::Unpopulated` reports the lock it saw rather than a history; and the module quotes the retired decision record as its own words, the trust-boundary question going to the owner. After the third pass (on `9dd3a79`, six findings, no P1, which converged it): `can_be_refname` applies every rule `git check-ref-format` documents for a full refname, so a `branch` the record accepts is one Git would accept; `has_checked_out` is exact bytes and no longer certified as ref identity, since a case-insensitive files backend can hold two spellings of one ref (`SWEEP-WORKTREE-015`, deferred with the parent's check/use race, `SWEEP-WORKTREE-016`); and the retired record's normative wording is out of the module entirely, the missing design mapping being `SWEEP-WORKTREE-014`. |
| `src/workspace_manager/tests.rs` | `31e2e44` | 2026-09-04 | The suite of the Worktree/Snapshot/Ref/Object funnel, 8,529 lines and 126 `#[test]` items at the base `95c5bd3` (122 of them on Linux), swept under the owner's amendment 7 with the tests as the whole job rather than a coda. §6, re-derived by reading after pass 1 found the first version of this sentence false: no `Rc`, and three kinds of sharing each with a reason at its site -- the `Arc<Mutex<HookHarness>>` the `harness()` helper builds, two holders with independent lifetimes and the observer `src/workspace_manager/hooks.rs` states; `SAMPLED_LAUNCHES`, **a lock this file does own**, process-global because the record must be written by the statement that performs the kill; and six `Arc<Atomic...>` cloned into a thread or an observer closure, which arrived with PR #109's merge-in and are not this sweep's lines. §7: no `.unwrap()`, and the three `?` are inside `parse_budget_spec`, each mapping into a `BudgetSpecError` variant that names what was wrong with the operator's spec; what the rule found was the discard clause, and two `let _ =` are gone with the things they were hiding. **Two groups did not pin what they claimed.** The `IdUnread` abort accepted `!helper.status.success()`, which the helper's own `unreachable!` panic also satisfies: measured, with `Injection::Kill` replaced by `Injection::Proceed` the test passed, and the oracle is now `died_by_abort`. The intent-ordering assertion compared two positions in a first-observation log the test itself appended to in that order, so no edit to `src/` could falsify it; it is deleted and the claim named where it is enforced. **One public method had no test in this suite at all:** `candidate_diff`, whose one appearance was the hostile-slot grid, where every call is expected to fail -- a body diffing from current HEAD, and one dropping `--binary`, each passed master's whole crate suite of 1,903 tests. **Three tests are deleted as weaker duplicates** of the swept `src/workspace_manager/parsers.rs`, one of them vacuous on Windows because its `/repository` fixture is not absolute there. The hostile-slot grid gains the legal-name control it never had, both source censuses now blank through `crate::effects` and carry the positive control §12 requires, and `launch_end` reads the shared `died_by_kill` instead of a second copy of the platform fingerprint. 19 rows fixed and 10 deferred, 13 P2 and 16 P3 with no P1 at any point; `reviews/FINDINGS.md` §54 has every row, each fix witnessed against a stated mutation. Deferred, each naming its file: 61 refusals asserted by message substring rather than by value (needs the parent's flattening decided, row 11), `SampledChild` and the kill-helper launch duplicating `src/workspace_manager/fixture.rs` (each also edits `effects/allowlist.toml`, whose funnel row counts this file's process sites), `recover_sample` comparing two discarded results, the acted-through generator reading metadata with `.ok()`, `recovered` true by construction for the two commit-tree sites (row 28 owns the type), and `--no-ext-diff` and `--no-textconv` unwitnessed here. After review (PR #136, pass 1 on `966775e`, five findings, four P2 and one P3, no P1, every one of them a test or census that appears to guard something it does not, and three of the four P2s inside this sweep's own repairs): the `candidate_diff` test staged one more file after the capture, because `reset --soft` leaves the index equal to the tree just captured and a body ignoring `tree` and diffing the index produced identical output; the sampler asserts a classifier refusal before anything derived from the tally, since a refusal became `None` and the generic total assertion fired first with the refusal's words unprinted; the argv census counts **every** string literal in each funnel body and declares what each is, the reviewer's `.into()` reproduction having moved neither `OsString::from` count, and the claim that it stops a funnel growing an argument is withdrawn as more than a text census can hold; and `kill_git_child` records the kill's error beside whether the child had already exited, where discarding it let a shape's kills all fail with its firing count intact and the global floor met by another shape. Each is witnessed by running the reviewer's own reproduction at `966775e` and at the repaired head. The P3 corrected this record: the file does own a lock, `SAMPLED_LAUNCHES`, and the §6 paragraph is re-derived by reading. After pass 2 (on `142c321`, two findings, both P2, no P1, both again inside this sweep's own repairs): the census's function extent is found in the fully blanked text, since a raw string carrying four spaces and a brace forged a boundary and truncated the body while the declared counts still matched; and the abort oracle compares against a **measured** abort — a child whose whole body is `std::process::abort()` — because the shared predicate's Windows arm is a negation that accepts `process::exit(1)`. The second is witnessed on every platform by a test of the oracle rather than of the funnel, the Unix legs being unable to see a Windows-only mutation. |
| `src/rundir/names.rs` | `7724ed1` | 2026-09-04 | Forty-five lines: a module doc, a `#![deny]` of the three governed lints, and eight `pub const &str` names. §6 and §7 have nothing to bind — no ownership, no lock, no clone, no `?`, and not one expression that can panic, so §7's panic surface (indexing, slicing, `unreachable!`), tightened on 2026-09-04, finds nothing here either. The sweep was therefore §5, §8, §12 and §13, under the owner's amendment 7. §5/§8: the eight stay `&str` and are not newtyped. They are compile-time constants with no parse boundary for a validated type to sit at (the `object.rs` case had ids arriving from Git at runtime). `&str` is what their uses take without a conversion, and the uses are **not** uniform, which an earlier version of this row got wrong: all eight are joined onto a `Path`; only `MARKER` (`rundir.rs`, `remove_public_husk`) and `MARKER_STAGED` (`ownership.rs`, `unbound_shape`) are compared against a directory entry; and `OWNER_RECORD` and `OWNER_RECORD_STAGED` are the two taken into a `Vec<String>` expectation by `to_owned()`. `const X: &Path` is not constructible at all on MSRV 1.85, measured (`rustc +1.85.0`: "cannot call non-const associated function `Path::new`"). The `#![deny]` was re-measured rather than trusted, one primitive per lint: `std::fs::write`, `std::process::Command` and `println!` are refused three times here with `names.rs:25-27` cited as the level, and the identical three lines in `src/rundir.rs` emit no `disallowed_*` at all under that file's own allow — only `clippy::print_stdout`, which the comment never claimed. The module takes no `effects/allowlist.toml` row and reaches no governed primitive; `mod names;` is private and `src/rundir.rs`'s `pub use` is the only path to it, so nothing reaches past the re-export, and the re-export carries all eight. Seven of the eight are published by a funnel in `src/rundir.rs`; `EVENT_LOG` is not, and the row said otherwise until pass 2 — `src/rundir.rs` only names it, and `EventLog` in `src/events/log.rs` opens and appends the log. **The finding was not in the file.** Its doc claimed one byte string agreed by census, funnel and test, and two of the eight did not have that: `RunPaths::events` and `RunPaths::plan_json` spelt `"events.jsonl"` and `"plan.normalized.json"` for themselves while `classify.rs` — which decides husk from committed — read the log through `EVENT_LOG`. Both accessors now go through their constant, accompanied by `the_event_log_and_plan_accessors_return_the_paths_their_constants_name`, which is named for the sentence it proves and no more: it fails on an accessor drift and, as its own doc says, cannot witness a substitution of equal values at all. The claim stays small because it was measured rather than argued: the `EVENT_LOG` mutation, stated once in PR #143's body with what it does and does not witness, and deliberately not restated here or in `SWEEP-NAMES-002`. It measures what routing `RunPaths::events` changed and nothing else: it mutates `EVENT_LOG` only, so leave `plan_json` at its literal and every number is unchanged — the `PLAN` half of the repair has no witness beyond the equal strings. Seven production sites in five files outside the module still spell the byte string, and the doc names them rather than claiming a single source of truth. That measurement, not the equality test, is the repair's witness: substituting a constant for a literal of equal value is behaviour-neutral by construction, so no assertion over values can fail on the pre-repair code, and the test says so itself. Two further doc claims corrected against the head: the ownership proof consults **four** of these names in three ways, reading `MARKER` and `OWNER_RECORD` back, *stats* `COMMIT_RECORD` — conjunct 12 is its existence, never its content — and comparing a directory entry against `MARKER_STAGED`; and `DESIGN.md` §15's drawing carries `events.jsonl` and `plan.normalized.json` while the other six persisted names appear in no `design/` section, which is a missing compatibility contract rather than a documentation gap (`SWEEP-NAMES-008`). No citation of the retired `decisions.workspace_candidates.run_creation` stands in for that: a retired record establishes no product contract, and neither does code quoting one. Deferred, each naming its file: the five outside spellings (`SWEEP-NAMES-002`), the one test that spells `committed.json` by hand, which pass 2 established is a **guard** and not a duplicate; `SWEEP-NAMES-003` holds the measurement and the count, is blocked on `SWEEP-NAMES-008`, and the assertion carries a comment at its own site saying not to tidy it away, the same one-name-two-spellings class for nine more names inside `src/rundir.rs` — `report.json` and the eight skeleton directories, eighteen spellings (`SWEEP-NAMES-004`) — and `read_dir_names`' `to_string_lossy`, which makes a non-UTF-8 entry name unremovable and so a husk permanent (`SWEEP-NAMES-005`); and the six persisted names with no `design/` authority, where a `COMMIT_RECORD` rename fails exactly one test today — the `emit/tests.rs` literal — and, once that assertion is routed through the constant, goes green and lets conjunct 12 stat the new spelling in an old P5b directory still holding `committed.json`, get `NotFound` and delete a private half that had crossed the deletion boundary (`SWEEP-NAMES-008`, for the owner: the remedy is in §15). After review (PR #143, pass 1 on `b81c101`, four findings, no P1 or P2 — three claim corrections and one deferral — fixed in `d323eeb`): the equality test is no longer offered as the repair's witness, the retired-record citation is gone rather than reworded, the census is one derivation quoted identically in three places, and the commits are stated as reverting together rather than independently. After pass 2 (on `ca77864`, five findings, again no P1 or P2, every one a claim in this record or in a row rather than a defect in the code): `SWEEP-NAMES-003` prescribed removing the only test that catches a `COMMIT_RECORD` rename and is now blocked on `SWEEP-NAMES-008` with the literal commented at its site; the mutation measurement is withdrawn from `SWEEP-NAMES-002`, which `MAINTAINING.md`'s witness-outranks-severity rule would otherwise have obliged fixing; the new test's direction is corrected to accessor drift; and this row's own usage and funnel accounts were false and are re-derived above, because a file should not be marked swept on an account that is itself wrong. After pass 3 (on `fe4f776`, three findings, all P3 and all one class — a sentence about a measurement disagreeing with the measurement, in a claim restated across four files): the test is renamed to the sentence it proves; `SWEEP-NAMES-008`'s sequence no longer says the suite is green after a `COMMIT_RECORD` rename, which this pull request's own measurement contradicts; `SWEEP-NAMES-002` attributes the consequence per constant instead of claiming five stale sites for both; and each measurement now has one canonical home that the other places point at rather than restate. |
| `src/agent/proc/ambient.rs` | `d3f224c` | 2026-09-05 | The first file of the `#117 agent/proc` family, swept under amendment 7 (PR #147). §6: no shared ownership, lock or clone -- none written, and the file holds none; the `OnceLock` and the `Mutex` it fronts are the parent's (`windows_job::AMBIENT`, `termination::CONTAINER_SCOPE`). §7: two `?` in `join_ambient_job_with`, each after a `map_err` into a `Refused` naming the operation, kept as deliberate and spelt out in the `# Errors`; no panic, index, slice or `unreachable!`. Correctness: no defect in the file. What it reads from its environment is nothing, stated in the module doc; the environment reads reachable from it are `termination`'s resolution of a bare reaper program on Unix through `find_program` — `PATH`, the working directory for a relative entry, and the filesystem's metadata — which refuses a name no entry resolves to a regular file but accepts a non-executable one (`SWEEP-AMBIENT-012`, row 51) and is made again per reaper start with the failure folded by `.ok()` (`SWEEP-AMBIENT-010`, row 51). Fixed as text: the `INV-18`, `crash_reconstruction` and `decisions.*` citations are declared the retired packet's wording and not authority, since `design/15` describes only the private per-invocation Job Object and no public document defines `INV-18`, which the operator-facing refusal names (`SWEEP-AMBIENT-002`, the owner's); both joins' `# Errors` name all three refusals, `set_container_reclaim_scope`'s names the `PATH` and non-UTF-8 refusals, and the two `Option` oracles say a failed kernel query is also `None` (the parent's `ambient_contains` doc corrected with them). Shape: `set_container_reclaim_scope` takes the two-`cfg`-function form its sibling has, `_scope` named rather than `let _ = scope`; `poison_ambient_for_tests` is `#[must_use]`. Tests: the Unix contract "the hook is not consulted" had none -- `the_unix_ambient_join_is_a_no_op_that_consults_no_observer`, witnessed on the box against consult-and-apply and consult-and-discard; every Windows behaviour of the file but one is pinned by a `#[cfg(windows)]` test named in the PR body's table with the mutation it would catch, none of them run here; the one is a hand-written observer answering `Error` at the kill coordinate after a successful join, documented in `join_ambient_job_with`'s `# Errors` and unpinned. Deferred: the raw `FILETIME` identity in `process_alive` (`SWEEP-AMBIENT-011`, row 51; pid identity is an open owner decision and was not touched). After review (PR #147, pass 1 on `7150ea9`, three unlabelled findings, all read as P3 and all text): the error-return contract had assumed a first call, the environment account had overclaimed fail-closed, and this row and the body had contradicted the body's own disclosure and ledger; corrected in `d3f224c` and the record commit, with `SWEEP-AMBIENT-012` filed from the second. |
| `src/agent/proc/drain.rs` | `94c2343c7431f9fb901e3ee0d20bfe7145837060` | 2026-09-05 | Row 47, the second file of the #117 agent/proc family. PR #154 retains both CHANGES_REQUIRED reviews, the preserved 6574728 repair and each finding in its canonical ledger. The current pipe repair uses owned nonblocking endpoints, cancellation and joining on collection and drop, preserves secondary failures on early supervisor errors, and distinguishes released capture from observed EOF. Raw and text collection share one lifecycle, with the vector moved after joining. Ownership, locking, retry bounds and native mode-setting are documented. Focused evidence includes 21 drain and ten stdin tests, child-refusal and 1 MiB echo cases, two worker tests, an isolated native inactive-peer collection/drop test, the effect census and MSRV check. Nine current mutation witnesses cover cancellation, failure reporting, joining, raw bytes and native mode-setting, with source restoration verified; the earlier helper repair has its own two-path and mutation evidence. The stdin startup/write and helper setup-failure findings are fixed. The public agent completeness limitation remains recorded. This records the local pipe preparation commit, not independent approval or a full passing baseline. The separately requested all-exit process-owner assessment and shared CI findings retain their own unresolved scope and evidence. |
| `src/validate/graph.rs` | `047abff` | 2026-09-05 | The first file of the `src/validate/` family (#69), swept under amendment 7 (PR 167). §6: no shared ownership, lock or clone, before or after; the ids copied into a cycle report and a warning are owned because the error value outlives the plan's borrow. §7: no `?`; nine indexing and slicing sites in `find_cycle` at the base (the lint: lines 127, 129, 133, 135, 137, 139, 142, 149, 156), one `unwrap_or(0)` whose fallback named a path that is not a cycle, and one `unwrap_or_default` folding an impossible absence -- all gone, the search keyed by id over one `BTreeMap<&str, &Task>` shared with the wiring check, the file denying `clippy::indexing_slicing` and `clippy::unreachable` itself, tests included, with the head census reporting no site (`SWEEP-GRAPH-002`, `-003`, `-005`, `-006`). Correctness: the recursive search took its depth from the plan (`SWEEP-GRAPH-001`; measured at the base, a 50,000-task chain overflows Linux threads of 256 KiB, 1 MiB and 8 MiB in the test profile; Windows not measured) and now walks an explicit path; a task needing an artifact it is recorded as producing is silently accepted, as at the base: the first head made it warn, and pass 1 (PR #167) showed the warning wrong for a task that needs and declares an artifact an earlier task it depends on also declares -- input the design gives no meaning to -- because the record holds one producer per artifact; withdrawn and pinned silent through the adapter with the reviewer's input (`SWEEP-GRAPH-004`). No diagnostic changes for a plan the base answered; a chain deep enough to overflow the base's recursion is now refused, or accepted, instead of aborting the process, the one observable change. Kept: `warnings: &mut Vec<String>` (the parent's accumulator convention, shared with `config::load_captured`; row 60's question) and `ValidationErrors`' strings (the parent's type). Tests: none in the file before; eleven now, ten over hand-built `Plan` values and one through the markdown adapter, each witnessed in the PR body's table against the mutation it catches (`M1`--`M10`), the parent's four end-to-end tests untouched and still green. Astra reassessment at admission head `24c88fd`: `SWEEP-GRAPH-004` and `SWEEP-GRAPH-009` rejected as correctness claims. The examples reproduce the self-producer silence and recorded-producer warning specified by `design/09`, not a contract violation. All declarations remain in the tasks' `artifacts_out`; the design leaves multiple declarations undefined. The two resolved finding files are removed and both histories remain in PR #167's ledger. No runtime or test expression changed in that reassessment. After review (pass 1 on `2bbf35b`, three unlabelled findings): the warning withdrawn as above; `depends_transitively`'s doc corrected (an edge whose id is the target matches before the index is consulted); the PR body's Scope corrected to declare the `design/09` paragraph. After pass 2 on `e05492a` (one P2, three P3, all text): this row and the body no longer say "no behaviour change" (the deep-chain abort became a refusal), no longer call the two-declaration plan a valid update, state the M10 stack sizes as Linux threads, and name the three private helper signatures that changed. |
| `src/status/render.rs` | candidate pending independent review | 2026-09-05 | PR #166, status family row 56. The earlier c56edf4 entry overstated its evidence; both earlier reviews returned CHANGES_REQUIRED. Follow lines now report recorded answer policy without inventing a later failure, preserve independent attempt/terminal reasons, review verdicts, transitions and parking, and retain exact integer milliseconds. Calendar/clock/fraction/zone checks control timestamp abbreviation; malformed and leap-second values remain whole. Shared terminal sanitation covers follow lines plus report, ledger and trailing status lines without altering serialized data. Schema-3 validation now refuses non-passing reviews without their failure record before replay, closing SWEEP-RENDER-014. Section 6: no shared ownership, locks or clones in this module. Section 7: clock_of deliberately propagates Option absence to the whole-timestamp fallback, using checked ranges; there are no panicking indexes/slices or unreachable branches. Remaining String formatting result discards have their reason at the import. Source inventory: 21 parent status tests, 12 added over the original base, and six added fixture helpers; expanded repairs also add three report tests, two schema tests and one terminal utility test. Exact final check/review evidence belongs to the PR body; historical M1-M12 labels are not new candidate evidence. SWEEP-RENDER-012 and 014 are repaired; separate P3 records remain for FailureKind spelling policy, 011, and manually constructible public RunStatus flag inconsistency, 013. |
| `src/connect/render.rs` | `6ae3f20` | 2026-09-05 | The first file of the `src/connect/` family, queue row 61; the commit column records the initial pass-1 repair. The renderer encodes strings, table keys and allowance numbers, and prefixes each physical TOML comment line. Its only clock read is `pools_file`; `pools_file_at` takes a timestamp for tests. Both renderers use `usable` and mark a defaulted kind. The CLI prefixes every physical discovery-note line and replaces control characters. §6 has no shared ownership or locks; test snapshots are owned values. §7 has no `?`, panic, index, slice or `unreachable!` in production; writes into a String discard only its infallible fmt result. Pass 2 found that the parent still collapsed quoted whitespace, could persist invalid allowances, and carried overbroad compatibility and malformed-file claims. The subsequent Astra repairs replace the scanner with `settings_match`, comparing complete TOML tables without coercing integers to floats; reject invalid allowances with a warning while preserving the other valid operator keys; preserve existing-file read errors; name creation/write failures; share the timestamp prefix and filter it only on the first header; and report a skipped agent once. Design sections 17 and 18 describe the resulting behavior, including the integer-to-float upgrade boundary and malformed-comment refusal. `quoted_pool_renames_preserve_every_string_byte_without_force`, `force_rejects_invalid_allowances_and_preserves_other_operator_keys`, `discovery_note_lines_cannot_impersonate_warning_records` and `a_different_header_timestamp_is_unchanged_and_keeps_the_existing_bytes` exercise the repaired contracts. The PR body records exact later heads, regressions and mutation evidence. Parent row 62 remains queued; the affected -009, -010 and -011 finding files are resolved. The broader public AgentReport shape (-008) and hypothetical future key/name compatibility policy (-014) remain documented findings. |
| `src/config/parse.rs` | `1755431` | 2026-09-04 | The six section readers of `upstroke.toml`, swept under the owner's amendment 7. §6: no shared ownership, lock or clone; the one copy per refusal is the file's path into the error value, made in `config_error`, the helper that replaced three closures and four inline constructions of the same error. §7: thirteen `?` at the base, each propagating an `UpstrokeError::Config` built at the site or by a helper that names the key (after a `map_err`, an `ok_or_else`, the `limit` closure, `check_budget` or `refuse_or_announce`); every `unwrap_or`, `map_or` and `unwrap_or_else` but one folds an *absent* key, a written key that cannot be read being refused by the section's `try_into` first, and the one exception is `[engine] shell`, which folds an unrecognised written value into the platform default and warns by name; the two panic-surface lints report no site in the file (the command is in PR #150). §8: the one host path, a mount `source`, is a `PathBuf`; a mount `target` is a container-side path held as `String` by the parent's documented decision on `RunnerMount`, recorded as debt rather than claimed (`SWEEP-CONFIG-PARSE-020`, row 54). **Fixed:** an unknown key in `[interaction]` and in a `[[gates]]` entry loaded without a word -- `ask_befor = { … }` was a deleted spend approval, `mod = "never"` a CI run that stops to ask, `timeout_sec = 3600` a gate killed at the default -- and both are now refused (`RawInteraction` by its derive; `RawGate` collects the key and `parse_gates` decides), the parent's two structs being the immediate call site; an unknown key in `[engine]` warned and left the default in place, which for `on_task_failur = "continue"` was a halted run with a footnote, and is now refused by name on every reading (a compatibility change: measured, nothing in the tree's fixtures, docs or examples writes a key under `[engine]` outside the six accepted); two `[[gates]]` entries with one name were accepted although the name keys the attempt's log file and the failure report, and are now refused with names compared without regard to ASCII case, since NTFS and APFS keep one log file for `check` and `Check`; a sequential resume with `max_parallel = 3` alone announced two companion values the file does not contain; a `[runner]` refusal quoted a `kind = "host"` the operator never wrote, and named only the first unknown key; a blank gate field is named. After review (PR #150, pass 1 on `74476b6`, four findings, two P2): the two new `[[gates]]` refusals refused the resume of a run recorded under the earlier rules, against `design/15`'s standing sentence that gates are taken from the record and not refused over -- the design had the authority and the code was wrong against it -- so `parse_gates` took `EngineLimits` and both shapes warned on a sequential run's resume; the `check`/`Check` control collided on case-insensitive filesystems and is now the refused pair; the §7, §8 and §13 claims in this row and the body were corrected to what the file holds. After pass 2 (on `3c3f01f`, two P2 and one P3): that downgrade was keyed off "a resume" alone, which cannot tell whether the log recorded gates -- `run_started.gate_cmds` is absent on a log predating the record at any schema, and on such a resume today's section becomes the record -- so it promised the record would run where there was none and kept refusing where there was one; `EngineLimits` now has three readings chosen by `for_resume(schema, gates_recorded)` from `events::recorded_gates`, which `resume.rs` reads before validating: with a record today's `[[gates]]` is compared only and never refused over (every shape a warning, an unbuildable entry skipped, an unreadable section `None`), without one it governs and reads as a fresh run's; witnessed where the composition lives, by two resume-path tests in `engine::tests` (a log with a record resumed through a file rewritten into a zero timeout, a repeated name and an unknown key completes and runs the recorded gate; a log stripped of its record is refused before any effect over the same typo, and resumes once it is spelt right). The forced call sites outside this file are `resume.rs`'s two `for_resume` calls and one exhaustive match in `runner/container/resolve/tests.rs`; every match over `EngineLimits` in the crate was read (five, none with a wildcard over the enum). After pass 3 (on `db15914`, one P2 and two P3): the third variant and `for_resume`'s second parameter are a public API break -- downstream of the ruling, inherent to adding a reading to a public enum, failing at compile time (E0061, E0004), `#[non_exhaustive]` deliberately not added so a downstream wildcard cannot absorb the next variant silently, unmeasured by tooling and derived by reading -- and the body now says so where it had said no public item changes; a `[gates]` table under the compare-only reading comes back as `None` and the engine then reports drift against derived defaults, which the file answers with a warning that disowns that comparison while the typed unreadable state is deferred to row 54 and `src/engine/preflight.rs` (`SWEEP-CONFIG-PARSE-026`); four stale claims corrected. After pass 4 (on `d24154a`, one P2 and three P3): the compare-only reading had been put on the public `SequentialResume` itself, a silent change of meaning for a caller passing it to `load_limits` directly that recreated the zero-timeout defect for a legacy log -- the mapping is inverted, `SequentialResume` governs as it always did and the new `SequentialResumeWithRecordedGates` carries compare-only, and the body records the found-and-removed change beside the two compile-time breaks; a `timeout_secs` above what `run_started`'s `timeout_ms` can hold (`u64::MAX / 1000` seconds; the record saturates and a resume would run a smaller value than the file says) is refused, the bound pinned to the serialiser's arithmetic and stated in `design/17`; `RawGate`'s required keys are optional at the wire so `nmae = "check"` is refused naming the missing `name` beside the unknown `nmae`; the body's row for `SWEEP-CONFIG-PARSE-021` re-derived from the finding file. §12: twelve tests in the file on `toml::Value` fixtures, one in the parent through `load_limits` and two in `engine::tests` through `resume`, each witnessed against a named mutation, two of them pinning refusals the parent's suite left unpinned (an `ask_before` threshold that is negative or not finite; a `[budgets]` ceiling that is not finite); the parent's tests that pin this file's other refusals were each witnessed against a mutation as well. §13: a `# Errors` contract on every reader; a refusal names the section and the key, and the value when the value is what is wrong; no byte offset is carried (spans are gone at `toml::Value`; a `toml::Spanned` reading is row 54's); `design/17` states what the repo-level file refuses, the `[engine]` refusal, and the two resume readings of `[[gates]]` as the contract. Deferred, each with a file: a misspelled section header is dropped whole by `RawRepoConfig` (`SWEEP-CONFIG-PARSE-007`, P2, rows 53 and 54); `RawStrategy` drops unknown keys (`SWEEP-CONFIG-PARSE-008`); a relative mount `source` or `target` is accepted while mounts are inert (`SWEEP-CONFIG-PARSE-009`); credential-volume agent ids are not checked against the adapter registry (`SWEEP-CONFIG-PARSE-010`); `gates::run_all` folds distinct names into one log file (`SWEEP-CONFIG-PARSE-011`); an unknown `shell` warns and degrades, kept as the product decision the parent's suite pins (`SWEEP-CONFIG-PARSE-012`, accepted-risk); the `target` `String` (`SWEEP-CONFIG-PARSE-020`); `capacity::report` loads the repo config under the fresh reading and refuses what a fresh run refuses, the refusals this sweep added included, while reading only `[pools]` and `strategy.mode` (`SWEEP-CONFIG-PARSE-021`, `src/capacity.rs`). Refusals stay `UpstrokeError::Config` strings, so a test pins a substring; the typed refusal is row 54's flattening decision. |
| `src/plan/markdown/annotation.rs` | `28627ca` | 2026-09-05 | PR #169, first file of the Markdown family (rows 63–68). §6: no shared ownership or lock; found comments and parsed string attributes own data beyond the parser event, and inline normalization replaces container prefixes in owned text. Clone remains on Annotation for unchanged callers in drafts.rs/assemble.rs (SWEEP-ANNOTATION-002). §7: the Option propagation in AnnotationSink::take means an author comment is not ours; the two checked-span propagations in strip_spans refuse an invalid range, which callers report while keeping the body. No panicking index or range slice remains in the swept file. Comment spans use a piece table because CRLF/container prefixes make HTML event source ranges non-contiguous; blocks close independently, preventing unrelated later HTML from completing an annotation. Empty IDs, repeated attributes, orphan annotations and unterminated comments warn; only the last attribute value is parsed. Pass 1 repaired quoted inline floors, checklist prose, consequence warnings and duplicate heading annotations, and initially narrowed warning-delivery claims. Pass 2 exposed global quote trimming and lone-CR handling: only the actual inline quote/list continuation prefixes are normalized, accounting for indentation and partial tabs, literal keys are retained, and all parser walks use a length-preserving lone-CR view while body spans/hash retain original input. Caller repairs preserve exact unterminated checklist text and warn on unclosed heading annotations. Typed errors now retain warnings through no-tasks and validation refusals. Regression matrices cover LF/CRLF/lone-CR containers, literal markers and original text/hash; warning tests cover successful previews and typed refusals. SWEEP-ANNOTATION-003 and -017 are fixed; -001 is rejected as a breach of the current explicit warn-and-absent contract, leaving refusal as an optional policy change. Only annotation.rs is swept; changed caller bodies obey activation and their files remain queued. |
| `src/runner/host/environment.rs` | `b1e25b58` | 2026-09-06 | Row 41, `#111 host` family. §6: grep-verified no `Rc`, `Arc`, `Mutex` or `RwLock` anywhere in the file; the file's two `.clone()` sites are `self.base.clone()` in `compose` (an independent owned copy the function mutates into its return value while `self.base` survives for the next call — the extended notes already explain why the reserved-key drop-then-resupply must run over a copy rather than the shared base) and one `OsString` value clone inside `lookup` (a small owned value returned from a borrowing scan). Neither satisfies the borrow checker; both are the intended semantics. §7: grep-verified and `cargo clippy --all-targets --all-features -- -D warnings` and `-W clippy::unreachable -W clippy::indexing_slicing` (crate-wide) both report zero sites in this file for `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!`, indexing, slicing or `unreachable!`. The one `?` (`compose`'s `self.preflight(overlay)?`) propagates `preflight`'s own fully-formed `UpstrokeError::Refused`, which already names the offending and reserved keys; `compose` has nothing to add. No code change was needed for either section. Correctness: read in full against DESIGN.md:258-264 and the file's own `docs/internals` notes — the base/reserved-values/overlay composition order, the role-scoped credential gate (`supplies_credentials`), and the "absent stays absent" lookup behaviour all match; no defect found, and the paired notes file needed no correction. Regression evidence: four targeted mutations, applied one at a time and reverted (`git diff` empty before and after), each killed by the existing `runner::host` suite in `src/runner/host/tests.rs` (row 44, a separate unswept file, out of this sweep's scope to edit) against a clean baseline of 61 passed/0 failed/5 ignored: inverting `supplies_credentials(role)` in `reserved_values` (7 failed), dropping the reserved-key removal loop in `compose` so a role that does not get a credential location supplied could still inherit the host process's own copy of it (4 failed), making `KeyCase::Insensitive::same_key` case-sensitive (1 failed, the Windows-parity test), and disabling `preflight`'s refusal loop (5 failed, including `a_reserved_key_in_the_overlay_is_a_preflight_error`). No test gap found; no new test needed or added. `cargo fmt --check` is clean. After review (PR #181, pass 1 on `d15d9310`, two P3, no P0/P1/P2): the `#[derive(Debug, Clone)]` on `HostEnvironment` carried an unexplained, unused `Clone` — its only field owner, `HostRunner`, does not derive `Clone` and never calls `.clone()` on the field, and no other in-tree caller clones the struct — so `Clone` is dropped; `HostEnvironment` now derives `Debug` alone (`SWEEP-HOST-ENVIRONMENT-001`, fixed, finding file deleted). A regression in `src/runner/host/tests.rs` (`host_environment_does_not_implement_clone`) asks the trait system, not the source text: an inherent associated const bounded on `T: Clone` shadows a blanket-trait `false`, so it fails whenever `HostEnvironment: Clone` holds, whatever the spelling (one derive, a stacked `#[derive(Clone)]`, a hand-written impl), and it reads no comment, so a justified `Clone` returns only by replacing the assertion together with the §6 explanation beside the derive; a companion test proves the probe on fixtures of each spelling, plus `KeyCase` (`Clone`) and `HostRunner` (not). Confirmed failing against `#[derive(Debug, Clone)]` and against the stacked spelling, passing after the removal. Review pass 1 of the fix PR (#187, on `f97c04e8`) found the first instrument, a nearest-derive-line source scan, blind to the stacked spelling and over-claiming a comment exception it never read (`SWEEP-HOST-ENVIRONMENT-REVIEW-001`); the trait probe replaced it. `SWEEP-HOST-ENVIRONMENT-002` (stale `DESIGN.md`/decisions citations) remains open and out of this fix's scope. |
| `examples/probe.rs` | `a0ba69b3` | 2026-09-06 | Row 55, the file outside `src/` that closes the W2 wave; it belongs to no family. §6: no shared ownership, lock or clone — none written, none to bind. §7: no `?`; no `.unwrap()`/`.expect()` outside the file's own `#![expect(clippy::print_stdout, …)]`, which §13 requires of an example and which the module comment already states as the file's contract; `cargo clippy --example probe --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports zero sites of either construct. The crate-level `#![allow(clippy::disallowed_macros)]` is the frozen legacy allowance `effects/allowlist.toml` (`[[legacy]] path = "examples/probe.rs"`) already carries, unchanged and re-measured rather than trusted. Correctness: no defect — the loop's two fallible calls (`probe`, `discover`) are each matched, an `Err` is printed and handled without propagating, and neither ever `panic`s. **Fixed (simpler implementation):** after matching `&probed` to print and `continue` on `Err`, the body re-destructured `let Ok(caps) = &probed else { continue };` purely to recover the binding the first match had already established, for the `discover` call below — a second, redundant check of an invariant the exhaustive match just proved. The two matches are now one: the `Ok` arm binds and returns `caps` from a `match` expression, so the value flows out without re-inspecting `probed`. No behaviour change (`a0ba69b3`). Tests: none added. `main()` has no function of its own to unit test — every fallible step is a trait method (`AgentAdapter::probe`/`discover`) exercised by that trait's own suite in `src/agent/`; this file's whole content is formatting already-typed values into `println!`, and compiling it (`cargo clippy --all-targets`, part of the nine-gate baseline) is the only mechanical check its zero-branching print loop admits. The file's own doc comment states its manual contract: `cargo run --example probe` is zero-spend and its output is what it demonstrates (§13). No findings deferred. |
| `src/topology/fold/check_candidate.rs` | `cf5ea934` | 2026-09-06 | Row 31, `#108 fold` family (PR #186). §6: grep-verified no `Rc`, `Arc`, `Mutex`, `RwLock` or `clone()` in the file, before or after. §7: `cargo clippy --all-targets --all-features -- -D warnings` and the same command with `-W clippy::unreachable -W clippy::indexing_slicing` both report zero sites in this file for `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!`, indexing, slicing or `unreachable!`; the three `?` (`self.entry`, `self.task`, `self.open_generation`) each propagate an already-contextualized `FoldError` built by a sibling helper that was given this checker's own `kind`, and each is kept as deliberate. **§5, fixed:** `check_candidate_prepared`'s final match dispatched on `(&prepared.lease_effect, entry.lineage)` — a two-variant `CandidateLeaseEffect` crossed with `Option<Lineage>` — with a wildcard `_` absorbing the two mismatched combinations, so a third variant would have fallen into that arm's generic message instead of failing to compile; the match now names all four combinations (`SWEEP-CHECK_CANDIDATE-001`). **Tests.** Review pass 1 (`gpt-5.6-sol`, medium) returned CHANGES_REQUIRED on two P2 findings: three guards in this file whose mutations survived the entire crate suite. Both checkers take `&RunState`, whose construction needs the `RunStarted4`, plan, chain and registry-digest fixture built in `src/topology/fold/tests.rs` (row 39), which this sweep's scope does not include, so the two relations those guards implement — the record's lease effect against its task's lineage, and which candidate a generation may promote — are private functions (`check_lease_effect`, `promoting_candidate`) pinned by a `#[cfg(test)]` block in this file: the only one in the `fold` family, for that reason. The arms, their order and their refusal text are unchanged; each test asserts the refusal's `kind` and whole `detail`, since all three refusals share `InconsistentRecord` and a variant-only assertion would pass on the wrong arm. **Regression evidence:** sixteen mutations, applied one at a time and reverted (`git diff` empty before and after), the eight unchanged guards re-witnessed at the final head. At the reviewed head four survived; at this head one does. Killed: the in-flight guard, the successful-attempt guard, the obliged-review-passes guard, the wrong-attempt guard, the parent-is-base guard, the base-sha guard and the `task_candidate_created` mismatch (all by `src/topology/fold/tests.rs`); the two region checks and the mismatched-pairing arm (by both suites); and — newly — the widening's root check, the widening's region check and the promotion guard, each by the one test written for it (`SWEEP-CHECK_CANDIDATE-003`, `-004`, both fixed). The extraction's own two seams were measured rather than assumed: dropping the call site's `entry.lineage.map(|lineage| lineage.root)` is caught by six existing fold tests, and handing the helper a wrong `kind` was caught by nothing — so the kind is no longer a parameter but the module's one `CANDIDATE_PREPARED` constant, whose own mutation the sibling suite kills. **`-004`'s mechanism, corrected:** the finding read a closed generation as able to promote a stale candidate. It cannot — `TaskFold::open` does not return a closed generation, so `open_generation` refuses one step earlier. The mutation survived because the class conjunct is redundant while `apply_candidate_prepared` remains the only writer of `.candidate` and writes `class = Promoting` with it; `check_attempt_started` and `in_flight` both refuse a promoting generation, and `check_dispatched` refuses a second open one. The conjunct is kept and pinned directly, because that invariant is maintained in `src/topology/fold/apply.rs` (row 29, still open). **Deferred:** the `generation.candidate.is_some()` guard is unreachable on the same reasoning and its mutation still survives; it is kept as defence in depth and recorded, not claimed tested (`SWEEP-CHECK_CANDIDATE-002`). `cargo fmt --check` clean; full crate suite green before and after (2,141 passed, 0 failed, 42 ignored on Linux). After the P3 findings workflow's fix (PR #198, on `351556fc`): `SWEEP-CHECK_CANDIDATE-002`'s guard is extracted the way `-004`'s was, as `refuse_repeated_candidate(&GenerationFold, TaskKey, GenerationId)`, and pinned directly by two new tests in this file's own `#[cfg(test)]` block rather than left recorded and untested; the call site, refusal text and behavior are unchanged. |
| `src/topology/fold/check_attempt.rs` | `fada7445` | 2026-09-06 | Row 30, `#108 fold` family. §6: no shared ownership, lock or clone anywhere in the file (grep-verified: no `Rc`, `Arc`, `Mutex`, `RwLock`, `.clone()`). §7: no `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!`, panicking index/slice or `unreachable!`; every `?` is same-type propagation on the file's own `self.entry`/`self.task`/`self.open_generation`/`self.in_flight` helpers, each returning `FoldError` with nothing to add; the one arithmetic op that could overflow uses `checked_add`. No code change was needed for either section. Correctness: read in full against `check_admission`'s two-variant `Admission` enum and three-variant `SpawnAdmission` (confirmed the match is exhaustive-and-correct, not missing a `HumanRequired`/`HumanBinding` arm as first suspected) and against the file's own `docs/internals` notes; that read missed one defect, found by review pass 2 and **fixed** in `fada7445` (`PR180-REVIEW2-001`, P2): `check_attempt_started` indexed the frozen ladder by the event's own rung and never compared it with the task's replay-derived `TaskFold.rung`, so a rung-0 `attempt_started` carrying rung 0's valid binding was accepted on a task already escalated to rung 1 (the fold held rung 1, the persisted attempt said rung 0, and the next failure was charged to rung 1's allowance although the weaker worker ran), and `check_attempt_finished` had no `Escalated` arm, so `apply_settlement` took whatever rung a settlement carried. An attempt now runs at exactly the task's replay-derived rung on both the override and the frozen-binding path, and an escalation must name `task.rung + 1` and a rung the frozen ladder has — the driver's own contract (`ladder_position` in `src/engine/topology/run.rs`, `next_step` in `src/ladder.rs`) and `design/11` step 4's retry-then-escalate ordering — refused as the new typed `FoldError::WrongRung`. Regression `an_attempt_runs_at_the_replay_derived_rung_and_an_escalation_climbs_exactly_one` in `src/topology/fold/tests.rs` (row 39, which this does not sweep): zeta spends rung 0 and escalates onto rung 1; a start below and above the position, an escalation backward, sideways, over a rung and off the top are each refused with the position untouched, live and through the wire; two existing tests that escalated the one-rung alpha onto rung 1 or started zeta on rungs it had not reached now use zeta and walk the ladder by escalating between rungs. Failing-before witnessed by running the new tests against the reviewed head's validator (2 failed); five mutations of the two checks (a start below accepted, a start above accepted, the ladder bound dropped, the escalation refusal disarmed, the bound off by one) each killed by the regression. Regression evidence: four targeted mutations (the dense-key comparison and the dependency-merged-state comparison in `check_spawn`, and each of the two byte-identical `WrongAttempt` checks in `check_attempt_finished`'s `Retained`/`Closed` arms, applied and reverted one at a time) each killed by the existing `topology::fold::tests` suite (121 tests baseline green; 32/16/18/28 failures respectively) — no gap found, no new test added. Fixed: the paired `docs/internals/topology/fold/check_attempt.md` cited the retired `decisions/2026-08-12-merge-queue-execution-topology.md` twice (the `candidate_prepared`-is-sole-successful-settlement claim); repointed to `design/26_design_merge_queue_protocol.md` §26, where that decision's substance now lives (quote verified verbatim against source). Deferred, each filed: the same dead citation at twelve sites across ten other `docs/internals/**` files outside this file's scope (P3, `PR180-DOCS-DEAD-DECISIONS-PATH`; the count was re-derived on two engines after pass 1 — this row first said nine files and the pass's own correction said eleven sites, and both were short); the two `WrongAttempt` copies could be hoisted out of the match, but doing so changes which error wins when a record violates more than one condition at once and no test was found pinning today's priority, so left as-is rather than risk an unwitnessed behaviour change (P3, `PR180-DUPLICATED-WRONGATTEMPT-CHECK`). After review (PR #180, pass 1 on `50bcfab0`, two findings, both P3, neither against the Rust file — the reviewer accepted it as satisfying the activated §6/§7 obligations — and both against this sweep's own new prose, fixed in the repair commit): the repaired citation still named `INV-07` beside §26 as though both were authorities, and no living design section defines any `INV-` id, so §26 is now the sole authority and `INV-07` is marked the retired packet's label (the treatment `src/agent/proc/ambient.rs`'s row already set for `INV-18`); and this row's dead-citation count was wrong. After pass 2 (on `ce56bd6f`, the last of the sweep workflow's two): the P2 above, which parked the row on 2026-09-06 because its regression belongs in row 39's file; recovered under the owner's parked-PR workflow the same day with the fix, the regression and the two adapted tests, reviewed there by gpt-5.6-sol at high effort (the recovery's own passes are recorded in the PR body). |
| `src/topology/effects/export.rs` | `990b838e` | 2026-09-06 | Row 21, `#106 effects` family, 123 lines at assignment. Sole finding, §7: `effect_sites_json` returned `serde_json::Error` directly, the raw external-crate error as this module's own public error type ("library modules return typed errors, normally derived with thiserror"). It now returns `ExportError`, a `thiserror`-derived struct wrapping the source with `#[from]`, `Display` "failed to serialize the effect site inventory: {0}" (lowercase start, no trailing period, source not repeated), and a `# Errors` doc section on the function (§13). §6: no `Rc`, `Arc`, `Mutex`, `RwLock` or clone-to-satisfy-the-borrow-checker in the file; the three `.to_vec()` calls each copy a borrowed `'static` or associated slice into the owned `Vec` field the export DTO's `Serialize`/`Deserialize` round-trip requires, stated at each field's doc. §5: fields stay `pub` -- these are one-way, `#[serde(deny_unknown_fields)]` records generated *from* the closed `EffectSiteId`/`ResidueClass` enums, with no invariant of their own beyond what those enums already carry. No `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!`, wildcard arm over a closed enum, or panicking index/slice in production code, confirmed by `cargo clippy --all-targets --all-features -- -D warnings` and a full read. `residue_classes`'s `elements: site.residue_elements().to_vec()` reads as a per-class field built from a per-site method until `residue_authority.rs` and `sites.rs` are read: the framework has exactly one `ResidueClass` variant today, and which elements a site can leave is per-site by design, per `sites.rs`'s own doc on `residue_elements` -- matching the existing `the_generated_inventory_describes_every_site_and_invents_none`'s own assertion, not a defect. §13: the module's existing rustdoc already carries the file's contract and no design sentence names `effect_sites.json`, so no `docs/internals` notes file is added. `export_error_names_the_operation_and_wraps_the_source`, added inside this file, constructs a real `serde_json::Error` from a malformed-JSON deserialize and asserts the wrapped `Display` text; the pre-existing `the_checked_in_effect_sites_json_is_what_the_enums_generate`, `the_generated_inventory_describes_every_site_and_invents_none` and the unknown-field census in the parent's `tests.rs` exercise the unchanged serialization path and still pass unaffected by the error-type change. `effect_sites_json`'s two existing callers (`src/effects/tests.rs`, `src/topology/effects/tests.rs`) use `.expect(...)` on the `Result`, which does not name the error type, so neither needed a change. After review (PR #177, pass 2 on `e94c64e0`, one P2, `SWEEP-EXPORT-001`): the parent `src/topology/effects.rs` re-exported `effect_sites_json` without its new error type, so a caller outside `topology::effects` could infer `ExportError` and never name it, and this file's own doc claim that the parent re-exports every item here was false; fixed in `119c81db` by adding `ExportError` to the parent's `pub use self::export::{...}` list, the one companion change the sweep's new public item forces. `export_error_is_nameable_through_the_parent_re_export`, in this file, passes a value typed as `crate::topology::effects::ExportError` through a local function and so compiles only while the re-export holds; with the re-export line removed the lib test target fails to build (E0425, cannot find type `ExportError` in module `crate::topology::effects`). The parent's other re-exports and code are unchanged and it remains queued at row 28. After recovery review 1 (on `69cf98d3`, one P2, `SWEEP-EXPORT-002`): neither test called `effect_sites_json`, so the function could revert to its raw `serde_json::Error` signature with the type and re-export left in place and the suite stayed green; fixed in `84891d7c` by `effect_sites_json_returns_the_typed_error_at_the_parent_path`, which binds `crate::topology::effects::effect_sites_json()` to `Result<String, crate::topology::effects::ExportError>` and asserts the inventory round-trips through `effect_sites()`; against the raw-error signature the lib test target fails to build (E0308, expected `Result<String, ExportError>`, found `Result<String, Error>`). |
| `src/topology/effects/harness.rs` | `e26f526b` | 2026-09-06 | Row 22, `#106 effects` family, 360 lines at assignment: the hook phases, the injection answer, the observation record, the arming refusal, and the fast-sequence declaration. **§6:** no `Rc`, `Arc`, `Mutex`, `RwLock` or `clone()` in the file, grep-derived over the whole file rather than sampled; the only owning copies are `name.to_owned()` in `begin_fast_sequence`, which is the sequence storing the name it was given (taking `&str` and owning inside, the shape `EffectSiteId::from_name` uses one module up), and the two `site.name()` allocations the arming refusals made, which are gone with the typed field below. `HookHarness` derives `Clone` and `Default` and every caller wraps its own in an `Arc<Mutex<..>>` outside this file, so the sharing this type is used through is not written here. **§7:** the file contains no `?` at all, so there is no propagation site to disposition; no `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!` or `unreachable!` in production and none anywhere outside the test module this sweep added; `cargo clippy --all-targets --all-features -- -W clippy::indexing_slicing -W clippy::unreachable` reports zero sites of either construct in this file (1,065 indexing, 100 slicing and 64 `unreachable!` sites in the rest of the tree, none here). The one panicking shape it did have is fixed below. **Fixed, five.** (1) Both arming refusals rendered the typed `EffectSiteId` they were handed into a `String`, so the one field of `HarnessError` a caller might match on was the one it would have had to parse: `DESIGN.md` §26's "diagnostics are typed" reserves free text for a document's own words, and an arming is a Rust call with no document in it, which is exactly why the registry's own errors, quoting a deserialized entry, correctly keep theirs as text. Both variants now carry `site: EffectSiteId`; the rendered message is unchanged because `{site}` was already going through `EffectSiteId`'s `Display`, and `arm` gained the `# Errors` section §13 asks of a changed public item. These two fields are the whole public API change, and no call site outside the file needed an edit -- the lib and bin test targets compile unchanged. (2) `arm`'s doc claimed a suite "cannot quietly arm a fault that no funnel will ever consult" and refuses two of the three ways to arm one: a point whose `platform()` is not this host's is armed successfully. The code is right and the sentence was wider than it -- `every_family_of_the_harness_bundle_records_into_the_same_harness` in `src/engine/topology/seams.rs` arms `Process.Spawn`'s Windows-only `AmbientJobJoined` under no `cfg` and asserts the `Injection::Error` it returns, on every host, so a harness that refused the point would make a platform's contract untestable off that platform -- and the doc now says what is refused and that `check_bijection` is where `SubEffectPoint::platform` is read against a host. (3) The open fast sequence was an `Option<usize>` index assigned `Some(self.fast.len() - 1)` after a push and read back through `get_mut`: an invariant kept by hand and defended on every hook, written with the file's only subtraction and only index. It is now `recording: bool` beside `fast.last_mut()` behind one private `open_sequence()`, with the first-execution-order dedupe moved into `FastSequence::touch`; `hook` also records the phase it was handed instead of rebuilding `HookPhase::Point` from its own fields, a second spelling of the coordinate the injection had just been decided by. No behaviour and no API change. (4) `record` saturates each count and `executions()` summed them with `sum()`, which is `+`, under `overflow-checks` in every profile -- the accessor reporting a run's coverage was the one place here that could terminate the run, on a total whose parts cannot. It is a `fold` with `saturating_add` now (§5, the arithmetic is chosen). **No test witnesses this one**: `count` reaches `u32::MAX` only through `u32::MAX` hooks of one coordinate and nothing outside the harness constructs an `Observation` it reads, so there is no state a test can put a `HookHarness` in to see it, and it is recorded rather than claimed as swept over. (5) Three public answers had no test that dies when they change, each measured by mutating it and running the whole `topology::effects::` filter: `touched`'s `reached` arm (every existing test drives its sites through both hook phases, so a site only *reached* was never asked about -- dropping the arm leaves all 60 pre-existing tests green), `HookPhase`'s `Display` (the spelling `BijectionFailure::Unobserved` quotes a phase by -- swapping "before" and "after" leaves all 60 green), and the module doc's claim that the parent re-exports every item here, which is the sentence the `export.rs` sweep found false in its own file (`SWEEP-EXPORT-001`) and which is true here and was unchecked. **Tests added, six, all inside this file** (the parent's `tests.rs` is queue row 27 and is being swept in parallel): `an_arming_refusal_names_the_site_it_refused_by_type` (witnesses: the field reverted to `String` -- lib test target fails to build, E0308 twice; `UnsupportedMode`'s message reworded -- test fails), `the_open_sequence_is_the_one_begun_last_and_stops_at_the_end` (witnesses: `end_fast_sequence` made a no-op; `open_sequence` answering `first_mut()` -- both also fail four tests of the parent's `tests.rs`), `a_reused_sequence_name_is_a_second_sequence_and_not_a_return_to_the_first` (witness: `begin_fast_sequence` reusing an existing sequence of that name -- measured, the only failing test in `topology::effects::`), `a_site_whose_funnel_only_walked_past_a_point_was_still_touched` (witness: `touched` drops its `reached` arm -- the only failing test), `a_hook_phase_renders_as_the_name_a_failure_quotes_it_by` (witnesses: the `Before`/`After` strings swapped -- the only failing test; the mode spelled `error_return` -- also fails `a_point_and_a_mode_are_one_coverage_coordinate_and_not_two_axes`), and `every_item_of_this_module_is_nameable_through_the_parent_re_export` (witness: `Observation` dropped from the parent's `pub use self::harness` -- lib test target fails to build, E0432 and E0425 twice; the six names are constructed and driven through the parent's paths, not merely imported). Every mutation above was applied to the source, run, seen to fail, and reverted; the scoped filter is `topology::effects::`, 66 tests, green at the head this row cites. **Deferred, one:** `SWEEP-HARNESS-001` (P3, `findings/`), the mode spelling `Kill => "kill", ErrorReturn => "error-return"` written out once here in `HookPhase`'s `Display` and again in `EntryPhase`'s in `registry.rs`, two hand-maintained copies of one spelling that a reader compares across two messages; the repair is a `Display` for `InjectionMode` in `vocab.rs` (row 26) with both call sites, which is past this file's bound. **Three shapes measured and left alone.** A reused sequence name records a second sequence rather than returning to the first: merging them would let a re-begun name inherit the first's observation and hide an empty trace from `EmptyFastSequence`, which is the check `DESIGN.md` §26 puts on a trace the harness saw nothing in, so the name is documented as a label rather than an identity and `fast_sequence`'s first-match answer is stated and pinned. `Observation`'s public fields with `Deserialize` admit a wire `count: 0`, and nothing can carry one into a harness: `HookHarness` derives no serde and the type is a record `coverage()` hands out. `begin_fast_sequence` takes `&str` and owns inside, which §5's "take owned values when data is stored" reads against until it is read beside `EffectSiteId::from_name` and `UnknownSite`, the same shape at the same layer; changing it would edit six files outside this one to no measured end. |

## Second pass owed

The first five files were swept under a narrower brief than the one now in force. That brief
asked for §6 and §7 conformance; from 2026-09-04 a sweep also asks for correctness defects, for
a better implementation where the current shape is clumsier than it needs to be, and for tests
that would not survive the obvious mutation.

These five are not unswept, and their frontier passes did find real defects — the time-of-check
race in `containment.rs`, the null id accepted on the new side of a compare-and-swap in
`object.rs`, a registration bound to a checkout Git does not read from it in `parsers.rs`. What
those passes did not ask for is the second limb: the shape improvement nobody raises because the
code is already correct.

§7's panic surface post-dates every row in the swept table, not just the first five: the rule was
tightened on 2026-09-04 and the last of these landed the same day under the brief as it then read.
What is owed on the eight production files is the indexing half — each `v[i]` and `&v[a..b]`
dispositioned, or replaced by `get`, `first`, `last`, `split_at_checked` or a pattern. That is a
cheap re-read rather than a session, and the pull request that lands the lints can settle those
eight from its own output, since the run above already names every site.

The ninth row, `src/workspace_manager/tests.rs`, owes something different and smaller than an
earlier draft of this section claimed. Its indexing sites fall under the test allowance the section
above takes, so nothing is owed there. Its one `unreachable!`, at line 7390, is denied in tests like
any other and needs an `#[expect(clippy::unreachable, reason = "…")]` or the proof that removes it —
one site, inherited from before the sweep rather than introduced by it.

They are listed here so the gap is recorded rather than forgotten. The queue comes first: a file
with no pass at all earns attention before a file that has had several. Decide whether to spend
sessions on these once the queue is empty.

| File | Swept at | Under | What a second pass is looking for |
|---|---|---|---|
| `src/workspace_manager/containment.rs` | `2dd1350` | seven passes | Shape of the walk and the per-primitive re-check; whether the refusal vocabulary is larger than it needs to be |
| `src/workspace_manager/hooks.rs` | `f58747a` | three passes | The `Arc<Mutex<HookHarness>>` was kept with its lifecycle stated; whether the protocol needs shared ownership at all |
| `src/workspace_manager/naming.rs` | `5a0ae59` | four passes | The hand-written wire reader against the derived one; whether `Slot` and `SlotId` earn being two types |
| `src/workspace_manager/object.rs` | `af382fa` | two passes | Whether the two refusal types and the parent's flattening are one concept too many |
| `src/workspace_manager/parsers.rs` | `196f641` | one pass | The two NUL-delimited grammars are parsed separately; whether one reader serves both |
| `src/topology/effects/registry.rs` | `4210613d` | 2026-09-06 | Row 23, `#106 effects` family, 727 lines at assignment: the registry format — what an entry is about, what evidence it carries, and `validate_entry`, the whole validity rule as one function. Swept at `12a5cdc6`, `753b73a3` and `4210613d`. §6: grep-verified no `Rc`, `Arc`, `Mutex` or `RwLock` in the file, and exactly three `.clone()`, all in one block of `validate_entry`, each the entry's own words moved into the error value that outlives the borrow and quotes the document back to a reader — the intended semantics, not a borrow-checker workaround, stated at the site; the authority's side of each of those comparisons is moved or `&'static` and is never cloned. §7: one `?`, `validate_entry(&entry)?` in `insert`, kept as same-type propagation with nothing to add and now stated at the site; `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports zero sites of either construct in this file; no `.unwrap()` and no `panic!`, and the four `.expect(` are the new test module's own setup, each naming the failed premise. §13: `insert` and `validate_entry` gained the `# Errors` sections neither had. **Fixed:** three of `MislabelledEntry`'s four construction sites were not about a mislabelling — the phase-versus-evidence-kind cross-check built `found: ExecutionObserved, required: ExecutionObserved`, a tautology naming no defect, and the label-versus-evidence check reported "its phase requires ExecutionObserved" of an entry whose label the line above had just confirmed is exactly what its phase requires — they are now `NoExecutionClaimsHook`, `HookClaimsNoExecution` and `LabelContradictsEvidence`, with the accept/refuse decision unchanged and still complete over the phase-evidence-label product; the existence of a coordinate is now decided before its semantics are quoted, because `EffectSiteId::semantics` answers for a `Point` from the point's own table and for a `Residue` without reading the class, so an entry naming a point the site does not expose could be refused with `WrongResidueRows` quoting "this site's `{phase}` leaves ..." for a phase the site has no such phase of; and `ClassHistogram::total` now sums into `u64` instead of saturating in `u32`, which is the shape `check_bijection` was repaired for in `aa52b417` and whose comment names this method as the reason it reads the three fields itself, forcing one mechanical edit at the only caller in the tree (`grep -rn '\.total()' src/`), `src/workspace_manager/tests.rs`. **Tests:** `MislabelledEntry` had no test at any of its four sites, so all four refusals were unwitnessed; the file now carries its own `#[cfg(test)] mod tests` — in this file rather than the family suite, which a sibling session is sweeping, following `export.rs`'s precedent — with eight tests, each named in its commit with the mutation it was witnessed against and each mutation applied, run and reverted. **Deferred**, one file each in `findings/`: `SWEEP-EFFECTS-REGISTRY-DUPLICATE-SYNTHETIC-RECORD` (a `RecoveryProven` synthetic list may name one element twice against its own "one record per element" sentence; `check_bijection` holds every record to its three predicates, so no false claim is admitted and the finding says to settle which of the sentence and the bag is the contract before tightening anything) and `SWEEP-EFFECTS-REGISTRY-DESIGN-TEXT-CHANNEL` (DESIGN.md §26 says a refusal carries "a site or phase name as text" that the entry wrote, which closed enums and serde make unreachable — this is why `RegistryError`'s `site` and `phase` fields were left as `String` rather than retyped, since retyping them is a design change and belongs with the sentence). |
| `src/topology/effects/residue_authority.rs` | `d7967eb9` | 2026-09-06 | Row 24, `#106 effects` family, 1,086 lines at assignment: the residue and recovery vocabulary and the per-`(site, phase)` and per-point authority. **§6:** grep-verified no `Rc`, `Arc`, `Mutex`, `RwLock` or `.clone()` anywhere in the file, before the sweep or after it; the file is `const fn` tables over `Copy` enums, and its only allocations are `SubEffectPoint::residue_rows`'s `vec![...]` and `Vec::new()`, which construct the `Vec` that `PhaseSemantics.rows` owns rather than copying anything. Nothing to disposition. **§7:** zero `?` operators (no `?;`, `?)`, `?.` or line-final `?` in the file), zero `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!` and `unreachable!`, and zero indexing or slicing: `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports 1,227 warnings across the crate and not one of them names this file. So §7 is discharged by measurement rather than by repair, and the test module added below keeps it that way -- its walk reads `walked.last()`, not an index. **§5:** every match in the file is over a closed enum with no wildcard arm, which is the file's own exhaustiveness argument, and the three variants added below keep it. **Fixed, two.** (1) `SubEffectPoint::residue_artifact` was blind to the injection mode while `resume_action` was not, and one point needs it: `agent::proc::ambient::join_ambient_job_with` applies the error-return hook *before* `join()` and the kill hook *after* it, so a kill at `Spawn.AmbientJobJoined` leaves the ambient handle to close and the kernel to terminate the stub or tree while an `Err` refuses with "No process was spawned" and terminates nothing. The entry at `(Process.Spawn, AmbientJobJoined, ErrorReturn)` was therefore required to carry two false sentences to be accepted -- `NoHostProcess`'s kill mechanism, and `RefuseResumably`'s "the next open repeats the barrier", which is the event log's stable-prefix barrier and belongs to `Event.OpenLog`'s points. The artifact now reads the mode and answers a new `NoProcessSpawned`, and the recovery a new `RefuseUnspawned` in the refusal's own words; every other point still answers one artifact for both modes, asserted over the whole `SubEffectPoint::ALL`. The Windows self-test fixture measures 18 distinct details and 9 distinct resume actions where it measured 17 and 8, on the same 42 entries -- the collapse this repair ends -- and the Unix shape is unchanged because the point is Windows-only. (2) `Lock.ProbeCleanupExclusive` claimed `AfterEffect::Referenced`, so the authority answered rows `[R17]`, "the artifact is present and referenced by the row row() names" and "resume adopting the completed effect" for a hold the code deliberately gives back: `rundir`'s `cleanup::take` takes an exclusive non-blocking `flock` and then unlocks before returning, saying why in its own comment, and returns a lease that is a path and no hold, while R17 is "the coordinator's own lock holds (OS lock state only)". That is the shape the generic `After` arm returning the site row was removed for, surviving at the one site the per-group tables inherited it. A sixth `AfterEffect`, `MomentaryHold`, with `HoldReleased` and `RepeatProbe`, now answers it; `AcquireRun` and `AcquireWorktree` keep `Referenced` because each retains its `File` for the guard's lifetime, which is the discriminator and is asserted, and the probe is still not the read-only claim (`is_read_only() == false`, it creates the file it probes). **Added:** `ResidueElement::wire_name` and a `Display` forwarding to it, the vocabulary half of `SWEEP-WORKTREE-008`, which named this file as its guard; the adoptions at the two call sites are outside the bound and stay open. **Tests, five, all in a new `#[cfg(test)] mod tests` at the end of the file** (the family's `tests.rs` is queue row 27 and a sibling session's subject). Each was witnessed by applying its mutation, running it, seeing it fail and reverting: the mode split dies under either half of the pre-fix table; the probe dies under folding it back into `Referenced` and under giving the parent's new arm the site row; the `Display` pin dies under one arm spelling the `Debug` name and under a `Display` that writes a constant. The fifth is the one that found a gap rather than a defect: the five `pub const ALL` lists are the domain of five censuses and nothing held any of them to its enum -- **measured**, with `Self::ReaperHeldGroup` deleted from `ResidueArtifact::ALL` the whole `topology::effects` filter passed, 64 of 65, because the distinct-count assertion compares 19 with 19 and the exhaustive `match` beside it is over values taken *from* `ALL`. Each list is now derived by a bounded walk of an exhaustive `successor` match, the idiom `src/ir.rs` already uses, so a new variant does not compile until it is placed and the comparison then fails until `ALL` carries it. **Forced call sites, mechanical and minimal:** `src/topology/effects.rs` (row 28) passes the mode and gains the `MomentaryHold` arm; `src/topology/effects/tests.rs` (row 27) takes the mode at two calls, the new variants in three exhaustive matches, and the new values in its `POINT_ORACLE`, `AFTER_EFFECT_ORACLE`, Windows `FIXTURE_SHAPE` and after-effect coverage list. Neither file is otherwise touched and both stay queued. **No `design/` change:** §26 requires that an entry's expected residue and resume action be the site's own semantics rather than the entry's opinion of them, which both repairs make more nearly true and neither reworded; no design section names a per-site residue or a per-point artifact. No `docs/internals` notes file exists for this module, so §13's contract stays in the rustdoc at the site. **Deferred, two, both P3 and both outside the bound:** `SWEEP-RESIDUE-AUTHORITY-001`, `bijection.rs`'s three Debug-spelled failure texts, the same defect as `SWEEP-WORKTREE-008` in a second file; and `SWEEP-RESIDUE-AUTHORITY-002`, the `cleanup.lock` file that `ProbeCleanupExclusive` creates and no row of that site accounts for, which `sites.rs` (row 25) answers. **Run:** `cargo fmt`, `cargo clippy --all-targets --all-features -- -D warnings` clean, and the scoped `topology::effects` (65 passed), `effects::` (124 passed) and `workspace_manager::` (186 passed, 3 ignored) filters, each through `upstroke-build` with the sources touched inside the wrapper's slot lock and the run refused unless its log carried a `Compiling upstroke` line naming this worktree. |
| `src/topology/effects/sites.rs` | `b2b6f578` | 2026-09-06 | Row 25, `#106 effects` family, 1,617 lines at assignment: eleven site enums, eleven `ALL` slices and ninety-nine `pub const fn`, and no other item. **§6 found nothing to bind and nothing was added:** a grep for `Rc<`, `Arc<`, `Mutex<`, `RwLock<` and `.clone()` returns zero, before and after. **§7:** zero `?` operators -- the only two `?` a text search finds are the `$(,)?` repetition operators of the two test macros -- and zero `.unwrap()`, `.expect(`, `panic!`, `todo!`, `unimplemented!`, `unreachable!` and `dbg!`. The panic surface tightened on 2026-09-04 finds nothing either: `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports **zero** sites in this file on a run that reported 1,229 elsewhere (1,065 indexing, 100 slicing, 64 `unreachable!`), so the zero is a measurement and not a vacuous run. **§5:** not one wildcard arm over any of the eleven closed enums (zero `_ =>`), which is what the parent's compile-time `identity` contract rests on and the reason the long same-answer arm lists are kept rather than collapsed; measured, not asserted. **Fixed, two, both docs-contract on `pub` items and neither caught by any gate** (`cargo doc --no-deps --document-private-items` reports no warning naming this file, because both referents were prose rather than intra-doc links). `RunDirSite::PublishCommitRecord` cited `decisions/2026-08-30-test-scratch-tree-ownership.md`, retired with the `decisions/` directory on 2026-09-03; it now cites `DESIGN.md` §15 and `CODING_STANDARDS.md` §8, the destination `DESIGN.md`'s own record index names. That citation was one of the `.rs` occurrences of the standing P2 `W2-RETIRED-DECISIONS-PATHS-CITED-AND-MISSING`, whose census of 26 `.rs` occurrences at `3af9696` measures **3** at this head; the two survivors are `src/rundir/scratch_tree.rs:31` and `src/engine/topology/create.rs:97`, and they get no new finding file because that P2 is the class and already names this family. `ReportSite` sent the reader to a "`topology::effects` worker report" that no tracked file is -- the cross-reference the open P2 `a-gates-stated-domain-exceeds-what-it-counts` records at its item (6) as prose the #106 round-3 sweep's counted scope missed -- and in the same breath claimed `report.json` is "reached through two funnels" and that "both are implemented". False at this head and measured so: `Report.Write` is the single entry of `SITES_WITHOUT_A_FUNNEL`, `grep -c ReportSite src/util.rs` is 0, and `src/rundir.rs:895` funnels `RunDir.WriteReport`. The comment now says one of the two is funnelled and names the census as the live answer. **No data moved.** Every row, adjacency, fault row, scope, read-only flag, sub-effect point, residue class and residue element was re-derived from the source and cross-checked against the two independent tables the tree holds -- the hand-maintained `effects/residue-classes.json` (nine sites, same rows, same element lists in the same order) and `tests.rs`'s literal attribute table -- and agreed everywhere, so `effect_sites.json` is byte-identical. **Tests added, two, in the file** after every production item (§12), following `export.rs`'s in-tree precedent rather than the sibling `tests.rs` a parallel session is sweeping. `name()` answers its own variant's spelling, with `stringify!` as the oracle and exhaustive matches forcing the lists; witnessed by exchanging the names of `RunDir.RemovePrivateHusk` and `RunDir.RemovePublicHusk`, one of eight groups of sites whose whole attribute profile agrees, which leaves the other sixty-one tests of `topology::effects` green and is caught elsewhere only by the checked-in `effect_sites.json` comparison. `residue_elements()` is non-empty exactly where `residue_classes()` is, with the domain pinned at 70 sites and the registering count at 9 so the biconditional cannot be satisfied by emptying both sides; witnessed by giving `Lock.AcquireRun` an element and no class, which nothing else in the tree catches. A third test, asserting the module header's re-export claim, was written, measured unable to fail -- a descendant module sees a parent's private import through the full path, so demoting the parent's `pub use` breaks six call sites elsewhere and not it -- and removed. **Deferred, one:** `SWEEP-SITES-READ-ONLY-ORDER` (P3), three of the four read-only sites answering `observable_orders() == [EffectBeforeEvent]`, "the effect is durable", while their own `after_effect()` is `NoEffect`; either repair is in `observable_orders()` in the parent (row 28) and in §26, outside this bound. **Not claimed:** nothing here checks the row and fault-matrix assignments against the packet, which is not in this repository. What is checked is their internal consistency against the two tables above, and the queue row is swept on that basis. The commit column is the cleanup commit; one later `style` commit on the same branch normalised three dashes in the new test comments and changed nothing else. |
| `src/topology/effects/vocab.rs` | `5bb99697` | 2026-09-06 | Row 26, `#106 effects` family; 796 lines at assignment, eleven `pub enum`s with their accessors and `Display` impls and nothing else. §6: no shared ownership, lock or clone, before or after -- grep-verified no `Rc`, `Arc`, `Mutex`, `RwLock` or `.clone()` anywhere in the file; every accessor takes `self` by `Copy` and answers a `&'static` slice or `&'static str`, so there is no owner to decide. §7: no `?`, no `.unwrap(`, no `.expect(`, no `panic!`, no `todo!`, no `unreachable!`, no indexing and no slicing in the production half, so neither rule had a site to disposition; the test module added below has none either, and its `.expect(` calls are test setup naming the failed premise, which §7 allows. **Fixed:** five rustdoc contracts untrue of the code beside them. `ResourceRow` said "only the external-physical and process-local-OS rows appear" while R20 is external-physical and deliberately absent (`operator_owned`, "never created or pruned by a run", quoted at the refusal in `src/runner/container.rs`), so both exclusion rules are now stated and the doc no longer claims the fifteen are every such row. `SubEffectPoint::modes` opened "Kill is universal" four lines above its own match giving `WrittenFull` error-return only. `SubEffectPoint::platform` was summarised as "the host this point exists on" and answers a `Platform`, which its own type doc says is not a host. `Host::ALL` claimed every self-test asserting a platform-dependent shape is quantified over it, measured false at `the_bijection_fails_on_every_missing_link`, which filters points by `required_on(Host::current())`, and at eleven further sites that drive `check_bijection` at `Host::current()`. `FunnelGroup::module` described its answer as where a group's effects are confined, which for the Answer group it is not -- the bodies are in `src/rundir.rs` behind delegations, a divergence already checked in as `effects/funnel-modules.json` and previously recorded only in that file's own note. Also removed `deny_unknown_fields` from `Adjacent`: its variants are two newtypes and a unit and have no field to deny on, the externally tagged representation is what refuses a second or undeclared key, and the removal was measured by seeing all five refusals still hold. **Tests:** the file had none of its own. A mutation of `FunnelGroup::name`, `ResourceRow::name`, `SubEffectPoint::name` or `Host::name` killed nothing in the topology suite -- three of them are read only by their own `Display`, and the sibling suite's apparent cover for the fourth is `site.name().starts_with(group().name())`, both sides of one expression, `EffectSiteId::name` being built from `group().name()`. Nineteen tests added in-file after every production item: per-variant spelling tables for the three untested `name()`s, `Display`-against-accessor for all six impls, the two `ResourceRow` exclusion rules as the numbers they exclude, every row's enforcement domain with both domains populated, `SiteScope::is_claimed`, `InjectionMode::ALL`, `Adjacent::event`, `Platform::required_on` crossed over both hosts and all three platforms, `Host::other` as a fixed pair rather than the reflexive `current().other()`, the `modes` contract with its one exception, the `Adjacent` wire form as three round trips and five refusals, the serde form of every variant of the five renamed enums stated as the rule that produces it with its awkward outputs spelled out, and a source census over this file that blanks comments and string, byte-string, raw-string and character literals -- preserving byte length and reading `'static` as the lifetime it is -- then counts each `pub enum`'s variants against that enum's `ALL`, because a variant added to an enum and not to its list was silent and `SubEffectPoint::ALL` is the bijection's coverage obligation. The census asserts its domain as the eleven enums by name and order and carries three positive controls: a fixture whose quoted and commented variants are not counted, the same fixture with one variant injected moving the count, and a fixture with a `'static` lifetime and a character literal a naive scanner loses its place on. Twelve mutations were applied to the source, each seen failing on a recompiled binary and reverted, against an unmutated control on a recompiled binary; they are listed in `5bb99697`'s body. **Deferred:** `SWEEP-VOCAB-001`, the generated inventory spelling `run_dir`, `r9` and `t_cand_obj` where the same record's `site` field says `RunDir.` and the ledger and matrix say `R9` and `T-CAND-OBJ` -- repairing it regenerates `effects/residue-classes.json`, outside one file's bound, and `the_wire_form_is_not_the_ledger_id_for_the_four_types_that_have_one` is written to fail when it is taken up; `SWEEP-VOCAB-002`, two assertions in `src/topology/effects/tests.rs` that cannot fail; `SWEEP-VOCAB-003`, a test name there claiming a wider row domain than it iterates. Both of those are row 27's. **Measured and not defects:** `Adjacent` alone lacks `PartialOrd`, `Ord` and `Hash`, and nothing in the crate orders or hashes one, so the derives would widen a published API with no caller; `EnforcementDomain` and `Platform` carry no `name()` or `Display` and neither is ever printed; `SubEffectPoint::supports` is not `const` because `<[T]>::contains` is not, and both const forms are worse -- an indexing loop is a §7 site, and a second `matches!` table is a second source of truth for the `modes` table it would duplicate. |
| `src/topology/effects/tests.rs` | `254bd183` | 2026-09-06 | Row 27, `#106 effects` family: the family's whole test suite, 6,469 lines and 57 `#[test]` items at the base `ee5dc81f`, swept under amendment 7 with the tests as the whole job rather than a coda. §6: grep-verified no `Rc`, `Arc`, `Mutex` or `RwLock` anywhere in the file, before or after; the 37 `.clone()` sites are two shapes and neither is shared ownership -- an owned copy of a sound fixture entry that the next line doctors while the original stays as the comparison (10 of them `sound.clone()`), and an owned document, field or record moved into a constructed value or a distinct-count measurement. §7: no `?` and no `.unwrap()`; the 69 `.expect(` and 9 `panic!` are the test carve-outs and each names a premise. The panic surface was the whole of what §7 found here, and it is measured with the lints rather than by grep: `cargo clippy --all-targets --all-features -- -W clippy::indexing_slicing -W clippy::unreachable` reported 18 sites in this file at the base (14 indexing, 4 `unreachable!`, the last of which take no allowance in tests) and reports 0 in it and 0 anywhere under `src/topology/effects/` at the head, with 1,201 still reported tree-wide as that run's own control. All 18 are gone and not one is under an `#[expect]`: `all.get(position)` compared with `Some(site)`, `first()`/`first_mut()` for the four `[0]` reads, `iter_mut().find(..)` in place of the four indexes derived from a `position()` search, one `get(position).cloned()`, `get(..).copied().expect(..)` for the two `BTreeMap` index sites whose totality is asserted ten lines above, `sampling_for` answering an `Option` its one caller `.expect`s, and three `assert!(matches!(..))` naming the premise where an `unreachable!` arm stood. Correctness and shape (limbs 1 and 2): no defect found in the suite's own logic; what limb 4 found was ten places where a claim was not pinned, all ten fixed here, each with the mutation it is witnessed against applied to the real tree, run, seen to fail and reverted -- the commit bodies of `9eaabb0d`, `02435b65` and `254bd183` name every one. All ten were then re-run under the owner's 2026-09-06 rule for this box, the `upstroke-build` slot pool being shared with sixteen sibling sweep worktrees and having twice served a binary built from another tree: each run touched the suite first and was confirmed to carry `Compiling upstroke v0.1.0 (/srv/worktrees/topo-effects-tests)` before its failure was counted, ten for ten. One earlier run in this session did serve a foreign binary, and it was caught the same way -- it listed a `topology::effects::vocab::tests` module this tree does not contain. Four assertions could not fail or could not discriminate: a dotted name asserted to start with `group().name()`, which `name()` is built from and which is therefore a tautology that survives renaming a group to `worktree`; the two directions of `observable_orders` compared through a test-local restatement of the rule that no production mutation can reach; `HookPhase::PHASES` used as the domain of two sweeps and pinned nowhere, so an empty slice made both vacuous; and the thirty-cell evidence-and-label grid asserting only `is_ok()`, which one refusal for every wrong cell satisfies (the refusal's variant is now derived per cell in the order `validate_entry` applies its clauses). Six contracts of the family had no test at all: `EffectSiteId::claimed()` (a `claimed()` returning `all()` unfiltered satisfies every other use in the suite), `HookHarness::touched`'s reached-but-unexecuted arm, the fast-sequence marker that any hook sets including an unarmed point together with `HookHarness::fast_sequence(name)`, the literal spellings of `ResourceRow::name`, `Host::name` and `ResidueClass::name`, `ClassHistogram::total`, and the third arm of the bijection's resumes-as-before relation. One test was deleted, `the_fixture_shape_table_is_measured_and_not_asserted_into_being`, which could fail only when the assertion it duplicated already had; its better message moved to the survivor. 57 `#[test]` items at the base, 62 at the head. Deferred: three P3s against sibling files this session's bound forbids editing, each a file in `findings/` naming the file that should take it -- `SWEEP-EFFECTS-REGISTRY-001`, a `MislabelledEntry` whose `found` and `required` are filled with the same value at two reached refusals, which is why this sweep pins the grid's refusal variant and deliberately not its fields; `SWEEP-EFFECTS-REGISTRY-002`, `ClassHistogram::total` saturating without saying so, which is why `check_evidence` sums the three fields itself in `u64`; and `SWEEP-EFFECTS-HARNESS-001`, `begin_fast_sequence`'s opening `end_fast_sequence()` call, a provable no-op that no mutation can witness. Scoped runs only, as the brief directs: `cargo test --all-features topology::effects::` (65 passed, 0 failed, Linux) and `cargo clippy --all-targets --all-features -- -D warnings` (clean). |
| `src/topology/effects.rs` | `828e711e` | 2026-09-06 | Row 28, `#106 effects` family: the parent the split left, 723 lines at assignment. Every measurement below was taken on a target directory no sibling worktree shares, after the shared `upstroke-build` slot pool was observed serving this worktree another tree's test binary (all eight slots hold the same `deps/upstroke-1f05c71869a3a005` path, so `-C metadata` does not vary with the worktree); the first three readings taken through the shared pool were discarded and retaken, and every mutation below was applied, recompiled (the `Compiling upstroke` line checked in the same invocation) and seen failing, against an unmutated control that recompiled and passed. §6: grep-verified none, before or after — no `Rc`, `Arc`, `Mutex`, `RwLock` or `.clone()` anywhere in the file; the one `.to_owned()` builds the `String` that `UnknownSite` owns and returns, which is the error's own data and not a borrow-checker clone. §7: no `?` operator at all (the one `?` a grep finds is the `$(,)?` in `const_identity_walk!`'s matcher), and no `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!` or `unreachable!` in production code; the fifteen `assert!`s all sit inside `const _: () = { ... }` blocks, where a violation is a compile error and not a run-time panic, and the added test module's two `.expect_err(` calls are the tests' own premise failures. One panicking index, `$group::ALL[index]` under a `while index < len` in `const_identity_walk!`, is now a `while let [first, tail @ ..]` subslice pattern that needs no `#[expect]` because a matched pattern cannot be out of bounds: measured with `cargo clippy --all-targets --all-features --message-format short -- -W clippy::indexing_slicing -W clippy::unreachable`, one site in this file at the reviewed head (`src/topology/effects.rs:605:52`), zero after, against 1053 sites elsewhere in the crate as the positive control. **Fixed**, four. (1) The dotted name had two authorities: `EffectSiteId::name()` and the `Display` impl were separate `format!` calls of one shape and only `name()` was under test — rewriting `Display` to `"{}:{}"` at the reviewed head passes all 60 scoped `topology::effects` tests, while `BijectionFailure`'s twenty messages all interpolate their site through `Display` and would then name it in a spelling `from_name` and the wire format refuse. The shape is written once in `Display` now and `name()` returns `self.to_string()`. (2) `from_name` rebuilt up to seventy dotted names through `name()` to compare whole strings, which made `a_site_name_no_group_declares_is_refused`'s round-trip assertion compare `name()` with itself, so it could not fail for a change to the name's shape; it splits at the separator and compares the halves against the static group and variant names, and its `# Errors` contract is written. (3) `semantics(EntryPhase::Residue { .. })` discarded the closed-domain `ResidueClass` it was handed, so a second class would inherit `ObjectInternal`'s rows, artifact and resume action in silence — the §5 wildcard rule, and the shape this very function exists to deny a registry entry one level up; the class is matched, and the arm no longer reads its artifact back off `rows.len()` but asks once whether the site's own row is a second administrative row. No value changed. With one variant in the enum the guard on the discarded field is the compiler and not a test, and this row claims no more. (4) The compile-time contract covered six of the type's fifteen `pub const fn`: the walk left `group`, `variant`, `module`, `observable_orders`, `is_read_only`, `sub_effects`, `residue_classes`, `residue_elements` and `skipped_on_fast_path` in exactly the state its own comment calls the defect — measured, demoting `pub const fn observable_orders` to `pub fn` builds the crate clean at the reviewed head and is `error[E0015]` after. The walk now names all fifteen. Also fixed, §13: `INVENTORY_SIZE`'s doc cited `the_generated_inventory_describes_every_site_and_invents_none` as asserting it against `EffectSiteId::all()`, which that test does not mention (the property was pinned transitively, by `the_residue_and_recovery_authority_is_exhaustive_and_says_what_the_packet_says`); `exposes` did not say that the point's platform is deliberately not one of its questions, which is load-bearing because a document written on one host is validated on the other; and `semantics` did not say it is total over `(site, phase)` including the pairs the site does not have, which `validate_entry` refuses separately and deliberately. Four tests added, in a new in-file `#[cfg(test)] mod parent_tests` rather than in `src/topology/effects/tests.rs`, which is queue row 27 and under concurrent sweep at this base — the placement `src/topology/effects/export.rs` (`990b838e`) set the same day. `the_dotted_name_has_one_authority_and_it_is_display` (witnessed against `Display` writing `"{}:{}"`), `a_name_crossing_two_real_sites_is_refused` (against dropping the `site.variant() == variant` half of `from_name`'s predicate, which makes `Object.PublishCommitRecord` resolve to the first Object site), `the_const_inventory_count_is_the_length_of_the_inventory` (against dropping `ContainerSite` from `all()`; failed `left: 62, right: 70`) and `a_residue_class_names_the_administrative_row_only_where_there_is_one`, which pins the whole `PhaseSemantics` triple for both shapes where the shared test file pins the rows of both and neither artifact (against swapping the one-row arm's artifact). Deferred, filed: `observable_orders` returns a `&'static [ObservableOrder]` of length zero or one where §5 asks for `Option<ObservableOrder>` — `check_orders`'s own comment records that the wide type already produced an `[0]` index in one loop beside an iteration in another — not fixed because the narrowing forces `validate_entry` and `check_orders` in two files under concurrent sweep and `EffectSiteExport::observable_orders` is a `Vec` in the published `effect_sites.json`, making it a wire-format decision with `design/25` and `design/26` sentences to move, all past a sweep's own-file bound (P3, `SWEEP-EFFECTS-PARENT-001`). Read in full against `design/26`'s verification-contract section, `standards/05`-`08`, `12` and `13`, and against the callees the contracts rest on (`vocab.rs`, `residue_authority.rs`, `registry.rs`'s `validate_entry`, `bijection.rs`'s `check_orders`, `export.rs`); no defect was found in those files that this row withholds. Verified by `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and the scoped `cargo test --all-features topology::effects` (64 passed, 0 failed; 60 at the reviewed head). The eight gates were not run here; the owner runs them on the assembled branch. |
| `src/topology/fold/check_end.rs` | `8b866a2d` | 2026-09-06 | Row 32, `#108 fold` family: the question checks, the budget stop and the end of a run, 248 lines at assignment. **§6:** nothing to disposition -- grep-verified zero `Rc`, `Arc`, `Mutex`, `RwLock` and zero `.clone()` in the file; the only copies it makes are the `format!` and `to_owned` strings a refusal's own message needs. **§7:** six `?` sites, each same-type propagation of an error already naming the record it refused and the kind that refused it, so none has anything to add: `self.entry` and `self.task` in `check_question_raised` and `self.entry` in `check_budget_exceeded` (`UnknownKey` carrying kind and key), `check_question_can_park_lineage` (a fully formed `InconsistentRecord`), the `map_err` on `QuestionAnswered4::self_consistency` (mapped into `InconsistentRecord` at the site) and the `ok_or_else` on the open-question lookup, which is the one `?` on an `Option` and does not turn absence into failure silently -- it names `WrongQuestion` and distinguishes an already-answered id from one this log never asked. No `.unwrap()`, `.expect()`, `panic!`, `todo!` or `unimplemented!`; `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports 1,229 sites across the tree and **none in this file** (the file contains no `[` at all). **Fixed, three.** `check_budget_exceeded` read neither recorded amount, so a record stopping the run for a ceiling its own spend had not reached, or carrying `NaN` or an infinity, was folded into a budget stop; both are refused now as `InconsistentRecord`, on propositions that already exist rather than new ones -- `DESIGN.md` §17 refuses a budget ceiling that is not a positive finite number of dollars at load, and `Ceiling::breach` in `src/engine/topology/select.rs`, the only producer of this event, emits it exactly when the recorded spend has reached the recorded ceiling. §5: `usize::try_from(option_index).unwrap_or(usize::MAX)` was a sentinel that was correct only because `usize::MAX` cannot be below any option count; the conversion and the range test are one refusal now. §13: `DerivedOutcome::FoldError` -- the fold failing to classify its own state, which `TotalityAudit` counts separately from a disagreement -- was reported to the operator as the word `unreachable`, reading as a claim about the recorded outcome; it now says what the arm means, and the file's only occurrence of that token, a string literal, is gone. **Tests.** One arm had no test and is reachable from the fixture already in the tree: an answer choosing an option inside its question's list and past its admission's authorized-binding list, since nothing pairs the two lengths. `an_answer_may_not_take_an_option_its_admission_authorized_no_binding_for` asserts both refusals by their exact detail, the override-less one proving the range check is not what refuses it. Three mutations, each applied to `check_end.rs`, run on a recompiled binary and reverted: the spend-below-ceiling refusal dropped (1 failed), the finiteness refusal dropped (1 failed, since `NaN` compares false against every ceiling and an ordering test alone accepts it), and `authorized.get(chosen)` to `authorized.first()` (3 failed, this file's new test on the exact message swap). Both new tests are in `src/topology/fold/tests.rs`, which this sweep does not otherwise touch and which is row 39. **Measured, not changed.** The halt half of the answer-ingestion guard is epoch-scoped (`halted_epoch == Some(self.epoch)`) while the sibling refusal for the same condition, `check_defer_wait_elapsed`, tests `halted_at.is_some()` and so refuses permanently; a probe at this base measured that after a resume of a halted-but-unfinished run the answer is accepted (`Ok(())`), the parked task moves to `Pending` and the derived outcome stays `Ending(Halted)`, which contradicts `docs/internals/topology/fold/tests.md`'s claim that a halted run never ingests one, and agrees with the same file's next section and with `fold.md`'s note on the field. No design sentence settles which is the contract and the clean form of either fix reaches three files with three live sweep sessions, so it is filed rather than taken. A `question_raised` carries no run-ending guard where an answer and a wait do: left, because a question raised under a halt or a stop cannot admit work -- `ready`, `ready_retry` and `eligible_continuation` all consult `run_is_ending` -- and cannot change the derived outcome, which reads the halt and the stop before it reads open questions. A second `budget_exceeded` in one epoch is accepted and `apply` keeps the first: left, because refusing it is not derivable from a design proposition and would remove a transition `src/topology/census.rs` offers at every explored state, in a file outside this bound. **Deferred, four, each a file in `findings/`:** the epoch-scoped halt guard and the two notes that disagree about it (`SWEEP-CHECKEND-001`, P3 docs-contract, with the escalate-if-reclassified clause); `RunFinished4`'s `merged` and `parked` accepted unread while the record's other two fields are derived and refused, with two candidate derivations for each and two fixtures that would have to be repointed (`SWEEP-CHECKEND-002`); a `HumanBinding` spawn whose question offers more options than its admission authorizes bindings, which belongs in `check_admission` in `src/topology/fold/check_attempt.rs` (`SWEEP-CHECKEND-003`); and `check_question_answered`'s returned `QuestionOrigin`, which the only consumer matches in one arm with the other variant, so no test can pin it and dropping it would strand `OpenQuestion::origin` (`SWEEP-CHECKEND-004`). **Evidence at `8b866a2d`:** `cargo fmt --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; scoped `cargo test --all-features topology::` 634 passed, 0 failed, 10 ignored (all pre-existing subprocess children with reasons). Every run here was made in a private `UPSTROKE_RAMTARGET` pool and its log checked for `Compiling upstroke v0.1.0 (/srv/worktrees/topo-fold-check-end)`, after a shared slot served this worktree another tree's binary and reported a failure in a test this branch does not touch. No independent review pass has run at this head. |
| `src/topology/fold/outcome.rs` | `303a023a` | 2026-09-06 | Row 34, `#108 fold` family: the derived outcome (INV-15, the retired packet's label) and the structural predicates the run-end decision and the selection accessors are both answered from. §6: no shared ownership, lock or clone anywhere in the file, before or after — grep-verified, no `Rc`, `Arc`, `Mutex`, `RwLock` or `.clone()`; the change adds none. §7: no `?` at all, and no `.unwrap()`, `.expect()`, `panic!`, `todo!` or `unimplemented!`; `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` at this head reports 1229 warnings across the crate (1065 indexing, 100 slicing, 64 `unreachable!`) and names this file in none of them, and names the new test file in none either. The module's whole discarded-error surface was three checked-conversion fallbacks, and two of them named a different task than the one being asked about. **Fixed** in `303a023a`: `structurally_admissible` mapped every index through `u32::try_from(index).unwrap_or(u32::MAX)`, so past `u32::MAX` every index aliased onto key `u32::MAX` and an `any` answered about that task instead — the key range is now converted once from the length, narrowing the domain scanned, which is the idiom `engine::topology::select::keys` already uses; `dispatch_lease_check` built the lease owner from a second lookup of a task its only caller had already proved present, `map_or(0, ..)` fabricating generation 0 for an absent task and `unwrap_or(u32::MAX)` aliasing the count, each of them an owner the run can hold a lease under and `LeaseTable::overlaps_another` excluding exactly the owner it is passed — it now takes the `TaskFold` from `ready` and refuses when the count does not fit, which is the true answer; and `blocked_tasks` returned `BTreeSet<usize>`, a set of indices where a dependency is a `TaskKey` and every other set in the fold is keyed by one, so the membership test converted each key back to an index to ask — it now returns `BTreeSet<TaskKey>`, and an index no key can name makes `complete_shape` answer false, the half that cannot invent a completion. No behaviour change on any reachable input: the aliasing needs a task vector longer than `u32::MAX`. **Not a defect, with the measurement** (amendment 1): `pipeline_reservable`'s `usize::try_from(max_parallel).unwrap_or(usize::MAX)` saturates a limit upwards, and `usize::MAX` is the narrowest true bound on a limit no reachable held count can meet, so it admits nothing the real limit would refuse; and `backoff_pending` reading `deferred_tasks` rather than `TaskState::Deferred` is DESIGN §26 in as many words, "Execution backoff remains pending beneath a question", measured reachable at this head (a task deferred by a settlement then parked by a `question_raised`, which `check_question_raised` admits, answers `backoff_pending` true while its state says `AwaitingInput`, and the run derives `NotEnding`) — both reasons are now in the notes file (`1e29ed12`), neither needed a `design/` change. Tests, **added** in `ea311812`: three of the accessors carried clauses no test in the tree could tell from their absence, because both callers short-circuit first — `derived_outcome` answers `Halted` or `BudgetExceeded` before it asks `structurally_admissible`, and `engine::topology::select` returns `Step::Closure` before it asks for a dispatch, a retry or an integration, so deleting `!self.run_is_ending()` from `ready`, from `ready_retry`, from `eligible_continuation` or from `eligible_integration_candidate` left the whole suite green; `eligible_continuation`'s deliberate absence of a `pipeline_reservable` clause was unrecorded and unpinned, though adding it for symmetry would wedge a run at its ceiling; and `integration_admissible` is a boolean, so every existing test of that door pins whether a candidate is offered and none pins which. Three tests in a new `src/topology/fold/tests/outcome.rs`, each witnessed by applying its mutation, seeing the scoped run red on a verified recompile, and reverting: the four `run_is_ending` deletions (four runs), the added entitlement clause, and `first_eligible` replaced by the last eligible entry — six mutations, each failing exactly one test and leaving the other 133 green. The only edit outside this row's file is the one `mod outcome;` line in `src/topology/fold/tests.rs` (row 39, which this does not sweep). Scoped runs at the head: `topology::fold::` 134 passed, `topology::census::` 20 passed, `engine::topology::select` 23 passed, `test-internals-notes.sh` 139 markers and 139 notes files. **Not taken, and the row does not claim it**: `PR7-R4-LOOP-004` is an open P2 against this code — `derived_outcome` answers `NotEnding` for a budget-stopped run with an open generation, because `common()` is asked first, and `select` hands that to `checkpoint` as "closure derives NotEnding" — its own finding file says the choice between the two admissible shapes is a design decision the slice that found it had no standing to make, its guard names the closure slice, and three tests here pin today's ordering as intended (the census's "a run with open work is not ending", the crossed grid's blocker-outranks-everything row, and `halt_and_budget_outrank_every_structural_source_that_can_coexist_with_them`), so a sweep reversing it would be an unwitnessed design change; its `location` still names the pre-split `src/topology/fold.rs` and the code is now here. **Deferred here and since taken**: `SWEEP-FOLD-OUTCOME-ZERO-PARALLEL` (P2, `src/topology/fold/start.rs`), a `run_started` recording `max_parallel: 0` is accepted because `check_run_started` does not check `limits`, after which `pipeline_reservable` is `0 < 0` for the life of the run, `derived_outcome` answers `DerivedOutcome::FoldError` — the arm `topology::census` asserts no explored state reaches — and `check_run_finished` refuses all four outcomes with "derived ... unreachable", measured at this head; deferred because the refusal belongs in row 38's file and no ordering here can invent an outcome for a run that has none. It is fixed on `sweep/topology-p2s`, which holds row 38's file: `check_run_started` now refuses `max_parallel = 0` with `FoldError::UnusableLimit`, DESIGN §26 gained "Frozen limits" for the contract, and the finding file is deleted. **Deferred, each filed**: `SWEEP-FOLD-OUTCOME-ADMISSIBILITY-NOT-REFUSED` (P3, `check_attempt.rs` and `check_integration.rs`), `ready` admits a dispatch on seven clauses and `check_dispatched` refuses on four of them, taking neither the dependency clause (`check_spawn` checks dependencies, but at registration and only for the repairs a rejection registers) nor the overlapping-lease, entitlement or run-ending clauses, with `check_transaction_start` missing the last two as well; and `SWEEP-FOLD-OUTCOME-CENSUS-BACKOFF-MIRROR` (P3, `src/topology/census.rs`), whose mirror of `backoff_pending` reads the task state and so, in the state above, agrees with the mutant it exists to catch. |
| `src/topology/fold/parse.rs` | `2958b1d0` | 2026-09-06 | Row 35, `#108 fold` family, and the smallest file of the family: 35 lines at the base `ee5dc81f`, where the queue table records the 57 the split produced — `88ac2aad` moved this module's comments into `docs/internals/topology/fold/parse.md` afterwards. §6: grep-verified no `Rc`, `Arc`, `Mutex`, `RwLock` or `clone()` in the file, before or after. §7: the two `?` are kept as deliberate — each is on a `map_err` that has just built the `FoldError::RewrittenLog` this function's caller acts on, and the function has nothing further to add; both panicking sites are dispositioned by removal rather than by `#[expect]`, `&bytes[..committed_end]` and `bytes[..error.valid_up_to()]` going with the two-pass shape below, and `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports 1,163 sites in the crate and none in this file, the sibling `src/topology/fold/tests.rs` being among the files it does report. **Fixed (`SWEEP-FOLD_PARSE-001`, correctness):** the refusal named the wrong line. `parse_log` read the log in two passes, one `from_utf8` over the whole committed prefix and then a per-line parse, and the first pass answered first, so a log whose line 1 is not an event and whose line 3 is not UTF-8 was refused as line 3 — against the module's own documented contract, `FoldError::RewrittenLog` naming *the first* committed line that is not a valid event, and against the one reader a refusal exists for, an operator looking for where a rewritten log stops agreeing with what was appended. The bytes are now walked one committed line at a time with `split_inclusive`, each line validated and parsed before the next is looked at. Acceptance is unchanged: `\n` is ASCII and cannot fall inside a multi-byte sequence, so a line boundary never splits a character and per-line validation admits exactly the prefixes whole-prefix validation admitted; the `Utf8Error` in `detail` now carries its byte index within the line rather than within the log. **Tests:** three, in this file's own `#[cfg(test)]` block — the second such block in the `fold` family, after `src/topology/fold/check_candidate.rs` and for the same reason, that the fold's suite is `src/topology/fold/tests.rs`, queue row 39, which a sibling session is sweeping. Each was witnessed by a mutation applied and reverted, `git diff` empty before and after: the regression test answers 3 where the first committed non-event is line 1 under master's body restored under it, and 0 under `position + 1` mutated to `position`; the torn-tail test, whose fixture is the lead byte of `é` left behind by an interrupted write, dies under UTF-8 validated over the whole input with master's line count, a mutation the rest of the `topology::fold` suite survives at 132 passed, including `a_committed_line_that_is_not_an_event_is_a_rewritten_log`, whose own torn fixture is all ASCII; the no-marker test, taking the empty log and one unterminated first line, dies under master's `map_or(0, …)` answering `map_or(bytes.len(), …)`, and is the only test whose failure that mutation causes — the run is 132 passed and 2 failed, the second failure being the regression test, which fails on master's body unmutated too. Every mutation above was re-run 2026-09-06 with the source touched first and the `Compiling upstroke v0.1.0 (/srv/worktrees/topo-fold-parse)` line read out of each log, after the shared slot pool was found serving other worktrees' binaries. **Deferred, both P3, one file each:** `SWEEP-FOLD_PARSE-002`, a committed line ending `\r\n` folds as if appended while a blank one is refused for being unaccountable, the same tolerance living in `src/events/log.rs::parse_bytes`, so neither reader should change alone; `SWEEP-FOLD_PARSE-003`, `parse_log` is `pub` with one production consumer and none outside the crate, a §5 question that belongs to `TopologyFold`'s own surface at row 40 beside `pub fn replay`. Scoped green at this head on Linux: `topology::fold` 134 passed, `events::log` 47 passed with 1 ignored, `engine::topology` 308 passed with 10 ignored; `cargo fmt` and `cargo clippy --all-targets --all-features -- -D warnings` both clean. |
| `src/topology/fold/region.rs` | `f64737ad` | 2026-09-06 | Row 37, `#108 fold` family. Six rendering helpers a `FoldError`'s sentence is written from, plus the one comparison this module owns; nothing here reads the filesystem, the clock or a process, so every item is a function of its arguments and is tested as one. **§6:** no `Rc`, `Arc`, `Mutex`, `RwLock` or `.clone()` anywhere in the file, before or after the sweep, measured by grep for those five constructs at `f64737ad`; the fourteen `to_owned()` calls each copy a `&'static str` or a small formatted string into a `String` the error type declares, which is the intended semantics and not a borrow-checker convenience. **§7:** the file has no `?` at all, its one fallible function returning `Result` for three callers that propagate, and no `.unwrap()`, `panic!`, `todo!`, `unimplemented!`, index, slice or `unreachable!`: `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports zero sites of either construct in this file, and the `Some([])` arm of `describe_region` is a slice pattern, which answers the absence instead of terminating on it. The only `expect`-shaped calls are the two `.expect_err(` in the new test block, each naming the premise that failed, which is the test allowance §7 states. **Fixed:** `GitRefName`, a trait with one implementation whose method delegated to the `pub` `GitRef::as_str`, is gone and its one call site in `check_integration.rs` takes the inherent method (§5: a trait wants multiple implementations or a real behavioural contract, and this had neither); and `check_lease_disposition`'s refusal no longer fills its two value fields with `format!("{recorded:?}")` and `format!("{expected:?}")` but with a named `disposition_name`. That second one was measured before it was changed, because the fix had to follow the fold's convention rather than invent one: refusals in this crate name enum values through named helpers returning display prose, not a serde tag and not `Debug` — `GenerationClass::name` answers `open with no attempt` — so the message keeps its words and stops depending on what a derive currently produces. **§12 is the substance of this row.** The file had no test module and no coverage anywhere in the tree: at `ee5dc81f` none of the strings it produces was asserted anywhere in `src/`, and `check_lease_disposition`'s only coverage was two `matches!(..., FoldError::InvalidLeaseDisposition { .. })` assertions in `src/topology/fold/tests.rs` that pin none of the six fields and never construct a lineage generation. Thirteen tests now pin every arm of every renderer, the six-row lease-disposition table written out rather than computed from `GenerationLease::expected` (the code under test), and two properties the refusal sentences depend on: that no two names a refusal prints side by side are equal, and that an empty region does not read as a repo-wide one. Each test was witnessed against a mutation applied to the tree, run, seen to fail and reverted; the thirteen mutations are named in `f64737ad`'s body. One trap recorded with them: a mutation applied file-wide rewrites the test's own literal as well as the production arm and the mutant then survives, so each was bounded to the production lines and the first attempt at `the whole repository` had to be re-run. **§13:** `docs/internals/topology/fold/region.md` gains the four item sections it lacked, the module's display-spelling-not-wire-spelling convention, the reason the `false` in `check_lease_disposition` is its three callers' one reachable kind of generation rather than a policy this function chooses, and why `admission_name` is deliberately not `Admission::tag`: that one feeds `encode_entry`'s content digest, so routing a diagnostic through it would make rewording a refusal a change to every recorded digest. No design sentence changed — `grep -rn` for this module's vocabulary over `design/` returns nothing at `ee5dc81f`, and the substance it renders is DESIGN §26's dispatch path leases, which govern the leases and not the refusal's text. **Deferred, both P3 and both files under `findings/`:** `SWEEP-REGION-001`, `FoldError::InvalidLeaseDisposition` carrying `recorded` and `expected` as `String` and a `fate` field that is `closes` at its sole construction site, because typing them is an edit to `src/topology/fold.rs`, queue row 40 and outside this bound; and `SWEEP-REGION-002`, `describe_region`'s unescaped backtick quoting, under which a single path containing a backtick renders byte for byte as a two-path region does. |
| `src/topology/fold/start.rs` | `759f2e61` | 2026-09-06 | Row 38, `#108 fold` family, 293 lines at assignment: the `run_started` check, the dispatch of every event after it, the derived-outcome accessor and `check_ladder`. **§6:** grep-verified no `Rc`, `Arc`, `Mutex` or `RwLock` in the file, before or after. Four `.clone()` sites at the base, one fixed and three kept. Fixed: `check_started_run` opened `if let Some(outcome) = run.finished.clone()`, and `crate::events::RunOutcome` is a four-variant fieldless enum deriving `Clone` and not `Copy`, so that clone existed only to bind by value out of a `&RunState` — the borrow-checker clone §6 forbids. It borrows now, and since the two `RunIsOver` constructions either side of it were byte-identical the outcome match yields a `continues` flag and the error is built once. Kept, each an owned `String` snapshot the returned error value needs after the borrow it was read through is gone, stated at the site: the two `normalized_plan_digest` copies and the `registry_digest` copy that `FoldError::DigestMismatch` carries. **§7:** `cargo clippy --all-targets --all-features -- -D warnings`, and the same command with `-W clippy::indexing_slicing -W clippy::unreachable`, both report zero sites in this file; it contains no `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!`, indexing, slicing or `unreachable!` anywhere, the new test block included. Three `?`, all in `check_run_started` and all kept as deliberate: two are `map_err` into typed variants that name the operation and carry the source's text (`IncompleteRunner` from `RunnerPolicy::completeness`, `RegistryUnbuildable` from `TaskRegistry::originals_with_agents`), and the third propagates `check_ladder`'s own `FoldError::MalformedLadder`, which already names the task key and the defect, so the frame has nothing to add. `check_started_run` has no `?` before or after; its match yields a `Result` and the delta is mapped over it. **§5, read and kept:** the 24-arm dispatch has no wildcard over `TopologyEventBody`, and the run-is-over gate's match over `RunOutcome` stays exhaustive rather than collapsing to a `matches!`, so a new variant of either still forces a decision here rather than falling into a default. The `RunStarted` arm of `check_started_run` is unreachable through `plan_transition`, which routes that body to `check_run_started`; it is kept answering `AlreadyStarted` rather than becoming a wildcard. `derived_outcome` does not consult `TopologyFold::poisoned` although every admissibility predicate in `predicates.rs` does, and that was measured rather than assumed to be an omission: it is the total outcome function over durable state alone, poisoning is a fact about this process's last append and not about the log, and `select` in `src/engine/topology/select.rs` answers `Step::Poisoned` before it ever asks. Kept unguarded, deliberately. **Better implementation, fixed:** `check_started_run` built `self.delta(event, Derived::None)` in twenty-one arms and `self.delta(event, Derived::Answer(origin))` in the twenty-second, burying the one arm that derives something; the match now yields `Result<Derived, FoldError>` and the delta is built once from its value, with the arms, their order and their refusals unchanged. `check_ladder`'s escalation check carried a `previous: Option<Tier>` accumulator that it shadowed inside the loop; it zips the tier list with itself offset by one and compares the pair, which is also why no `windows(2)` index appears. The function's `#[allow(clippy::too_many_lines)]` is removed: it was already stale at the base, since the function was 92 source lines against clippy's 100-line default and `too_many_lines` is pedantic, absent from `Cargo.toml`'s `[lints]` and from CI's clippy command, so it could not fire; measured after removal, `-W clippy::too_many_lines` reports ten sites in the crate and none in this file. **Tests.** `check_ladder`'s eight refusals are exercised from `src/topology/fold/tests.rs` (row 39, not swept here) through `run_started` and `task_spawned`; enumerating that table against the function found the rung-count refusal with no case behind it, because the table's runnable-with-no-rungs case is caught one check earlier by the admission arm. Two tests are added in this module's own `#[cfg(test)]` block, the way `src/topology/fold/check_candidate.rs` does it and for the same reason: `check_ladder` is a free function over `TaskKey` and `&FrozenLadder` and needs none of row 39's `RunStarted4`, plan, chain and registry-digest fixture. Each asserts the whole `defect` string rather than the variant, since all eight refusals are `FoldError::MalformedLadder`. **Regression evidence:** five mutations, applied one at a time and reverted, `git diff` empty before and after, against an unmutated control of the same filter (`cargo test --all-features topology::`, 634 passed, 0 failed, 10 ignored on Linux). Deleting the rung-count refusal fails only the new `a_runnable_ladder_binds_one_rung_for_every_tier`, so nothing in the tree was holding it. Dropping that refusal's non-empty-rungs conjunct fails six, the new `a_human_binding_ladder_keeps_its_tiers_and_binds_none_of_them` and five in row 39's file that build a human-binding spawn — so the conjunct was already witnessed incidentally, by tests about admission and overrides rather than about the ladder, and the new test is kept as the direct pin rather than as new coverage. Weakening the escalation comparison from `<=` to `<` fails `a_malformed_ladder_is_refused_before_it_is_stored`, so the rewritten loop is still guarded. Making a `Complete` or `Halted` run resumable fails `a_finished_run_is_continued_only_by_the_resume_its_outcome_allows`, and making the answer arm discard its `QuestionOrigin` and derive `Derived::None` fails twenty-four, so the reshaped dispatch's one semantically distinct arm and its run-is-over gate are both live. **Deferred here and since taken, as `SWEEP-START-001` (P2):** `check_ladder` compared the recorded floor with the recorded ceiling and never with the tier the chain starts at, although `design/07` writes `min_tier` as clipping the chain start, `design/10` §2 has floors truncating it, and `design/26` ¶6 has a repair's `mid` floor binding inside the frozen constraints; `src/route.rs`'s `raise_start` implements the clip, and `ladder.floor` had no other reader in the topology path, so a recorded ladder starting below its floor was accepted and every attempt below the floor then ran. Not fixed at the time because the check was expected to subsume the existing floor-above-ceiling refusal whose distinct-message assertion lives in row 39's file, and because that file's own `plan()` fixture records task `mid` with `min_tier = Mid` against tiers small then frontier: applied alone, the check took the `topology::` filter from 634 passed and 0 failed to 522 passed and 112 failed at `ee5dc81f`, and from 721 to 599 passed and 122 failed on the assembled `sweep/topology-layer`. It is fixed on `sweep/topology-p2s`, which holds row 39's file too. The subsumption did not arise: placed **after** the floor-above-ceiling refusal rather than before it, the new refusal takes only what falls through, every existing case in row 39's table is refused by the same check and with the same message as before, and no message had to be merged or deleted. The fixture needed one line — `chain("mid")` now records tiers mid then frontier, which is what `raise_start` produces for a `min_tier = Mid` task — and the whole `topology::` filter is green with it. DESIGN §26 gained "Frozen ladders" for the contract, the table gained one case on each of its two paths, and the finding file is deleted. **Measurement note.** Every figure above was taken on a target base private to this worktree, passed to `upstroke-build` as `UPSTROKE_RAMTARGET`. The shared slot pool cannot be trusted for a `cargo test` result while seventeen worktrees build concurrently: cargo's metadata hash does not include the source path, so all eight slots held a binary named `upstroke-1f05c71869a3a005`, and two runs of the same filter on this tree returned one foreign failure and then forty-three, the first naming a fixture string that does not exist in this worktree. `w1-eight-iso`'s header records the same mechanism from 2026-09-02. On the owner's direction of 2026-09-06 every run above was then repeated with the mutated file touched first and the log checked for its own compile line: each of the control and the five mutation runs printed exactly one `Compiling upstroke v0.1.0 (/srv/worktrees/topo-fold-start)`, so none of them reused an earlier binary. That repetition corrected one figure — the answer-arm mutation kills twenty-four tests, not the eighteen a truncated first listing showed. `cargo fmt --check` clean. The full suite and the eight gates were not run here; the owner runs those on the assembled branch. |
| `src/topology/fold.rs` | `f9632aa0` | 2026-09-06 | Row 40, `#108 fold` family: the parent the split left behind -- the refusal enum, the fold's state types, `TopologyFold`, `RunState`'s shared readers and the dispatch-region derivation. **§6:** grep-verified no `Rc`, `Arc`, `Mutex` or `RwLock` anywhere in the file, before or after. Four clones; two removed and two kept with their reason stated. Removed: `apply_delta` took its `TopologyDelta` by value and then matched the owned pair *by reference*, so recovering the payload cost `data.clone()` on the `Box<RunStarted4>` and `(**registry).clone()` on the whole rebuilt `TaskRegistry` -- a clone to satisfy the borrow checker, which §6 forbids by name; one match over the owned pair moves both instead. Kept: `event.clone()` in `TopologyFold::delta` is the delta taking ownership of the event it checked, which `TopologyDelta::event` exposes so a caller can append the exact bytes -- visible at the boundary; `started.incarnation.clone()` in `RunState::start` seeds a field that moves independently of the run record on every `run_resumed`, so it is a second value and not a duplicate. **§7:** no `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!`, panicking index, slice or `unreachable!`; `cargo clippy --all-targets --all-features -- -W clippy::indexing_slicing -W clippy::unreachable` reports **0** sites in this file against 1,229 across `src/` and 50 in this family's own `tests.rs`, so the zero is a measurement and not an empty domain. Three `?`, each kept as deliberate: `check_run_started(data)?` and `fold.plan_transition(event)?` propagate a `FoldError` a sibling built with its own `kind` and this layer has nothing to add to; `self.run.as_ref().ok_or(FoldError::NotStarted { kind })?` decides at the site, turning absence into the named refusal rather than propagating one. **§5, dispositioned not changed:** `plan_transition`'s `_` arm is a wildcard over the closed `TopologyEventBody`, kept because it is a two-way started/not-started split whose exhaustive dispatch is one call down in `check_started_run`, so a new variant still forces a decision. **Fixed (correctness):** `predicted_region` derived a dispatch lease region narrower than the hints it was derived from, in two shapes -- a metacharacter inside a component cut the component in half, so `src/eng*` derived `src/eng`, which `topology::leases::paths_overlap` does not match to `src/engine/mod.rs` because it compares component by component; and a `.` or `..` component survived verbatim, so `src/./alpha` never overlapped `src/alpha`. Both are anti-conservative, and `RunState::ready` admits a dispatch precisely when it finds no overlapping lease, so two tasks covering one file were predicted disjoint and both admitted. DESIGN.md §26 requires "normalized non-glob prefixes ... prefix ancestor/descendant pairs overlap" and calls the derivation deliberately conservative; `src/workspace_manager/parsers.rs` refuses the same shapes on the actual paths and cites this function as the authority it matches. The per-hint rule is now `hint_prefix`, taking whole components up to the first that carries a metacharacter and answering repo-wide for a dotted component; empty components are still kept because the comparator filters them, so `src/doubled//inner/` derives byte for byte what it did. Fourteen of the fifteen shapes the family's `HINT_SHAPES` table pins derive exactly what they derived before, each glob already sitting at a component boundary; the backslash shape now derives repo-wide, because the frontier pass on PR #212 showed a backslash is an escape under the frozen globset grammar on Unix and a separator on Windows, and `5f2c2ad7` froze the whole derivation as path policy `v2`, refusing a run recorded under `v1` at `run_started` rather than replaying it under a derivation that did not write its records. No supported historical run is affected: no `0.2.0` is tagged and schema 4 engages only by explicit choice. **Fixed (docs):** the paired `docs/internals/topology/fold.md` carried four sections appended by `6db11660` under headings that name code they do not sit above (`open_mut` for two `QuestionOrigin` variants and for a `RunState` field, `event` for a `Derived` variant), breaking the file's own stated rule that a heading is the grep string that finds the code; three of them duplicated a correctly headed section whose text still described the behaviour PR #152 replaced, so a reader got one of two contradictory answers depending on which heading they hit. Each correct heading now carries the corrected text, `deferred_tasks` gains the section it never had, and the four are gone. **Tests.** Six added, all in this file's own `parent_tests` module because the family suite is row 39's file, which this sweep does not touch. `FoldError`'s 32 messages had no content assertion anywhere -- the only rendered-text assertion in the crate was a distinctness set over seven `task_spawned` cases -- so a format string that dropped a field or was copied onto a second variant changed no test; the census builds one instance per variant with a distinct sentinel in every field, asserts each sentinel is rendered and the 32 renderings are pairwise distinct, and states its domain three ways (an exhaustive `variant_name` match that will not compile without an arm, an asserted sample count of 32, and a refusal of two samples of one variant). The `TaskState` and `GenerationClass` vocabularies and `TaskFold::open` are pinned directly, and `hint_prefix` by 24 hint shapes plus two tests that run the derived prefix through `paths_overlap` against a path the hint matches and assert the negative control, so the tests state the mechanism and not just the string. **Regression evidence:** eight mutations, applied one at a time and reverted, each with its compile confirmed (a first run served a stale binary and read green). Reverting `hint_prefix` to the character-wise rule fails all three region tests while all 131 pre-existing `topology::fold` tests pass, which is why the defect was unwitnessed; the dotted-component guard alone and the glob-component cut alone each fail two; `self.run = None` in `apply_delta`'s `run_started` arm fails 113; dropping `{recorded}` from `DigestMismatch` and giving `Poisoned` another variant's message each fail the refusal census and nothing else in the crate; renaming `TaskState::AwaitingMerge` fails the vocabulary test; and reducing `TaskFold::open` to the first generation fails the open-generation test and much of the family suite. **Deferred, one file each:** `SWEEP-FOLD-001`, a replay refusal names no event, whose fix belongs at the `src/events/log.rs` barrier because two tests pin that live and replay refuse with an *equal* `FoldError` and the live path has no line to carry; `SWEEP-FOLD-002`, `Derived::Answer`'s origin is written at one site and read at one that ignores it, whose removal changes signatures in rows 32 and 29; `SWEEP-FOLD-003`, `apply_delta` does not read `poisoned` although `plan_transition` refuses on it, measured unreachable at both production poisoning sites and recorded rather than patched because the shape that closes it is a fallible `apply_delta`. **Not filed, with the measurement:** a hint naming a path the repository does not hold derives a prefix overlapping nothing, which is indistinguishable without a repository from a hint at a directory the task is about to create, and §26 already places it as a plan-quality warning with merge verification as the correctness boundary. `cargo fmt --check` and `cargo clippy --all-targets --all-features -- -D warnings` clean; `topology::` green (638 passed, 0 failed, 10 ignored, Linux); `test-internals-notes.sh` and `test-docs-consistency.sh` pass. |
| `src/topology/fold/predicates.rs` | `b9262276` | 2026-09-06 | Row 36, `#108 fold` family; the fold's reader surface, thirty items over 167 lines at this head, every one a delegation to a private predicate or a field read. **§6: nothing to disposition.** The file contains no `Rc`, no `Arc`, no `Mutex`, no `RwLock` and no `.clone()` at all: it owns no value, and every reader hands back a borrow into `RunState` or a `Copy` derived from it. **§7: eleven `?`, and no panic surface.** `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports zero spans in this file, against 1,065 indexing spans it reported elsewhere in the same run; there is no `.unwrap()`, `.expect()`, `panic!`, `unreachable!`, `todo!`, `unimplemented!` or `#[expect]` in it either. Ten of the eleven `?` are absence propagated as absence through a function that returns `Option`, so none can turn absence into failure, and all ten are kept: seven `self.run.as_ref()?` field reads, plus `self.registry()?.get(key)?` and `self.task(key)?`. The eleventh, `usize::try_from(rung).ok()?` in `frozen_rung_binding`, discards a `TryFromIntError` and is kept: a `u32` that does not fit a `usize` cannot index a `Vec` whose length is a `usize`, so "the ladder has no such rung" is the whole content of that error, and `check_attempt_started` reaches the same decision on the same input by saturating with `unwrap_or(usize::MAX)` instead. **Fixed:** `open_no_attempt` scanned every generation of a task for the first of class `OpenNoAttempt` while `eligible_continuation` asked `TaskFold::open()` and then filtered the class — two readers of "the generation this task holds open", agreeing at every state the fold can reach (`check_dispatched` refuses while `task.open()` is `Some`, so the vector is closed generations with at most one open one last) and disagreeing the moment that invariant does not hold, where the old form named a generation `recreate_open_no_attempt` must not build a worktree for; it now reads `TaskFold::open()` (`59e71166`). `is_poisoned` was the only bool-returning item in the file without `#[must_use]` and now has it (`1ae70bd4`). **Tested:** three contracts that no test in the repository detected, each measured by its own mutation at `ee5dc81f` against `topology::fold` (131 tests) and `engine::topology::select` (23) — the poison guard in `eligible_continuation`, the poison guard in `eligible_integration_candidate`, and `open_no_attempt`'s class filter all survived deletion with both suites green. A new `src/topology/fold/tests/predicates.rs` (`b9262276`) kills all three, each mutation applied, run and reverted for real; the only line outside this sweep's file is its `mod predicates;` declaration beside `mod questions;`. The whole of each selection reader was already exercised (returning `None` unconditionally reddens seven tests for the integration candidate and five for the continuation); it was the poison arm alone that nothing reached. **Docs:** the notes' claim that a poisoned fold has four statement readers was wrong and contradicted four sections later by `open_no_attempt`'s own paragraph; measured, seven of the fourteen items below that paragraph refuse once poisoned and seven answer from durable state, and the paragraph now names both halves and says which reading was wrong (`318ee2fa`). **Deferred:** `frozen_rung_binding` answers the frozen rung and never a recorded human override, by design, and both call sites in `src/engine/topology/run.rs` use it without consulting `binding_override`; latent at this head because nothing populates `RunState::overrides` (answer ingest is refused before any append as PR9's), filed as `SWEEP-FOLD-PREDICATES-OVERRIDE-READER`, P3, guarded by the PR9 answers slice. The defect is in `run.rs`, outside this sweep's bound, so it is a finding file rather than an edit. **Not claimed:** the eight gates were not run for this row; `cargo fmt`, `cargo clippy --all-targets --all-features -- -D warnings` and the two scoped filters above are what ran here, and the owner runs the gates on the assembled branch. |
| `src/topology/fold/check_integration.rs` | `d83f04de` | 2026-09-06 | Row 33, `#108 fold` family; 659 lines in the queue's count at the family's split, 600 at assignment. §6: no `Rc`, `Arc`, `Mutex`, `RwLock` or `.clone()` in the file, before or after (grep-verified at the swept head); the one clone this sweep briefly added to a test fixture was removed rather than justified, and the owned strings are refusal details built for error values that outlive the borrow they were read from. §7: no `.unwrap()`, `.expect()`, `panic!`, `todo!` or `unimplemented!`, and the panic surface was measured rather than read — `cargo clippy --all-targets --all-features -- -W clippy::indexing_slicing -W clippy::unreachable` on this tree reports zero sites in this file out of 1,231 crate-wide, so there is no indexing, slicing or `unreachable!` to disposition. Twenty `?` at the base and twenty-three now, each same-type propagation of a `FoldError` this file or a sibling helper built with this checker's own kind, all kept as deliberate. `lineage_members`'s `u32::try_from(..).unwrap_or(u32::MAX)` is kept saturating rather than wrapping, with the proof in the notes: a key is its own dense index into the registry, so the registry cannot hold `u32::MAX` entries, and `u32::MAX` matches no index a log can record where a wrapped count would match a small one. **Fixed: four unhandled cases, all four the same shape — a closed domain one arm short.** (1) `check_merge_rejected` crossed `RejectionLeaseEffect` with `Option<Lineage>` under a wildcard `_`, and (2) `check_task_merged` crossed `MergeLeaseRelease` with the published task's lineage root the same way; both name all four cells now, so a third variant fails to compile rather than taking a generic message. (3) A verified publication's proposal pin was compared under `if let VerificationBasis::StaleClean`, one arm of a two-variant enum, so an already-present publication could carry a pin naming an object no run created — `design/26` §1 says the queue manufactures no commit there and both producers record `None`. `check_proposal_pin` names both bases and refuses it; `design/26` states the rule in the same change. (4) The deferral partition was empty at `max_defers == 0`: the `Deferred` arm refused `defers >= max` and the `Parked` arm refused an infrastructure outage unless `next == max`, and `next` is at least 1, so no `merge_verification_unavailable` with an infrastructure cause could be folded at all, while `ladder::next_step` reads the same frozen number as "park the first outage". The boundary is `next < max` now, the same predicate for every allowance of one or more because an accepted deferral is always `taken + 1` and always below `max`. **Also §7 and shape:** `check_task_merged`'s settled root came from `registry.get(..)`, folding an unknown key into "not a lineage member" and so into a demand for a candidate release; it is `self.entry(TASK_MERGED, ..)?` now, the form `check_merge_rejected` already used. The six event kinds are module constants rather than a `const KIND` per function, because a `kind` parameter on an extracted function is a seam nothing witnesses. `check_verification_started` bound the queue entry `check_transaction_start` returns and discarded it with `let _ = queued;`, dead since `bc07139`. `check_transaction_start`'s ineligibility detail was two nested `map_or_else` that read back to front and is a `match`. Dispositioned rather than changed: five `let ... else` and `!matches!` sites over the private two-variant `TransactionClass` account for both variants at every site, so no cell is unhandled today; and in `satisfies_closure` the `lineage.parent >= current` break is unreachable for any entry `check_spawn` admitted while the `dedup` can never remove an element, both kept and recorded in the notes rather than deleted. **Tests.** Five, in this file's own `#[cfg(test)]` block rather than `src/topology/fold/tests.rs`, which is queue row 39 and another session's subject; the four extracted functions are private and pure, so every cell is pinned directly — the shape `check_candidate.rs`'s row set. `topology::fold` is 136 tests at this head against 131 at the base. **Regression evidence.** Twelve mutations, applied one at a time and reverted, `git diff` empty before and after each, and every one run on a binary the run is proved to have recompiled: the box's slot pool is shared across seventeen worktrees and its dep-info is relative, so a sibling that builds later leaves artifacts newer than this tree's edits and cargo serves the sibling's test binary without recompiling. Every run here touched the source inside the slot lock and any run whose output carried no `Compiling upstroke` line was recorded as no verdict rather than as a kill. Killed: the two mismatched-pairing arms answering `Ok` (two tests each), the already-present pin arm accepting a pin, restoring `next != max`, dropping the exact-base refusal, and four of the five call-site seams — the pin argument forced to `None` by six tests, the rejected candidate's key replaced by the repair's by twenty, the settled root forced to `Some` by twenty-seven, the allowance arguments swapped by seven. Two survived and are recorded rather than claimed fixed: restoring `registry.get(..)` in `check_task_merged`, which is what an honest unreachable-branch disposition predicts, and substituting `entry.lineage` with a constant `None` at `rejection_lineage_root`'s call site, which is `SWEEP-CHECK_INTEGRATION-004`. **Deferred, four findings, one file each under `findings/`:** `-001`, `MergePrepared::self_consistency` in `src/topology/events.rs` does not refuse a fast publication carrying a verification record, the symmetric case of the stray pin it does refuse; `-002`, three integration grids in row 39's file assert only `is_err()`, so a case's label and the relation it exercises can differ, shown on the case that never reaches the relation it names; `-003`, `max_defers` is one allowance to `ladder::next_step` and one lower to this file, latent because nothing outside the fold, its tests and the census constructs a `MergeVerificationUnavailable` yet; `-004`, no test in the tree rejects a lineage member, so the widening path of `check_merge_rejected` has never run end to end. |
| `src/topology/fold/tests.rs` | `65729872` | 2026-09-06 | Row 39, `#108 fold` family: the family's suite, 9,029 lines and 102 `#[test]` items at `ee5dc81f` (9,386 and 104 after this sweep), swept under amendment 7 with the tests as the whole job. **§6:** grep-verified no `Rc`, `Arc`, `Mutex` or `RwLock` anywhere in the file, before or after, so there is no shared ownership or lock to justify; the 155 `.clone()` sites at the base (163 after this sweep) are all one shape, an owned fixture copy taken so a table can perturb one coordinate of it while the accepted original survives for the next row, which is §6's "owned snapshot ... small independent value" and is stated here rather than 155 times. **§7:** `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reported 50 sites in this file at the base -- 11 `unreachable!`, 35 indexing, 4 slicing. The 11 `unreachable!` are **gone**: §7 denies the construct in tests too and Clippy has no allowance to take, and all 11 were one idiom destructuring an event the same function had just built, now two `#[track_caller]` accessors that fail their own setup with `panic!` and the kind that arrived. The 39 indexing and slicing sites are **kept**, taking the `allow-indexing-slicing-in-tests` allowance §7 names for them; each indexes a collection the test built or a `Vec` the fold produced, where a panic is the test's own failure. No `.unwrap()` before or after; `.expect(` and `panic!` are the carve-outs `clippy.toml` already grants tests. No `?` operator: the file's one `?` character is a glob metacharacter inside a `HintShape` string literal, which is why the claim is made against the compiler's view and not against `grep`. **Fixed, each witnessed by running the mutation and seeing the scoped suite fail:** `pinned` was the one coordinate of `RungBinding` no table varied, and a `matches_frozen` that ignored it passed the whole fold suite; the epoch scope of the halt guard on `question_answered` was unpinned in both directions, and this file's own prose asserted the opposite of what the code does, which is why the test stopped one line short; `eligible_continuation` and `eligible_integration_candidate`, the two `pub(crate)` readers the engine's selector uses, had no test here at all, so the class filter of the first and the open-transaction guard of the second were both free; `a_refused_transition_changes_nothing` could not fail, `plan_transition` taking `&self` on a type with no interior mutability, and its claim now sits in a `const` item that a `&mut self` receiver fails to compile against; the `Budget::Older` grid cell held a stop one epoch **above** the run's, so only one of the two ways `budget_stop_is_current`'s equality can loosen was under test; and three smaller ones -- a settlement table with nothing holding it to the vocabulary, an identity assertion in the hostile-replay sweep that was an arithmetic tautology, and a diagnostic that named the opposite of the failure it fires on. **Deferred, both P3 and both filed:** `SWEEP-FOLD-TESTS-REFUSAL-ASSERTIONS-VACUOUS`, the eight remaining assertions of the class the rewritten test belonged to, each true by construction and each inside a test that does die under mutation, left as one judgement for the family rather than eight edits in one file; and `SWEEP-FOLD-OUTCOME-LINEAGE-QUESTION-UNREACHABLE`, which names `src/topology/fold/outcome.rs` and belongs to queue row 34. **Not swept, stated rather than hidden:** `src/topology/fold/tests/questions.rs` is a separate file on no queue row and was not touched; the path-shaped fixtures here (`worktree_path`, `execution_root`, `private_dir`) are POSIX-absolute literals, which is harmless only because the fold reads none of them, and the first check that does read one turns them into a Windows-only failure. Measurement caveat for anyone repeating the numbers: the `upstroke-build` slot pool is shared with the sibling sweeps and served stale binaries during this sweep, so every mutation above was re-run with the sources touched and the run's own `Compiling upstroke` line checked. |
| `src/config/read.rs` | `730519a` | 2026-09-06 | Row 53, `#123 config` family, PR #178: swept in the sweep workflow (pass 1 on `1633fcd`, three P2 and two P3), parked when the one remaining P2 needed a change outside the assigned file, and finished under the parked-PR recovery workflow, which admits the directly required companion change (recovery review 1 on `4253009`, one P2 and two P3, repaired in the cited commit; the companion fix is `3782385` and the pass-1 repairs `ae913e2` before it). §5: the one `as` cast (`monthly_allowance`'s `Integer` branch) is *checked* rather than argued: TOML supplies the full `i64` range and an `f64` carries 53 significant bits (a written `9223372036854775807` came back as `9223372036854775808`), so `converts_exactly` refuses by name any integer the cast would change -- the span between its highest and lowest set bits, in integer arithmetic, against `f64::MANTISSA_DIGITS` -- and accepts every other one unchanged, `2^53 + 2` and `design/17`'s `10^16` included; a first repair had put a blanket ceiling at `2^53` that refused those, and the notes file now states the check that is enforced (`SWEEP-READ-002`, twice); `warnings: &mut Vec<String>` is kept as the parent's accumulator convention across `read_pools`, `parse_gates` and `parse_engine`. §6: no shared ownership, lock or clone in the file; the two `PathBuf` clones `read_repo_config` and `read_pools` took into a `map_err` closure only because `?` split the path's two uses are gone, each replaced by an explicit `match` that moves the path into whichever arm runs (`SWEEP-READ-003`). §7: no `.unwrap()`, `.expect()`, `panic!`, indexing, slicing or `unreachable!` outside the test module; every `?` propagates an `UpstrokeError::Config` built at the site or by `config_error`, and the two `snapshot.text()?` propagate the snapshot's own path-carrying error unchanged; the three `unwrap_or`/`unwrap_or_default` fold absent optional keys. §12: eighteen unit tests in the file (none existed before), each on a `toml::Value` fixture or an absent path -- `parse_pool`'s blank name, unrecognized kind, missing agent, unparseable window, `safety_margin` and `reserve` out of range, `monthly_allowance`'s `"auto"`, integer, float, exactness (2^53, 2^53+2 and 10^16 accepted unchanged; 2^53+1 and `i64::MAX` refused; witnessed both ways, the old ceiling restored failing the first and the check removed failing the second), zero/negative/non-finite and wrong-typed shapes, duplicate `sources`, the unusable-adapter warning, the unknown-key warning -- and `read_repo_config`'s required-absent and optional-absent branches on a `FileSnapshot` built with the `Ok(None)` content `snapshot_file` records for a missing file, so no filesystem state is in the oracle and the module still reaches no governed primitive (`SWEEP-READ-004`, `-005`). §13: `docs/internals/config/read.md` gains a `read_repo_config` section stating the explicit/discovered asymmetry, a section for the exactness check and sections for the test fixtures; the source carries the `Extended notes:` marker and no other prose, the inline comments a first repair had added being moved to the notes (`SWEEP-READ-006`). **Fixed** (`SWEEP-READ-001`, the guarded `SWEEP-CONFIG-PARSE-007`, P2): a misspelled top-level section -- `[budgts]`, `[interation]`, `[runer]` -- deserialized into nothing through `RawRepoConfig`, the section vanished without a warning and its defaults took effect while `validate` reported a clean file; the struct in `src/config.rs` (row 54, the immediate call site, not swept by this change) now denies unknown fields, the refusal names the typo and the accepted seven sections, the regression through `load_captured` on a capture built in memory (`a_misspelled_top_level_section_is_refused_not_dropped`) is witnessed against removing the attribute, and `design/17` states the refusal. Still row 54's, each with a file: `SWEEP-CONFIG-PARSE-008` (`RawStrategy` drops unknown keys; its file once paired it with `-007`, which this sweep resolved, so it is now guarded to row 54 alone), `-009` through `-012`, `-020`, `-021` (`src/capacity.rs`), per `src/config/parse.rs`'s row. |
| `src/plan/markdown/assemble.rs` | `2b511e0d` | 2026-09-06 | Row 64, `#123 plan/markdown` family, second file after `annotation.rs` (row 63, PR #169). §6: no `Rc`, `Arc`, `Mutex` or `RwLock`. `assemble` held four `.clone()` sites at the base; **three are gone and the fourth changed shape and stayed** (corrected after review pass 1, which caught this row claiming all four had become moves — `SWEEP-REVIEW-ASSEMBLE-001`). Gone by moving: the loop's `ann.id.clone()` and `ann.paths.clone()`, fields of its own owned `ann` each read once and never used again, so `match ann.id` and `let mut path_hints = ann.paths` take them by value; and `previous_id.clone()`, whose old value is unconditionally overwritten later in the same iteration regardless of branch, so `.take()` is exact. Changed shape and kept: the base seeded `taken` from a `drafts.iter().filter_map(…)` whose closure called `d.annotation().id.clone()` — a second `Draft::annotation()` call per draft, and `Draft::annotation` returns `self.ann.clone()`, so the base copied every annotation twice per draft, the call site `SWEEP-ANNOTATION-002`'s guard names. Each draft is now paired with its annotation once and the reservation reads that pairing, so the seed is an `annotated.iter().filter_map(…)` whose closure calls `ann.id.clone()` (line 17 of the swept file): **still a clone**, kept deliberately, because `taken` must stay valid after the loop below consumes the pairing by value, while each annotation's own id moves from there into its task, so the registry and the task are two owners of one string. That is §6's owned-snapshot semantics, not a borrow the checker refused, and the paired notes file states it at the site. Net per draft: one `Draft::annotation()` copy where the base made two, and the explicit id copied once into `taken` and then moved into its task, where the base copied it out of each of its two annotation copies. The complete `.clone()` inventory of the swept file, re-derived by `grep -n "clone()" src/plan/markdown/assemble.rs` at `2b511e0d` after review pass 2 found this row presenting six of eight sites as the whole remainder (`SWEEP-REVIEW-ASSEMBLE-004`; the first account was patched at the one line pass 1 named instead of being derived from the file), is eight sites: line 17, the reservation copy above; and seven the refactor did not touch, each kept as an owned snapshot a second owner holds and none larger than a handle. Line 56, `task.id.clone()` into `previous_id` before the task moves into the vector: the next iteration's dependency chain and the vector each own the id. Line 81 in `unique_slug`, `let mut candidate = base.clone()`: `base` stays the owned stable suffix template that `format!("{base}-{n}")` reads again on every later round of the `while` loop while `candidate` is replaced, so the first candidate must be an independent owned copy, not a move of `base`. Line 87, `taken.push(candidate.clone())`: the chosen slug needs two owners, the reservation registry `taken` that stops a later draft deriving the same slug again and the returned value that becomes this task's id. Lines 128, 129, 137 and 140 in `collect_artifacts` (`out.clone()`, `task.id.clone()`, `id.clone()`, `design.id.clone()`): each copies a handle-sized `TaskId` or `ArtifactId` (a `String` wrapper) out of a task the slice keeps on into an artifact record that outlives the borrow. None of the eight satisfies the borrow checker in §6's sense; the paired notes file states the reservation and the `unique_slug` copies at their sites. §7: no `?`, `.unwrap()`, `.expect()`, `panic!`, `todo!`, `unimplemented!`, `unreachable!`, indexing or slicing anywhere in the file, before or after (grep-verified; `cargo clippy --all-targets --all-features -- -W clippy::unreachable -W clippy::indexing_slicing` reports zero sites in this file). Correctness: read in full against `docs/internals/plan/markdown/assemble.md`; explicit-id reservation before slug derivation, the `depends=` chain-and-break semantics, and the artifact-collection fallback all match, and no code defect was found. The notes did need correcting, which this row first claimed they did not: their second section heading quoted `let mut taken: Vec<String> = drafts`, the source line this sweep replaced, and §13 makes that heading the grep string that finds the code, so it named nothing (`SWEEP-REVIEW-ASSEMBLE-002`, review pass 1). The heading now quotes `let mut taken: Vec<String> = annotated`, a second section covers the one-call pairing above it, and the reservation copy is explained there. No behaviour change; `src/plan/markdown.rs`'s existing black-box suite (`derived_ids_are_slugged_and_uniquified`, `empty_depends_is_explicit_none_and_absent_is_document_order`, `bare_fixture_uses_heuristics`, `needs_without_producer_warns`, and the rest of that file's tests, which reach `assemble`, `collect_artifacts`, `slugify`, `unique_slug` and `heuristic_kind` only through `MarkdownPlanAdapter::parse_with_warnings`) passed unchanged before and after (`cargo test --all-targets --all-features`, 2,137 passed). **Deferred:** a mutation dropping the id-reservation prepass entirely (`taken` starts and stays empty) was not caught by that suite or any other test reachable from this file — no test builds a plan where an early draft's derived slug and a later draft's explicit `id=` are the same string — recorded as `SWEEP-ASSEMBLE-001` (P3, `findings/P3_correctness_202609060817_explicit-id-reservation-untested-across-drafts.md`); closing it needs a fixture in `markdown.rs` (row 68, the family's parent and a separate unswept row this sweep's `require_scope` does not admit editing; this row first misnamed it row 66, which is `hints.rs` — `SWEEP-REVIEW-ASSEMBLE-003`, review pass 1) or a private test seam whose only production cost would be an extra clone for the seam's own sake, and this row does not resolve `SWEEP-ANNOTATION-002` (`drafts.rs`'s own `self.ann.clone()` inside `Draft::annotation()` is unchanged and out of this sweep's scope), only the two-call site in `assemble` that its guard names. |
| `src/runner/host/naming.rs` | `7a95c651` | 2026-09-06 | Row 42, `#111 host` family. §6: grep-verified zero `Rc`, `Arc`, `Mutex`, `RwLock` or `.clone()` anywhere in the file, before and after the repair below; this file copies nothing. §7: grep-verified and `cargo clippy --all-targets --all-features -- -D warnings` and the same command with `-W clippy::unreachable -W clippy::indexing_slicing` (crate-wide, through `upstroke-build`) report zero sites for `.unwrap()`, `panic!`, `todo!`, `unimplemented!`, indexing, slicing or `unreachable!`; `.expect(` appears only inside the file's own `#[cfg(test)] mod tests`, which `clippy.toml`'s `allow-expect-in-tests` covers; the file has no `?` at all, before or after — `resolve_program` builds its `Result` directly and returns explicitly on every path. **Fixed, first pass:** four stale `DESIGN.md` line citations (`:118`, `:263`, `:398-402`, `:612`) into what is now a 99-line index — the staleness class row 41's review recorded as `SWEEP-HOST-ENVIRONMENT-002` — resolved through `DESIGN.md`'s own "Line citations" table against the pre-split file at `cfec136` and rewritten as `DESIGN.md §6`/`§8`/`§15`/`§21`, each checked against the current section for the sentence it supports. **Fixed, after review pass 1 (two P2, no P0/P1):** `SWEEP-HOST-NAMING-001` — `is_program` asked `Path::is_file` and then, on Unix, asked the filesystem again for the mode bits, and both folded every error into "not a program", so a candidate whose `stat` failed with `PermissionDenied`, `ELOOP`, `EIO` or a dead mount was indistinguishable from an absent one and the search then reported that nothing of that name was on the `PATH`, against §7's rule that only an actual not-found becomes absence; it now takes one `fs::metadata` reading, answers `Ok(false)` only for `NotFound`, returns the `io::Error` otherwise, and `executable_bit` reads the mode from that same reading instead of asking twice; `resolve_program` returns `UpstrokeError::Filesystem { operation: "stat", path, source }` naming the first candidate it could not decide (at `b9c73630` it kept that candidate and searched on, reporting it only at exhaustion; corrected at `7a95c651`, below). `SWEEP-HOST-NAMING-002` — `extensions` converted `PATHEXT` with `to_string_lossy` before splitting it and `candidates` built each name with `format!`, so a `PATHEXT` entry carrying code units no `String` can hold (non-UTF-8 bytes on Unix, an unpaired surrogate on Windows) produced a candidate spelt with `U+FFFD` and a real executable of that name was refused; `PATHEXT` is now split, trimmed and ASCII-folded in the platform's own code units by `pathext_entries` (two thin `#[cfg]` conversion arms) and appended with `OsString::push`. The grammar itself is `normalised_extensions`, one function generic over the code-unit width, so the width Windows reads is executed by the Linux suite on every run rather than only by the guest. Correctness: read in full against the paired `docs/internals` notes, whose section headings are verified to be exact grep strings for the source after every signature change. Regression evidence, two kinds. Four mutations of the **pre-existing** behaviour, applied one at a time and reverted (`git diff` empty after each), each killed by the existing `runner::host` suite in `src/runner/host/tests.rs` (row 44, a separate unswept file, out of this sweep's scope to edit) against a then-clean baseline of 61 passed/0 failed/5 ignored: dropping `:` from `is_bare_name`'s Windows separator set (1 failed); disabling the `!dir.is_absolute()` guard, `PR6-LANED-003` (3 failed); trying an extensionless name verbatim on Windows (2 failed); inverting the Unix executable-bit test (29 failed). Then four **new** tests for the repair, in the file itself because this module denies the three governed lints and so cannot build a filesystem fixture (`clippy.toml` disallows every `std::fs` creation primitive); all four are built from values, and each was witnessed failing under the mutation that restores the shape it is about: folding every `stat` error into absence kills `a_candidate_this_platform_cannot_stat_is_never_reported_as_absence` alone; removing the `NotFound` arm kills its control `a_candidate_that_is_merely_absent_is_still_absence`; restoring the lossy `PATHEXT` conversion kills `a_pathext_entry_no_string_can_carry_reaches_the_candidate_intact` alone; dropping the trim kills `the_pathext_grammar_reads_the_same_over_both_code_unit_widths` alone. The suite was 65 passed/0 failed/5 ignored at `b9c73630`, +4 and no regression. Deferred, with a file: `HostRunner::program_for` memoises a failed resolution as its rendered string and reconstructs it as `Refused`, so the typed `Filesystem` error above is flattened on a memo hit and a transient failure is cached for the life of the runner (`SWEEP-HOST-NAMING-003`, `src/runner/host.rs`, row 45). **Fixed, after review pass 2 (one P2, one P3), in the parked-PR recovery of 2026-09-06 (`7a95c651`):** `SWEEP-HOST-NAMING-004` — the pass-1 repair carried an undetermined candidate to the end of the search and returned a later executable candidate over it, so a non-`NotFound` `stat` error was discarded whenever a later `PATH` entry held the program, against §7's rule that an error is not discarded through a catch-all match unless the operation is explicitly best-effort with defined observability, which nothing here defines; the `Err` arm now returns the `Filesystem` error at that candidate and only a `NotFound` miss continues the search. The cost is stated in the notes and accepted: `execvp` walks past an unreadable entry (measured on the box) and this function now refuses there, naming the entry. `SWEEP-HOST-NAMING-005` — the absence control pointed `PATH` at the ambient temporary directory and a fixed name, which §12 does not call hermetic; it now points at a uniquely named directory this process never creates (pid, per-process counter, clock), asserted absent with `symlink_metadata` before the search. Two new tests, built from values like the four before them, the later match being the running test binary itself: `an_undetermined_candidate_stops_the_search_before_a_later_match` (matched on the `Filesystem` variant's fields) and its control `a_directory_that_is_merely_absent_is_walked_past_to_a_later_match`. Witnessed on the committed head over `runner::host::` (68 passed/0 failed/5 ignored unmutated): restoring `b9c73630`'s fall-through fails the new test alone (67/1); making `NotFound` propagate fails both absence controls and 21 row-44 tests (45/23); folding every `stat` error into absence fails the two undetermined-candidate tests (66/2). Still deferred with a file: `SWEEP-HOST-NAMING-003` (row 45's `program_for` memo, above). |
| `src/topology/fold/apply.rs` | `8c0ecc35` | 2026-09-06 | Row 29, `#108 fold` family, swept from master `ee5dc81f` after PR #152 merged the repair its seven `CHANGES_REQUIRED` passes drove; this sweep judges the merged file, and PR #152's ledger keeps that history. **Correctness.** Read against `design/26`'s question, answer and decline paragraphs clause by clause: an answer removes only its question and derives the task's state from current facts in the order §26 states; a hidden execution backoff survives a question, since `set_state`'s `AwaitingInput` arm is the one arm that does not clear `deferred_tasks`, and is consumed once by an elapsed wait or a resume, since `wake_backoff` takes the set before deriving; a decline fails every unmerged lineage member including the root, closes their generations, removes their candidates and questions, clears their backoff, releases their generation, candidate and lineage holdings, cancels a matching `VerificationStarted` and leaves a `Prepared` transaction and all unrelated work alone. No divergence found and no behaviour changed. **§6, grep-derived over the file:** no `Rc`, `Arc`, `Mutex` or `RwLock`, before or after; all 23 `.clone()` calls store owned event data into fold state that outlives the borrowed event — the registry entry, a question and the option list its `HumanBinding` admission froze, the dispatch's predicted region, the generation's base sha, a retained session id, the prepared candidate's three fields, the candidate lease's region, the queue entry's region and candidate ref, the transaction's candidate, basis, heads and satisfies list, a validated binding override, the run outcome and the incarnation. `apply_merge_rejected` borrows the recorded candidate region while widening the disjoint lease table, which is field-level disjointness rather than a clone. **§7, measured:** `cargo clippy --all-targets --all-features` with `-W clippy::indexing_slicing -W clippy::unreachable -W clippy::panic -W clippy::expect_used -W clippy::unwrap_used` reports no site in this file for any of them, and the file has no `as` cast. The one `?` is `open_generation_mut`'s `self.tasks.get_mut(key.index())?`, an `Option` propagated into an `Option` return, which is the one shape §7 allows a `?` on an `Option`. Two `saturating_add` counters — `next_sequence` and `epoch` — cannot panic but have no refusal at their ceiling the way the attempt counter gained one in PR #152, and that refusal belongs to the checkers, so it is filed (`SWEEP-FOLD-APPLY-SATURATING-COUNTERS`). **§5, fixed:** an `#[allow(clippy::too_many_lines)]` on `RunState::apply` that suppressed nothing — measured by rewriting it as an `#[expect]`, which reported the expectation unfulfilled on both targets — is gone; and 18 of the file's 23 items were `pub(super)` with no user outside it, so they are private now. The compiler is the measurement: narrowing all 23 named exactly five that are reached from elsewhere, `apply` from `fold.rs` and `open_generation_mut`, `charge_allowance`, `set_state` and `open_question` from `src/topology/fold/tests.rs`. The exhaustive application dispatches PR #152 landed are kept and re-read: over the event body, `Derived` and its `QuestionOrigin` payload, `SpawnAdmission`, `AttemptSettlement` and `SettlementTransition`, `LeaseGrant`, `CandidateLeaseEffect`, `UnavailableOutcome`, `PreparedDisposition`, `RejectionDisposition`, `RejectionLeaseEffect`, `MergeLeaseRelease`, `Answer4`, `TaskState` and `GenerationLease` — no wildcard over a closed domain remains. **Tests: 56 mutations at the base, each applied alone and reverted, judged by `upstroke-build cargo test --all-features topology::fold`.** Every run is recorded as having recompiled and as having seen exactly this tree's 131 tests, because the box's build slots are shared across seventeen worktrees and have twice served a binary from another tree. 42 killed, 14 survived. Three of the survivors are repaired here, by extracting the relation so it can be reached with values — the treatment row 31 used, and for the same reason: every method here takes `&mut RunState`, whose fixture lives in `src/topology/fold/tests.rs` (row 39), which this sweep may not edit. `take_candidate_region` now holds `apply_candidate_prepared`'s lease half, `releases_own_region` holds `close_generation`'s rule, and `derived_state` over a six-field `VisibleFacts` holds `refresh_task_state`'s derivation, which is `design/26`'s precedence sentence and is pinned by a table rather than a trace. Seven tests in this file's own `#[cfg(test)]` block, each witnessed against the mutation it dies under; all eight helper mutations are killed at the head, and the two that no test could see at the base — a lineage member's candidate not widening its lineage, and a closing lineage generation releasing a generation lease — are each killed by the one test written for it. **The extractions' eight seams were measured rather than assumed.** Five die to the sibling suite: dropping the `take_candidate_region` call, and computing `own_question`, `queued_candidate`, `repair_child` or `unelapsed_backoff` as `false`. Three survive, each because the state it distinguishes is unreachable: a lineage generation holds no `LeaseOwner::Generation` lease so `releases_own_region`'s answer cannot matter at the close site; a candidate under a transaction still holds its queue position, so `owns_transaction` cannot fire where `queued_candidate` does not; and nothing reaches `refresh_task_state` terminal. Those three are stated in the notes rather than claimed tested. **Re-witnessed at the final head**, the same 56 mutations again: eight of them no longer match anything, because the code they named moved into the three helpers, and the eight helper mutations above stand in their place; of the remaining 48, 39 are killed and 9 survive, and every one of the nine is one of the deferred findings below. Every mutation run at either head carries its own recompile witness — a `Compiling upstroke` line and this tree's own test total, 131 at the base and 138 at the head — because the box's eight build slots are shared across the seventeen sweep worktrees and a run that reused another tree's binary would read as a survivor. **Deferred, six findings, each naming the file that should take it:** `QueueEntry::sequence` is written five times here and read nowhere in the crate, which is why three of this file's decisions about it cannot be witnessed (`SWEEP-FOLD-APPLY-QUEUE-SEQUENCE-UNREAD`, guard row 39 and `src/topology/queue.rs`); `OpenQuestion::origin` is public, written here and consulted by nothing, so recording a bare question as a verification park survives the suite (`SWEEP-FOLD-APPLY-QUESTION-ORIGIN-UNREAD`, guard row 40); a decline's clearing of execution backoff has no test, and neither half alone nor both together kills anything, although losing it would leave `backoff_pending` true forever and `derived_outcome` at `NotEnding` for a run with nothing left to run (`SWEEP-FOLD-APPLY-DECLINE-BACKOFF-UNWITNESSED`); `record_halt` being first-in-wins is unwitnessed, and a second halting settlement is a reachable trace (`SWEEP-FOLD-APPLY-FIRST-HALT-UNWITNESSED`); two guards are unreachable and kept as defence in depth — the interruption's return to `Pending`, which `check_dispatched` already guarantees, and the decline's sparing of a `Prepared` transaction, which `check_question_answered` refuses one step earlier (`SWEEP-FOLD-APPLY-UNREACHABLE-GUARDS`); and the two saturating counters above. **§13:** the notes file cited the retired 2026-08-12 packet in five places, three by labels (`INV-02`, `T-ATTEMPT`) no living document defines; each is repointed at `design/04`, `design/11`, `design/15` or `design/26` with the sentence quoted, the way rows 30 and 31 treated `INV-07` and `INV-18`. Two of its section headings were not grep strings — one matched nothing, one matched two different arms of `apply` — and every one of the 37 headings now resolves to exactly one line of the source, checked mechanically. `cargo fmt --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; the scoped suite green at 131 before and 138 after, with `topology::census` (20) and `the_rungs_allowance_is_counted_in_one_production_place`, the census that names this file's two `attempts_on_rung` writes and one `spends_allowance` consult, both green. |

`src/workspace_manager.rs` (queue row 11) reads every one of these files' call sites when it is
swept. That session should record improvements it notices in the children as rows here, which is
the cheapest form this second pass can take.
