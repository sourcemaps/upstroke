---
id: PR7-WRAPPERS-EMPTY-DOMAIN
severity: P1
disposition: deferred
category: security-trust
pr: 7
reviewed_sha: 1ff88a780916f27b0ef6de5816c68c1c2b92a70f
location: src/effects.rs:767
provenance: undetermined
first_bad:
guard: project owner — the post-v0.2 pass over PR3's layer
---

## Failure sequence

**Since #325 (2026-09-26)** the three routes below are refused where they are written, at item position in an allowing module, and a non-local `impl` a macro writes into a function body is refused by rustc; the route this finding records as executed now, and why it stays `deferred`, is under Remaining.

What this finding is *for* is `mechanism` (3)'s guarantee: a topology module cannot reach an effect through a legacy wrapper while enforcement stays green. **It does not hold, and the route that remains is executed.** A module that carries a recorded allow (`src/engine/coordinator.rs` is the one the reviews used; the same holds of every allowed, classified module a topology module can see) defines a macro that emits `pub(super) fn $name(path) { std::fs::write(..) }` and invokes it with a fresh name -> the function compiles under the module's EXISTING allow, so clippy reports nothing and no allowance is added -> the classification domain (`effects::externally_reachable_fns`, through `declared_fns`, `src/effects.rs:767`) reads a literal `fn name` in the source text, and the text spells `fn $name`, so the generated function is neither a classification obligation nor a multiplicity increment -> nothing classifies it and nothing denies it -> the production body of `park_question` in `src/engine/topology/integrate.rs` references it, a topology test calls it and reads back the bytes it wrote. Executed at `1ff88a78` by both lenses of #309's second review, each with clippy at **exit 0** and **185 tests passing**, and re-run at the head this file was restored at:

- **No module, no new allowance** — main lens, `~/orch-pr10/reviews/rfacade-2/main-evidence/09-inherited-macro.patch`. Control `09-inherited-macro-deny-control.patch`: a `deny` on the generated function makes clippy exit **101** at the write, so the inherited allow is what admits it.
- **Module keyword, allowance level and function name all substituted** — main lens `01-relocated-macro.patch`, regression lens `regression-evidence/11-macro-sibling-witness.patch`. Controls `01-relocated-literal-name-control.patch` and `regression-evidence/10-macro-literal-name-control.patch`: with the function name written literally the classification census exits **101**, so the census works against a written name and identifier substitution is what defeats it; `regression-evidence/12-macro-deny-control.patch` is the deny control.
- **An aliased `include!`** — main lens `05-aliased-include.patch`: `use std::include as rf2_read; rf2_read!("rf2_review.inc");` brings in a `.inc` holding an allowed inline module and its effectful function. Control `05-literal-include-control.patch`: the literal spelling exits **101** from `effects::tests::no_scanned_source_includes_a_file_no_scan_reads`, which recognises the word `include` before `!` and not the macro a name resolves to.

Those runs at the restored head are under `~/orch-pr10/facade-escape-evidence/r2-05-after/`. The cause is one thing, it is structural, and it is wider than expansion: every reading here — the placement scan, the fences, the module walk, the classification census, the multiplicity pins — is a hand-written recogniser of source text, and **whatever rustc compiles and a recogniser does not read is in no census**. It has two halves. **What expansion writes**: a source text does not contain a name, a module or an attribute that a macro or an aliased `include!` produces — the three executed routes above. **What the text itself writes and a recogniser misreads**: the fourth review of #309 executed two at `84123789` with no macro and no `include!` — `#[rustfmt::skip]` and `pub(super) mod`, U+200E, `rf4_child;` in `src/engine/attempt.rs`, whose recorded allow the child inherited, and `#[rustfmt::skip]` and `pub(crate) fn`, U+200E, a name in `src/engine/coordinator.rs`, which needed no new file, no new allowance and no record change — each with fmt, clippy and the whole effects suite green while a topology caller wrote its bytes, because three readers split tokens on three different sets of whitespace and none was the eleven code points rustc reads; and its regression lens executed one a level down, a legal `\u{…}` escape longer than the char-literal reader's window, which let a brace out of a literal, closed two `impl` blocks early and had two effectful paths admitted as one (`~/orch-pr10/reviews/rfacade-4/`). **The module-walk half fails open.** A `#[path]` is refused, so it fails closed; a `mod` declaration the walk does not read is refused by nothing — it is a child no guard judges, and the cross-check beside the walk shares the walk's reading and agrees with it. rustc reports none of this: its text-direction lints cover literals and comments, not the space between tokens. Four rounds across #306 and #309 each closed the spelling in front of them and left the next; #309's fourth round closed those three under one definition (below), and the class stands, because these are recognisers of the shapes this tree and its reviews have produced and not rustc's lexer and parser.

## What the change that takes this up should do

Owner, as the ledger records it: project owner — **the post-v0.2 pass over PR3's layer**. The second review of #309 names what a real fix needs: *an enforced restriction or expansion-aware classification* on the allowed legacy modules. That is an architecture decision, not a scanner extension, and #309 deliberately does not attempt one. `#![forbid(..)]` is rustc's text-independent mechanism and does not fit here: the witness's effect is compiled inside the allowed module, outside its caller's lint scope, and a file-level `forbid` with per-site `allow`s in the same file is `E0453`. **That is a statement about a module that carries the allowance, and it still holds there.** Where a file carries no allowance of a lint, `forbid` fits exactly, and since the fence change of 2026-09-21 it is what 54 of the 59 file-level fences write — the bullet for that change below says what it closes and what it does not.

**What #325 leaves for that pass** (the evidence is under Remaining): an item written inside a function body and reached by symbol. `#![forbid(unsafe_code)]` refuses each piece of it in a file that forbids it -- a `no_mangle` definition, an `export_name` definition and an `unsafe extern` block, measured on rustc 1.97.1 and 1.85.0 -- so forbidding `unsafe_code` in every topology module and in every allowing file that needs no `unsafe` is a direction worth measuring; it cannot be one line at a crate root, because the process funnels use `unsafe`, and it is not measured against this tree.

**Closed, and held by tests, so the next pass does not redo it.**

- By #306: the domain is derived from `production_code` rather than the truncating region; the 62 names that added are classified against a stated module-closed rule; every library record carries a real crate path; the five nameable effectful engine wrappers are denied by path; `src/engine/topology.rs` and the five children of the facade that write no allow deny all three governed lints, and `every_child_the_engine_facade_declares_re_denies_or_records_what_it_inherits` walks the facade's declarations recursively.
- By #309: **the facade's allowance is gone.** The v0.1 conductor's six entry points and their four seams moved, unchanged, into `src/engine/coordinator.rs` and `src/engine/resume.rs`; the facade re-exports the six, denies the three governed lints at file level, and **holds no code** — its leading attributes, `mod x;` declarations and `use` re-exports only, by whitelist (`the_engine_facade_allows_no_governed_lint_and_refuses_both_escape_routes`, `a_declaring_module_holds_declarations_and_re_exports_and_nothing_else`). Its allowlist row and its `FROZEN_LEGACY_ALLOWLIST` entry are removed, so the frozen legacy section is what PR5 froze. **Both routes the fourth review of #306 reported are refused by clippy**, exit 101 at the write: an attribute-free inline module in the facade, and a function placed directly in it, private or not (`PR306-FACADE-INLINE-ESCAPE`, whose file is deleted). The facade forms of the expansion escape — `include!`, and a macro substituting `mod` and `allow`, in the facade — are refused by the whitelist. The ten moved functions are classified effectful and denied by path, and all six public entry points are refused from a topology module through their re-exports.
- Also by #309, for names the source writes: a second callable under an already-classified bare name is refused unless its count is pinned (`every_name_more_than_one_callable_bears_is_pinned_by_its_count`). A pinned **effectful** name is admitted in two shapes only, because no record can classify and deny a second path: every bearer written directly in one and the same braces, reached through inline `mod`s alone (`cfg` twins), or one trait's declaration and the impls written under its own name beside it. Which braces hold a bearer is read **by position**; what a header spells decides only whether a scope is one of those shapes, and a header the reading cannot place refuses the pin (`an_effectful_name_is_shared_only_by_bearers_of_one_path`, `a_shared_effectful_name_is_placed_by_its_braces_and_not_by_what_a_header_spells`). The first form of this rule compared what the owners' headers spelled, and the third review of #309 executed two distinct effectful paths through it, clippy at exit 0 and 186 tests passing each time: const-generic braces that lost both `impl` headers, and one alias for two receivers in two `const _` blocks. Both, and the control of each, are held against the real `src/engine/resume.rs` and its real record. The fourth review passed two receivers as one once more, under the position reading and not through it: a legal Unicode escape made long by underscores outran the char-literal reader's window, a brace survived as code, both `impl` blocks closed early and both bearers read as file-scope twins (clippy at exit 0, 188 tests passing, the bytes written). A position is only as good as the tokenization under it; an escape now ends where rustc's lexer ends it, and that witness and its control are held the same way (`a_fn_behind_any_separator_is_unclassified_and_a_legal_escape_hides_no_brace`). **This is a reading of the text and not callable identity**: it evaluates no `cfg` and resolves no name (the second shape takes the impl's trait to be the one declared beside it, which holds where that declaration is compiled), it judges `effectful` rows only, and like everything here it sees the bearers the source writes. `pub(in path)` and `extern` forms are in the domain. Two omissions of the reading itself, found in #309's third round and executed as parser omissions only, are closed and pinned by `the_reachable_fn_parser_finds_each_shape_this_tree_uses`: a `;` inside brackets no longer ends a header (`impl Trait for [u8; 4]`, a default body returning an array), and a function's name is read by what ends it, so a name after a comment or a line break, a non-ASCII name and a raw identifier (`r#raw` was read as `r`) are in the domain under their own names. Neither moves a name at #309's head (759 in the 54 classified modules, 2267 in the tree).
- By #309's fourth round, for what the text writes and a recogniser misread: **one definition of a token separator, rustc's eleven code points (`RUSTC_WHITESPACE` in `src/effects.rs`), applied in the tokenizer every reader here starts from**, which writes the six that a byte test or `char::is_whitespace` can miss — U+000B, U+0085, U+200E, U+200F, U+2028, U+2029 — as spaces of the same byte length, so the module walk, the name reader, the facade whitelist, the `include!` reader, the file-level lint reader and the placement scan all read what rustc reads between tokens; the name reader names the definition itself, and the `trait`, `impl` and `for` readers read a keyword as a whole word and ask nothing of what follows it, where they wanted exactly one U+0020 (a tab after `trait` or `impl`, or a keyword that touches its neighbour as rustc allows — `impl::path::Trait for X`, `Trait<T>for X`, `for&'a str` — hid a default body or a trait impl's methods; found by reading, executed as parser omissions only, the bypass each would allow reasoned; the price is an inherent impl with a higher-ranked bound in its header reading as a trait impl, which over-reads and fails closed); the walk reads an attribute's name from the tokenizer's text, so `#[`, U+200E, `path = ".."]` is refused like any `path`; and a char escape ends where rustc's lexer ends it, at the next unescaped quote on its line, whatever its length. Held by tests: the definition by value (rustc's own answer -- each of the eleven builds with nothing reported, U+00A0 and U+200B do not -- is compiled in the round's evidence, `~/orch-pr10/facade-escape-evidence/r4-03-rustc-separators/`, not in the suite), and the main lens's first escape read for all eleven separators against this tree's real `src/engine/attempt.rs` (`every_separator_rustc_reads_is_one_every_reader_here_reads`); its second escape a classification obligation for all eleven against the real `src/engine/coordinator.rs`, and the regression lens's balanced witness and its control both refused against the real `src/engine/resume.rs` (`a_fn_behind_any_separator_is_unclassified_and_a_legal_escape_hides_no_brace`); the eleven in the name reader, the walk and the whitelist (`the_reachable_fn_parser_finds_each_shape_this_tree_uses`, `the_module_scan_reports_inline_modules_at_every_depth_with_what_they_write`, `a_declaring_module_holds_declarations_and_re_exports_and_nothing_else`); each legal escape form beside a brace (`a_multi_byte_char_literal_does_not_desync_the_blanker`). The reviews' compiled witnesses are refused by the effects suite at the repaired head (`~/orch-pr10/facade-escape-evidence/r4-05-after/`). The readers' changes move no name: 759 in the 54 classified modules, unchanged, and 2268 in the tree, which is 2267 and the one function the round adds, `is_rustc_whitespace`.
- By the fence change of 2026-09-21: **54 of the 59 files that fence a governed lint at file level write `forbid`, not `deny`**, one keyword each and nothing else moved. `forbid` is the one level an inner `allow` or `expect` cannot lower: rustc refuses the attempt as `E0453`, after macro expansion and whatever the attribute's spelling, in the forbidding file and in every module file below it, so **inside those files the level no longer depends on any reader recognising anything**. The readers are not repaired; for a forbidden lint they are bypassed. The five that stay `deny` are the five where `forbid` does not compile, measured by flipping all 59 — 15 `E0453`, every one set by `src/engine/mod.rs` (over `attempt.rs`, `coordinator.rs`, `resume.rs`, `tests.rs`), `src/agent/proc/test_support/readiness.rs` (over its own six per-site `#[expect]`), or `census.rs`, `exec.rs`, `resolve.rs` under `src/runner/container/` (each over its `tests.rs`) (`~/orch-pr10/forbid-54-evidence/probe-59-clippy.log`). The 54 were derived from the tree and agree line for line with the measurement session's `forbid-54.patch` (`derived-54-files.txt`). Held by `every_fence_of_a_governed_lint_forbids_wherever_forbid_would_compile`, which reads the sources at run time, holds no list, finds a fence with rustc's whitespace removed so that `# ![deny(..)]` is still a fence to it, and excuses a `deny` only by an allowance of a lint that fence names, in the file or below it: each of the 54 dropped back to `deny` in turn fails naming exactly that file, 54 of 54, as do the three spelled-apart forms and a `cfg_attr` (`pin-matrix.tsv`). **What it does not hold**: a fence deleted outright passes (same file, row `fence-deleted-MISS`), and one attribute fencing three lints is excused whole by an allowance of one, so eight file-and-lint pairs in the five excused files could forbid if their attribute were split. **What the change does not reach is under Remaining.**
- By the attribute-token repair of 2026-09-26, for the route the fifth review of #309 executed: **the readers read an attribute's tokens where rustc reads them.** `#`, `!`, `[` and a lint attribute's `(` are separate tokens, and rustc reads whitespace and plain comments between any two of them: `# ![allow(..)]`, `#! [allow(..)]`, `#![allow (..)]`, `#/* c */[allow(..)]` and U+200E between each pair are applied under `-D warnings` as the joined spelling is. `effects::governed_allows` (the placement census), the module walk (`census_domain::scan_modules`) and `lint_levels::leading_inner_attributes` wanted them touching, so an allow the compiler applied was one the placement census did not record. Both lenses of that review executed it at `935fcf31`, behind `#[rustfmt::skip]`, with no macro, no `include!`, no new file and no record change, every gate green and the bytes written (`~/orch-pr10/reviews/rfacade-5/`: `regression-evidence/adjacency-live.patch` and its logs, `main-evidence/adj-w2.patch` and the `adj-w2-*` logs); the walk read `#`, a space or U+200E, `[path = ".."] mod y;` as a plain `mod y;` (`main-evidence/p1-reader-probe.log`, rows `MODWALK`); pre-existing (`regression-evidence/adjacency-provenance.json`); closed by the fence change for a forbidden lint inside a forbidding file and open everywhere else. The file-level lint reader had read the tokens apart since #318's fifth round and answered undecided, not `allow`, for an allowance the placement census did not record. All three now find an attribute through one function, `effects::attribute_open`, which skips rustc's whitespace and plain comments in the source and stops at a doc comment or a literal, as rustc does; `governed_allows` finds the keyword's `(` the same way and records a lint path without the separators between its tokens, and `is_module_level` reads a spaced visibility and `mod`. The same readers now name what rustc names where they did not: `normalize_lint` reads a raw segment, Clippy's two renames onto a governed lint and its two prefixless group aliases, from the tables the prologue reader already used — clippy applies each, a raw name alone and the others with `renamed_and_removed_lints` allowed beside them, and the placement census recorded none of them — and the walk reads `#[r#path = ".."]`, which rustc honours, as `path`. Held by `the_placement_census_reads_an_attribute_whatever_rustc_reads_between_its_tokens`, `the_placement_census_names_a_lint_as_clippy_does_and_no_further`, `the_module_walk_reads_an_attribute_whatever_rustc_reads_between_its_tokens` and `the_prologue_readers_read_an_inner_attribute_whatever_rustc_reads_between_its_tokens`, and compiled — against a `forbid` by `an_allowance_rustc_reads_whatever_separates_or_spells_its_tokens_is_one_the_placement_census_reads`, against a `deny` by the decided table of `the_file_level_lint_reader_answers_what_rustc_does`; with `a3767bcc`'s `src/effects.rs` under them all six fail, and each part of the repair reverted alone fails the witness written for it. **No reading moved in this tree**: over its 189 sources every reader reports byte-identically at `a3767bcc` and after the repair, and over the 11,041 `.rs` files of the box's cargo registry (386 crates) the readers differ in two readings, both in `pest_meta-2.9.0/src/grammar.rs`, a generated file that spells its attributes apart, and both of attributes rustc applies. **`cargo fmt --check` is not the second barrier this paragraph used to say it was.** rustfmt 1.9.0 refuses the whitespace-separated spellings unless `#[rustfmt::skip]`, `#![rustfmt::skip]` or `cfg_attr(rustfmt, rustfmt::skip)` covers them, but with no skip it passes a comment between the tokens (`#/* c */[allow(..)]`, `#/* c */![..]`, `#!/* c */[..]`, `#[allow/* c */(..)]`, `clippy::/* c */disallowed_methods`), a raw, renamed or alias lint name, and a spaced attribute inside a brace-delimited macro invocation; the comment spellings were as open as the others until this repair.
- By #325's second and third rounds (2026-09-26), **options 1 and 2 of the owner-delegated ruling on this finding: the restriction the second review of #309 asked for, enforced by rustc where rustc can and by one text property where it cannot.** Option 1: the eight file-and-lint allowance pairs no call uses on any of the three CI targets are dropped from their attributes and rows and fenced -- `forbid` where nothing below the file allows the lint, `cfg_attr(not(test), forbid(..))` where a whole-file test child still does -- `src/runner/mod.rs` forbids `disallowed_macros` in production, and `readiness.rs` splits into `deny(disallowed_methods)`, the one level its six expectations can narrow, and `forbid` of the other two; each funnel's level for each lint, and readiness's, is pinned exactly (`every_child_module_of_the_container_funnel_states_its_own_lint_level`, `the_readiness_expectations_are_per_site_and_both_records_say_so`). And **`#![forbid(non_local_definitions)]` at the three crate roots**, pinned by `every_crate_root_forbids_non_local_definitions` (each root's line removed fails it naming the file), which is what now refuses the expression-position route this paragraph recorded as executed on a scratch crate: executed on the real tree at `c0d59755`, a macro written into the body of the existing `topology_question_options` in `src/engine/coordinator.rs` generating `#[allow(non_local_definitions)] impl Rf3Host { pub(crate) fn rf3_fresh(..) }` over `std::fs::write`, with a `park_question` caller, is clippy exit 101, `E0453` at the allow it writes, and with `src/lib.rs`'s line removed (control) clippy exits 0, the live test writes the bytes, and the effects suite fails exactly that pin. What the lint does not reach was measured on rustc 1.97.1 and 1.85.0: a non-local `impl` is refused in a function body, a named `const` or `static`, an array length, an enum discriminant, a const-generic default, an associated `const`, an inline `const`, a closure in a `static`, a trait's default body and a `const _` inside a function, and **not linted in a module-level `const _` initializer**, nested or inside an inline `mod`, where a macro defines a method another module calls and the crate builds. Option 2 is for that and for item position: **no macro invocation outside a function body in a production file where some governed lint is not forbidden** (`every_macro_invocation_where_a_governed_lint_is_not_forbidden_is_inside_a_function_body`; the reader is `census_domain::macro_invocations_outside_function_bodies`, driven over 26 refused and 17 accepted positions by `the_macro_position_reader_refuses_every_position_outside_a_function_body`). The domain is derived from each file's own production level for each lint, else its nearest ancestor's, not from the allowlist, so a child that drops its fence under an allowing parent enters it; a file where all three are forbidden is outside it, because what a macro writes there can do nothing its caller could not. The line is the function body rather than a list of positions: module, `impl`, `trait` and `extern` item position, every `const` and `static` initializer, an enum discriminant, a type, a header and an attribute's arguments are all outside one. The four production `thread_local!` invocations and their five statics moved to `src/util/thread_barriers.rs`, `src/runner/host/counters.rs` and `src/rundir/cleanup_scopes.rs`, which forbid all three, and a `format!` in a thiserror attribute into a function. **The three executed routes of this finding, re-run with their own patches at `c0d59755`**: `09-inherited-macro`, `01-relocated-macro` and `05-aliased-include` each still build under clippy `-D warnings` (exit 0) and still write their bytes through the `park_question` caller, and the effects suite fails each on exactly one test, this census (208 passed, 1 failed); and the same macro, without the allow, in a module-level `const _` of `src/engine/coordinator.rs` -- the position the lint does not reach -- is clippy exit 0 with the crate-root forbid in place and writes its bytes through `park_question`, and the effects suite fails exactly this census (208 passed, 1 failed) (`~/findings-sweep/orch-p1/p1-six-modules-r3/`, `routes-at-c0d59755/`, `nonlocal-at-c0d59755/`).

**Remaining.** **One executed route, and the class.** **An item a macro writes inside a function body, reached by symbol rather than by name.** A function body keeps what expansion writes there local to the body, and the crate roots' forbid refuses the two ways it can reach out by name, a non-local `impl` and a `#[macro_export]` macro. It does not refuse the linker: a macro invoked in an existing function of an allowing module writes `#[unsafe(no_mangle)] pub extern "C" fn` over `std::fs::write`, and a module that forbids all three governed lints declares it in an `unsafe extern "C"` block as a `safe fn` and calls it. Executed on a scratch crate with the tree's shape -- the crate-root forbid, the allowing module's allow, the caller's forbid, the tree's `clippy.toml` denial of `std::fs::write` -- under clippy `-D warnings` on rustc 1.97.1 and 1.85.0: exit 0 both, the binary writes the bytes, and the control that calls `std::fs::write` from the forbidding module is refused (`~/findings-sweep/orch-p1/p1-six-modules-r3/linkage-route/`). Neither #325 census reads it: the invocation is inside a function body, and nothing refuses a symbol-export attribute or an `extern` block. A function body is where #325 drew the line, so this is the one position the line does not settle. **Not read, reasoned**: the items a derive or an attribute macro from a dependency writes -- no `!` is spelled, their output is fixed by `Cargo.lock`, and `macro_rules!` attributes and derives are refused as unstable on stable and do not parse on 1.85 -- and the class, which stands: the readers are recognisers of the shapes this tree and its reviews have produced and not rustc's lexer and parser. Readers outside the three the attribute-token repair was about still want an attribute's tokens touching, their directions not measured: `production_region` and `production_code` (`#[cfg(test)]`), `owner_header`, the engine facade's whitelist and header check, the parity test's restatement count, `src/effects/tests/contract_mappings.rs` (`#[test]`), and the cfg census in `src/effects/tests/cfg.rs`, where an unread `cfg` would be a platform it does not count (reasoned); for the macro-position census the one that matters, `production_code`, only ever leaves test code in, which refuses more. **Re-measured at c0d59755** with the tree's own readers: **45 files carry an allowance of a governed lint**, the same 45 as the non-empty rows of `effects/allowlist.toml`, 43 of them at file level in the production build; by lint, **43 allow `disallowed_methods`, 23 `disallowed_types`, 12 `disallowed_macros`** (44, 23 and 19 at `a3767bcc`). **Four files fence with `deny`**, `src/engine/mod.rs` (`disallowed_methods` only), `src/agent/mod.rs`, `src/runner/mod.rs` (`disallowed_methods` and `disallowed_types`) and the test-only `readiness.rs` (`disallowed_methods` only), and each denies only lints that an allowance or expectation in or below it lowers, so none of them can forbid; the pair set `readiness.rs` left unsplit is split. **85 files forbid at least one governed lint** in the production build (226 of their 255 file-and-lint pairs; 73 and 206 of 219 at `a3767bcc`), and **17 of the 45 are among them**. Inside the 45, in production code, no macro invocation outside a function body (held by the census above); the test-only files keep theirs, three `thread_local!` and the `logged` `macro_rules!` in `src/runner/container/resolve/tests.rs`, compiled into no production target. A body edit to an existing `effect_free` function needs no expansion and is filed on its own as `PR325-A-BODY-EDIT-TO-AN-EFFECT-FREE-FN-IS-UNREAD`. **Disposition: `deferred`, under this finding's project-owner guard**: it carries an executed route, and the decision on it is the owner's. A module name that is not ASCII is refused by the walk, which fails closed. **Executed at `db94a8ba`, and closed in a file that forbids**: the same substituting macro in a file that carries a `deny` rather than an allow — a fenced sibling of the facade, a topology file — overrides that file's fence with an allow `effects::governed_allows` does not read. This sentence called it reasoned until 2026-09-21. `01-relocated-macro.patch`'s macro, which writes its own `#[$level(clippy::disallowed_methods)]` with `allow`, moved unchanged into the fenced `src/engine/assembly.rs` at the literal base with its caller in `park_question`: clippy exit 0, and the effects suite with the live caller passes 189 tests, the bytes verified (`~/orch-pr10/forbid-54-evidence/witness/`, rows `B2`); at the changed head with `assembly.rs` back at `deny`, exit 0 again (`M3`), and with its `forbid`, exit 101, `E0453` at the macro's own `#[$level(..)]` (`M4`). Hosted where the reviews wrote it, in `coordinator.rs`, `09-inherited-macro.patch` and `01-relocated-macro.patch` were each still exit 0 at that changed head (`M1`, `M2`); since #325 they are refused by the macro-position census, which reads an invocation at item position whatever the macro is called. `09`'s macro writes no allow of its own — it inherits its host's — so in a fenced file it was already refused at `deny` and is refused the same way at `forbid`, `error: use of a disallowed method` (`M5`, `M6`); `forbid` changed nothing for it and is not what refuses it. (This was filed separately as `PR309-AN-ALLOW-THAT-ARRIVES-BY-EXPANSION-IS-UNREAD` during #309 and is folded in here: it is the same cause, the same owner and the same decision, and a second label for one class is how a P1 gets declared fixed under another name — which is also why the fence change files nothing new and closes nothing here: it narrows this finding and this finding stays `deferred`.) **Reasoned, not executed**: code carried by a `const` or `static` rather than a `fn`, which stays filed as `PR7-CLASSIFICATION-DOMAIN-READS-FNS-ONLY`.

Carried in `reviews/FINDINGS.md` §2, "Open — carried deliberately, with an owner", confirmed still carried by the full-ledger audit of 2026-08-31 (§39); deleted on #306 with a `fixed` row that two reviews found premature and restored at that pull request's close-out; deleted again by #309's first round and restored by its second, when both lenses executed the routes above. Narrowed, not closed, by #325 (2026-09-26): its three executed routes are refused at item position, and the one this finding records as executed now is reached by symbol from inside a function body. The row carried no severity label; **P1** here is the migration's judgement from the consequence described above, not the reviewer's own word.
