---
id: PR120-PARENT-CONTRACTS-OMIT-NEW-OUTCOMES
severity: P3
disposition: deferred
category: docs-contract
pr: 120
reviewed_sha: 62816dd4f3263e8dd252b009d3b0a6c999e5e9cc
location: src/workspace_manager.rs:1836
provenance: introduced_by_feature
first_bad: be57a33 (the runner's walk); e4bf5dc and 61398c6 (the two named before)
guard: deferred by owner direction (P3 and lower recorded): every parent # Errors list is rewritten in the parent's sweep, standards/SWEEP.md queue row…
---

## Failure sequence

the ref funnels return containment refusals and I/O errors while their `# Errors` promise ref and Git errors only -> `revalidate_hooks_path` in the runner changes the error surface of `worktree_records`, both commit-tree APIs and every other Git caller -> the body named two contracts where many omit a demonstrated outcome

## What the change that takes this up should do

deferred by owner direction (P3 and lower recorded): every parent `# Errors` list is rewritten in the parent's sweep, `standards/SWEEP.md` queue row 11; the body names each contract; `a_hook_written_into_hooks_none_at_the_before_hook_refuses_the_worktree_add_and_never_runs` pins one outcome the ref docs omit

Recorded by the PR #120 `src/workspace_manager/containment.rs` sweep; the row is carried out of `reviews/FINDINGS.md` in the words it
was written in.
