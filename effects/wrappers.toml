# The wrapper classification.
#
# `decisions.effect_site_inventory.mechanism` (3): "at PR5 every pubfn of a legacy
# or shared module is classified effectful or effect-free by review; effectful
# wrappers are added to the disallowed list themselves, so a topology module
# cannot reach an effect through a legacy wrapper while legacy modules keep
# calling them under their module allow".
#
# FOUR CLASSES, not two, because "effectful" alone loses the distinction the same
# sentence draws two clauses later ("EventLog::open and EventLog::append are
# classified as the Event funnel (site-taking) rather than as wrappers"):
#
#   funnel                -- issues its effect inside a site-taking funnel call.
#                            NOT denied: it is the sanctioned path.
#   effectful             -- reaches an effect, takes no site, and is nameable as
#                            a path. DENIED in clippy.toml; the test
#                            `every_effectful_wrapper_is_on_the_disallowed_list`
#                            asserts both directions.
#   effectful_unnameable  -- reaches an effect and cannot be named by a clippy
#                            path: a private module, a trait-dispatched method,
#                            or the binary crate root. Privacy or dispatch
#                            already denies it to a topology module; recorded so
#                            the absence from clippy.toml is a decision.
#   effect_free           -- reaches no effect of its own.
#
# THE BOUNDARY, STATED. This classifies reach at the MODULE'S OWN BOUNDARY: a fn
# whose own body performs or hands out an effect. It is not the transitive
# closure over the crate, because the transitive closure of "reaches an effect"
# in a program whose purpose is to perform effects is the whole program -- it
# would deny `engine::run_harness` and every CLI entry point beneath it. The
# residue that leaves is exactly one thing: a topology module reaching an effect
# through a chain of undenied public functions, and that is closed from the other
# end by `effects::tests::no_topology_module_calls_a_funnel_in_production`.
#
# Regenerate the membership (never the classification) with
# UPSTROKE_REGENERATE_EFFECT_ARTIFACTS=1 cargo test -p upstroke effects::

[[module]]
path = "src/workspace_manager.rs"
crate_path = "upstroke::workspace_manager"
funnel = [
    "add_site",
    "add_worktree",
    "candidate_commit_tree",
    "candidate_stage",
    "candidate_write_tree",
    "compare_and_swap_ref",
    "create_execution_root",
    "create_ref_zero_old",
    "delete_ref_expected_old",
    "proposal_cherry_pick",
    "remove_execution_root",
    "remove_intent_site",
    "remove_site",
    "remove_staging_leftovers",
    "repair_materialize",
    "snapshot_commit_tree",
    "verify_worktree",
    "write_intent_site",
]
effectful = []
effectful_unnameable = []
effect_free = [
    "acted_through",
    "acted_through_paths",
    "add_snapshot",
    "argv",
    "assert_publishable",
    "base",
    "candidate_diff",
    "changed_paths",
    "commit_parent",
    "commit_tree_sha",
    "common_git_dir",
    "derive",
    "direct_ref_target",
    "execution_root",
    "execution_root_of",
    "from",
    "intent_path",
    "intents",
    "names",
    "names_in_another_case",
    "object_directory",
    "object_exists",
    "parse",
    "private_root",
    "proposal_state",
    "quiescence",
    "reclaim_intents",
    "refs_under",
    "refuse_unexpected_refs",
    "remove_intent",
    "remove_snapshot",
    "remove_worktree",
    "repo_key",
    "repo_key_v1",
    "resolution_manifest",
    "resolved_conflicts",
    "revalidate",
    "row",
    "slot_path",
    "temporary_object_files",
    "unreachable_objects",
    "unresolved_conflicts",
    "worktree_records",
    "write_intent",
]

# The five production children the `m3-rundir` split gave `src/rundir.rs`, added
# in the same commit as the split. Seventeen externally reachable `fn`s left the
# parent's file, and a name that leaves the domain is a name nobody has to
# classify any more -- so the children join `CLASSIFIED_MODULES` by name, which
# is the only form that list has: its census reads every entry as a source file,
# so there is no prefix to widen `src/rundir.rs` into. `C-002` is the standing
# finding that the roll-call is hand-maintained, and it is not this split's.
#
# THE REPOINT IS A NET WIDENING OF FOUR, and it is written down rather than
# implied: 17 names leave the parent's block and 21 arrive in the children's,
# because four names are classified here that the parent's block did not carry
# separately.
#
#   * `first_line` and `first_line_within` were PRIVATE `fn`s of
#     `src/rundir.rs`, below the classification threshold entirely. The
#     extraction made them `pub(super)` in `classify.rs` so `rundir::tests` can
#     still drive them by name, and a `pub(super) fn` is externally reachable, so
#     they must be classified now. `pub(super)` here is `rundir` and its
#     descendants and nothing wider.
#   * `public_dir` and `fmt` each named TWO items inside the parent's file -- the
#     free `public_dir` and `PrivateHalfProof::public_dir`, `RepoKey`'s `Display`
#     and `RetainReason`'s -- and this record classifies by bare name, so one
#     entry covered both. The split separates each pair into different files, so
#     each half is now classified on its own. That is why they are not among the
#     seventeen: the parent still derives its half.
#
# Both are widenings. No name that was classified before is unclassified now.
#
# Every one of the five is `effect_free`, and that is what the split was arranged
# to make true: no funnel and no effect site moved. Each child DENIES all three
# governed lints at file scope and takes no `effects/allowlist.toml` row -- the
# whole run-directory, answer and lock funnel surface, its atomic publication
# primitives and its platform locking stayed in the parent, which keeps the
# allowlist identity.
[[module]]
path = "src/rundir/classify.rs"
crate_path = "upstroke::rundir::classify"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "classify_run_dir",
    "first_line",
    "first_line_within",
    "run_started_sha256",
]

[[module]]
path = "src/rundir/discovery.rs"
crate_path = "upstroke::rundir::discovery"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "describe",
    "find_question",
    "husk_report",
    "latest_run",
    "list_husks",
    "list_runs",
    "resolve_run_id",
    "run_dir_names",
]

[[module]]
path = "src/rundir/names.rs"
crate_path = "upstroke::rundir::names"
funnel = []
# The eight production children the `m4-workspace` split gave
# `src/workspace_manager.rs`, added in the same commit as the split. Twenty-six
# names left the parent's file, and a name that leaves the domain is a name
# nobody has to classify any more -- so the children join `CLASSIFIED_MODULES`
# by name, which is the only form that list has: `reachable_fns_are_classified`
# joins every entry onto the manifest root and reads it as a source file, so
# there is no directory prefix to widen `src/workspace_manager.rs` into. `C-002`
# is the standing finding that the roll-call is hand-maintained rather than
# derived, and it is not this split's to repair.
#
# THE REPOINT IS A NET WIDENING OF NINETEEN, and it is written down rather than
# implied: 26 names leave the parent's block and 45 arrive in the children's.
# The difference is two effects, both of them widenings:
#
#   * SEVENTEEN names were PRIVATE `fn`s of `src/workspace_manager.rs`, below
#     the classification threshold entirely. The extraction made them
#     `pub(super)` in a child so the parent (and, for `is_reparse_point`, the
#     suite) can still call them by name, and a `pub(super) fn` is externally
#     reachable, so they must be classified now. `pub(super)` here is
#     `workspace_manager` and its descendants and nothing wider. They are
#     `canonical_prefix`, `is_at_or_inside`, `is_reparse_point`,
#     `refuse_reparse_points`, `refuse_unreal_directory` and `strip_verbatim`
#     (containment); `apply`, `funnel` and `point` (hooks); `from_intent_name`,
#     `safe_component` and `validate` (naming); `refuse_expected_old` and
#     `refuse_malformed_object_id` (object); `parse_worktree_records` and
#     `registration_checkout` (parsers); and `administrative_residue_at`
#     (residue).
#   * `fmt` and `new` each named TWO items inside the parent's file -- the
#     `Display` impls of `SnapshotName` and `VerifyFailure`, and the
#     constructors of `HarnessEffects` and `ResidueTarget` -- and this record
#     classifies by bare name, so one entry covered both. The split separates
#     each pair into different files, so each half is now classified on its own.
#     That is why they are not among the seventeen: the pair was already
#     classified, once.
#
# Both are widenings. No name that was classified before is unclassified now.
#
# Every one of the eight is `effect_free` apart from the one `funnel` row the
# parent already carried, and that is what the split was arranged to make true:
# no funnel and no effect site moved. Each child DENIES all three governed lints
# at file scope and takes no `effects/allowlist.toml` row -- an allowance is what
# that file records, and these modules take none. Every `Command`, every `fs`
# write, every durability barrier and every platform removal retry stayed in the
# parent, which keeps the allowlist identity.

# The four containment predicates plus the two platform helpers they are built on. Every one observes -- `fs::symlink_metadata` and `fs::canonicalize` -- and none of the six is a governed primitive; nothing here creates, renames or removes a path.
[[module]]
path = "src/workspace_manager/containment.rs"
crate_path = "upstroke::workspace_manager::containment"
funnel = [
]
effectful = []
effectful_unnameable = []
effect_free = [
    "canonical_prefix",
    "is_at_or_inside",
    "is_reparse_point",
    "refuse_reparse_points",
    "refuse_unreal_directory",
    "strip_verbatim",
]

# The observer seam and the funnel protocol. `funnel`, `point` and `apply` perform no effect of their own: `funnel` takes the primitive as a closure and the parent supplies it, so the effect and its allowlist row stay in `src/workspace_manager.rs`. `apply` can end the process on `Injection::Kill`, and `std::process::abort` is not a governed primitive.
[[module]]
path = "src/workspace_manager/hooks.rs"
crate_path = "upstroke::workspace_manager::hooks"
funnel = [
]
effectful = []
effectful_unnameable = []
effect_free = [
    "apply",
    "consult",
    "durability_ledger",
    "funnel",
    "harness",
    "ledger",
    "new",
    "phase",
    "point",
    "poisoned",
    "recording_durability",
    "refusal_cause",
]

# Slot and snapshot name arithmetic, and the intent record they are written into. Pure string and path work; not one of these reads or writes the filesystem. `Slot`'s five effect-site accessors are deliberately NOT here -- they stay in the parent, where the site-inventory census reads them by path.
[[module]]
path = "src/workspace_manager/naming.rs"
crate_path = "upstroke::workspace_manager::naming"
funnel = [
]
effectful = []
effectful_unnameable = []
effect_free = [
    "as_str",
    "fmt",
    "from",
    "from_intent_name",
    "gates",
    "deserialize",
    "expecting",
    "id",
    "incarnation",
    "integration",
    "integration_review",
    "intent_kind",
    "intent_name",
    "kind",
    "new",
    "relative",
    "review",
    "run_id",
    "safe_component",
    "serialize",
    "slot",
    "try_from",
    "validate",
    "visit_map",
    "visit_str",
]

# Object-id well-formedness and the refusals every ref transition is gated on. Predicates over a `&str`; the transitions they guard are the parent's funnels.
[[module]]
path = "src/workspace_manager/object.rs"
crate_path = "upstroke::workspace_manager::object"
funnel = [
]
effectful = []
effectful_unnameable = []
effect_free = [
    "is_null_object_id",
    "is_object_id",
    "refuse_expected_old",
    "refuse_new",
]

# Decoders for the bytes Git's plumbing hands back. Functions over `&[u8]`; the commands whose output they read are run by the parent, inside its funnels.
[[module]]
path = "src/workspace_manager/parsers.rs"
crate_path = "upstroke::workspace_manager::parsers"
funnel = [
]
effectful = []
effectful_unnameable = []
effect_free = [
    "changed_path_records",
    "decode_changed_paths",
    "decode_index_path",
    "parse_worktree_records",
    "plain_record_paths",
    "registration_checkout",
    "stage_record_paths",
]

# The read-only residue classifier. `residue_classified_sites` keeps the `funnel` class the parent's row gave it -- it derives its answer from the frozen site enums and is reached as part of that surface -- and every other name here observes: no object is written, no ref moved, no index touched. The Git inspections it reads through are the parent's helpers, and so is every process start.
[[module]]
path = "src/workspace_manager/residue.rs"
crate_path = "upstroke::workspace_manager::residue"
funnel = [
    "residue_classified_sites",
]
effectful = []
effectful_unnameable = []
effect_free = [
    "administrative_residue_at",
    "at",
    "classify_object_residue",
    "element_breaks_quiescence",
    "from_base",
    "new",
    "observed_residue_elements",
    "published",
    "repository",
    "worktree",
]

# Four value types and their constructors and accessors: `ObjectId` (validated once, at `new`), `SnapshotObject` (the role of an input id and its `rev-parse` peel), `SnapshotInput` and `Snapshot` (private fields, built by the parent's funnel through `new`, read through accessors). Every function is a check or a field read over owned values; the funnel that resolves an input against the repository and checks a snapshot out is the parent's.
[[module]]
path = "src/workspace_manager/snapshot_ref.rs"
crate_path = "upstroke::workspace_manager::snapshot_ref"
funnel = [
]
effectful = []
effectful_unnameable = []
effect_free = [
    "as_str",
    "ephemeral",
    "fmt",
    "head",
    "id",
    "new",
    "object_type",
    "path",
    "peel",
    "slot",
]

[[module]]
path = "src/rundir/ownership.rs"
crate_path = "upstroke::rundir::ownership"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    # Conjunct 12's classification, read-only and total: it takes the stat that
    # was already performed and answers whether it *proves* the private commit
    # record absent. Only `NotFound` does.
    "commit_record_proves_absence",
    "prove_private_half_ownership",
    "public_dir",
    "run_id",
    "target",
]

[[module]]
path = "src/rundir/retention.rs"
crate_path = "upstroke::rundir::retention"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "fmt",
    "kind",
    "name",
    "owner_field",
]

# The worktree record, the quiescence a reuse demands and the reasons the answer can be no. `OpenRecord` (`at`, one reader per attribute, `close`) applies the attribute grammar to the bytes the parser feeds it and the record's shape rule when it closes, and is the one way a record is built; the record's methods read fields; `fmt` is `VerifyFailure`'s `Display`. The verification that produces these values runs a Git child and is the parent's.
[[module]]
path = "src/workspace_manager/worktree.rs"
crate_path = "upstroke::workspace_manager::worktree"
funnel = [
]
effectful = []
effectful_unnameable = []
effect_free = [
    "at",
    "bare",
    "branch",
    "close",
    "detached",
    "fmt",
    "has_checked_out",
    "head",
    "into_path",
    "is_initializing",
    "lock_reason",
    "locked",
    "path",
    "prunable",
    "prunable_reason",
]

[[module]]
path = "src/rundir.rs"
crate_path = "upstroke::rundir"
funnel = [
    "acquire_hooked",
    "acquire_in_hooked",
    "create_private_dir",
    "create_private_skeleton",
    "create_public_dir",
    "ingest_answer",
    "observe_cleanup_hold",
    "publish_answer",
    "publish_commit_record",
    "publish_marker",
    "publish_owner_record",
    "release",
    "remove_marker",
    "remove_private_husk",
    "remove_public_husk",
    "stage_answer",
    "stage_commit_record",
    "stage_marker",
    "stage_owner_record",
    "write_plan",
    "write_question_payload",
    "write_report",
]
effectful = [
    # Opens the run's cleanup lease, takes a shared `flock` on it and arranges
    # for the next child the `Command` spawns to inherit that hold (R28). Takes
    # no site of its own: it is called inside the Ref funnel's site-taking
    # primitives in src/workspace_manager.rs, for the lifetime of each
    # `git update-ref` child, and denied to every other caller in clippy.toml.
    "hold_cleanup_lease_for_child",
]
effectful_unnameable = [
    "is_held",
    "take",
]
effect_free = [
    "acquire",
    "acquire_in",
    "active_cleanup_lease_paths",
    "active_paths",
    "answers",
    "artifacts",
    "as_str",
    "commit_record_after_error",
    "create",
    # Delegates to the private site-taking fresh-creation implementation.
    "create_fresh",
    "create_hooked",
    "default_private_root",
    "drop",
    "durability_ledger",
    "enter",
    "enter_cleanup_scope",
    "events",
    "fmt",
    "for_repo",
    "for_worktree_git_dir",
    "from_parts",
    "from_recorded",
    "gate_worktrees",
    "gates",
    "harness",
    "holder",
    "hook",
    "is_running",
    "ledger",
    "lock_file",
    "new",
    "permits_deletion",
    "plan_json",
    "public_dir",
    "questions",
    "recording_durability",
    "report_json",
    "reviews",
    "runs_root",
    "settings",
    "transcripts",
    "v1",
    "with_private_root",
]

[[module]]
path = "src/interaction.rs"
crate_path = "upstroke::interaction"
funnel = []
effectful = [
    "write_question",
    "write_answer",
]
effectful_unnameable = [
    "ask",
    "poll",
    "resolve",
]
effect_free = [
    "answer_for_option",
    "answer_path",
    "answers_for",
    "defer_backoff",
    "fmt",
    "id",
    "interactive",
    "interpret",
    "is_decline_option",
    "is_open",
    "new",
    "new_question_id",
    "notifiers_for",
    "open",
    "parse",
    "read_answer",
    "render_question",
    "sleep",
    "with_poll",
]

[[module]]
path = "src/util.rs"
crate_path = "upstroke::util"
funnel = []
effectful = [
    "fsync_dir",
    "fsync_file",
    "write_text",
    "write_json",
]
effectful_unnameable = []
effect_free = [
    # Reads two counters. The thread-local half is what makes an assertion on a
    # durability barrier EXACT rather than a lower bound a neighbouring test's
    # thread can satisfy (`PR6-LANEF-001`).
    "barriers_on_this_thread",
    "barriers_performed",
    "clear",
    "deserialize",
    "executable_extensions",
    "fence_for",
    "filename_component",
    "find_program",
    "head",
    "is_recording",
    "off",
    "probe_extensions",
    "read_file_bounded",
    "record",
    "recording",
    "records",
    "records_for",
    "rfc3339_utc_now",
    "serialize",
    "steps",
    "tail",
    "user_upstroke_dir",
]

[[module]]
path = "src/events/log.rs"
crate_path = "upstroke::events::log"
funnel = [
    "append",
    "append_hooked",
    "append_topology",
    "append_topology_hooked",
    "establish_stable_prefix",
    "open",
    "open_hooked",
    "opened_at",
    "phase",
    "point",
    "site",
    "site_for",
]
effectful = []
effectful_unnameable = []
effect_free = [
    # `EventHooks` default bodies, in the classification domain since the
    # parser was repaired (`PR6-LANEF-007`). Each returns a constant or does
    # nothing: `WrittenShape::Complete`, `DurabilityLedger::off()`, `{}`.
    #
    # `durability_ledger` and `synced` are also `HarnessEventHooks`'s overrides
    # from PR7. Both stay effect-free: one hands back a clone of an in-memory
    # `Vec` handle and the other pushes a `SyncRecord` onto one. Neither
    # touches a file — the sync they describe was performed by
    # `sync_log_file`/`sync_directory`, which are the funnel's own.
    "durability_ledger",
    "events",
    "synced",
    "written_kill_shape",
    "bytes",
    "committed_bytes",
    "first_line_digest",
    "fmt",
    "fold",
    "from",
    "harness",
    "into_log_and_fold",
    "kind",
    # PR7: `HarnessEventHooks`'s durability read-back. In-memory only.
    "ledger",
    "log",
    "name",
    "new",
    "parse_bytes",
    "path",
    "poisoned_at",
    "poisoned_site",
    "poll",
    "read_all",
    "read_bytes",
    # PR7: turns this observer's ledger on. Allocates a `Vec`; performs nothing.
    "recording_durability",
    "round_trip",
    "skip_existing",
    # PR7: the collected `SyncRecord`s, cloned out of a mutex.
    "syncs",
    # PR7: sets which of `Written`'s two tabled durable shapes this observer
    # asks the funnel for. Stores an enum; the funnel performs the write.
    "with_written_kill_shape",
]

[[module]]
path = "src/runner/host.rs"
crate_path = "upstroke::runner::host"
funnel = []
effectful = [
]
effectful_unnameable = []
effect_free = [
    "contain_write_command",
    "containment_establishments",
    "credential_location",
    "default",
    "environment",
    "fmt",
    "for_legacy_workspace",
    "new",
    "policy",
    "policy_digest",
    "program_resolutions",
    "program_searches",
    "reserved_keys",
    "run",
    "shell_probe",
    "shell_probe_request",
    "start_write_command",
    "with_environment",
    "with_hooks",
]

# The three production children the `m5-host` split gave `src/runner/host.rs`,
# added in the same commit as the split. Ten externally reachable `fn`s left
# the parent's file, and a name that leaves the domain is a name nobody has to
# classify any more -- so the children join `CLASSIFIED_MODULES` by name, one
# path each, which is the only form that list has: its census joins every entry
# onto the manifest root and reads it as a source file, so there is no prefix to
# widen `src/runner/host.rs` into. `C-002` is the standing finding that the
# roll-call is hand-maintained, and it is not this split's.
#
# THE REPOINT IS A NET WIDENING OF SIX, written down rather than implied: 10
# names leave the parent's block and 16 arrive in the children's.
#
#   * `lookup` (`HostEnvironment::lookup`), `is_bare_name` and `is_program`
#     (`ProgramNaming`'s), `composed_value` and `resolve_program` were PRIVATE
#     `fn`s of `src/runner/host.rs`, below the classification threshold
#     entirely. The extraction makes them `pub(super)` in their children so the
#     parent and `runner::host::tests` can still reach them by name, and a
#     `pub(super) fn` is externally reachable, so they must be classified now.
#     `pub(super)` here is `runner::host` and its descendants and nothing wider.
#   * `current` named TWO items inside the parent's file -- `KeyCase::current`
#     (`pub`) and `ProgramNaming::current` (private) -- and this record
#     classifies by bare name, so one entry covered the reachable one. The split
#     separates the pair into different files, and the private half becomes
#     `pub(super)`, so each is now classified on its own.
#
# Every one is a widening. No name that was classified before is unclassified
# now.
#
# All three children are `effect_free`, and that is what the split was arranged
# to make true: no funnel, no effect site and no `cfg` moved. `HostRunner::run`
# with its two `ProcessSite` values, `build_command_at` with the crate's one
# host-side `std::process::Command`, the `Contained` mint and the reserved-key
# vocabulary all stayed in the parent, which keeps the allowlist identity. Each
# child DENIES all three governed lints at file scope and takes no
# `effects/allowlist.toml` row.
#
# `crate_path` is EMPTY for all three, because each is a private `mod`. The
# `ModuleClassification`'s `crate_path` contract in
# `docs/internals/effects/tests.md` says the path is empty when the module is
# not reachable from outside its parent, including a private `mod` or the
# binary crate root, and
# `src/engine/{coordinator,resume,attempt,preflight}.rs` are the tree's
# convention. An earlier revision of this note recorded
# `upstroke::runner::host::probe` and argued the value was harmless because the
# field is read only for a `funnel` or `effectful` row. That argument was true
# and beside the point: `E0603` makes the path unnameable, so it was false
# metadata that validation happens not to look at, and if one of these `fn`s is
# ever reclassified effectful the stored path could not name it for the
# denylist. Review finding 2 on PR #111.
[[module]]
path = "src/runner/host/environment.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "base",
    "case",
    "compose",
    "current",
    "from_process",
    "lookup",
    "objects",
    "preflight",
    "reading",
    "reserved_values",
    "same_key",
    "with_base",
]

# `is_program` and `resolve_program` read the filesystem -- `Path::is_file` and,
# on Unix, the execute bit -- and are classified EFFECT-FREE for the tree's own
# reason: a read-only inspection is not an effect (`classify_object_residue` is
# the precedent, and `ContainerRuntime`'s inspections are classified the same
# way beside it). Nothing here creates, writes, renames or removes a path, and
# the spawn that follows a resolution is the parent's `HostRunner::run`, which
# `runner::tests::every_production_process_start_is_classified` accounts for
# under `src/runner/host.rs`.
[[module]]
path = "src/runner/host/naming.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "composed_value",
    "current",
    "is_bare_name",
    "is_program",
    "resolve_program",
]

[[module]]
path = "src/runner/host/probe.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "run_shell_probe",
]

# PR6's Container funnel. The read-only inspections of `ContainerRuntime`
# (`probe`, `image_by_reference`, `image_by_id`, `volume_present`,
# `containers_with_label`, `observe`, `collect`) are classified EFFECT-FREE:
# `ContainerSite` has no inspection variant, `classify_object_residue` is the
# tree's precedent for "a read-only inspection (no effect)", and lane B's policy
# resolution is required to call them ("resolved by read-only inspection"). They
# do start a `docker` subprocess, and that subprocess start is accounted by
# `src/runner/container.rs`'s row in
# `runner::tests::every_production_process_start_is_classified` rather than
# here.
[[module]]
path = "src/runner/container.rs"
crate_path = "upstroke::runner::container"
funnel = [
    "write_intent",
    "create_container",
    "start_container",
    "mount_git_view",
    "stop_container",
    "remove_container",
    "unmount_git_view",
    "remove_intent",
    # `PR6-ACCT-007`: `Container.RemoveIntent` reached for a name whose
    # published half never landed. The frozen `ContainerSite` has eight
    # variants and this is that same site, not a ninth.
    "remove_staged_intent",
    "launch",
    "release",
    # `release` keeping whether the container itself was stopped and removed,
    # for the runner's process-fate classification (the reviews of `916852c9`,
    # regression 1); the same four sites in the same order.
    "release_classified",
    # The exhaustive cancel the runner's `cancel` and `release` delegate to,
    # public since the runner reads its `container_released` fact.
    "cancel_reached",
    "reclaim",
]
effectful = [
    "runtime::ContainerRuntime::create",
    "runtime::ContainerRuntime::start",
    "runtime::ContainerRuntime::stop",
    "runtime::ContainerRuntime::remove",
    "GitView::materialize",
    "GitView::discard",
]
effectful_unnameable = []
effect_free = [
    # `ContainerHooks::trace`'s default body -- `ContainerTrace::off()`, what
    # production passes. In the domain since `PR6-LANEF-007`.
    "trace",
    "available",
    # `PR6-RECOV-005`: the two halves of "could not be reached" vs "reached and
    # refused". Both read a string and return a value; neither touches the
    # machine, which is the point -- the classification is reachable, and
    # testable, without a daemon.
    "classify_docker_failure",
    "is_unreachable_diagnostic",
    # `CancelResidue::is_empty`: whether a cancel or a release had anything
    # to report; a read of the value it is asked about.
    "is_empty",
    "closed_by_a_reaper",
    "collect",
    "containers_with_label",
    # `HarnessHooks`'s two accessors, the pair `src/rundir.rs`'s own
    # `HarnessHooks` already classifies here by the same names. Both hand back a
    # handle the caller already owns -- the shared `HookHarness`, and the
    # `ContainerTrace` the funnel writes into. Neither reaches the daemon.
    "harness",
    "image_by_id",
    "image_by_reference",
    "list_intents",
    # `PR6-ACCT-007`: the staged half of the same namespace scan. A directory
    # read and a parse, like `list_intents` beside it.
    "list_staged_intents",
    "new",
    "observe",
    "observe_terminated",
    "orphan_window",
    "phase",
    "probe",
    "read_intent",
    "recording_trace",
    "volume_present",
]

[[module]]
path = "src/runner/invocation.rs"
crate_path = "upstroke::runner::invocation"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "attempt",
    "deserialize",
    "fmt",
    "legacy_attempt",
    "parse",
    "probe",
    "probe_target",
    "render",
    "sequence",
    "serialize",
]

[[module]]
# The body of `Container.MountGitView` / `Container.UnmountGitView` (R19), added
# to the domain by PR7's census repair. It carries
# `#![allow(clippy::disallowed_methods)]` over its production region and was the
# only non-test production module in the tree in that position with no row here,
# so nothing required its reachable surface to be classified at all -- and
# `every_effectful_wrapper_is_on_the_disallowed_list` can only force a name onto
# the denylist for a module that has a row.
path = "src/runner/container/view.rs"
crate_path = "upstroke::runner::container::view"
funnel = []
effectful = []
# `RoleGitView`'s two `GitView` methods, and this class rather than `effectful`
# for the reason the class exists: both reach an effect -- `materialize` creates
# the view directory and writes every projected entry, `discard` removes the
# whole of it -- and a trait-dispatched method has no clippy path of its own.
# What denies them is the trait's own pair,
# `upstroke::runner::container::GitView::{materialize,discard}`, classified
# `effectful` by `src/runner/container.rs` above and on the disallowed list as
# `Container.MountGitView` / `Container.UnmountGitView`. Recorded here so the
# absence from clippy.toml under THIS module's path is a decision.
effectful_unnameable = [
    "discard",
    "materialize",
]
# Two readers and three pure constructors/accessors. `resolve` reads
# `<workspace>/.git` and `commondir`; `detached_head` reads `HEAD` and the loose
# ref it may name. Both return values and write nothing, and a read is not an
# effect on this denylist -- which governs creation, writing, removal, rename
# and process start. The same answer `src/runner/container.rs` gives
# `read_intent` and `list_intents`. `new`, `for_reader` and `reader_paths` touch
# no path at all: they build and read back a `RoleGitView`'s reader projection.
effect_free = [
    "detached_head",
    "for_reader",
    "new",
    "reader_paths",
    "resolve",
]

[[module]]
path = "src/engine/coordinator.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = []

[[module]]
path = "src/engine/resume.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = []

[[module]]
path = "src/engine/attempt.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = []

[[module]]
path = "src/engine/preflight.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "chain_summaries",
    "confirm_under_lease",
    "gates_differ",
    "normalized_plan_bytes",
    "preflight",
    "preflight_with_recorded",
    "repo_relative",
    "validate_inputs",
]

[[module]]
path = "src/workspace.rs"
crate_path = "upstroke::workspace"
funnel = []
effectful = [
    "Workspace::run_git_with_private_hooks",
    "Workspace::branch_exists",
    "Workspace::current_branch_ref",
    "Workspace::parent_sha",
    "Workspace::prepared_commit_matches",
    "Workspace::prepared_pin_target",
    "Workspace::ensure_run_exclusions",
]
effectful_unnameable = [
    "drop",
]
effect_free = [
    "advance_prepared_commit",
    "capture_candidate",
    "capture_diff",
    "commit",
    "commit_subject",
    "create_branch",
    "current_branch",
    "discard_uncommitted",
    "ensure_execution_prerequisites",
    "gate_snapshot",
    "gate_snapshot_for_candidate",
    "gate_snapshot_for_candidate_in_store",
    "gate_snapshot_for_tree",
    "head_sha",
    "head_sha_full",
    "is_clean",
    "open",
    "prepare_commit_from_candidate",
    "reclaim_gate_workspaces",
    "remove_orphan_prepared_pin",
    "remove_prepared_pin",
    "review_input_problem",
    "review_input_problem_for_tree",
    "root",
    "staged_tree_oid",
    "switch_branch",
    "uncommitted_summary",
    "workspace",
    "worktree_git_dir",
]

[[module]]
path = "src/gates.rs"
crate_path = "upstroke::gates"
funnel = []
effectful = [
    "run_all",
]
effectful_unnameable = []
effect_free = [
    "ShellKind::command",
    "check",
    "derive",
    "diff_adds_tests",
    "from_record",
    "name",
    "native",
    "parse",
    "preview_resolution",
    "program",
    "resolve_programs",
    "resolves_via_path",
    "shell_available",
    "spec",
]

[[module]]
path = "src/review.rs"
crate_path = "upstroke::review"
funnel = []
effectful = [
    "run_review",
]
effectful_unnameable = []
effect_free = [
    "agents",
    "complete_diff_error",
    "default",
    "describe",
    "drop_alternative",
    "fmt",
    "name",
    "new",
    "of",
    "obliged_lenses",
    "of_plan",
    "parse_verdict",
    "pass_timeout",
    "passes_for",
    "plan_for",
    "profile",
    "profile_for",
    "required_agents",
    "self_review_warning",
]

[[module]]
path = "src/agent/proc.rs"
crate_path = "upstroke::agent::proc"
funnel = [
    "run_with_timeout_at",
    # The same funnel keeping the process fate it established
    # (`ProcessFailure`), for the host Runner's `RunnerError`.
    "run_with_timeout_classified",
]
effectful = []
effectful_unnameable = []
effect_free = [
    "active_processes_with",
    # The test-only group oracle and its lifecycle record. `observe_child_group`
    # asks `waitid` with `WNOWAIT`, `getpgid` and on macOS `proc_pidinfo`,
    # every one a read of process-table state that reaps, signals and moves
    # nothing; `await_exit_without_reaping` blocks in the same `WNOWAIT` wait
    # on a helper thread and leaves the child for its owner to reap. The rest
    # read the record.
    "await_exit_without_reaping",
    "child_leads_its_own_group",
    "deref",
    "deref_mut",
    "exit_answer",
    "fmt",
    "had_exited_before_the_look",
    "leads_own_group",
    # A pure decision over a memoised `Result`: it clones a message or returns
    # `Ok`, and touches nothing.
    "memoised_outcome",
    "observe_child_group",
    "terminate_and_wait",
    "terminate_and_wait_with",
]

# The observation and injection surface, out of line since the `m6-proc` split.
# A private `mod` of the funnel, so nothing here is reachable from outside
# `agent::proc` except through the root's own re-exports of `SpawnHooks` and
# `NoHooks` -- the two names that were `pub` before the move and are `pub` after
# it. `crate_path` is the module's real path all the same, because the two
# appliers are `pub(super)` and `externally_reachable_fns` counts that.
[[module]]
path = "src/agent/proc/hooks.rs"
crate_path = "upstroke::agent::proc::hooks"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    # `apply`/`apply_io`: turn a hook's answer back into control flow. Both
    # return `Ok` or an error carrying the point's name; neither starts,
    # signals or waits on anything. The one non-returning path is
    # `std::process::abort()` under an injected `Kill`, which is the harness
    # deliberately ending THIS process without running a destructor -- the
    # whole claim under test -- and not an effect on anything outside it.
    "apply",
    "apply_io",
    # `SpawnHooks` default bodies. `child_created` does nothing; `point_mode`
    # delegates to `point`, which is an observer question and not an effect.
    # In the domain since `PR6-LANEF-007`.
    "child_created",
    "point",
    "point_mode",
    # PR10 (ST-07 over the full inventory): `phase` is the default body for
    # the two hook phases of `Process.Spawn` and `Process.Terminate`, which
    # answers `Proceed` and touches nothing; `apply_phase` is `apply` for a
    # phase rather than a point, with the same one non-returning path.
    "apply_phase",
    "phase",
]

# The ambient Job Object and the cleanup reapers' reclaim scope, out of line
# since the `m6-proc` split. Every Win32 call underneath these is made by
# `windows_job`, which stayed in the parent; every Unix one by `termination`,
# which stayed there too. The classifications are the ones these names carried
# in the parent's block, unchanged by the move.
[[module]]
path = "src/agent/proc/ambient.rs"
crate_path = "upstroke::agent::proc::ambient"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "ambient_job_established",
    "child_in_ambient_job",
    "join_ambient_job",
    # `join_ambient_job` over an explicit join step, so the error arm can be
    # executed at all. Same classification as the entry point it implements.
    "join_ambient_job_with",
    # `#[cfg(all(windows, test))]`: writes a value into a process-local
    # `OnceLock` so a subprocess helper can carry a real ambient failure. No
    # effect outside this process, and no production caller at all.
    "poison_ambient_for_tests",
    "process_alive",
    "process_creation_time",
    # Writes a value into a process-local `OnceLock<Mutex<..>>` that every
    # cleanup reaper started afterwards renders into argv before its `fork`.
    # No effect outside this process: the reaper performs the effect, inside
    # `Container.Stop`/`Container.Remove`'s own primitives, and only after its
    # coordinator has died. The same classification `poison_ambient_for_tests`
    # carries, for the same reason.
    "set_container_reclaim_scope",
]

# The pipe reader, out of line since the `m6-proc` split. It reads the stdout
# and stderr handles `run_with_timeout_and_limit` already holds, inside that
# funnel call's own `Process.Spawn` site; it starts no process, opens nothing,
# and names no path. So it reaches no effect of its own and takes no site --
# which is why all four are `effect_free` and none is denied. The four were
# private items of `src/agent/proc.rs` before the move and are `pub(super)`
# after it, the same visibility, and that is what puts them in this domain.
[[module]]
path = "src/agent/proc/drain.rs"
crate_path = "upstroke::agent::proc::drain"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    # Release and join the nonblocking reader before taking its capture.
    "collect",
    "collect_bytes",
    # Whether either stream hit the capture allowance; the supervisor asks
    # before it decides to terminate the tree.
    "drain_limit_exceeded",
    # Drain::drop cancels and joins, then records an uncollected failure.
    "drop",
    "failure_report",
    # `Display for Stream`: the two stream labels, for diagnostics.
    "fmt",
    "limit_exceeded",
    # Hands the reader to a thread. `thread::spawn` is not
    # `std::process::Command::spawn`, and this module's own `start` is not a
    # denied path: a segment-matching scan flags both and neither is one.
    "start",
]

[[module]]
path = "src/agent/proc/input.rs"
crate_path = "upstroke::agent::proc::input"
funnel = []
effectful = []
# Starting the feeder schedules stdin writes inside the private Process funnel.
effectful_unnameable = ["start"]
effect_free = ["collect", "drop", "failure_report"]

[[module]]
path = "src/agent/proc/pipe_io.rs"
crate_path = "upstroke::agent::proc::pipe_io"
funnel = []
effectful = []
# These private operations create/transfer writable pipe handles or write the
# Process funnel's stdin. They cannot be named outside the private module.
effectful_unnameable = ["configure", "nonblocking", "pair", "take", "try_write"]
effect_free = ["try_read"]

[[module]]
path = "src/agent/proc/worker.rs"
crate_path = "upstroke::agent::proc::worker"
funnel = []
effectful = []
effectful_unnameable = []
# Cancellation, joining and failure slots affect only process-local state.
effect_free = ["drop", "record", "settle", "spawn", "take"]

[[module]]
path = "src/agent/claude.rs"
crate_path = "upstroke::agent::claude"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = []

[[module]]
path = "src/agent/codex.rs"
crate_path = "upstroke::agent::codex"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = []

[[module]]
path = "src/agent/copilot.rs"
crate_path = "upstroke::agent::copilot"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = []

[[module]]
path = "src/capacity.rs"
crate_path = "upstroke::capacity"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "describe",
    "discovered",
    "drain_of",
    "effective_remaining",
    "estimate",
    "fmt",
    "is_empty",
    "observe",
    "parse",
    "parse_duration",
    "pool_for",
    "read_in_v0_1",
    "render",
    "render_duration",
    "report",
    "strategy_preview",
]

[[module]]
path = "src/export.rs"
crate_path = "upstroke::export"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "load",
    "write",
]

[[module]]
path = "src/main.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "establish",
    "from",
]

[[module]]
path = "src/answer.rs"
crate_path = "upstroke::answer"
funnel = []
effectful = [
    "answer",
]
effectful_unnameable = []
effect_free = [
    "show",
]

[[module]]
path = "src/config.rs"
crate_path = "upstroke::config"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "any",
    "capture",
    "chain_for",
    "check_budget",
    "default_chain",
    "effort_for",
    "files",
    "for_resume",
    # `RunnerSelection::host_default` — what an absent `[runner]` section means.
    # A struct literal over owned values; it reaches no primitive and no path.
    "host_default",
    "implementation_effort",
    "load",
    "load_captured",
    "load_captured_with",
    "load_limits",
    "load_with",
    "path",
    "resolved_effort_policy",
    "review_effort",
    "snapshot_file",
    "text",
]

[[module]]
path = "src/connect.rs"
crate_path = "upstroke::connect"
funnel = []
effectful = [
    "run_with",
]
effectful_unnameable = []
effect_free = [
    "refused",
    "render_report",
    "run",
]

[[module]]
path = "src/route.rs"
crate_path = "upstroke::route"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "fmt",
    "resolve",
]

[[module]]
path = "src/status.rs"
crate_path = "upstroke::status"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "describe",
    "follow",
    "interrupted_run",
    "load",
    "render",
    "report",
]

[[module]]
path = "src/validate.rs"
crate_path = "upstroke::validate"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "analyze",
    "analyze_captured",
    "builtin_adapter",
    "capture",
    "paths",
    "render",
    "run",
    "write_normalized_json",
]

[[module]]
path = "src/events/mod.rs"
crate_path = "upstroke::events"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "apply",
    "ensure_supported_schema",
    "event",
    "fmt",
    "index_of",
    "interrupted_attempts",
    "kind",
    "legacy_unsettled_failure",
    "new",
    "normalized_plan_digest",
    "now",
    "open_questions",
    "passed",
    "recorded_chains",
    "recorded_complete_reviews",
    "recorded_effort_policy",
    "recorded_gates",
    "recorded_normalized_plan_digest",
    "recorded_reviews",
    "replay",
    "review_cost_incomplete",
    "review_cost_usd",
    "is_successful",
    "review_models",
    "settle_interrupted",
    "shape",
    "started_of",
    "task",
    "validate_review_identity",
]

[[module]]
path = "src/events/log/premove.rs"
crate_path = ""
funnel = []
effectful = []
effectful_unnameable = [
    "open",
    "append",
]
effect_free = [
    "path",
]

[[module]]
path = "src/agent/bin.rs"
crate_path = "upstroke::agent::bin"
funnel = []
effectful = []
effectful_unnameable = []
effect_free = [
    "boundary_refused",
    "display",
    "extract_version",
    "named",
    "spec",
]

# Every `libc::` item this tree names, classified.
#
# `claim_scope` makes exhaustiveness "the disallowed list is complete for the
# **primitives the crate uses**", so this list is derived from the tree, not
# transcribed from the sentence's `fork/kill/setpgid/setsid/flock/fcntl/exec*` --
# which is six names out of the two dozen effect primitives this crate calls.
#
# An item is an EFFECT when calling it changes state outside the calling
# thread's own memory: the filesystem, the process/signal table, a descriptor,
# a lock, or a resource limit. Constants (`SIG*`, `O_*`, `F_*`, `LOCK_*`, `E*`),
# types (`pid_t`, `pollfd`, `sigset_t`) and read-only queries (`getpid`,
# `sysconf`, `poll`, `read`, `proc_pidinfo`) are not.
[libc]
# `setsid` and the `exec*` family are in this list and appear nowhere in the
# tree: `mechanism` (1) names them regardless ("libc fork/kill/setpgid/setsid/
# flock/fcntl/exec*"), and a denial of a primitive the crate does not yet call is
# the cheapest kind there is. `every_platform_conditional_denial_names_something_
# real` carries them in its PACKET_ONLY set for the same reason.
effect = [
    "_exit",
    "close",
    # Rewrites the descriptor table of the process that calls it. The Unix
    # reaper's container half puts its listing pipe on fd 1 with it, in the
    # forked child before `execv`.
    "dup2",
    "execl",
    "execle",
    "execlp",
    "execv",
    "execve",
    "execvp",
    "fcntl",
    "flock",
    "fork",
    "kill",
    "mkdtemp",
    "mkfifo",
    "open",
    "pipe",
    "pipe2",
    "pthread_sigmask",
    "raise",
    "rmdir",
    "setpgid",
    "setrlimit",
    "setsid",
    "sigaction",
    "signal",
    "sigprocmask",
    "syscall",
    "unlink",
    "waitid",
    "waitpid",
    "write",
]
not_an_effect = [
    "BPF_ABS",
    "BPF_JEQ",
    "BPF_JMP",
    "BPF_K",
    "BPF_LD",
    "BPF_RET",
    "BPF_W",
    "CLD_DUMPED",
    "CLD_EXITED",
    "CLD_KILLED",
    "CLONE_PIDFD",
    "EACCES",
    "EAGAIN",
    "EBADF",
    "ECHILD",
    "EINTR",
    "EINVAL",
    "EIO",
    "EMFILE",
    "ENOENT",
    "ENOMEM",
    "ENOSYS",
    "EPERM",
    "ESRCH",
    "ETXTBSY",
    "EWOULDBLOCK",
    "FD_CLOEXEC",
    "F_GETFD",
    "F_GETFL",
    "F_GETLK",
    "F_SETFL",
    "F_SETFD",
    "F_SETLK",
    "F_SETPIPE_SZ",
    "F_UNLCK",
    "F_WRLCK",
    "LOCK_EX",
    "LOCK_NB",
    "LOCK_SH",
    "LOCK_UN",
    "O_CLOEXEC",
    "O_DIRECTORY",
    "O_NOFOLLOW",
    "O_NONBLOCK",
    "O_RDONLY",
    "O_WRONLY",
    "POLLERR",
    "POLLHUP",
    "POLLIN",
    "POLLNVAL",
    "PROC_PIDT_SHORTBSDINFO",
    "PR_SET_NO_NEW_PRIVS",
    "PR_SET_SECCOMP",
    "P_ALL",
    "P_PID",
    "P_PIDFD",
    "RLIMIT_CORE",
    "RLIMIT_NOFILE",
    "SA_RESTART",
    "SA_SIGINFO",
    "SECCOMP_MODE_FILTER",
    "SECCOMP_RET_ALLOW",
    "SECCOMP_RET_ERRNO",
    "SECCOMP_RET_KILL_PROCESS",
    "SEEK_SET",
    "SIGABRT",
    "SIGBUS",
    "SIGCHLD",
    "SIGCONT",
    "SIGFPE",
    "SIGHUP",
    "SIGILL",
    "SIGINT",
    "SIGKILL",
    "SIGPIPE",
    "SIGQUIT",
    "SIGSEGV",
    "SIGSTOP",
    "SIGSYS",
    "SIGTERM",
    "SIGTRAP",
    "SIGTSTP",
    "SIGTTIN",
    "SIGTTOU",
    "SIGUSR1",
    "SIG_BLOCK",
    "SIG_DFL",
    "SIG_ERR",
    "SIG_IGN",
    "SIG_SETMASK",
    "SIG_UNBLOCK",
    "SRUN",
    "SSTOP",
    "STDERR_FILENO",
    "STDIN_FILENO",
    "STDOUT_FILENO",
    "SYS_clone3",
    "SYS_close_range",
    "SYS_fcntl",
    "SYS_getdents64",
    "SYS_kill",
    "SYS_pidfd_open",
    "SYS_pidfd_send_signal",
    "SYS_prctl",
    "SYS_wait4",
    "SYS_waitid",
    "SZOMB",
    "WEXITED",
    "WEXITSTATUS",
    "WIFEXITED",
    "WIFSIGNALED",
    "WIFSTOPPED",
    "WNOHANG",
    "WNOWAIT",
    "WSTOPPED",
    "WTERMSIG",
    "WUNTRACED",
    "_SC_OPEN_MAX",
    "__errno_location",
    "__error",
    "c_char",
    "c_int",
    "c_long",
    "c_short",
    "c_void",
    "getpgid",
    "getpgrp",
    "getpid",
    "getppid",
    "getrlimit",
    "id_t",
    "nanosleep",
    "pause",
    "pid_t",
    "poll",
    "pollfd",
    "proc_bsdshortinfo",
    "proc_listpids",
    "proc_pidinfo",
    "read",
    "rlimit",
    "sigaddset",
    "sigemptyset",
    "sighandler_t",
    "siginfo_t",
    "sigismember",
    "sigpending",
    "sigset_t",
    "sleep",
    "sock_filter",
    "sock_fprog",
    "suseconds_t",
    "sysconf",
    "time_t",
    "timespec",
    "timeval",
]

