# Where an allow of a governed lint may live.
#
# `decisions.effect_site_inventory.mechanism` (2): "an allow-placement scan (a
# unit test over src/**/*.rs, Cargo.toml [lints], and crate-level attributes in
# lib.rs/main.rs) permits allow/expect of disallowed_methods, disallowed_types,
# disallowed_macros, clippy::style, clippy::all, or warnings only as
# module-level attributes in files listed in effects/allowlist.toml, which has
# two sections: funnel modules … and legacy modules frozen at PR5 … each
# carrying a LEGACY-EFFECT justification; the legacy section may only shrink
# after PR5 (the test compares against the frozen list) and never contains a
# topology module (src/topology/**, src/runner/**, src/workspace_manager.rs,
# src/engine/topology.rs)".
#
# The scan is `effects::tests::every_allow_of_a_governed_lint_is_module_level_
# and_in_the_allowlist`. It reads this file, so an entry deleted here fails the
# build of the file that relied on it.
#
# `allows` is the exact lint set the file's attribute names, asserted equal, not
# merely contained: an attribute that allows a lint this file does not record is
# an unreviewed widening, and a file that starts violating a fourth lint fails
# clippy until both are updated together. It is the union over platforms — a
# `#[cfg(windows)]` body's lint cannot be measured from a Unix host — which is
# why `allow` is used rather than `expect`; `expect` would make the union fail
# on whichever platform did not need all of it.
#
# `scanned_beyond_the_packet`: the mechanism sentence says `src/**/*.rs`. The
# scan also covers `examples/**/*.rs`, because `cargo clippy --all-targets`
# compiles examples and `examples/probe.rs` reaches an effect. Scanning wider
# than the sentence can only find more, never less.

# ---------------------------------------------------------------------------
# Funnel modules
#
# "each reviewed to perform effects only inside site-taking APIs and never to
# return writable handles."
#
# The review is over each module's PRODUCTION surface. That is the packet's own
# reading, not a relaxation invented here: the sentence lists
# `src/topology/effects.rs`, whose production surface performs no effect at all,
# beside `src/rundir.rs`, whose test region plants husks with raw `fs` calls.
# ---------------------------------------------------------------------------

[[funnel]]
path = "src/workspace_manager.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): named first in the funnel-module list"
review = """
Every effect is issued inside a `funnel` call taking an `EffectSiteId` by value \
(Worktree/Snapshot/Ref/Object groups, `FunnelGroup::module()`), and no public \
function returns a `File`, an `OpenOptions` or a `Command`."""

[[funnel]]
path = "src/workspace_manager/fixture.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] pub(crate) mod fixture;` of src/workspace_manager.rs"
review = """
The Worktree/Snapshot/Ref/Object funnel's shared test fixture, extracted from \
`src/workspace_manager.rs` into a sibling file and given attributes of its own \
IN THE SAME COMMIT -- never inheriting one. A Rust lint level is scoped by the \
MODULE TREE and not by the file, so this file would otherwise have inherited \
`src/workspace_manager.rs`'s inner allow of all three lints silently: that is \
`PR6-LANEF-004`, and this is the first out-of-line child this funnel has had. \
`runner::container::tests::every_child_module_of_the_container_funnel_states_\
its_own_lint_level` is the census that refuses such a child. Its `FUNNELS` \
domain did NOT reach this parent when this row was written, so nothing was red \
and the level was written here because the hole was the same one, not because a \
gate caught it. THAT IS NO LONGER TRUE, and the sentence is corrected rather \
than left standing: the `m4-workspace` split added \
`src/workspace_manager.rs` to that census's `FUNNELS` domain in the same \
commit, so this file is now graded against all three governed lints and named \
in the domain individually. The level below is unchanged; what changed is that \
a gate now checks it. \
THE ALLOWANCE IS NOT WIDER THAN WHAT MOVED. These same bodies made these same \
calls inside `src/workspace_manager.rs`, under that file's own inner allow of \
all three lints; this row allows TWO of that three, so it is a strict subset \
and the move changes where the level is stated, not what it permits. \
WHAT IT NEEDS EACH FOR, measured at extraction by denying all three and \
counting the errors: `clippy::disallowed_methods`, 15 sites -- it builds and \
tears down the scratch repositories the suites measure against \
(`fs::create_dir_all` 5, `fs::write` 4, `fs::remove_dir_all` 2, \
`fs::remove_file` 1) and runs real children (`Command::output` 1, \
`Command::status` 1, `Command::spawn` 1), because no funnel owns `git init` and \
a fixture that never runs `git` builds no repository to measure; \
`clippy::disallowed_types`, 3 sites, every one `std::process::Command`. \
`clippy::disallowed_macros` is RE-DENIED here rather than inherited -- measured \
at zero sites, and the two `#[cfg(windows)]` bodies a Unix host cannot measure \
are `ExitStatus` arithmetic naming no macro -- so a `println!` here is still a \
build error. \
CONTAINMENT, STATED AS IT IS. Every write and every directory creation is under \
a root from `scratch`, which is `std::env::temp_dir()` joined with a name \
carrying the tag, the process id and a per-call ordinal, and `Fixture::drop` \
removes its own root. Two effects are NOT confined to that root, and they are \
named rather than elided: `scratch` opens with `let _ = \
fs::remove_dir_all(&dir)` on that predictable path, so a process-id collision \
after a crashed run can delete a stale tree this call did not create; and \
`run_kill_child` re-execs THIS test binary `--exact --ignored` while `git_out` \
and `KillableGitChild::spawn` run the system `git` in a caller-supplied \
directory. Both bodies are byte-identical to what already ran inside the parent. \
HANDLES, PRECISELY, because a blanket claim here would be false: no item returns \
a `File`, an `OpenOptions` or a `Command`, but one DOES hand back a live process \
handle and it is named rather than covered. `KillableGitChild::spawn` returns a \
`pub(crate)` type owning a `std::process::Child`, because the residue kill \
sampler has to kill a real `git` at a chosen instant and a handle it cannot \
reach is a kill it cannot time. Its `child` field is private, it exposes only \
`kill`, `exited`, `wait` and `fired`, and the whole module is `#[cfg(test)]` so \
the type is not in the rlib at all. \
Recorded here rather than left inherited, so the scope is a reviewed artifact \
rather than an accident of the module tree."""

[[funnel]]
path = "src/workspace_manager/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/workspace_manager.rs"
review = """
The Worktree/Snapshot/Ref/Object funnel's own test module, extracted from \
`src/workspace_manager.rs` into a sibling file and given an attribute of its \
own IN THE SAME COMMIT -- never inheriting one, for the `PR6-LANEF-004` reason \
the `fixture.rs` row above states in full. \
THE ALLOWANCE IS NOT WIDER THAN WHAT MOVED. This file's three lints are exactly \
the three `src/workspace_manager.rs` already allowed at file scope, and these \
same bodies made these same calls under that allow before the extraction: an \
equal set, so a subset, and the move changes where the level is stated and not \
what it permits. \
WHAT IT NEEDS EACH FOR, measured at extraction by denying all three and \
counting the errors: `clippy::disallowed_methods`, 88 sites -- it drives the \
site-taking APIs and plants the residue and the husks they are meant to find \
(`fs::write` 49, `fs::create_dir_all` 15, `fs::remove_dir_all` 6, \
`fs::remove_file` 6, `fs::rename` 6, `std::os::unix::fs::symlink` 2, \
`std::io::Write::write_all` 1) and drives real Git children \
(`Command::spawn` 2, `Command::output` 1); `clippy::disallowed_types`, 3 \
sites, every one `std::process::Command`; `clippy::disallowed_macros`, 3 \
sites, every one `std::println` in a diagnostic. \
CONTAINMENT, STATED AS IT IS. The filesystem effects are under roots \
`fixture::scratch` minted, with the predictable-path pre-clean that row \
describes; the process effects are the system `git` and re-execs of this test \
binary. HANDLES: nothing in this file is `pub` or `pub(crate)` at all, so \
nothing crosses the module boundary; the one type owning a live \
`std::process::Child` -- `SampledChild`, the kill sampler's handle -- is \
private, as is its constructor. \
Recorded here rather than left inherited, so the scope is a reviewed artifact \
rather than an accident of the module tree."""

[[funnel]]
path = "src/topology/effects.rs"
allows = []
packet = "effect_site_inventory.mechanism (2): named in the funnel-module list"
review = """
The `EffectSiteId` union over the eleven site enums, its `row()` mapping and the \
inventory constants. The site enums themselves are in the `sites` child and \
`effect_sites()` in the `export` child: this module was split into per-concern \
children, and this row is the root of that module rather than the whole of it. \
Performs no effect at all, in production or in tests, so it carries no \
attribute — listed because the packet lists it, and because a future effect \
here would have to be reviewed into `allows` rather than appearing silently. \
WHAT HOLDS THE SAME PROPERTY FOR THE SEVEN CHILDREN, which this row does not \
reach: none of them carries a module-level `allow` either, so `clippy.toml`'s \
denials bind them exactly as written and the lint gate is what enforces this — \
not this entry. An `allow` added to any of them would have to be recorded here, \
which is what \
`every_allow_of_a_governed_lint_is_module_level_and_in_the_allowlist` fails on. \
Said out loud because the split narrowed what this row covers without narrowing \
the property: a reader who mistook the entry for the guarantee could remove the \
thing actually providing it."""

[[funnel]]
path = "src/runner/host.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): src/runner/{host,container,invocation}.rs"
review = """
The Process funnel: `FunnelGroup::Process.module()` is this file. Process start \
is `HostRunner::run`, which passes `ProcessSite::Spawn` by value and threads \
`SpawnHooks` through every containment sub-effect point. The site-taking \
supervision primitives live in `src/agent/proc.rs`, the second Process-funnel \
module listed below."""

[[funnel]]
path = "src/runner/host/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/runner/host.rs"
review = """
The Process funnel host half's own test module, extracted from \
`src/runner/host.rs` into a sibling file and given attributes of its own IN THE \
SAME COMMIT -- never inheriting one. A Rust lint level is scoped by the MODULE \
TREE and not by the file, so this file would otherwise have inherited \
`src/runner/host.rs`'s inner allow of these same three lints silently: that is \
`PR6-LANEF-004`, and `runner::container::tests::every_child_module_of_the_\
container_funnel_states_its_own_lint_level` refuses a child that does not state \
its own level. That census already named `src/runner/host.rs` as a funnel whose \
directory it would walk the day one appeared; this extraction is that day, and \
the census found the child rather than a reviewer having to. \
THE ALLOWANCE IS NOT WIDER THAN WHAT MOVED. Every one of these calls was \
already made by these same test bodies inside `src/runner/host.rs`, under that \
file's own inner allow of the same three lints; the move changes where the \
level is stated, not what it permits. \
WHAT IT NEEDS EACH FOR, measured at extraction by denying all three and \
counting the errors: `clippy::disallowed_methods`, 82 sites -- the fixtures \
build and tear down scratch trees (`fs::remove_dir_all` 43, `fs::create_dir_\
all` 20, `fs::write` 7, `fs::set_permissions` 5, `fs::File::create` 2, \
`fs::remove_file` 1) and run real child processes (`Command::output` 3, \
`Command::status` 1); `clippy::disallowed_types`, 5 sites, every one \
`std::process::Command`, because a host-runner suite that never spawns \
establishes nothing -- availability cannot be established by inspection; \
`clippy::disallowed_macros`, 4 sites, `println!` in a diagnostic. It performs \
no effect outside a scratch directory of its own making. \
Recorded here rather than left inherited, so the scope is a reviewed artifact \
rather than an accident of the module tree."""

[[funnel]]
path = "src/agent/proc.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): Process funnel primitives beneath src/runner/host.rs"
review = """
The Process supervision primitives. Spawn enters through an API taking \
`ProcessSite::Spawn` by value; every tree-termination primitive takes \
`ProcessSite::Terminate` by value. No writable process handle leaves this \
combined funnel."""

[[funnel]]
path = "src/agent/proc/pipe_io.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2), by attachment to the private Process funnel in src/agent/proc.rs"
review = """
Private parent-side pipe preparation and nonblocking I/O for the Process funnel. \
The methods allowance covers the Unix owned-file write; the types allowance \
covers Command configuration. Reader and Writer cannot leave the private proc \
module. Spawn remains in the existing site-taking Process API. Windows creates \
synchronous anonymous pipe pairs and uses the same owned endpoint boundary. \
No file or process handle is returned outside the combined funnel. The module \
denies governed macros instead of inheriting the parent's allowance."""

[[funnel]]
path = "src/agent/proc/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/agent/proc.rs"
review = """
The Process funnel's supervision half's own test module, extracted from \
`src/agent/proc.rs` into a sibling file and given attributes of its own IN THE \
SAME COMMIT -- never inheriting one. A Rust lint level is scoped by the MODULE \
TREE and not by the file, so this file would otherwise have inherited \
`src/agent/proc.rs`'s inner allow of these same three lints silently: that is \
`PR6-LANEF-004`, and `runner::container::tests::every_child_module_of_the_\
container_funnel_states_its_own_lint_level` refuses a child that does not state \
its own level. \
THE ALLOWANCE IS NOT WIDER THAN WHAT MOVED. Every one of these calls was already \
made by these same test bodies inside `src/agent/proc.rs`, under that file's own \
inner allow of the same three lints; the move changes where the level is stated, \
not what it permits. \
WHAT IT NEEDS EACH FOR, measured at extraction by denying all three and counting \
the errors -- 143 sites on Linux, and a Linux figure: the Windows-gated bodies \
are not compiled here, so the count is a floor rather than the whole file. \
`clippy::disallowed_methods`, 120 sites -- the supervision suite asks the kernel \
directly about process groups, signals and reaping (`libc::kill` 41, \
`libc::signal` 5, `libc::sigprocmask` 2, `libc::flock` 2, and one each of \
`libc::fork`, `libc::_exit`, `libc::waitpid`, `libc::waitid`, `libc::setpgid`, \
`libc::setrlimit`, `libc::raise`, `libc::open`, `libc::close`, `libc::fcntl`), \
builds and tears down scratch trees for its helpers (`std::fs::write` 15, \
`std::fs::remove_dir_all` 12, `std::fs::create_dir_all` 9, `std::fs::remove_file` \
4, `std::fs::File::create` 2, and one each of `std::fs::create_dir` and \
`std::fs::set_permissions`), re-execs this test binary as its agent \
(`std::process::Command::spawn` 8, `std::process::Command::output` 1) and speaks \
the readiness protocol over pipes (`std::io::Write::flush` 4, \
`std::io::Write::write_all` 3); `clippy::disallowed_types`, 19 sites, every one \
`std::process::Command`, because a supervision suite that never spawns \
establishes nothing -- containment cannot be established by inspection; \
`clippy::disallowed_macros`, 4 sites, `std::print` 2 and one each of \
`std::println` and `std::eprint`, all of them a subprocess helper reporting to \
its parent over the stream the test reads. \
CONTAINMENT, STATED AS IT IS, BECAUSE IT IS NOT UNIFORM. Most of these bodies \
build a scratch directory of their own making under `std::env::temp_dir()`, \
keyed by process id plus a ulid or a nanosecond timestamp, and touch nothing \
outside it; the process effects are re-execs of this test binary and the trees \
it starts and kills. FOUR SITES ARE NOT THAT, in two shapes, and every one of \
them moved out of `src/agent/proc.rs` rather than being anything this \
extraction wrote. \
(1) `timeout_kills_a_background_grandchild_before_it_can_escape` and \
`successful_direct_exit_still_kills_detached_group_members` put a marker FILE \
directly in `std::env::temp_dir()` and call `std::fs::remove_file` on it before \
creating any directory or child, so the removal is of a path they have not \
proven they own. The first keys on process id and thread name ALONE: after a \
crashed run leaves its marker behind and the operating system reuses that pid, \
that `remove_file` deletes a file this suite did not create. The second adds a \
nanosecond timestamp, which makes the same collision remote rather than \
impossible. \
(2) `spawn_signal_helper` and the reaper suite's `scratch` helper call \
`std::fs::remove_dir_all` on their scratch path BEFORE `create_dir_all`, which \
is the same unproven-ownership removal one level up; both key on process id plus \
a ulid or nanoseconds. \
That is the older, pre-token shape `rundir::scratch_tree::acquire` was written \
to replace, and it is the same hazard the `src/rundir/tests.rs` row above \
records for the same reason. THIS ROW RECORDS IT RATHER THAN IMPLYING A \
CONTAINMENT THE FILE DOES NOT HAVE: all four ran inside the parent under the \
parent's own allow, and repairing them is a change to test behaviour that this \
move deliberately does not make. \
HOW EQUAL "MOVED" IS, STATED EXACTLY, BECAUSE "BYTE-IDENTICAL" WOULD BE FALSE \
OF THIS FILE. Lifting the block out of `mod tests { … }` dedents every line by \
four columns, which changes what fits inside the width and makes `rustfmt` \
reflow. Every difference it produces is formatting and nothing else -- \
expressions that now fit on one line, a dropped trailing comma, an elided \
closure brace, one `panic!` match arm braced -- and the property is stated \
without a line count on purpose: a count here mixes the reflowed lines together \
with the imports the split forced this file to name for itself, which are not \
formatting, and it goes stale at the next edit while the next reader trusts it. \
THE EQUALITY THAT DOES HOLD IS REPRODUCIBLE, and it is the one worth stating: dedent the body of \
`mod tests` in `src/agent/proc.rs` at this branch's base by four columns, run \
`rustfmt --edition 2024` over it, and the result is this file byte-for-byte once \
the three imports the split forced it to name for itself are removed \
(`std::io::Read`, `crate::topology::effects::Injection`, and a `#[cfg(windows)]` \
`super::ambient::join_ambient_job_with`; every one of them reached this suite \
through `use super::*` before the move -- the first two as names the parent \
imported, the third as a sibling item of the parent that the split relocated). \
The four bodies named above are byte-identical after the dedent alone; \
`rustfmt` touched none of them. So \
the suite is semantically equivalent and formatting-normalised rather than \
byte-equal, and this row says which rather than claiming the stronger thing. \
Recorded here rather than left inherited, so the scope is a reviewed artifact \
rather than an accident of the module tree."""

[[funnel]]
path = "src/agent/proc/test_support/readiness.rs"
allows = ["clippy::disallowed_methods"]
expect_sites = 6
packet = "effect_site_inventory.mechanism (2), by attachment: the `mod readiness;` of src/agent/proc.rs's `#[cfg(test)] mod test_support`"
review = """
CODING_STANDARDS.md §12's readiness protocol, moved out of line from \
`src/agent/proc.rs` and given attributes of its own in the same commit -- \
never inheriting one. A Rust lint level is scoped by the MODULE TREE and not by \
the file, so this file would have inherited the Process funnel's \
`#![allow(disallowed_methods, disallowed_types, disallowed_macros)]` silently: \
that is `PR6-LANEF-004`, measured twice in the Container subtree, and this is \
the first out-of-line child the Process funnel has ever had. \
ALL THREE GOVERNED LINTS ARE DENIED AT FILE SCOPE, AND THE ALLOWANCE IS SIX \
PER-SITE `#[expect]` ATTRIBUTES rather than a file-scope allow. That is \
`PR72-PLACEMENT-001`, and `standards/02_standards_automated_baseline.md` \
(the lints paragraph) admits it: this file's own mechanism otherwise permits an \
allowance "only as module-level attributes", and a per-site expectation is \
NARROWER than the module-level allow it replaces, not a widening of it. \
MEASURED, and it is why the old shape was wrong rather than merely untidy: \
deleting the previous `#![allow(clippy::disallowed_methods)]` changed nothing \
at `cargo clippy --all-targets --all-features -- -D warnings`, because \
`src/agent/proc.rs`'s allow reaches here through the module tree. So it bought \
no diagnostic and made a claim about the WHOLE FILE when what is true is a \
claim about six lines of it: a seventh denied call, anywhere, arrived under the \
same allowance and nothing said so. With the deny in place a seventh call is a \
build error, and a site that stops reaching a denied path is \
`unfulfilled_lint_expectations` on its own `#[expect]` -- so THE COMPILER OWNS \
THE COUNT IN BOTH DIRECTIONS under the `-D warnings` the gate runs with, and \
`expect_sites` above is the reviewed number it is checked against. \
The calls the six are written against: the atomic publication in \
`publish_between` is `std::fs::File::create_new` of a staging sibling, \
`std::io::Write::write_all` and `std::io::Write::flush` of the framed record, \
`std::fs::rename` onto the signal name, and `std::fs::remove_file` of the \
staging name on either failure path. \
FIVE DISTINCT DENIED PATHS ACROSS SIX SITES \
-- `std::fs::remove_file` is called twice, once on the write-side failure \
path and once on the rename-side one -- AND NO OTHERS: there is no \
`fs::write`, no `OpenOptions`, no `create_dir`, and no durability barrier -- \
§8 separates visibility from durability and a readiness signal needs only the \
first, so nothing here touches `util::fsync_file`/`util::fsync_dir` or the \
counters two other suites assert deltas against. Every effect is inside a \
scratch directory the caller owns and no writable handle is returned: the \
functions return `std::io::Result<()>`, `Vec<String>` and `Waited`, and \
`Producer::child` hands back the `&mut Child` its caller already owns. \
`clippy::disallowed_types` and `clippy::disallowed_macros` are RE-DENIED here \
rather than inherited, so a `std::process::Command`, a `std::fs::OpenOptions` \
or a `println!` in this file is still a build error. \
`runner::container::tests::every_child_module_of_the_container_funnel_states_\
its_own_lint_level` is the census that refuses a Process- or Container-funnel \
child stating neither, and \
`effects::tests::the_readiness_expectations_are_per_site_and_both_records_say_so` \
is the one that keeps this row, this file's prologue and the six annotations \
saying the same thing."""

[[funnel]]
path = "src/runner/container.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): src/runner/{host,container,invocation}.rs; `FunnelGroup::Container.module()`"
review = """
The Container funnel, added by PR6: `FunnelGroup::Container.module()` is this \
file and all EIGHT of `ContainerSite`'s variants are taken BY VALUE by an API \
here (`write_intent`, `create`, `start`, `mount_git_view`, `stop`, `remove`, \
`unmount_git_view`, `remove_intent`), each of which calls `hook(Before, site)` \
-> primitive -> `hook(After, site)` through the one private `funnel` and \
refuses a site that does not name its operation. Every effect is issued inside \
one of them: the `docker` CLI (`DockerCli`, the only `std::process::Command` \
in the file -- a coordinator-side control-plane call that deliberately does not \
cross the boundary, with its row in `runner::tests::every_production_process_\
start_is_classified`), the intent record's stage/fsync/rename/dir-fsync through \
`util::fsync_file` and `util::fsync_dir`, and the R19 view directory. \
NO PUBLIC FUNCTION RETURNS A WRITABLE HANDLE: the APIs return `PathBuf`, \
`CreatedContainer`, `Liveness` and `()`, never a `Command`, a `File` or a \
runtime handle -- `PR5D-FUNNEL-RETURNS-A-COMMAND` is the entry for what that \
costs. The effectful methods of `ContainerRuntime` and `GitView` are on the \
denylist ("docker invocation helpers"), so a lane's module cannot reach one \
without appearing in this file; `runner::container::tests::every_container_\
effect_in_the_tree_goes_through_the_funnel` is the source census beside it."""

[[funnel]]
path = "src/runner/container/view.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2), by attachment: the body of `Container.MountGitView` / `Container.UnmountGitView`, declared by src/runner/container.rs"
review = """
R19, the disposable role-scoped Git view, added by PR6 lane A. The two \
functions here ARE the primitives of `Container.MountGitView` and \
`Container.UnmountGitView` — `GitView::materialize` and `GitView::discard` are \
themselves on the denylist, so nothing outside the funnel can call one, and the \
funnel that does is `mount_git_view`/`unmount_git_view` in \
`src/runner/container.rs`, each taking its site by value. The effects are the \
view directory and the seven files and directories inside it \
(`view::PROJECTED_ENTRIES`), all under `<R>/views/<container-name>`, and every \
one is pruned by `discard`. NO WRITABLE HANDLE IS RETURNED: `materialize` \
returns a `PathBuf` and `discard` returns `()`; the one `fs::File` is a local \
that does not outlive `write_file`. Attached to the funnel section rather than \
listed as a module of its own, in the idiom of `src/events/log.rs` and \
`src/events/log/tests.rs` — PR5's precedent for a funnel body beside the module \
the packet names."""

[[funnel]]
path = "src/runner/container/tests.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/runner/container.rs"
review = """
The Container funnel's own test module, added by PR6 and given an attribute of \
its own by repair round F1. Until then it had NO attribute and relied on \
inheriting `src/runner/container.rs`'s inner `#![allow(...)]` through the module \
tree — which is `PR6-LANEF-004`, the hole that let a lane's PRODUCTION module \
inherit the same allow and call a container primitive with the clippy gate \
green. Every child of `src/runner/container/` now states its own lint level and \
`runner::container::tests::every_child_module_of_the_container_funnel_states_\
its_own_lint_level` refuses a new one that does not. \
WHAT IT NEEDS THE ALLOW FOR: it drives all eight site-taking APIs, plants the \
residue they are meant to find (`fs::write` of a staged half, `fs::create_dir_\
all` of a view a reclaimer must prune) and delegates through `ContainerRuntime` \
in its wrapper runtimes. It performs no effect outside a scratch directory of \
its own making and returns nothing. `clippy::disallowed_types` and \
`clippy::disallowed_macros` are RE-DENIED here rather than inherited, so a \
`std::process::Command` or a `println!` in this file is still a build error."""

[[funnel]]
path = "src/runner/container/census/tests.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/runner/container/census.rs"
review = """
The startup census's test module (PR6 lane C), given an attribute of its own by \
repair round F1 for the same reason as `src/runner/container/tests.rs`: it used \
to inherit the funnel's allow through the module tree. It plants intents, views \
and labelled containers for the census to classify and reclaim, which is what \
needs `fs::write` and `fs::create_dir_all`; `census.rs` itself now carries \
`#![deny(...)]` and reaches no denied primitive. `clippy::disallowed_types` and \
`clippy::disallowed_macros` are re-denied here."""

[[funnel]]
path = "src/runner/container/resolve/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/runner/container/resolve.rs"
review = """
The container `RunnerPolicy` resolution suite (PR6 lane B). Lane B landed after \
repair round F1 built the guard, so this file arrived with NO attribute of its \
own and inherited the Container funnel's allow through the module tree — \
`PR6-LANEF-004` reopening, caught by `runner::container::tests::every_child_\
module_of_the_container_funnel_states_its_own_lint_level` and red on the merged \
tree. `resolve.rs`, the PRODUCTION half, carries `#![deny(...)]` for all three \
governed lints and reaches no denied primitive at all, so the build-error leg of \
`mechanism` (1) holds for lane B's production code. \
THE RESIDUAL, STATED RATHER THAN IMPLIED: this is the one child of \
`src/runner/container/` that allows `clippy::disallowed_types` as well as \
`clippy::disallowed_methods`, because its fixtures build real temporary Git \
repositories with `std::process::Command` running `git` — three call sites — \
alongside `fs::write`, `fs::create_dir_all` and `fs::remove_dir_all`, and its \
wrapper runtime delegates the seam's four effectful methods. So a \
`std::process::Command` in THIS file is not the build error it is in \
`src/runner/container/tests.rs` and `src/runner/container/census/tests.rs`, \
which both re-deny that lint deliberately. `src/events/log/tests.rs` and \
`src/engine/tests.rs` are the precedent for a test module needing both. \
`clippy::disallowed_macros` is re-denied, so a `println!` here is still an \
error."""

[[funnel]]
path = "src/runner/container/exec.rs"
allows = []
packet = "effect_site_inventory.mechanism (2), by attachment: the `ContainerRunner`, declared beside src/runner/container.rs"
review = """
The `ContainerRunner` (PR6 lane A). Its PRODUCTION REGION performs no denied \
effect at all and carries a file-level `#![deny(clippy::disallowed_methods, \
disallowed_types, disallowed_macros)]`, so a container primitive called from a \
lane's production code here is a build error — which is the guarantee \
`PR6-LANEF-004` found had been inherited away. \
THIS ROW ALLOWS NOTHING NOW, AND THAT IS A CHANGE. It carried \
`clippy::disallowed_methods` for an OUTER attribute on the `#[cfg(test)] mod \
tests` item, which covered the fixtures while they were inline in this file. \
They are out of line since W1, in `src/runner/container/exec/tests.rs`, which \
has a row of its own and states that level for itself rather than inheriting \
one. The outer attribute was then allowing one lint twice for one module — \
`clippy::duplicated_attributes` — so it was deleted with the move rather than \
suppressed. No attribute in this file allows a governed lint today, and the \
empty list is the claim: `effects::tests::every_allow_of_a_governed_lint_is_\
module_level_and_in_the_allowlist` compares this field against the attributes \
actually written here, in both directions, so a row that outlived its \
attribute and a file that grew one back both fail it."""

[[funnel]]
path = "src/runner/container/exec/tests.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/runner/container/exec.rs"
review = """
The `ContainerRunner`'s own test module (PR6 lane A), extracted from \
`src/runner/container/exec.rs` into a sibling file and given an attribute of \
its own IN THE SAME COMMIT -- never inheriting one. A Rust lint level is scoped \
by the MODULE TREE and not by the file, so this file would otherwise have been \
covered by the outer allow that stood on its declaration in `exec.rs` while \
stating nothing itself: that is `PR6-LANEF-004`, and `runner::container::tests::every_\
child_module_of_the_container_funnel_states_its_own_lint_level` refuses a child \
that does not state its own level. \
THE ALLOWANCE IS NOT WIDER THAN WHAT MOVED. `exec.rs` carries a file-level \
`#![deny(...)]` of all three governed lints and carried, while these bodies \
were inline in it, an OUTER `#[allow(clippy::disallowed_methods)]` on the test \
declaration, so the test region already had exactly this one lint allowed and \
the other two denied. These same bodies made these same calls there; the move \
changes where the level is stated, not what it permits. That outer attribute \
is GONE -- deleted with the move, because with the level stated here it \
allowed one lint twice for one module, which is `clippy::duplicated_attributes` \
-- and `exec.rs`'s own row now records an empty `allows`. \
WHAT IT NEEDS THE ALLOW FOR, measured at extraction by denying all three and \
counting the errors: 23 sites, every one `clippy::disallowed_methods` -- the \
fixtures build worktrees and scratch trees (`fs::write` 9, `fs::create_dir_all` \
7, `fs::set_permissions` 1), delegate the seam's four effectful methods through \
a wrapper runtime (`ContainerRuntime::{create,start,stop,remove}`, one site \
each) and drive the view directly (`GitView::{materialize,discard}`, one site \
each) to seed containers the runner then supervises. \
WHAT IT REACHES BEYOND ITS OWN SCRATCH TREE, stated because a containment \
claim that overstates is worse than one that admits: the ten `real_docker_*` \
tests behind `docker_gate` create, start, stop and remove REAL CONTAINERS IN \
THE SHARED DOCKER DAEMON and query that daemon by label, and one of them reads \
the host process table through `/proc` to find a container's daemonised \
descendant. Daemon state is not inside any scratch directory, so deleting the \
scratch tree does not undo it. Two guards bound it and neither is absolute: a \
`LeaveNoResidue` `Drop` reclaims every container carrying the test's own \
private-root label — `Drop` rather than a line at the end of the test, because \
that line does not run when the test fails, which is when residue is most \
likely — and the `GATED_RUNS` ledger gives each gated test a distinct run id, \
so two trees' suites do not fight over one container name. \
WHEN THE GUARD CANNOT RUN, RESIDUE SURVIVES: a `SIGKILL`ed process runs no \
`Drop` at all, and a failing label query returns early from the `Drop` that \
does run, so the container stays in the shared daemon and nothing in the \
scratch-directory cleanup removes it. ONE gated test pre-cleans its own \
container name before the create for exactly that case (`reviews/FINDINGS.md` \
§16; the key it derives is per build slot and is fixed when \
`CARGO_TARGET_DIR` is unset, `R5-SEAMS-006`); the other nine rely on the \
`Drop`. \
`clippy::disallowed_types` and `clippy::disallowed_macros` are RE-DENIED here \
rather than inherited -- both measured at zero sites -- so a \
`std::process::Command` or a `println!` in this file is still a build error. \
The PRODUCTION region of `exec.rs` keeps its file-level deny and is untouched \
by this row."""

[[funnel]]
path = "src/runner/invocation.rs"
allows = []
packet = "effect_site_inventory.mechanism (2): src/runner/{host,container,invocation}.rs"
review = """
Invocation identity and policy shaping. Reaches no denied primitive today; \
listed because the packet lists it."""

[[funnel]]
path = "src/rundir.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): the lock and run-directory funnels in src/rundir.rs"
review = """
Every run-directory, answer and lock effect is issued inside a `funnel` call \
naming one `EffectSiteId` (RunDir, Answer, Lock groups). The private-half \
deletion funnel takes a `PrivateHalfProof` by value and no other constructor \
exists. No public function returns a writable handle; `WorktreeLock`/`RunLock` \
own their descriptors and release them on drop. \
THE TEST REGION CARRIES ONE MORE RECURSIVE DELETION, and it is reviewed here \
rather than left to the file-scope allow: `#[cfg(test)] pub(crate) mod \
scratch_tree` reclaims a test's own scratch tree under a SECOND TOKEN, \
`ScratchTreeOwnership`. That token is taken BY VALUE, is minted only by \
`acquire`, and `acquire` CREATES the root it binds with a non-recursive \
exclusive `fs::create_dir` -- so the token names a root that did not exist \
before the token did, an occupied or undecidable root refuses without \
pre-cleaning anything, the name carries a fresh ULID rather than a predictable \
tag, and there is no path-taking removal a caller could aim at an ancestor. \
`decisions/2026-08-30-test-scratch-tree-ownership.md` is the record and states \
the completeness rule in its two-token form. \
IT ADDS NO SITE. Reclaiming a test's scratch tree is not an engine effect, so \
there is no `RunDirSite` variant, no `effect_sites.json` row and nothing \
censused; the module is `#[cfg(test)]`, so it is in no production region and \
not in the rlib at all -- which is why its `Clone`/`Copy`/`Default`/by-value \
refusals are compiler-decided inside the crate rather than rlib fixtures \
beside `PrivateHalfProof`'s. \
AND SINCE W1 IT CARRIES AN ATTRIBUTE AND A ROW OF ITS OWN. It was INLINE, so \
the file-scope allow this row records WAS its lint level by the module tree; it \
is now the sibling file `src/rundir/scratch_tree.rs`, which is exactly the \
OUT-OF-LINE shape `PR72-PLACEMENT-001` and \
`standards/02_standards_automated_baseline.md` are about -- a child \
inheriting a level nobody had written down -- so it states its own and its row \
below records the two lints it needs of the three this file allows."""

[[funnel]]
path = "src/rundir/scratch_tree.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] pub(crate) mod scratch_tree;` of src/rundir.rs"
review = """
PR #149 additionally retains an acquired directory handle and compares it with \
the current root at checked use and reclaim. Unix uses device/inode identity; \
Windows uses volume serial and the full FileIdInfo identifier through a safe \
wrapper over GetFileInformationByHandleEx. The token's File remains private, \
survives ordinary refusal/disarm/rearm, and is consumed after successful \
removal before the root-absence check. A consumed claim never reopens a \
replacement. The additional filesystem/identity observations and replacement \
witnesses use this module's existing two governed-lint allowances; no raw \
handle, arbitrary-path acquisition, remover or reporter is exported. Native \
Linux/macOS/Windows tests and Clippy cover their respective branches. The \
following counts describe the original extraction, not the current file. \
The second deletion-authority token and its witnesses, extracted from \
`src/rundir.rs` into a sibling file and given attributes of its own IN THE SAME \
COMMIT -- never inheriting one. The row above reviewed this module's recursive \
deletion while it was INLINE, where the file-scope allow was its lint level by \
the module tree; it is out of line since W1, so that paragraph now points here \
and this file states its own level. `PR6-LANEF-004` is the shape it would \
otherwise have taken. `runner::container::tests::every_child_module_of_the_\
container_funnel_states_its_own_lint_level` is the census that refuses such a \
child. Its `FUNNELS` domain did NOT reach this parent when this row was \
written, so nothing was red and the level was written here because the hole was \
the same one, not because a gate caught it; `src/rundir.rs` joined that domain \
in W2 and the census now grades this file like any other child. \
THE ALLOWANCE IS NOT WIDER THAN WHAT MOVED. These same bodies made these same \
calls inside `src/rundir.rs`, under that file's own inner allow of all three \
lints; this row allows TWO of that three, a strict subset, and the move changes \
where the level is stated and not what it permits. \
WHAT IT NEEDS EACH FOR, measured at extraction by denying all three and \
counting the errors: `clippy::disallowed_methods`, 14 sites -- `fs::create_dir` \
3, ONE of which is `acquire_named`'s exclusive, non-recursive create, the call \
that MINTS the token, the other two seeding an occupied-root witness; \
`fs::write` 5, seeding witnesses inside trees already acquired; \
`fs::remove_dir_all` 1, which is the point of the module -- `remove_tree`, the \
real remover, reachable only with a `ScratchTreeOwnership` taken by value; \
`std::io::Write::write_all` 2 and `std::io::Write::flush` 1 in \
`report_to_stderr`, which exists precisely BECAUSE the print macros panic on a \
write error during an unwind; `fs::File::options` 1, opening `/dev/full` on the \
Linux leg, the only reachable device that accepts a descriptor and refuses the \
bytes; and `Command::output` 1. `clippy::disallowed_types`, 1 site, the \
`std::process::Command` in `one_test_as_a_child`, which re-execs this test \
binary `--exact` because the channel under observation is a real fd 2 and no \
in-process double is one. \
`clippy::disallowed_macros` is RE-DENIED here rather than inherited -- measured \
at zero sites, and deliberately rather than incidentally: this module's report \
path is written through a stderr handle for the stated reason that `eprintln!` \
panics on a write error, so a print macro appearing in this file is a build \
error and not a style note. \
CONTAINMENT. Every filesystem effect is under a root `acquire` created with a \
non-recursive exclusive `fs::create_dir` of a fresh-ULID name -- so the root did \
not exist before the token did, an occupied or undecidable root refuses without \
pre-cleaning anything, and there is no path-taking removal a caller could aim \
at an ancestor; `Remover` stays module-private for that reason. Two effects are \
outside any such tree and are named rather than elided: one diagnostic line on \
the process's own stderr, and the re-exec above. `/dev/full` is opened \
write-only and by construction accepts no bytes. \
IT STILL ADDS NO SITE, and the counts above are the whole of what changed: \
reclaiming a test's scratch tree is not an engine effect, so there is no \
`RunDirSite` variant, no `effect_sites.json` row and nothing censused. \
`decisions/2026-08-30-test-scratch-tree-ownership.md` is the record."""

[[funnel]]
path = "src/rundir/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/rundir.rs"
review = """
The run-directory, answer and lock funnels' own test module, extracted from \
`src/rundir.rs` into a sibling file and given an attribute of its own IN THE \
SAME COMMIT -- never inheriting one, for the `PR6-LANEF-004` reason the \
`scratch_tree.rs` row above states in full. \
THE ALLOWANCE IS NOT WIDER THAN WHAT MOVED. This file's three lints are exactly \
the three `src/rundir.rs` already allowed at file scope, and these same bodies \
made these same calls under that allow before the extraction: an equal set, so \
a subset, and the move changes where the level is stated and not what it \
permits. \
WHAT IT NEEDS EACH FOR, measured at extraction by denying all three and \
counting the errors: `clippy::disallowed_methods`, 87 sites -- it plants husks \
and residue for the funnels to classify (`fs::create_dir_all` 38, `fs::write` \
13, `fs::remove_file` 6, `fs::remove_dir_all` 3, `std::io::Write::flush` 3, \
`fs::rename` 2, `fs::set_permissions` 2, `std::os::unix::fs::symlink` 2, \
`fs::create_dir` 1, `fs::hard_link` 1, `fs::File::options` 1), runs real \
children (`Command::spawn` 4, `Command::status` 2, `Command::output` 1) and \
reaches libc directly for the two contracts no safe wrapper expresses -- \
`libc::fork` 2, `libc::waitpid` 2 and `libc::_exit` 2 for a SECOND PROCESS \
holding a lock, `libc::flock` 1 for the advisory lock itself, and \
`libc::mkfifo` 1 for a path that blocks on open, which is how the lock funnel's \
contention and refusal paths are reached at all; `clippy::disallowed_types`, 7 \
sites, every one `std::process::Command`; `clippy::disallowed_macros`, 3 sites, \
every one `std::println` in a diagnostic. \
CONTAINMENT, STATED AS IT IS. The filesystem effects are under a root from this \
file's own `scratch`, which is `std::env::temp_dir()` joined with a tag and the \
process id, and which opens with `let _ = fs::remove_dir_all(&dir)` on that \
predictable path -- so a process-id collision after a crashed run can delete a \
stale tree this call did not create. That is the older, pre-token shape \
`scratch_tree::acquire` was written to replace; the body is byte-identical to \
what already ran inside the parent, and this row records the hazard rather than \
implying a containment the file does not have. The process effects are forks of \
this test binary and re-execs of it. HANDLES: nothing in this file is `pub` or \
`pub(crate)` either; the seven `pub fn`s a grep finds are inside the \
raw-string rustc fixtures, whose interiors are compile-refusal source rather \
than items of this module. \
Recorded here rather than left inherited, so the scope is a reviewed artifact \
rather than an accident of the module tree."""

[[funnel]]
path = "src/interaction.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): the answer funnels in src/interaction.rs"
review = """
The packet places the answer funnels here; PR5 lane B moved the funnel BODIES to \
`src/rundir.rs` (Answer.StageWrite / PublishRename / Ingest) and left \
`write_question`/`write_answer`/`read_answer` as delegations. Both files are \
listed and the delegations are classified as wrappers in `effects/wrappers.toml`, \
so the disagreement is recorded rather than resolved by silence. The remaining \
effects here are the notifier's stderr and the test region's scratch \
directories."""

[[funnel]]
path = "src/util.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): the write primitives in src/util.rs"
review = """
`write_text`/`write_json` are the crate's shared write primitives. They take no \
site, so `mechanism` (3) classifies them as effectful WRAPPERS rather than as \
funnels — `util::write_json` is that sentence's own example — and both are on \
the disallowed list. `ReportSite::Write`'s `module()` names this file and no \
function here takes that site; see `effects/wrappers.toml` and the site \
inventory."""

[[funnel]]
path = "src/events/log.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2): the Event funnel src/events/log.rs"
review = """
Every write, flush, sync and truncation is inside `EventLog`, whose two entry \
points take an `EventSite` by value. `mechanism` (3): "EventLog::open and \
EventLog::append are classified as the Event funnel (site-taking) rather than as \
wrappers, and the raw writer they wrap is reachable only inside \
src/events/log.rs" — the `File` field is private and \
`every_declared_build_refusal_fails_for_the_reason_it_declares` compiles the \
fixture that proves it."""

[[funnel]]
path = "src/events/log/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2), by attachment: the `#[cfg(test)] mod tests;` of src/events/log.rs"
review = """
The Event funnel's own test module, declared by `src/events/log.rs`. It plants \
torn tails and unsynced prefixes with raw `fs` calls, which is what a funnel's \
test region is for; the module's production surface is the funnel's and is \
reviewed there. Attached to the funnel section rather than frozen into the \
legacy section, because a PR5 file is not legacy and the legacy section may only \
shrink."""

[[funnel]]
path = "src/effects/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.outputs, by attachment: the `#[cfg(test)] mod tests;` of src/effects.rs"
review = """
This layer's own test module. It compiles the build-refusal fixtures as \
subprocesses, writes the generated artifacts, and walks the tree; the module's \
production surface is `src/effects.rs`'s and performs no effect at all."""

[[funnel]]
path = "src/effects.rs"
allows = []
packet = "effect_site_inventory.outputs: clippy.toml; effects/allowlist.toml; the wrapper classification; the residue-class evidence record"
review = """
This enforcement layer itself, and it carries NO attribute — which is a stronger \
claim than any other entry in this section makes. Its whole surface is paths, \
parsers and classification over `&str`; it reaches no denied primitive, and the \
one `std::process::Command` its text contains is inside `DENIAL_FIXTURES`, a \
string constant that `src/effects/tests.rs` compiles in order to prove it is \
refused. Listed because a future effect here must be reviewed into `allows` \
rather than appearing silently, and because the layer that governs the allowlist \
being absent from it would be the one omission nobody would think to look for."""

# ---------------------------------------------------------------------------
# Legacy modules, FROZEN AT PR5
#
# "legacy modules frozen at PR5 (schema-1..3 engine paths in
# src/engine/{coordinator,resume,attempt,preflight}.rs, legacy branch/checkout/
# commit operations in src/workspace.rs, src/gates.rs, src/review.rs, src/agent
# supervision until PR4 lands the process funnel, src/capacity.rs, notifier and
# export modules, and any other module the PR5 review enumerates) each carrying
# a LEGACY-EFFECT justification".
#
# This list may only SHRINK. `effects::tests::the_legacy_section_is_frozen_and_
# may_only_shrink` compares it against `FROZEN_LEGACY_ALLOWLIST` in
# `src/effects.rs` and fails on any entry that is not in the frozen list. It
# contains no topology module and
# `the_legacy_section_never_contains_a_topology_module` fails if one appears.
# Both tests are also driven with synthetic input that DOES grow and DOES name a
# topology module, so the refusals are executed rather than assumed.
#
# ONE GROWTH SINCE PR5, by the owner's decision on #306: `src/engine/mod.rs`,
# the first row below. `FROZEN_LEGACY_ALLOWLIST` grew with it in the same
# commit, which is the only way the test above admits an entry, and the row
# says what it is for and what it cost. The section is otherwise as PR5 froze
# it.
# ---------------------------------------------------------------------------

# Added on #306 (`PR7-WRAPPERS-EMPTY-DOMAIN`). The three engine conductors'
# effectful `pub(super)` fns are denied by their crate paths since then; two of
# them, `coordinator::run_harness_inner_on` and `resume::resume_harness_inner_on`,
# have their only production caller in this facade, which carried no allow. A
# module-level allow here reaches every module under `engine::topology` by
# inheritance -- a lint level is scoped by the module tree, not the file, and
# the placement scan records only what a file writes -- so the same commit put
# `#![deny(..)]` of all three governed lints on `src/engine/topology.rs`, the
# root all forty of those files descend from, and
# `effects::tests::the_topology_root_re_denies_every_lint_the_engine_facade_allows`
# holds that deny lexically and by compiling the shape.
[[legacy]]
path = "src/engine/mod.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): schema-1..3 engine paths -- the public facade of src/engine/{coordinator,resume}.rs, added on #306"
legacy_effect = """
LEGACY-EFFECT: the v0.1 conductor's public entry points (`run`, `run_with`, \
`run_harness`, `resume`, `resume_with`, `resume_harness`) call \
`coordinator::run_harness_inner_on` and `resume::resume_harness_inner_on`, \
effectful wrappers denied by path since #306 (`PR7-WRAPPERS-EMPTY-DOMAIN`): \
the coordinator removes both run halves with `fs::remove_dir_all` on a failed \
start, and the resume conductor re-materialises open questions through \
`interaction::write_question`. Nothing else in this file reaches an effect: it \
declares the engine's modules and threads a `Contained` token. The allow is \
`disallowed_methods` alone, and `src/engine/topology.rs` denies all three \
governed lints at its root, so the allow stops at this file's own items."""
shrinks_when = "the v0.1 conductor is retired, or its entry points move into the allowed legacy modules they call, at which point this file calls nothing denied."


[[legacy]]
path = "src/engine/coordinator.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): schema-1..3 engine paths in src/engine/{coordinator,resume,attempt,preflight}.rs"
legacy_effect = """
LEGACY-EFFECT: the schema-1..3 coordinator's own run-directory teardown \
(`fs::remove_dir_all` of a public and a private half on a failed start). \
`invariants_preserved[1]` requires the legacy engine's behaviour untouched by \
this slice, so the calls are frozen rather than moved."""
shrinks_when = "PR7-PR11 route the legacy coordinator's teardown through the RunDir husk funnels, which need the ownership proof this slice adds."

[[legacy]]
path = "src/engine/resume.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): schema-1..3 engine paths"
legacy_effect = """
LEGACY-EFFECT: reaches `interaction::write_question`, an effectful wrapper, when \
the legacy resume path asks a question. Frozen with the rest of the legacy \
engine."""
shrinks_when = "the schema-4 coordinator asks questions through RunDir.WriteQuestionPayload directly, at which point this call site goes."

[[legacy]]
path = "src/engine/attempt.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): schema-1..3 engine paths"
legacy_effect = """
LEGACY-EFFECT: writes agent transcripts through `util::write_text`, an effectful \
wrapper. `RunDir` has no transcript site in the frozen inventory, so there is no \
funnel to move it to inside this slice."""
shrinks_when = "the inventory gains a transcript site, or transcripts move under RunDir.WriteReport's row."

[[legacy]]
path = "src/engine/preflight.rs"
allows = []
packet = "effect_site_inventory.mechanism (2): schema-1..3 engine paths"
legacy_effect = """
LEGACY-EFFECT: named by the packet in the frozen legacy list. Reaches no denied \
primitive today, so it carries no attribute; listed so that a legacy effect \
added here is reviewed into `allows` rather than appearing silently."""
shrinks_when = "immediately — it can be dropped from this list at any time without weakening anything."

[[legacy]]
path = "src/workspace.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): legacy branch/checkout/commit operations in src/workspace.rs"
legacy_effect = """
LEGACY-EFFECT: the schema-1..3 engine's Git operations — branch, checkout, \
stage, commit, rollback — each a `std::process::Command` on the system git, plus \
the working-tree writes around them. `invariants_preserved[1]` requires this \
module's behaviour untouched; the schema-4 equivalents live behind funnels in \
`crate::workspace_manager` and nothing here calls them."""
shrinks_when = "the schema-4 engine replaces the legacy paths that call this module (PR7-PR11); the module goes with them."

[[legacy]]
path = "src/gates.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2): src/gates.rs"
legacy_effect = """
LEGACY-EFFECT: gate execution writes gate logs with `fs::write`. PR4 routed \
the gate's process through the Runner, and the former production `command()` \
builder is now test-only."""
shrinks_when = "gate logs move under a RunDir site."

[[legacy]]
path = "src/review.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): src/review.rs"
legacy_effect = """
LEGACY-EFFECT: review transcript writes and the review scratch directories its \
tests build. Frozen with the legacy engine that calls it."""
shrinks_when = "review transcripts move under a RunDir site."

[[legacy]]
path = "src/agent/claude.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: the adapter writes a permission-settings file through \
`util::write_json` before an attempt, and its live-probe test prints to stderr. \
Enumerated rather than claimed as "src/agent supervision", which it is not."""
shrinks_when = "the settings file moves under a RunDir site."

[[legacy]]
path = "src/agent/codex.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: the adapter creates and removes its own scratch directory with \
`fs::create_dir`/`fs::remove_dir`, and its live-probe test prints to stderr."""
shrinks_when = "the adapter scratch directory moves under R18 or a RunDir site."

[[legacy]]
path = "src/agent/copilot.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: the adapter writes its configuration through `util::write_json`, \
and its live-probe test prints to stderr."""
shrinks_when = "the configuration write moves under a RunDir site."

[[legacy]]
path = "src/agent/bin.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT limited to the `#[cfg(test)]` module: the Windows batch-shim \
witness creates a scratch directory, writes a local `.cmd`, and executes it. \
The allow is an outer attribute on that test module, so production remains \
governed and carries only `CommandSpec` data into the Process funnel."""
shrinks_when = "the Windows batch-shim witness can exercise the platform translation without direct filesystem and process primitives."

[[legacy]]
path = "src/capacity.rs"
allows = []
packet = "effect_site_inventory.mechanism (2): src/capacity.rs"
legacy_effect = """
LEGACY-EFFECT: named by the packet in the frozen legacy list. Reaches no denied \
primitive today — PR4 moved its process starts behind the Runner — so it carries \
no attribute; listed so a legacy effect added here is reviewed rather than \
silent."""
shrinks_when = "immediately — it can be dropped from this list at any time without weakening anything."

[[legacy]]
path = "src/export.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): notifier and export modules"
legacy_effect = """
LEGACY-EFFECT: the CSV and JSONL writers emit through `io::Write::write_all` on \
a caller-supplied sink, and the export tests build whole run directories with \
raw `fs` calls. The sink is usually stdout rather than a file, but the denial \
cannot tell a `File` from a `Vec<u8>` and the packet's answer to that is this \
list."""
shrinks_when = "export writes go to a sink type this crate owns, or the export tests build their fixtures through the RunDir funnels."

[[legacy]]
path = "src/main.rs"
allows = ["clippy::disallowed_macros", "clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates; the scan's own domain names main.rs"
legacy_effect = """
LEGACY-EFFECT: the CLI surface writes the process's own stdout and stderr. That \
is an external effect with no resource row, no site and no funnel, and \
`production_effect` is "none in behavior", so it is frozen and listed rather \
than moved. This is a CRATE-LEVEL attribute — `main.rs` is a crate root — and \
the mechanism sentence scans exactly that: "crate-level attributes in \
lib.rs/main.rs". `src/lib.rs` is deliberately NOT in this file, so a crate-level \
allow there fails the scan."""
shrinks_when = "the CLI reports through a sink the engine owns rather than through the print macros."
# `disallowed_types` is the union-over-platforms case this file's header
# predicts: `src/main.rs`'s Windows-only test
# `a_write_command_establishes_the_ambient_job_and_a_read_only_command_does_not`
# builds a `std::process::Command`, invisible to a Unix clippy run and caught by
# `cargo clippy --target x86_64-pc-windows-msvc`.

[[legacy]]
path = "src/answer.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: the `answer` command creates the answers directory with \
`fs::create_dir_all` before staging. `RunDir` has no create-answers-directory \
site in the frozen inventory, so there is no funnel to move it to inside this \
slice; the staging and publication either side of it already go through \
Answer.StageWrite and Answer.PublishRename."""
shrinks_when = "the inventory gains a site for the answers directory, or its creation folds into RunDir.CreatePublicDir."

[[legacy]]
path = "src/config.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: test region only — configuration loading is read-only, and the \
fixtures that exercise it write scratch config files directly."""
shrinks_when = "the config fixtures build their scratch trees through a funnel."

[[legacy]]
path = "src/connect.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: `connect` writes the user's own `upstroke.toml` region and creates \
its parent directory, outside any run and outside every resource row — it is a \
setup command, not an engine path."""
shrinks_when = "never, on current reading: the file it writes belongs to the operator, not to a run."

[[legacy]]
path = "src/route.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = "LEGACY-EFFECT: test region only — routing is pure; its fixtures write scratch pool files directly."
shrinks_when = "the routing fixtures build their scratch trees through a funnel."

[[legacy]]
path = "src/status.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: two lints for two different reasons, and only one of the two has \
a production site at all. `clippy::disallowed_methods` covers six sites. The one \
in production is `follow`, which streams each event to the caller's `&mut dyn \
io::Write`; `writeln!` expands to `std::io::Write::write_fmt`, which is why the \
denial names that path rather than the macro. The other five are the \
husk-trichotomy fixture `status_asked_for_a_husk_id_names_which_husk_it_is`, \
which builds and tears down a scratch repository with `fs::create_dir_all` \
twice, `fs::remove_dir_all` twice and `Command::status` once. \
`clippy::disallowed_types` covers exactly one site, and it is not production: \
the same fixture's `Command::new("git")`, which needs a real repository because \
the husk answer takes this repository's key over its canonical common git dir. \
Neither lint is earned by `render` any more. The view is accumulated into a \
`String` through `std::fmt::Write`, whose `write_fmt` is a different `DefId` and \
is not denied, and it now lives in the private child `src/status/render.rs`, \
which restores both denials rather than inheriting this allowance."""
shrinks_when = """
The two retire separately, and `clippy::disallowed_types` retires first, because \
no production site names the type: it goes when \
`status_asked_for_a_husk_id_names_which_husk_it_is` obtains its scratch \
repository without constructing a `Command` — through the Runner, or through a \
fixture funnel that owns the `git init` — and `follow` need not change for it. \
`clippy::disallowed_methods` has two halves and goes only when both are done: \
that same fixture's two `create_dir_all`, two `remove_dir_all` and one \
`Command::status` move behind the RunDir funnels and the Runner, AND `follow` \
stops writing to a sink from inside this module — by yielding its lines to a \
caller that owns the sink, the way `render` already returns a `String`."""

[[legacy]]
path = "src/validate.rs"
allows = ["clippy::disallowed_methods"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: `validate --write` writes the normalized plan back beside the \
source with `fs::write`, outside any run directory, plus the fixtures that \
exercise it."""
shrinks_when = "the write-back moves under a site, which needs a resource row for a file that is not run-scoped."

[[legacy]]
path = "src/events/mod.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: the schema-1..3 event types and their tests. The Event funnel \
itself is `src/events/log.rs` and is in the funnel section; what is left here is \
the type layer plus a test region that plants torn tails with `OpenOptions` and \
`fs::write`."""
shrinks_when = "the event-type tests plant their logs through the Event funnel's own kill and error-return points."

[[legacy]]
path = "src/events/log/premove.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT, and this file is the most literally legacy thing in the tree: it \
is `git show ff0490a:src/events.rs | sed -n '1478,1585p'`, the PRE-MOVE writer, \
kept verbatim as the independent oracle for "byte-identical legacy behaviour". \
Its whole value is that it is unchanged, so its `OpenOptions`, `set_len`, \
`write_all`, `flush` and `sync_data` must stay exactly where they are."""
shrinks_when = "the byte-identity obligation retires with the legacy engine; the file goes with it."

[[legacy]]
path = "src/engine/tests.rs"
allows = ["clippy::disallowed_methods", "clippy::disallowed_types"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: the whole-file `#[cfg(test)] mod tests;` of the legacy engine. It \
builds real repositories and run directories with `fs` and `git` `Command`s to \
drive the schema-1..3 paths end to end. Attached to the legacy section because \
the modules it tests are in it."""
shrinks_when = "the legacy engine goes; these tests go with it."

[[legacy]]
path = "examples/probe.rs"
allows = ["clippy::disallowed_macros"]
packet = "effect_site_inventory.mechanism (2): and any other module the PR5 review enumerates"
legacy_effect = """
LEGACY-EFFECT: an example binary whose entire output is `println!`. Outside \
`src/**`, which is why the scan's domain is widened to `examples/**`; \
`cargo clippy --all-targets` compiles it and an ungoverned example is a hole in \
the same wall."""
shrinks_when = "never, on current reading: an example that printed nothing would demonstrate nothing."
